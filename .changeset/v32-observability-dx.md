---
"awesome-coolify-mcp": minor
---

v3.2 Observability & DX: `system.version` capability flags, `deployment.logs`, `application.logs` follow mode with bounded polling, composite `diagnose.logs` triage, incident prompt DX, dual-icon MCP branding, and docs/coverage parity updates.
