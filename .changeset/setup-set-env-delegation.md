---
"awesome-coolify-mcp": patch
---

Phase 23.1: `setup.wire(set_env:true)` delegates to `application envs:sync` with XOR env input, abort-on-conflict semantics, and `deploy_and_watch` application guard.
