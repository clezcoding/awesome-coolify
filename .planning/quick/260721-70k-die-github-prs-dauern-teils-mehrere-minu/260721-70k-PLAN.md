# Quick Task 260721-70k: CI/CD Optimierung + Kodiak Autodeploy

**Goal:** PR CI schneller + Kodiak automerge aktivieren.

## Findings

- CI ~3m47s; Bottleneck: MegaLinter Docker pull (~183s von 227s)
- Lint/Test/Publint selbst ~20s — korrekt eingerichtet (npm cache, concurrency cancel)
- Kodiak App installiert (kodiakhq check auf PR #32), skippt ohne `automerge` label

## Tasks

### Task 1: CI parallelisieren + MegaLinter optimieren

**Files:** `.github/workflows/ci.yml`, `scripts/setup-branch-protection.sh`

**Action:**
- Job `Lint, Test & Build`: checkout, npm ci, lint, test, publint (~20s)
- Job `MegaLinter` parallel: javascript flavor, fetch-depth 0, VALIDATE_ALL_CODEBASE nur auf push
- Branch protection: beide Checks required

**Verify:** `actionlint` / YAML valid; job names match protection contexts

**Done:** Zwei parallele Jobs, kleineres Docker image, PR-only diff lint

### Task 2: Kodiak autodeploy aktivieren

**Files:** `.kodiak.toml` (verify), `dev-docs/github-setup-overview.md`, `scripts/verify-github-setup.sh`, PR #32 label

**Action:**
- PR #32 mit `automerge` labeln
- verify-github-setup: Kodiak-Check via gh statt nur Warnung
- Docs: Kodiak status ✅

**Verify:** `./scripts/setup-kodiak.sh` exit 0; PR #32 hat automerge label

**Done:** Kodiak merged PRs wenn CI grün
