---
phase: quick-260725-fx9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/codeql.yml
  - .github/workflows/dependency-review.yml
  - scripts/gsd-ship-post.sh
  - scripts/gsd-pr-labels.sh
  - .cursor/rules/gsd-ship-labels.mdc
  - CONTRIBUTING.md
autonomous: true
requirements: [QUICK-260725-fx9]
user_setup: []

must_haves:
  truths:
    - CodeQL and Dependency Review skip docs/planning-only PRs via path filters; they are not required checks
    - Required CI jobs Lint, Test & Build and MegaLinter still run on every PR (no workflow-level paths skip)
    - Phase ship via gsd-ship-post does not create a changeset unless --with-changeset is passed
    - Ship mode applies automerge and clears needs-changeset so Kodiak can merge phase PRs without a changeset
    - CONTRIBUTING documents milestone-close release: create changeset then merge Version Packages; release.yml OIDC unchanged
  artifacts:
    - .github/workflows/codeql.yml
    - .github/workflows/dependency-review.yml
    - scripts/gsd-ship-post.sh
    - scripts/gsd-pr-labels.sh
    - .cursor/rules/gsd-ship-labels.mdc
    - CONTRIBUTING.md
  key_links:
    - codeql.yml paths (+ concurrency) gate non-required analysis without touching ci.yml required job names
    - gsd-ship-post --with-changeset gates gsd-ensure-changeset.sh; default path goes straight to gsd-pr-labels --mode ship
    - gsd-pr-labels ship mode clears needs-changeset (Kodiak blocking_labels) and sets automerge
    - CONTRIBUTING + gsd-ship-labels.mdc describe accumulate-at-milestone release path
---

<objective>
Audit-driven CI speedups for non-required workflows, plus milestone-only npm: phase `/gsd-ship` stops auto-creating Changesets; npm publish stays on intentional milestone release via Changesets + Version Packages + existing `release.yml`.

Purpose: Cut wasted minutes on docs/planning PRs and stop per-phase npm churn while keeping Kodiak automerge + required checks healthy.
Output: Path-filtered CodeQL/Dependency Review; opt-in `--with-changeset` ship post; ship labels that automerge without changeset; documented milestone release steps.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260725-fx9-audit-and-optimize-github-actions-ci-cd-/260725-fx9-CONTEXT.md
@.planning/quick/260725-fx9-audit-and-optimize-github-actions-ci-cd-/260725-fx9-RESEARCH.md
@.github/workflows/ci.yml
@.github/workflows/codeql.yml
@.github/workflows/dependency-review.yml
@.github/workflows/release.yml
@scripts/gsd-ship-post.sh
@scripts/gsd-ensure-changeset.sh
@scripts/gsd-pr-labels.sh
@.cursor/rules/gsd-ship-labels.mdc
@.kodiak.toml
@CONTRIBUTING.md
@scripts/setup-branch-protection.sh
</context>

## User story

**As a** maintainer shipping GSD phases, **I want** fast PR checks and npm publish only at milestone close, **so that** phase merges are not blocked by Version Packages churn and docs-only PRs avoid CodeQL/dep-review cost.

<tasks>

<task type="tracer">
  <name>Task 1: Path-filter CodeQL + Dependency Review (required CI untouched)</name>
  <files>.github/workflows/codeql.yml, .github/workflows/dependency-review.yml</files>
  <action>
Implement the safe CI speed slice per CONTEXT (D-01 path policy: never path-skip required workflows) and RESEARCH recommendation.

1. `.github/workflows/ci.yml` — leave triggers, job names (`Lint, Test & Build`, `MegaLinter`), concurrency, and pnpm cache unchanged. Required checks must keep reporting on every PR (matches `scripts/setup-branch-protection.sh` / `verify-github-setup.sh`).

2. `.github/workflows/codeql.yml` (non-required, D-02):
   - Add `concurrency:` group `codeql-${{ github.workflow }}-${{ github.ref }}` with `cancel-in-progress: true`.
   - On `push` and `pull_request` only, add path filters so docs/planning-only changes do not run analysis. Include at least: `src/**`, `package.json`, `pnpm-lock.yaml`, `.github/workflows/codeql.yml`, `.github/codeql/**`. Keep the weekly `schedule` cron without path filters so scheduled scans still cover the repo.
   - Do not mark CodeQL as a required check.

3. `.github/workflows/dependency-review.yml` (non-required, D-02):
   - Add `paths` limited to dependency manifests that make the action useful: `package.json`, `pnpm-lock.yaml` (and workflow self-path if desired). Docs/planning-only PRs must not trigger this workflow.
   - Leave permissions and `actions/dependency-review-action@v4` as-is.

4. Lightweight bots (`semantic-pull-request.yml`, `pr-labels.yml`) stay every-PR — do not path-filter them (D-03). Keep existing MegaLinter PR diff mode and CI concurrency (D-04) — no new heavy tooling.

Avoid workflow-level `paths` on `ci.yml` that would leave required checks stuck pending.
  </action>
  <verify>
    <automated>command -v actionlint >/dev/null && actionlint .github/workflows/codeql.yml .github/workflows/dependency-review.yml .github/workflows/ci.yml || npx --yes actionlint@latest .github/workflows/codeql.yml .github/workflows/dependency-review.yml .github/workflows/ci.yml; grep -q 'MegaLinter' scripts/setup-branch-protection.sh; grep -q 'Lint, Test' scripts/setup-branch-protection.sh; ! grep -q '^[[:space:]]*paths:' .github/workflows/ci.yml; grep -q 'cancel-in-progress' .github/workflows/codeql.yml; grep -q 'paths:' .github/workflows/codeql.yml; grep -q 'paths:' .github/workflows/dependency-review.yml</automated>
  </verify>
  <done>
    CodeQL has concurrency + path-gated push/PR triggers; Dependency Review path-gated to lockfile/package changes; ci.yml still has no paths key and keeps both required job names; actionlint exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Milestone-only changeset — ship-post opt-in + automerge without needs-changeset</name>
  <files>scripts/gsd-ship-post.sh, scripts/gsd-pr-labels.sh, .cursor/rules/gsd-ship-labels.mdc</files>
  <action>
Wire milestone-only npm root-cause fix per CONTEXT (D-05, D-06). Leave `release.yml` / OIDC publish mechanics completely unchanged (D-07).

1. `scripts/gsd-ship-post.sh`:
   - Add `--with-changeset` flag (default off). Update header Usage/help to list `[--with-changeset]` and `[--bump patch|minor|major]`.
   - Default path: do not invoke `gsd-ensure-changeset.sh`; print a one-line skip notice (e.g. `gsd-ship-post: changeset skipped (pass --with-changeset to create)`), then run `gsd-pr-labels.sh --mode ship`.
   - When `--with-changeset` is set: keep current ensure → commit → push behavior (fail-closed before labels if ensure fails).
   - Update the top comment that currently implies every ship creates a changeset.

2. `scripts/gsd-pr-labels.sh` ship/ready mode (D-06):
   - Automerge must succeed for phase ships that have no changeset. In `ship` and `ready` modes: do not withhold automerge when release-relevant files lack a changeset; always set automerge unless `--no-automerge`.
   - In `ship` and `ready` modes: clear `needs-changeset` if present (it remains in `.kodiak.toml` `blocking_labels` — leaving it on would stall Kodiak forever after default ship skips changesets). Prefer: skip adding `needs-changeset` in ship/ready; still remove it when present.
   - Keep `ci` mode behavior that can still surface `needs-changeset` on open/sync for visibility; ship-post is the path that must strip it for phase merges.
   - Update the comment above the automerge block that currently says automerge is never set when a changeset is still required.

3. `.cursor/rules/gsd-ship-labels.mdc`:
   - Rewrite the bullet list so default `./scripts/gsd-ship-post.sh <pr-number>` applies labels + automerge and does not create a changeset.
   - Document `./scripts/gsd-ship-post.sh <pr-number> --with-changeset` for hotfix / out-of-band releases.
   - Keep the warning about never pushing `[ci skip]` / `[skip ci]` on PR tips (required checks pending).

Do not edit `scripts/gsd-ensure-changeset.sh` logic beyond callers — the script remains the creator when opt-in is used. Do not edit `.kodiak.toml` blocking list (needs-changeset stays a useful blocker for intentional release PRs that still need a fragment).
  </action>
  <verify>
    <automated>bash -n scripts/gsd-ship-post.sh && bash -n scripts/gsd-pr-labels.sh && grep -q -- '--with-changeset' scripts/gsd-ship-post.sh && grep -q 'changeset skipped' scripts/gsd-ship-post.sh && ./scripts/gsd-ship-post.sh --help | grep -q -- '--with-changeset' && ! grep -n 'NEVER when a changeset is still' scripts/gsd-pr-labels.sh</automated>
  </verify>
  <done>
    Default gsd-ship-post skips ensure-changeset; --with-changeset restores create+commit+push; ship/ready always opt in automerge (unless --no-automerge) and clear needs-changeset; gsd-ship-labels.mdc matches; bash -n clean; release.yml untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document accumulate-at-milestone npm release path</name>
  <files>CONTRIBUTING.md</files>
  <action>
Document the milestone release path per CONTEXT (D-08) and Claude's Discretion (accumulate-at-milestone, not long-lived Version Packages PRs).

In `CONTRIBUTING.md`:
1. Update the Pull Requests bullet that currently tells every release-relevant PR to run `npx changeset` — clarify that routine GSD phase ships do not add Changesets by default; npm publish is milestone-scoped.
2. Rewrite the `gsd-ship-post` numbered list (around the Labels section) to match Task 2: default = labels + automerge only; `--with-changeset` for hotfixes; ship clears `needs-changeset`.
3. Add a short **Milestone npm release** subsection (after Auto-merge or Labels) with the accumulate-at-milestone steps:
   - During the milestone: merge phase PRs without Changesets (no Version Packages churn).
   - At milestone close: on a release PR (or dedicated chore branch), create fragment(s) via `npx changeset` or `./scripts/gsd-ship-post.sh <pr-number> --with-changeset` / `scripts/gsd-ensure-changeset.sh`.
   - Merge that PR to `main` → Changesets action opens **Version Packages**.
   - Merge Version Packages → existing `.github/workflows/release.yml` publishes to npm via OIDC (no workflow edits in this task).
   - Prefer one accumulated release at close over leaving Version Packages open for weeks.

Keep semantic PR / pr-labels description accurate (`needs-changeset` may still appear from `--mode ci` until ship clears it).
  </action>
  <verify>
    <automated>grep -q 'Milestone npm release' CONTRIBUTING.md && grep -q -- '--with-changeset' CONTRIBUTING.md && grep -q 'Version Packages' CONTRIBUTING.md && grep -q 'release.yml' CONTRIBUTING.md</automated>
  </verify>
  <done>
    CONTRIBUTING documents default no-changeset phase ship, --with-changeset escape hatch, and milestone-close → Version Packages → release.yml publish path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → repo checkout | Workflow YAML controls what runs on untrusted PR heads |
| Ship scripts → gh API | Local maintainer scripts mutate PR labels / push commits with caller credentials |
| release.yml → npm (OIDC) | Out of scope for edits; publish still gated by Changesets Version Packages merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-fx9-01 | Tampering | codeql.yml / dependency-review.yml path filters | medium | mitigate | Path-filter only non-required workflows; keep required Lint/Test/Build + MegaLinter always reporting so Kodiak cannot merge on skipped required checks |
| T-fx9-02 | Elevation of Privilege | gsd-ship-post skipping changesets | medium | mitigate | Default skip is intentional; publish still requires an explicit changeset + Version Packages merge + release.yml OIDC; document --with-changeset for hotfixes |
| T-fx9-03 | Tampering | gsd-pr-labels clearing needs-changeset | low | accept | Label clear is ship/ready only; Kodiak still requires green required checks + automerge label; release publish path unchanged |
| T-fx9-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this plan |
</threat_model>

## Source audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Audit/optimize GHA; path-filter PR runs; npm only on milestone | 01 | COVERED | Tasks 1–3 |
| REQ | QUICK-260725-fx9 | Same as goal | 01 | COVERED | Quick task id |
| RESEARCH | — | Path-filter non-required only; avoid stuck required checks | 01 T1 | COVERED | |
| RESEARCH | — | CodeQL concurrency + dep-review paths | 01 T1 | COVERED | |
| RESEARCH | — | Default ship no changeset; --with-changeset opt-in | 01 T2 | COVERED | |
| RESEARCH | — | Ship automerge without needs-changeset blocker | 01 T2 | COVERED | |
| RESEARCH | — | Document milestone release; leave release.yml | 01 T3 | COVERED | |
| CONTEXT | D-01 | Required checks stay; no path-skip of required CI | 01 T1 | COVERED | |
| CONTEXT | D-02 | CodeQL + Dependency Review path-filterable | 01 T1 | COVERED | |
| CONTEXT | D-03 | Lightweight bots every PR | 01 T1 | COVERED | |
| CONTEXT | D-04 | Keep concurrency/cache/MegaLinter PR diff | 01 T1 | COVERED | |
| CONTEXT | D-05 | Phase ship no changeset; --with-changeset | 01 T2 | COVERED | |
| CONTEXT | D-06 | Automerge without needs-changeset | 01 T2 | COVERED | |
| CONTEXT | D-07 | release.yml OIDC untouched | 01 T2/T3 | COVERED | |
| CONTEXT | D-08 | Document milestone release | 01 T3 | COVERED | |

Excluded: Deferred Ideas (none); MegaLinter Docker pull re-solve (CONTEXT specifics — prior 260721-70k).

<verification>
- actionlint clean on touched workflows; ci.yml has no `paths:` key
- `bash -n` on ship scripts; `--help` shows `--with-changeset`
- Default dry mental path: ship-post → skip ensure → labels ship → automerge without needs-changeset
- CONTRIBUTING contains Milestone npm release + Version Packages + release.yml
</verification>

<success_criteria>
1. Docs/planning-only PRs do not run CodeQL or Dependency Review; required CI still always runs.
2. Default `gsd-ship-post.sh` does not create `.changeset/*` unless `--with-changeset`.
3. Ship mode sets `automerge` and removes `needs-changeset` so Kodiak is not blocked by missing fragments.
4. Maintainer docs describe accumulate-at-milestone npm publish; `release.yml` binary-unchanged.
</success_criteria>

<output>
Create `.planning/quick/260725-fx9-audit-and-optimize-github-actions-ci-cd-/260725-fx9-SUMMARY.md` when done
</output>
