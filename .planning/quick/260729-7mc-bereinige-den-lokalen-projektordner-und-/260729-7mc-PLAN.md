---
phase: quick-260729-7mc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: false
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
user_setup:
  - service: github
    why: "Remote branch deletion and Dependabot manual recheck require gh auth with repo admin"
must_haves:
  truths:
    - Git index contains zero ephemeral GSD, dev-only, build, coverage, or local-artifact paths; prior 260727-4hd rules remain intact
    - Local gitignored artifacts (tgz, dist, coverage, graphify-out, .DS_Store, mcp-spy.log) are removed from disk; rebuild outputs regenerate via normal dev commands
    - Stale merged branches are deleted locally and on origin; protected branches (main, current working branch, changeset-release/main) remain
    - Dependabot config is validated, a dependency drift baseline is recorded, and a manual update check is triggered with observable outcome
  artifacts:
    - .gitignore
  key_links:
    - .gitignore rules from 260727-4hd must not be weakened; only additive hardening for newly found local-only paths
    - Remote branch deletion uses merged-PR headRefName from gh, never heuristic delete of open or unmerged work
    - Dependabot trigger falls back to GitHub UI only when REST/GraphQL APIs return 404
---

<objective>
Deep-clean local workspace and GitHub remote after prior 260727-4hd index hygiene: purge ignored local artifacts, harden `.gitignore`, delete stale merged branches, and run a Dependabot update check.

Purpose: Reduce local disk noise (~53 stale local branches, build/coverage/graphify artifacts) and remote branch clutter without re-breaking GSD tracked planning artifacts or deleting active work.

Output: Updated `.gitignore` (if gaps found), clean working tree, pruned branches, Dependabot recheck triggered, SUMMARY with deletion manifest.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.gitignore
@.github/dependabot.yml
@.planning/quick/260727-4hd-r-ume-den-projektordner-auf-und-sortiere/260727-4hd-SUMMARY.md

Known baseline (pre-execution):
- Index already clean: 0 tracked ephemeral paths; `dev-docs/` and `.planning/mcp_features.md` gitignored per 260727-4hd
- Local artifacts present: `awesome-coolify-mcp-1.0.0.tgz`, `coverage/`, `dist/`, `graphify-out/`, multiple `.DS_Store`, `.cursor/mcp-spy.log`
- ~53 local branches; 1 merged into `origin/main` locally; 14 remote branches on `clezcoding/awesome-coolify`
- Remote merged PR heads still present include `gsd/quick-260729-6pb-*`, `chore/v3.2-milestone-closeout`, etc.
- No open Dependabot PRs; last merged Dependabot PRs #81–#82 on 2026-07-25
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Audit index + purge local ignored artifacts</name>
  <files>.gitignore</files>
  <precondition>`git status` is clean or only contains expected quick-task branch work; halt if unexpected staged production edits exist.</precondition>
  <action>
Per CLEAN-01 and 260727-4hd continuity: confirm zero tracked paths under `.planning/forensics`, `.planning/spikes`, `.planning/sketches`, `.planning/notes`, `.planning/todos`, `.planning/debug`, `.planning/research/.cache`, `dev-docs/`, and root `mcp_features.md`. If any appear in `git ls-files`, run `git rm -r --cached` for those paths only (keep local copies).

Harden `.gitignore` additively only where gaps exist — do not remove or relax 260727-4hd rules. Candidate additions if not already covered: explicit `.cursor/mcp-spy.log` under the Logs section (documents MCP debug intent beyond generic `*.log`), and `awesome-coolify-mcp-*.tgz` under npm artifacts (narrows `*.tgz` to project pack output). Skip additions already satisfied by existing patterns.

Delete local gitignored artifacts only (never delete tracked source): `awesome-coolify-mcp-*.tgz`, `coverage/`, `dist/`, `graphify-out/`, `.cursor/mcp-spy.log`, and all `.DS_Store` files outside `node_modules/`. Use `find` with `-path './node_modules' -prune` guard. Do not delete `.env`, `.coolify-mcp/`, `dev-docs/`, or any tracked `.planning/` core files.

Record counts before/after in commit body or SUMMARY (files removed, bytes reclaimed estimate via `du`).
  </action>
  <verify>
    <automated>test "$(git ls-files | rg -c '\.planning/(forensics|spikes|sketches|notes|todos|debug)|\.planning/research/\.cache|^dev-docs/|^mcp_features\.md$' || true)" -eq 0 && test ! -e awesome-coolify-mcp-1.0.0.tgz && test ! -d coverage && test ! -d dist && test ! -d graphify-out && test ! -f .cursor/mcp-spy.log && git check-ignore -q .cursor/mcp-spy.log awesome-coolify-mcp-9.9.9.tgz</automated>
  </verify>
  <done>Index still ephemeral-free; known local build/coverage/graphify/tgz/log/DS_Store artifacts gone; `.gitignore` updated only if additive gaps found; `git check-ignore` passes for new patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Prune stale merged branches (local + origin)</name>
  <reversibility rating="costly">Remote branch deletion needs reflog to recover; only merged PR heads are in scope.</reversibility>
  <action>
Per CLEAN-02: run `git fetch --prune origin` first.

Protected — never delete: `main`, `origin/main`, current branch (`gsd/quick-260729-7mc-*`), `changeset-release/main`.

Local cleanup:
1. Delete branches merged into `origin/main`: `git branch --merged origin/main` minus protected names → `git branch -d`.
2. Delete local branches whose upstream is gone after prune: `git branch -vv | rg ': gone\]'` → `git branch -D` only when branch is not current.

Remote cleanup (merged PR heads only):
1. Build candidate list: `gh pr list --state merged --limit 200 --json headRefName -q '.[].headRefName' | sort -u`.
2. Subtract protected branch names.
3. For each candidate, confirm branch exists on origin (`gh api repos/clezcoding/awesome-coolify/branches/<name>`) then `git push origin --delete <name>`. Skip on 404 or if an open PR still references the branch.
4. Do not delete remote branches with open PRs or without a merged PR record.

Log every deleted branch name (local vs remote) in SUMMARY. If a candidate fails deletion, note reason and continue.
  </action>
  <verify>
    <automated>test "$(git branch --merged origin/main | rg -vc '^\*|main$')" -eq 0 && REMOTE_COUNT=$(gh api repos/clezcoding/awesome-coolify/branches --paginate -q 'length') && test "$REMOTE_COUNT" -lt 14</automated>
  </verify>
  <done>Zero local branches merged into origin/main remain; remote branch count reduced; protected branches untouched; deletion manifest recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Dependabot baseline, trigger, and fallback</name>
  <files>.github/dependabot.yml</files>
  <action>
Per CLEAN-03: validate `.github/dependabot.yml` parses and still defines npm `/` weekly + github-actions weekly with ignore rules for typescript 6.x/7.x and `@types/node` >=25.

Run drift baseline (capture for SUMMARY): `pnpm outdated --format list` and `pnpm audit --audit-level moderate` (record counts only; do not fix).

Attempt automated Dependabot trigger; stop at first non-404 success:
1. `gh api -X POST repos/clezcoding/awesome-coolify/dependency-graph/dependabot-updates`
2. If 404, try any current GraphQL dependency-update mutation on GitHub API

If all API attempts return 404 (expected): halt and ask the user to open https://github.com/clezcoding/awesome-coolify/network/updates, click **Check for updates**, then reply **approved**. Do not mark task done until user confirms or API succeeded.

After trigger (API or user UI), wait up to 2 minutes and poll `gh pr list --author app/dependabot --state open --limit 5`. Record whether new PRs appeared; if none, note weekly schedule in dependabot.yml and attach `pnpm outdated` baseline to SUMMARY.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('.github/dependabot.yml','utf8');if(!y.includes('package-ecosystem'))process.exit(1)" && pnpm outdated --format list >/dev/null 2>&1; echo dependabot-baseline-ok</automated>
  </verify>
  <done>dependabot.yml valid; outdated/audit baseline captured; update check triggered via API or confirmed UI click; outcome documented in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local disk → git index | Accidental untrack of production or GSD core artifacts |
| executor → GitHub API | Remote branch deletion is irreversible |
| Dependabot UI → origin | Third-party dependency PRs enter repo supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7mc-01 | Tampering | `git rm --cached` scope | high | mitigate | Only paths listed in Task 1 audit; never `git rm` tracked `src/`, `tests/`, core `.planning/` |
| T-7mc-02 | Denial of Service | remote branch delete | medium | mitigate | Merged-PR-only policy; protected branch denylist; skip on open PR |
| T-7mc-03 | Elevation | Dependabot PRs | medium | accept | Existing CI + Kodiak review gates on all dependency PRs |
| T-7mc-SC | Tampering | branch delete API | high | mitigate | `reversibility: one-way` on Task 2; manifest logging before delete |
</threat_model>

<verification>
- `git status` clean after `.gitignore` commit (if any)
- `npm test` or `pnpm test` passes after cleanup (dist rebuilt if needed: `pnpm build`)
- Remote branch count reduced; `main` and `changeset-release/main` present
- SUMMARY lists deleted branches, removed local paths, dependabot trigger outcome, `pnpm outdated` snapshot
</verification>

<success_criteria>
- Local workspace free of known ignored artifacts; index ephemeral count 0
- Stale merged branches pruned locally and on origin without touching protected branches
- Dependabot config validated; manual update check completed; drift baseline recorded
- No regression to 260727-4hd tracked/ignored `.planning/` contract
</success_criteria>

<output>
Create `.planning/quick/260729-7mc-bereinige-den-lokalen-projektordner-und-/260729-7mc-SUMMARY.md` when done
</output>

<!-- gsd-source-audit
GOAL: Bereinige lokalen Projektordner und GitHub Repo auf alte nicht benötigte Dateien/Ordner; update .gitignore; delete stale branches; run Dependabot
REQ: CLEAN-01 local+index cleanup | CLEAN-02 branch prune | CLEAN-03 dependabot run
RESEARCH: n/a (quick, no research) — baseline from orchestrator context_gathered
CONTEXT: extends 260727-4hd; do not weaken prior gitignore; merged-PR-only branch delete
COVERAGE: all items → Plan 01 Tasks 1–3
-->
