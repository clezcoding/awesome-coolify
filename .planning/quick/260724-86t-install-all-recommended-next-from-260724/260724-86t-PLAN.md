---
phase: quick-260724-86t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/CODEOWNERS
  - .github/workflows/codeql.yml
  - .github/workflows/dependency-review.yml
  - .github/workflows/stale.yml
  - .github/workflows/semantic-pull-request.yml
  - dev-docs/github-setup-overview.md
autonomous: true
requirements: [QUICK-260724-86t]
user_setup: []

must_haves:
  truths:
    - CODEOWNERS-Datei existiert und weist @clezcoding als Owner für alle Pfade aus
    - CodeQL-Workflow läuft bei push auf main und bei PRs (javascript-typescript) ohne bestehende CI-Checks zu brechen
    - Dependency Review Action läuft bei pull_request und blockiert vulnerable Dep-Änderungen
    - actions/stale schließt inaktive Issues/PRs nach 60 Tagen Stale + 14 Tagen Close
    - amannn/action-semantic-pull-request prüft PR-Titel auf Conventional-Commit-Format bei pull_request (nicht pull_request_target)
    - dev-docs/github-setup-overview.md dokumentiert alle fünf neuen Bausteine und listet Branch-Protection-Follow-up
  artifacts:
    - .github/CODEOWNERS
    - .github/workflows/codeql.yml
    - .github/workflows/dependency-review.yml
    - .github/workflows/stale.yml
    - .github/workflows/semantic-pull-request.yml
    - dev-docs/github-setup-overview.md
  key_links:
    - github/codeql-action@v3 init+analyze für javascript-typescript
    - actions/dependency-review-action@v4 auf pull_request
    - actions/stale@v9 schedule cron + days-before-stale:60 / days-before-close:14
    - amannn/action-semantic-pull-request@v5 auf pull_request types opened/edited/reopened/synchronize
---

<objective>
Installiere die fünf "Recommended next"-Bausteine aus SOURCE-RECOMMENDATIONS.md (CODEOWNERS, CodeQL, Dependency Review Action, actions/stale, amannn/action-semantic-pull-request) und aktualisiere dev-docs/github-setup-overview.md. Nur kostenlose GitHub-native Tools für das öffentliche Repo clezcoding/awesome-coolify. Bestehende Required Checks (Lint, Test & Build + MegaLinter) dürfen nicht gebrochen werden.

Purpose: Schließt die "Should-have"-Lücken aus der GitHub-Audit-Empfehlung (2026-07-24) ohne Risiko für bestehende CI/Branch-Protection.
Output: CODEOWNERS-Datei, vier neue Workflow-Dateien, aktualisierte Setup-Übersicht.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260724-86t-install-all-recommended-next-from-260724/SOURCE-RECOMMENDATIONS.md
@dev-docs/github-setup-overview.md
@.github/workflows/ci.yml
@.github/workflows/pr-labels.yml
@.github/dependabot.yml
@.kodiak.toml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: CODEOWNERS + vier Workflows anlegen</name>
  <files>.github/CODEOWNERS, .github/workflows/codeql.yml, .github/workflows/dependency-review.yml, .github/workflows/stale.yml, .github/workflows/semantic-pull-request.yml</files>
  <action>
Repo-Owner verifiziert via `gh api repos/clezcoding/awesome-coolify --jq .owner.login` → `clezcoding` (per D-01 / SOURCE-RECOMMENDATIONS.md). Kein @main-Pinning — nur stabile Major-Tags.

1. `.github/CODEOWNERS` anlegen mit `* @clezcoding` als Default-Owner. Zusätzlich explizite Sektionen für sensible Pfade: `/.github/ @clezcoding`, `/src/ @clezcoding`, `/scripts/ @clezcoding`, `/docs/ @clezcoding`, `/dev-docs/ @clezcoding`. Datei mit abschließendem Leerzeichen pro Zeile vermeiden (CODEOWNERS-Parser strikt).

2. `.github/workflows/codeql.yml` — Name `CodeQL`, on push branches [main] und pull_request branches [main], plus schedule `cron: '17 3 * * 2'` (wöchentlich Di 03:17 UTC). permissions contents: read, security-events: write. Jobs: `analyze` runs-on ubuntu-latest, steps: checkout@v7, `github/codeql-action/init@v3` mit `languages: javascript-typescript`, `github/codeql-action/analyze@v3`. Kein upload-artifacts nötig. Kein `mode: tracing` (Default ist ausreichend für js-ts). Nicht als required check deklarieren — Branch-Protection-Update bleibt Task 2-Follow-up nach erstem grünen Run.

3. `.github/workflows/dependency-review.yml` — Name `Dependency Review`, on pull_request branches [main]. permissions contents: read, pull-requests: write. Single job `dependency-review` runs-on ubuntu-latest, steps: checkout@v7, `actions/dependency-review-action@v4` (keine externen Kommentare, default config blockt vulnerable Änderungen auf PRs). Kein `fail-on-severity`-Override nötig — defaults sind für public repos geeignet.

4. `.github/workflows/stale.yml` — Name `Stale`, on schedule `cron: '17 3 * * *'` (täglich 03:17 UTC), plus `workflow_dispatch` für manuelle Trigger. permissions issues: write, pull-requests: write. Job `stale` runs-on ubuntu-latest, steps: `actions/stale@v9` mit `days-before-stale: 60`, `days-before-close: 14`, `stale-issue-label: 'stale'`, `stale-pr-label: 'stale'`, `exempt-issue-labels: 'pinned,security,help wanted'`, `exempt-pr-labels: 'pinned,security,help wanted'`, `operations-per-run: 30`, `remove-stale-when-updated: true`, `days-before-issue-stale: 60`, `days-before-pr-stale: 60`. Keine `any-of-labels`-Logik — bestehende `automerge`-Labels nicht tangieren.

5. `.github/workflows/semantic-pull-request.yml` — Name `Semantic Pull Request`, on pull_request types [opened, edited, reopened, synchronize]. permissions pull-requests: read. Job `validate` runs-on ubuntu-latest, steps: `amannn/action-semantic-pull-request@v5` mit `types: 'feat,fix,docs,chore,refactor,test,style,perf,build,ci,revert'`, `requireScope: false`, `subjectCase: 'lower-case'`. Bewusst `pull_request` statt `pull_request_target` (per Constraint — vermeidet Token-Leakage-Pitfall bei Fork-PRs; Action benötigt keine Secrets). Bei Fork-PRs läuft die Action im read-only-Kontext des PR-Head, was für Titel-Validierung ausreicht.

Versions-Pinning: actions/checkout@v7 (matcht ci.yml), alle anderen wie oben. Keine `@main` oder `@master`-Refs.
  </action>
  <verify>
    <automated>ls -1 .github/CODEOWNERS .github/workflows/codeql.yml .github/workflows/dependency-review.yml .github/workflows/stale.yml .github/workflows/semantic-pull-request.yml && npx --yes actionlint@latest .github/workflows/codeql.yml .github/workflows/dependency-review.yml .github/workflows/stale.yml .github/workflows/semantic-pull-request.yml</automated>
  </verify>
  <done>
    Alle fünf Dateien existieren; actionlint exit 0; keine @main-Pins; CODEOWNERS referenziert @clezcoding; CodeQL auf javascript-typescript; stale days-before-stale:60/days-before-close:14; semantic-pr nutzt pull_request (nicht pull_request_target).
  </done>
</task>

<task type="auto">
  <name>Task 2: Setup-Übersicht aktualisieren + Branch-Protection-Follow-up dokumentieren</name>
  <files>dev-docs/github-setup-overview.md</files>
  <action>
`dev-docs/github-setup-overview.md` aktualisieren:

1. Datum "Stand" in Zeile 3 auf `2026-07-24` setzen.

2. Neue Sektion `## Security & Hygiene (2026-07-24)` nach `## Dependency-Pflege` einfügen mit Bullet-Liste der fünf neuen Bausteine:
   - `.github/CODEOWNERS` — auto Review-Requests für `@clezcoding` auf sensible Pfade
   - `.github/workflows/codeql.yml` — CodeQL SAST (javascript-typescript), wöchentlicher Scan + PR/Push-Trigger, github/codeql-action@v3
   - `.github/workflows/dependency-review.yml` — Dependency Review Action auf PRs, blockt vulnerable Dep-Änderungen
   - `.github/workflows/stale.yml` — actions/stale@v9, 60/14-Tage-Cycle, exempt Labels `pinned,security,help wanted`
   - `.github/workflows/semantic-pull-request.yml` — amannn/action-semantic-pull-request@v5, erzwingt Conventional-Commit-PR-Titel (ergänzt lokales commitlint)

3. Audit-Tabelle `### Audit-Status (2026-07-19)` überschreiben zu `### Audit-Status (2026-07-24)` und neue Zeilen anhängen:
   - `CODEOWNERS` | ✅ grün | `@clezcoding` für `*`, `.github/`, `src/`, `scripts/`, `docs/`, `dev-docs/`
   - `CodeQL (SAST)` | ✅ aktiv | js-ts, wöchentlich + PR/Push, NICHT required (Follow-up nach erstem grünen Run)
   - `Dependency Review` | ✅ aktiv | PR-Trigger, blockt vulnerable Deps
   - `actions/stale` | ✅ aktiv | 60/14, exempt pinned/security/help wanted
   - `Semantic PR Title` | ✅ aktiv | pull_request (nicht pull_request_target), lower-case subject

4. Unter `### Manuelle Follow-ups` neuen Punkt 4 anfügen: **Branch Protection ggf. erweitern** — nach erstem grünen CodeQL-Run auf main optional `./scripts/setup-branch-protection.sh` erneut ausführen oder manuell `CodeQL` als required check hinzufügen. Vorher nicht required setzen (Constraint: kein Auto-Require bis erster grüner Run). Dependency Review und Semantic PR sind PR-Only-Checks und können optional als required gesetzt werden, wenn erste Runs grün waren.

5. Im Absatz `## Repo-Schutz` kein Eingriff (Branch-Protection-Script bleibt unangetastet — nur Doku-Follow-up).

Keine Links auf .planning/, keine Speicherung von Secrets, keine Änderung an .gitignore.
  </action>
  <verify>
    <automated>grep -c 'CODEOWNERS\|CodeQL\|Dependency Review\|actions/stale\|Semantic PR' dev-docs/github-setup-overview.md | grep -v '^0$' && grep -c '2026-07-24' dev-docs/github-setup-overview.md | grep -v '^0$'</automated>
  </verify>
  <done>
    Setup-Übersicht hat Datum 2026-07-24, neue Security & Hygiene-Sektion, erweiterte Audit-Tabelle mit fünf neuen Zeilen, Follow-up-Punkt 4 für Branch-Protection-Update dokumentiert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fork-PR → Workflow | Untrusted PR-Head wird in Workflow ausgeführt |
| PR-Titel → Action | User-kontrollierter String wird geparst |
| Schedule → Stale-Action | GitHub-Cron triggert Mutation auf Issues/PRs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-86t-01 | Elevation of Privilege | semantic-pull-request.yml | high | mitigate | pull_request statt pull_request_target — keine Token-Exposition an Fork-PR-Code; permissions pull-requests: read only |
| T-quick-86t-02 | Tampering | CodeQL workflow runs on PR head | medium | accept | CodeQL init/analyze liest nur Quellcode; security-events:write ist Standard für CodeQL; keine Secrets im CodeQL-Job nötig |
| T-quick-86t-03 | Denial of Service | stale.yml schließt aktive Issues | medium | mitigate | exempt-issue-labels pinned/security/help wanted; operations-per-run:30; remove-stale-when-updated:true; 60+14 Tage Puffer |
| T-quick-86t-04 | Information Disclosure | dependency-review.yml | low | accept | Action nutzt nur GITHUB_TOKEN; keine Secrets im Step; public repo — keine privaten Dep-Registries |
| T-quick-86t-SC | Tampering | actions/stale@v9, github/codeql-action@v3, actions/dependency-review-action@v4, amannn/action-semantic-pull-request@v5 installs | high | mitigate | Alle Actions auf stabile Major-Tags gepinnt (kein @main); Dependabot github-actions-Ecosystem aktiv (dependabot.yml) liefert automatische Patch-Updates; [ASSUMED] — keine Package Legitimacy Audit-Tabelle in SOURCE-RECOMMENDATIONS.md, aber alle vier Actions sind offiziell gewartet (github.com/github/codeql-action, github.com/actions/stale, github.com/actions/dependency-review-action, github.com/amannn/action-semantic-pull-request) |
</threat_model>

<verification>
- `ls -1` aller fünf neuen Dateien zeigt keine 404
- `actionlint` über alle vier neuen Workflow-Dateien exit 0
- `gh workflow list` zeigt nach Push die neuen Workflows (CodeQL, Dependency Review, Stale, Semantic Pull Request)
- Bestehende `ci.yml` und `pr-labels.yml` unverändert — `git diff .github/workflows/ci.yml .github/workflows/pr-labels.yml` leer
- `dev-docs/github-setup-overview.md` enthält alle fünf Baustein-Namen + Datum 2026-07-24
</verification>

<success_criteria>
- Fünf neue Dateien existieren und sind syntaktisch valide (actionlint grün)
- Keine @main-Pins; alle Actions auf Major-Tags
- semantic-pull-request nutzt pull_request, NICHT pull_request_target
- Bestehende Required Checks (Lint, Test & Build + MegaLinter) nicht tangiert
- Branch-Protection nicht auto-erweitert — Follow-up in dev-docs dokumentiert
- Setup-Übersicht steht auf 2026-07-24, neue Audit-Zeilen vorhanden
- Commit-Subject lowercase (kein "GitHub" PascalCase) — commitlint-konform
</success_criteria>

<output>
Create `.planning/quick/260724-86t-install-all-recommended-next-from-260724/260724-86t-SUMMARY.md` when done
</output>
