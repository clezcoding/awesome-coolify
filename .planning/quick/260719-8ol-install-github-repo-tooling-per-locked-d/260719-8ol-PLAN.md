---
phase: quick-260719-8ol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .kodiak.toml
  - .megalinter.yml
  - .github/release-drafter.yml
  - .github/workflows/release-drafter.yml
  - .github/workflows/publish-comfy.yml
  - .github/workflows/publish-mcp.yml
  - .github/workflows/ci.yml
  - package.json
  - dev-docs/github-setup-overview.md
autonomous: true
requirements:
  - D-1-comfy-publish
  - D-2-mcp-publish
  - D-3-keep-oidc-publish
  - D-4-release-drafter
  - D-5-setup-node-pin
  - D-6-kodiak
  - D-7-publint
  - D-8-megalinter
  - D-9-docs-update
must_haves:
  truths:
    - Kodiak config exists and docs explain manual App install
    - All workflows use consistent setup-node@v7 + Node 24
    - Existing OIDC publish.yml is preserved unchanged
    - Release Drafter workflow runs alongside existing Changesets release.yml
    - Comfy publish-to-registry workflow exists with REGISTRY_ACCESS_TOKEN + pyproject.toml prerequisites documented
    - MCP publish workflow runs once on v* tags with OIDC, registry_type npm, identifier awesome-coolify-mcp
    - publint runs as npm script and as CI step
    - MegaLinter runs with narrow config (TS/JS/YAML/Markdown/Actions only)
    - dev-docs/github-setup-overview.md describes every new piece
  artifacts:
    - .kodiak.toml
    - .megalinter.yml
    - .github/release-drafter.yml
    - .github/workflows/release-drafter.yml
    - .github/workflows/publish-comfy.yml
    - .github/workflows/publish-mcp.yml
  key_links:
    - ci.yml -> publint + MegaLinter steps
    - package.json scripts.publint -> publint CLI
    - publish-mcp.yml trigger v* -> release.yml tag flow
---

<objective>
Install GitHub repo tooling per locked decisions in 260719-8ol-CONTEXT.md: Kodiak config, Release Drafter, Comfy publish-to-registry, MCP publish action, publint, narrow MegaLinter, setup-node consistency verification, and docs update. Keep existing OIDC publish.yml and Changesets release.yml untouched.

Purpose: Complete the GitHub Actions / repo tooling setup the user locked in discuss-phase, without re-litigating any decision.
Output: 3 new workflow files, 2 new repo config files, 1 modified workflow (ci.yml), 1 modified package.json, 1 updated docs file.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260719-8ol-install-github-repo-tooling-per-locked-d/260719-8ol-CONTEXT.md
@.github/workflows/ci.yml
@.github/workflows/publish.yml
@.github/workflows/release.yml
@.github/workflows/pages.yml
@.github/workflows/labels.yml
@package.json
@dev-docs/github-setup-overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo config files (Kodiak, MegaLinter, Release Drafter config)</name>
  <files>.kodiak.toml, .megalinter.yml, .github/release-drafter.yml</files>
  <action>
Create three static repo config files (per D-6, D-8, D-4):

1. `.kodiak.toml` — minimal Kodiak config for the clezcoding/awesome-coolify repo. Include:
   - `version = "1"`
   - `[merge]` block with `method = "squash"`, `automerge_label = "automerge"`, `require_automerge_label = true`
   - `[merge.message]` with `title = "pull_request_title"`, `body = "pull_request_body"`
   - `[update]` with `always = true` (keep PRs up to date with base)
   - `[approve]` with `auto_approve_usernames = []` (empty — no auto-approval)
   Kodiak is a GitHub App the user installs manually; this file only configures behavior once installed.

2. `.megalinter.yml` — NARROW flavor per D-8 (NOT full default). Use:
   - `APPLY_FIXES: none`
   - `ENABLE: typescript,javascript,yaml,markdown,actionlint`
   - `DISABLE_ERRORS: false`
   - `FAIL_IF_IF: error` (omit if unsupported)
   - `SHOW_ELAPSED_TIME: true`
   - `REPORT_STYLE: console`
   - `GITHUB_STATUS_FILTER: true`
   - Skip flavor (do not set `FLAVOR`) so default image is used; the ENABLE list narrows linters to TS/JS/YAML/Markdown/Actions only. Do NOT enable all linters.

3. `.github/release-drafter.yml` — Release Drafter config (per D-4). Use:
   - `name-template: "$RESOLVED_VERSION"`
   - `tag-template: "v$RESOLVED_VERSION"`
   - `change-template: "* $TITLE (#$NUMBER) @$AUTHOR"`
   - `change-title-escapes: "\\<\\>\\*"`
   - `template: |` with a header line and `## What's Changed\n\n$CHANGES\n\n## 🚀 Features\n\n$CONTRIBUTOR\n`
   - `categories:` mapping conventional commit prefixes (feat, fix, perf, refactor, docs, test, chore, ci) to section headers
   - `exclude-labels: ["skip-changelog"]`
   - `filter-by-commitish: true`
   - `commitish: "main"`

Do NOT add the Release Drafter workflow in this task — that lands in Task 2.
  </action>
  <verify>
    <automated>test -f .kodiak.toml && test -f .megalinter.yml && test -f .github/release-drafter.yml && grep -q "automerge" .kodiak.toml && grep -q "ENABLE:" .megalinter.yml && grep -q "tag-template" .github/release-drafter.yml</automated>
  </verify>
  <done>Three config files exist on disk with the locked narrow MegaLinter scope, Kodiak squash-merge settings, and Release Drafter template + categories.</done>
</task>

<task type="auto">
  <name>Task 2: Add new workflows (Release Drafter, Comfy publish, MCP publish) + extend ci.yml (publint + MegaLinter) + package.json publint script</name>
  <files>.github/workflows/release-drafter.yml, .github/workflows/publish-comfy.yml, .github/workflows/publish-mcp.yml, .github/workflows/ci.yml, package.json</files>
  <action>
Create three new workflow files and extend ci.yml + package.json. Per D-3, do NOT touch .github/workflows/publish.yml (existing OIDC Trusted Publishing stays). Per D-4, do NOT touch .github/workflows/release.yml (Changesets stays).

1. `.github/workflows/release-drafter.yml` (per D-4) — runs alongside existing Changesets release.yml:
   - `name: Release Drafter`
   - `on:` `push` to `main` and `pull_request` targeting `main` (so draft release accumulates as PRs merge)
   - `permissions: contents: write, pull-requests: read`
   - Single job `update_release_draft` using `release-drafter/release-drafter@v6` with `config-name: release-drafter.yml` and `GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}`.

2. `.github/workflows/publish-comfy.yml` (per D-1) — stub workflow using Comfy-Org/publish-node-action:
   - `name: Publish to Comfy Registry`
   - `on:` `release` `types: [published]` (same trigger as publish.yml; gated by tag)
   - `permissions: contents: read`
   - Single job `comfy-publish` runs-on ubuntu-latest, steps: checkout@v7, then `uses: Comfy-Org/publish-node-action@v1` with `personal_access_token: ${{ secrets.REGISTRY_ACCESS_TOKEN }}`.
   - Add a top-of-file comment block explaining prerequisites the user must satisfy manually:
     - Create `pyproject.toml` at repo root with `[project]` section matching Comfy custom-node metadata (name, version, description).
     - Add `REGISTRY_ACCESS_TOKEN` secret in repo Settings -> Secrets and variables -> Actions.
     - This workflow targets Comfy custom nodes; if repo is not a Comfy custom node, this workflow is a no-op stub kept for future use.
   - Job-level `if: ${{ vars.COMFY_PUBLISH_ENABLED == 'true' }}` gate so the workflow only runs when the maintainer flips the `COMFY_PUBLISH_ENABLED` repo variable — prevents stub from failing on every release until prerequisites are in place.

3. `.github/workflows/publish-mcp.yml` (per D-2) — single MCP publish using OtherVibes/mcp-publish-action@v1:
   - `name: Publish MCP Server`
   - `on:` `push` `tags: ["v*"]` (tag trigger only — runs once per release tag, never on branch pushes)
   - `permissions: contents: read, id-token: write` (OIDC required)
   - Single job `publish-mcp` runs-on ubuntu-latest, steps:
     - checkout@v7
     - uses: OtherVibes/mcp-publish-action@v1 with `registry_type: npm` and `identifier: awesome-coolify-mcp` (sourced from package.json `name` per CONTEXT.md discretion note).
   - Add top-of-file comment: this publishes the MCP server to the MCP Registry on every `v*` tag; the npm package itself is published separately by publish.yml via OIDC Trusted Publishing.

4. Extend `.github/workflows/ci.yml` (per D-7, D-8, D-5):
   - Verify setup-node is already `actions/setup-node@v7` with `node-version: 24` — it is. No bump needed; do NOT change the version. Per D-5, only bump if a newer stable exists; v7 is current.
   - Add a new step after `Tests` named `publint` running `npm run publint`.
   - Add a new step after `publint` named `MegaLinter` using `oxsecurity/megalinter@v8` with `mega-linter-flags: --fix --env-file .megalinter.yml` (or the action's documented input name). Set the step to use the `.megalinter.yml` config file already created in Task 1. Do NOT override ENABLE in the workflow — the config file is the source of truth.

5. Extend `package.json` (per D-7):
   - Add `"publint": "publint"` to `scripts`.
   - Add `"publint": "^0.3.0"` to `devDependencies` (latest stable line).
   - Do NOT touch any other script or dependency.
  </action>
  <verify>
    <automated>test -f .github/workflows/release-drafter.yml && test -f .github/workflows/publish-comfy.yml && test -f .github/workflows/publish-mcp.yml && grep -q "release-drafter/release-drafter@v6" .github/workflows/release-drafter.yml && grep -q "Comfy-Org/publish-node-action@v1" .github/workflows/publish-comfy.yml && grep -q "OtherVibes/mcp-publish-action@v1" .github/workflows/publish-mcp.yml && grep -q "awesome-coolify-mcp" .github/workflows/publish-mcp.yml && grep -q "publint" .github/workflows/ci.yml && grep -q "oxsecurity/megalinter" .github/workflows/ci.yml && grep -q "\"publint\":" package.json && grep -c "actions/setup-node@v7" .github/workflows/ci.yml | grep -q 1</automated>
  </verify>
  <done>Three new workflow files exist with the correct pinned actions; ci.yml has publint + MegaLinter steps; package.json has publint script + devDep; setup-node@v7 + Node 24 consistency preserved; existing publish.yml and release.yml untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Update dev-docs/github-setup-overview.md with all new pieces</name>
  <files>dev-docs/github-setup-overview.md</files>
  <action>
Update the existing German overview doc in place (per D-9). Keep the existing structure and sections; add new entries under the relevant H2 sections. Do NOT rewrite the file from scratch — append/edit only.

1. Update the `Stand:` line at top to `2026-07-19`.

2. Under `## CI/CD` add four new bullets (keep existing four bullets):
   - `.github/workflows/release-drafter.yml` — Release Drafter, pflegt einen Draft-Release bei jedem Push/PR auf `main` (parallel zu Changesets `release.yml`).
   - `.github/workflows/publish-comfy.yml` — Comfy-Org/publish-node-action Stub; läuft nur, wenn Repo-Variable `COMFY_PUBLISH_ENABLED=true`; benötigt `REGISTRY_ACCESS_TOKEN` Secret + `pyproject.toml` (siehe Workflow-Kommentar).
   - `.github/workflows/publish-mcp.yml` — OtherVibes/mcp-publish-action@v1; veröffentlicht den MCP-Server bei `v*`-Tags ins MCP-Registry (npm-Typ, Identifier `awesome-coolify-mcp`); OIDC `id-token: write`.
   - Publint + MegaLinter Schritte in `ci.yml` ergänzt (siehe `## Lint` unten).

3. Add a new H2 section `## Lint & Quality` (place after `## CI/CD`):
   - `.megalinter.yml` — schmaler MegaLinter (nur TypeScript, JavaScript, YAML, Markdown, actionlint); läuft als CI-Schritt in `ci.yml`.
   - `publint` npm-Script + CI-Schritt — prüft Package-Layout (exports/files/bin) vor jedem Publish.

4. Add a new H2 section `## Merge-Automatisierung` (place after `## Repo-Schutz`):
   - `.kodiak.toml` — Kodiak-Config (Squash-Merge, `automerge`-Label); Kodiak ist eine GitHub-App, die der Repo-Owner manuell installiert (https://kodiakhq.com -> Add to GitHub -> Repo `clezcoding/awesome-coolify` auswählen). Diese Datei steuert nur das Verhalten nach der Installation.
   - `.github/release-drafter.yml` — Release-Drafter-Config (Templates, Conventional-Commit-Kategorien).

5. Under `## Repo-Modell` add a bullet noting `awesome-coolify-mcp` is the npm/MCP-Registry identifier (already mentioned but reinforce: same name used by `publish-mcp.yml`).

Do NOT remove any existing content. Do NOT change the `## Repo-Schutz` or `## .gitignore` sections.
  </action>
  <verify>
    <automated>grep -q "release-drafter" dev-docs/github-setup-overview.md && grep -q "publish-comfy" dev-docs/github-setup-overview.md && grep -q "publish-mcp" dev-docs/github-setup-overview.md && grep -q "kodiak" dev-docs/github-setup-overview.md && grep -q "megalinter" dev-docs/github-setup-overview.md && grep -q "publint" dev-docs/github-setup-overview.md</automated>
  </verify>
  <done>dev-docs/github-setup-overview.md mentions all five new pieces (Kodiak, Release Drafter, Comfy publish, MCP publish, MegaLinter, publint) with the correct prerequisites and the Stand date is 2026-07-19.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> npm registry | OIDC token exchange in publish.yml (existing, untouched) |
| GitHub Actions runner -> MCP Registry | OIDC token exchange in new publish-mcp.yml |
| GitHub Actions runner -> Comfy Registry | PAT-based auth in new publish-comfy.yml (gated by repo variable) |
| PR author -> main branch | Kodiak automerge gate (label-gated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8ol-01 | Tampering | npm/pip installs in CI | high | mitigate | Pin all GitHub Actions to tagged versions (@v7, @v6, @v8, @v1) — no @main; publint devDep pinned to ^0.3.0 |
| T-8ol-02 | Spoofing | publish-mcp.yml OIDC | medium | mitigate | Require id-token: write + tag trigger v* only; identifier locked to package.json name |
| T-8ol-03 | Elevation | publish-comfy.yml stub | medium | mitigate | Gate job on COMFY_PUBLISH_ENABLED repo variable; document REGISTRY_ACCESS_TOKEN + pyproject.toml prerequisites in workflow comments and docs |
| T-8ol-04 | Tampering | Kodiak automerge | medium | mitigate | require_automerge_label=true; auto_approve_usernames empty; only squash method |
| T-8ol-05 | Information | MegaLinter reports | low | accept | Reports are CI logs only; no secrets exposed; narrow scope reduces surface |
</threat_model>

<verification>
- All five new files exist on disk and parse (YAML/TOML).
- `npm run publint` succeeds locally after `npm install`.
- `ci.yml` retains setup-node@v7 + Node 24.
- `publish.yml` and `release.yml` are byte-identical to pre-task versions (git diff empty for those two files).
- `dev-docs/github-setup-overview.md` mentions every new piece.
</verification>

<success_criteria>
- Kodiak `.kodiak.toml` exists with squash + automerge-label config.
- Release Drafter workflow + config exist alongside untouched Changesets release.yml.
- Comfy publish workflow exists as variable-gated stub with prerequisite comments.
- MCP publish workflow exists with v* trigger, OIDC, registry_type npm, identifier awesome-coolify-mcp.
- publint added to package.json scripts + devDependencies + ci.yml step.
- MegaLinter added to ci.yml with narrow .megalinter.yml (TS/JS/YAML/Markdown/Actions only).
- setup-node@v7 + Node 24 verified consistent across all workflows; no bumps needed.
- Existing publish.yml (OIDC) untouched.
- dev-docs/github-setup-overview.md updated with all new pieces.
</success_criteria>

<output>
Create `.planning/quick/260719-8ol-install-github-repo-tooling-per-locked-d/260719-8ol-SUMMARY.md` when done.
</output>

