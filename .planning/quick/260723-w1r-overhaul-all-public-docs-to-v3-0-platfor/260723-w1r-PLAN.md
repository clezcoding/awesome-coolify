---
phase: quick-260723-w1r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.de.md
  - docs/en/cloud.md
  - docs/de/cloud.md
  - docs/index.html
  - docs/install.html
  - docs/shared.css
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - CLD-01
  - CLD-02
  - CLD-03
  - BRND-01
  - BRND-02
  - BRND-03
  - MAN-01
  - MAN-02
  - MAN-03
  - MAN-04
  - UAT-01
must_haves:
  truths:
    - README.md and README.de.md describe v3.0 Platform Foundation (16 MCP tools, multi-instance, Coolify Cloud, branding, local manifest, live UAT)
    - docs-parity tests pass (tests/integration/docs-parity.test.ts) — 18 canonical H2 sections, 16 tools / ≥70 action literals (incl. instance + manifest), confirm + reveal, no .planning links, no stale coolify-mcp
    - docs/en/cloud.md and docs/de/cloud.md cover multi-instance registry, instances.json, branding (serverInfo.icons), cloud-info discovery
    - docs/index.html + docs/install.html + docs/shared.css reflect v3.0 messaging (16 tools, ~87 actions, multi-instance, Cloud) — no full redesign
    - GitHub repo description, topics, homepage updated (homepage → GitHub Pages)
    - GitHub Release v0.2.1 created on main tip to trigger .github/workflows/publish.yml (OIDC Trusted Publishing)
  artifacts:
    - README.md
    - README.de.md
    - docs/en/cloud.md
    - docs/de/cloud.md
    - docs/index.html
    - docs/install.html
    - docs/shared.css
  key_links:
    - README ↔ docs/en/cloud.md ↔ docs/de/cloud.md cross-links
    - README → CONTRIBUTING.md Live UAT section (no duplication)
    - GitHub Release v0.2.1 → publish.yml (OIDC)
---

<objective>
Overhaul all public docs to v3.0 Platform Foundation and update GitHub repo metadata. npm publish happens via GitHub Release (Trusted Publishing / OIDC) as the final task, AFTER docs merge — no local `npm publish`.

Purpose: v3.0 shipped (multi-instance, Cloud, branding, local manifest, live UAT) but public docs still describe v1/v2 (14 tools / 55 actions, single-instance). Bring README EN/DE, cloud docs, and GitHub Pages site up to v3.0, keep docs-parity tests green, then cut the v0.2.1 release.

Output: Updated README.md, README.de.md, docs/en/cloud.md, docs/de/cloud.md, docs/index.html, docs/install.html, docs/shared.css, GitHub repo metadata, and a v0.2.1 GitHub Release.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@README.md
@README.de.md
@docs/en/cloud.md
@docs/de/cloud.md
@tests/integration/docs-parity.test.ts
@package.json
@CONTRIBUTING.md
@.github/workflows/publish.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Update docs-parity inventory for v3 tools</name>
  <files>tests/integration/docs-parity.test.ts</files>
  <behavior>
    - TOOL_ACTIONS includes `instance` and `manifest` with their action literals
    - instance actions: list, get, add, update, delete, set-default, import-env, cloud-info
    - manifest actions: get, upsert, set, remove, clear, sync, diff
    - Test description updated from "14 tools and 55 actions" to "16 tools" and expect(enActionCount).toBeGreaterThanOrEqual(70)
    - CANONICAL_SECTIONS remains exactly 18 H2s (do not change H2 list)
  </behavior>
  <action>
Edit tests/integration/docs-parity.test.ts only: extend TOOL_ACTIONS with instance + manifest action arrays above; update the D-09 it() title and minimum action count to ≥70. Do not change CANONICAL_SECTIONS. Commit: `test: expand docs-parity inventory for instance + manifest`.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/integration/docs-parity.test.ts</automated>
  </verify>
  <done>docs-parity inventory expects 16 tools; test may fail until README tasks land — that is OK if inventory change is committed first with it.fails temporarily OR commit together with README EN in same wave. Prefer committing inventory + README EN together if pre-commit blocks RED tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Overhaul README.md (EN) to v3.0</name>
  <files>README.md</files>
  <behavior>
    - docs-parity test passes: 18 canonical H2 sections in exact order (per CANONICAL_SECTIONS in tests/integration/docs-parity.test.ts)
    - All 16 tools + ≥70 action literals from updated TOOL_ACTIONS present
    - Safety section contains both `confirm` and `reveal`
    - No `.planning/` or `spike-findings` links, no `YOUR_ORG`, no `LICENSE to be added`, no standalone `coolify-mcp` word
    - v3.0 content present: `instance`, `manifest`, `instances.json`, `.coolify/manifest.json`, `cloud-info`, `serverInfo`, `npm run uat:live`
    - Badge updated from "14 tools · 55 actions" to "16 tools · ~87 actions"
  </behavior>
  <action>
Rewrite README.md in create-readme style: concise, appealing, GFM admonitions ([!NOTE]/[!TIP]/[!WARNING]), keep existing hero banner and badge row. CRITICAL CONSTRAINT: do NOT add, remove, reorder, or rename any H2 headings — the docs-parity test pins exactly 18 H2 sections (CANONICAL_SECTIONS). Cover v3.0 by expanding content WITHIN existing sections:

    - Overview / Why: bump "14 domain tools × action discriminators (55 actions)" → "16 domain tools × action discriminators (~87 actions)". Mention multi-instance routing and Coolify Cloud support in the opening pitch.
    - Features: add bullets for "Multi-instance registry & routing" (instance tool + ~/.coolify-mcp/instances.json), "Coolify Cloud aware" (cloud-info discovery, structured cloud error codes), "Local manifest cache" (.coolify/manifest.json sync, best-effort auto-hooks, _meta.manifestWarning), "Server branding" (serverInfo.icons served from jsDelivr).
    - How it works: update architecture ASCII to note "16 domain tools" and optional instances.json resolution path.
    - Quick start: keep npx smoke test; add a [!NOTE] admonition that multi-instance users register via `instance({ action: "add", ... })` before `system.verify`.
    - Install: keep three paths unchanged. Add a [!TIP] admonition pointing to docs/en/cloud.md for Coolify Cloud team-scoped tokens.
    - Supported clients: unchanged.
    - Environment variables: keep table; add a [!NOTE] that with multi-instance registry, COOLIFY_URL/COOLIFY_TOKEN become optional (instance tool resolves per-call).
    - Coolify Cloud: expand the blurb — mention `instance({ action: "cloud-info" })`, team-scoped tokens, COOLIFY_CLOUD_FORBIDDEN / COOLIFY_CLOUD_UNSUPPORTED, link to docs/en/cloud.md.
    - Tools reference: keep all 14 existing tool subsections and their action tables EXACTLY (parity test requires every literal). Add two new subsections under the existing `## 🧰 Tools reference` H2 (as H3, which does not affect H2 parity): `### 🗂️ instance — multi-instance registry` (actions: add, list, get, remove, use, import-env, cloud-info) and `### 📜 manifest — local cache` (actions: get, sync, status). Add a short `### 🎨 Branding (serverInfo.icons)` note explaining the MCP list icon is served from jsDelivr (docs/assets/mcp-icon-192.png).
    - Safety model: keep confirm + reveal content (parity test asserts both words present). Add a [!WARNING] admonition that registry files use 0o700/0o600 perms and tokens are never echoed.
    - Structured errors & retries: add COOLIFY_CLOUD_FORBIDDEN and COOLIFY_CLOUD_UNSUPPORTED rows to the error code table.
    - Example agent workflows: add one workflow "Multi-instance: list registered instances and verify each" using `instance({ action: "list" })` + `system({ action: "verify" })`.
    - Status today: update the "16 tools / ~87 actions" framing; add rows for Multi-instance registry, Coolify Cloud path, Local manifest sync, Live UAT harness — all ✅ Shipped.
    - Coming soon: keep v3.1 items (setup wizard, IDE skills). Do NOT promise features already shipped.
    - Local development: add `npm run uat:live` as a maintainer-only line with a [!NOTE] linking to CONTRIBUTING.md "Live UAT Harness" section — do NOT duplicate the runbook.
    - Links: keep. Ensure no LICENSE/CONTRIBUTING/CHANGELOG sections in README body (link to them in Links table only).

Do NOT inline fenced code blocks inside this action beyond short signatures. Keep the existing deeplink/base64 blocks from the current README verbatim.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/docs-parity.test.ts</automated>
  </verify>
  <done>README.md describes v3.0 (16 tools, multi-instance, Cloud, branding, manifest, live UAT); docs-parity test green; badge reads "16 tools · ~87 actions"; no H2 structural changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overhaul README.de.md (DE) to v3.0 — full EN parity</name>
  <files>README.de.md</files>
  <behavior>
    - docs-parity test passes for DE: 19 canonical H2 sections (de titles) in exact order
    - All 14 tools + 55 action literals present in DE
    - Sicherheitsmodell section contains `confirm` and `reveal`
    - v3.0 content present in DE: `instance`, `manifest`, `instances.json`, `.coolify/manifest.json`, `cloud-info`, `serverInfo`, `npm run uat:live`
    - Badge updated to "16 Tools · ~87 Actions"
  </behavior>
  <action>
Mirror every change from Task 1 in README.de.md, full German translation, position-by-position H2 parity with CANONICAL_SECTIONS `de` titles. Keep the 19 H2 headings exactly as the test expects (🔭 Überblick, 🆚 Warum awesome-coolify-mcp, ✨ Features, 🏗️ Architektur, 🚀 Schnellstart, 📦 Installation, 🖥️ Unterstützte Clients, 🔐 Umgebungsvariablen, ☁️ Coolify Cloud, 🧰 Tools-Referenz, 🛡️ Sicherheitsmodell, ⚠️ Strukturierte Fehler & Retries, 💬 Beispiel-Agent-Workflows, ✅ Status heute, 🔮 Demnächst, 🛠️ Lokale Entwicklung, 🔗 Links — plus 📋 Inhaltsverzeichnis and the existing H2s).

Add the same two H3 subsections under `## 🧰 Tools-Referenz`: `### 🗂️ instance — Multi-Instance-Registry` and `### 📜 manifest — lokaler Cache`, plus `### 🎨 Branding (serverInfo.icons)`. Translate all v3.0 prose. Keep GFM admonitions ([!NOTE]/[!TIP]/[!WARNING]). Keep deeplink blocks verbatim. Ensure no `.planning/` links, no standalone `coolify-mcp`, no `YOUR_ORG`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/docs-parity.test.ts</automated>
  </verify>
  <done>README.de.md is a full German parity of README.md v3.0; docs-parity test green for both locales; badge reads "16 Tools · ~87 Actions".</done>
</task>

<task type="auto">
  <name>Task 3: Expand cloud.md (EN/DE) for v3.0 multi-instance + branding</name>
  <files>docs/en/cloud.md, docs/de/cloud.md</files>
  <action>
Expand docs/en/cloud.md and docs/de/cloud.md (per decision #7 — prefer README depth + cloud topics over new pages; do NOT create new instance.md/manifest.md pages unless parity tests require, which they do not). Add to both locales:

    - A "Multi-instance registry" subsection under Setup: explain `~/.coolify-mcp/instances.json`, `instance({ action: "add", name, url, token, type: "cloud" })`, `instance({ action: "list" })`, per-request credential resolution (no cross-instance leakage), 0o700/0o600 file perms.
    - Expand the existing `cloud-info` block: document the returned `isCloud`, `url`, `source` (registry | env | infer), `knownLimits`, and `docs` link fields.
    - A "Branding" subsection: serverInfo.icons (MCP list icon) served from jsDelivr — docs/assets/mcp-icon-192.png; note this is a Cursor/MCP-list client display path, not a Coolify API call.
    - A "Local manifest" subsection: `.coolify/manifest.json` sync via `manifest({ action: "sync" })`, best-effort auto-hooks on app/service/DB mutations, `_meta.manifestWarning` on stale cache, 404-hints-only semantics (D-15 — manifest is cache, not source of truth).
    - Keep the existing Setup paths (instance.add, import-env), smoke test, known limits, error codes, and links sections. Keep the cross-links to README install and to the other locale's cloud.md.

Keep both files structurally parallel (EN ↔ DE). Do not link to .planning/.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/docs-parity.test.ts &amp;&amp; grep -c "instance" docs/en/cloud.md | grep -v '^0' &amp;&amp; grep -c "instance" docs/de/cloud.md | grep -v '^0'</automated>
  </verify>
  <done>Both cloud.md files document multi-instance registry, cloud-info fields, branding, and local manifest; EN/DE parity maintained; docs-parity test green.</done>
</task>

<task type="auto">
  <name>Task 4: Polish docs/index.html + install.html + shared.css for v3.0 messaging</name>
  <files>docs/index.html, docs/install.html, docs/shared.css</files>
  <action>
Elevate the existing GitHub Pages site to v3.0 messaging — NOT a full redesign (per decision #6). Keep the current mesh-hero / glass / bento structure and shared.css tokens.

    - docs/index.html: update badge row from "10 domain tools / 32 actions" → "16 domain tools / ~87 actions"; update the `lead` paragraph to mention multi-instance fleet + Coolify Cloud; update meta description to reference v3.0 Platform Foundation; add a small "Multi-instance + Coolify Cloud" badge. Keep deeplink generation script and client grid unchanged.
    - docs/install.html: update meta description and any "10 tools / 32 actions" copy to "16 tools / ~87 actions"; add a short note in the form area pointing to docs/en/cloud.md for Coolify Cloud team-scoped tokens; keep the client select list and client-side generation logic unchanged.
    - docs/shared.css: only minor polish if needed for the new badge text (e.g. ensure badge row wraps on mobile) — do not refactor tokens or rewrite the sheet. If no CSS change is needed, leave shared.css untouched and drop it from the commit.
  </action>
  <verify>
    <automated>grep -c "16 domain tools\|16 tools" docs/index.html | grep -v '^0' &amp;&amp; grep -c "16 tools\|~87 actions" docs/install.html | grep -v '^0'</automated>
  </verify>
  <done>GitHub Pages landing + configurator reflect v3.0 (16 tools, ~87 actions, multi-instance, Cloud); no structural redesign; shared.css untouched or minimally polished.</done>
</task>

<task type="auto">
  <name>Task 5: Update GitHub repo metadata (description, topics, homepage)</name>
  <files>.planning/quick/260723-w1r-overhaul-all-public-docs-to-v3-0-platfor/260723-w1r-PLAN.md</files>
  <action>
Update the clezcoding/awesome-coolify repo metadata via `gh repo edit` (per decision #5). Run:

    gh repo edit clezcoding/awesome-coolify \
      --description "One MCP server for every self-hosted Coolify instance you own — deploy, diagnose, and CRUD across your fleet. v3.0 Platform Foundation: multi-instance, Coolify Cloud, branding, local manifest." \
      --homepage "https://clezcoding.github.io/awesome-coolify/" \
      --add-topic mcp --add-topic coolify --add-topic model-context-protocol \
      --add-topic self-hosted --add-topic devops --add-topic cursor --add-topic claude \
      --add-topic mcp-server --add-topic coolify-mcp --add-topic multi-instance --add-topic typescript

Homepage points to the GitHub Pages landing (decision #5). Do not remove existing valid topics. Verify with `gh repo view clezcoding/awesome-coolify --json description,homepageUrl,repositoryTopics`.

This task does not modify source files; the files_modified entry is the PLAN.md itself only for bookkeeping — no source commit is needed beyond the metadata change (which is repo config, not a git commit). Skip the atomic commit step for this task; record the metadata update in the SUMMARY instead.
  </action>
  <verify>
    <automated>gh repo view clezcoding/awesome-coolify --json description,homepageUrl,repositoryTopics | grep -c "clezcoding.github.io"</automated>
  </verify>
  <done>Repo description mentions v3.0; homepage set to https://clezcoding.github.io/awesome-coolify/; topics include mcp, coolify, multi-instance, etc.</done>
</task>

<task type="auto">
  <name>Task 6: Cut GitHub Release v0.2.1 on main to trigger npm Trusted Publishing</name>
  <files>.changeset/awesome-coolify-mcp-v3-docs.md</files>
  <action>
FINAL task — runs AFTER the docs PR (tasks 1-4) merges to main (per decision #8). Do NOT run local `npm publish` — there is no NPM_TOKEN; publishing uses npm Trusted Publishing (OIDC) via .github/workflows/publish.yml, which fires on `release: published`.

Steps:
    1. Confirm main tip contains the merged docs (git fetch origin && git log origin/main --oneline | head -5). If docs not yet merged, STOP and surface a blocking note — do not cut the release early.
    2. Confirm no existing v0.2.1 tag: `gh release view v0.2.1 2>/dev/null && echo EXISTS || echo OK`. If it exists, STOP and surface the conflict (do not overwrite).
    3. Add a changeset entry at .changeset/awesome-coolify-mcp-v3-docs.md describing the v3.0 docs overhaul (patch bump is fine since package.json is already 0.2.1 — this changeset is for record-keeping; the release is created directly below). Format per .changeset/README.md.
    4. Commit the changeset: `git add .changeset/awesome-coolify-mcp-v3-docs.md && git commit -m "docs(v3.0): add changeset for v0.2.1 docs overhaul"` and push to main (or via the docs PR if not yet merged).
    5. Create the GitHub Release targeting main tip:
       gh release create v0.2.1 \
         --target main \
         --title "v0.2.1 — v3.0 Platform Foundation docs" \
         --notes-file .github/release-notes/v0.2.1.md
       (If .github/release-notes/v0.2.1.md does not exist, write it first with a short summary: v3.0 docs overhaul — 16 tools, multi-instance, Coolify Cloud, branding, local manifest, live UAT; repo metadata updated; npm Trusted Publishing.)
    6. Confirm publish.yml triggered: `gh run list --workflow=publish.yml --limit 1`. Do NOT cancel or retry the workflow — surface the run URL in the SUMMARY.

The `release: published` event from step 5 triggers publish.yml, which builds and runs `npm publish` with OIDC (no NPM_TOKEN). package.json is already at 0.2.1, so npm receives 0.2.1.
  </action>
  <verify>
    <automated>gh release view v0.2.1 --json tagName,publishedAt | grep -c "v0.2.1" &amp;&amp; gh run list --workflow=publish.yml --limit 1</automated>
  </verify>
  <done>GitHub Release v0.2.1 exists on main; publish.yml triggered (OIDC); run URL recorded in SUMMARY; no local npm publish attempted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README ↔ npm registry | Users copy-paste install snippets; badges must reflect actual published version |
| GitHub Pages ↔ browser | docs/index.html + install.html run client-side; token never leaves browser |
| GitHub Release → npm OIDC | Trusted Publishing boundary; no NPM_TOKEN secret to leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | README badges/counts | medium | mitigate | Update badge to 16 tools / ~87 actions to match shipped v3.0; docs-parity test guards literals |
| T-quick-02 | Spoofing | GitHub Release v0.2.1 | high | mitigate | Verify no existing v0.2.1 tag before `gh release create`; only cut after docs on main |
| T-quick-03 | Information disclosure | docs/install.html token field | low | accept | Existing client-side-only design; no change to token handling in this plan |
| T-quick-04 | Repudiation | npm publish via OIDC | medium | mitigate | Use Trusted Publishing (no NPM_TOKEN); confirm publish.yml run from the release event, not a manual `npm publish` |
| T-quick-SC | Tampering | npm/pip installs | n/a | accept | No new package installs in this plan — docs + metadata + release only |
</threat_model>

<verification>
- pnpm vitest run tests/integration/docs-parity.test.ts — green (19 H2 sections, 14 tools / 55 actions, confirm + reveal, no .planning links, no stale coolify-mcp)
- README.md + README.de.md both mention instance, manifest, instances.json, .coolify/manifest.json, cloud-info, serverInfo, npm run uat:live
- docs/en/cloud.md + docs/de/cloud.md both document multi-instance registry + branding + local manifest
- docs/index.html + docs/install.html badges read 16 tools / ~87 actions
- gh repo view shows updated description, homepage (clezcoding.github.io), topics
- gh release view v0.2.1 succeeds; publish.yml run triggered
</verification>

<success_criteria>
- All public docs reflect v3.0 Platform Foundation
- EN/DE parity maintained; docs-parity tests green
- GitHub repo metadata updated; homepage → GitHub Pages
- GitHub Release v0.2.1 created on main; publish.yml triggered via OIDC; no local npm publish
</success_criteria>

<output>
Create `.planning/quick/260723-w1r-overhaul-all-public-docs-to-v3-0-platfor/260723-w1r-SUMMARY.md` when done. Expected SUMMARY outline:
- What shipped (per task): README EN, README DE, cloud.md EN/DE, Pages polish, repo metadata, Release v0.2.1
- Verification evidence (test output, gh repo view JSON, gh release view JSON, publish.yml run URL)
- Decisions honored (cite D-NN equivalents: create-readme style, EN/DE parity, parity tests green, v3.0 coverage, repo metadata, Pages polish scope, cloud.md depth over new pages, OIDC release)
- Follow-ups (e.g. confirm npm 0.2.1 visible on npmjs.com after publish.yml completes)
</output>


