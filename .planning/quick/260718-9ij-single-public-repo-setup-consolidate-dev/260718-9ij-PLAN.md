---
phase: quick-260718-9ij
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sync-public-repo.sh
  - github-setup-kit/
  - .github/workflows/publish.yml
  - .github/workflows/pages.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - README.md
  - README.de.md
  - docs/index.html
  - docs/install.html
  - docs/assets/README.md
  - .gitignore
  - package.json
autonomous: true
requirements:
  - Q-260718-9ij-01
  - Q-260718-9ij-02
  - Q-260718-9ij-03
must_haves:
  truths:
    - "clezcoding/awesome-coolify is the single public canonical repo (no dual-repo sync)"
    - "No `awesome-coolify-mcp` repo URL references remain in GitHub/Pages/jsDelivr/clone/issues/discussions links; npm package name `awesome-coolify-mcp` is preserved"
    - "GitHub Pages workflow exists and deploys docs/ on push to main"
    - "Repo is public, main is branch-protected, custom labels synced, legacy repo archived"
  artifacts:
    - ".github/workflows/pages.yml"
    - "updated .gitignore with recursive .DS_Store, logs, tsbuildinfo, .npm, .tgz, .env.local"
    - "package.json with LOW-risk July-2026 stable bumps applied"
  key_links:
    - "publish.yml trusted-publisher repo ref matches clezcoding/awesome-coolify"
    - "Pages site URL = clezcoding.github.io/awesome-coolify/"
---

<objective>
Consolidate dev + public repos into a single public canonical repo `clezcoding/awesome-coolify`, complete remaining GitHub setup from github-setup-overview.md, fix .gitignore, bump stale packages to July-2026 stable, and apply gh CLI runtime steps (public, Pages, branch protection, label sync, archive legacy repo).

Purpose: Eliminate the dual-repo sync model so there is one source of truth; unblock trusted publishing and Pages by going public; ship a clean public surface.
Output: Updated README/docs/workflows, new pages.yml, hardened .gitignore, bumped package.json, applied gh CLI state changes.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260718-9ij-single-public-repo-setup-consolidate-dev/260718-9ij-RESEARCH.md
@github-setup-overview.md
@.planning/STATE.md
@.gitignore
@package.json
@CONTRIBUTING.md
@.github/workflows/publish.yml
@README.md
@README.de.md
@docs/index.html
@docs/install.html
@docs/assets/README.md
@.github/ISSUE_TEMPLATE/config.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo consolidation cleanup — drop sync scaffolding, repoint URLs to clezcoding/awesome-coolify</name>
  <files>scripts/sync-public-repo.sh, github-setup-kit/, .github/workflows/publish.yml, .github/ISSUE_TEMPLATE/config.yml, README.md, README.de.md, docs/index.html, docs/install.html, docs/assets/README.md</files>
  <action>
Delete `scripts/sync-public-repo.sh` (dual-repo sync obsolete). Delete the entire `github-setup-kit/` directory (was a one-time setup template; superseded by in-repo `.github/`).

Update `.github/workflows/publish.yml` line 10 comment: change `clezcoding/awesome-coolify-mcp` to `clezcoding/awesome-coolify` (trusted-publisher repo must match the actual repo on npmjs.com).

Update `.github/ISSUE_TEMPLATE/config.yml` line 4 discussions URL: `clezcoding/awesome-coolify-mcp/discussions` → `clezcoding/awesome-coolify/discussions`.

In `README.md`, `README.de.md`, `docs/index.html`, `docs/install.html`, `docs/assets/README.md` replace ONLY repo-URL references — GitHub repo, Pages site, jsDelivr CDN `@main`, clone URL, issues, discussions, blob links — from `clezcoding/awesome-coolify-mcp` to `clezcoding/awesome-coolify`. Specifically: `cdn.jsdelivr.net/gh/clezcoding/awesome-coolify-mcp@main` → `clezcoding/awesome-coolify@main`; `clezcoding.github.io/awesome-coolify-mcp` → `clezcoding.github.io/awesome-coolify`; `github.com/clezcoding/awesome-coolify-mcp` → `github.com/clezcoding/awesome-coolify` (repo, blob, issues, discussions).

DO NOT change the npm package name `awesome-coolify-mcp` — keep `npmjs.com/package/awesome-coolify-mcp` URLs, `npx -y awesome-coolify-mcp` invocations, MCP server name string `'awesome-coolify-mcp'` in `src/mcp/server.ts`, `package.json` name field, `package-lock.json` name, or `run-mcp.mjs` server-name string. Package name ≠ repo name is intentional per A1/Q2 in RESEARCH.

Do NOT touch `src/mcp/server.ts:547` or `scripts/run-mcp.mjs` server-name strings — those are the npm package identity, not repo URLs.
  </action>
  <verify>
    <automated>test ! -f scripts/sync-public-repo.sh && test ! -d github-setup-kit && grep -c "clezcoding/awesome-coolify-mcp" README.md README.de.md docs/index.html docs/install.html docs/assets/README.md .github/workflows/publish.yml .github/ISSUE_TEMPLATE/config.yml | grep -v ':0$' | grep -v 'awesome-coolify-mcp"' | grep -v 'npmjs.com' | wc -l | grep -q '^0$' && echo OK</automated>
  </verify>
  <done>sync-public-repo.sh and github-setup-kit/ gone; publish.yml comment + ISSUE_TEMPLATE config.yml discussions URL point to clezcoding/awesome-coolify; all GitHub/Pages/jsDelivr/clone/issues/discussions URLs in README*.md, docs/*.html, docs/assets/README.md point to clezcoding/awesome-coolify; npm package name awesome-coolify-mcp preserved everywhere it appears as a package identifier.</done>
</task>

<task type="auto">
  <name>Task 2: Complete GitHub infra — pages.yml, .gitignore hardening, LOW-risk package bumps</name>
  <files>.github/workflows/pages.yml, .gitignore, package.json</files>
  <action>
Create `.github/workflows/pages.yml` deploying `docs/` on push to main (paths: docs/**) plus workflow_dispatch. Permissions: contents: read, pages: write, id-token: write. Concurrency group `pages`, cancel-in-progress: false. Single job `deploy` on ubuntu-latest with environment `github-pages` (url from `steps.deploy.outputs.page_url`). Steps: `actions/checkout@v4`, `actions/configure-pages@v5`, `actions/upload-pages-artifact@v3` with `path: docs`, then `actions/deploy-pages@v4` (id: deploy).

Harden `.gitignore`: change root-only `.DS_Store` to recursive `**/.DS_Store` (`.github/.DS_Store` already leaked per RESEARCH). Append: `*.log`, `npm-debug.log*`, `yarn-debug.log*`, `yarn-error.log*`, `.npm/`, `*.tsbuildinfo`, `.tsbuildinfo`, `*.tgz`, `.env.local`, `.env.*.local`, `.idea/`, `.vscode/`. Keep existing `docs/readme/node_modules/` line (redundant but harmless). Keep `.planning/`, `.cursor/`, `.claude/`, `.agents/`, `graphify-out/`, `.coolify-mcp/`, `scripts/test-mcp-stdio.mjs` ignored — these MUST NOT leak to public repo.

In `package.json` bump ONLY LOW-risk deps to July-2026 stable (verified via `npm view` this session):
- `@modelcontextprotocol/server`: `^2.0.0-beta.3` → `^2.0.0-beta.4`
- `ofetch`: `^1.4.0` → `^1.5.1`
- `tsup`: `^8.0.0` → `^8.5.1`

DO NOT bump HIGH-risk packages in this task (typescript 7, vitest 4, @vitest/coverage-v8 4, @types/node 26) — they require a separate validation pass with `npm run build && npm test` and may surface type/test failures. Note them as deferred in the SUMMARY. Run `npm install` after edits to refresh `package-lock.json`, then `npm run build` and `npm test` to confirm green.
  </action>
  <verify>
    <automated>test -f .github/workflows/pages.yml && grep -q "upload-pages-artifact@v3" .github/workflows/pages.yml && grep -q "deploy-pages@v4" .github/workflows/pages.yml && grep -q "^\*\*/\.DS_Store" .gitignore && grep -q "^\*\.tsbuildinfo" .gitignore && grep -E '"@modelcontextprotocol/server": "\^2\.0\.0-beta\.4"' package.json && grep -E '"ofetch": "\^1\.5\.1"' package.json && grep -E '"tsup": "\^8\.5\.1"' package.json && npm run build && npm test</automated>
  </verify>
  <done>pages.yml workflow exists with correct actions/permissions/concurrency; .gitignore hardened with recursive .DS_Store + logs + tsbuildinfo + .npm + .tgz + .env.local + IDE dirs; package.json has the 3 LOW-risk bumps; npm install + build + test green; HIGH-risk bumps noted as deferred in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: gh CLI runtime — public, Pages, branch protection, label sync, archive legacy, drop legacy remote</name>
  <files></files>
  <action>
Run gh CLI steps in order. Pre-flight: verify `git log --all --full-history -- .planning/ .env` returns empty (no history leaks of dev-only or secret files before going public). If non-empty, STOP and surface to operator — history rewrite via git filter-repo required first.

1. Public visibility:
   `gh repo edit clezcoding/awesome-coolify --visibility public --accept-visibility-change-consequences`
2. Enable Pages with GitHub Actions build type (so pages.yml workflow owns deploys):
   `gh api -X PUT repos/clezcoding/awesome-coolify/pages -f build_type=workflow`
3. Branch protection (now unblocked on public Free plan):
   `bash scripts/setup-branch-protection.sh`
   Verifies: force-push/delete blocked, "Lint, Test & Build" status check required, linear history.
4. Label sync first bootstrap:
   `gh workflow run labels.yml -R clezcoding/awesome-coolify`
   Then poll `gh run list -w labels.yml -R clezcoding/awesome-coolify --limit 1` until conclusion=success. Confirm `gh label list -R clezcoding/awesome-coolify` shows custom labels from `.github/labels.yml` (not just the 9 defaults).
5. Archive legacy public repo (preserves issues/PRs; npm package `awesome-coolify-mcp` is unaffected since package name ≠ repo name):
   `gh repo archive clezcoding/awesome-coolify-mcp --yes`
6. Remove stale git remote:
   `git remote remove origin-legacy-mcp`
   Verify `git remote -v` shows only `origin`.

If any step returns a non-zero exit, STOP and surface the exact gh error to the operator — do not attempt destructive workarounds.
  </action>
  <verify>
    <automated>test -z "$(git log --all --full-history -- .planning/ .env 2>/dev/null)" && [ "$(gh repo view clezcoding/awesome-coolify --json visibility -q .visibility)" = "PUBLIC" ] && gh api repos/clezcoding/awesome-coolify/pages --jq .build_type | grep -q workflow && gh api repos/clezcoding/awesome-coolify/branches/main/protection --jq .required_status_checks.contexts | grep -q "Lint, Test & Build" && [ "$(gh label list -R clezcoding/awesome-coolify | wc -l)" -gt 9 ] && [ "$(gh repo view clezcoding/awesome-coolify-mcp --json isArchived -q .isArchived)" = "true" ] && ! git remote get-url origin-legacy-mcp 2>/dev/null && echo OK</automated>
  </verify>
  <done>Repo public; Pages source = workflow; main branch-protected with "Lint, Test & Build" required; custom labels synced (>9 labels); legacy clezcoding/awesome-coolify-mcp repo archived; origin-legacy-mcp remote removed; git log has no .planning/ or .env history leaks.</done>
</task>

</tasks>

<verification>
- `gh repo view clezcoding/awesome-coolify --json visibility -q .visibility` → `PUBLIC`
- `gh api repos/clezcoding/awesome-coolify/pages --jq .build_type` → `workflow`
- `gh api repos/clezcoding/awesome-coolify/branches/main/protection` → 200 with required status check
- `gh label list -R clezcoding/awesome-coolify | wc -l` → > 9
- `gh repo view clezcoding/awesome-coolify-mcp --json isArchived -q .isArchived` → `true`
- `git remote -v` → only `origin`
- `npm run build && npm test` → green
- `grep -r "clezcoding/awesome-coolify-mcp" README.md README.de.md docs/ .github/ | grep -vE 'npmjs\.com|npx|awesome-coolify-mcp"'` → empty
- `test ! -f scripts/sync-public-repo.sh && test ! -d github-setup-kit` → true
</verification>

<success_criteria>
- Single public canonical repo clezcoding/awesome-coolify with no dual-repo sync scaffolding
- GitHub Pages workflow live and deploying docs/ on push to main
- Repo public, main branch-protected, custom labels synced
- Legacy clezcoding/awesome-coolify-mcp repo archived
- npm package awesome-coolify-mcp unchanged and still installable
- .gitignore hardened for public surface
- LOW-risk package bumps applied; build + test green
- No .planning/ or secret history leaks
</success_criteria>

<output>
Create `.planning/quick/260718-9ij-single-public-repo-setup-consolidate-dev/260718-9ij-SUMMARY.md` when done
</output>

