---
phase: quick-260808-tk8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/ROOT-OVERVIEW-AUDIT.md
  - .gitignore
  - .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/260808-tk8-SUMMARY.md
autonomous: true
requirements:
  - OVERVIEW-01
  - OVERVIEW-02
  - OVERVIEW-03
user_setup: []

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Classification table exists listing every tracked GitHub-visible root entry as keep, keep-but-noisy, or candidate-untrack with rationale and impact"
    - "No tracked path that currently matches an active .gitignore rule remains in the index (clear leakages fixed)"
    - "commit_docs stays true; core .planning/, shared .cursor ship hooks/rule, skills/, and tooling configs stay tracked unless a future user decision says otherwise"
    - "SUMMARY ends with a short German-labeled user decision menu for next-step untrack/policy choices"
  artifacts:
    - path: ".planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/ROOT-OVERVIEW-AUDIT.md"
      provides: "Root overview classification (keep / keep-but-noisy / candidate-untrack)"
      contains: "rationale and impact per root entry"
    - path: ".planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/260808-tk8-SUMMARY.md"
      provides: "Execution summary + Entscheidungsmenü"
      contains: "Entscheidungsmenü"
  key_links:
    - from: ".gitignore"
      to: "git ls-files"
      via: "check-ignore / cached-path audit"
      pattern: "ignore rules match intent; leakages cleared"
    - from: ".planning/config.json commit_docs: true"
      to: "ROOT-OVERVIEW-AUDIT.md keep class for .planning"
      via: "policy constraint (prior 260727-4hd)"
      pattern: "core .planning stays keep"
---

<objective>
Audit what GitHub shows at repo root: classify each tracked root folder/file for overview clutter, verify index vs `.gitignore`, apply only safe additive hygiene, and leave a clear next-step decision menu — without untracking `.planning` or flipping `commit_docs`.

Purpose: Root listing clarity for visitors/contributors; policy gray areas stay recommendations, not silent mass untracks.
Output: `ROOT-OVERVIEW-AUDIT.md`, optional additive `.gitignore` tweaks for clear leakages only, `260808-tk8-SUMMARY.md` with Entscheidungsmenü.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/config.json
@.gitignore
@.planning/quick/260727-4hd-r-ume-den-projektordner-auf-und-sortiere/260727-4hd-SUMMARY.md
@.planning/quick/260729-7mc-bereinige-den-lokalen-projektordner-und-/260729-7mc-SUMMARY.md

# Known tracked root set (GitHub overview):
# .changeset .coolify-manifest.example.json .cursor .editorconfig .env.example .github .gitignore
# .husky .kodiak.toml .markdownlint.json .mega-linter.yml .planning .yamllint.yml
# CHANGELOG.md CONTRIBUTING.md LICENSE README.de.md README.md SECURITY.md
# commitlint.config.js docs lint-staged.config.mjs package.json pnpm-lock.yaml pnpm-workspace.yaml
# scripts skills src tests tsconfig.json tsup.config.ts vitest.config.ts
#
# Counts (approx): .planning≈603, src=105, docs=43, scripts=25, .github=23, tests=16,
# skills=4, .cursor=3 (hooks.json, hooks/gsd-ship-post-hook.sh, rules/gsd-ship-labels.mdc)
#
# Policy (locked by prior work + config — honor, do not reverse here):
# - commit_docs: true → core .planning/ stays tracked
# - Ephemeral .planning subpaths already ignored (research/.cache, forensics, spikes, …)
# - .cursor/* ignored except shared ship hooks + gsd-ship-labels.mdc
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Classify tracked root entries into overview buckets</name>
  <files>.planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/ROOT-OVERVIEW-AUDIT.md</files>
  <read_first>
    - `.gitignore` (current ignore intent)
    - `.planning/config.json` (`commit_docs: true`)
    - Prior summaries `260727-4hd-SUMMARY.md`, `260729-7mc-SUMMARY.md`
  </read_first>
  <action>
    1. Refresh the tracked root listing with `git ls-tree --name-only HEAD` (or equivalent) so the audit matches HEAD, not memory.
    2. For each root entry, assign exactly one class:
       - **keep** — required for product, CI, packaging, license, or public docs
       - **keep-but-noisy** — legitimately tracked under current policy but dominates/clutters GitHub root overview (expect `.planning` here given ~603 files and `commit_docs: true`)
       - **candidate-untrack** — would improve overview clarity if ignored + removed from tracking, but needs an explicit future user decision (gray areas: broader `.cursor/`, `skills/`, tooling configs). Put rationale + impact (what breaks if untracked) on every row.
    3. Write `ROOT-OVERVIEW-AUDIT.md` in this quick dir with: summary counts per class, a table of all root entries, and a short "policy anchors" section citing `commit_docs: true` and prior 260727-4hd (core `.planning` stays tracked in this task).
    4. Hard safety: do not schedule or perform mass `git rm --cached` of `.planning/`, `.cursor/`, `skills/`, or tooling configs in this task. Classification only.
  </action>
  <verify>
    <automated>test -f .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/ROOT-OVERVIEW-AUDIT.md &amp;&amp; grep -E 'keep-but-noisy|candidate-untrack|^\\|.*keep' .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/ROOT-OVERVIEW-AUDIT.md | head -20; ROOTS=$(git ls-tree --name-only HEAD | wc -l | tr -d ' '); ROWS=$(grep -cE '^\| [^|]+ \| (keep|keep-but-noisy|candidate-untrack) \|' .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/ROOT-OVERVIEW-AUDIT.md || true); test "$ROWS" -ge "$ROOTS"</automated>
  </verify>
  <done>ROOT-OVERVIEW-AUDIT.md exists; every tracked root entry classified with rationale + impact; `.planning` marked keep or keep-but-noisy (not silently queued for untrack).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify index vs .gitignore; fix clear leakages only</name>
  <files>.gitignore</files>
  <read_first>
    - `.gitignore`
    - `ROOT-OVERVIEW-AUDIT.md` (from Task 1)
  </read_first>
  <action>
    1. Find tracked paths that match ignore rules: e.g. `git ls-files -ci --exclude-standard` (or loop `git check-ignore -v` over `git ls-files`). Record the list in the audit file under a "Leakage check" section (even if empty).
    2. If clear leakages exist (paths already covered by ignore intent from 260727-4hd / existing rules — ephemeral `.planning/*`, `dev-docs/`, `graphify-out/`, `node_modules/`, `dist/`, `.agents/`, etc.): remove those paths from the index with `git rm --cached` (keep local files) and note each path in the audit.
    3. If ignore rules are missing for something already documented as local-only intent, add **additive** `.gitignore` lines only — no rewrites of keep-tracking policy for `.planning` core, shared `.cursor` ship files, `skills/`, or package/CI configs.
    4. Do not change `commit_docs` in `.planning/config.json`. Do not untrack healthy core `.planning` trees to "declutter" the overview.
  </action>
  <verify>
    <automated>LEAK=$(git ls-files -ci --exclude-standard | wc -l | tr -d ' '); test "$LEAK" -eq 0; grep -q 'Leakage check' .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/ROOT-OVERVIEW-AUDIT.md</automated>
  </verify>
  <done>Leakage count is 0; audit documents the check; any fix was index-only or additive ignore for clear local-only paths.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document recommendations + SUMMARY Entscheidungsmenü</name>
  <files>.planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/ROOT-OVERVIEW-AUDIT.md, .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/260808-tk8-SUMMARY.md</files>
  <action>
    1. Append a "Recommendations (no policy change this task)" section to `ROOT-OVERVIEW-AUDIT.md`: what would improve GitHub root clarity if the user later opts in (e.g. `commit_docs: false` + ignore `.planning/`, narrower/wider `.cursor` tracking, moving large planning history to a private docs repo). Each recommendation must state tradeoff vs GSD workflow.
    2. Write `260808-tk8-SUMMARY.md` with accomplishments, files touched, verification results, and a short **Entscheidungsmenü** (German labels OK) offering next-step choices such as:
       - A) Status quo beibehalten (`commit_docs: true`, Overview bleibt noisy wegen `.planning`)
       - B) Nur zusätzliche ephemeral `.planning`-Pfade ignorieren (wenn Audit welche gefunden hat)
       - C) Folge-Quick: `.planning` untracken + `commit_docs: false` (explizite Policy-Änderung)
       - D) Folge-Quick: weitere root-noise Kandidaten (`skills/`, Tooling) gezielt entscheiden
    3. Still no mass untrack of gray-area trees in this task.
  </action>
  <verify>
    <automated>test -f .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/260808-tk8-SUMMARY.md &amp;&amp; grep -q 'Entscheidungsmenü' .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/260808-tk8-SUMMARY.md &amp;&amp; grep -q 'Recommendations' .planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/ROOT-OVERVIEW-AUDIT.md &amp;&amp; node -e 'const c=require("./.planning/config.json"); if(c.commit_docs!==true) process.exit(1)'</automated>
  </verify>
  <done>SUMMARY has Entscheidungsmenü; audit has recommendations; `commit_docs` still true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → git index / GitHub | What stays tracked becomes public repo surface |
| `.gitignore` → index | Ignore rules must not hide secrets while keeping intentional shared tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-tk8-01 | Information Disclosure | Accidental track of local-only paths (secrets, caches, agent dirs) | medium | mitigate | Task 2 leakage audit + `git rm --cached` for clear ignore matches only |
| T-tk8-02 | Tampering | Mass untrack of `.planning` / ship hooks without policy change | medium | mitigate | Hard safety: no mass untrack; `commit_docs` untouched; gray areas only in decision menu |
| T-tk8-03 | Denial of Service | Over-aggressive ignore breaks CI/hooks/docs | low | mitigate | Additive `.gitignore` only; preserve `.cursor/hooks**`, `gsd-ship-labels.mdc`, package/CI configs |
| T-tk8-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this quick task |
</threat_model>

<verification>
- `ROOT-OVERVIEW-AUDIT.md` classifies every `git ls-tree --name-only HEAD` root entry
- `git ls-files -ci --exclude-standard` exits empty (0 lines)
- `commit_docs === true` in `.planning/config.json`
- SUMMARY contains Entscheidungsmenü for follow-up
</verification>

<success_criteria>
- Overview clutter is diagnosed with keep / keep-but-noisy / candidate-untrack
- Clear ignore leakages fixed; no silent policy reversal on `.planning` or `commit_docs`
- User has an explicit German-labeled menu for any real declutter that needs a decision
</success_criteria>

<source_audit>
SOURCE    | ID          | Feature/Requirement                                      | Plan | Status  | Notes
--------- | ----------- | -------------------------------------------------------- | ---- | ------- | -----
GOAL      | —           | Audit GitHub root folders/files for overview clutter     | 01   | COVERED | Classification + leakage + menu
REQ       | OVERVIEW-01 | Produce root classification artifact                     | 01   | COVERED | Task 1
REQ       | OVERVIEW-02 | Verify tracked vs .gitignore; fix clear leakages         | 01   | COVERED | Task 2
REQ       | OVERVIEW-03 | Safe hygiene only + SUMMARY Entscheidungsmenü            | 01   | COVERED | Task 3
CONTEXT   | prior-4hd   | Core .planning tracked (commit_docs: true)               | 01   | COVERED | Explicit non-change + keep/keep-but-noisy
RESEARCH  | —           | (none — quick mode, no research)                         | —    | N/A     |
</source_audit>

<output>
Create `.planning/quick/260808-tk8-task-1-audit-github-repo-root-folders-fi/260808-tk8-SUMMARY.md` when done
</output>
