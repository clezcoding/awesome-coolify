---
phase: quick-260729-6el
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.de.md
  - tests/integration/docs-parity.test.ts
autonomous: true
requirements:
  - QUICK-README
must_haves:
  truths:
    - "README.md gives a new user a technically accurate path from product value through install, first verification, core workflows, safety, and support."
    - "README.de.md mirrors English structure and technical facts in natural German."
    - "Public claims match package.json and live registrations: package 1.0.1, Node.js >=24, Coolify API 4.1.x, 18 MCP tools, and four MCP prompts."
    - "No README claims the stale 17-tool/~91-action surface, unshipped application/service/database CRUD, unshipped setup tooling, or an obsolete next-milestone roadmap."
    - "Existing project artwork and dynamic, verifiable badges produce a polished GitHub presentation without fabricated metrics or endorsements."
    - "README parity, Markdown formatting, relative links, image paths, install snippets, and safety guidance remain verifiable."
  artifacts:
    - README.md
    - README.de.md
    - tests/integration/docs-parity.test.ts
  key_links:
    - "README install commands and prerequisites ↔ package.json name, engines, packageManager, and scripts"
    - "README tool count and capability inventory ↔ src/mcp/server.ts registrations and src/mcp/tools/*ActionsCatalog"
    - "README EN/DE structure and required literals ↔ tests/integration/docs-parity.test.ts"
    - "README images ↔ docs/assets/README.md and committed docs/assets files"
---

<objective>
Rewrite both project READMEs into one polished, detailed, technically current bilingual entry point, then strengthen the existing parity guard so future feature drift is caught.

Purpose: Current READMEs look substantial but contain stale 17-tool/~91-action counts, claim shipped CRUD/setup capabilities are still upcoming, omit or understate newer setup/log/diagnose surfaces, and preserve obsolete roadmap copy. Replace accumulated patches with a coherent user journey grounded in current code and tests.

Output: Rewritten `README.md`, parity-matched `README.de.md`, and updated README parity assertions for the shipped 18-tool surface.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@README.md
@README.de.md
@package.json
@docs/assets/README.md
@src/mcp/server.ts
@src/mcp/server.test.ts
@src/mcp/tools/application.ts
@src/mcp/tools/deployment.ts
@src/mcp/tools/diagnose.ts
@src/mcp/tools/service.ts
@src/mcp/tools/database.ts
@src/mcp/tools/recipe.ts
@src/mcp/tools/setup.ts
@docs/coverage-map.yaml
@tests/integration/docs-parity.test.ts
@.markdownlint.json
@.mega-linter.yml
@CONTRIBUTING.md
@SECURITY.md
@CHANGELOG.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Rewrite English and German READMEs against live product truth</name>
  <files>README.md, README.de.md, tests/integration/docs-parity.test.ts</files>
  <behavior>
    - Both locales retain position-by-position H2 parity and contain the same install paths, shipped capabilities, limitations, safety rules, prompts, and links.
    - Tool claims use the 18 registrations asserted by `src/mcp/server.test.ts`, including `recipe` and `setup`.
    - Stale `17 tools`, `17 Tools`, `~91 actions`, `~91 Actions`, `16 domain tools`, and `16 Domänen-Tools` claims disappear.
    - No replacement approximate action count is published unless executor derives it reproducibly from every live `*ActionsCatalog`; prefer an exact `18 tools` claim with no action-total badge because `docs/coverage-map.yaml` tracks API mappings, not the complete MCP surface.
    - Shipped application/service/database CRUD, recipes, `service.list-types`, setup wizard, IDE skills, deployment watch/build logs, bounded application log follow, `diagnose.logs`, capability discovery, and four prompts are described as shipped.
    - Coolify 4.1.x limitation remains explicit: service/database logs are unavailable until compatible upstream APIs are stable; no stub support claim.
  </behavior>
  <action>
Start with a fact inventory, then rewrite both files in lockstep. Treat `package.json`, `src/mcp/server.test.ts`, registration blocks in `src/mcp/server.ts`, relevant `*ActionsCatalog` constants, and current behavior tests as sources of truth; do not copy milestone prose blindly.

Build a clearer information architecture while preserving the 19 canonical H2 headings and order currently enforced by `tests/integration/docs-parity.test.ts`: polished hero, concise value proposition, verified badges, table of contents, problem/value comparison, capability overview, architecture, quick start, three install paths, supported clients, environment variables, Cloud guidance, prompts, tool reference, safety, errors, runnable workflows, current status, honest future scope, local development, and resource links. Use concise GFM tables, collapsible details only when they reduce scanning cost, and GitHub admonitions for security, limitations, and setup notes.

Correct all known drift:
- Package remains `awesome-coolify-mcp` 1.0.1; Node.js requirement is >=24; package manager is pnpm; target is Coolify API 4.1.x with 4.1.2 capability caveats.
- Advertise exactly 18 registered MCP tools and four prompts. Remove stale approximate action totals rather than replacing one unverifiable estimate with another.
- Present full application/service/database CRUD, recipe orchestration, dynamic one-click type discovery, setup preflight/wire/resume, IDE skill installation, deployment watch/logs, application log follow, `diagnose.logs`, `incident`, and `system.version` capabilities as shipped.
- Remove statements saying CRUD, setup wizard, or IDE skills are upcoming or absent. Replace broad obsolete roadmap with only source-backed future boundaries: service/database logs when compatible Coolify APIs are available, tracked OpenAPI coverage gaps, and deferred cross-instance fan-out. Avoid release dates and promises.
- Preserve accurate safety semantics: confirm gates, dry-run/preview behavior, default secret masking, human-approved `reveal: true`, unmasked log-body warning, registry permissions, and no automatic rollback for partial recipes.
- Keep install commands copyable and locale-independent. Preserve EN links to `docs/en/cloud.md` and `docs/en/setup.md`; DE links to `docs/de/cloud.md` but must retain `docs/en/setup.md` because no German setup guide exists.
- Do not add body sections named License, Contributing, Changelog, or Security. Link `LICENSE`, `CONTRIBUTING.md`, `CHANGELOG.md`, and `SECURITY.md` from the resource area where useful.
- Keep community/non-affiliation wording. Do not invent usage numbers, compatibility guarantees, testimonials, endorsements, performance claims, or roadmap commitments.

Update `tests/integration/docs-parity.test.ts` as a truth-preserving guard, not as a way to weaken failures. Change stale “all 16 tools” wording and inventory to all 18 registered names by adding `recipe` and `setup`; include current high-value action literals missing from the old inventory, including `diagnose.logs`, `deployment.watch`, `deployment.logs`, shipped CRUD/env/backup actions, `service.list-types`, recipe actions, and setup `preflight`/`wire`/`resume`. Keep canonical H2 parity, safety checks, forbidden internal links, and stale package-name checks. Prefer explicit required literals over a fragile approximate global action count.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/integration/docs-parity.test.ts src/mcp/server.test.ts</automated>
  </verify>
  <done>Both rewritten READMEs tell the same current product story; 18-tool/four-prompt claims match live registration; obsolete capability and roadmap claims are gone; strengthened parity tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Apply visual polish, asset discipline, and final documentation checks</name>
  <files>README.md, README.de.md</files>
  <action>
Polish both locales as one visual system using committed assets cataloged in `docs/assets/README.md`. Keep `hero-banner.png`; use `features.png` and `architecture.png` only where they support adjacent content. Use `logo-transparent.png` sparingly if it materially improves hierarchy. Remove `coming-soon.png` if its depicted roadmap conflicts with shipped features. Reference README art through the established jsDelivr `@main/docs/assets/...` URLs and keep descriptive, locale-appropriate alt text.

Keep badge styling consistent with brand violet `#6b16ed`, canvas/surface neutrals, and accent `#fcd34d`. Retain dynamic npm version/download badges and verified static Node.js, TypeScript, Coolify API, 18-tool, and MIT badges. Add meaningful dynamic badges such as repository CI status and latest GitHub release only when their target URLs are valid for `clezcoding/awesome-coolify`. Do not add stars, popularity, coverage, security-score, compatibility, sponsorship, or endorsement badges unless backed by an authoritative current source. Keep one-click Cursor and VS Code controls visually subordinate to the hero and explain placeholder credentials.

Run a final bilingual copy edit: English direct and technical; German idiomatic rather than word-for-word. Keep terminology and code identifiers exact, remove duplicated claims, limit emojis to navigational headings, ensure tables remain readable, and keep all local links relative. Check every relative file/image target exists, every table-of-contents anchor resolves under GitHub slug rules, all code fences close, and no `.planning/` link leaks into public docs. Respect `.markdownlint.json`; existing CI runs MegaLinter MARKDOWN validation through `.github/workflows/ci.yml`.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/integration/docs-parity.test.ts src/mcp/server.test.ts &amp;&amp; pnpm run lint &amp;&amp; pnpm test</automated>
    <human-check>Preview both files in GitHub-flavored Markdown. Confirm hero/assets load, badge rows wrap cleanly, tables scan well, admonitions render, one-click controls remain usable, TOC links resolve, and EN/DE sections correspond.</human-check>
  </verify>
  <done>Both READMEs render as polished bilingual project entry points, use only valid project assets and defensible badges, pass repository checks, and contain no broken local target or stale public claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → user shell/IDE | Users copy install commands, URLs, environment-variable examples, and MCP calls into trusted local tooling. |
| README → external badge/image services | GitHub rendering requests shields.io, jsDelivr, Cursor, npm, and repository URLs. |
| README claims → Coolify API | Capability wording must not imply endpoints absent from supported Coolify 4.1.x installations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Spoofing | Badges and affiliation copy | medium | mitigate | Link badges to authoritative npm/GitHub targets and retain explicit non-affiliation note. |
| T-quick-02 | Tampering | Install/deeplink examples | high | mitigate | Preserve reviewable placeholder credentials; test docs parity and manually validate decoded target/config behavior before changing encoded URLs. |
| T-quick-03 | Information disclosure | Tokens and log guidance | high | mitigate | Use placeholders only; preserve masking, reveal-consent, registry-permission, and raw-log warnings. |
| T-quick-04 | Denial of service | Unsupported service/database log claims | medium | mitigate | State 4.1.x API limitation and never document unavailable stub actions. |
| T-quick-SC | Tampering | Package installation instructions | medium | mitigate | Use existing published package name and pnpm workflow from package.json; add no dependency. |
</threat_model>

<verification>
- `pnpm exec vitest run tests/integration/docs-parity.test.ts src/mcp/server.test.ts`
- `pnpm run lint`
- `pnpm test`
- Existing PR CI MegaLinter MARKDOWN job passes using `.mega-linter.yml` and `.markdownlint.json`.
- GitHub-rendered manual preview confirms assets, badges, TOCs, admonitions, tables, and one-click controls.
- No dedicated repository link checker exists; executor must validate all relative targets/anchors during preview instead of claiming an absent automated link gate.
</verification>

<success_criteria>
- New user can understand value, install safely, verify connectivity, and choose a representative workflow without consulting internal planning artifacts.
- Technical claims trace to current package metadata, server registrations, action catalogs, tests, or dedicated public docs.
- English and German versions remain structurally and semantically parallel.
- README presentation uses existing brand assets and useful badges without visual clutter or unverifiable proof.
- Stale counts, shipped-as-future claims, and obsolete milestone copy are eliminated.
- Full test suite, focused README/server tests, build lint, and CI Markdown validation pass.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Comprehensive, polished, detailed README rewrite | Tasks 1–2 |
| GOAL | Technical accuracy and stale-claim removal | Task 1 |
| GOAL | Additional meaningful badges and suitable project assets | Task 2 |
| REQ | EN/DE parity where tests require it | Task 1 + focused parity test |
| REQ | No invented metrics, endorsements, or compatibility claims | Tasks 1–2 |
| REQ | No License/Contributing/Changelog body sections | Task 1 |
| REQ | GFM and GitHub admonitions | Task 1 |
| REQ | Runnable parity, format, and repository checks | Task 2 + verification |
| CONTEXT | Use requested create-readme, copywriting, and high-end visual-design guidance | Tasks 1–2, adapted to GitHub README constraints |
| CONTEXT | Prefer existing assets | Task 2 |
| CONTEXT | Do not modify ROADMAP.md | Entire plan; ROADMAP.md absent from `files_modified` |

No source item missing. No deferred item introduced.

<output>
Create `.planning/quick/260729-6el-fixe-und-berarbeite-die-readme-des-proje/260729-6el-SUMMARY.md` when done.
</output>
