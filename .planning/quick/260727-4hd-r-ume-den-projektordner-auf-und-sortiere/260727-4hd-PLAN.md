---
quick_id: 260727-4hd
status: planned
mode: quick
---

# Quick Task 260727-4hd: Projektordner aufräumen + .gitignore

## Goal

Nur projektrelevante Dateien im Git-Index behalten. Ephemeres GSD-/Dev-Artefakte ignorieren und aus dem Index entfernen. `mcp_features.md` nach `.planning/` verschieben.

## Scope

**Behalten (tracked):** `src/`, `tests/`, `docs/`, `skills/`, `scripts/`, `.github/`, Packaging, README, CHANGELOG, CONTRIBUTING, SECURITY, `.cursor/hooks`, Kern-`.planning/` (STATE, ROADMAP, phases, quick, milestones, codebase, research/*.md).

**Ignorieren + untrack:** Cache, Forensics, Spikes, Sketches, Notes, Todos, Debug, `mcp_features.md`, `dev-docs/`.

---

## Task 1: .gitignore erweitern

**files:** `.gitignore`

**action:**

Neue Sektion `# Dev / GSD ephemeral (local only)` mit:

- `mcp_features.md`
- `dev-docs/`
- `.planning/research/.cache/`
- `.planning/forensics/`
- `.planning/spikes/`
- `.planning/sketches/`
- `.planning/notes/`
- `.planning/todos/`
- `.planning/debug/`

**verify:** `git check-ignore -v mcp_features.md dev-docs/ .planning/research/.cache/foo.json`

**done:** Alle ephemeral Pfade werden von git ignoriert.

---

## Task 2: Dateien verschieben + Index bereinigen

**files:** `mcp_features.md` → `.planning/mcp_features.md`, Index-Cleanup

**action:**

1. `git mv mcp_features.md .planning/mcp_features.md` (oder `mv` + untrack root)
2. `git rm -r --cached` für alle bereits getrackten Pfade unter den neuen ignore-Regeln
3. `dev-docs/` aus Index entfernen (lokal behalten)

**verify:**

```bash
test ! -f mcp_features.md
test -f .planning/mcp_features.md
test "$(git ls-files mcp_features.md dev-docs .planning/research/.cache .planning/forensics .planning/spikes .planning/sketches .planning/notes .planning/todos .planning/debug 2>/dev/null | wc -l | tr -d ' ')" -eq 0
```

**done:** Keine ephemeral/dev-only Pfade mehr im Index; Dateien bleiben lokal.

---

## Task 3: CONVENTIONS.md Pfad aktualisieren

**files:** `.planning/codebase/CONVENTIONS.md`

**action:** `mcp_features.md` → `.planning/mcp_features.md` in Repository Model + Tabelle.

**verify:** `grep -q '.planning/mcp_features.md' .planning/codebase/CONVENTIONS.md`

**done:** Dokumentation stimmt mit neuer Ablage überein.
