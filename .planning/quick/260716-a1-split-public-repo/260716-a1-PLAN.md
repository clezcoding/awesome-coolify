---
quick_id: 260716-a1
status: planned
---

# Quick Task 260716-a1: Split into private dev repo + public repo, npm + Pages

## Task Boundary

Split the single `clezcoding/awesome-coolify-mcp` repo into two:

1. Rename the existing repo (this workspace) to `awesome-coolify-mcp-dev`, keep it **private**. This stays the maintainer's working repo — full `.planning/` history, spikes, research, dev tooling.
2. Create a new **public** repo `clezcoding/awesome-coolify-mcp` with a clean, prod-only file set and a fresh git history (no `.planning/` ever in its history).
3. Set up the npm package (`awesome-coolify-mcp`) for real publish from the clean snapshot.
4. Set up GitHub Pages for the public repo (`docs/` → `https://clezcoding.github.io/awesome-coolify-mcp/`) via GitHub Actions.
5. Update docs (`README.md`/`README.de.md`/`CONTRIBUTING.md`) to describe the two-repo model, and add a repeatable sync script for future public releases.

## Tasks

1. **Rename + repo split** — `gh repo rename` on the existing repo, fix local `origin`; build a clean file snapshot (whitelist: `src`, `tests`, `docs`, `README*`, `LICENSE`, `CONTRIBUTING.md`, `package*.json`, ts configs, `.env.example`, `.gitignore`, `.github/workflows/ci.yml`); strip dev-only npm scripts (`start`, `mcp`, `higgsfield:fix`, `gsd:fix-verification`) from the public `package.json`; `git init` fresh history; `gh repo create clezcoding/awesome-coolify-mcp --public --push`.
2. **CI/CD + Pages** — add `.github/workflows/pages.yml` (Actions-based Pages deploy for `docs/`) and `.github/workflows/publish.yml` (release-triggered npm publish); enable Pages via `gh api POST .../pages -f build_type=workflow`.
3. **npm publish + docs** — fix a latent `bin` path bug (leading `./` made npm strip the `bin` field on publish), verify `npm pack --dry-run` tarball is clean (7 files, no leaks), publish `awesome-coolify-mcp@0.1.0`; update `CONTRIBUTING.md` with the two-repo model and `scripts/sync-public-repo.sh` for repeatable future syncs; remove the dev-only `npm start` hint from `src/config/env.ts` so source stays identical across both repos.

## Claude's Discretion

- Exact exclude list for the public snapshot (decided: `.planning`, `.cursor`, `.claude`, `.agents`, `graphify-out`, `mcp_features.md`, and the 4 dev-only scripts).
- GitHub Pages build source: Actions workflow (user-selected) over branch-deploy.
- npm publish: user chose to run `npm publish` manually from `/tmp/awesome-coolify-mcp-public` due to 2FA OTP requirement blocking non-interactive publish.
