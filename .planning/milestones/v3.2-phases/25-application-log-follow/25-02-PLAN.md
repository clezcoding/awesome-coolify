---
phase: 25-application-log-follow
plan: 02
type: execute
wave: 2
depends_on:
  - "25-01"
files_modified:
  - src/mcp/tools/application.ts
  - src/mcp/tools/application.test.ts
  - tests/integration/logs-service-db-flow.test.ts
autonomous: true
requirements:
  - OBS-02
  - OBS-03
user_setup: []

must_haves:
  truths:
    - "applicationActionSchema accepts follow params only on logs action via actionAllowedFields + zodDefaultFields follow false (OBS-02, D-14, D-03)"
    - "follow:true with deployment_uuid rejected COOLIFY_422 in both applicationLogsSchema and applicationActionSchema extraRefine (D-02)"
    - "One-shot runtime logs with follow absent or follow false call fetchApplicationLogs with identical args as pre-phase golden (OBS-03, D-03)"
    - "One-shot build logs via deployment_uuid unchanged — no follow branch entered (OBS-03)"
    - "integration logs-service-db-flow accepts follow:true schema and runtime one-shot regression passes (OBS-02, OBS-03)"
    - "isRetryableRateLimit wired on follow path matches deployment.watch 429 behavior (D-15)"
  artifacts:
    - path: src/mcp/tools/application.ts
      provides: "full flat schema wiring for follow params"
      contains: "actionAllowedFields"
    - path: src/mcp/tools/application.test.ts
      provides: "OBS-03 golden + follow guard tests"
      contains: "follow: false"
    - path: tests/integration/logs-service-db-flow.test.ts
      provides: "integration follow acceptance"
      contains: "follow"
  key_links:
    - from: "applicationActionSchema logs branch"
      to: "handleApplicationLogs"
      via: "zodDefaultFields strips phantom follow defaults from deploy/get"
    - from: "OBS-03 golden tests"
      to: "fetchApplicationLogs mock call args"
      via: "unchanged lines/offset when follow absent"
  assumptions:
    - "A-EDGE-OBS-03: concurrent one-shot and follow calls are independent — no shared poller state between requests"
    - "deploy wait:true timeout default remains 300s — follow default 120s applied only in logs+follow handler branch (RESEARCH Q2)"
  prohibitions:
    - statement: "Do not change one-shot sliceLogBlob or processDeploymentBuildLogs semantics (D-03)"
      status: enforced
      verification: "npx vitest run src/mcp/tools/application.test.ts -t \"runtime logs|build logs\" exits 0"
    - statement: "Do not hard-block follow when application_logs_follow capability false (D-19)"
      status: enforced
      verification: "rg -n \"application_logs_follow\" src/mcp/tools/application.ts | rg -v description | rg \"safeParse|refine\" | wc -l is 0 for capability gate"
    - statement: "Do not edit incident prompt (D-21)"
      status: enforced
      verification: "no diff under src/mcp/prompts/"
---

<objective>
Harden flat action schema for follow params and lock OBS-03 regression — one-shot runtime/build paths byte-identical when follow absent/false; follow+deployment_uuid guard; integration acceptance.

Purpose: Prevent OBS-03 regression from schema default leakage and document agent-visible validation surface (D-02, D-03, D-14).

Output: Full `applicationActionSchema` follow wiring, golden OBS-03 tests, green integration schema test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-application-log-follow/25-CONTEXT.md
@.planning/phases/25-application-log-follow/25-RESEARCH.md
@.planning/phases/25-application-log-follow/25-PATTERNS.md
@.planning/phases/25-application-log-follow/25-01-SUMMARY.md
@src/mcp/tools/application.ts
@src/mcp/tools/shared-read-params.ts
@src/mcp/tools/application.test.ts
@tests/integration/logs-service-db-flow.test.ts
</context>

## Artifacts this phase produces

- `applicationActionSchema` follow fields in shape, `actionAllowedFields.logs`, `zodDefaultFields` with `follow: false` only (not global timeout 120).
- `superRefine` guards: follow+deployment_uuid COOLIFY_422; optional offset reject/ignore when follow true.
- OBS-03 explicit tests: `follow: false`, follow absent, build path unchanged.
- Green `tests/integration/logs-service-db-flow.test.ts` follow schema case.

<tasks>

<task type="auto" tdd="true">
  <name>Flat action schema wiring + follow/build XOR guards</name>
  <reversibility rating="costly">D-02/D-14 publish follow param surface on applicationActionSchema flat tool.</reversibility>
  <files>src/mcp/tools/application.ts, src/mcp/tools/application.test.ts</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-CONTEXT.md (D-02, D-03, D-14, D-16, D-19)
    - .planning/phases/25-application-log-follow/25-PATTERNS.md (createFlatActionSchema deployment watch analog; superRefine)
    - src/mcp/tools/application.ts (applicationActionSchema createFlatActionSchema ~L451-776; applicationLogsSchema superRefine)
    - src/mcp/tools/deployment.ts (watch actionAllowedFields + zodDefaultFields pattern)
    - src/mcp/tools/shared-read-params.ts (createFlatActionSchema zodDefaultFields strip)
  </read_first>
  <behavior>
    - applicationActionSchema safeParse logs action with follow true uuid succeeds
    - applicationActionSchema safeParse deploy action with follow true fails unrecognized or stripped — follow not allowed on deploy
    - applicationLogsSchema and applicationActionSchema reject follow true with deployment_uuid COOLIFY_422
    - min_interval less than or equal max_interval validated when follow true on logs action
  </behavior>
  <action>
    Per D-02/D-14/D-16: Mirror applicationLogsSchema follow fields onto applicationActionSchema flat shape: follow boolean optional, idle_timeout, min_interval, max_interval, timeout optional int. Add follow timeout idle_timeout min_interval max_interval to actionAllowedFields.logs array. Add 6th createFlatActionSchema arg zodDefaultFields with follow false only — do not add timeout 120 to zodDefaultFields (deploy wait keeps 300 default in deploy branch). Extend extraRefine for action logs: if follow true and deployment_uuid add COOLIFY_422 issue; if follow true and offset greater than 0 add COOLIFY_422 or document ignore — prefer superRefine reject offset with follow for clarity (RESEARCH A3). Mirror follow plus deployment_uuid guard in applicationLogsSchema superRefine. Update applicationActionsCatalog and tool description with follow params and capability check note per D-04. Flip application.test.ts deployment_uuid and schema acceptance it.fails to it; add test deploy action ignores follow param.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "deployment_uuid|follow" -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "'follow'" src/mcp/tools/application.ts | wc -l | awk '{exit ($1>=3)?0:1}'
    - rg -n "follow.*deployment_uuid|deployment_uuid.*follow" src/mcp/tools/application.ts | wc -l | awk '{exit ($1>=1)?0:1}'
    - npx vitest run src/mcp/tools/application.test.ts -t "deployment_uuid" exits 0
    - npx vitest run src/mcp/tools/application.test.ts -t "applicationLogsSchema accepts follow" exits 0
  </acceptance_criteria>
  <done>Full flat schema accepts follow on logs only; follow+build path blocked.</done>
</task>

<task type="auto" tdd="true">
  <name>OBS-03 golden regression + integration follow acceptance</name>
  <reversibility rating="costly">D-03 OBS-03 success criterion — one-shot path regression is costly to recover if broken.</reversibility>
  <files>src/mcp/tools/application.test.ts, tests/integration/logs-service-db-flow.test.ts</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-CONTEXT.md (D-03, OBS-03)
    - .planning/phases/25-application-log-follow/25-VALIDATION.md (25-03-02, 25-03-03, 25-04-01 rows)
    - src/mcp/tools/application.test.ts (runtime golden ~L1049; build logs describe)
    - tests/integration/logs-service-db-flow.test.ts (application.logs describe)
  </read_first>
  <behavior>
    - handleApplicationAction logs uuid without follow calls fetchApplicationLogs with lines 100 same as before phase
    - handleApplicationAction logs uuid follow false identical fetchApplicationLogs args
    - handleApplicationAction logs deployment_uuid calls fetchDeployment not fetchApplicationLogs in follow loop
    - applicationActionSchema accepts follow true in integration test
  </behavior>
  <action>
    Per D-03/OBS-03: Add explicit application.test.ts cases: follow false hits same fetchApplicationLogs mock args as omit follow; assert build logs path still calls fetchDeployment and never followApplicationLogs (mock followApplicationLogs or spy poll util if exported). Ensure existing runtime logs default lines=100 and build logs tests still pass without assertion drift. Flip logs-service-db-flow.test.ts accepts follow:true to it with expect parsed.success true. Add integration assertion that application.logs runtime one-shot handler path unchanged (mock client, no follow key). Run deploy-watch and deployment tests sanity: npx vitest run src/utils/deploy-watch-poll.test.ts src/mcp/tools/deployment.test.ts -t watch -x to confirm no watch regression from deploy-watch-poll export.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "runtime logs|build logs|follow: false" -x</automated>
    <automated>npx vitest run tests/integration/logs-service-db-flow.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/mcp/tools/application.test.ts -t "runtime logs" exits 0
    - npx vitest run src/mcp/tools/application.test.ts -t "build logs" exits 0
    - npx vitest run tests/integration/logs-service-db-flow.test.ts exits 0
    - npx vitest run src/utils/deploy-watch-poll.test.ts exits 0
  </acceptance_criteria>
  <done>OBS-03 locked: one-shot runtime and build paths unchanged; integration accepts follow schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → Zod schema | follow params validated strict; XOR runtime vs build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | schema default leakage | medium | mitigate | zodDefaultFields follow false only; golden OBS-03 tests (D-03) |
| T-25-02 | Elevation | follow on build path | medium | mitigate | superRefine follow+deployment_uuid COOLIFY_422 (D-02) |
| T-25-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/application.test.ts tests/integration/logs-service-db-flow.test.ts -x`
- `npm test` wave 2 gate
</verification>

<success_criteria>
- OBS-03: runtime and build one-shot paths regression-locked
- OBS-02: follow validation surface complete on flat tool schema
- deployment.watch tests still green
</success_criteria>

<output>
Create `.planning/phases/25-application-log-follow/25-02-SUMMARY.md` when done
</output>
