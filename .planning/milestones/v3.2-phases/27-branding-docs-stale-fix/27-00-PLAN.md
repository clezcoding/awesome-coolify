---
phase: 27-branding-docs-stale-fix
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/server.test.ts
  - src/mcp/server-icons.test.ts
  - tests/integration/doc-version-parity.test.ts
autonomous: true
requirements:
  - BRND-01
  - DOC-01
user_setup: []

must_haves:
  truths:
    - "server.test.ts has it.fails scaffolds asserting buildMcpServerIcons import, data URI prefix in icons output, readPackageVersion for version field, and no literal 0.1.0 in constructor block (BRND-01, D-08)"
    - "server-icons.test.ts has it.fails scaffolds for buildMcpServerIcons returning ≥3 entries with data URI first and jsDelivr CDN URLs (BRND-01, D-01, D-02)"
    - "doc-version-parity.test.ts has it.fails scaffold rejecting stale Version Packages opener in PROJECT.md (DOC-01, D-07)"
    - "All new RED cases use vitest it.fails so husky pre-commit stays green"
  artifacts:
    - path: src/mcp/server.test.ts
      provides: "BRND-01/D-08 it.fails branding block extensions"
      contains: "buildMcpServerIcons"
    - path: src/mcp/server-icons.test.ts
      provides: "buildMcpServerIcons unit RED scaffolds"
      contains: "buildMcpServerIcons"
    - path: tests/integration/doc-version-parity.test.ts
      provides: "DOC-01 PROJECT opener gate"
      contains: "PROJECT.md"
  key_links:
    - from: "server.test.ts it.fails"
      to: "createAndConnectServer (Plan 27-01)"
      via: "source-regex + buildMcpServerIcons import"
    - from: "server-icons.test.ts it.fails"
      to: "buildMcpServerIcons (Plan 27-01)"
      via: "direct import"
  prohibitions:
    - statement: "Do not implement production icon modules or change server.ts in Wave 0 — scaffolds only"
      status: enforced
      verification: "git diff --name-only -- src/mcp/server.ts src/mcp/server-icons.ts scripts/generate-mcp-icon-data.mjs package.json is empty after this plan"
    - statement: "Do not rewrite CHANGELOG or milestone archives (D-09)"
      status: enforced
      verification: "git diff --name-only -- CHANGELOG.md .planning/milestones is empty after this plan"
---

<objective>
Wave 0 Nyquist RED scaffolds for Phase 27 — lock BRND-01 icon shape and DOC-01 stale-opener acceptance as `it.fails` before implementation.

Purpose: Plan 27-01 flips scaffolds GREEN without rewriting intent. Mirrors Phase 24/26 Wave 0 pattern.

Output: Extended `server.test.ts`, new `server-icons.test.ts`, DOC-01 grep test scaffold; production handlers untouched.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-branding-docs-stale-fix/27-CONTEXT.md
@.planning/phases/27-branding-docs-stale-fix/27-RESEARCH.md
@.planning/phases/27-branding-docs-stale-fix/27-PATTERNS.md
@.planning/phases/27-branding-docs-stale-fix/27-VALIDATION.md
@src/mcp/server.test.ts
@src/mcp/tools/meta.test.ts
@tests/integration/docs-parity.test.ts
</context>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | MCP icon workarounds in initialize | 27-00 (RED), 27-01 (GREEN) |
| GOAL | Docs reflect npm 1.0.1 shipped | 27-00 (RED), 27-03 (GREEN) |
| GOAL | Maintainer re-verify gate | 27-02 |
| REQ BRND-01 | data URI + multi-size PNG icons | 27-00, 27-01 |
| REQ BRND-02 | dist/ + npx verify doc | 27-02 |
| REQ DOC-01 | no stale pending-release wording | 27-00, 27-03 |
| D-01 | dual data URI + CDN | 27-00, 27-01 |
| D-02 | data URI primary ordering | 27-00, 27-01 |
| D-03 | four experiment budget | 27-02 |
| D-04 | both verify paths | 27-02 |
| D-05 | client limitation pass | 27-02 |
| D-06 | refresh cursor-icon-verify.md | 27-02 |
| D-07 | public surfaces 1.0.1 | 27-03 |
| D-08 | readPackageVersion in server.ts | 27-00, 27-01 |
| D-09 | no CHANGELOG/archive rewrite | prohibitions all plans |
| Deferred | Cursor IDE fix, full README migration | excluded |

## Artifacts this phase produces

- `src/mcp/server.test.ts` — extended `McpServer branding metadata` `it.fails` block.
- `src/mcp/server-icons.test.ts` — `buildMcpServerIcons()` shape RED tests.
- `tests/integration/doc-version-parity.test.ts` — PROJECT.md opener stale-string gate.

<tasks>

<task type="auto" tdd="true">
  <name>RED: server.test.ts BRND-01 + D-08 branding scaffolds</name>
  <files>src/mcp/server.test.ts</files>
  <read_first>
    - .planning/phases/27-branding-docs-stale-fix/27-PATTERNS.md (server.test.ts lines 330-392)
    - src/mcp/server.test.ts (McpServer branding metadata describe)
    - src/mcp/tools/meta.test.ts (readPackageVersion parity pattern lines 19-22)
    - src/utils/package-version.ts
  </read_first>
  <behavior>
    - Constructor block imports buildMcpServerIcons and calls icons: buildMcpServerIcons()
    - Constructor block uses version: readPackageVersion() not a string literal semver drift
    - buildMcpServerIcons output includes data:image/png;base64, prefix
    - buildMcpServerIcons output includes jsDelivr mcp-icon-192.png URL
    - Existing jsDelivr BRND-03 assertions remain valid after refactor
  </behavior>
  <action>
    Extend describe McpServer branding metadata with it.fails cases per D-01/D-08: assert readServerSource contains import buildMcpServerIcons from ./server-icons.js and icons: buildMcpServerIcons(); assert constructor block contains version: readPackageVersion() and does not contain version: '0.1.0'; add it.fails that dynamically imports buildMcpServerIcons and expects icons[0].src to match /^data:image\/png;base64,/ and at least one jsDelivr CDN src. Keep existing title, websiteUrl, description, mimeType assertions; update jsDelivr test to tolerate icons living in server-icons.ts rather than inline array if needed. Use readPackageVersion from package-version.js in version assertion test.
  </action>
  <acceptance_criteria>
    - New it.fails cases exist for buildMcpServerIcons import, data URI prefix, readPackageVersion version wire-up
    - npm test -- src/mcp/server.test.ts -x exits 0 (existing tests pass; new cases are it.fails)
    - No edits to src/mcp/server.ts or server-icons.ts
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/mcp/server.test.ts -x</automated>
  </verify>
  <done>server.test.ts RED scaffolds committed; husky green via it.fails</done>
</task>

<task type="auto" tdd="true">
  <name>RED: server-icons.test.ts buildMcpServerIcons shape scaffolds</name>
  <files>src/mcp/server-icons.test.ts</files>
  <read_first>
    - .planning/phases/27-branding-docs-stale-fix/27-UI-SPEC.md (MCP Icon Contract table)
    - .planning/phases/27-branding-docs-stale-fix/27-PATTERNS.md (server-icons.test.ts suggested assertions)
    - src/mcp/tools/meta.test.ts (test file structure)
  </read_first>
  <behavior>
    - buildMcpServerIcons returns array length ≥ 3
    - icons[0].src starts with data:image/png;base64,
    - icons[0].sizes equals ['192x192']
    - At least one entry src includes jsdelivr.net and favicon-32.png
    - At least one entry src includes jsdelivr.net and mcp-icon-192.png
    - Every entry has mimeType image/png
  </behavior>
  <action>
    Create src/mcp/server-icons.test.ts with describe buildMcpServerIcons and it.fails cases importing from ./server-icons.js (module will not exist until Plan 27-01 — import inside it.fails or use dynamic import with expect rejection). Mirror meta.test.ts vitest style. Assert D-02 ordering: index 0 is data URI. Assert D-01 dual strategy: both data and CDN entries present.
  </action>
  <acceptance_criteria>
    - server-icons.test.ts exists with ≥4 it.fails cases covering length, data URI first, both CDN URLs, mimeType
    - npm test -- src/mcp/server-icons.test.ts -x exits 0
    - src/mcp/server-icons.ts does not exist yet
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/mcp/server-icons.test.ts -x</automated>
  </verify>
  <done>server-icons.test.ts RED scaffolds committed</done>
</task>

<task type="auto" tdd="true">
  <name>RED: DOC-01 PROJECT opener stale-string gate</name>
  <files>tests/integration/doc-version-parity.test.ts</files>
  <read_first>
    - .planning/PROJECT.md (line 5 stale opener vs line 15 table)
    - .planning/phases/27-branding-docs-stale-fix/27-UI-SPEC.md (Copywriting Contract banned phrases)
    - tests/integration/docs-parity.test.ts (integration test pattern)
  </read_first>
  <behavior>
    - PROJECT.md opener does not contain pending Version Packages phrasing
    - PROJECT.md opener references 1.0.1 shipped state aligned with table row
    - Test scope excludes CHANGELOG.md and .planning/milestones per D-09
  </behavior>
  <action>
    Create tests/integration/doc-version-parity.test.ts with it.fails that reads .planning/PROJECT.md and expects opener line NOT to match /pending Version Packages/i and TO contain 1.0.1. Restrict scan to PROJECT.md only — do not assert on CHANGELOG or milestone archives (D-09). Use readFileSync from node:fs like docs-parity.test.ts.
  </action>
  <acceptance_criteria>
    - doc-version-parity.test.ts exists with it.fails PROJECT opener case
    - npm test -- tests/integration/doc-version-parity.test.ts -x exits 0
    - No PROJECT.md edits in this plan
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/integration/doc-version-parity.test.ts -x</automated>
  </verify>
  <done>DOC-01 RED gate scaffold committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → source files | Tests read server.ts source; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-00-01 | Tampering | server.test.ts | low | accept | Wave 0 scaffolds only; no runtime change |
</threat_model>

<verification>
npm test exits 0 with all new cases as it.fails. No production src/mcp/*.ts changes except test files.
</verification>

<success_criteria>
Wave 0 RED scaffolds lock BRND-01 icon contract and DOC-01 opener gate before Plan 27-01 implementation.
</success_criteria>

<output>
Create `.planning/phases/27-branding-docs-stale-fix/27-00-SUMMARY.md` when done
</output>
