---
phase: 30-deploy-guard
plan: 03
type: execute
wave: 3
depends_on:
  - "30-01"
  - "30-02"
files_modified:
  - src/mcp/capabilities.ts
  - src/mcp/tools/system.test.ts
  - docs/coverage-map.yaml
  - docs/COVERAGE.md
  - README.md
  - README.de.md
autonomous: true
requirements:
  - GUARD-01
  - GUARD-02
  - GUARD-03
user_setup: []

must_haves:
  truths:
    - "system.version exposes deployment_preflight and deployment_rollback capability keys."
    - "docs/coverage-map.yaml records deployment.preflight and deployment.rollback as MCP composites — no fake rollback REST."
    - "README EN/DE document preflight factors, advisory semantics, rollback confirm gate, and composite pin+deploy."
    - "No docs claim dedicated Coolify rollback API or external DNS probes."
  artifacts:
    - path: src/mcp/capabilities.ts
      provides: "deployment_preflight and deployment_rollback"
      contains: "deployment_preflight"
    - path: docs/coverage-map.yaml
      provides: "coverage rows for deployment.preflight and deployment.rollback"
      contains: "deployment.preflight"
    - path: README.md
      provides: "English deploy guard docs"
      contains: "deployment.preflight"
---

<objective>
Publish Phase 30 discoverability: capabilities, coverage, README EN/DE.

Output: capability keys, regenerated COVERAGE.md, bilingual docs, final test sweep.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-deploy-guard/30-RESEARCH.md
@.planning/phases/30-deploy-guard/30-01-SUMMARY.md
@.planning/phases/30-deploy-guard/30-02-SUMMARY.md
@src/mcp/capabilities.ts
@docs/coverage-map.yaml
@README.md
@README.de.md
</context>

## Artifacts this phase produces

- capabilities.ts — deployment_preflight, deployment_rollback
- coverage-map.yaml + COVERAGE.md
- README EN/DE deploy guard sections

<tasks>

<task type="auto">
  <name>Task 1: Capabilities and system.version tests</name>
  <files>src/mcp/capabilities.ts, src/mcp/tools/system.test.ts</files>
  <read_first>
    - src/mcp/capabilities.ts (manifest_audit pattern)
    - src/mcp/tools/system.test.ts (Wave 0 deployment_preflight it.fails)
  </read_first>
  <action>
    Add deployment_preflight and deployment_rollback to COOLIFY_412_CAPABILITIES with supported:true, coolify_min_version 4.1.2, MCP composite notes. Flip Wave 0 system capability it.fails to it().
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/system.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - capabilities.ts contains deployment_preflight and deployment_rollback
    - system tests GREEN for both keys
  </acceptance_criteria>
  <done>Capabilities published.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage map, COVERAGE.md, README EN/DE</name>
  <files>docs/coverage-map.yaml, docs/COVERAGE.md, README.md, README.de.md</files>
  <read_first>
    - docs/coverage-map.yaml (manifest.audit row pattern)
    - README.md Phase 29 section
  </read_first>
  <action>
    Add coverage rows for deployment.preflight and deployment.rollback mapping to existing OpenAPI paths. Run npm run openapi:coverage. Add README EN+DE sections: four preflight factors, risk_score advisory, deployment.rollback confirm:true, git pin+deploy composite, no live DNS probes. Update action counts if README maintains literals.
  </action>
  <verify>
    <automated>npm run openapi:coverage && npx vitest run tests/integration/docs-parity.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - coverage-map.yaml contains deployment.preflight and deployment.rollback
    - README.md and README.de.md mention both actions
  </acceptance_criteria>
  <done>Docs aligned with implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Phase verification test sweep</name>
  <files>src/mcp/tools/deployment.test.ts</files>
  <read_first>
    - .planning/phases/30-deploy-guard/30-VALIDATION.md
  </read_first>
  <action>
    Run deployment.test.ts, deploy-preflight.test.ts, system.test.ts, client.test.ts targeted suites. Set 30-VALIDATION.md wave_0_complete true in SUMMARY note.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts src/utils/deploy-preflight.test.ts src/mcp/tools/system.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Zero it.fails for preflight or rollback
    - Full targeted command exits 0
  </acceptance_criteria>
  <done>Ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs | Must not claim nonexistent APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-03-01 | Tampering | false endpoint docs | medium | mitigate | Coverage rows map to real paths only |
| T-30-03-SC | Tampering | npm installs | low | accept | Existing devDeps only |
</threat_model>

<verification>
npx vitest run src/mcp/tools/deployment.test.ts src/utils/deploy-preflight.test.ts src/mcp/tools/system.test.ts -x && npm run openapi:coverage
</verification>

<success_criteria>
All GUARD requirements discoverable; tests green.
</success_criteria>

<output>
Create `.planning/phases/30-deploy-guard/30-03-SUMMARY.md` when done
</output>
