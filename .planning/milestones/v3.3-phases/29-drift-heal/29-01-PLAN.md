---
phase: 29-drift-heal
plan: 01
type: execute
wave: 1
depends_on:
  - "29-00"
files_modified:
  - src/utils/manifest.ts
  - src/utils/manifest-audit.ts
  - src/mcp/tools/manifest.ts
  - src/mcp/tools/manifest.test.ts
autonomous: false
requirements:
  - DRIFT-01
  - DRIFT-03
user_setup: []

must_haves:
  truths:
    - "Agent can call manifest.audit on an existing manifest and receive severity-tagged findings with FollowUpHint remediation objects (DRIFT-01, DRIFT-03, D-01, D-05)."
    - "manifest.diff remains the raw non-destructive structural report and is not redefined as audit (D-03)."
    - "manifest.audit compares resource presence by UUID, type, name, domains, and project/environment nesting using manifest-stored fields (D-04)."
    - "manifest.audit is advisory-only and never mutates local manifest or live Coolify state (D-06, D-16)."
    - "Missing local manifest returns structured error with manifest.sync/upsert recovery hints and no mid-call auto-sync (D-07)."
    - "Audit without usable credentials or requested instance returns COOLIFY_NO_INSTANCE with recovery hints, matching manifest.diff credential behavior (D-07, D-12, D-13)."
    - "Audit returns soft partials when one live fetch fails while keeping sibling findings (D-06, D-13)."
  artifacts:
    - path: src/utils/manifest.ts
      provides: "ManifestManager.exists() guard for missing-manifest detection"
      contains: "exists"
    - path: src/utils/manifest-audit.ts
      provides: "ManifestAuditFinding, buildManifestAuditFindings, rollupAuditSeverity"
      exports: ["buildManifestAuditFindings", "rollupAuditSeverity"]
    - path: src/mcp/tools/manifest.ts
      provides: "manifest.audit action, shared live snapshot path, advisory response envelope"
      contains: "audit"
    - path: src/mcp/tools/manifest.test.ts
      provides: "GREEN audit tests from Wave 0"
      contains: "manifest.audit"
  key_links:
    - from: "manifest.audit handler"
      to: "ManifestManager.exists/load"
      via: "missing file guard before load"
    - from: "manifest.audit handler"
      to: "fetchLiveManifestSnapshot"
      via: "strict diff/sync fetch remains separate from audit softPartial option"
    - from: "buildManifestAuditFindings"
      to: "FollowUpHint"
      via: "diagnose-hints remediation shape"
---

<objective>
Tracer vertical slice: production-quality `manifest.audit` end-to-end on the existing manifest tool.

Purpose: Prove the phase architecture through one real agent-callable path before expansion: schema/catalog -> handler -> local manifest guard -> live snapshot -> findings + hints -> read-only response.

Output: `manifest.audit`, `ManifestManager.exists()`, manifest audit helper, Wave 0 manifest tests GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-drift-heal/29-CONTEXT.md
@.planning/phases/29-drift-heal/29-RESEARCH.md
@.planning/phases/29-drift-heal/29-PATTERNS.md
@.planning/phases/29-drift-heal/29-VALIDATION.md
@.planning/phases/29-drift-heal/29-00-SUMMARY.md
@.cursor/skills/spike-findings-awesome-coolify/SKILL.md
@src/mcp/tools/manifest.ts
@src/mcp/tools/manifest.test.ts
@src/utils/manifest.ts
@src/utils/diagnose-hints.ts
</context>

## Artifacts this phase produces

- `src/utils/manifest.ts` — `ManifestManager.exists()` used before audit load.
- `src/utils/manifest-audit.ts` — reusable audit index, drift finding builder, severity rollup.
- `src/mcp/tools/manifest.ts` — `audit` action on existing `manifest` surface, preserving `diff`.
- `src/mcp/tools/manifest.test.ts` — GREEN DRIFT-01/DRIFT-03 audit coverage.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm D-06 advisory-only audit gate</name>
  <files>.planning/phases/29-drift-heal/29-CONTEXT.md</files>
  <read_first>
    - .planning/phases/29-drift-heal/29-CONTEXT.md (D-06, D-16)
    - .planning/phases/29-drift-heal/29-RESEARCH.md (Security Domain: audit-triggered auto-mutation)
  </read_first>
  <decision>Confirm the locked D-06 one-way safety contract before implementing manifest.audit.</decision>
  <context>D-06 publishes audit as read-only/advisory. Undoing this after agents rely on it would break the safety contract and downstream trust model.</context>
  <options>
    <option id="approve-advisory-only">
      <name>Approve advisory-only audit</name>
      <pros>Matches D-06, D-16, threat model T-29-04, and no-auto-heal scope.</pros>
      <cons>Audit returns hints only; caller must invoke existing mutation actions separately.</cons>
    </option>
    <option id="stop-and-replan">
      <name>Stop and replan</name>
      <pros>Use only if D-06 was recorded incorrectly.</pros>
      <cons>Blocks Phase 29 because current context explicitly locks advisory-only behavior.</cons>
    </option>
  </options>
  <action>Record checkpoint selection before touching production files. Continue only with `approve-advisory-only`; do not introduce any audit auto-sync, auto-upsert, live mutation, or manifest write behavior in this plan.</action>
  <verify>
    <automated>rg -n "D-06.*Audit is.*read-only|D-16.*auto-heal" .planning/phases/29-drift-heal/29-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - Executor records `approve-advisory-only` or halts for replanning.
    - No production file is edited before checkpoint resolution.
  </acceptance_criteria>
  <resume-signal>Select: approve-advisory-only or stop-and-replan</resume-signal>
  <done>D-06 one-way safety gate resolved for implementation.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end manifest.audit — one drift path with remediation hint</name>
  <reversibility rating="one-way">D-06 advisory-only audit is a published safety contract; changing it later would break agent expectations around non-mutating audit.</reversibility>
  <files>src/utils/manifest.ts, src/utils/manifest-audit.ts, src/mcp/tools/manifest.ts, src/mcp/tools/manifest.test.ts</files>
  <read_first>
    - .planning/phases/29-drift-heal/29-CONTEXT.md (D-01, D-03, D-04, D-05, D-06, D-07, D-12, D-13, D-14, D-16)
    - .planning/phases/29-drift-heal/29-RESEARCH.md (Patterns 1, 2, 4; Don't Hand-Roll)
    - .planning/phases/29-drift-heal/29-PATTERNS.md (manifest.ts, manifest-audit.ts, manifest.test.ts analogs)
    - src/mcp/tools/manifest.ts (manifestActionsCatalog, manifestActionSchema, diff handler, fetchRemoteManifest)
    - src/utils/manifest.ts (ManifestManager.load behavior)
    - src/utils/diagnose-hints.ts (FollowUpHint)
    - src/mcp/tools/intelligence.ts (ScorecardFinding severity + hint pattern)
  </read_first>
  <behavior>
    - Agent calls `{ action: 'audit', instance?: string }` on existing `manifest` tool (D-01, D-12).
    - Audit resolves credentials like `manifest.diff`; no usable env/instance credentials returns `COOLIFY_NO_INSTANCE` with recovery hints before any manifest load or live fetch (D-07, D-12, D-13).
    - Missing local manifest is detected via `ManifestManager.exists()` before `load()` and returns structured error with recovery hints (D-07).
    - One local-orphan drift path returns `findings[]` with severity `high`, kind `local_orphan`, uuid/type, issue, local/live details, and FollowUpHint pointing to `manifest.sync` dry_run or `manifest.upsert` (DRIFT-01, DRIFT-03, D-05).
    - Response includes rollup severity and summary counts; raw diff support may be included only as supporting detail, not sole payload (D-03, D-05).
    - Existing `manifest.diff` tests stay green and its response remains `{ diff, destructive:false }` style raw report (D-03).
  </behavior>
  <action>
    Add `ManifestManager.exists()` in `src/utils/manifest.ts` using the same manifest file path as `load()`. Create `src/utils/manifest-audit.ts` with `ManifestAuditFinding`, an index over project/environment resources, domain set comparison, `buildManifestAuditFindings(local, live, options?)`, and `rollupAuditSeverity`. Add `audit` to `manifestActionsCatalog`, `manifestActionSchema` actions, and per-action allowed keys with `instance` only unless existing shared read params are already present for manifest actions. In `manifest.ts`, refactor live fetching so `diff` keeps strict behavior and audit can call a shared snapshot path. Implement audit handler after D-06 gate: resolve credentials exactly like `diff` before loading the local manifest, return wrapped `COOLIFY_NO_INSTANCE` for missing env/instance credentials, then check `ManifestManager.exists()` before `load()`, fetch live snapshot, build findings, build supporting reconciliation report without mutating, and return via `buildReadResponse`. Flip the Wave 0 audit happy-path, missing-manifest, and no-creds `it.fails` cases to passing `it()`. Do not create any new MCP tool, do not touch `intelligence`, and do not call sync/upsert/write helpers from audit.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/manifest.test.ts -t "manifest.audit|manifest.diff|missing manifest|COOLIFY_NO_INSTANCE" -x</automated>
  </verify>
  <acceptance_criteria>
    - `ManifestManager.exists()` exists and audit calls it before `ManifestManager.load()`.
    - `manifest.audit` action is accepted by the manifest schema/catalog on the existing tool.
    - At least one audit finding has severity plus FollowUpHint-shaped remediation object.
    - Audit without usable credentials returns `COOLIFY_NO_INSTANCE` with recovery hints and performs no manifest load or live fetch.
    - Missing manifest returns structured error + recovery hints instead of comparing against `emptyManifest()`.
    - `manifest.diff` still passes existing raw non-destructive tests.
  </acceptance_criteria>
  <done>Agent can run `manifest.audit` end-to-end for one real drift path; tracer architecture proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete audit axes, soft partials, and read-only coverage</name>
  <files>src/utils/manifest-audit.ts, src/mcp/tools/manifest.ts, src/mcp/tools/manifest.test.ts</files>
  <read_first>
    - .planning/phases/29-drift-heal/29-CONTEXT.md (D-04, D-05, D-06, D-13, D-16)
    - .planning/phases/29-drift-heal/29-RESEARCH.md (Pattern 2 finding kinds, Pitfalls 2, 4, 5)
    - .planning/phases/29-drift-heal/29-PATTERNS.md (soft partials and domain compare)
    - src/mcp/tools/manifest.test.ts (Wave 0 audit axes and partial fetch cases)
    - src/mcp/tools/manifest.ts (domainsFromApiResource, buildReconciliationReport)
  </read_first>
  <behavior>
    - Audit emits critical findings for type_mismatch and nesting_mismatch (D-04, D-05).
    - Audit emits high finding for domain_drift using sorted domain set comparison (D-04).
    - Audit emits info findings for remote_only and name_drift (D-04, D-05).
    - Audit soft partial mode records failed live fetches in `partial` metadata while preserving findings from fulfilled sources (D-06, D-13).
    - Audit read-only tests prove no local write and no live mutation calls occur (D-06, D-16).
  </behavior>
  <action>
    Expand `buildManifestAuditFindings` to cover every D-04 axis from the research table: local_orphan, remote_only, nesting_mismatch, type_mismatch, name_drift, and domain_drift. Keep comparisons bounded to fields present in the manifest schema. Add audit-only soft partial handling around live manifest source fetches with allSettled; keep strict behavior for `diff` and `sync`. Include `partial` metadata with source key, code, and message for failed live reads. Flip remaining Wave 0 audit `it.fails` cases to `it()`. Preserve the advisory contract from Task 2: hints name follow-up tool/action, audit itself does not write.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/manifest.test.ts -t "manifest.audit" -x</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass for type, name, domain, nesting, local-only, and remote-only drift findings.
    - Soft partial audit test passes when one mocked live fetch rejects.
    - Read-only audit test passes and no local manifest write helper is invoked.
    - No `manifest.diff` test is rewritten to hide a regression.
  </acceptance_criteria>
  <done>DRIFT-01 and DRIFT-03 manifest audit implementation complete with actionable hints and read-only guarantees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → manifest.audit | Untrusted action args choose optional instance |
| Workspace `.coolify/manifest.json` → audit | Local cache may be missing, stale, or manually edited |
| Coolify API → live snapshot | Live inventory may be partially unavailable |
| Audit hints → later tools | Hints influence later mutations but must not execute them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Information Disclosure | envs:promote preview | high | mitigate | Not implemented in this plan; 29-02 must use `maskEnvRecord` and existing reveal policy |
| T-29-02 | Tampering | envs:promote apply | high | mitigate | Not implemented in this plan; 29-02 owns `dry_run:true`, `confirm:true`, `keep_remote` |
| T-29-03 | Spoofing | manifest.audit instance routing | high | mitigate | Reuse `InstanceManager.resolveCredentials` and `COOLIFY_NO_INSTANCE`; include optional instance in hints only from routed context |
| T-29-04 | Elevation of Privilege | manifest.audit auto-mutation | high | mitigate | D-06 checkpoint plus tests: audit never calls sync/upsert/local write/live mutation; returns hints only |
| T-29-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
`npx vitest run src/mcp/tools/manifest.test.ts -t "manifest.audit|manifest.diff" -x` green; audit action exists on manifest tool; diff remains separate; audit read-only behavior is under test.
</verification>

<success_criteria>
- DRIFT-01: Agent can audit local manifest vs live Coolify state.
- DRIFT-03: Audit findings include concrete remediation hints.
- D-01/D-03/D-04/D-05/D-06/D-07/D-12/D-13/D-14/D-16 satisfied for manifest path.
</success_criteria>

<output>
Create `.planning/phases/29-drift-heal/29-01-SUMMARY.md` when done.
</output>
