---
phase: 15-multi-instance-registry-routing
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/utils/instance-registry.test.ts
  - src/mcp/tools/instance.test.ts
  - src/config/env.test.ts
  - src/mcp/integration.test.ts
autonomous: true
requirements: [CTX-04, CTX-05, CTX-08, CTX-09]
must_haves:
  truths:
    - "instance-registry.test.ts exists and fails at import (InstanceManager not yet defined) — RED scaffold for CTX-04/05/08/09"
    - "instance.test.ts exists and fails at import (handleInstanceAction not yet defined) — RED scaffold for instance tool CRUD"
    - "env.test.ts gains soft-start cases asserting loadEnv succeeds with no COOLIFY_* env (currently fails because env.ts still hard-requires)"
    - "integration.test.ts exists with RED stubs for CTX-06 routing (application.get with instance param) — fails at import or via it.fails until Plans 15-03/15-04 wire routing"
    - "All scaffold tests use it.fails (or fail at import) so husky pre-commit stays green — mirrors Phase 10/11/12 Wave 0 pattern"
  artifacts:
    - src/utils/instance-registry.test.ts
    - src/mcp/tools/instance.test.ts
    - src/config/env.test.ts
    - src/mcp/integration.test.ts
  key_links:
    - "instance-registry.test.ts → src/utils/instance-registry.ts (import target, not yet present)"
    - "instance.test.ts → src/mcp/tools/instance.ts (import target, not yet present)"
  prohibitions:
    - "No GREEN implementations in Wave 0 — only failing scaffolds"
    - "No real filesystem writes in tests — mock or use os.tmpdir() with cleanup"
---

<objective>
Wave 0 RED scaffold plan for Phase 15. Create failing test files that pin the contract for InstanceManager (CTX-04/05/08/09), the `instance` tool CRUD actions, and softened `loadEnv` soft-start. Tests fail at import or via `it.fails` so husky pre-commit stays green; Plans 15-01..15-04 flip them GREEN without rewriting tests.

Purpose: Lock the behavioral contract before implementation so Plans 15-01..15-04 execute TDD RED→GREEN per task without re-authoring tests.
Output: Three test files with failing scaffolds covering all Phase 15 requirements.
</objective>

<artifacts_produced>
Test files (RED scaffolds, no production code):
- `src/utils/instance-registry.test.ts` — InstanceManager contract (CTX-04/05/08/09)
- `src/mcp/tools/instance.test.ts` — instance tool CRUD actions contract
- `src/config/env.test.ts` — soft-start + partial-env cases (appended)
- `src/mcp/integration.test.ts` — CTX-06 routing RED stubs (application.get with instance param)
</artifacts_produced>


<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-multi-instance-registry-routing/15-CONTEXT.md
@.planning/phases/15-multi-instance-registry-routing/15-RESEARCH.md
@.planning/phases/15-multi-instance-registry-routing/15-PATTERNS.md
@.planning/phases/15-multi-instance-registry-routing/15-VALIDATION.md
@src/utils/errors.ts
@src/config/env.ts
@src/mcp/tools/private_key.test.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED scaffold instance-registry.test.ts (CTX-04/05/08/09)</name>
  <files>src/utils/instance-registry.test.ts</files>
  <read_first>
    - src/utils/env-parser.test.ts (Zod schema test pattern)
    - src/utils/errors.ts (CoolifyApiError envelope shape for COOLIFY_INSTANCE_NOT_FOUND assertions)
    - .planning/phases/15-multi-instance-registry-routing/15-RESEARCH.md (Pattern 1 & 2 — InstanceManager API surface, atomic write, perms)
    - .planning/phases/15-multi-instance-registry-routing/15-CONTEXT.md (D-06..D-20)
  </read_first>
  <behavior>
    - loadRegistry returns { instances: [] } when instances.json absent (CTX-04 empty)
    - add persists instance to registry; first add into empty registry auto-sets default (D-15)
    - add with duplicate name fails with structured error — no merge/overwrite (CTX-04 adjacency collide)
    - list returns registry entries in insertion order (CTX-04 ordering stable)
    - set-default on already-default name is idempotent no-op (CTX-04 idempotency)
    - set-default on unknown name throws COOLIFY_INSTANCE_NOT_FOUND (D-16)
    - delete with confirm:true removes entry; delete on default or last instance requires force (D-04)
    - resolveCredentials: explicit instance param wins over env (D-10/D-11)
    - resolveCredentials: env both-set overrides registry default (CTX-05 adjacency)
    - resolveCredentials: partial env (only URL or only TOKEN) throws COOLIFY_PARTIAL_ENV (D-13)
    - resolveCredentials: no param, no env, no default throws COOLIFY_NO_INSTANCE (CTX-05 empty / D-18/D-20)
    - saveRegistry creates dir with 0o700 and file with 0o600 via chmodSync override (CTX-08)
    - saveRegistry uses temp file + rename; concurrent saves serialized by in-memory lock (CTX-09 concurrency)
    - instance.list/get redact token as *** unless reveal:true (CTX-08)
  </behavior>
  <action>
    Create src/utils/instance-registry.test.ts. Import from ./instance-registry.js: InstanceManager, instanceSchema, Registry, Instance, resolveCredentials (or InstanceManager.resolveCredentials — pick the static method shape from RESEARCH Pattern 1 and stay consistent). Tests must fail at import because the module does not exist yet — that is the RED state. For tests that can run without the import (none, since all assertions need InstanceManager), use it.fails. Use a per-test tmp registry path override (inject filePath/dirPath via a test-only constructor or env var like COOLIFY_MCP_TEST_REGISTRY_DIR — leave the exact injection point as a TODO comment so Plan 15-01 can wire it). Mock fs where needed; prefer real fs writes to os.tmpdir() with afterEach cleanup so atomic-rename and chmod behavior is exercised on POSIX. Assert file mode via fs.statSync(...).mode & 0o777. Cover every behavior bullet above with one it block; group with describe('InstanceManager') and describe('resolveCredentials'). Do not implement InstanceManager — only the test file.
  </action>
  <verify>
    <automated>npx vitest run src/utils/instance-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] src/utils/instance-registry.test.ts exists
    - [ ] ≥14 it blocks covering CTX-04/05/08/09 behaviors
    - [ ] Suite fails at import (RED) — InstanceManager module not yet present
    - [ ] `npm run build` (tsup) stays green (test files excluded from build graph)
    - [ ] Tests use os.tmpdir() with afterEach cleanup; no real ~/.coolify-mcp writes
  </acceptance_criteria>
  <done>instance-registry.test.ts exists with ≥14 it blocks covering CTX-04/05/08/09; suite fails at import (RED); `npm run build` (tsup) stays green because test files are not in the build graph.</done>
</task>

<task type="tdd">
  <name>Task 2: RED scaffold instance.test.ts + env.test.ts soft-start cases + integration.test.ts routing stubs</name>
  <files>src/mcp/tools/instance.test.ts, src/config/env.test.ts, src/mcp/integration.test.ts</files>
  <read_first>
    - src/mcp/tools/private_key.test.ts (action-schema handler test pattern)
    - src/config/env.ts (current hard-required schema — soften target)
    - src/mcp/tools/private_key.ts (discriminatedUnion action schema + validateDeleteConfirm pattern)
    - .planning/phases/15-multi-instance-registry-routing/15-CONTEXT.md (D-01..D-05 tool surface)
  </read_first>
  <behavior>
    instance.test.ts:
    - handleInstanceAction({action:'list'}) returns registry entries with tokens redacted as ***
    - handleInstanceAction({action:'list', reveal:true}) returns tokens in plaintext
    - handleInstanceAction({action:'list'}) includes _meta.envOverride:true when COOLIFY_URL+COOLIFY_TOKEN both set in env (D-17)
    - handleInstanceAction({action:'add', name,url,token,type,verifySsl}) persists and returns the new entry
    - handleInstanceAction({action:'add'}) with invalid name (regex ^[a-z][a-z0-9_-]{1,31}$) throws COOLIFY_VALIDATION_ERROR (D-08)
    - handleInstanceAction({action:'get', name}) returns single entry; redacts token unless reveal:true
    - handleInstanceAction({action:'update', name, ...fields}) mutates and returns updated entry
    - handleInstanceAction({action:'delete', name, confirm:true}) removes; without confirm throws COOLIFY_CONFIRM_REQUIRED (D-04)
    - handleInstanceAction({action:'delete', name, confirm:true}) on default or last instance without force throws structured error (D-04)
    - handleInstanceAction({action:'set-default', name}) updates registry.default
    - handleInstanceAction({action:'import-env'}) imports COOLIFY_URL+COOLIFY_TOKEN from env as a new entry (opt-in, D-05)
    - handleInstanceAction never accepts an `instance` routing param (D-03) — schema rejects it
    env.test.ts (new cases, appended):
    - loadEnv({}) with no COOLIFY_* keys returns EnvConfig with COOLIFY_URL undefined, COOLIFY_TOKEN undefined, COOLIFY_VERIFY_SSL true, COOLIFY_MCP_LOG 'info' (soft-start, D-18)
    - loadEnv({COOLIFY_URL:'x'}) with only URL throws COOLIFY_PARTIAL_ENV (D-13) — currently passes Zod because TOKEN is hard-required; this case goes RED until env.ts is softened in Plan 15-02
    integration.test.ts (new file, RED stubs for CTX-06):
    - application.get with instance:'prod' routes to prod creds (mock createCoolifyClient captures creds) — RED until Plan 15-03 wires routing
    - application.get with instance:'unknown' returns COOLIFY_INSTANCE_NOT_FOUND envelope — RED until Plan 15-03
    - application.get with no instance and no env and no default returns COOLIFY_NO_INSTANCE — RED until Plan 15-03
    - application.get with partial env returns COOLIFY_PARTIAL_ENV — RED until Plan 15-03
  </behavior>
  <action>
    Create src/mcp/tools/instance.test.ts importing handleInstanceAction, instanceActionSchema from ./instance.js (RED at import — module does not exist yet). Use it.fails for cases that depend on the not-yet-implemented handler, OR rely on import failure for the whole file (mirrors Phase 11/12 Wave 0). Inject a per-test InstanceManager path override (same mechanism chosen in Task 1) so handler tests do not touch the real ~/.coolify-mcp. Append new it blocks to src/config/env.test.ts for the soft-start and partial-env cases — these will fail until Plan 15-02 softens env.ts. Create src/mcp/integration.test.ts with RED stubs for CTX-06 routing: use it.fails or import-failure for application.get routing cases (named instance routes correctly, unknown → COOLIFY_INSTANCE_NOT_FOUND, no creds → COOLIFY_NO_INSTANCE, partial env → COOLIFY_PARTIAL_ENV). Mock or stub createCoolifyClient to capture which creds are passed; the tests fail because application.ts does not yet accept an instance param or call resolveCredentials (Plan 15-03 flips them GREEN). Do not implement handlers, soften env.ts, or wire routing in this plan — scaffolds only.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/instance.test.ts src/config/env.test.ts src/mcp/integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] src/mcp/tools/instance.test.ts exists with ≥12 it blocks covering all instance tool actions
    - [ ] src/config/env.test.ts has new soft-start + partial-env cases appended
    - [ ] src/mcp/integration.test.ts exists with ≥4 RED routing stubs for CTX-06
    - [ ] All three suites RED (failing at import or via it.fails)
    - [ ] husky pre-commit stays green (it.fails / import-failure prevents test-only RED commits from blocking)
  </acceptance_criteria>
  <done>instance.test.ts exists with ≥12 it blocks covering all instance tool actions; env.test.ts has new soft-start + partial-env cases; integration.test.ts has ≥4 RED routing stubs; all RED; husky pre-commit stays green because it.fails or import-failure prevents test-only RED commits from blocking.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → tmpdir | Scaffold tests write registries to os.tmpdir(); cleanup mandatory to avoid leaking tmp files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Status | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------|-------------|-----------------|
| T-15-00-SC | Tampering | npm/dev installs (vitest) | high | mitigated | accept | No new packages in Wave 0; vitest already pinned in package.json |
| T-15-00-01 | Information Disclosure | test tmp registries containing fake tokens | low | mitigated | mitigate | afterEach cleanup unlinks tmp registry dir; tests use throwaway tokens only |
</threat_model>

<verification>
- `npx vitest run src/utils/instance-registry.test.ts src/mcp/tools/instance.test.ts src/config/env.test.ts src/mcp/integration.test.ts` — all RED (failing at import or via it.fails)
- `npm run build` (tsup) stays green — test files excluded from build graph
- `git status` shows exactly 4 new/modified test files
</verification>

<success_criteria>
- 4 test files created/updated, all RED
- Every CTX-04/05/08/09 behavior has at least one it block
- instance tool actions list/get/add/update/delete/set-default/import-env all covered
- Soft-start and partial-env env.test.ts cases added
- CTX-06 routing RED stubs added to integration.test.ts (flipped GREEN by Plans 15-03/15-04)
- No production code modified in Wave 0
</success_criteria>

<output>
Create `.planning/phases/15-multi-instance-registry-routing/15-00-SUMMARY.md` when done
</output>
