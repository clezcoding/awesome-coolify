---
phase: 08-keys-server-crud
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/tools/private_key.test.ts
  - src/mcp/tools/server.test.ts
autonomous: true
requirements:
  - KEY-01
  - KEY-02
  - KEY-03
  - KEY-04
  - KEY-05
  - SRV-01
  - SRV-02
  - SRV-03
  - SRV-04
  - SRV-05
must_haves:
  truths:
    - private_key.test.ts exists and fails RED for KEY-01..KEY-05 behaviors
    - server.test.ts exists and fails RED for SRV-01..SRV-05 behaviors
    - Both test files mock src/api/client.js (fetchPrivateKeys, fetchPrivateKey, createPrivateKey, updatePrivateKey, deletePrivateKey, fetchServers, fetchServer, createServer, updateServer, deleteServer, validateServer, triggerServerValidate, pollServerUntilReachable, fetchServerResources)
    - No test touches the real Coolify API (all ofetch mocked)
  artifacts:
    - src/mcp/tools/private_key.test.ts
    - src/mcp/tools/server.test.ts
  key_links:
    - private_key.test.ts -> src/mcp/tools/private_key.ts (handler not yet implemented — RED by design)
    - server.test.ts -> src/mcp/tools/server.ts (handler not yet implemented — RED by design)
---

<objective>
Wave 0 RED scaffolds for the two new Phase 8 domain tools.

Purpose: Per VALIDATION.md `Wave 0 Requirements`, both test files MUST exist before implementation plans (08-02, 08-03) turn them GREEN. Tests assert every locked decision (D-01..D-16) and every phase requirement (KEY-01..05, SRV-01..05) so later plans only need to make them pass.

Output: Two failing vitest files that import handlers which do not exist yet. Running them MUST fail with import errors / assertions RED — never green, never skipped.
</objective>

<execution_context>
@/Users/puzzless/gstack/.cursor/gsd-core/workflows/execute-plan.md
@/Users/puzzless/gstack/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-keys-server-crud/08-CONTEXT.md
@.planning/phases/08-keys-server-crud/08-RESEARCH.md
@.planning/phases/08-keys-server-crud/08-VALIDATION.md
@.cursor/skills/spike-findings-awesome-coolify/SKILL.md
@src/mcp/tools/database.test.ts
@src/mcp/tools/emergency.test.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Scaffold private_key.test.ts RED (KEY-01..KEY-05)</name>
  <files>src/mcp/tools/private_key.test.ts</files>
  <read_first>
    - src/mcp/tools/database.test.ts (mock + assertion patterns)
    - src/mcp/tools/emergency.test.ts (confirm-gate / COOLIFY_CONFIRM_REQUIRED assertions)
    - .planning/phases/08-keys-server-crud/08-CONTEXT.md (D-01..D-16)
    - .planning/phases/08-keys-server-crud/08-RESEARCH.md (Common Pitfall 2: key delete 422 vs 409)
  </read_first>
  <behavior>
    - KEY-01 list: handlePrivateKeyAction({action:'list'}) returns summary projection with fields uuid, name, fingerprint, description (D-04); no `private_key` PEM field in output
    - KEY-01 list: passing `reveal: true` to list throws / returns COOLIFY_422 (D-11)
    - KEY-02 get: handlePrivateKeyAction({action:'get', uuid}) returns metadata; full projection still omits PEM even with reveal:true (D-02, D-04)
    - KEY-03 create: accepts inline `private_key` PEM string → calls createPrivateKey → returns {uuid, name, fingerprint?} (D-01, D-03)
    - KEY-03 create: accepts `key_file` local path → reads file → calls createPrivateKey with PEM content (D-01)
    - KEY-03 create: passing BOTH `private_key` and `key_file` → COOLIFY_422 (D-01 XOR)
    - KEY-03 create: passing NEITHER → COOLIFY_422 (D-01 XOR)
    - KEY-03 create: response never contains PEM material (D-02)
    - KEY-04 update: handlePrivateKeyAction({action:'update', uuid, name?, description?}) calls updatePrivateKey; metadata-only path (researcher discretion A1)
    - KEY-05 delete: confirm:false → COOLIFY_CONFIRM_REQUIRED (D-14)
    - KEY-05 delete: confirm:true, no dependents → calls deletePrivateKey → {ok:true, uuid}
    - KEY-05 delete: confirm:true, key referenced by servers → COOLIFY_409 listing dependent server UUIDs (D-15, Pitfall 2)
    - D-13 delete_preview: handlePrivateKeyAction({action:'delete_preview', uuid}) returns {dependents:[...], would_delete:true} without calling deletePrivateKey
    - D-15 no `force` param in schema (Phase 8 scope)
  </behavior>
  <action>
    Create src/mcp/tools/private_key.test.ts mirroring database.test.ts structure:
    - vi.mock('../../api/client.js', () => ({ fetchPrivateKeys: vi.fn(), fetchPrivateKey: vi.fn(), createPrivateKey: vi.fn(), updatePrivateKey: vi.fn(), deletePrivateKey: vi.fn(), fetchServers: vi.fn() }))
    - Import handlePrivateKeyAction, privateKeyActionSchema, isPrivateKeyErrorResult from './private_key.js' (file does not exist yet → RED)
    - Define testEnv helper (same shape as database.test.ts testEnv)
    - Write `describe('private_key list')`, `describe('private_key get')`, `describe('private_key create')`, `describe('private_key update')`, `describe('private_key delete')`, `describe('private_key delete_preview')` blocks with one `it(...)` per behavior bullet above
    - Each test calls handlePrivateKeyAction with the listed args and asserts on the returned structuredContent / error envelope
    - For PEM/file XOR tests, assert zod parse throws OR handler returns COOLIFY_422 error result
    - For reveal-on-list test, assert error code 'COOLIFY_422' in structuredContent.error
    - For COOLIFY_409 deps test, mock fetchServers to return one server whose private_key_id matches the key's id; assert error code 'COOLIFY_409' and data.dependent_server_uuids contains the server uuid
    - Do NOT stub implementations — the handler import must fail, making every test RED
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/private_key.test.ts 2>&amp;1 | tee /tmp/p08-00-priv.log; test $? -ne 0 || (grep -c 'failed\|Error' /tmp/p08-00-priv.log)</automated>
  </verify>
  <done>
    - File src/mcp/tools/private_key.test.ts exists
    - `npx vitest run src/mcp/tools/private_key.test.ts` fails (RED) because ./private_key.js does not exist
    - At least 13 `it(...)` blocks present (one per behavior bullet)
    - No test is marked `.skip` or `.todo`
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Scaffold server.test.ts RED (SRV-01..SRV-05)</name>
  <files>src/mcp/tools/server.test.ts</files>
  <read_first>
    - src/mcp/tools/database.test.ts (mock + assertion patterns)
    - src/mcp/tools/emergency.test.ts (confirm-gate + poll-loop mock patterns)
    - src/utils/deploy-poll.test.ts (pollUntilTerminal timeout mock pattern)
    - .planning/phases/08-keys-server-crud/08-CONTEXT.md (D-05..D-08, D-12, D-16)
    - .planning/phases/08-keys-server-crud/08-RESEARCH.md (Common Pitfall 1: server GET lacks private_key_uuid; Open Question 1: 30s validate timeout)
  </read_first>
  <behavior>
    - SRV-01 create: handleServerAction({action:'create', name, ip, port, user, private_key_uuid}) calls createServer → returns {uuid, validation:{status:'valid', reachable:true}} (D-05)
    - SRV-01 create: validate:true default → auto-runs validate after create (D-06); assert validateServer called
    - SRV-01 create: validate:false → skips auto-validate; returns {uuid, validation:{status:'skipped'}}
    - SRV-01 create + D-07 soft-unreachable: validateServer resolves but server.settings.is_reachable === false → response {ok:true, uuid, validation:{reachable:false}} with COOLIFY_SSH_UNREACHABLE recovery hint; no deleteServer rollback called
    - SRV-01 create + D-05 timeout: validate poll exceeds 30s → response {ok:true, uuid, validation:{status:'pending'}} + retry hint; no rollback
    - SRV-02 update: handleServerAction({action:'update', uuid, name?, ip?, port?, user?, private_key_uuid?}) calls updateServer; response reflects updated fields
    - SRV-05 update: handleServerAction({action:'update', uuid, is_build_server:true}) calls updateServer with is_build_server; response includes is_build_server:true
    - SRV-03 delete: confirm:false → COOLIFY_CONFIRM_REQUIRED (D-14)
    - SRV-03 delete: confirm:true → calls deleteServer; delete_volumes defaults to false (D-16); assert deleteServer called with delete_volumes=false (or omitted)
    - SRV-03 delete: confirm:true, delete_volumes:true explicitly → deleteServer called with delete_volumes:true
    - SRV-04 validate: handleServerAction({action:'validate', uuid}) calls validateServer + polls → returns {uuid, validation:{reachable:true|false, status:'valid'|'pending'}}
    - SRV-04 validate uses same wait/timeout model as create (D-08)
    - D-12 get: handleServerAction({action:'get', uuid}) returns metadata {uuid, name, ip, port, user, private_key_uuid, is_build_server, reachable}; no validate side-effect (assert validateServer NOT called)
    - D-12 get + Pitfall 1: server GET returns private_key_id (integer); handler fetches private keys to resolve private_key_uuid; assert fetchPrivateKeys called and response.private_key_uuid === resolved uuid
    - D-13 delete_preview: handleServerAction({action:'delete_preview', uuid}) returns {child_resources:[...], would_delete:true} without calling deleteServer
    - D-16 delete_preview: child resources listed as warning; delete with confirm still allowed (no hard block)
    - pollServerUntilReachable mock: resolves with last-seen server object {settings:{is_reachable:true|false|undefined}} — used by create/validate tests for reachable/unreachable/pending branches
    - fetchServerResources mock: resolves with [] or [{uuid:'app-1', name:'app', type:'application'}] — used by delete_preview tests for child resource listing
  </behavior>
  <action>
    Create src/mcp/tools/server.test.ts mirroring database.test.ts + emergency.test.ts patterns:
    - vi.mock('../../api/client.js', () => ({ fetchServer: vi.fn(), fetchServers: vi.fn(), fetchPrivateKeys: vi.fn(), createServer: vi.fn(), updateServer: vi.fn(), deleteServer: vi.fn(), validateServer: vi.fn(), triggerServerValidate: vi.fn(), pollServerUntilReachable: vi.fn(), fetchServerResources: vi.fn() }))
    - pollServerUntilReachable default mock resolves {settings:{is_reachable:true}}; per-test overrides: reachable:false -> {settings:{is_reachable:false}}; timeout/pending -> {settings:{is_reachable:undefined}} (last-seen after timeout, no throw)
    - fetchServerResources default mock resolves []; delete_preview child-resources test overrides to [{uuid:'app-1', name:'app', type:'application'}]
    - Import handleServerAction, serverActionSchema, isServerErrorResult from './server.js' (file does not exist yet → RED)
    - Reuse testEnv shape from database.test.ts
    - Write `describe('server create')`, `describe('server update')`, `describe('server delete')`, `describe('server validate')`, `describe('server get')`, `describe('server delete_preview')` blocks with one `it(...)` per behavior bullet
    - For auto-validate tests, mock createServer to resolve {uuid:'srv-1'} and validateServer + pollServerUntilReachable to resolve; assert validateServer invoked (use vi.fn().mockResolvedValue) and response.validation.status
    - For soft-unreachable test, mock pollServerUntilReachable to resolve {settings:{is_reachable:false}}; assert response.structuredContent.ok === true AND validation.reachable === false AND recoveryHints mention SSH
    - For timeout test, mock pollServerUntilReachable to resolve {settings:{is_reachable:undefined}} (last-seen after 30s timeout, no throw); assert response.validation.status === 'pending'
    - For private_key_uuid resolution test, mock fetchServer returning {private_key_id:7} and fetchPrivateKeys returning [{id:7, uuid:'key-uuid-7'}]; assert response.data.private_key_uuid === 'key-uuid-7'
    - For delete_volumes default false test, assert deleteServer mock called with delete_volumes argument === false (or query param absent)
    - Do NOT stub implementations — handler import must fail, making every test RED
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/server.test.ts 2>&amp;1 | tee /tmp/p08-00-srv.log; test $? -ne 0 || (grep -c 'failed\|Error' /tmp/p08-00-srv.log)</automated>
  </verify>
  <done>
    - File src/mcp/tools/server.test.ts exists
    - `npx vitest run src/mcp/tools/server.test.ts` fails (RED) because ./server.js does not exist
    - At least 15 `it(...)` blocks present (one per behavior bullet)
    - No test is marked `.skip` or `.todo`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → mocked ofetch | Mocks replace network layer; no real credentials cross boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-00-01 | Information Disclosure | test fixtures containing PEM strings | medium | mitigate | Use clearly-fake PEM strings (e.g. '-----BEGIN FAKE KEY-----\nFAKE\n-----END FAKE KEY-----') in test fixtures; never embed real PEM material. Tests run in CI and may be logged. |
| T-08-00-02 | Tampering | vi.mock('../../api/client.js') | low | accept | Mocks are test-only; no production code path is altered. Standard vitest pattern. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/private_key.test.ts src/mcp/tools/server.test.ts` exits non-zero (RED) — both files fail because handler modules do not exist yet
- `grep -c '^  it(' src/mcp/tools/private_key.test.ts` >= 13
- `grep -c '^  it(' src/mcp/tools/server.test.ts` >= 15
- `grep -c '.skip\|.todo' src/mcp/tools/private_key.test.ts src/mcp/tools/server.test.ts` == 0
</verification>

<success_criteria>
- Both RED scaffold files exist and fail predictably
- Every KEY-* and SRV-* requirement has at least one corresponding `it(...)` block
- Every locked decision D-01..D-16 that affects handler behavior has at least one assertion
- Downstream plans 08-02 and 08-03 can flip these files GREEN without rewriting them
</success_criteria>

<output>
Create `.planning/phases/08-keys-server-crud/08-00-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/mcp/tools/private_key.test.ts` — RED vitest scaffold covering KEY-01..KEY-05 + D-01..D-04, D-11, D-13..D-15
- `src/mcp/tools/server.test.ts` — RED vitest scaffold covering SRV-01..SRV-05 + D-05..D-08, D-12, D-16
