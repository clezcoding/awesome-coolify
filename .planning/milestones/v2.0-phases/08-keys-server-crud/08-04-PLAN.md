---
phase: 08-keys-server-crud
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - src/mcp/server.ts
autonomous: true
requirements:
  - KEY-01
  - KEY-02
  - KEY-03
  - KEY-04
  - KEY-05
  - SRV-01
  - SRV-02
  - SRV-03
  - SRV-04
  - SRV-05
must_haves:
  truths:
    - registerCoolifyTools registers 'private_key' and 'server' tools with the action schemas from 08-02 / 08-03
    - Both tools use toolOutputSchema as outputSchema (consistent with existing tools)
    - private_key tool annotations include openWorldHint (mutating actions exist); readOnlyHint omitted
    - server tool annotations include openWorldHint; readOnlyHint omitted
    - Error results from either tool surface as structuredContent { ok:false, error } matching the existing pattern
    - Full suite `npm run test` green
  artifacts:
    - src/mcp/server.ts (extended)
  key_links:
    - registerCoolifyTools -> handlePrivateKeyAction / isPrivateKeyErrorResult (08-02)
    - registerCoolifyTools -> handleServerAction / isServerErrorResult (08-03)
    - toolOutputSchema -> both new tools (existing schema, no change needed)
---

<objective>
Wire the `private_key` and `server` handlers into the MCP server registration so they become agent-callable tools. This is the final integration step that exposes Phase 8 work to MCP clients.

Purpose: 08-02 and 08-03 built the handlers in isolation; this plan makes them reachable. No new business logic — only registration boilerplate mirroring the existing 9 tools.

Output: Updated `src/mcp/server.ts` with two new `server.registerTool` blocks and the corresponding imports.
</objective>

<execution_context>
@/Users/puzzless/gstack/.cursor/gsd-core/workflows/execute-plan.md
@/Users/puzzless/gstack/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-keys-server-crud/08-CONTEXT.md
@.planning/phases/08-keys-server-crud/08-02-PLAN.md
@.planning/phases/08-keys-server-crud/08-03-PLAN.md
@.cursor/skills/spike-findings-awesome-coolify/SKILL.md
@src/mcp/server.ts
@src/mcp/tools/database.ts
@src/mcp/tools/private_key.ts
@src/mcp/tools/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register private_key and server tools in registerCoolifyTools</name>
  <files>src/mcp/server.ts</files>
  <read_first>
    - src/mcp/server.ts (existing 9 registerTool blocks — mirror exactly)
    - src/mcp/tools/private_key.ts (08-02 exports: handlePrivateKeyAction, isPrivateKeyErrorResult, privateKeyActionSchema)
    - src/mcp/tools/server.ts (08-03 exports: handleServerAction, isServerErrorResult, serverActionSchema)
  </read_first>
  <action>
    In src/mcp/server.ts:
    - Add imports after the database import block:
      - `import { handlePrivateKeyAction, isPrivateKeyErrorResult, privateKeyActionSchema } from './tools/private_key.js';`
      - `import { handleServerAction, isServerErrorResult, serverActionSchema } from './tools/server.js';`
    - Inside registerCoolifyTools, after the `database` registration block, add a `private_key` registration:
      - description: 'Private key CRUD (list, get, create, update, delete, delete_preview) for SSH keys registered in Coolify. PEM material is never returned by any action — full projection masks the private_key field even with reveal:true (D-02). list rejects reveal:true with COOLIFY_422 (D-11). delete requires confirm:true (D-14); deleting a key still referenced by servers returns COOLIFY_409 with dependent_server_uuids (D-15). delete_preview lists dependents without deleting.'
      - inputSchema: privateKeyActionSchema
      - outputSchema: toolOutputSchema
      - annotations: { openWorldHint: true } (omit readOnlyHint — mutating actions exist; omit destructiveHint per existing Cursor host lease constraint noted in emergency block)
      - handler: async (args) => { const result = await handlePrivateKeyAction(args, env); if (isPrivateKeyErrorResult(result)) return { ...result, structuredContent: { ok:false, error: result.structuredContent.error } }; return { content: [{ type:'text', text: result._formattedText }], structuredContent: { ok:true, data: result.data, _meta: result._meta } }; }
    - Add a `server` registration block after `private_key`:
      - description: 'Server CRUD + validate (get, create, update, delete, delete_preview, validate). Servers are listed via resource tool with type=server (D-10). create auto-validates SSH reachability with a 30s poll unless validate:false (D-05/D-06); unreachable hosts return ok:true with validation.reachable:false and a COOLIFY_SSH_UNREACHABLE recovery hint — no auto-rollback (D-07). validate uses the same wait/timeout model (D-08). delete requires confirm:true (D-14) and defaults delete_volumes:false (D-16). delete_preview lists child resources as a warning, not a block (D-16).'
      - inputSchema: serverActionSchema
      - outputSchema: toolOutputSchema
      - annotations: { openWorldHint: true }
      - handler: async (args) => { const result = await handleServerAction(args, env); if (isServerErrorResult(result)) return { ...result, structuredContent: { ok:false, error: result.structuredContent.error } }; return { content: [{ type:'text', text: result._formattedText }], structuredContent: { ok:true, data: result.data, _meta: result._meta } }; }
    - Do NOT modify existing tool registrations
    - Do NOT change toolOutputSchema (already supports the error/data/_meta envelope used by both new tools)
  </action>
  <verify>
    <automated>npx vitest run &&amp; npm run build</automated>
  </verify>
  <done>
    - `grep -c "registerTool" src/mcp/server.ts` >= 11 (was 9, now 11)
    - `grep -c "private_key\|server" src/mcp/server.ts` reflects new imports + registrations
    - `npm run test` full suite green (including 08-02 and 08-03 tests)
    - `npm run build` (tsup) succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → registered tool | New tools now exposed to agents; inputSchema is the gate (Zod discriminatedUnion from 08-02/08-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Elevation of Privilege | agent could call private_key.delete without confirm | high | mitigate | Handler enforces confirm gate (08-02, D-14); registration is just wiring. Existing test coverage guards the gate. |
| T-08-04-02 | Elevation of Privilege | agent could call server.delete with delete_volumes=true by default | high | mitigate | Handler defaults delete_volumes=false (08-03, D-16); registration does not override defaults. |
| T-08-04-03 | Information Disclosure | tool descriptions in registerTool could leak PEM handling nuance to agent logs | low | accept | Descriptions document the masking guarantees (D-02/D-11) — this is intended disclosure to guide safe agent use. |
</threat_model>

<verification>
- `npm run test` full suite green (289+ tests from v1 plus new Phase 8 tests)
- `npm run build` (tsup) succeeds — no new TS errors
- `grep -c "registerTool" src/mcp/server.ts` >= 11
- `graphify update .` run after multi-file code changes (per .cursor/rules/graphify.mdc — keeps AST-only knowledge graph current; non-blocking gate)
- Manual MCP stdio E2E (MANUAL-ONLY, per P1–P5 precedent): spawn the server, list tools, confirm `private_key` and `server` appear with correct input schemas; not a blocking gate for this plan
</verification>

<success_criteria>
- Both Phase 8 tools are agent-callable via MCP
- Existing 9 tools unchanged
- Full test suite green
- `graphify update .` run to refresh knowledge graph after Phase 8 code changes
- Phase 8 ready for `/gsd-verify-work`
</success_criteria>

<output>
Create `.planning/phases/08-keys-server-crud/08-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- Two new `server.registerTool` blocks (`private_key`, `server`) in src/mcp/server.ts
- Two new import lines for `./tools/private_key.js` and `./tools/server.js` in src/mcp/server.ts
