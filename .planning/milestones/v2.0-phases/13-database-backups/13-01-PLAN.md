---
phase: 13-database-backups
plan: 01
type: execute
wave: 1
depends_on:
  - 13-00
files_modified:
  - src/api/client.ts
  - src/mcp/tools/backup-shared.ts
autonomous: true
requirements:
  - BAK-01
  - BAK-02
  - BAK-03
  - BAK-04
  - BAK-05
  - BAK-06
must_haves:
  truths:
    - "src/api/client.ts exports fetchDatabaseBackups, createDatabaseBackup, updateDatabaseBackup, deleteDatabaseBackup, fetchBackupExecutions"
    - "deleteDatabaseBackup forwards delete_s3 query parameter (default false)"
    - "fetchBackupExecutions returns { executions: [...] } per OpenAPI"
    - "src/mcp/tools/backup-shared.ts exports maskBackupConfig, validateBackupDeleteConfirm, buildBackupCreatePayload, buildBackupUpdatePayload, BACKUP_FREQUENCY_PRESETS, backupFrequencyCreateSchema, backupFrequencyUpdateSchema"
    - "backupFrequencyCreateSchema accepts presets OR cron string (D-04 — create allows cron)"
    - "backupFrequencyUpdateSchema accepts presets ONLY (Pitfall 1 — update rejects cron with COOLIFY_VALIDATION_ERROR)"
    - "maskBackupConfig masks S3 credential fields (secret_key, key, password, token) as *** unless reveal: true (D-15)"
    - "validateBackupDeleteConfirm throws COOLIFY_CONFIRM_REQUIRED without confirm (D-08) and when delete_s3: true without confirm (D-09)"
    - "All RED tests from 13-00 for client methods now pass (GREEN)"
  artifacts:
    - "src/api/client.ts (extended with 5 backup methods)"
    - "src/mcp/tools/backup-shared.ts (new — mirrors env-shared.ts)"
  key_links:
    - "Backup client methods use the same ofetch + mapApiError pattern as existing client functions"
    - "backup-shared.ts reuses ASK_HUMAN_REVEAL_HINT from env-shared.ts (D-16 continuity)"
    - "backup-shared.ts frequency schemas encode the Pitfall 1 create-vs-update asymmetry"
---

## Artifacts this phase produces

New exports from `src/api/client.ts`:
- `function fetchDatabaseBackups(url, token, databaseUuid, verifySsl=true): Promise<unknown[]>`
- `function createDatabaseBackup(url, token, databaseUuid, payload, verifySsl=true): Promise<{ uuid: string; message: string }>`
- `function updateDatabaseBackup(url, token, databaseUuid, scheduledBackupUuid, payload, verifySsl=true): Promise<{ message: string }>`
- `function deleteDatabaseBackup(url, token, databaseUuid, scheduledBackupUuid, deleteS3=false, verifySsl=true): Promise<{ message: string }>`
- `function fetchBackupExecutions(url, token, databaseUuid, scheduledBackupUuid, verifySsl=true): Promise<{ executions: unknown[] }>`

New exports from `src/mcp/tools/backup-shared.ts`:
- `const BACKUP_FREQUENCY_PRESETS = ['every_minute', 'hourly', 'daily', 'weekly', 'monthly', 'yearly'] as const`
- `const backupFrequencyCreateSchema: z.ZodString` (accepts any non-empty string — presets OR cron)
- `const backupFrequencyUpdateSchema: z.ZodEnum` (presets only — Pitfall 1)
- `function maskBackupConfig(raw, reveal): Record<string, unknown>` (S3 credential masking per D-15)
- `function validateBackupDeleteConfirm(confirm, deleteS3, dbUuid, scheduledBackupUuid): void` (D-08, D-09)
- `function buildBackupCreatePayload(parsed): Record<string, unknown>` (curated POST body per D-06)
- `function buildBackupUpdatePayload(parsed): Record<string, unknown>` (curated PATCH body per D-06)

No changes to `src/utils/errors.ts` error-code enum — D-08/D-09 use the existing `COOLIFY_CONFIRM_REQUIRED` code.

<objective>
Ship the five API client backup methods and the `backup-shared.ts` helper module (masking, confirm gate, frequency schemas, payload builders). These are the foundation that 13-02 and 13-03 handlers will call.

Purpose: Centralize all Coolify backup HTTP interaction in client.ts and all backup-specific validation/masking in backup-shared.ts so handlers stay thin and testable. The frequency preset-vs-cron asymmetry (Pitfall 1) is encoded at the schema layer so 13-02 (create) and 13-03 (update) cannot accidentally bypass it.
Output: Extended src/api/client.ts, new src/mcp/tools/backup-shared.ts. All 13-00 RED tests for client methods flip GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-database-backups/13-CONTEXT.md
@.planning/phases/13-database-backups/13-RESEARCH.md
@.planning/phases/13-database-backups/13-00-SUMMARY.md
@src/api/client.ts
@src/api/client.test.ts
@src/mcp/tools/env-shared.ts
@src/mcp/tools/database.ts
@src/utils/projections.ts
@src/utils/errors.ts
@src/utils/redact.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Five backup client methods in src/api/client.ts</name>
  <read_first>
    - src/api/client.ts (existing function shape: signature, createCoolifyClient, ofetch usage, mapApiError, return types; see fetchDatabase, deleteService, createPostgresqlDatabase as templates)
    - src/utils/errors.ts (CoolifyErrorCode enum, mapApiError, RECOVERY_HINTS)
    - src/api/client.test.ts (RED expectations from 13-00 Task 2)
    - docs/coolify_openapi.yaml lines 2589-2711 (POST /databases/{uuid}/backups), 2988-3107 (DELETE/PATCH .../backups/{scheduled_backup_uuid}), 3968-4003 (GET .../executions)
    - .planning/phases/13-database-backups/13-RESEARCH.md (Code Examples section — exact signatures)
  </read_first>
  <files>src/api/client.ts</files>
  <behavior>
    - fetchDatabaseBackups(url, token, databaseUuid, verifySsl=true) GETs /databases/{databaseUuid}/backups and returns array (or [] if response is not an array)
    - createDatabaseBackup(url, token, databaseUuid, payload, verifySsl=true) POSTs /databases/{databaseUuid}/backups with payload and returns { uuid, message }
    - updateDatabaseBackup(url, token, databaseUuid, scheduledBackupUuid, payload, verifySsl=true) PATCHes /databases/{databaseUuid}/backups/{scheduledBackupUuid} and returns { message }
    - deleteDatabaseBackup(url, token, databaseUuid, scheduledBackupUuid, deleteS3=false, verifySsl=true) DELETEs /databases/{databaseUuid}/backups/{scheduledBackupUuid}?delete_s3={deleteS3} and returns { message }
    - fetchBackupExecutions(url, token, databaseUuid, scheduledBackupUuid, verifySsl=true) GETs /databases/{databaseUuid}/backups/{scheduledBackupUuid}/executions and returns { executions: [...] } (normalize to { executions: [] } if response is missing the executions key)
    - All methods map HTTP errors via mapApiError (existing pattern)
  </behavior>
  <action>
    Add five exported async functions to src/api/client.ts: fetchDatabaseBackups, createDatabaseBackup, updateDatabaseBackup, deleteDatabaseBackup, fetchBackupExecutions. Use the existing createCoolifyClient + mapApiError pattern. For deleteDatabaseBackup, forward the delete_s3 query parameter (default false) — mirror the deleteService query-param forwarding pattern. For fetchDatabaseBackups and fetchBackupExecutions, defensively normalize the response shape (array for fetchDatabaseBackups, { executions: [...] } for fetchBackupExecutions) so handlers do not need to re-check. Do NOT add masking here — masking is the handler's responsibility via maskBackupConfig (built in Task 2). Do NOT add new error codes. Run the 13-00 RED tests for client methods and confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/api/client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function fetchDatabaseBackups" src/api/client.ts` returns 1
    - `grep -c "export async function createDatabaseBackup" src/api/client.ts` returns 1
    - `grep -c "export async function updateDatabaseBackup" src/api/client.ts` returns 1
    - `grep -c "export async function deleteDatabaseBackup" src/api/client.ts` returns 1
    - `grep -c "export async function fetchBackupExecutions" src/api/client.ts` returns 1
    - `grep -c "delete_s3" src/api/client.ts` returns at least 1 (deleteDatabaseBackup query param forwarding)
    - `npx vitest run src/api/client.test.ts` exits 0 (GREEN)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - Five backup client methods exported from src/api/client.ts
    - delete_s3 query parameter forwarded with default false
    - fetchBackupExecutions response normalized to { executions: [...] }
    - All 13-00 client.test.ts RED tests now GREEN
    - No existing client tests regressed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/mcp/tools/backup-shared.ts with frequency schemas, masking, confirm gate, payload builders</name>
  <read_first>
    - src/mcp/tools/env-shared.ts (maskEnvRecord, validateEnvMutationConfirm, ASK_HUMAN_REVEAL_HINT — mirror these patterns)
    - src/mcp/tools/database.ts (DATABASE_UPDATE_CURATED_FIELD_KEYS, buildUpdatePayload, validateDeleteConfirm, omitUndefined — reuse patterns)
    - src/utils/projections.ts (sanitizeFullProjection — call this first, then apply S3-specific masking on top)
    - src/utils/errors.ts (CoolifyApiError, RECOVERY_HINTS, CoolifyErrorCode)
    - .planning/phases/13-database-backups/13-CONTEXT.md (D-04 frequency presets|cron, D-06 S3 optional, D-08 confirm on delete, D-09 delete_s3 default false, D-15 S3 masking, D-16 ask_human_reveal)
    - .planning/phases/13-database-backups/13-RESEARCH.md (Pitfall 1 — update_backup restricts frequency to presets only; create allows cron)
    - docs/coolify_openapi.yaml lines 2643-2690 (POST body fields), 3064-3107 (PATCH body fields)
  </read_first>
  <files>src/mcp/tools/backup-shared.ts</files>
  <behavior>
    - BACKUP_FREQUENCY_PRESETS = ['every_minute', 'hourly', 'daily', 'weekly', 'monthly', 'yearly'] as const
    - backupFrequencyCreateSchema: z.string().min(1) — accepts any non-empty string (presets OR cron expression) per D-04
    - backupFrequencyUpdateSchema: z.enum(BACKUP_FREQUENCY_PRESETS) — presets only per Pitfall 1; cron strings fail Zod parse with COOLIFY_VALIDATION_ERROR before HTTP
    - maskBackupConfig(raw, reveal): runs sanitizeFullProjection first, then explicitly masks S3 credential fields (secret_key, secret_access_key, access_key, session_token, token, password, key) as *** unless reveal: true; leaves uuid, frequency, retention counts, enabled, save_s3, s3_storage_uuid visible per D-15
    - validateBackupDeleteConfirm(confirm, deleteS3, dbUuid, scheduledBackupUuid): throws COOLIFY_CONFIRM_REQUIRED when confirm !== true (D-08); when deleteS3 === true, also requires confirm: true (D-09 — purging S3 artifacts is destructive)
    - buildBackupCreatePayload(parsed): curated POST body — frequency (required), enabled (default true per D-05), save_s3 (default false), s3_storage_uuid (only when save_s3: true), databases_to_backup, dump_all, backup_now, database_backup_retention_* (local + S3), timeout; omits undefined fields
    - buildBackupUpdatePayload(parsed): curated PATCH body — same fields as create minus frequency-whitelist (frequency uses backupFrequencyUpdateSchema at the handler layer), plus backup_now for the backup:now action; omits undefined fields
    - Reuses ASK_HUMAN_REVEAL_HINT from env-shared.ts (D-16 continuity — no duplicated hint string)
  </behavior>
  <action>
    Create src/mcp/tools/backup-shared.ts exporting BACKUP_FREQUENCY_PRESETS, backupFrequencyCreateSchema, backupFrequencyUpdateSchema, maskBackupConfig, validateBackupDeleteConfirm, buildBackupCreatePayload, buildBackupUpdatePayload. Import ASK_HUMAN_REVEAL_HINT from env-shared.ts (do not duplicate the string). The frequency schemas are the CRITICAL encoding of Pitfall 1: create accepts any non-empty string (Coolify create_backup validates via validate_cron_expression), update restricts to the six-preset enum (Coolify update_backup controller uses `string|in:every_minute,hourly,daily,weekly,monthly,yearly`). maskBackupConfig must call sanitizeFullProjection first, then explicitly mask S3 credential-like keys even if the generic sanitizer missed them — this is the D-15 explicit-allowlist approach chosen per CONTEXT.md discretion. validateBackupDeleteConfirm takes both confirm and deleteS3 so the D-09 rule (delete_s3: true still requires confirm) is enforced in one place. buildBackupCreatePayload and buildBackupUpdatePayload use the omitUndefined pattern from database.ts. Do NOT add COOLIFY_SYNC_CONFLICT or any new error codes. Run the 13-00 RED tests and confirm any that depend on these helpers (via database.test.ts) remain RED only because the handlers are not yet wired — the helper module itself should compile cleanly.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/api/client.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const BACKUP_FREQUENCY_PRESETS" src/mcp/tools/backup-shared.ts` returns 1
    - `grep -c "backupFrequencyCreateSchema" src/mcp/tools/backup-shared.ts` returns at least 1
    - `grep -c "backupFrequencyUpdateSchema" src/mcp/tools/backup-shared.ts` returns at least 1
    - `grep -cE "'(every_minute|hourly|daily|weekly|monthly|yearly)'" src/mcp/tools/backup-shared.ts` returns at least 6 (the preset enum)
    - `grep -c "export function maskBackupConfig" src/mcp/tools/backup-shared.ts` returns 1
    - `grep -c "export function validateBackupDeleteConfirm" src/mcp/tools/backup-shared.ts` returns 1
    - `grep -c "export function buildBackupCreatePayload" src/mcp/tools/backup-shared.ts` returns 1
    - `grep -c "export function buildBackupUpdatePayload" src/mcp/tools/backup-shared.ts` returns 1
    - `grep -c "ASK_HUMAN_REVEAL_HINT" src/mcp/tools/backup-shared.ts` returns at least 1 (reused from env-shared.ts, not duplicated)
    - `grep -cE "'(fail|skip)'" src/mcp/tools/backup-shared.ts` returns 0 (no rejected enum values)
    - `grep -c "mapApiError" src/api/client.ts` returns at least 1 (backup client errors inherit existing redaction path per D-17 — see 13-VALIDATION.md manual map)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - src/mcp/tools/backup-shared.ts exports the two frequency schemas, masking helper, confirm gate, and two payload builders
    - backupFrequencyCreateSchema accepts any non-empty string (D-04)
    - backupFrequencyUpdateSchema accepts presets only (Pitfall 1)
    - maskBackupConfig explicitly masks S3 credential fields (D-15)
    - validateBackupDeleteConfirm enforces D-08 and D-09 in one place
    - ASK_HUMAN_REVEAL_HINT reused from env-shared.ts (D-16 continuity, no duplication)
    - Build passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Handler → client.ts | Handler passes user-supplied uuid, scheduled_backup_uuid, payload; client constructs URL and body |
| client.ts → Coolify API | Outbound HTTP with bearer token; errors mapped back through mapApiError |
| User payload → backup-shared.ts schemas | Zod validates frequency, S3 fields, retention before any HTTP call (D-18) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01-01 | Information Disclosure | error messages from client.ts | high | mitigate | mapApiError already redacts secrets via redactSecrets; do not append raw response bodies to error envelopes. Verify RECOVERY_HINTS does not echo S3 secrets. |
| T-13-01-02 | Tampering | frequency whitelist bypass on update | high | mitigate | backupFrequencyUpdateSchema is z.enum(presets) — cron strings fail Zod parse with COOLIFY_VALIDATION_ERROR before HTTP (Pitfall 1). |
| T-13-01-03 | Information Disclosure | S3 credentials in backup config responses | high | mitigate | maskBackupConfig explicitly masks secret_key, secret_access_key, access_key, session_token, token, password, key as *** unless reveal: true (D-15). |
| T-13-01-04 | Tampering | backup:delete without confirm | high | mitigate | validateBackupDeleteConfirm throws COOLIFY_CONFIRM_REQUIRED without confirm (D-08) and when delete_s3: true without confirm (D-09). |
| T-13-01-05 | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed in this plan (ofetch, zod already audited [OK] in 13-RESEARCH.md). No slopcheck checkpoint needed. |
</threat_model>

<verification>
- src/api/client.ts exports the five backup methods
- src/mcp/tools/backup-shared.ts exports the frequency schemas, masking helper, confirm gate, and payload builders
- All 13-00 RED tests for client methods now GREEN
- Frequency preset-vs-cron asymmetry encoded at the schema layer (Pitfall 1)
- S3 credential masking explicit (D-15)
- Confirm gate enforces D-08 and D-09 in one place
- No new error codes introduced
- No existing tests regressed
</verification>

<success_criteria>
- Five backup client methods parameterized correctly (URL paths, verbs, delete_s3 query forwarding, executions response normalization)
- backup-shared.ts mirrors env-shared.ts patterns (masking, confirm gate, ask_human_reveal reuse)
- Frequency create-vs-update asymmetry encoded at the schema layer (Pitfall 1)
- S3 credential masking explicit and allowlisted (D-15)
- Confirm gate covers both D-08 (delete requires confirm) and D-09 (delete_s3: true still requires confirm)
- All 13-00 client.test.ts RED tests GREEN
- Build passes
</success_criteria>

<output>
Create `.planning/phases/13-database-backups/13-01-SUMMARY.md` when done
</output>

