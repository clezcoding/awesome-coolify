---
phase: 13-database-backups
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/mcp/tools/database.ts
autonomous: true
requirements:
  - BAK-01
  - BAK-02
  - BAK-06
must_haves:
  truths:
    - "databaseActionSchema discriminatedUnion includes backup:create, backup:list, backup:history branches"
    - "backup:create handler resolves parent DB via uuid|name (D-03), validates payload via Zod (D-18), calls createDatabaseBackup, returns schedule uuid with S3 credentials masked (D-15)"
    - "backup:create accepts frequency presets OR cron expression (D-04 — create allows cron)"
    - "backup:create with save_s3: true requires s3_storage_uuid (D-06) — Zod superRefine throws COOLIFY_VALIDATION_ERROR"
    - "backup:create with backup_now: true returns schedule uuid (D-07)"
    - "backup:create does NOT require confirm (D-10)"
    - "backup:list handler calls fetchDatabaseBackups, masks S3 credentials, returns array of schedule summaries (BAK-02)"
    - "backup:list with reveal: true surfaces ask_human_reveal recovery hint (D-16)"
    - "backup:history handler calls fetchBackupExecutions, returns executions with status, timestamps, size per run (BAK-06, D-14)"
    - "backup:history with reveal: true surfaces ask_human_reveal recovery hint (D-16)"
    - "All 13-00 RED tests for backup:create, backup:list, backup:history now pass (GREEN)"
  artifacts:
    - "src/mcp/tools/database.ts (extended with 3 backup:* action schemas + handlers + dispatch)"
  key_links:
    - "database.ts imports backup-shared.ts (maskBackupConfig, buildBackupCreatePayload, backupFrequencyCreateSchema, ASK_HUMAN_REVEAL_HINT)"
    - "database.ts imports fetchDatabaseBackups, createDatabaseBackup, fetchBackupExecutions from src/api/client.ts"
    - "backup:* actions reuse resolveDatabaseUuid for parent DB identity (D-03)"
---

## Artifacts this phase produces

Modified symbols in `src/mcp/tools/database.ts`:
- New Zod schemas: `backupCreateActionSchema`, `backupListActionSchema`, `backupHistoryActionSchema` — added to the `databaseActionSchema` discriminatedUnion alongside existing get/start/stop/restart/create/update/delete/delete_preview/envs:* branches.
- New handler functions: `handleDatabaseBackupCreate`, `handleDatabaseBackupList`, `handleDatabaseBackupHistory`.
- New result types: `DatabaseBackupCreateResult`, `DatabaseBackupListResult`, `DatabaseBackupHistoryResult` — added to the `DatabaseActionResult` union.
- New dispatch cases in `handleDatabaseAction` switch: `backup:create`, `backup:list`, `backup:history`.

No new exports beyond database.ts. No new error codes. No new files.

<objective>
Implement the read/create half of the `backup:*` action namespace on the existing `database` MCP tool: `backup:create` (BAK-01), `backup:list` (BAK-02), and `backup:history` (BAK-06). These three actions share no confirm gate (D-10) and cover schedule creation, schedule listing, and execution history.

Purpose: Ship the non-destructive backup operations first so the agent can configure schedules and inspect history before 13-03 adds the mutating/trigger operations (update, delete, now). The frequency preset-vs-cron asymmetry for create is encoded by importing `backupFrequencyCreateSchema` from backup-shared.ts.
Output: Extended src/mcp/tools/database.ts with three new action schemas, three handlers, and three dispatch cases. All 13-00 RED tests for backup:create, backup:list, backup:history flip GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-database-backups/13-CONTEXT.md
@.planning/phases/13-database-backups/13-RESEARCH.md
@.planning/phases/13-database-backups/13-00-SUMMARY.md
@.planning/phases/13-database-backups/13-01-SUMMARY.md
@src/mcp/tools/database.ts
@src/mcp/tools/database.test.ts
@src/mcp/tools/backup-shared.ts
@src/api/client.ts
@src/utils/projections.ts
@src/utils/errors.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backup:create schema + handler + dispatch (BAK-01)</name>
  <read_first>
    - src/mcp/tools/database.ts (existing action schema pattern: requireDatabaseIdentifier, mutationResponseParamsSchema, discriminatedUnion, handleDatabaseAction switch; see envsCreateActionSchema and handleDatabaseEnvsCreate as templates)
    - src/mcp/tools/backup-shared.ts (backupFrequencyCreateSchema, buildBackupCreatePayload, maskBackupConfig — exported by 13-01)
    - src/api/client.ts (createDatabaseBackup, fetchDatabaseBackups — exported by 13-01)
    - src/mcp/tools/database.test.ts (RED expectations from 13-00 Task 1 for backup:create)
    - .planning/phases/13-database-backups/13-CONTEXT.md (D-02 backup:create, D-03 parent DB identity, D-04 frequency presets|cron, D-05 enabled default true, D-06 S3 optional + s3_storage_uuid required when save_s3, D-07 backup_now optional, D-10 no confirm, D-15 S3 masking, D-18 Zod validates first)
    - docs/coolify_openapi.yaml lines 2621-2711 (POST /databases/{uuid}/backups request body and 201 response)
  </read_first>
  <files>src/mcp/tools/database.ts</files>
  <behavior>
    - backup:create action schema: action literal 'backup:create'; uuid|name parent DB identifier (D-03); frequency via backupFrequencyCreateSchema (presets OR cron — D-04); enabled default true (D-05); save_s3 default false; s3_storage_uuid optional but required when save_s3: true (D-06 — superRefine); databases_to_backup, dump_all, backup_now (D-07), database_backup_retention_* (local + S3), timeout all optional; reveal default false; format/max_chars via mutationResponseParamsSchema; .strict()
    - superRefine: when save_s3 === true and !s3_storage_uuid, add COOLIFY_VALIDATION_ERROR issue (D-06)
    - handler: resolve parent DB uuid via resolveDatabaseUuid; build payload via buildBackupCreatePayload; call createDatabaseBackup; mask response via maskBackupConfig(raw, reveal); return { uuid, schedule: maskedConfig, hints: ['Use database backup:now to trigger an immediate run', 'Use database backup:history to inspect executions'] }
    - dispatch: add 'backup:create' case to handleDatabaseAction switch
    - No confirm gate (D-10)
    - Zod validates before any API call (D-18)
  </behavior>
  <action>
    Add backupCreateActionSchema to src/mcp/tools/database.ts. Use requireDatabaseIdentifier for parent DB identity (D-03). Import backupFrequencyCreateSchema, buildBackupCreatePayload, maskBackupConfig from backup-shared.ts. Import createDatabaseBackup from src/api/client.ts. Add a superRefine enforcing D-06 (save_s3: true requires s3_storage_uuid). Add handleDatabaseBackupCreate that resolves the parent DB, builds the curated payload, calls createDatabaseBackup, masks the response via maskBackupConfig, and returns a result with the schedule uuid and hints. Add the 'backup:create' case to the handleDatabaseAction switch. Add DatabaseBackupCreateResult to the DatabaseActionResult union. Do NOT add a confirm field (D-10). Do NOT add reveal to the action schema as required — it defaults false. Run the 13-00 RED tests for backup:create and confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/database.test.ts -t "backup:create"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "backupCreateActionSchema" src/mcp/tools/database.ts` returns at least 2 (definition + union member)
    - `grep -c "handleDatabaseBackupCreate" src/mcp/tools/database.ts` returns at least 2 (definition + dispatch)
    - `grep -c "case 'backup:create'" src/mcp/tools/database.ts` returns 1
    - `grep -c "backupFrequencyCreateSchema" src/mcp/tools/database.ts` returns at least 1 (import or usage)
    - `grep -c "save_s3" src/mcp/tools/database.ts` returns at least 2 (schema field + superRefine)
    - `grep -c "s3_storage_uuid" src/mcp/tools/database.ts` returns at least 2 (schema field + superRefine)
    - `grep -c "backup_now" src/mcp/tools/database.ts` returns at least 1 (D-07 optional field)
    - `npx vitest run src/mcp/tools/database.test.ts -t "backup:create"` exits 0 (GREEN)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - backup:create action schema, handler, and dispatch wired
    - Parent DB identity via uuid|name (D-03)
    - Frequency accepts presets OR cron (D-04)
    - save_s3: true requires s3_storage_uuid (D-06)
    - backup_now optional on create (D-07)
    - S3 credentials masked in response (D-15)
    - No confirm gate (D-10)
    - All 13-00 backup:create RED tests GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: backup:list + backup:history schemas + handlers + dispatch (BAK-02, BAK-06)</name>
  <read_first>
    - src/mcp/tools/database.ts (existing read action pattern: envsListActionSchema, handleDatabaseEnvsList; sharedReadParamsSchema usage)
    - src/mcp/tools/backup-shared.ts (maskBackupConfig, ASK_HUMAN_REVEAL_HINT — exported by 13-01)
    - src/mcp/tools/env-shared.ts (withRevealRecoveryHints pattern — mirror for backup)
    - src/api/client.ts (fetchDatabaseBackups, fetchBackupExecutions — exported by 13-01)
    - src/mcp/tools/database.test.ts (RED expectations from 13-00 Task 1 for backup:list and backup:history)
    - .planning/phases/13-database-backups/13-CONTEXT.md (D-02 backup:list/backup:history, D-03 scheduled_backup_uuid required for history, D-10 no confirm, D-14 history projection includes status/timestamps/size, D-15 S3 masking, D-16 ask_human_reveal)
    - docs/coolify_openapi.yaml lines 2589-2620 (GET /databases/{uuid}/backups), 3968-4003 (GET .../executions)
  </read_first>
  <files>src/mcp/tools/database.ts</files>
  <behavior>
    - backup:list action schema: action literal 'backup:list'; uuid|name parent DB identifier (D-03); reveal default false; format/max_chars via mutationResponseParamsSchema; .strict(); no scheduled_backup_uuid (list is per-DB, not per-schedule)
    - backup:list handler: resolve parent DB; call fetchDatabaseBackups; mask each schedule via maskBackupConfig(raw, reveal); return array of masked schedule summaries; when reveal: true, surface ask_human_reveal recovery hint (D-16)
    - backup:history action schema: action literal 'backup:history'; uuid|name parent DB identifier (D-03); scheduled_backup_uuid required string (D-03 — Coolify path param); reveal default false; format/max_chars via mutationResponseParamsSchema; .strict()
    - backup:history handler: resolve parent DB; call fetchBackupExecutions(url, token, dbUuid, scheduledBackupUuid); project each execution to { uuid, filename, size, created_at, message, status } (D-14 — per-run status, timestamps, size); return { scheduled_backup_uuid, executions: [...] }; when reveal: true, surface ask_human_reveal recovery hint (D-16) — note: executions themselves do not contain S3 secrets, but the hint is still surfaced for consistency with the reveal policy
    - dispatch: add 'backup:list' and 'backup:history' cases to handleDatabaseAction switch
    - Neither action requires confirm (D-10)
  </behavior>
  <action>
    Add backupListActionSchema and backupHistoryActionSchema to src/mcp/tools/database.ts. backupListActionSchema uses requireDatabaseIdentifier and does NOT include scheduled_backup_uuid (list is per-DB). backupHistoryActionSchema uses requireDatabaseIdentifier AND requires scheduled_backup_uuid as a non-optional z.string() (D-03 — Coolify path param). Import fetchDatabaseBackups and fetchBackupExecutions from src/api/client.ts. Import maskBackupConfig and ASK_HUMAN_REVEAL_HINT from backup-shared.ts (or reuse withRevealRecoveryHints from env-shared.ts directly). Add handleDatabaseBackupList that resolves the parent DB, calls fetchDatabaseBackups, masks each schedule via maskBackupConfig, and returns the array (with reveal hint when reveal: true). Add handleDatabaseBackupHistory that resolves the parent DB, calls fetchBackupExecutions, projects each execution to the D-14 shape (uuid, filename, size, created_at, message, status), and returns { scheduled_backup_uuid, executions: [...] } (with reveal hint when reveal: true). Add 'backup:list' and 'backup:history' cases to the handleDatabaseAction switch. Add DatabaseBackupListResult and DatabaseBackupHistoryResult to the DatabaseActionResult union. Do NOT add confirm fields (D-10). Run the 13-00 RED tests for backup:list and backup:history and confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/database.test.ts -t "backup:(list|history)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "backupListActionSchema" src/mcp/tools/database.ts` returns at least 2 (definition + union member)
    - `grep -c "backupHistoryActionSchema" src/mcp/tools/database.ts` returns at least 2 (definition + union member)
    - `grep -c "handleDatabaseBackupList" src/mcp/tools/database.ts` returns at least 2 (definition + dispatch)
    - `grep -c "handleDatabaseBackupHistory" src/mcp/tools/database.ts` returns at least 2 (definition + dispatch)
    - `grep -c "case 'backup:list'" src/mcp/tools/database.ts` returns 1
    - `grep -c "case 'backup:history'" src/mcp/tools/database.ts` returns 1
    - `grep -c "fetchDatabaseBackups" src/mcp/tools/database.ts` returns at least 1 (import or usage)
    - `grep -c "fetchBackupExecutions" src/mcp/tools/database.ts` returns at least 1 (import or usage)
    - `grep -c "ask_human_reveal" src/mcp/tools/database.ts` returns at least 1 (D-16 hint surfaced on reveal paths)
    - `npx vitest run src/mcp/tools/database.test.ts -t "backup:(list|history)"` exits 0 (GREEN)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - backup:list and backup:history action schemas, handlers, and dispatch wired
    - backup:list masks S3 credentials per schedule (D-15) and surfaces ask_human_reveal on reveal (D-16)
    - backup:history requires scheduled_backup_uuid (D-03) and projects executions to D-14 shape (status, timestamps, size)
    - Neither action requires confirm (D-10)
    - All 13-00 backup:list and backup:history RED tests GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → backup:create payload | User-supplied frequency, S3 fields, retention cross into MCP; Zod validates before HTTP (D-18) |
| Agent → backup:list/backup:history | Read-only; reveal: true gated by ask_human_reveal hint (D-16) |
| Handler → Coolify API | Outbound HTTP with bearer token; errors mapped via mapApiError |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | S3 credentials in backup:create response | high | mitigate | maskBackupConfig explicitly masks S3 credential fields as *** unless reveal: true (D-15). |
| T-13-02-02 | Information Disclosure | S3 credentials in backup:list response | high | mitigate | Each schedule masked via maskBackupConfig; reveal: true surfaces ask_human_reveal hint (D-15, D-16). |
| T-13-02-03 | Tampering | save_s3: true without s3_storage_uuid | medium | mitigate | Zod superRefine throws COOLIFY_VALIDATION_ERROR before HTTP (D-06). |
| T-13-02-04 | Tampering | frequency cron injection on create | low | accept | Coolify create_backup validates cron via validate_cron_expression; we pass through after Zod string check (D-04). |
| T-13-02-05 | Information Disclosure | execution history contains S3 secrets | low | mitigate | backup:history projects only non-secret fields (uuid, filename, size, created_at, message, status) per D-14; no S3 credentials surfaced. |
</threat_model>

<verification>
- databaseActionSchema includes backup:create, backup:list, backup:history branches
- handleDatabaseAction switch dispatches all three actions
- All 13-00 RED tests for backup:create, backup:list, backup:history now GREEN
- S3 credentials masked in create and list responses (D-15)
- ask_human_reveal hint surfaced on reveal paths (D-16)
- backup:history requires scheduled_backup_uuid (D-03) and projects D-14 shape
- No confirm gate on these three actions (D-10)
- No existing database tests regressed
</verification>

<success_criteria>
- Three backup:* actions (create, list, history) wired into the database tool
- BAK-01, BAK-02, BAK-06 satisfied
- Frequency create accepts presets OR cron (D-04)
- S3 optional with s3_storage_uuid required when save_s3: true (D-06)
- S3 masking + ask_human_reveal enforced (D-15, D-16)
- All 13-00 RED tests for these three actions GREEN
- Build passes
</success_criteria>

<output>
Create `.planning/phases/13-database-backups/13-02-SUMMARY.md` when done
</output>

