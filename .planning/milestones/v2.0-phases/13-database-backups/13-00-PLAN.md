---
phase: 13-database-backups
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/tools/database.test.ts
  - src/api/client.test.ts
autonomous: true
requirements:
  - BAK-01
  - BAK-02
  - BAK-03
  - BAK-04
  - BAK-05
  - BAK-06
must_haves:
  truths:
    - "database.test.ts contains RED describe blocks for backup:create, backup:list, backup:update, backup:delete, backup:now, backup:history"
    - "client.test.ts contains RED tests for fetchDatabaseBackups, createDatabaseBackup, updateDatabaseBackup, deleteDatabaseBackup, fetchBackupExecutions"
    - "RED tests encode the frequency preset-vs-cron asymmetry: create accepts cron, update rejects cron with COOLIFY_VALIDATION_ERROR (Pitfall 1)"
    - "RED tests encode confirm gate on backup:delete (D-08) and delete_s3 default false (D-09)"
    - "RED tests encode S3 masking (D-15) and ask_human_reveal recovery hint (D-16)"
    - "All new RED tests fail before any implementation in plans 13-01..13-04"
  artifacts:
    - "src/mcp/tools/database.test.ts (extended with backup:* describe blocks)"
    - "src/api/client.test.ts (extended with backup client method describe blocks)"
  key_links:
    - "RED tests reference the backup:* action strings that 13-02/03 handlers will dispatch on"
    - "RED tests reference the client function names that 13-01 will export from src/api/client.ts"
---

## Artifacts this phase produces

This plan (13-00) produces no production code — only RED test scaffolds. Symbols referenced by 13-01..13-04 implementations:

- `src/mcp/tools/database.test.ts` — six new describe blocks: `describe('database backup:create', ...)`, `backup:list`, `backup:update`, `backup:delete`, `backup:now`, `backup:history`; vi.mock additions for `fetchDatabaseBackups`, `createDatabaseBackup`, `updateDatabaseBackup`, `deleteDatabaseBackup`, `fetchBackupExecutions`.
- `src/api/client.test.ts` — five new describe blocks for the backup client methods, asserting URL paths and HTTP verbs exactly.

No new exports from `src/` production code in this plan.

<objective>
Lay RED TDD scaffolds for all six `backup:*` actions on the existing `database` MCP tool, plus the five API client backup methods. Tests MUST fail before any implementation ships in plans 13-01..13-04.

Purpose: TDD discipline — every handler and client method gets a failing test first, so 13-01..13-04 implement against executable specifications rather than prose. The frequency preset-vs-cron asymmetry (Pitfall 1) and the S3 masking/confirm gates are encoded as test expectations before any code exists.
Output: Two test files extended with RED describe blocks covering BAK-01..06 and D-01..D-17.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-database-backups/13-CONTEXT.md
@.planning/phases/13-database-backups/13-RESEARCH.md
@src/mcp/tools/database.test.ts
@src/api/client.test.ts
@src/mcp/tools/database.ts
@src/api/client.ts
@src/mcp/tools/env-shared.ts
@src/utils/projections.ts
@src/utils/errors.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED scaffolds for database.test.ts backup:* actions</name>
  <read_first>
    - src/mcp/tools/database.test.ts (existing vi.mock pattern + database lifecycle/create/update/delete describe blocks)
    - src/mcp/tools/database.ts (action discriminatedUnion shape, resolveDatabaseUuid, validateDeleteConfirm, throwValidationError)
    - .planning/phases/13-database-backups/13-CONTEXT.md (D-01..D-20; D-02 backup:* namespace, D-03 scheduled_backup_uuid, D-04 frequency presets|cron, D-08 confirm on delete, D-09 delete_s3 default false, D-12 backup:now via PATCH, D-15 S3 masking, D-16 ask_human_reveal)
    - .planning/phases/13-database-backups/13-RESEARCH.md (Pitfall 1: update_backup restricts frequency to presets only; create allows cron)
    - docs/coolify_openapi.yaml lines 2589-2711 (POST /databases/{uuid}/backups), 2988-3107 (DELETE/PATCH .../backups/{scheduled_backup_uuid}), 3968-4003 (GET .../executions)
  </read_first>
  <files>src/mcp/tools/database.test.ts</files>
  <behavior>
    - backup:create with frequency preset 'daily' returns schedule uuid; response masks S3 credential fields by default (D-15)
    - backup:create with cron expression '0 12 * * *' is accepted (D-04 — create allows cron)
    - backup:create with save_s3: true but no s3_storage_uuid throws COOLIFY_VALIDATION_ERROR (D-06)
    - backup:create with backup_now: true returns schedule uuid (D-07)
    - backup:create does NOT require confirm (D-10)
    - backup:list returns array of schedules with schedule/retention/destination summary; S3 secrets masked (D-15)
    - backup:list with reveal: true surfaces ask_human_reveal recovery hint (D-16)
    - backup:update with frequency preset 'hourly' succeeds (D-04 — update accepts presets only)
    - backup:update with cron expression '0 12 * * *' throws COOLIFY_VALIDATION_ERROR (Pitfall 1 — update rejects cron)
    - backup:update does NOT require confirm (D-10)
    - backup:delete without confirm throws COOLIFY_CONFIRM_REQUIRED (D-08)
    - backup:delete with confirm returns deletion disposition; delete_s3 defaults false (D-09)
    - backup:delete with delete_s3: true but no confirm throws COOLIFY_CONFIRM_REQUIRED (D-09)
    - backup:now requires parent DB identity AND scheduled_backup_uuid (D-13); triggers PATCH with backup_now: true (D-12)
    - backup:now does NOT require confirm (D-10)
    - backup:history returns executions array with status, timestamps, size per run (D-14, BAK-06)
    - backup:history does NOT require confirm (D-10)
    - All backup:* actions resolve parent DB via uuid|name with COOLIFY_AMBIGUOUS_MATCH on multi-match (D-03)
  </behavior>
  <action>
    Extend src/mcp/tools/database.test.ts with six new describe blocks for backup:create, backup:list, backup:update, backup:delete, backup:now, backup:history. Follow the existing vi.mock pattern for the Coolify client and resolveDatabaseUuid. Add vi.mock entries for fetchDatabaseBackups, createDatabaseBackup, updateDatabaseBackup, deleteDatabaseBackup, fetchBackupExecutions (these exports do not exist yet — RED). Reference action strings backup:create, backup:list, backup:update, backup:delete, backup:now, backup:history verbatim — these are the dispatch keys 13-02/03 will match on. For backup:update, include a negative test asserting that a cron expression frequency throws COOLIFY_VALIDATION_ERROR (Pitfall 1 — update_backup controller validates frequency against the six-preset whitelist). For backup:create, include a positive test asserting a cron expression is accepted (create_backup allows any string). For backup:delete, assert confirm gate (COOLIFY_CONFIRM_REQUIRED without confirm) and delete_s3 default false. For backup:list and backup:history, assert reveal:true surfaces an ask_human_reveal recovery hint (D-16) and that S3 credential fields are masked as *** by default (D-15). For backup:now, assert that the handler calls PATCH /databases/{uuid}/backups/{scheduled_backup_uuid} with body { backup_now: true } (D-12) and that both parent DB identity and scheduled_backup_uuid are required (D-13). Tests MUST fail (RED) at commit time because the handler branches and client methods do not exist yet. Do NOT stub handler implementations — only test scaffolds.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/database.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('database backup:" src/mcp/tools/database.test.ts` returns 6 (create, list, update, delete, now, history)
    - `grep -c "ask_human_reveal" src/mcp/tools/database.test.ts` returns at least 2 (one for backup:list, one for backup:history)
    - `grep -c "COOLIFY_CONFIRM_REQUIRED" src/mcp/tools/database.test.ts` returns at least 2 (backup:delete without confirm, backup:delete with delete_s3 without confirm)
    - `grep -c "delete_s3" src/mcp/tools/database.test.ts` returns at least 2 (default false test, delete_s3: true requires confirm test)
    - `grep -c "scheduled_backup_uuid" src/mcp/tools/database.test.ts` returns at least 4 (update, delete, now, history)
    - `grep -cE "'(every_minute|hourly|daily|weekly|monthly|yearly)'" src/mcp/tools/database.test.ts` returns at least 2 (create preset, update preset)
    - `grep -c "0 12 \* \* \*" src/mcp/tools/database.test.ts` returns at least 2 (create accepts cron, update rejects cron)
    - `npx vitest run src/mcp/tools/database.test.ts` exits non-zero with RED failures in new backup:* describe blocks
    - No existing database tests modified (diff confined to new describe blocks)
  </acceptance_criteria>
  <done>
    - database.test.ts contains describe blocks for all six backup:* actions
    - All new backup:* tests fail with RED status (handler not implemented)
    - No existing database lifecycle/create/update/delete/envs tests regressed
    - Frequency preset-vs-cron asymmetry encoded (Pitfall 1)
    - Confirm gate and delete_s3 default false encoded (D-08, D-09)
    - S3 masking and ask_human_reveal encoded (D-15, D-16)
    - backup:now PATCH-with-backup_now:true semantics encoded (D-12, D-13)
  </done>
</task>

<task type="tdd">
  <name>Task 2: RED scaffolds for client.test.ts backup methods</name>
  <read_first>
    - src/api/client.test.ts (existing ofetch mock pattern, URL/verb assertions)
    - src/api/client.ts (existing client function shape, createCoolifyClient, mapApiError)
    - .planning/phases/13-database-backups/13-RESEARCH.md (Code Examples section — client method signatures)
    - docs/coolify_openapi.yaml (backup endpoint paths and verbs)
  </read_first>
  <files>src/api/client.test.ts</files>
  <behavior>
    - fetchDatabaseBackups(url, token, databaseUuid, verifySsl) GETs /databases/{uuid}/backups and returns array
    - createDatabaseBackup(url, token, databaseUuid, payload, verifySsl) POSTs /databases/{uuid}/backups with payload and returns { uuid, message }
    - updateDatabaseBackup(url, token, databaseUuid, scheduledBackupUuid, payload, verifySsl) PATCHes /databases/{uuid}/backups/{scheduled_backup_uuid} and returns { message }
    - deleteDatabaseBackup(url, token, databaseUuid, scheduledBackupUuid, deleteS3=false, verifySsl) DELETEs /databases/{uuid}/backups/{scheduled_backup_uuid}?delete_s3={deleteS3} and returns { message }
    - fetchBackupExecutions(url, token, databaseUuid, scheduledBackupUuid, verifySsl) GETs /databases/{uuid}/backups/{scheduled_backup_uuid}/executions and returns { executions: [...] }
    - All methods map HTTP errors via mapApiError (existing pattern)
  </behavior>
  <action>
    Extend src/api/client.test.ts with five new describe blocks for fetchDatabaseBackups, createDatabaseBackup, updateDatabaseBackup, deleteDatabaseBackup, fetchBackupExecutions. Use the existing ofetch mock pattern. Assert URL paths and HTTP verbs exactly. For deleteDatabaseBackup, assert the delete_s3 query parameter is forwarded (default false, and true when explicitly passed). For fetchBackupExecutions, assert the response shape is { executions: [...] } per OpenAPI. Tests MUST fail (RED) at commit time because the client methods do not exist yet. Do NOT stub client implementations.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/api/client.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('fetchDatabaseBackups'" src/api/client.test.ts` returns at least 1
    - `grep -c "describe('createDatabaseBackup'" src/api/client.test.ts` returns at least 1
    - `grep -c "describe('updateDatabaseBackup'" src/api/client.test.ts` returns at least 1
    - `grep -c "describe('deleteDatabaseBackup'" src/api/client.test.ts` returns at least 1
    - `grep -c "describe('fetchBackupExecutions'" src/api/client.test.ts` returns at least 1
    - `grep -c "delete_s3" src/api/client.test.ts` returns at least 2 (default false, explicit true)
    - `npx vitest run src/api/client.test.ts` exits non-zero with RED failures in new describe blocks
    - No existing client tests modified
  </acceptance_criteria>
  <done>
    - client.test.ts has describe blocks for all five backup client methods
    - URL paths and HTTP verbs asserted exactly per OpenAPI
    - delete_s3 query parameter forwarding asserted
    - All new tests RED
    - No existing client tests regressed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → test file | Hard-coded backup fixtures cross into test scope; must not contain real S3 secrets |
| RED test → (unimplemented) handler | Tests assert that handlers WILL enforce confirm gates, frequency whitelist, and S3 masking; failure mode is RED, not silent pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-00-01 | Information Disclosure | test fixtures in database.test.ts | low | mitigate | Use synthetic fixture values only (e.g. `FAKE_S3_SECRET`, `***`); never embed real tokens. Assert masked output is `***` not the fixture value. |
| T-13-00-02 | Tampering | RED tests that pass silently | medium | mitigate | Verify gate: grep for backup:* test names AND assert vitest reports failures (RED) before 13-01 ships. |
| T-13-00-03 | Tampering | frequency whitelist not enforced on update | high | mitigate | RED test for backup:update with cron expression MUST assert COOLIFY_VALIDATION_ERROR before 13-03 ships (Pitfall 1). |
</threat_model>

<verification>
- Both test files extended with backup:* and backup client method describe blocks
- `npx vitest run src/mcp/tools/database.test.ts src/api/client.test.ts` reports RED on every new backup test
- No existing tests regressed
- Confirm-gate, frequency whitelist, S3 masking, and ask_human_reveal tests reference exact error codes and hints from src/utils/errors.ts and src/mcp/tools/env-shared.ts
- backup:now PATCH-with-backup_now:true semantics encoded (D-12, D-13)
</verification>

<success_criteria>
- 6 backup:* action describe blocks in src/mcp/tools/database.test.ts
- 5 backup client method describe blocks in src/api/client.test.ts
- All new tests RED at commit
- Every BAK-* requirement has at least one RED test asserting its behavior
- Frequency preset-vs-cron asymmetry encoded (Pitfall 1)
- Confirm gate and delete_s3 default false encoded (D-08, D-09)
- S3 masking and ask_human_reveal encoded (D-15, D-16)
</success_criteria>

<output>
Create `.planning/phases/13-database-backups/13-00-SUMMARY.md` when done
</output>

