---
phase: 11-service-database-crud
plan: 00
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/service.test.ts
  - src/mcp/tools/database.test.ts
  - src/api/client.test.ts
autonomous: true
requirements:
  - SVC-06
  - SVC-07
  - SVC-08
  - SVC-09
  - SVC-10
  - DB-01
  - DB-02
  - DB-03
  - DB-04
must_haves:
  truths:
    - "service.test.ts contains failing (RED) describe blocks for create one-click (SVC-06), create compose (SVC-07), update (SVC-08), delete + safe defaults (SVC-09), 409 force_domain_override (SVC-10), delete_preview, D-18 ambiguous match on update+delete, D-06 decoded compose in get/create/update responses"
    - "database.test.ts contains failing (RED) describe blocks for create 8 engines (DB-01), update (DB-02), delete + safe defaults (DB-03), public access confirm gate (DB-04), delete_preview, D-18 ambiguous match on update+delete"
    - "client.test.ts contains failing (RED) specs for createService/updateService/deleteService + 8 createDatabase posters + updateDatabase/deleteDatabase"
    - "vi.mock('../../api/client.js') factories in service.test.ts and database.test.ts include the new client function stubs (createService, updateService, deleteService, createPostgresqlDatabase, createMysqlDatabase, createMariadbDatabase, createMongodbDatabase, createRedisDatabase, createClickhouseDatabase, createDragonflyDatabase, createKeydbDatabase, updateDatabase, deleteDatabase) so tests can mock them before plan 11-01 ships the real client"
    - "service.test.ts D-06 RED tests mock fetchService/createService/updateService to return a raw object with `docker_compose_raw` (base64) and no `compose` field, then assert the agent-visible result has `compose` set to the decoded YAML and `docker_compose_raw` stripped"
    - "service.test.ts and database.test.ts D-18 RED tests mock fetchServices/fetchDatabases to return 2+ same-name records, then assert update/delete by name returns COOLIFY_AMBIGUOUS_MATCH and the mutation client function is NOT called"
    - "Running `npx vitest run src/mcp/tools/service.test.ts src/mcp/tools/database.test.ts` fails on the new describe blocks (RED) — handlers not yet implemented"
  artifacts:
    - "src/mcp/tools/service.test.ts (extended with new RED scaffolds)"
    - "src/mcp/tools/database.test.ts (extended with new RED scaffolds)"
    - "src/api/client.test.ts (extended with new RED scaffolds)"
  key_links:
    - "Test files import handleServiceAction/handleDatabaseAction + serviceActionSchema/databaseActionSchema from ./{service,database}.js — symbols already exported; new action branches added by plans 11-02/11-03/11-04/11-05"
    - "vi.mock factories extended with the 13 new client function stubs (3 service + 8 database posters + updateDatabase + deleteDatabase) so tests can mock them before 11-01 ships the real client"
---

<objective>
Wave 0 RED test scaffolds for Phase 11 — service + database CRUD.

Purpose: Establish failing tests for all 9 Phase 11 requirements before any handler code is written. Plans 11-02/11-03/11-04/11-05 flip these tests GREEN without rewriting them. Mirrors the 08-00 / 09-00 / 10-00 Wave 0 pattern.

Output: Extended `src/mcp/tools/service.test.ts`, `src/mcp/tools/database.test.ts`, and `src/api/client.test.ts` with new describe blocks that fail at handler import or runtime because the create/update/delete/delete_preview actions do not yet exist on `serviceActionSchema` / `databaseActionSchema` and the new client functions are not yet exported.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-service-database-crud/11-CONTEXT.md
@.planning/phases/11-service-database-crud/11-RESEARCH.md
@.planning/phases/11-service-database-crud/11-VALIDATION.md
@.planning/phases/10-application-crud-safety/10-00-PLAN.md
@src/mcp/tools/service.test.ts
@src/mcp/tools/database.test.ts
@src/api/client.test.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend service.test.ts vi.mock + add RED describe blocks for create/update/delete/delete_preview (SVC-06..SVC-10)</name>
  <files>src/mcp/tools/service.test.ts</files>
  <read_first>
    - src/mcp/tools/service.test.ts (lines 1-50 for existing vi.mock + testEnv + mockService patterns)
    - src/mcp/tools/application.test.ts (Phase 10 RED scaffold pattern — describe blocks for create/update/delete/delete_preview)
    - src/mcp/tools/private_key.test.ts (XOR file-path vs inline content pattern — mirror for compose_file XOR compose)
    - .planning/phases/11-service-database-crud/11-CONTEXT.md (D-01..D-13)
    - docs/coolify_openapi.yaml (lines 6847-6961 for POST /services shape; 6962-7152 for PATCH/DELETE /services/{uuid})
  </read_first>
  <behavior>
    - SVC-06: create with type='actualbudget' (one-click), server_uuid, project_uuid, environment_name -> calls createService with type in body; returns { uuid, ... }
    - SVC-07: create with compose (inline YAML string), server_uuid, project_uuid, environment_name -> calls createService with docker_compose_raw=base64(yaml); agent never sees base64 in input or response
    - SVC-07 (compose_file): create with compose_file (local path) -> handler reads file, encodes contents to base64, calls createService
    - SVC-07 (XOR): create with both type and compose -> Zod rejects with COOLIFY_VALIDATION_ERROR; create with neither -> Zod rejects
    - SVC-06/07 (instant_deploy default true per D-11): create without instant_deploy -> createService called with instant_deploy=true (D-11 override vs Phase 10 apps default false)
    - SVC-10 (409 path): create where client throws HTTP 409 with conflicts array -> handler returns COOLIFY_409 error with data.conflicts passthrough and force_domain_override recovery hint
    - SVC-10 (happy-path override): create with force_domain_override:true and a payload the API accepts (no 409) -> createService called with force_domain_override:true in the body; handler returns success with service UUID (no COOLIFY_409)
    - SVC-08: update with uuid, compose (or curated fields) -> updateService called with base64-encoded docker_compose_raw + curated fields
    - SVC-09: delete with confirm:true -> deleteService called with delete_volumes=false, delete_configurations=false, docker_cleanup=false, delete_connected_networks=false defaults
    - SVC-09 (confirm gate): delete without confirm -> COOLIFY_CONFIRM_REQUIRED; deleteService NOT called
    - D-18 (update ambiguous): update by `name` where fetchServices returns 2+ matches -> COOLIFY_AMBIGUOUS_MATCH; updateService NOT called
    - D-18 (delete ambiguous): delete by `name` where fetchServices returns 2+ matches -> COOLIFY_AMBIGUOUS_MATCH; deleteService NOT called
    - D-06 (get decode): service.get response `compose` field is decoded YAML (e.g. `'services:\n  redis:\n    image: redis:7'`), NOT base64; `docker_compose_raw` is absent from the agent-visible response
    - D-06 (create decode): create success response `compose` field is decoded YAML; `docker_compose_raw` is absent
    - D-06 (update decode): update success response `compose` field is decoded YAML; `docker_compose_raw` is absent
    - SAF-03: create/update with unknown field -> Zod rejects via .strict() before any client call
    - D-02: create with no project identifier (neither project_uuid nor project_name) -> Zod rejects with COOLIFY_VALIDATION_ERROR
    - delete_preview: returns { uuid, child_resources?, would_delete } without deleting
  </behavior>
  <action>
    Extend the existing `vi.mock('../../api/client.js', ...)` block to include the 3 new service client functions: `createService`, `updateService`, `deleteService` — each as `vi.fn()`. Add them to the import-from-client list below the mock.

    Add new `describe('service create', ...)`, `describe('service update', ...)`, `describe('service delete', ...)`, and `describe('service delete_preview', ...)` blocks with one `it(...)` per behavior listed above. Each test calls `handleServiceAction({ action: 'create'|'update'|'delete'|'delete_preview', ... }, testEnv)` and asserts on the returned result or thrown error. Tests MUST fail RED at commit time (handler not yet implemented — `serviceActionSchema.safeParse({ action: 'create', ... }).success` is false because the 'create' branch is not yet on the discriminatedUnion, so `handleServiceAction` throws a Zod error).

    For SVC-07 (compose encode): mock `createService` to capture its call argument; assert `createService` was called with a body whose `docker_compose_raw` field is the base64 encoding of the input `compose` string (use `Buffer.from(input, 'utf8').toString('base64')` in the test expectation). The test asserts the contract that the handler encodes transparently.

    For SVC-10 (409): mock `createService` to reject with an ofetch-shaped error whose `response.status === 409` and `response._data.conflicts` is a non-empty array; assert `isServiceErrorResult(result) === true` and `result.structuredContent.error.code === 'COOLIFY_409'` and `result.structuredContent.error.data.conflicts` matches the mock array, and recoveryHints mention `force_domain_override: true`.

    For SAF-03: assert `createService` (or `updateService`) is NOT called (vi.fn() not-called assertion) when required fields are missing or unknown fields are present.

    For D-18 (ambiguous match): mock `fetchServices` (existing client function used by `resolveServiceMutationUuid`) to return an array of 2+ services sharing the same `name`; call update/delete with `name` only and assert `isServiceErrorResult(result) === true`, `result.structuredContent.error.code === 'COOLIFY_AMBIGUOUS_MATCH'`, and `updateService` / `deleteService` was NOT called.

    For D-06 (decode in responses): mock `fetchService` (for get/update) and `createService` (for create) to return a raw object containing `docker_compose_raw: Buffer.from('services:\n  redis:\n    image: redis:7', 'utf8').toString('base64')` and NO `compose` field. Call service.get / create / update and assert the agent-visible result has `compose === 'services:\n  redis:\n    image: redis:7'` (decoded YAML) and `docker_compose_raw` is `undefined` (stripped by projectServiceCompose). The test asserts the D-06 decode contract.

    Do NOT implement the handler or schemas — only the tests. Tests must fail when run.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/service.test.ts -t "service (create|update|delete|delete_preview|get)"</automated>
  </verify>
  <acceptance_criteria>
    - service.test.ts contains `describe('service create', ...)`, `describe('service update', ...)`, `describe('service delete', ...)`, `describe('service delete_preview', ...)` blocks
    - At least 18 new `it(...)` cases covering SVC-06, SVC-07 (inline + file + XOR), SVC-06/07 instant_deploy default true, SVC-10 (409 + happy-path override), SVC-08, SVC-09 (confirm + safe defaults), D-18 (update ambiguous + delete ambiguous), D-06 (get decode + create decode + update decode), SAF-03, D-02, delete_preview
    - `vi.mock('../../api/client.js')` factory includes the 3 new service client function stubs
    - `npx vitest run src/mcp/tools/service.test.ts -t "service (create|update|delete|delete_preview)"` exits non-zero with failures in the new describe blocks (RED) — because `serviceActionSchema.safeParse({ action: 'create', ... }).success === false`
    - No existing tests in service.test.ts are modified or broken
  </acceptance_criteria>
  <done>
    - New RED describe blocks committed; CI/test run shows the new service tests failing as expected; existing tests still pass.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Extend database.test.ts vi.mock + add RED describe blocks for create/update/delete/delete_preview (DB-01..DB-04)</name>
  <files>src/mcp/tools/database.test.ts</files>
  <read_first>
    - src/mcp/tools/database.test.ts (lines 1-50 for existing vi.mock + testEnv + mockDatabase patterns)
    - src/mcp/tools/application.test.ts (Phase 10 RED scaffold pattern — describe blocks for create/update/delete/delete_preview)
    - .planning/phases/11-service-database-crud/11-CONTEXT.md (D-08..D-13)
    - docs/coolify_openapi.yaml (lines 3124-3230 for POST /databases/postgresql; 2712-2900 for PATCH/DELETE /databases/{uuid}; 3231-3910 for the other 7 engine posters)
  </read_first>
  <behavior>
    - DB-01: create with engine='postgresql', server_uuid, project_uuid, environment_name, postgres_user, postgres_password, postgres_db -> calls createPostgresqlDatabase; response masks postgres_password as '***' unless reveal:true
    - DB-01 (8 engines): one RED test per engine (postgresql, mysql, mariadb, mongodb, redis, clickhouse, dragonfly, keydb) asserting the matching create<Engine>Database client function is called
    - DB-01 (instant_deploy default true per D-11): create without instant_deploy -> create<Engine>Database called with instant_deploy=true
    - DB-04 (create public access confirm gate): create with is_public:true but no confirm -> COOLIFY_CONFIRM_REQUIRED; create<Engine>Database NOT called (D-12)
    - DB-04 (create public access happy path): create with is_public:true, public_port:5432, confirm:true -> create<Engine>Database called with is_public:true and public_port in body
    - DB-02: update with uuid, is_public:false (disable public access), public_port -> updateDatabase called with curated payload
    - DB-02 (update public access confirm gate per planner discretion): update with is_public:true but no confirm -> COOLIFY_CONFIRM_REQUIRED; updateDatabase NOT called (mirror D-12 for update path — chosen for safety consistency)
    - DB-03: delete with confirm:true -> deleteDatabase called with delete_volumes=false, delete_configurations=false, docker_cleanup=false, delete_connected_networks=false defaults
    - DB-03 (confirm gate): delete without confirm -> COOLIFY_CONFIRM_REQUIRED; deleteDatabase NOT called
    - D-18 (update ambiguous): update by `name` where fetchDatabases returns 2+ matches -> COOLIFY_AMBIGUOUS_MATCH; updateDatabase NOT called
    - D-18 (delete ambiguous): delete by `name` where fetchDatabases returns 2+ matches -> COOLIFY_AMBIGUOUS_MATCH; deleteDatabase NOT called
    - SAF-04: create/update response masks postgres_password / connection strings as '***' unless reveal:true
    - SAF-03: create/update with unknown field -> Zod rejects via .strict() before any client call
    - D-02: create with no project identifier -> Zod rejects with COOLIFY_VALIDATION_ERROR
    - delete_preview: returns { uuid, child_resources?, would_delete } without deleting
  </behavior>
  <action>
    Extend the existing `vi.mock('../../api/client.js', ...)` block to include the 10 new database client functions: `createPostgresqlDatabase`, `createMysqlDatabase`, `createMariadbDatabase`, `createMongodbDatabase`, `createRedisDatabase`, `createClickhouseDatabase`, `createDragonflyDatabase`, `createKeydbDatabase`, `updateDatabase`, `deleteDatabase` — each as `vi.fn()`. Add them to the import-from-client list below the mock.

    Add new `describe('database create', ...)`, `describe('database update', ...)`, `describe('database delete', ...)`, and `describe('database delete_preview', ...)` blocks with one `it(...)` per behavior listed above. Each test calls `handleDatabaseAction({ action: 'create'|'update'|'delete'|'delete_preview', ... }, testEnv)` and asserts on the returned result or thrown error. Tests MUST fail RED at commit time (handler not yet implemented).

    For DB-01 (8 engines): use a `it.each([...8 engines...])` block asserting the matching create<Engine>Database client function is called when `engine` is set to that value. The test asserts the dispatcher contract.

    For DB-04 (confirm gate): call create with `is_public: true` and no `confirm` and assert `isDatabaseErrorResult(result) === true`, `result.structuredContent.error.code === 'COOLIFY_CONFIRM_REQUIRED'`, and the matching create<Engine>Database was NOT called.

    For D-18 (ambiguous match): mock `fetchDatabases` (existing client function used by `resolveDatabaseUuid`) to return an array of 2+ databases sharing the same `name`; call update/delete with `name` only and assert `isDatabaseErrorResult(result) === true`, `result.structuredContent.error.code === 'COOLIFY_AMBIGUOUS_MATCH'`, and `updateDatabase` / `deleteDatabase` was NOT called.

    For SAF-04 masking: mock the create/update response to include `postgres_password: 'plain-secret'` (or engine-equivalent); call without `reveal` and assert the result data shows `'***'`; call with `reveal: true` and assert the plain value is returned.

    Do NOT implement the handler or schemas — only the tests. Tests must fail RED.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/database.test.ts -t "database (create|update|delete|delete_preview)"</automated>
  </verify>
  <acceptance_criteria>
    - database.test.ts contains `describe('database create', ...)`, `describe('database update', ...)`, `describe('database delete', ...)`, `describe('database delete_preview', ...)` blocks
    - At least 18 new `it(...)` cases covering DB-01 (8 engines + instant_deploy default), DB-04 (create confirm gate + happy path), DB-02 (update + update-path confirm gate), DB-03 (confirm + safe defaults), D-18 (update ambiguous + delete ambiguous), SAF-04, SAF-03, D-02, delete_preview
    - `vi.mock('../../api/client.js')` factory includes the 10 new database client function stubs
    - `npx vitest run src/mcp/tools/database.test.ts -t "database (create|update|delete|delete_preview)"` exits non-zero with failures in the new describe blocks (RED)
    - No existing tests in database.test.ts are modified or broken
  </acceptance_criteria>
  <done>
    - New RED describe blocks committed; CI/test run shows the new database tests failing as expected; existing tests still pass.
  </done>
</task>

<task type="tdd">
  <name>Task 3: Extend client.test.ts with RED specs for service + database CRUD client functions</name>
  <files>src/api/client.test.ts</files>
  <read_first>
    - src/api/client.test.ts (existing patterns for createProject/updateProject/deleteProject + deleteServer with query params)
    - src/api/client.ts (lines 313-341 for updateApplication/deleteApplication pattern; lines 343-361 for fetchService/fetchDatabase pattern)
    - docs/coolify_openapi.yaml (lines 6847-6961 POST /services; 6962-7152 PATCH/DELETE /services/{uuid}; 3124-3910 8 POST /databases/{engine}; 2712-2900 PATCH/DELETE /databases/{uuid})
  </read_first>
  <behavior>
    - createService(url, token, payload, verifySsl) -> POST /services with JSON body -> returns raw response
    - updateService(url, token, uuid, payload, verifySsl) -> PATCH /services/{uuid} with JSON body
    - deleteService(url, token, uuid, params, verifySsl) -> DELETE /services/{uuid} with query params { delete_configurations, delete_volumes, docker_cleanup, delete_connected_networks }
    - createPostgresqlDatabase(url, token, payload, verifySsl) -> POST /databases/postgresql
    - createMysqlDatabase -> POST /databases/mysql
    - createMariadbDatabase -> POST /databases/mariadb
    - createMongodbDatabase -> POST /databases/mongodb
    - createRedisDatabase -> POST /databases/redis
    - createClickhouseDatabase -> POST /databases/clickhouse
    - createDragonflyDatabase -> POST /databases/dragonfly
    - createKeydbDatabase -> POST /databases/keydb
    - updateDatabase(url, token, uuid, payload, verifySsl) -> PATCH /databases/{uuid} with JSON body
    - deleteDatabase(url, token, uuid, params, verifySsl) -> DELETE /databases/{uuid} with query params { delete_configurations, delete_volumes, docker_cleanup, delete_connected_networks }
    - All 13 functions route through createCoolifyClient + withMappedErrors
  </behavior>
  <action>
    Add RED specs to `src/api/client.test.ts` for each of the 13 new client functions. Each spec mocks the ofetch client (or createCoolifyClient) and asserts the correct HTTP method + path + body/query is used. Specs MUST fail RED at commit time because the functions are not yet exported from `src/api/client.ts` (plan 11-01 ships them).

    Use the existing `createProject`/`updateProject`/`deleteProject`/`deleteServer` test patterns as templates. For `deleteService` and `deleteDatabase`, assert the query params object includes all four flags (delete_configurations, delete_volumes, docker_cleanup, delete_connected_networks).

    Do NOT implement the client functions — only the tests. Tests must fail when run (import error or undefined export).
  </action>
  <verify>
    <automated>npx vitest run src/api/client.test.ts -t "(createService|updateService|deleteService|createPostgresqlDatabase|createMysqlDatabase|createMariadbDatabase|createMongodbDatabase|createRedisDatabase|createClickhouseDatabase|createDragonflyDatabase|createKeydbDatabase|updateDatabase|deleteDatabase)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createService" src/api/client.test.ts` returns at least 1
    - `grep -c "createPostgresqlDatabase" src/api/client.test.ts` returns at least 1
    - `grep -c "deleteDatabase" src/api/client.test.ts` returns at least 1
    - `npx vitest run src/api/client.test.ts` fails on the new specs (RED) — because the functions are not yet exported from client.ts
    - No existing client tests broken
  </acceptance_criteria>
  <done>
    - New RED client specs committed; CI/test run shows the new client tests failing as expected (undefined exports); existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file -> service.ts | RED scaffolds import symbols that will exist after 11-02/11-04; no trust boundary crossed at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-00-01 | Tampering | Test fixtures (mockService, mock conflicts) | low | accept | Test-only fixtures; no production code path. Real conflict-shape handling validated in 11-01/11-02 against OpenAPI spec. |
| T-11-SC | Tampering | npm installs | high | mitigate | No new packages in this plan — vitest already pinned. slopcheck not required. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/service.test.ts` — new describe blocks RED (failing as expected), existing tests GREEN
- `npm run build` (tsup) — must remain green (test file is type-checked; new mocks must match existing import shape)
- `grep -c "describe('service create'" src/mcp/tools/service.test.ts` — at least 1
- `grep -c "describe('service update'" src/mcp/tools/service.test.ts` — at least 1
- `grep -c "describe('service delete'" src/mcp/tools/service.test.ts` — at least 1
</verification>

<success_criteria>
- All 5 Phase 11 service requirements (SVC-06..SVC-10) have at least one failing (RED) test in service.test.ts
- D-18 ambiguous-match RED cases for service update+delete by name are present and failing
- D-06 decoded-compose RED cases for service.get/create/update responses are present and failing
- D-18 ambiguous-match RED cases for database update+delete by name are present and failing
- No existing test broken
- Tests are structured so plans 11-02/11-04/11-05 can flip them GREEN without rewrite
- Wave 0 contract satisfied (per 08-00 / 09-00 / 10-00 precedent)
</success_criteria>

<output>
Create `.planning/phases/11-service-database-crud/11-00-SUMMARY.md` when done.
</output>
