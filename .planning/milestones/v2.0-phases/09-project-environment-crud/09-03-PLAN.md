---
phase: 09-project-environment-crud
plan: 03
type: execute
wave: 2
depends_on: [09-00, 09-01]
files_modified:
  - src/mcp/tools/environment.ts
autonomous: true
requirements: [PROJ-04, PROJ-05]
must_haves:
  truths:
    - "environment tool exposes actions: list, get, create, delete, delete_preview per D-03 (no update — API has no PATCH)"
    - "list accepts project_uuid XOR project_name; name multi-match → COOLIFY_AMBIGUOUS_MATCH per D-12"
    - "get/delete accept uuid XOR name within the parent project scope; parent always required per D-13"
    - "create accepts project_uuid XOR project_name + name; duplicate env name → COOLIFY_409 with recovery hint per D-15"
    - "delete without confirm:true → COOLIFY_CONFIRM_REQUIRED, no DELETE call per D-05"
    - "delete on non-empty environment (has apps/services/dbs, INCLUDING children in deleting/destroying async state per RESEARCH pitfall 2) → COOLIFY_409 listing child_resource_uuids via /resources pre-check per D-06"
    - "delete on empty environment with confirm:true succeeds per PROJ-05"
    - "delete_preview returns { uuid, child_resources, would_delete } without deleting per D-08"
    - "Empty environment name on create → COOLIFY_422 (Zod min(1)) — specless probe edge: empty input rejected"
    - "Duplicate environment create is non-idempotent — second call gets COOLIFY_409 (D-15); no auto-overwrite / create-as-get"
    - "Parallel create same name → one 201, one 409 (Coolify server-side serialization); concurrency assumption documented"
    - "Delete on already-deleted environment → Coolify 404 → COOLIFY_404 surfaced; idempotency assumption documented"
  artifacts:
    - src/mcp/tools/environment.ts
  key_links:
    - "handleEnvironmentAction list → resolveProjectUuid → fetchEnvironments(projectUuid) per D-03/D-12"
    - "handleEnvironmentAction create → resolveProjectUuid → createEnvironment (POST /projects/{uuid}/environments) per D-12"
    - "handleEnvironmentAction delete → fetchResources emptiness pre-check → COOLIFY_409 with child_resource_uuids if non-empty per D-06"
  prohibitions:
    - statement: "No environment update action — Coolify API has no PATCH /projects/{uuid}/environments/{env} (PROHIB_ABSENT, CONTEXT scope fence)"
      status: enforced
      verification: "environmentActionSchema has no action:'update' variant; grep -c \"action: z.literal\\('update'\\)\" src/mcp/tools/environment.ts returns 0"
    - statement: "No cascade/force delete of non-empty environments — always hard-block with COOLIFY_409 per D-06 (scope fence)"
      status: enforced
      verification: "grep -c 'force' src/mcp/tools/environment.ts returns 0; delete handler throws COOLIFY_409 when child_resources.length > 0"
    - statement: "No app/service/db CRUD on environment tool — Phases 10-11 (scope fence)"
      status: enforced
      verification: "environmentActionSchema actions are exactly list/get/create/delete/delete_preview"
---

<objective>
Wave 2 environment MCP tool handler. Implements list/get/create/delete/delete_preview (no update — API gap) with project-scoped name resolution, duplicate-name 409, and child-resource emptiness pre-check. Flips 09-00 environment.test.ts from RED to GREEN.

Purpose: Second domain CRUD tool; environments are the immediate scope for every app/service/database created in Phases 10-13.
Output: src/mcp/tools/environment.ts (handler + Zod action schema + summary projection).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-project-environment-crud/09-CONTEXT.md
@.planning/phases/09-project-environment-crud/09-RESEARCH.md
@.planning/phases/09-project-environment-crud/09-PATTERNS.md
@src/mcp/tools/private_key.ts
@src/mcp/tools/shared-read-params.ts
@src/utils/project-lookup.ts
@src/api/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement environment.ts handler + schema</name>
  <files>src/mcp/tools/environment.ts</files>
  <read_first>
    - src/mcp/tools/private_key.ts (action schema + confirm gate + COOLIFY_409 pattern)
    - src/mcp/tools/shared-read-params.ts (sharedReadParamsSchema + rejectTableFormatOnFullProjection)
    - src/utils/project-lookup.ts (resolveProjectUuid + resolveEnvironmentUuid — added in 09-01)
    - src/api/client.ts (fetchEnvironments, fetchEnvironment, createEnvironment, deleteEnvironment, fetchResources — added in 09-01)
    - .planning/phases/09-project-environment-crud/09-CONTEXT.md (D-03, D-05..D-08, D-12, D-13, D-15)
    - src/mcp/tools/environment.test.ts (RED tests from 09-00 — must pass without edits)
  </read_first>
  <behavior>
    - list: resolveProjectUuid(project_uuid XOR project_name) per D-12; fetchEnvironments(projectUuid); summary { uuid, name, project_uuid, project_name } per D-03; paginate; buildReadResponse
    - get: resolveProjectUuid; resolveEnvironmentUuid(uuid XOR name, projectUuid) per D-13; fetchEnvironment(projectUuid, envNameOrUuid); summary or sanitized full
    - create: resolveProjectUuid(project_uuid XOR project_name) per D-12; Zod requires name min(1); createEnvironment(projectUuid, { name }); on API 409 (duplicate) → surface COOLIFY_409 with recovery hint per D-15; return { uuid, name, project_uuid } per PROJ-04
    - delete: validateDeleteConfirm → COOLIFY_CONFIRM_REQUIRED if confirm !== true per D-05; resolveProjectUuid; resolveEnvironmentUuid; fetchResources emptiness pre-check — filter resources where environment matches (resource.environment_id or resource.environment.uuid === env.id or env.uuid) AND resource is NOT in a terminal-deletion state (status NOT in ['deleting','destroying'] per RESEARCH pitfall 2 — async deletion in Coolify means a child in deleting/destroying state still counts as present and must block env delete until fully gone); if non-empty → COOLIFY_409 with data.child_resource_uuids per D-06; else deleteEnvironment; return { ok:true, uuid }
    - delete_preview: resolveProjectUuid; resolveEnvironmentUuid; fetchResources pre-check (same deleting/destroying inclusion); return { uuid, child_resources:[{uuid,name,type}], would_delete: child_resources.length === 0 } per D-08
    - No update action (API has no PATCH — prohibition)
  </behavior>
  <action>
Create src/mcp/tools/environment.ts mirroring private_key.ts. Import z from 'zod/v4', EnvConfig, buildReadResponse + paginateArray, CoolifyApiError + RECOVERY_HINTS + wrapMcpError + McpErrorResult, sharedReadParamsSchema + rejectTableFormatOnFullProjection, resolveProjection + sanitizeFullProjection. From ../../api/client.js: fetchEnvironments, fetchEnvironment, createEnvironment, deleteEnvironment, fetchResources, fetchProjects. From ../../utils/project-lookup.js: resolveProjectUuid, resolveEnvironmentUuid. Define mutationResponseParamsSchema per private_key.ts. Define action schemas: listActionSchema (action:'list', project_uuid?:string, project_name?:string, +sharedReadParamsSchema) with superRefine requiring project_uuid XOR project_name, getActionSchema (action:'get', project_uuid?:string, project_name?:string, uuid?:string, name?:string, +sharedReadParamsSchema) with superRefine requiring project_uuid XOR project_name AND (uuid XOR name), createActionSchema (action:'create', project_uuid?:string, project_name?:string, name:z.string().min(1), +mutationResponseParamsSchema) with superRefine requiring project_uuid XOR project_name, deleteActionSchema (action:'delete', project_uuid?:string, project_name?:string, uuid?:string, name?:string, confirm:z.boolean().default(false), +mutationResponseParamsSchema) with superRefines, deletePreviewActionSchema (action:'delete_preview', same shape as delete without confirm). Export environmentActionSchema = z.discriminatedUnion('action', [...]). NO update action (prohibition). Implement projectEnvironmentSummary(rawEnv, projectUuid, projectName) → { uuid, name, project_uuid, project_name }. Implement validateDeleteConfirm per private_key.ts. Implement findEnvironmentChildResources(env, projectUuid, envUuid) — fetchResources, filter resources whose environment_id or environment.uuid matches the resolved environment's id or uuid; INCLUDE resources whose status is 'deleting' or 'destroying' (per RESEARCH pitfall 2 — Coolify async deletion means these are still present and must block); return [{ uuid, name, type }]. Implement handleEnvironmentAction(args, env) with try/catch + wrapMcpError. In create: resolve projectUuid via resolveProjectUuid; call createEnvironment; if it throws CoolifyApiError with COOLIFY_409 (API 409 duplicate name) re-throw with D-15 recovery hint 'An environment with this name already exists in this project. Use the existing UUID or pick a new name.' In delete: validateDeleteConfirm; resolve projectUuid + envUuid; fetchResources; filter children; if children.length > 0 throw COOLIFY_409 with data.child_resource_uuids; else deleteEnvironment. In delete_preview: same child check, return { uuid, child_resources, would_delete }. Export isEnvironmentErrorResult. Pass all 09-00 environment.test.ts cases without editing tests.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/environment.test.ts 2>&1 | tail -5 | grep -qE 'passed|Tests.*passed' && npx tsc --noEmit src/mcp/tools/environment.ts 2>&1 | grep -c 'error TS' | grep -qE '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/mcp/tools/environment.ts exists with environmentActionSchema + handleEnvironmentAction + isEnvironmentErrorResult exports
    - environment.test.ts (09-00) flips GREEN — all ≥11 tests pass without edits
    - list with project_name resolves via resolveProjectUuid (D-12)
    - create with project_name resolves project then creates env (D-12)
    - create duplicate name surfaces COOLIFY_409 with recovery hint (D-15)
    - delete without confirm returns COOLIFY_CONFIRM_REQUIRED, no deleteEnvironment call (D-05)
    - delete on non-empty env (incl. children in deleting/destroying state) returns COOLIFY_409 with child_resource_uuids, no deleteEnvironment call (D-06 + RESEARCH pitfall 2)
    - get/delete by env name within project resolves via resolveEnvironmentUuid (D-13)
    - No 'force' field; no action:'update' variant (prohibitions)
    - tsc --noEmit on environment.ts reports 0 new errors
  </acceptance_criteria>
  <done>environment.ts handler ships; 09-00 environment.test.ts GREEN; PROJ-04, PROJ-05 + D-03, D-05..D-08, D-12, D-13, D-15 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→environment tool | untrusted Zod-validated args (project_uuid/name, env uuid/name) cross here |
| environment tool→Coolify API | bearer-token authenticated HTTP; no secrets in payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-03-T | Tampering | environmentActionSchema project_uuid XOR project_name + env uuid XOR name | medium | mitigate | Zod min(1) on env name; superRefine enforces both XOR constraints; .strict() rejects unknown fields |
| T-9-03-E | Elevation | delete without confirm | high | mitigate | validateDeleteConfirm throws COOLIFY_CONFIRM_REQUIRED before any DELETE call per D-05; no force bypass per D-06 |
| T-9-03-D | Denial | non-empty environment delete (incl. async deleting/destroying children) | high | mitigate | Client-side fetchResources pre-check yields COOLIFY_409 with child_resource_uuids per D-06 — prevents cascade and gives recovery path; includes resources in 'deleting'/'destroying' state so async deletion in Coolify cannot race past the gate (RESEARCH pitfall 2) |
| T-9-03-R | Repudiation | duplicate env name create | medium | mitigate | COOLIFY_409 with recovery hint surfaces existing-name conflict per D-15; no silent overwrite |
| T-9-03-I | Information disclosure | environment responses | low | accept | Environment objects carry no secrets (uuid/name/project_uuid/project_name only) |
| T-9-03-SC | Tampering | npm installs (none new) | low | accept | No new packages; reuses approved zod/ofetch |
</threat_model>

<verification>
- environment.test.ts GREEN (≥11 tests pass)
- tsc --noEmit on environment.ts clean
- npm test full suite green (no regressions)
- No update action; no force/cascade path exists
</verification>

<success_criteria>
- PROJ-04 (create environment), PROJ-05 (delete with confirm + 409 on non-empty) satisfied
- D-03, D-05, D-06, D-07 (env side), D-08, D-12, D-13, D-15 implemented
- 09-00 environment.test.ts GREEN without test edits
</success_criteria>

<output>
Create `.planning/phases/09-project-environment-crud/09-03-SUMMARY.md` when done
</output>
