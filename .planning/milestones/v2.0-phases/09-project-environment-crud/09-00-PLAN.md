---
phase: 09-project-environment-crud
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/project.test.ts
  - src/mcp/tools/environment.test.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05]
must_haves:
  truths:
    - "project.test.ts exists and fails at handler import (RED) covering list/get/create/update/delete/delete_preview per PROJ-01..03"
    - "environment.test.ts exists and fails at handler import (RED) covering list/get/create/delete/delete_preview per PROJ-04..05"
    - "Tests assert confirm gate (COOLIFY_CONFIRM_REQUIRED), non-empty 409 (COOLIFY_409 with child UUIDs), and initial_environment requirement per D-05..D-10"
    - "environment.test.ts includes an integration assertion (SC#4): after environment.create succeeds, resource.list type='environment' shows the new env — verified by mocking fetchProjects to return the post-create state and asserting the new env uuid appears in the list projection"
  artifacts:
    - src/mcp/tools/project.test.ts
    - src/mcp/tools/environment.test.ts
  key_links:
    - "project.test.ts imports handleProjectAction + projectActionSchema + isProjectErrorResult from ./project.js (not yet created → RED)"
    - "environment.test.ts imports handleEnvironmentAction + environmentActionSchema + isEnvironmentErrorResult from ./environment.js (not yet created → RED)"
  prohibitions:
    - statement: "No environment update action is tested — API has no PATCH (PROHIB_ABSENT)"
      status: enforced
      verification: "environment.test.ts contains no test for action:'update'"
    - statement: "No cascade/force delete tests — non-empty always hard-blocks (scope fence)"
      status: enforced
      verification: "no test passes force:true and expects success"
---

<objective>
Wave 0 RED test scaffolds for project + environment MCP tools. Tests fail at handler import (handlers do not exist yet); 09-02 and 09-03 flip them GREEN without test rewrites. Mirrors Phase 8 08-00 pattern.

Purpose: Lock the PROJ-01..05 + D-01..D-15 behavior contract before implementation so 09-02/09-03 implement against an unchanging test surface.
Output: Two RED test files (project.test.ts, environment.test.ts) that exercise every required action and error path.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-project-environment-crud/09-CONTEXT.md
@.planning/phases/09-project-environment-crud/09-RESEARCH.md
@.planning/phases/09-project-environment-crud/09-PATTERNS.md
@src/mcp/tools/private_key.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED project.test.ts scaffold</name>
  <files>src/mcp/tools/project.test.ts</files>
  <read_first>
    - src/mcp/tools/private_key.test.ts (mocking + test structure pattern)
    - .planning/phases/09-project-environment-crud/09-CONTEXT.md (D-01..D-11, D-14)
  </read_first>
  <behavior>
    - list: returns summary projection { uuid, name, description } per D-02
    - get by uuid: returns project fields per D-14
    - get by name (single match): resolves to uuid per D-14
    - get by name (multi-match): COOLIFY_AMBIGUOUS_MATCH, no API call per D-14
    - create with initial_environment='production': POST /projects, ensures production env exists, returns { project, environment, environments? } per D-09..D-11
    - create with initial_environment='staging': POST /projects, then POST /projects/{uuid}/environments { name:'staging' }, returns { project, environment:{ uuid, name:'staging' }, environments:[production, staging] } per D-10/D-11
    - create missing initial_environment (omitted): Zod rejects (no default) → COOLIFY_422 with recovery hint to ask user per D-09/D-10
    - create empty initial_environment (''): Zod rejects min(1) → COOLIFY_422 with recovery hint per D-09/D-10
    - create never auto-deletes the auto-spawned production env per D-09/Claude's Discretion
    - update name/description: PATCH /projects/{uuid}, response reflects new values per PROJ-02
    - delete without confirm: COOLIFY_CONFIRM_REQUIRED, no DELETE call per D-05
    - delete with confirm on empty project (zero environments): succeeds per D-07
    - delete with confirm on project with environments: COOLIFY_409 listing environment_uuids per D-07
    - delete_preview: returns { uuid, environment_uuids, would_delete } without DELETE per D-08
  </behavior>
  <action>
Create src/mcp/tools/project.test.ts mirroring private_key.test.ts structure. vi.mock('../../api/client.js') with the functions 09-01 will export: fetchProjects, fetchProject, createProject, updateProject, deleteProject, fetchEnvironments, fetchEnvironment, createEnvironment, deleteEnvironment, fetchResources. Import handleProjectAction, projectActionSchema, isProjectErrorResult from ./project.js (RED — file absent). Define testEnv, mockProject { uuid, name, description, environments:[{ uuid, name, id }] }, mockEnv. Write describe blocks per behavior list above using vi.mocked(...).mockResolvedValue. Tests must FAIL at import (module not found) — that is the RED state. Cite D-NN in test names. Per D-09/D-10: create test with initial_environment='staging' asserts createEnvironment called with name:'staging' and response.environment.name==='staging' and response.environments contains both production and staging. Per D-09/D-10: two create tests assert COOLIFY_422 with recovery hint — one omitting initial_environment, one passing initial_environment:'' (empty string); both must NOT call createProject (Zod rejects before any API call). Per D-07: delete on project whose fetchEnvironments returns [env1] throws COOLIFY_409 with data.environment_uuids=['env1']. Per D-14: get by name multi-match (fetchProjects returns 2 same-named) throws COOLIFY_AMBIGUOUS_MATCH. Do NOT write the handler. Do NOT add environment update tests (prohibited).
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/project.test.ts >/tmp/09-00-t1.log 2>&1 || { echo RED_OK; exit 0; }; echo GREEN_UNEXPECTED; exit 1</automated>
  </verify>
  <acceptance_criteria>
    - File src/mcp/tools/project.test.ts exists
    - vitest run on the file fails (RED) due to missing ./project.js module
    - At least 12 it() blocks covering list, get (uuid + name-single + name-multi), create (production + staging + missing-initial_environment-422 + empty-initial_environment-422), update, delete (no-confirm + empty + non-empty-409), delete_preview
    - No test for action:'update' on environment (prohibition)
    - No test expects force:true success (prohibition)
  </acceptance_criteria>
  <done>project.test.ts RED with ≥12 tests failing at handler import; covers PROJ-01, PROJ-02, PROJ-03 and D-01..D-11, D-14</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RED environment.test.ts scaffold</name>
  <files>src/mcp/tools/environment.test.ts</files>
  <read_first>
    - src/mcp/tools/private_key.test.ts (mocking + test structure pattern)
    - .planning/phases/09-project-environment-crud/09-CONTEXT.md (D-03, D-05..D-08, D-12, D-13, D-15)
  </read_first>
  <behavior>
    - list scoped by project_uuid: returns [{ uuid, name, project_uuid, project_name }] per D-03
    - list scoped by project_name (single match): resolves project, returns envs per D-03/D-12
    - list scoped by project_name (multi-match): COOLIFY_AMBIGUOUS_MATCH per D-12
    - get by uuid within project: returns environment fields per D-13
    - get by name within project: resolves via /projects/{uuid}/{name_or_uuid} per D-13
    - create with project_uuid + name: POST /projects/{uuid}/environments { name }, returns { uuid, name, project_uuid } per PROJ-04
    - create with project_name + name: resolves project, then creates env per D-12
    - create duplicate env name: COOLIFY_409 with recovery hint per D-15
    - delete without confirm: COOLIFY_CONFIRM_REQUIRED per D-05
    - delete with confirm on empty env (no child resources): succeeds per PROJ-05
    - delete with confirm on non-empty env (has apps/services/dbs): COOLIFY_409 listing child_resource_uuids per D-06
    - delete with confirm on env whose child resources include a resource with status 'deleting' or 'destroying': COOLIFY_409 listing child_resource_uuids per D-06 + RESEARCH pitfall 2 (pre-check must hard-block, not silently proceed)
    - delete_preview: returns { uuid, child_resources, would_delete } without DELETE per D-08
    - SC#4 integration: after environment.create returns { uuid:'env-new' }, a subsequent resource.list call with type='environment' (mocked fetchProjects returning the project with environments:[..., { uuid:'env-new', name:'staging' }]) surfaces 'env-new' in the projected list — verifies ROADMAP Success Criteria #4 (resource.list shows new env after create)
  </behavior>
  <action>
Create src/mcp/tools/environment.test.ts mirroring private_key.test.ts. vi.mock('../../api/client.js') with: fetchProjects, fetchProject, fetchEnvironments, fetchEnvironment, createEnvironment, deleteEnvironment, fetchResources. Import handleEnvironmentAction, environmentActionSchema, isEnvironmentErrorResult from ./environment.js (RED — file absent). Define testEnv, mockEnv { uuid:'env-1', name:'production', project_uuid:'proj-1' }, mockProject. Write describe blocks per behavior list. Per D-12: create test passes project_name (not uuid) and asserts resolveProjectUuid path. Per D-06: delete on non-empty env where fetchResources returns resources with environment_id matching env.id throws COOLIFY_409 with data.child_resource_uuids. Per RESEARCH pitfall 2: add a dedicated it() where fetchResources returns a child resource with status:'deleting' (and a second with status:'destroying') — handler must still hard-block with COOLIFY_409 listing child_resource_uuids, NOT silently proceed past the pre-check. Per D-15: create duplicate name — mock createEnvironment to reject with COOLIFY_409 (simulate API 409) — handler surfaces structured error. Per D-13: get by name within project uses /projects/{project_uuid}/{name_or_uuid} endpoint (fetchEnvironment with project_uuid + name). Do NOT write the handler. Do NOT add environment update tests (prohibited — no API PATCH).
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/environment.test.ts >/tmp/09-00-t2.log 2>&1 || { echo RED_OK; exit 0; }; echo GREEN_UNEXPECTED; exit 1</automated>
  </verify>
  <acceptance_criteria>
    - File src/mcp/tools/environment.test.ts exists
    - vitest run on the file fails (RED) due to missing ./environment.js module
    - At least 11 it() blocks covering list (project_uuid + project_name-single + project_name-multi), get (uuid + name), create (project_uuid + project_name + duplicate-409), delete (no-confirm + empty + non-empty-409 + non-empty-with-deleting/destroying-child-409), delete_preview, plus SC#4 resource.list-after-create integration assertion
    - No test for action:'update' (prohibition — API has no PATCH)
    - No test expects force:true or cascade success (prohibition)
  </acceptance_criteria>
  <done>environment.test.ts RED with ≥12 tests failing at handler import; covers PROJ-04, PROJ-05 and D-03, D-05..D-08, D-12, D-13, D-15 + RESEARCH pitfall 2 (deleting/destroying child pre-check)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→MCP tool | untrusted Zod-validated args cross here |
| MCP→Coolify API | bearer-token authenticated HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-00-SC | Tampering | npm installs (none new) | low | accept | No new packages introduced in 09-00; reuses approved zod/ofetch/vitest from Phase 8 audit |
</threat_model>

<verification>
- Both test files exist and are RED (fail at handler import)
- No handler files created (those are 09-02/09-03)
- npm test still passes for the rest of the suite (new files are isolated RED)
</verification>

<success_criteria>
- project.test.ts and environment.test.ts committed RED
- Test surface locks PROJ-01..05 + D-01..D-15 contract
- 09-02 and 09-03 can implement handlers without editing tests
</success_criteria>

<output>
Create `.planning/phases/09-project-environment-crud/09-00-SUMMARY.md` when done
</output>
