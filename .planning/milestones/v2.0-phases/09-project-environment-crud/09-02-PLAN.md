---
phase: 09-project-environment-crud
plan: 02
type: execute
wave: 2
depends_on: [09-00, 09-01]
files_modified:
  - src/mcp/tools/project.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03]
must_haves:
  truths:
    - "project tool exposes actions: list, get, create, update, delete, delete_preview per D-01/D-02"
    - "create requires initial_environment (string, min 1, NO default); missing or empty → COOLIFY_422 with recovery hint to ask user per D-09/D-10 (never silently assume 'production')"
    - "create returns { project, environment, environments? } where environment is the ensured initial_environment per D-11"
    - "create never auto-deletes the auto-spawned production env; if initial_environment != 'production', creates it additionally per D-09/Claude's Discretion"
    - "get/update/delete/delete_preview accept uuid XOR name; name multi-match → COOLIFY_AMBIGUOUS_MATCH, no mutation per D-14"
    - "delete without confirm:true → COOLIFY_CONFIRM_REQUIRED, no DELETE call per D-05"
    - "delete on project with remaining environments → COOLIFY_409 listing environment_uuids; no force/cascade per D-07"
    - "delete_preview returns { uuid, environment_uuids, would_delete } without deleting per D-08"
    - "Empty project name on create → COOLIFY_422 (Zod min(1)) — specless probe edge: empty input rejected"
    - "Duplicate project name on create → Coolify permits (no 409 in OpenAPI); second project created — non-idempotent, documented assumption"
    - "Project name equality is Coolify server-side UTF-8 codepoint comparison (PHP strings) — encoding assumption documented"
    - "Interrupted create (project created, env ensure not done) leaves project without desired env — agent re-runs create; concurrency assumption documented"
  artifacts:
    - src/mcp/tools/project.ts
  key_links:
    - "handleProjectAction → createProject (POST /projects) → fetchEnvironments → createEnvironment if initial_environment missing"
    - "handleProjectAction delete → fetchEnvironments emptiness pre-check → COOLIFY_409 if non-empty per D-07"
    - "handleProjectAction get/update/delete → resolveProjectUuid (project-lookup.ts) for name→uuid per D-14"
  prohibitions:
    - statement: "No cascade/force delete of non-empty projects — always hard-block with COOLIFY_409 per D-07 (scope fence)"
      status: enforced
      verification: "grep -c 'force' src/mcp/tools/project.ts returns 0; delete handler throws COOLIFY_409 when environments.length > 0"
    - statement: "No environment update action on project tool — environments are managed by the environment tool per D-01 (scope fence)"
      status: enforced
      verification: "projectActionSchema has no action:'update_environment' variant"
---

<objective>
Wave 2 project MCP tool handler. Implements list/get/create/update/delete/delete_preview with the two-stage delete model, initial_environment guarantee, and name-resolution ambiguity guard. Flips 09-00 project.test.ts from RED to GREEN.

Purpose: First of two domain CRUD tools that scope every app/service/database in later phases.
Output: src/mcp/tools/project.ts (handler + Zod action schema + summary projection).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-project-environment-crud/09-CONTEXT.md
@.planning/phases/09-project-environment-crud/09-RESEARCH.md
@.planning/phases/09-project-environment-crud/09-PATTERNS.md
@src/mcp/tools/private_key.ts
@src/mcp/tools/shared-read-params.ts
@src/utils/project-lookup.ts
@src/api/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement project.ts handler + schema</name>
  <files>src/mcp/tools/project.ts</files>
  <read_first>
    - src/mcp/tools/private_key.ts (action schema discriminatedUnion + validateDeleteConfirm + COOLIFY_409 pattern)
    - src/mcp/tools/shared-read-params.ts (sharedReadParamsSchema + rejectTableFormatOnFullProjection)
    - src/utils/project-lookup.ts (resolveProjectUuid — added in 09-01)
    - src/api/client.ts (createProject/updateProject/deleteProject/fetchEnvironments/fetchEnvironment/createEnvironment — added in 09-01)
    - .planning/phases/09-project-environment-crud/09-CONTEXT.md (D-01..D-11, D-14)
    - src/mcp/tools/project.test.ts (RED tests from 09-00 — must pass without edits)
  </read_first>
  <behavior>
    - list: fetchProjects → summary { uuid, name, description } per D-02; paginate via paginateArray; buildReadResponse
    - get: resolveProjection; rejectTableFormatOnFullProjection; resolveProjectUuid(uuid XOR name) per D-14; fetchProject; summary or sanitized full
    - create: Zod requires name (min 1) + initial_environment (string, min 1, NO default — required field per D-09/D-10); description optional; POST /projects; then fetchEnvironments; if initial_environment not in envs → createEnvironment(projectUuid, { name: initial_environment }); re-fetch envs; return { project:{ uuid, name, description }, environment:{ uuid, name: initial_environment }, environments: [...] } per D-09/D-10/D-11
    - create missing or empty initial_environment: Zod rejects (min 1, no default) → COOLIFY_422 with recovery hint 'Ask the user whether to default to production or use a custom environment name.' per D-09/D-10 (never silently assume 'production')
    - create never deletes the auto-spawned production env per D-09
    - update: resolveProjectUuid(uuid XOR name) per D-14; PATCH /projects/{uuid} with defined fields; return { uuid, name?, description? }
    - delete: validateDeleteConfirm → COOLIFY_CONFIRM_REQUIRED if confirm !== true per D-05; fetchEnvironments emptiness pre-check; if envs.length > 0 → COOLIFY_409 with data.environment_uuids per D-07; else deleteProject; return { ok:true, uuid }
    - delete_preview: resolveProjectUuid; fetchEnvironments; return { uuid, environment_uuids, would_delete: envs.length === 0 } per D-08
  </behavior>
  <action>
Create src/mcp/tools/project.ts mirroring private_key.ts structure. Import z from 'zod/v4', EnvConfig, buildReadResponse + paginateArray, CoolifyApiError + RECOVERY_HINTS + wrapMcpError + McpErrorResult, sharedReadParamsSchema + rejectTableFormatOnFullProjection, resolveProjection + sanitizeFullProjection, and from ../../api/client.js: fetchProjects, fetchProject, createProject, updateProject, deleteProject, fetchEnvironments, createEnvironment. From ../../utils/project-lookup.js: resolveProjectUuid. Define mutationResponseParamsSchema (format + max_chars) per private_key.ts. Define action schemas: listActionSchema (action:'list' + sharedReadParamsSchema), getActionSchema (action:'get', uuid?:string, name?:string, +sharedReadParamsSchema) with superRefine requiring uuid XOR name, createActionSchema (action:'create', name:z.string().min(1), initial_environment:z.string().min(1) (REQUIRED — NO .default() per D-09/D-10, missing/empty rejected by Zod → COOLIFY_422), description:z.string().optional(), +mutationResponseParamsSchema), updateActionSchema (action:'update', uuid?:string, name?:string, + new_name?:string, description?:string, +mutationResponseParamsSchema) with superRefine uuid XOR name, deleteActionSchema (action:'delete', uuid?:string, name?:string, confirm:z.boolean().default(false), +mutationResponseParamsSchema) with superRefine uuid XOR name, deletePreviewActionSchema (action:'delete_preview', uuid?:string, name?:string, +mutationResponseParamsSchema) with superRefine uuid XOR name. Export projectActionSchema = z.discriminatedUnion('action', [...]). Implement projectProjectSummary(raw) → { uuid, name, description? }. Implement validateDeleteConfirm per private_key.ts pattern (COOLIFY_CONFIRM_REQUIRED). Implement handleProjectAction(args, env) with try/catch + wrapMcpError. In create: after createProject returns { uuid }, call fetchEnvironments(projectUuid); find env with name === initial_environment; if not found call createEnvironment(projectUuid, { name: initial_environment }) and re-fetch envs; build response { project:{ uuid, name, description }, environment:{ uuid, name: initial_environment }, environments: envs.map(summary) }. In delete: after validateDeleteConfirm, call fetchEnvironments(projectUuid); if length > 0 throw COOLIFY_409 with data.environment_uuids and RECOVERY_HINTS.COOLIFY_409; else deleteProject. For get/update/delete/delete_preview: resolve projectUuid via resolveProjectUuid(parsed.uuid, parsed.name, env) when name provided. Export isProjectErrorResult. Pass all 09-00 project.test.ts cases without editing tests.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/project.test.ts 2>&1 | tail -5 | grep -qE 'passed|Tests.*passed' && npx tsc --noEmit src/mcp/tools/project.ts 2>&1 | grep -c 'error TS' | grep -qE '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/mcp/tools/project.ts exists with projectActionSchema + handleProjectAction + isProjectErrorResult exports
    - project.test.ts (09-00) flips GREEN — all ≥12 tests pass without edits
    - create with initial_environment='staging' calls createEnvironment and returns both production + staging in environments
    - create missing or empty initial_environment returns COOLIFY_422 with recovery hint (D-09/D-10 — no silent 'production' default)
    - delete without confirm returns COOLIFY_CONFIRM_REQUIRED, no deleteProject call
    - delete on project with environments returns COOLIFY_409 with environment_uuids, no deleteProject call
    - get/update/delete by name multi-match throws COOLIFY_AMBIGUOUS_MATCH
    - No 'force' field in any schema (prohibition)
    - No environment update action (prohibition)
    - tsc --noEmit on project.ts reports 0 new errors
  </acceptance_criteria>
  <done>project.ts handler ships; 09-00 project.test.ts GREEN; PROJ-01, PROJ-02, PROJ-03 + D-01..D-11, D-14 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→project tool | untrusted Zod-validated args (name, initial_environment, uuid) cross here |
| project tool→Coolify API | bearer-token authenticated HTTP; no secrets in payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-02-T | Tampering | createActionSchema name/initial_environment | medium | mitigate | Zod min(1) on name + initial_environment; .strict() on all schemas rejects unknown fields; superRefine enforces uuid XOR name on mutations |
| T-9-02-E | Elevation | delete without confirm | high | mitigate | validateDeleteConfirm throws COOLIFY_CONFIRM_REQUIRED before any DELETE call per D-05; no force bypass per D-07 |
| T-9-02-D | Denial | non-empty project delete | medium | mitigate | Client-side fetchEnvironments pre-check yields COOLIFY_409 with child UUIDs per D-07 — prevents accidental cascade and gives agent recovery path |
| T-9-02-I | Information disclosure | project responses | low | accept | Project objects carry no secrets (uuid/name/description only); sanitizeFullProjection still applied for full projection parity |
| T-9-02-SC | Tampering | npm installs (none new) | low | accept | No new packages; reuses approved zod/ofetch |
</threat_model>

<verification>
- project.test.ts GREEN (≥12 tests pass)
- tsc --noEmit on project.ts clean
- npm test full suite green (no regressions)
- No force/cascade path exists in the handler
</verification>

<success_criteria>
- PROJ-01 (create), PROJ-02 (update), PROJ-03 (delete with confirm + 409 on non-empty) satisfied
- D-01, D-02, D-05, D-07, D-08, D-09, D-10, D-11, D-14 implemented
- 09-00 project.test.ts GREEN without test edits
</success_criteria>

<output>
Create `.planning/phases/09-project-environment-crud/09-02-SUMMARY.md` when done
</output>
