---
phase: 07-distribution-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/index.ts
  - src/mcp/server.ts
  - tests/integration/docs-parity.test.ts
  - CONTRIBUTING.md
autonomous: true
requirements: [DIST-01, DIST-02]

must_haves:
  truths:
    - "npm pack --dry-run succeeds and the tarball contains only dist, .env.example, LICENSE, README.md, README.de.md (no src, no .planning, no scripts)"
    - "package.json name is awesome-coolify-mcp and bin.awesome-coolify-mcp points at ./dist/index.js"
    - "package.json has prepublishOnly: npm run build and NO prepare script"
    - "src/mcp/server.ts contains the literal `new McpServer({ name: 'awesome-coolify-mcp', version: '0.1.0' })` (source-literal handshake identity — asserted via regex on the source file, NOT a runtime stdio handshake requirement)"
    - "src/index.ts contains the fatal-hint prefix literal `[awesome-coolify-mcp]` (source-literal, asserted via readFileSync)"
    - "tests/integration/docs-parity.test.ts exists as a Wave 0 scaffold (RED ok — Plan 02 turns it GREEN)"
    - "CONTRIBUTING.md documents maintainer publish workflow (npm run build, npm pack --dry-run, npm publish --access public, credential prerequisites) with no .planning/ links and no .mcpb references (D-18: no Claude Desktop .mcpb packaging in Phase 7 — Claude Desktop = manual JSON / Pages configurator output only)"
  artifacts:
    - package.json
    - src/index.ts
    - src/mcp/server.ts
    - tests/integration/docs-parity.test.ts
    - CONTRIBUTING.md
  key_links:
    - "package.json bin.awesome-coolify-mcp → ./dist/index.js → src/index.ts shebang"
    - "package.json prepublishOnly → npm run build → tsup → dist/index.js"
    - "CONTRIBUTING.md → npm publish --access public → package.json publishConfig.access: public"
---

## Phase Goal

**As a** community user, **I want to** install the MCP via `npx -y awesome-coolify-mcp` and read a complete GitHub README, **so that** I can adopt the MCP without reading source code.

<objective>
Make the npm package publish-ready (DIST-01) and lay the Wave 0 docs-parity test scaffold that Plan 02 turns green.

Purpose: Package metadata, bin, build hooks, and tarball contents must be correct so `npm publish` (manual, post-phase) produces a working `npx -y awesome-coolify-mcp` install. The parity test scaffold enforces README.md ↔ README.de.md structural symmetry before Plan 02 rewrites both files.

Output: Updated `package.json`, `src/index.ts`, `src/mcp/server.ts`; new `tests/integration/docs-parity.test.ts` Wave 0 scaffold.
</objective>

<execution_context>
@/Users/puzzless/gstack/.cursor/gsd-core/workflows/execute-plan.md
@/Users/puzzless/gstack/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distribution-docs/07-CONTEXT.md
@.planning/phases/07-distribution-docs/07-RESEARCH.md
@.planning/phases/07-distribution-docs/07-VALIDATION.md
@package.json
@src/index.ts
@src/mcp/server.ts
@src/config/env.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 docs-parity test scaffold (RED)</name>
  <files>tests/integration/docs-parity.test.ts</files>
  <read_first>
    - tests/integration/mcp-schema-validation.test.ts (Vitest + beforeAll/afterAll pattern)
    - README.md (current section structure — H2 headings, tool table, safety wording)
    - README.de.md (current DE structure — to verify parity expectations)
    - src/mcp/server.ts (10 registered tools: system, meta, resource, diagnose, application, deployment, service, database, docs, emergency)
  </read_first>
  <behavior>
    - Test 1: README.md and README.de.md both exist and are non-empty
    - Test 2 (structural parity, NOT literal H2 strings): Both READMEs have the same H2 section COUNT, same H2 ORDER (by position index), and a canonical-slug map pairing EN↔DE titles position-by-position. The test ships a `CANONICAL_SECTIONS` array of `{ en, de }` title pairs (e.g. `{ en: 'Why awesome-coolify-mcp?', de: 'Warum awesome-coolify-mcp?' }`, `{ en: 'Quick start', de: 'Schnellstart' }`, `{ en: 'Install', de: 'Installation' }`, `{ en: 'Clients', de: 'Clients' }`, `{ en: 'Environment variables', de: 'Umgebungsvariablen' }`, `{ en: 'Tools (action-based)', de: 'Tools (aktionsbasiert)' }`, `{ en: 'Safety', de: 'Sicherheit' }`, `{ en: 'Structured errors', de: 'Strukturierte Fehler' }`, `{ en: 'Not in v1', de: 'Nicht in v1' }`, `{ en: 'Development', de: 'Entwicklung' }`, `{ en: 'License', de: 'Lizenz' }`). The test extracts H2 lines from each README in order, asserts `enH2.length === deH2.length === CANONICAL_SECTIONS.length`, and asserts `enH2[i] === CANONICAL_SECTIONS[i].en` and `deH2[i] === CANONICAL_SECTIONS[i].de` for every index. It MUST NOT compare EN H2 strings against DE H2 strings directly (translated headings are expected per D-11).
    - Test 3 (full action inventory, not just 10 tool names): Both READMEs contain a tool reference table covering all 10 registered tools AND every action literal from `src/mcp/tools/*.ts`. The test ships a `TOOL_ACTIONS` map (source of truth = `src/mcp/tools/*.ts` Zod `z.literal('...')` action literals): `system: [health, version, verify, infrastructure_overview]`, `meta: [version]`, `resource: [list, find]`, `diagnose: [app, server, scan]`, `application: [start, stop, restart, deploy, logs, get]`, `deployment: [list, get, cancel]`, `service: [start, stop, restart, deploy, get]`, `database: [start, stop, restart, get]`, `docs: [search]`, `emergency: [stop_all, redeploy_project, restart_project]`. For each tool, the test asserts the tool name AND every action literal for that tool appears in the README body. Asserts at least 32 total action occurrences per README (sum of action counts across all 10 tools).
    - Test 4 (Safety section, locale-aware heading): Both READMEs contain a Safety section whose H2 heading is either `## Safety` (EN) or `## Sicherheit` (DE). The test extracts the section body under that H2 (lines until the next `## `) and asserts the section body contains the literal `confirm` and the literal `reveal` for BOTH locales. Accept either heading per locale; do NOT require the EN heading string in the DE file.
    - Test 5: Neither README contains a link to .planning/ (negative grep: no occurrence of ".planning/" or "spike-findings" in README.md or README.de.md)
    - Test 6: Neither README contains stale placeholders "YOUR_ORG" or "LICENSE to be added" or the old `coolify-mcp` package name (only `awesome-coolify-mcp` allowed). The negative grep for the old name MUST use a word-boundary regex that does NOT match the `coolify-mcp` substring inside `awesome-coolify-mcp`: `/(?<![\w-])coolify-mcp(?![\w-])/g`. Sanity check: `'awesome-coolify-mcp'.match(/(?<![\w-])coolify-mcp(?![\w-])/g)` → `null` (0 matches); `'coolify-mcp'.match(/(?<![\w-])coolify-mcp(?![\w-])/g)` → `['coolify-mcp']` (1 match). A plain `/coolify-mcp/g` regex is FORBIDDEN — it matches the substring inside `awesome-coolify-mcp` and would fail on correct content.
  </behavior>
  <action>
    Create `tests/integration/docs-parity.test.ts` as a Vitest spec that reads README.md and README.de.md from the repo root via `node:fs.readFileSync` and asserts the behaviors above. Tests MUST fail RED against the current stale READMEs (Phase-1-only tool table, multi-instance-as-v2, .planning links, YOUR_ORG, coolify-mcp name, missing action literals) — that is the intended Wave 0 state. Use `describe('docs parity (Wave 0)', ...)` and individual `it(...)` blocks per behavior. Do NOT skip or `.todo` any test. Per D-11/D-13/D-14, Plan 02 rewrites both READMEs to make these tests GREEN. For Test 2, ship the `CANONICAL_SECTIONS` array inline in the test file (11 entries matching the 11 H2 sections Plan 02 will produce); extract H2 lines via `content.split('\n').filter(l => l.startsWith('## '))` and compare position-by-position against the canonical titles. For Test 3, ship the `TOOL_ACTIONS` map inline in the test file with the 32 action literals enumerated above; for each tool, assert the tool name and every action literal appears in the README body (case-sensitive, since action names are code identifiers). For Test 4, locate the Safety/Sicherheit section by scanning for a line equal to `## Safety` (EN) or `## Sicherheit` (DE), then slice lines until the next `## ` and assert `confirm` and `reveal` both appear in that slice. Do not gate on `.planning/` removal here — that is a negative assertion only (grep == 0). For Test 6, the old-name negative grep MUST use the word-boundary regex `/(?<![\w-])coolify-mcp(?![\w-])/g` (NOT plain `/coolify-mcp/g`) so the `coolify-mcp` substring inside `awesome-coolify-mcp` is NOT flagged. Assert `expect(content.match(/(?<![\w-])coolify-mcp(?![\w-])/g)||[]).toHaveLength(0)` for both READMEs. Use `import { readFileSync } from 'node:fs'` and `import { resolve } from 'node:path'` per the mcp-schema-validation.test.ts pattern. Reference decision IDs D-11 (parity, structural not literal), D-13 (no .planning links), D-14 (fix stale claims), D-09 (full action inventory) in test names.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run tests/integration/docs-parity.test.ts 2>&1 | tail -40</automated>
  </verify>
  <done>
    - File `tests/integration/docs-parity.test.ts` exists with 6 `it(...)` blocks covering the behaviors listed
    - `npx vitest run tests/integration/docs-parity.test.ts` exits non-zero (RED) against current stale READMEs
    - No test is skipped or marked `.todo`
    - Tests reference D-11, D-13, D-14 in their names
  </done>
</task>

<task type="auto">
  <name>Task 2: Update package.json to awesome-coolify-mcp publish-ready metadata + document maintainer publish workflow in CONTRIBUTING.md (D-01..D-08)</name>
  <files>package.json, CONTRIBUTING.md</files>
  <read_first>
    - package.json (current state — name=coolify-mcp, prepare script, files=[dist, .env.example])
    - .planning/phases/07-distribution-docs/07-CONTEXT.md (D-01 through D-08 locked decisions; D-01 publish workflow documentation)
    - .planning/phases/07-distribution-docs/07-RESEARCH.md (Code Examples → package.json publishing metadata setup)
    - LICENSE (verify MIT + clezcoding 2026 copyright before referencing)
    - .env.example (verify it exists before adding to files)
  </read_first>
  <action>
    Update `package.json` per D-01..D-08:
    - `name`: `awesome-coolify-mcp` (was `coolify-mcp`) per D-02
    - `version`: `0.1.0` (unchanged) per D-03
    - `description`: keep current MCP server description
    - `type`: `module` (unchanged)
    - `bin`: `{ "awesome-coolify-mcp": "./dist/index.js" }` per D-02
    - `scripts`: REMOVE `prepare` (D-04); ADD `"prepublishOnly": "npm run build"` (D-04); keep build/dev/start/mcp/test; keep higgsfield:fix and gsd:fix-verification dev scripts
    - `files`: `["dist", ".env.example", "LICENSE"]` per D-07 (README auto-included by npm)
    - `keywords`: sensible list including at least `coolify`, `mcp`, `model-context-protocol`, `ai`, `self-hosted`, `devops`, `deploy`, `cursor`, `claude` per D-08 (exact list at Claude discretion)
    - `license`: `MIT` per D-06
    - `engines`: `{ "node": ">=20" }` per D-06
    - `repository`: `{ "type": "git", "url": "https://github.com/clezcoding/awesome-coolify-mcp.git" }` per D-05
    - `bugs`: `{ "url": "https://github.com/clezcoding/awesome-coolify-mcp/issues" }` per D-05
    - `homepage`: `https://github.com/clezcoding/awesome-coolify-mcp#readme` per D-05
    - `publishConfig`: `{ "access": "public" }` (Claude discretion per CONTEXT.md)
    - Keep existing dependencies and devDependencies unchanged
    - Do NOT add new dependencies — Package Legitimacy Audit says none needed

    Then create `CONTRIBUTING.md` at repo root documenting the maintainer publish workflow per D-01. Content (English, community-facing — NO `.planning/` or `spike-findings` links per D-13):
    - `## Contributing` — short intro: clone, `npm install`, `npm test`, `npm run build` before opening a PR
    - `## Development` — `npm run dev` / `npm run start` for local MCP stdio; logs go to stderr only
    - `## Publishing a release` (maintainer-only) — explicit ordered steps:
      1. Prerequisites: an npm account with 2FA enabled that is a member of the `clezcoding` org (or has publish rights to `awesome-coolify-mcp`); `npm login` completed locally; `node >=20` installed; `git remote -v` shows `https://github.com/clezcoding/awesome-coolify-mcp.git` as origin
      2. `npm run build` — produces `dist/index.js` via tsup
      3. `npm pack --dry-run` — verify the tarball contains ONLY `dist/`, `.env.example`, `LICENSE`, `README.md`, `README.de.md`, `package.json` (no `src/`, `scripts/`, `.planning/`, `tests/`, `.cursor/`, `docs/`)
      4. `npm publish --access public` — `prepublishOnly` re-runs `npm run build` automatically (per D-04); the `--access public` flag matches `publishConfig.access: public`
      5. Post-publish: tag the release on git (`git tag v0.1.0 && git push --tags`), open a GitHub Release, link the npm package URL
    - `## Version bump policy` — semver: patch for fixes, minor for new tools/actions, major for breaking schema changes; bump `package.json` `version` before publishing
    - `## Code of conduct` — short pointer to be excellent; no external links to internal planning docs
    Branding: `awesome-coolify-mcp` (not `coolify-mcp`). Do NOT link `.planning/` or `spike-findings` anywhere. Do NOT document `instances.json` as shipped per D-10. Do NOT document Claude Desktop `.mcpb` packaging per D-18 — Claude Desktop = manual JSON / Pages configurator output only.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && node -e "const p=require('./package.json'); console.assert(p.name==='awesome-coolify-mcp','name'); console.assert(p.bin['awesome-coolify-mcp']==='./dist/index.js','bin'); console.assert(p.scripts.prepublishOnly==='npm run build','prepublishOnly'); console.assert(!p.scripts.prepare,'prepare removed'); console.assert(p.files.includes('LICENSE'),'LICENSE in files'); console.assert(p.files.includes('.env.example'),'.env.example in files'); console.assert(p.license==='MIT','MIT'); console.assert(p.engines.node==='>=20','engines'); console.assert(p.repository.url.includes('clezcoding/awesome-coolify-mcp'),'repo'); console.log('package.json OK')" && test -f CONTRIBUTING.md && echo 'CONTRIBUTING.md exists' && node -e "const c=require('fs').readFileSync('CONTRIBUTING.md','utf8'); const assert=(cond,msg)=>{if(!cond){console.error('FAIL: '+msg);process.exit(1)}}; assert(c.includes('npm run build'),'build step'); assert(c.includes('npm pack --dry-run'),'pack dry-run step'); assert(c.includes('npm publish --access public'),'publish step'); assert(c.includes('npm login'),'credential prerequisite'); assert(!c.includes('.planning/'),'no .planning links'); assert(!c.includes('spike-findings'),'no spike-findings links'); assert(!c.includes('instances.json'),'no instances.json'); assert(!c.includes('.mcpb'),'no .mcpb (D-18)'); assert((c.match(/(?<![\w-])coolify-mcp(?![\w-])/g)||[]).length===0,'no old coolify-mcp name (word-boundary regex, does not match substring inside awesome-coolify-mcp)'); assert(c.includes('awesome-coolify-mcp'),'new name present'); console.log('CONTRIBUTING.md OK')"</automated>
  </verify>
  <done>
    - `package.json` name is `awesome-coolify-mcp`
    - `bin.awesome-coolify-mcp` → `./dist/index.js`
    - `prepublishOnly: npm run build` present; `prepare` script removed
    - `files` includes `dist`, `.env.example`, `LICENSE`
    - `license: MIT`, `engines.node: >=20`
    - `repository`/`bugs`/`homepage` point at clezcoding/awesome-coolify-mcp
    - `publishConfig.access: public` present
    - `CONTRIBUTING.md` exists at repo root
    - CONTRIBUTING.md documents `npm run build`, `npm pack --dry-run`, `npm publish --access public` in order
    - CONTRIBUTING.md documents credential prerequisites (`npm login`, npm account with publish rights, node >=20)
    - No `.planning/` or `spike-findings` references in CONTRIBUTING.md per D-13
    - No `instances.json` references per D-10
    - No `.mcpb` references per D-18
    - CONTRIBUTING.md uses `awesome-coolify-mcp` branding (not `coolify-mcp`)
    - Verify command prints `package.json OK` and `CONTRIBUTING.md OK`
  </done>
</task>

<task type="auto">
  <name>Task 3: Update server name + fatal hint wording; verify npm pack --dry-run green</name>
  <files>src/mcp/server.ts, src/index.ts</files>
  <read_first>
    - src/mcp/server.ts (line 403: `new McpServer({ name: 'coolify-mcp', version: '0.1.0' })`)
    - src/index.ts (line 73: `[coolify-mcp]` fatal hint string)
    - src/config/env.ts (COOLIFY_ENV_KEYS + formatEnvLoadHint — env contract to document)
    - package.json (after Task 2 — verify bin name before sign-off)
  </read_first>
  <action>
    Update `src/mcp/server.ts` line 403: change `name: 'coolify-mcp'` to `name: 'awesome-coolify-mcp'` (keep `version: '0.1.0'`). Update `src/index.ts` fatal hint prefix from `[coolify-mcp]` to `[awesome-coolify-mcp]`. Do NOT change any logic, imports, or other strings — these are the only two stale identity references in src/. Then run `npm run build` (tsup) and `npm pack --dry-run` and verify the tarball file list contains ONLY: `package.json`, `README.md`, `README.de.md`, `LICENSE`, `.env.example`, `dist/index.js` (and any dist sourcemap if tsup emits one). The tarball MUST NOT contain `src/`, `scripts/`, `.planning/`, `tests/`, `.cursor/`, or `docs/`. If `npm pack --dry-run` lists any forbidden path, tighten `files` in package.json and re-run. Per D-01, do NOT run `npm publish` — dry-run is the phase gate.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npm run build >/dev/null 2>&1 && npm pack --dry-run 2>&1 | tee /tmp/pack.out >/dev/null && echo '--- tarball file list:' && grep -E '^(npm notice|package\.json|README|LICENSE|\.env\.example|dist/)' /tmp/pack.out | head -30 && echo '--- forbidden-path check:' && if grep -E '(^|/)(src|scripts|\.planning|tests|\.cursor|docs)/' /tmp/pack.out | grep -v 'npm notice'; then echo 'FAIL: forbidden path present in tarball'; exit 1; else echo 'OK: no forbidden paths'; fi && echo '--- server identity check:' && node -e "const assert=(cond,msg)=>{if(!cond){console.error('FAIL: '+msg);process.exit(1)}}; const s=require('fs').readFileSync('src/mcp/server.ts','utf8'); const m=s.match(/new McpServer\(\{\s*name:\s*'([^']+)',\s*version:\s*'([^']+)'/); assert(m,'McpServer name/version literal found in server.ts'); assert(m[1]==='awesome-coolify-mcp','server name === awesome-coolify-mcp, got '+m[1]); assert(m[2]==='0.1.0','server version === 0.1.0, got '+m[2]); const idx=require('fs').readFileSync('src/index.ts','utf8'); assert(idx.includes('[awesome-coolify-mcp]'),'src/index.ts contains fatal-hint prefix literal [awesome-coolify-mcp]'); assert(!/(?<![\w-])coolify-mcp(?![\w-])/.test(idx),'src/index.ts has no old coolify-mcp name (word-boundary regex)'); const p=require('./package.json'); assert(p.name==='awesome-coolify-mcp','package name === awesome-coolify-mcp, got '+p.name); assert(p.version==='0.1.0','package version === 0.1.0, got '+p.version); console.log('server identity OK: '+m[1]+'@'+m[2]+'; fatal hint OK: [awesome-coolify-mcp]')"</automated>
  </verify>
  <done>
    - `src/mcp/server.ts` McpServer name is `awesome-coolify-mcp` and version is `0.1.0` (asserted in verify via regex on the `new McpServer({...})` literal)
    - `src/index.ts` fatal hint prefix is `[awesome-coolify-mcp]` (asserted in verify via `readFileSync('src/index.ts').includes('[awesome-coolify-mcp]')`)
    - `npm run build` exits 0 and produces `dist/index.js`
    - `npm pack --dry-run` exits 0 and lists only allowed files (dist, .env.example, LICENSE, README.md, README.de.md, package.json)
    - No `src/`, `scripts/`, `.planning/`, `tests/`, `.cursor/`, `docs/` in the tarball
    - Verify command prints `server identity OK: awesome-coolify-mcp@0.1.0; fatal hint OK: [awesome-coolify-mcp]`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry ← package tarball | Tarball published by maintainer; consumers run `npx -y awesome-coolify-mcp` which executes `dist/index.js` with their COOLIFY_TOKEN in env |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `files` allowlist in package.json | high | mitigate | Set `files: ["dist", ".env.example", "LICENSE"]` so npm tarball excludes `src/`, `scripts/`, `.planning/`, `tests/`, `.cursor/`, `docs/` — prevents internal planning docs and source from leaking to npm consumers. Verified via `npm pack --dry-run` in Task 3. |
| T-07-02 | Tampering | `prepublishOnly` build hook | medium | mitigate | Replace `prepare` (auto-build on every install, slow + flaky) with `prepublishOnly: npm run build` so the published tarball always ships a fresh `dist/` built from current source — no stale build ships to registry. Per D-04. |
| T-07-03 | Spoofing | `repository`/`bugs`/`homepage` URLs | medium | mitigate | Point all three at `https://github.com/clezcoding/awesome-coolify-mcp` (matches existing git remote) so users filing issues reach the real maintainer, not a typosquatter. Per D-05. |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm pack --dry-run` exits 0 and tarball contains only allowed files
- `npx vitest run tests/integration/docs-parity.test.ts` is RED (Wave 0 scaffold — Plan 02 turns GREEN)
- `node -e "require('./package.json')"` parses JSON cleanly
- `git grep -nP '(?<![\w-])coolify-mcp(?![\w-])' -- package.json src/index.ts src/mcp/server.ts` returns zero matches after Task 2+3 (only `awesome-coolify-mcp` remains). The `-P` flag enables PCRE so the lookbehind/lookahead word boundaries work; a plain `git grep -n 'coolify-mcp'` is FORBIDDEN because it matches the substring inside `awesome-coolify-mcp` and would report false positives on correct content.
- `src/index.ts` contains the literal `[awesome-coolify-mcp]` fatal-hint prefix (asserted via `readFileSync('src/index.ts').includes('[awesome-coolify-mcp]')` in Task 3 verify)
</verification>

<success_criteria>
- Package is publish-ready: `npm pack --dry-run` green, tarball minimal, bin name correct
- Wave 0 docs-parity test scaffold exists and is RED (Plan 02 will turn it GREEN)
- Server handshake identity is `awesome-coolify-mcp` v0.1.0 as a source literal in `src/mcp/server.ts` (asserted via regex on the `new McpServer({...})` literal — NOT a runtime stdio handshake requirement)
- `src/index.ts` fatal-hint prefix is `[awesome-coolify-mcp]` (source-literal, asserted via readFileSync)
- No stale `coolify-mcp` identity remains in package.json, src/index.ts, or src/mcp/server.ts
- All D-01..D-08 locked decisions implemented
</success_criteria>

<output>
Create `.planning/phases/07-distribution-docs/07-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `package.json` — updated name (`awesome-coolify-mcp`), bin, scripts (`prepublishOnly` added, `prepare` removed), files (LICENSE added), license, engines, repository/bugs/homepage, keywords, publishConfig
- `src/mcp/server.ts` — McpServer name updated to `awesome-coolify-mcp`
- `src/index.ts` — fatal hint prefix updated to `[awesome-coolify-mcp]`
- `tests/integration/docs-parity.test.ts` — Wave 0 Vitest scaffold (6 RED tests: structural parity via canonical-slug map, full 32-action inventory, locale-aware Safety section, no .planning links, no stale placeholders)
- `CONTRIBUTING.md` — maintainer publish workflow (npm run build → npm pack --dry-run → npm publish --access public, credential prerequisites, version bump policy); no .planning/ links
- `dist/index.js` — rebuilt via `npm run build` (tsup output, gitignored)
- `awesome-coolify-mcp-0.1.0.tgz` — `npm pack` artifact (dry-run only; gitignored)
