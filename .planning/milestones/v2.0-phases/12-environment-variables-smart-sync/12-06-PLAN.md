---
phase: 12-environment-variables-smart-sync
plan: 06
type: execute
wave: 4
depends_on:
  - 12-02
  - 12-03
  - 12-04
  - 12-05
files_modified:
  - src/mcp/server.ts
  - README.md
  - README.de.md
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04
  - ENV-05
  - ENV-06
must_haves:
  truths:
    - "src/mcp/server.ts tool descriptions for application, service, database mention the envs:* actions (descriptions live in server.ts registerTool calls, NOT in src/mcp/tools/{application,service,database}.ts)"
    - "README.md and README.de.md action lists include envs:list/get/create/update/delete/bulk-update on all three tools and envs:sync on application"
    - "README documents the confirm gate requirement on envs:delete, envs:bulk-update, envs:sync apply, envs:sync prune"
    - "README documents the reveal policy (default masked, reveal:true opt-in, agent must ask human first)"
    - "README documents the database is_preview omission (Pitfall 1, D-16)"
  artifacts:
    - "src/mcp/server.ts (application/service/database tool description strings updated — descriptions live here, NOT in src/mcp/tools/{application,service,database}.ts which only hold schemas + handlers)"
    - "README.md (action lists + env behavior docs)"
    - "README.de.md (German action lists + env behavior docs)"
  key_links:
    - "server.ts descriptions reference the exact envs:* action strings dispatched in 12-02/03/04/05"
    - "README action lists match server.ts descriptions"
---

## Artifacts this phase produces

Modified symbols / content (no new files; updates to existing files):
- `src/mcp/server.ts` application tool `description` string (around line 243-245) — extended to advertise `envs:list`, `envs:get`, `envs:create`, `envs:update`, `envs:delete`, `envs:bulk-update`, `envs:sync` (7 actions). NOTE: tool descriptions live in `src/mcp/server.ts` (the `registerTool('application', ...)` / `registerTool('service', ...)` / `registerTool('database', ...)` calls), NOT in `src/mcp/tools/{application,service,database}.ts` — those modules hold schemas + handlers only.
- `src/mcp/server.ts` service tool `description` string (around line 339-341) — extended to advertise `envs:list`, `envs:get`, `envs:create`, `envs:update`, `envs:delete`, `envs:bulk-update` (6 actions, no sync per D-09)
- `src/mcp/server.ts` database tool `description` string (around line 370-372) — extended to advertise the 6 envs:* actions (no sync, no `is_preview` per Pitfall 1 / D-16)
- `README.md` — new "Environment Variables" section + per-tool action tables + confirm/reveal/sync semantics docs
- `README.de.md` — German mirror of the README.md additions

No new exports. No new error codes. No production-code changes (description strings only).

<objective>
Update MCP tool descriptions in src/mcp/server.ts and the bilingual README files to advertise the envs:* actions shipped in 12-02..12-05. This is the discoverability layer — without it, the agent cannot know envs:* actions exist.

Purpose: Close the loop between handler implementation (12-02..05) and agent-facing tool descriptions. Documentation parity (EN + DE) per project convention.
Output: Updated src/mcp/server.ts, README.md, README.de.md. No new tests required — this is description/docs parity only; existing tests remain GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md
@.planning/phases/12-environment-variables-smart-sync/COVERAGE.md
@.planning/phases/12-environment-variables-smart-sync/12-02-SUMMARY.md
@.planning/phases/12-environment-variables-smart-sync/12-03-SUMMARY.md
@.planning/phases/12-environment-variables-smart-sync/12-04-SUMMARY.md
@.planning/phases/12-environment-variables-smart-sync/12-05-SUMMARY.md
@src/mcp/server.ts
@src/mcp/tools/application.ts
@src/mcp/tools/service.ts
@src/mcp/tools/database.ts
@README.md
@README.de.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update application/service/database tool descriptions in src/mcp/server.ts</name>
  <read_first>
    - src/mcp/server.ts (existing tool registration, description strings for application/service/database)
    - src/mcp/tools/application.ts (12-02 + 12-05 final action union — confirm envs:list/get/create/update/delete/bulk-update/sync)
    - src/mcp/tools/service.ts (12-03 final action union — envs:list/get/create/update/delete/bulk-update, no sync)
    - src/mcp/tools/database.ts (12-04 final action union — envs:list/get/create/update/delete/bulk-update, no sync, no is_preview)
    - .planning/phases/12-environment-variables-smart-sync/COVERAGE.md (capability matrix)
  </read_first>
  <files>src/mcp/server.ts</files>
  <action>
    Update the description strings passed to the application, service, and database tool registrations in src/mcp/server.ts (the `registerTool('application', ...)`, `registerTool('service', ...)`, and `registerTool('database', ...)` calls — descriptions live in server.ts, NOT in src/mcp/tools/{application,service,database}.ts which only hold schemas + handlers). For application, list envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update, envs:sync. For service, list envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update (no sync). For database, list envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update and note that is_preview is not supported for database envs. Mention confirm requirement on envs:delete, envs:bulk-update, and envs:sync (apply and prune). Mention reveal policy (default masked, reveal:true opt-in). Do NOT change tool names, input schemas, or handlers — only the description string literals in server.ts. Run a narrow smoke verify (the application/service/database + server tool tests only) to confirm no regression in tool registration; do NOT run the full vitest suite for this docs-only change.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/application.test.ts src/mcp/tools/service.test.ts src/mcp/tools/database.test.ts src/mcp/server.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "envs:sync" src/mcp/server.ts` returns at least 1 (application description mentions sync)
    - `grep -cE "envs:(list|get|create|update|delete|bulk-update)" src/mcp/server.ts` returns at least 18 (6 actions × 3 tool descriptions; application adds +1 for envs:sync on top)
    - `grep -c "is_preview" src/mcp/server.ts` returns at least 1 (database description notes is_preview omission per Pitfall 1 / D-16)
    - `grep -cE "envs:sync" src/mcp/server.ts` count in service/database description regions is 0 (D-09 app-only) — verify via `grep -n "envs:sync" src/mcp/server.ts` showing all matches cluster near the application tool registration (around line 243), none near service (339) or database (370)
    - `grep -c "confirm" src/mcp/server.ts` returns at least 3 (confirm gate documented in all three tool descriptions)
    - `grep -c "reveal" src/mcp/server.ts` returns at least 3 (reveal policy documented in all three tool descriptions)
    - `npx vitest run src/mcp/tools/application.test.ts src/mcp/tools/service.test.ts src/mcp/tools/database.test.ts src/mcp/server.test.ts` exits 0 (smoke verify — narrow scope, not full suite)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - application tool description in src/mcp/server.ts lists all seven envs:* actions including sync
    - service tool description in src/mcp/server.ts lists six envs:* actions (no sync)
    - database tool description in src/mcp/server.ts lists six envs:* actions (no sync) and notes is_preview omission
    - Confirm and reveal policies mentioned in all three descriptions in server.ts
    - src/mcp/tools/{application,service,database}.ts untouched (descriptions do NOT live there)
    - Narrow smoke verify GREEN (no regression in tool registration)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and README.de.md action lists and env behavior docs</name>
  <read_first>
    - README.md (existing action lists for application/service/database, existing flag/confirm/reveal docs)
    - README.de.md (German mirror)
    - .planning/phases/12-environment-variables-smart-sync/COVERAGE.md (capability matrix, confirm gate matrix, reveal policy, flag compatibility matrix)
    - .planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md (D-01..D-16)
  </read_first>
  <files>README.md, README.de.md</files>
  <action>
    Update README.md and README.de.md action tables for application, service, and database to include the envs:* actions shipped in 12-02..05. Add a new section (or extend the existing env section if present) documenting: (1) the envs:* action list per tool, (2) confirm gate requirements (envs:delete, envs:bulk-update, envs:sync apply, envs:sync prune), (3) reveal policy (default masked, reveal:true opt-in, agent must ask human first), (4) database is_preview omission (Pitfall 1, D-16), (5) envs:sync semantics (XOR env_file|env_content, dry_run default false = apply per D-06, prune, conflict_policy overwrite|keep_remote|abort per D-08, app-only per D-09; apply with conflicts and no conflict_policy → COOLIFY_CONFIRM_REQUIRED + ask-human hint). Mirror the German content in README.de.md. Keep both files in sync. Do NOT change any other sections. Run a narrow smoke verify (the docs-parity test only) to confirm no regression; do NOT run the full vitest suite for this docs-only change.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run tests/integration/docs-parity.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "envs:sync" README.md` returns at least 1 (sync documented)
    - `grep -c "envs:sync" README.de.md` returns at least 1 (German mirror)
    - `grep -cE "envs:(list|get|create|update|delete|bulk-update)" README.md` returns at least 18 (6 actions × 3 tools)
    - `grep -cE "envs:(list|get|create|update|delete|bulk-update)" README.de.md` returns at least 18
    - `grep -c "is_preview" README.md` returns at least 1 (Pitfall 1 / D-16 documented)
    - `grep -c "is_preview" README.de.md` returns at least 1
    - `grep -cE "conflict_policy|overwrite|keep_remote|abort" README.md` returns at least 4 (D-08 documented)
    - `grep -cE "conflict_policy|overwrite|keep_remote|abort" README.de.md` returns at least 4
    - `grep -c "dry_run" README.md` returns at least 1 (D-06 documented)
    - `npx vitest run tests/integration/docs-parity.test.ts` exits 0 (smoke verify — narrow scope, not full suite)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - README.md action lists include all envs:* actions per tool
    - README.md documents confirm gates, reveal policy, database is_preview omission, sync semantics (D-05..D-08, D-12, D-06 dry_run default false)
    - README.de.md mirrors README.md content in German
    - Both files in sync
    - Narrow smoke verify GREEN (no regression in docs-parity)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool description → agent | Description text is the agent's source of truth for available actions; misleading descriptions cause agent to attempt unsupported actions or miss supported ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06-01 | Information Disclosure | README examples include real-looking secrets | low | mitigate | Use placeholder values (e.g. `***`, `EXAMPLE_VALUE`) in all README examples; never real tokens. |
| T-12-06-02 | Tampering | description lists an action the handler does not implement | medium | mitigate | Cross-reference 12-02/03/04/05 SUMMARYs to ensure description action lists match the actual action unions. |
</threat_model>

<verification>
- server.ts descriptions list envs:* actions accurately per tool
- README.md and README.de.md action lists match server.ts
- Confirm, reveal, is_preview omission, and sync semantics (D-05..D-08, D-12, D-06 dry_run default false) documented in both languages
- Narrow smoke verify GREEN (no full vitest suite for docs-only changes)
</verification>

<success_criteria>
- Tool descriptions advertise envs:* actions
- Bilingual README documents env behavior, confirm gates, reveal policy, is_preview omission, sync semantics (D-05..D-08, D-12, D-06)
- Narrow smoke verify GREEN (no full vitest suite for docs-only changes)
- No test regression
</success_criteria>

<output>
Create `.planning/phases/12-environment-variables-smart-sync/12-06-SUMMARY.md` when done
</output>
