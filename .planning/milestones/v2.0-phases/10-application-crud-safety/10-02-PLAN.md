---
phase: 10-application-crud-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 10-00
  - 10-01
files_modified:
  - src/mcp/tools/application.ts
autonomous: true
requirements:
  - APP-12
  - APP-13
  - APP-14
  - APP-15
  - APP-16
  - APP-20
  - APP-21
  - SAF-03
must_haves:
  truths:
    - "applicationActionSchema discriminatedUnion includes a 'create' branch keyed by source_type discriminator with five variants: public_git, private_deploy_key, private_github_app, dockerfile, dockerimage (D-01)"
    - "create schema requires project_uuid XOR project_name plus environment_name or environment_uuid (D-02); missing both project and environment identifiers -> COOLIFY_VALIDATION_ERROR before any API call"
    - "create schema requires server_uuid (D-22); private_deploy_key requires private_key_uuid UUID (D-03); private_github_app requires github_app_uuid UUID (D-03)"
    - "create schema rejects build_pack='dockercompose' with COOLIFY_VALIDATION_ERROR and a hint pointing to service.create (D-04)"
    - "instant_deploy defaults to false (D-05); when true, handler is fire-and-forget — returns app UUID + deploy queued status, no wait/poll on create (D-06)"
    - "on instant_deploy queue failure, handler returns soft success ok:true with app UUID and deploy.status='failed_to_queue' plus recovery hints — no auto-rollback/delete (D-08)"
    - "HTTP 409 from any create POST maps to COOLIFY_409 with data.conflicts passthrough and a force_domain_override:true recovery hint (D-09, D-12, APP-21 409 path)"
    - "D-11: force_domain_override alone gates domain override — no extra confirm:true required (confirm remains delete-only / SAF-01)"
    - "create with force_domain_override:true on a non-conflicting payload passes the flag through to the create* client body and returns success (no COOLIFY_409) — APP-21 happy-path override, ROADMAP criterion 4"
    - "D-20: Zod validates create payload before any API call (SAF-03); malformed payloads never reach the API client"
    - "create response includes follow-up hints: logs_available-style hint when deployment_uuid is known, plus hints for deployment.get and application.deploy wait:true (D-07)"
    - "npx vitest run src/mcp/tools/application.test.ts -t 'application create' is GREEN — 10-00 RED scaffolds flipped green"
  artifacts:
    - "src/mcp/tools/application.ts (extended with createActionSchema + handleApplicationCreate)"
  key_links:
    - "Imports the 5 new create* client functions from ../../api/client.js (added in 10-01)"
    - "Imports COOLIFY_VALIDATION_ERROR + RECOVERY_HINTS + CoolifyApiError + wrapMcpError from ../../utils/errors.js (10-01 extended)"
    - "Imports logsAvailableHint from ../../utils/diagnose-hints.js for D-07 follow-up hints"
    - "Imports triggerDeploy + extractDeploymentUuid (existing) for the instant_deploy fire-and-forget path"
---

<objective>
Wave 2 create handler: 5 source types + instant_deploy + 409 + Zod schemas.

Purpose: Implement the `create` action on the `application` tool — the canonical reference for Phase 11 service/database create to mirror. Five Coolify POST routes dispatched by a `source_type` discriminator, with curated payloads, Zod validation before any API call (SAF-03), fire-and-forget instant_deploy (D-05/D-06), soft success on deploy queue failure (D-08), and 409 domain conflict mapping with force_domain_override hint (D-09/D-12/APP-21).

Output: `createActionSchema` (5-variant discriminated union) + `handleApplicationCreate` wired into `handleApplicationAction` switch. Flips the `application create` RED scaffolds from 10-00 to GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-application-crud-safety/10-CONTEXT.md
@.planning/phases/10-application-crud-safety/10-RESEARCH.md
@.planning/phases/10-application-crud-safety/10-PATTERNS.md
@.planning/phases/10-application-crud-safety/10-00-SUMMARY.md
@.planning/phases/10-application-crud-safety/10-01-SUMMARY.md
@src/mcp/tools/application.ts
@src/mcp/tools/project.ts
@src/mcp/tools/server.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add createActionSchema — 5-variant discriminated union by source_type (D-01, D-02, D-03, D-04, D-05, D-22, SAF-03)</name>
  <files>src/mcp/tools/application.ts</files>
  <read_first>
    - src/mcp/tools/application.ts (lines 1-46 for imports; lines 59-100 for mutationResponseParamsSchema + requireMutationIdentifier pattern; lines 238-249 for applicationActionSchema discriminatedUnion)
    - src/mcp/tools/project.ts (lines 84-97 for createActionSchema .strict() pattern; lines 164-188 for throwValidationError)
    - docs/coolify_openapi.yaml (lines 41-323 /applications/public required+optional fields; lines 324-610 private-github-app; lines 611-897 private-deploy-key; lines 898-1121 dockerfile; lines 1122-1335 dockerimage)
    - .planning/phases/10-application-crud-safety/10-CONTEXT.md (D-01..D-04, D-22)
    - .planning/phases/10-application-crud-safety/10-RESEARCH.md (Pattern 1 — discriminatedUnion example)
  </read_first>
  <behavior>
    - createActionSchema = z.discriminatedUnion('source_type', [public_git, private_deploy_key, private_github_app, dockerfile, dockerimage])
    - All 5 variants share: action:z.literal('create'), project_uuid?:string, project_name?:string, environment_name?:string, environment_uuid?:string, server_uuid:string (required, D-22), name?:string, description?:string, domains?:string, ports_exposes?:string, ports_mappings?:string, instant_deploy:z.boolean().default(false) (D-05), force_domain_override:z.boolean().default(false) (D-10), ...mutationResponseParamsSchema
    - public_git variant: source_type:z.literal('public_git'), git_repository:string, git_branch:string, build_pack:z.enum(['nixpacks','railpack','static','dockerfile']) — NOTE: 'dockercompose' excluded from the enum so Zod rejects it (D-04)
    - private_deploy_key variant: source_type:z.literal('private_deploy_key'), private_key_uuid:string (UUID-only, D-03), git_repository:string, git_branch:string, build_pack:z.enum(['nixpacks','railpack','static','dockerfile'])
    - private_github_app variant: source_type:z.literal('private_github_app'), github_app_uuid:string (UUID-only, D-03), git_repository:string, git_branch:string, build_pack:z.enum(['nixpacks','railpack','static','dockerfile'])
    - dockerfile variant: source_type:z.literal('dockerfile'), dockerfile:string (required), build_pack:z.literal('dockerfile').optional()
    - dockerimage variant: source_type:z.literal('dockerimage'), docker_registry_image_name:string (required), docker_registry_image_tag?:string
    - superRefine on each variant: if neither project_uuid nor project_name -> COOLIFY_VALIDATION_ERROR issue; if neither environment_name nor environment_uuid -> COOLIFY_VALIDATION_ERROR issue (D-02)
    - superRefine on git variants: if build_pack==='dockercompose' -> COOLIFY_VALIDATION_ERROR issue with message "build_pack='dockercompose' is not supported on application create — use service.create (Phase 11)" (D-04). (Belt-and-suspenders: the enum already excludes it, but the explicit refine produces a better error code than Zod's enum-mismatch.)
    - Add 'create' branch to applicationActionSchema discriminatedUnion
  </behavior>
  <action>
    Add `createActionSchema` as a `z.discriminatedUnion('source_type', [...])` with 5 object variants. Each variant is `z.object({...}).strict()` with the fields listed above. Apply the project/environment superRefine via a shared helper `requireProjectAndEnvironment(data, ctx)` that adds COOLIFY_VALIDATION_ERROR issues. Apply the dockercompose reject via a `rejectDockercomposeBuildPack(data, ctx)` helper on the three git variants.

    Use `params: { code: 'COOLIFY_VALIDATION_ERROR' }` on every superRefine issue so `throwValidationError` (existing pattern from project.ts) maps it to the new error code from 10-01.

    Add the `createActionSchema` to the `applicationActionSchema` discriminatedUnion array (currently 6 entries — get/start/stop/restart/deploy/logs — add create as the 7th).

    Do NOT implement the handler in this task — only the schema. The handler lands in Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "application create"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createActionSchema" src/mcp/tools/application.ts` returns at least 2 (definition + inclusion in discriminatedUnion)
    - `grep -c "discriminatedUnion('source_type'" src/mcp/tools/application.ts` returns 1
    - `grep -c "COOLIFY_VALIDATION_ERROR" src/mcp/tools/application.ts` returns at least 3 (project refine + env refine + dockercompose refine)
    - `applicationActionSchema.safeParse({ action:'create', source_type:'public_git', project_uuid:'p', environment_name:'production', server_uuid:'s', git_repository:'https://github.com/a/b', git_branch:'main', build_pack:'nixpacks' }).success === true`
    - `applicationActionSchema.safeParse({ action:'create', source_type:'public_git', server_uuid:'s', git_repository:'...', git_branch:'main', build_pack:'dockercompose' }).success === false` (D-04)
    - `applicationActionSchema.safeParse({ action:'create', source_type:'public_git', server_uuid:'s', git_repository:'...', git_branch:'main', build_pack:'nixpacks' }).success === false` (D-02 — no project)
    - Tests in `application create` describe block that assert schema acceptance/rejection now pass; handler-call tests still fail (handler not implemented in this task)
  </acceptance_criteria>
  <done>
    - 5-variant discriminated union committed; schema-level RED tests (D-02, D-04, SAF-03) flip GREEN; handler-call tests still RED until Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement handleApplicationCreate — dispatcher, instant_deploy fire-and-forget, 409 mapping, follow-up hints (D-05, D-06, D-07, D-08, D-09, D-12, APP-12..16, APP-20, APP-21)</name>
  <files>src/mcp/tools/application.ts</files>
  <read_first>
    - src/mcp/tools/application.ts (after Task 1 — createActionSchema in place; lines 628-693 for handleApplicationDeploy fire-and-forget + extractDeploymentUuid pattern; lines 800-866 for handleApplicationAction switch)
    - src/utils/errors.ts (after 10-01 — COOLIFY_VALIDATION_ERROR + 409 conflicts passthrough)
    - src/utils/diagnose-hints.ts (logsAvailableHint signature for D-07)
    - .planning/phases/10-application-crud-safety/10-CONTEXT.md (D-05..D-12, D-17)
  </read_first>
  <behavior>
    - handleApplicationCreate(parsed, env) dispatches by parsed.source_type to the matching create* client function (5 routes)
    - Builds the curated API body from parsed fields (project_uuid, environment_name/environment_uuid, server_uuid, name, description, domains, ports_exposes, ports_mappings, instant_deploy, force_domain_override, plus source-type-specific fields)
    - Resolves project_uuid from project_name via fetchProjects + resolveProjectUuid (existing util) when only project_name is given (D-02)
    - On HTTP 409 from the create POST: the error already enriched by 10-01's toStructuredError (data.conflicts passthrough) is caught and re-thrown as CoolifyApiError with code COOLIFY_409, data.conflicts, and recoveryHints including "Retry with force_domain_override: true" (D-09, D-12, APP-21)
    - On 201 success: extract app UUID from response; if instant_deploy is true, fire-and-forget triggerDeploy (no wait/poll per D-06); catch deploy-queue failure and return soft success ok:true with deploy.status='failed_to_queue' + recovery hints (D-08); no auto-rollback/delete
    - On instant_deploy success: include follow-up hints — logsAvailableHint(deployment_uuid) when deployment_uuid known, plus a hint for deployment.get and a hint for application.deploy wait:true (D-07)
    - On instant_deploy false (default): return { uuid, deploy: { status: 'not_triggered' } } (or omit deploy field) — agent uses existing application.deploy action to trigger deploy
    - Wire 'create' case into handleApplicationAction switch (before the default)
  </behavior>
  <action>
    Implement `handleApplicationCreate(parsed, env)`:

    1. Resolve project_uuid: if `parsed.project_uuid` use it; else if `parsed.project_name` call `resolveProjectUuid(undefined, parsed.project_name, env)` (existing util from project-lookup.ts); else Zod superRefine already rejected (D-02).
    2. Build the curated body object: include `project_uuid`, `environment_name` (or `environment_uuid` if that's what the caller supplied), `server_uuid`, `name`, `description`, `domains`, `ports_exposes`, `ports_mappings`, `instant_deploy`, `force_domain_override`, plus source-type-specific fields (`git_repository`, `git_branch`, `build_pack` for git variants; `private_key_uuid` for private_deploy_key; `github_app_uuid` for private_github_app; `dockerfile` for dockerfile; `docker_registry_image_name`, `docker_registry_image_tag` for dockerimage). Omit undefined fields (don't send null).
    3. Dispatch by `parsed.source_type`:
       - 'public_git' -> createPublicApplication
       - 'private_deploy_key' -> createPrivateDeployKeyApplication
       - 'private_github_app' -> createPrivateGithubAppApplication
       - 'dockerfile' -> createDockerfileApplication
       - 'dockerimage' -> createDockerimageApplication
    4. Extract app UUID from the 201 response (response.uuid).
    5. If `parsed.instant_deploy === true`:
       - Call `triggerDeploy(env.COOLIFY_URL, env.COOLIFY_TOKEN, appUuid, false, env.COOLIFY_VERIFY_SSL)` (fire-and-forget, force=false — instant_deploy is not a force-rebuild).
       - Wrap in try/catch. On success: extract deployment_uuid via existing `extractDeploymentUuid`, build response `{ uuid: appUuid, deploy: { status: 'queued', deployment_uuid }, logs_available: logsAvailableHint(deployment_uuid), hints: ['Use deployment.get with deployment_uuid to poll status', 'Use application.deploy with wait:true to block until terminal'] }`.
       - On catch (deploy failed to queue): return soft success `{ ok: true, uuid: appUuid, deploy: { status: 'failed_to_queue', error: <message> }, recoveryHints: [...] }` — DO NOT re-throw, DO NOT delete the created app (D-08).
    6. If `parsed.instant_deploy === false`: return `{ uuid: appUuid, deploy: { status: 'not_triggered' }, hints: ['Use application.deploy to trigger a deployment'] }`.
    7. Wire `case 'create': return await handleApplicationCreate(parsed, env);` into `handleApplicationAction` switch (before the `default`).
    8. The 409 mapping is automatic: 10-01's `toStructuredError` already enriches the envelope with `data.conflicts`. The handler just needs to NOT swallow it — let `wrapMcpError` (called at the outer try/catch in handleApplicationAction) propagate. Add `force_domain_override` to the recoveryHints by overriding RECOVERY_HINTS.COOLIFY_409 in the handler when `conflicts` is present: append "Retry with force_domain_override: true on the same create call to override the domain conflict." (D-09/D-12/APP-21).

    Use the existing `wrapMcpError` outer wrapper in `handleApplicationAction` for all error propagation. Do NOT add a new try/catch around the create POST itself — let the client's `withMappedErrors` + the outer wrapper handle it.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "application create"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleApplicationCreate" src/mcp/tools/application.ts` returns at least 2 (definition + switch case)
    - `grep -c "case 'create'" src/mcp/tools/application.ts` returns 1
    - `npx vitest run src/mcp/tools/application.test.ts -t "application create"` is GREEN — all 10+ create tests pass
    - APP-12: createPublicApplication called for source_type='public_git'
    - APP-13: createPrivateDeployKeyApplication called for source_type='private_deploy_key'
    - APP-14: createPrivateGithubAppApplication called for source_type='private_github_app'
    - APP-15: createDockerfileApplication called for source_type='dockerfile'
    - APP-16: createDockerimageApplication called for source_type='dockerimage'
    - APP-20: instant_deploy:true response includes deploy.status='queued' + follow-up hints; no pollDeploymentUntilTerminal call
    - APP-21 (409 path): 409 from create POST -> COOLIFY_409 error with data.conflicts + force_domain_override recovery hint
    - APP-21 (happy-path override): create with force_domain_override:true -> createPublicApplication called with force_domain_override:true in the body; handler returns success with app UUID (no COOLIFY_409) — satisfies ROADMAP criterion 4 ("retry with force_domain_override succeeds") on the create path
    - APP-21 (happy-path override): create with force_domain_override:true and a non-conflicting payload -> createPublicApplication called with force_domain_override:true in the request body; handler returns success with app UUID (no COOLIFY_409) — flips the 10-00 RED scaffold for ROADMAP criterion 4 ("retry with force_domain_override succeeds") to GREEN
    - D-08: deploy-queue failure -> ok:true, uuid returned, deploy.status='failed_to_queue', no deleteApplication call
    - SAF-03: missing required fields -> COOLIFY_VALIDATION_ERROR before any create* client call (verified by vi.fn() not-called assertion)
    - `npm run build` is green
  </acceptance_criteria>
  <done>
    - Create handler committed; all `application create` RED scaffolds from 10-00 flipped GREEN; build green.
  </done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> MCP create handler | Untrusted input crosses here; Zod (SAF-03) is the first gate before any API call |
| MCP handler -> Coolify POST /applications/* | 5 create routes; existing bearer auth + retry + error mapping |
| Coolify 409 -> MCP error envelope | Conflicts array passthrough from 10-01; recovery hint adds force_domain_override |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Tampering | create payload validation | high | mitigate | Zod discriminatedUnion with .strict() on every variant + superRefine for project/env/dockercompose — rejects before any client call (SAF-03). |
| T-10-02-02 | Information Disclosure | instant_deploy soft-success response | low | accept | Soft-success returns app UUID + deploy status only — no secrets. Existing sanitizeFullProjection not invoked on create response (UUID is not secret). |
| T-10-02-03 | Denial of Service | instant_deploy fire-and-forget loop | medium | mitigate | Handler calls triggerDeploy exactly once per create; no polling on create (D-06). Agent follows up via existing application.deploy wait:true. |
| T-10-02-04 | Repudiation | create with no audit trail | low | accept | Coolify server logs the POST; MCP does not own audit. Out of scope. |
| T-10-02-05 | Spoofing | project_uuid resolution from name | medium | mitigate | resolveProjectUuid uses fetchProjects + name match; multi-match -> COOLIFY_AMBIGUOUS_MATCH (existing Phase 9 pattern, D-21 spirit). |
| T-10-SC | Tampering | npm installs | high | mitigate | No new packages — reuses zod/ofetch already pinned and audited in 10-RESEARCH.md. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/application.test.ts -t "application create"` — GREEN
- `npm run build` (tsup) — green
- `grep -c "handleApplicationCreate" src/mcp/tools/application.ts` — at least 2
- `grep -c "discriminatedUnion('source_type'" src/mcp/tools/application.ts` — 1
- `grep -c "COOLIFY_VALIDATION_ERROR" src/mcp/tools/application.ts` — at least 3
- Existing `application get`/`deploy`/`logs`/`start`/`stop`/`restart` tests still GREEN (no regression)
</verification>

<success_criteria>
- All 5 create source types dispatch to the correct Coolify POST route (APP-12..APP-16)
- instant_deploy is fire-and-forget with soft success on queue failure (APP-20, D-05, D-06, D-08)
- 409 domain conflicts map to COOLIFY_409 with force_domain_override hint (APP-21 409 path, D-09, D-12)
- force_domain_override:true on a non-conflicting create passes the flag to the API body and returns success (APP-21 happy-path override, ROADMAP criterion 4)
- Zod rejects malformed payloads before any API call (SAF-03, D-02, D-04)
- Follow-up hints guide the agent to deployment.get / application.deploy wait:true (D-07)
- 10-00 `application create` RED scaffolds flipped GREEN
- No regression in existing application actions
</success_criteria>

<output>
Create `.planning/phases/10-application-crud-safety/10-02-SUMMARY.md` when done.
</output>
