---
phase: 10-application-crud-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/client.ts
  - src/utils/errors.ts
autonomous: true
requirements:
  - APP-12
  - APP-13
  - APP-14
  - APP-15
  - APP-16
  - APP-17
  - APP-18
  - APP-21
must_haves:
  truths:
    - "src/api/client.ts exports createPublicApplication, createPrivateGithubAppApplication, createPrivateDeployKeyApplication, createDockerfileApplication, createDockerimageApplication — each POSTing to the corresponding Coolify /applications/<route> with a JSON body and returning the raw response"
    - "src/api/client.ts exports updateApplication(url, token, uuid, payload, verifySsl) PATCHing /applications/{uuid} with a JSON body"
    - "src/api/client.ts exports deleteApplication(url, token, uuid, params, verifySsl) DELETEing /applications/{uuid} with query params { delete_configurations, delete_volumes, docker_cleanup, delete_connected_networks }"
    - "src/utils/errors.ts CoolifyErrorCode union includes 'COOLIFY_VALIDATION_ERROR' with RECOVERY_HINTS entry pointing the agent to use service.create for dockercompose build_pack"
    - "src/utils/errors.ts toStructuredError/mapApiError enriches HTTP 409 responses with data.conflicts passthrough when the ofetch response._data contains a conflicts array (per D-09/D-12) — distinguishes domain 409 from dependency 409 via presence of conflicts vs dependent_uuids"
    - "npm run build (tsup) is green; npx vitest run src/mcp/tools/application.test.ts is unchanged (RED scaffolds from 10-00 still RED — handlers not yet wired)"
  artifacts:
    - "src/api/client.ts (extended with 7 new application CRUD functions)"
    - "src/utils/errors.ts (extended with COOLIFY_VALIDATION_ERROR code + 409 conflicts passthrough)"
  key_links:
    - "All 7 new client functions use createCoolifyClient(url, token, verifySsl) and the existing withMappedErrors retry/error-mapping wrapper — no new HTTP plumbing"
    - "COOLIFY_VALIDATION_ERROR recovery hint references service.create (Phase 11) so the dockercompose rejection routes the agent to the right tool"
    - "Domain 409 enrichment reuses the existing extractCoolifyMessage path; new helper extracts conflicts array from response._data and attaches to envelope.data.conflicts"
---

<objective>
Wave 1 foundation: API client CRUD methods + error code/enrichment for Phase 10.

Purpose: Ship the HTTP plumbing and error envelope extensions that plans 10-02/10-03/10-04 will consume. No handler logic, no Zod schemas. Mirrors 08-01 / 09-01 foundation pattern.

Output: 7 new client functions in `src/api/client.ts` + 1 new error code + 409 conflicts passthrough in `src/utils/errors.ts`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-application-crud-safety/10-CONTEXT.md
@.planning/phases/10-application-crud-safety/10-RESEARCH.md
@.planning/phases/10-application-crud-safety/10-PATTERNS.md
@src/api/client.ts
@src/utils/errors.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 7 application CRUD client functions to src/api/client.ts</name>
  <files>src/api/client.ts</files>
  <read_first>
    - src/api/client.ts (lines 1-78 for createCoolifyClient/withMappedErrors; lines 144-180 for createProject/updateProject/deleteProject pattern; lines 439-461 for deleteServer with query params; lines 488-597 for triggerApp* patterns)
    - docs/coolify_openapi.yaml (lines 41-323 /applications/public; lines 324-610 /applications/private-github-app; lines 611-897 /applications/private-deploy-key; lines 898-1121 /applications/dockerfile; lines 1122-1335 /applications/dockerimage; lines 1336-1430 DELETE /applications/{uuid}; lines 1431-1694 PATCH /applications/{uuid})
    - .planning/phases/10-application-crud-safety/10-RESEARCH.md (Code Examples section — createPublicApplication/updateApplication/deleteApplication signatures)
  </read_first>
  <behavior>
    - createPublicApplication(url, token, payload, verifySsl=true) -> POST /applications/public with JSON body -> returns raw response (expect { uuid } on 201)
    - createPrivateGithubAppApplication(url, token, payload, verifySsl=true) -> POST /applications/private-github-app
    - createPrivateDeployKeyApplication(url, token, payload, verifySsl=true) -> POST /applications/private-deploy-key
    - createDockerfileApplication(url, token, payload, verifySsl=true) -> POST /applications/dockerfile
    - createDockerimageApplication(url, token, payload, verifySsl=true) -> POST /applications/dockerimage
    - updateApplication(url, token, uuid, payload, verifySsl=true) -> PATCH /applications/{uuid} with JSON body
    - deleteApplication(url, token, uuid, params, verifySsl=true) -> DELETE /applications/{uuid} with query params { delete_configurations?: boolean; delete_volumes?: boolean; docker_cleanup?: boolean; delete_connected_networks?: boolean }
    - All 7 functions route through createCoolifyClient + withMappedErrors (inherited retry + error mapping)
  </behavior>
  <action>
    Add 7 new exported async functions to `src/api/client.ts` following the existing `createProject`/`updateProject`/`deleteProject`/`deleteServer` patterns. Each function:
    - Accepts `(url: string, token: string, payload: unknown, verifySsl = true)` for create/update (payload typed as `unknown` or a minimal interface — the handler in 10-02/10-03 owns the curated shape)
    - For `deleteApplication`, signature is `(url, token, uuid, params: { delete_configurations?: boolean; delete_volumes?: boolean; docker_cleanup?: boolean; delete_connected_networks?: boolean }, verifySsl = true)` and passes `params` as the `query` field to the client call (mirroring `deleteServer`)
    - For `updateApplication`, signature is `(url, token, uuid, payload, verifySsl = true)` and PATCHes `/applications/${uuid}`
    - Uses `createCoolifyClient(url, token, verifySsl)` and returns the raw client call (no response reshaping — handler owns projection)

    Place the new functions immediately after the existing `fetchApplication`/`fetchApplicationLogs` block (around line 256) to keep application-related functions colocated. Do NOT modify existing functions.

    Type the create payloads as `unknown` (the Zod schema in plan 10-02 owns the curated field allowlist). Do not introduce a new `CreateApplicationPayload` interface at this layer — keep the client permissive and let the handler validate.
  </action>
  <verify>
    <automated>npx vitest run src/api/client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function createPublicApplication" src/api/client.ts` returns 1
    - `grep -c "export async function createPrivateGithubAppApplication" src/api/client.ts` returns 1
    - `grep -c "export async function createPrivateDeployKeyApplication" src/api/client.ts` returns 1
    - `grep -c "export async function createDockerfileApplication" src/api/client.ts` returns 1
    - `grep -c "export async function createDockerimageApplication" src/api/client.ts` returns 1
    - `grep -c "export async function updateApplication" src/api/client.ts` returns 1
    - `grep -c "export async function deleteApplication" src/api/client.ts` returns 1
    - `npm run build` (tsup) is green
    - All 7 functions use `createCoolifyClient` (no raw ofetch calls)
  </acceptance_criteria>
  <done>
    - 7 new client functions exported; build green; existing client tests unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add COOLIFY_VALIDATION_ERROR code + 409 conflicts passthrough to src/utils/errors.ts</name>
  <files>src/utils/errors.ts</files>
  <read_first>
    - src/utils/errors.ts (full file — CoolifyErrorCode union, RECOVERY_HINTS, statusToCode, extractCoolifyMessage, mapApiError, toStructuredError, wrapMcpError)
    - docs/coolify_openapi.yaml (lines 311-323, 1109-1121, 1323-1335, 1682-1691 for 409 response shape with `conflicts` array)
    - .planning/phases/10-application-crud-safety/10-CONTEXT.md (D-09, D-12 — domain 409 vs dependency 409 distinction)
    - .planning/phases/10-application-crud-safety/10-RESEARCH.md (Common Pitfalls §1 — Domain Conflict 409)
  </read_first>
  <behavior>
    - CoolifyErrorCode union includes 'COOLIFY_VALIDATION_ERROR' (distinct from COOLIFY_422 — used when Zod rejects at the MCP layer before any API call, per SAF-03 / D-04 dockercompose rejection)
    - RECOVERY_HINTS.COOLIFY_VALIDATION_ERROR tells the agent: payload rejected by MCP before reaching Coolify; for build_pack=dockercompose use service.create (Phase 11)
    - When ofetch response._data contains a `conflicts` array (HTTP 409 domain conflict), the resulting CoolifyErrorEnvelope.data.conflicts passes the array through verbatim (not redacted — conflicts contain domain names + resource names, not secrets)
    - Dependency 409 (existing pattern from Phase 8/9 — dependent_uuids in data) is unchanged; the two 409 flavors are distinguished by which field is present (conflicts vs dependent_uuids)
    - statusToCode(409) still returns 'COOLIFY_409' (no new HTTP-status-to-code mapping); the enrichment happens in toStructuredError when reading response._data
  </behavior>
  <action>
    1. Add `'COOLIFY_VALIDATION_ERROR'` to the `CoolifyErrorCode` union type.
    2. Add a `COOLIFY_VALIDATION_ERROR` entry to `RECOVERY_HINTS` with two strings:
       - "Payload rejected by MCP Zod validation before any Coolify API call — fix the offending field paths in the error message."
       - "For build_pack='dockercompose' use service.create (Phase 11) — application dockercompose create is not supported."
    3. Extend `extractCoolifyMessage` (or add a sibling helper `extractConflicts`) to read `data.conflicts` from the ofetch `response._data` when it is an array. The helper returns `undefined` when no conflicts array is present.
    4. In `toStructuredError`, when `status === 409` and `conflicts` is present, attach `{ conflicts }` to the returned envelope's `data` field (merge with any existing `dependent_uuids` — both can coexist, but in practice only one flavor fires per response). Do NOT redact the conflicts array (domain names and resource names are not secrets; redacting them would make the hint useless).
    5. Do NOT change `statusToCode` — 409 still maps to `COOLIFY_409`. The conflicts passthrough is enrichment, not a new code.

    Keep the change surgical. Do not refactor `mapApiError`/`wrapMcpError` beyond what is needed for the conflicts passthrough.
  </action>
  <verify>
    <automated>npx vitest run src/utils/errors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "COOLIFY_VALIDATION_ERROR" src/utils/errors.ts` returns at least 2 (union + RECOVERY_HINTS key)
    - `grep -c "conflicts" src/utils/errors.ts` returns at least 1 (the passthrough logic)
    - `npm run build` is green
    - statusToCode(409) still returns 'COOLIFY_409' (unchanged)
    - Existing error tests (if any) still pass; new behavior is exercised by 10-02 handler tests
  </acceptance_criteria>
  <done>
    - COOLIFY_VALIDATION_ERROR code + recovery hints committed; 409 conflicts passthrough committed; build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server -> Coolify API | All 7 new client functions cross this boundary via createCoolifyClient; existing retry/auth/error-mapping applies |
| Coolify API -> MCP error envelope | 409 conflicts array crosses into the envelope; must not carry secrets (domain names + resource names only — verified against OpenAPI 409 schema) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-01 | Information Disclosure | 409 conflicts passthrough | low | accept | OpenAPI 409 schema (lines 311-323) confirms conflicts contain only domain/resource_name/resource_uuid/resource_type/message — no secrets. Passthrough is safe. |
| T-10-01-02 | Tampering | Client payload construction | medium | mitigate | Client functions accept `unknown` payload — handler (10-02/10-03) owns Zod validation before calling client (SAF-03). No client-side trust of payload. |
| T-10-01-03 | Spoofing | Bearer token reuse | low | accept | Existing createCoolifyClient + createRetryOptions enforce Authorization header per request; no change. |
| T-10-SC | Tampering | npm installs | high | mitigate | No new packages in this plan — ofetch/zod already pinned and audited in 10-RESEARCH.md Package Legitimacy Audit. |
</threat_model>

<verification>
- `npm run build` (tsup) — green
- `grep -c "export async function create" src/api/client.ts` — at least 5 new create functions (public, private-github-app, private-deploy-key, dockerfile, dockerimage) plus existing createProject/createEnvironment/createServer/createPrivateKey
- `grep -c "COOLIFY_VALIDATION_ERROR" src/utils/errors.ts` — at least 2
- `npx vitest run src/mcp/tools/application.test.ts` — RED scaffolds from 10-00 still fail (handlers not yet wired) — this is expected; do NOT flip them green here
</verification>

<success_criteria>
- 7 new client functions exported and build-green
- COOLIFY_VALIDATION_ERROR code + recovery hints added
- 409 conflicts passthrough enriches the error envelope without redaction
- 10-00 RED scaffolds still RED (no handler changes in this plan)
- Phase 8/9 error patterns unchanged (dependency 409, confirm gate, ambiguous match)
</success_criteria>

<output>
Create `.planning/phases/10-application-crud-safety/10-01-SUMMARY.md` when done.
</output>
