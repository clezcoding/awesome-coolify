---
phase: 10-application-crud-safety
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - src/mcp/tools/application.ts
autonomous: true
requirements:
  - APP-17
  - APP-19
  - APP-21
  - SAF-03
  - SAF-04
must_haves:
  truths:
    - "applicationActionSchema discriminatedUnion includes an 'update' branch accepting uuid|name|fqdn identity (D-21) with curated fields: domains, build_command, install_command, start_command, base_directory, publish_directory, health_check_* fields, custom_labels, ports_mappings, name, description, git_repository, git_branch, is_static, is_auto_deploy_enabled, is_force_https_enabled, redirect, watch_paths, use_build_server, is_preserve_repository_enabled, is_http_basic_auth_enabled, http_basic_auth_username, http_basic_auth_password, force_domain_override (D-13, D-14)"
    - "update schema uses .strict() and rejects unknown fields; D-20: Zod validates update payload before any API call (SAF-03)"
    - "update handler resolves identity via existing resolveAppMutationUuid (uuid|name|fqdn, multi-match -> COOLIFY_AMBIGUOUS_MATCH per D-21)"
    - "update handler calls updateApplication(url, token, uuid, curatedPayload, verifySsl) — PATCH /applications/{uuid}"
    - "D-15: HTTP basic-auth credentials are caller-supplied only — MCP does not generate passwords"
    - "D-11: force_domain_override alone gates update-path domain override — no extra confirm:true (confirm remains delete-only / SAF-01)"
    - "update response masks http_basic_auth_password as '***' unless reveal:true on the update call (D-16, SAF-04); http_basic_auth_username remains visible"
    - "HTTP 409 on update (domain conflict) maps to COOLIFY_409 with data.conflicts + force_domain_override recovery hint (APP-21 update 409 path, D-09, D-10)"
    - "force_domain_override defaults to false on update (D-10); when true, included in the PATCH body so Coolify overrides the domain conflict; on a non-conflicting payload the flag passes through and the handler returns success (APP-21 update happy-path override, ROADMAP criterion 4)"
    - "npx vitest run src/mcp/tools/application.test.ts -t 'application update' is GREEN"
  artifacts:
    - "src/mcp/tools/application.ts (extended with updateActionSchema + handleApplicationUpdate)"
  key_links:
    - "Imports updateApplication from ../../api/client.js (added in 10-01)"
    - "Imports sanitizeFullProjection from ../../utils/projections.js for SAF-04 masking"
    - "Imports resolveAppMutationUuid (existing in application.ts) for uuid|name|fqdn resolution (D-21)"
    - "Imports RECOVERY_HINTS + CoolifyApiError + wrapMcpError from ../../utils/errors.js (10-01 enriched)"
---

<objective>
Wave 3 update handler: curated fields + HTTP basic auth + secret masking + 409 force_domain_override.

Purpose: Implement the `update` action on the `application` tool — the canonical reference for Phase 11 service/database update to mirror. Curated PATCH body (D-13), HTTP basic auth fields (D-14), secret masking unless reveal:true (D-16/SAF-04), 409 domain conflict with force_domain_override (D-09/D-10/APP-21 update path).

Output: `updateActionSchema` + `handleApplicationUpdate` wired into `handleApplicationAction` switch. Flips the `application update` RED scaffolds from 10-00 to GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-application-crud-safety/10-CONTEXT.md
@.planning/phases/10-application-crud-safety/10-RESEARCH.md
@.planning/phases/10-application-crud-safety/10-PATTERNS.md
@.planning/phases/10-application-crud-safety/10-02-SUMMARY.md
@src/mcp/tools/application.ts
@src/mcp/tools/project.ts
@src/utils/projections.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add updateActionSchema — curated fields + basic auth + force_domain_override (D-13, D-14, D-21, SAF-03, SAF-04)</name>
  <files>src/mcp/tools/application.ts</files>
  <read_first>
    - src/mcp/tools/application.ts (after 10-02 — createActionSchema in place; lines 87-100 for requireMutationIdentifier pattern; lines 238-249 for applicationActionSchema discriminatedUnion)
    - src/mcp/tools/project.ts (lines 99-109 for updateActionSchema .strict() + superRefine pattern)
    - src/mcp/tools/application.ts (lines 331-389 for resolveAppMutationUuid — uuid|name|fqdn identity)
    - docs/coolify_openapi.yaml (lines 1431-1694 for PATCH /applications/{uuid} full field list)
    - .planning/phases/10-application-crud-safety/10-CONTEXT.md (D-13, D-14, D-16, D-21)
  </read_first>
  <behavior>
    - updateActionSchema = z.object({ action:z.literal('update'), uuid?:string, name?:string, fqdn?:string, ...curatedFields, ...mutationResponseParamsSchema, reveal?:boolean }).strict().superRefine(requireMutationIdentifier('update'))
    - Identity: at least one of uuid|name|fqdn required (D-21); multi-match handled by resolveAppMutationUuid at runtime -> COOLIFY_AMBIGUOUS_MATCH
    - Curated fields (D-13) — include only these from the OpenAPI PATCH schema:
      - name, description
      - domains
      - git_repository, git_branch, git_commit_sha
      - build_pack (enum: nixpacks|railpack|static|dockerfile — exclude dockercompose, same as create)
      - docker_registry_image_name, docker_registry_image_tag
      - is_static, is_spa, is_auto_deploy_enabled, is_force_https_enabled
      - install_command, build_command, start_command
      - ports_exposes, ports_mappings
      - base_directory, publish_directory
      - health_check_enabled, health_check_path, health_check_port, health_check_host, health_check_method, health_check_return_code, health_check_scheme, health_check_response_text, health_check_interval, health_check_timeout, health_check_retries, health_check_start_period
      - custom_labels, custom_docker_run_options
      - redirect (enum: www|non-www|both)
      - watch_paths, use_build_server, is_preserve_repository_enabled
      - connect_to_docker_network
      - is_container_label_escape_enabled
    - HTTP basic auth (D-14): is_http_basic_auth_enabled:boolean, http_basic_auth_username:string, http_basic_auth_password:string
    - force_domain_override:z.boolean().default(false) (D-10)
    - reveal:z.boolean().default(false) — for SAF-04 unmask on the update response
    - superRefine: if build_pack==='dockercompose' -> COOLIFY_VALIDATION_ERROR (same as create, D-04 spirit)
    - Add 'update' branch to applicationActionSchema discriminatedUnion
  </behavior>
  <action>
    Add `updateActionSchema` as a `z.object({...}).strict()` with the curated fields listed above. Reuse the existing `requireMutationIdentifier(schema, 'update')` helper (already in application.ts) for the uuid|name|fqdn superRefine (D-21). Add the dockercompose reject via a superRefine (or rely on the build_pack enum excluding it — same belt-and-suspenders as create).

    Include `reveal: z.boolean().default(false)` on the update schema so the handler can unmask http_basic_auth_password on the response when reveal:true (D-16, SAF-04).

    Add the `updateActionSchema` to the `applicationActionSchema` discriminatedUnion array (now 8 entries — get/start/stop/restart/deploy/logs/create/update).

    Do NOT implement the handler in this task — only the schema.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "application update"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "updateActionSchema" src/mcp/tools/application.ts` returns at least 2 (definition + inclusion in discriminatedUnion)
    - `grep -c "is_http_basic_auth_enabled" src/mcp/tools/application.ts` returns at least 1
    - `grep -c "http_basic_auth_password" src/mcp/tools/application.ts` returns at least 1
    - `grep -c "force_domain_override" src/mcp/tools/application.ts` returns at least 2 (create + update)
    - `applicationActionSchema.safeParse({ action:'update', uuid:'x', domains:'https://new.example.com' }).success === true`
    - `applicationActionSchema.safeParse({ action:'update', domains:'x' }).success === false` (no identity — D-21)
    - `applicationActionSchema.safeParse({ action:'update', uuid:'x', build_pack:'dockercompose' }).success === false` (D-04)
    - Schema-acceptance tests in `application update` describe block flip GREEN; handler-call tests still RED until Task 2
  </acceptance_criteria>
  <done>
    - updateActionSchema committed; schema-level tests GREEN; handler-call tests still RED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement handleApplicationUpdate — identity resolution, curated PATCH, masking, 409 mapping (APP-17, APP-19, APP-21, D-09, D-10, D-13, D-14, D-16, D-21, SAF-03, SAF-04)</name>
  <files>src/mcp/tools/application.ts</files>
  <read_first>
    - src/mcp/tools/application.ts (after Task 1 — updateActionSchema in place; lines 331-389 for resolveAppMutationUuid; lines 800-866 for handleApplicationAction switch)
    - src/mcp/tools/project.ts (lines 427-469 for update handler pattern — payload building + response shaping)
    - src/utils/projections.ts (sanitizeFullProjection signature for SAF-04 masking)
    - .planning/phases/10-application-crud-safety/10-CONTEXT.md (D-13..D-16, D-21)
  </read_first>
  <behavior>
    - handleApplicationUpdate(parsed, env) resolves uuid via resolveAppMutationUuid({ uuid, name, fqdn }, env) (D-21)
    - Builds curated PATCH payload from parsed fields — only fields the caller explicitly set (undefined fields omitted, not null)
    - Calls updateApplication(env.COOLIFY_URL, env.COOLIFY_TOKEN, uuid, payload, env.COOLIFY_VERIFY_SSL)
    - On 200 success: fetch the updated application via fetchApplication (existing) to return the post-update state; route through sanitizeFullProjection(raw, parsed.reveal) so http_basic_auth_password is masked as '***' unless reveal:true (D-16, SAF-04); http_basic_auth_username remains visible
    - On HTTP 409 (domain conflict): the 10-01 enrichment already attached data.conflicts; handler appends "Retry with force_domain_override: true on the same update call to override the domain conflict." to recoveryHints (D-09, D-10, APP-21 update path)
    - force_domain_override=true is included in the PATCH body when the caller sets it, so the retry succeeds (D-10)
    - Wire 'update' case into handleApplicationAction switch
  </behavior>
  <action>
    Implement `handleApplicationUpdate(parsed, env)`:

    1. Resolve uuid: `const uuid = await resolveAppMutationUuid({ uuid: parsed.uuid, name: parsed.name, fqdn: parsed.fqdn }, env);` (D-21 — existing helper, multi-match -> COOLIFY_AMBIGUOUS_MATCH).
    2. Build the curated payload: iterate the curated field list from the schema; for each field, if `parsed[field] !== undefined`, add to payload. Include `force_domain_override` when explicitly true (D-10). Omit `action`, `uuid`, `name`, `fqdn`, `format`, `max_chars`, `reveal` from the payload (those are MCP-layer concerns).
    3. Call `await updateApplication(env.COOLIFY_URL, env.COOLIFY_TOKEN, uuid, payload, env.COOLIFY_VERIFY_SSL)`.
    4. On success (no throw): fetch the updated state via `fetchApplication(env.COOLIFY_URL, env.COOLIFY_TOKEN, uuid, env.COOLIFY_VERIFY_SSL)` so the response reflects what Coolify now stores. Route through `sanitizeFullProjection(raw, parsed.reveal)` (D-16, SAF-04). Return via `buildReadResponse(data, { format: parsed.format, max_chars: parsed.max_chars })`.
    5. On 409 (caught by outer wrapMcpError — the 10-01 enrichment already attached data.conflicts): the handler does not need to re-throw; the outer wrapper propagates. To add the force_domain_override hint, catch CoolifyApiError in the handler, check `error.envelope.data?.conflicts`, and if present, append the override hint to `error.envelope.recoveryHints` before re-throwing. (This mirrors how 10-02 create handler enriches the 409 hint.)
    6. Wire `case 'update': return await handleApplicationUpdate(parsed, env);` into `handleApplicationAction` switch.

    Do NOT add a new try/catch around the PATCH itself — let the outer wrapper in `handleApplicationAction` handle non-409 errors. Only catch to enrich the 409 hint.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "application update"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleApplicationUpdate" src/mcp/tools/application.ts` returns at least 2 (definition + switch case)
    - `grep -c "case 'update'" src/mcp/tools/application.ts` returns 1
    - `npx vitest run src/mcp/tools/application.test.ts -t "application update"` is GREEN
    - APP-17: update with domains/build_command/health_check_path/custom_labels -> updateApplication called with curated payload; response reflects changed fields
    - APP-19: update with is_http_basic_auth_enabled:true + http_basic_auth_username + http_basic_auth_password -> updateApplication called with those fields
    - SAF-04: update response masks http_basic_auth_password as '***' by default; reveal:true returns the plain value; http_basic_auth_username always visible
    - APP-21 (update 409 path): 409 from PATCH -> COOLIFY_409 + data.conflicts + force_domain_override recovery hint
    - APP-21 (update happy-path override): update with force_domain_override:true on a non-conflicting payload -> updateApplication called with force_domain_override:true in the PATCH body; handler returns success (no COOLIFY_409) — flips the 10-00 update-path RED scaffold for ROADMAP criterion 4 ("retry with force_domain_override succeeds") to GREEN
    - D-21: update by name with multi-match -> COOLIFY_AMBIGUOUS_MATCH, updateApplication NOT called
    - SAF-03 (update path): update with unknown field -> Zod rejects (strict schema) before any API call; updateApplication NOT called — flips the 10-00 update-path SAF-03 RED scaffold to GREEN
    - `npm run build` is green
    - Existing create/get/deploy/logs/start/stop/restart tests still GREEN
  </acceptance_criteria>
  <done>
    - Update handler committed; all `application update` RED scaffolds from 10-00 flipped GREEN; build green.
  </done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> MCP update handler | Untrusted input; Zod .strict() + superRefine is the first gate (SAF-03) |
| MCP handler -> Coolify PATCH /applications/{uuid} | Existing bearer auth + retry + error mapping |
| Coolify 200 response -> MCP update result | Routed through sanitizeFullProjection for SAF-04 masking |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-01 | Information Disclosure | http_basic_auth_password in update response | high | mitigate | sanitizeFullProjection masks http_basic_auth_password as '***' unless reveal:true (D-16, SAF-04). Username remains visible (D-16). Test asserts masked by default. |
| T-10-03-02 | Tampering | update payload validation | high | mitigate | Zod .strict() rejects unknown fields; curated allowlist (D-13) prevents mass-assignment of unintended fields (e.g., project_uuid, server_uuid — not in the update schema). |
| T-10-03-03 | Spoofing | identity resolution by name/fqdn | medium | mitigate | resolveAppMutationUuid multi-match -> COOLIFY_AMBIGUOUS_MATCH, no mutation (D-21). Existing Phase 4 pattern. |
| T-10-03-04 | Tampering | force_domain_override on update | medium | accept | Flag alone gates the override (D-11 — no extra confirm). Caller-supplied; documented in tool description. |
| T-10-03-05 | Information Disclosure | 409 conflicts passthrough on update | low | accept | Conflicts contain domain/resource names only (verified OpenAPI 409 schema); no secrets. |
| T-10-SC | Tampering | npm installs | high | mitigate | No new packages — reuses zod/ofetch already pinned and audited. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/application.test.ts -t "application update"` — GREEN
- `npm run build` (tsup) — green
- `grep -c "handleApplicationUpdate" src/mcp/tools/application.ts` — at least 2
- `grep -c "sanitizeFullProjection" src/mcp/tools/application.ts` — at least 2 (existing get + new update)
- Existing create/get/deploy/logs/start/stop/restart tests still GREEN
</verification>

<success_criteria>
- Curated update PATCH with FQDN, build commands, healthcheck, labels (APP-17, D-13)
- HTTP basic auth fields accepted on update (APP-19, D-14)
- http_basic_auth_password masked unless reveal:true (SAF-04, D-16)
- 409 domain conflict on update -> COOLIFY_409 + force_domain_override hint (APP-21 update 409 path, D-09, D-10)
- force_domain_override:true on a non-conflicting update passes the flag to the PATCH body and returns success (APP-21 update happy-path override, ROADMAP criterion 4)
- Identity resolution uuid|name|fqdn with COOLIFY_AMBIGUOUS_MATCH (D-21)
- Zod validates before any API call (SAF-03 — both create-path rejection from 10-02 and update-path .strict() unknown-field rejection)
- 10-00 `application update` RED scaffolds flipped GREEN
- No regression in existing application actions
</success_criteria>

<output>
Create `.planning/phases/10-application-crud-safety/10-03-SUMMARY.md` when done.
</output>
