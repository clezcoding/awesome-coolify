---
phase: 10-application-crud-safety
plan: 00
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/application.test.ts
autonomous: true
requirements:
  - APP-12
  - APP-13
  - APP-14
  - APP-15
  - APP-16
  - APP-17
  - APP-18
  - APP-19
  - APP-20
  - APP-21
  - SAF-01
  - SAF-02
  - SAF-03
  - SAF-04
must_haves:
  truths:
    - "application.test.ts contains failing (RED) describe blocks covering all five create source types (public_git, private_deploy_key, private_github_app, dockerfile, dockerimage) per APP-12..APP-16"
    - "application.test.ts contains failing (RED) describe blocks for update (APP-17), HTTP basic auth on update (APP-19), and secret masking unless reveal:true (SAF-04)"
    - "application.test.ts contains failing (RED) describe blocks for delete without confirm -> COOLIFY_CONFIRM_REQUIRED (SAF-01) and delete safe defaults delete_volumes=false / delete_configurations=false / docker_cleanup=false / delete_connected_networks=false (SAF-02 — all four flags)"
    - "application.test.ts contains failing (RED) describe blocks for instant_deploy fire-and-forget (APP-20) and 409 domain conflict -> force_domain_override recovery hint (APP-21 409 path) plus force_domain_override:true happy-path override passing the flag to the create API body (APP-21 override path, ROADMAP criterion 4)"
    - "application.test.ts contains failing (RED) describe block asserting Zod rejects malformed create payloads before any API call (SAF-03 create path) AND rejects unknown fields on update with .strict() before any updateApplication call (SAF-03 update path)"
    - "application.test.ts contains failing (RED) describe block for delete_preview (Phase 8/9 parity)"
    - "Running `npx vitest run src/mcp/tools/application.test.ts` fails on the new describe blocks (RED) — handlers not yet implemented in application.ts"
  artifacts:
    - "src/mcp/tools/application.test.ts (extended with new RED scaffolds)"
  key_links:
    - "Test file imports `handleApplicationAction`, `applicationActionSchema`, `isApplicationErrorResult` from `./application.js` — symbols already exported; new action branches will be added by plans 10-02/10-03/10-04"
    - "vi.mock('../../api/client.js') extended with the new client functions (createPublicApplication, createPrivateGithubAppApplication, createPrivateDeployKeyApplication, createDockerfileApplication, createDockerimageApplication, updateApplication, deleteApplication) so tests can mock them before plan 10-01 ships the real client"
---

<objective>
Wave 0 RED test scaffolds for Phase 10 — application CRUD + safety.

Purpose: Establish failing tests for all 14 Phase 10 requirements before any handler code is written. Plans 10-02/10-03/10-04 flip these tests GREEN without rewriting them. Mirrors the 08-00 / 09-00 Wave 0 pattern.

Output: Extended `src/mcp/tools/application.test.ts` with new describe blocks that fail at handler import or runtime because the create/update/delete/delete_preview actions do not yet exist on `applicationActionSchema` / `handleApplicationAction`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-application-crud-safety/10-CONTEXT.md
@.planning/phases/10-application-crud-safety/10-RESEARCH.md
@.planning/phases/10-application-crud-safety/10-VALIDATION.md
@.planning/phases/10-application-crud-safety/10-EDGE-COVERAGE.json
@src/mcp/tools/application.test.ts
@src/mcp/tools/project.test.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend vi.mock with new client functions and add RED describe blocks for create (APP-12..APP-16, APP-20, APP-21 409 + happy-path override, SAF-03)</name>
  <files>src/mcp/tools/application.test.ts</files>
  <read_first>
    - src/mcp/tools/application.test.ts (lines 1-120 for existing mock + testEnv + mockApplication patterns)
    - src/mcp/tools/project.test.ts (lines 139-263 for create test pattern)
    - src/mcp/tools/application.ts (lines 238-249 for applicationActionSchema discriminatedUnion shape — new branches added by 10-02)
    - .planning/phases/10-application-crud-safety/10-CONTEXT.md (D-01..D-12, D-17..D-22)
    - docs/coolify_openapi.yaml (lines 41-323 for /applications/public required fields + 409 conflicts shape)
  </read_first>
  <behavior>
    - APP-12: create with source_type='public_git', project_uuid, environment_name, server_uuid, git_repository, git_branch, build_pack='nixpacks' -> returns { uuid, ... } and calls createPublicApplication
    - APP-13: create with source_type='private_deploy_key', private_key_uuid (UUID) -> calls createPrivateDeployKeyApplication
    - APP-14: create with source_type='private_github_app', github_app_uuid (UUID) -> calls createPrivateGithubAppApplication
    - APP-15: create with source_type='dockerfile', dockerfile string -> calls createDockerfileApplication
    - APP-16: create with source_type='dockerimage', docker_registry_image_name -> calls createDockerimageApplication
    - APP-20: create with instant_deploy:true -> response includes deploy:{status:'queued'|'failed_to_queue'} and follow-up hints; no wait/poll on create
    - APP-21 (409 path): create where client throws HTTP 409 with conflicts array -> handler returns COOLIFY_409 error with data.conflicts passthrough and force_domain_override recovery hint
    - APP-21 (happy-path override): create with force_domain_override:true and a payload that the API accepts (no 409) -> createPublicApplication called with force_domain_override:true included in the request body; handler returns success with app UUID (no COOLIFY_409)
    - SAF-03: create with missing required field (e.g., no server_uuid) -> Zod rejects before any client call (createPublicApplication not called)
    - D-04: create with build_pack='dockercompose' -> Zod rejects with COOLIFY_VALIDATION_ERROR and hint to use service.create
    - D-02: create with no project identifier (neither project_uuid nor project_name) -> Zod rejects with COOLIFY_VALIDATION_ERROR
  </behavior>
  <action>
    Extend the existing `vi.mock('../../api/client.js', ...)` block to also include the seven new client functions that plan 10-01 will implement: `createPublicApplication`, `createPrivateGithubAppApplication`, `createPrivateDeployKeyApplication`, `createDockerfileApplication`, `createDockerimageApplication`, `updateApplication`, `deleteApplication` — each as `vi.fn()`. Add them to the import-from-client list below the mock.

    Add new `describe('application create', ...)` block with one `it(...)` per behavior listed above. Each test calls `handleApplicationAction({ action: 'create', source_type: '<type>', ... }, testEnv)` and asserts on the returned result or thrown error. Tests MUST fail RED at the time of commit (handler not yet implemented — `applicationActionSchema.safeParse({ action: 'create', ... }).success` is false because the 'create' branch is not yet on the discriminatedUnion, so `handleApplicationAction` throws a Zod error).

    For APP-21 (409): mock `createPublicApplication` to reject with an ofetch-shaped error whose `response.status === 409` and `response._data.conflicts` is a non-empty array; assert `isApplicationErrorResult(result) === true` and `result.structuredContent.error.code === 'COOLIFY_409'` and `result.structuredContent.error.data.conflicts` matches the mock array, and recoveryHints mention `force_domain_override: true`.

    For SAF-03: assert `createPublicApplication` (or whichever client fn the source_type maps to) is NOT called (use `vi.fn()` not-called assertion) when required fields are missing.

    For D-04 (dockercompose): assert Zod rejects `build_pack: 'dockercompose'` on create with a COOLIFY_VALIDATION_ERROR code in the error envelope.

    Do NOT implement the handler or schemas — only the tests. Tests must fail when run.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "application create"</automated>
  </verify>
  <acceptance_criteria>
    - `application.test.ts` contains a `describe('application create', ...)` block with at least 11 `it(...)` cases covering APP-12..APP-16, APP-20, APP-21 (409 path), APP-21 (happy-path override), SAF-03, D-04, D-02
    - `vi.mock('../../api/client.js')` factory includes the 7 new client function stubs
    - `npx vitest run src/mcp/tools/application.test.ts -t "application create"` exits non-zero with failures in the new describe block (RED) — because `applicationActionSchema.safeParse({ action: 'create', ... }).success === false`
    - No existing tests in application.test.ts are modified or broken
  </acceptance_criteria>
  <done>
    - New RED describe block committed; CI/test run shows the new create tests failing as expected; existing tests still pass.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Add RED describe blocks for update, basic auth, masking, delete, delete_preview, confirm, safe defaults, update-path Zod rejection (APP-17, APP-18, APP-19, SAF-01, SAF-02, SAF-03, SAF-04)</name>
  <files>src/mcp/tools/application.test.ts</files>
  <read_first>
    - src/mcp/tools/application.test.ts (after Task 1 — to extend consistently)
    - src/mcp/tools/server.test.ts (delete + confirm + delete_volumes default false pattern)
    - src/mcp/tools/project.test.ts (delete_preview pattern)
    - .planning/phases/10-application-crud-safety/10-CONTEXT.md (D-13..D-19, D-21)
    - docs/coolify_openapi.yaml (lines 1431-1691 for PATCH /applications/{uuid} + 409 on update)
  </read_first>
  <behavior>
    - APP-17: update with uuid, domains, build_command, health_check_path, custom_labels -> updateApplication called with curated payload; response reflects changed fields
    - APP-19: update with is_http_basic_auth_enabled:true, http_basic_auth_username, http_basic_auth_password -> updateApplication called with those fields
    - SAF-04: update response masks http_basic_auth_password as '***' unless reveal:true; username remains visible
    - SAF-03 (update path): update with an unknown field (e.g., unexpected_field:'foo') -> Zod rejects via .strict() with COOLIFY_VALIDATION_ERROR before any updateApplication call (updateApplication NOT called) — mirrors the create-path SAF-03 scaffold in Task 1
    - APP-21 (update 409 path): update where client throws 409 with conflicts -> COOLIFY_409 + force_domain_override hint (mirrors create 409 path)
    - APP-21 (update happy-path override): update with force_domain_override:true and a payload the API accepts (no 409) -> updateApplication called with force_domain_override:true in the PATCH body; handler returns success (no COOLIFY_409) — RED scaffold for ROADMAP criterion 4 on the update path; GREEN flip in 10-03 Task 2
    - APP-18: delete with confirm:true -> deleteApplication called with delete_volumes=false, delete_configurations=false defaults
    - SAF-01: delete without confirm -> COOLIFY_CONFIRM_REQUIRED; deleteApplication NOT called
    - SAF-02: delete with confirm:true but no override flags -> deleteApplication called with { delete_volumes: false, delete_configurations: false, docker_cleanup: false, delete_connected_networks: false } (all four flags — matches 10-04 Task 2 contract)
    - D-21: update/delete accept uuid | name | fqdn; multi-match name -> COOLIFY_AMBIGUOUS_MATCH, no mutation
    - delete_preview: returns { uuid, child_resources?, would_delete } without deleting
  </behavior>
  <action>
    Add `describe('application update', ...)`, `describe('application delete', ...)`, and `describe('application delete_preview', ...)` blocks with `it(...)` per behavior. Use the same `testEnv` and mockApplication fixtures already in the file. Mock `updateApplication` and `deleteApplication` (added in Task 1) per test. For mutation identity (D-21), reuse the existing `fetchResources` mock pattern from the deploy tests to simulate multi-match -> COOLIFY_AMBIGUOUS_MATCH.

    For SAF-04 masking: mock `updateApplication` to resolve with a response containing `http_basic_auth_password: 'plain-secret'`; call update without `reveal` and assert the result data shows `'***'` for the password; call update with `reveal: true` and assert the plain value is returned. (This requires the handler to route through `sanitizeFullProjection` — the test asserts the contract.)

    For SAF-01: call delete with `confirm: false` (or omitted) and assert `isApplicationErrorResult(result) === true`, `result.structuredContent.error.code === 'COOLIFY_CONFIRM_REQUIRED'`, and `deleteApplication` was NOT called.

    For SAF-02: call delete with `confirm: true` and no override flags; assert `deleteApplication` was called with `{ delete_volumes: false, delete_configurations: false, docker_cleanup: false, delete_connected_networks: false }` in the query param object — all four flags false (matches 10-04 Task 2 contract; the mock assertion must include delete_connected_networks:false).

    For SAF-03 (update path): call update with an unknown field (e.g., `unexpected_field: 'foo'`) and assert `updateApplication` was NOT called (vi.fn() not-called) and the result is a COOLIFY_VALIDATION_ERROR — the .strict() schema rejects before any client call. This mirrors the create-path SAF-03 scaffold in Task 1 and is the RED scaffold that 10-03 Task 2 flips GREEN.

    For APP-21 (update happy-path override): mock `updateApplication` to resolve with the updated application shape; call update with `force_domain_override: true` and assert `updateApplication` was called with a PATCH body whose `force_domain_override` field is `true`, and the handler returns success (no COOLIFY_409). RED scaffold for ROADMAP criterion 4 on the update path; GREEN flip in 10-03 Task 2.

    Do NOT implement handlers or schemas. Tests must fail RED.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "application (update|delete|delete_preview)"</automated>
  </verify>
  <acceptance_criteria>
    - `application.test.ts` contains `describe('application update', ...)`, `describe('application delete', ...)`, `describe('application delete_preview', ...)` blocks
    - At least 14 new `it(...)` cases covering APP-17, APP-19, SAF-04, SAF-03 (update path), APP-21 (update 409 path), APP-21 (update happy-path override), APP-18, SAF-01, SAF-02 (with delete_connected_networks:false assertion), D-21 (uuid|name|fqdn + ambiguous), delete_preview
    - SAF-02 mock assertion includes all four flags: `delete_volumes: false, delete_configurations: false, docker_cleanup: false, delete_connected_networks: false`
    - `npx vitest run src/mcp/tools/application.test.ts` fails on the new describe blocks (RED) because `applicationActionSchema` does not yet accept `update` / `delete` / `delete_preview` actions
    - Existing tests still pass
  </acceptance_criteria>
  <done>
    - All new RED scaffolds committed; full `npx vitest run src/mcp/tools/application.test.ts` shows the new tests failing as expected (handler not implemented) and the existing tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file -> application.ts | RED scaffolds import symbols that will exist after 10-02/10-03/10-04; no trust boundary crossed at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-00-01 | Tampering | Test fixtures (mockApplication, mock conflicts) | low | accept | Test-only fixtures; no production code path. Real conflict-shape handling validated in 10-01/10-02 against OpenAPI spec. |
| T-10-SC | Tampering | npm installs | high | mitigate | No new packages in this plan — vitest already pinned. slopcheck not required. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/application.test.ts` — new describe blocks RED (failing as expected), existing tests GREEN
- `npm run build` (tsup) — must remain green (test file is type-checked; new mocks must match existing import shape)
- `grep -c "describe('application create'" src/mcp/tools/application.test.ts` — at least 1
- `grep -c "describe('application update'" src/mcp/tools/application.test.ts` — at least 1
- `grep -c "describe('application delete'" src/mcp/tools/application.test.ts` — at least 1
</verification>

<success_criteria>
- All 14 Phase 10 requirements have at least one failing (RED) test in application.test.ts
- No existing test broken
- Tests are structured so plans 10-02/10-03/10-04 can flip them GREEN without rewrite
- Wave 0 contract satisfied (per 08-00 / 09-00 precedent)
</success_criteria>

<output>
Create `.planning/phases/10-application-crud-safety/10-00-SUMMARY.md` when done.
</output>
