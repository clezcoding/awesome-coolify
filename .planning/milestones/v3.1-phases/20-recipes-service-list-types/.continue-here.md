---
context: phase
phase: 20-recipes-service-list-types
task: verification
total_tasks: 4
status: gaps_found
last_updated: 2026-07-24T06:36:28.634Z
branch: gsd/phase-20-recipes-service-list-types
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Mark complete on gaps_found | Phase 20 verification scored 13/14; marking ROADMAP/STATE complete before gap closure would ship incomplete D-20 | advisory | Only run `phase.complete` after VERIFICATION.md `status: passed` |
| Trust plan-index objectives over PLAN.md | phase-plan-index wave-1 objective text drifted (mentioned buildpack-detector utils); actual 20-02 PLAN ships recipe.ts | advisory | Executors read PLAN.md must_haves; orchestrator spot-checks against PLAN not index blurbs |
| Switch phase branch with dirty STATE | Uncommitted STATE.md blocked `git switch` to phase branch at execute start | advisory | Stash/commit `.planning/` before branch fork; verify `git rev-parse --abbrev-ref HEAD` after switch |

<current_state>
Phase 20 execution finished (4/4 plans). Verification: **gaps_found** (13/14). Branch `gsd/phase-20-recipes-service-list-types`. Working tree clean at pause. Phase NOT marked complete in ROADMAP checkbox sense for shipping — wait for gap closure.
</current_state>

<completed_work>

### Plans (all SUMMARY present)
- **20-00** — RED `recipe.test.ts` scaffolds (it.fails + dynamic import) — `d049ac6`
- **20-01** — `fetchServiceTemplates` (jsDelivr + GitHub Raw, version pin) + `service.list-types` slim `{id,label}[]` — RECIPE-01
- **20-02** — `recipe.ts`: `create-git-app` + `create-one-click`; `create-app-db` stubbed `COOLIFY_NOT_IMPLEMENTED` — RECIPE-02, RECIPE-04
- **20-03** — `create-app-db` real handler, register `recipe` in `server.ts` (17 tools), README EN/DE — RECIPE-03

### Gates
- Build + 1020 vitest green
- Regression vs Phase 19 suite: pass
- Code review: `20-REVIEW.md` — 0 critical, 6 warning, 3 info (advisory)
- Verification: `20-VERIFICATION.md` — gaps_found
</completed_work>

<remaining_work>

1. **Gap D-20** — Append `MANIFEST_HINT` (instance/manifest tip) to `create-git-app` error envelopes (Zod validation + CoolifyApiError paths in `handleCreateGitApp`). Mirror create-one-click D-20 test in `recipe.test.ts`.
2. `/gsd-plan-phase 20 --gaps` → gap_closure PLAN
3. `/gsd-execute-phase 20 --gaps-only`
4. Re-verify → `phase.complete` only if passed
5. Optional: `/gsd-code-review 20 --fix` for WR-01 (`Object.hasOwn` vs `in`), WR-02 deploy.status, WR-05 repo_path allowlist
</remaining_work>

<decisions_made>

- Dynamic CDN catalog only (no bundled YAML) — D-01/D-03
- Recipe creates: no confirm gate (D-17), no dry-run (D-18), instant_deploy default true (D-16)
- Partial failure on create-app-db: `COOLIFY_RECIPE_PARTIAL_FAILURE` + UUIDs, no auto-rollback (D-15)
- Sequential executors (worktrees off) on phase branch
</decisions_made>

<blockers>
- **Verification gap D-20** (technical): create-git-app errors lack soft manifest hints. Blocks phase completion / ship. Fix via gap plan — not a human-action blocker.
</blockers>

## Required Reading (in order)
1. `.planning/phases/20-recipes-service-list-types/20-VERIFICATION.md` — gap truth + missing artifacts
2. `.planning/phases/20-recipes-service-list-types/20-CONTEXT.md` — D-20 soft manifest hints
3. `src/mcp/tools/recipe.ts` — `handleCreateGitApp` / compare `handleCreateOneClick` MANIFEST_HINT usage
4. `.planning/phases/20-recipes-service-list-types/20-03-SUMMARY.md` — latest shipped surface
5. `.planning/HANDOFF.json` — machine resume state
6. `.planning/phases/20-recipes-service-list-types/20-REVIEW.md` — optional advisory fixes

## Critical Anti-Patterns (do NOT repeat these)
- **COMPLETE_ON_GAPS**: Do not call `phase.complete` while VERIFICATION status is `gaps_found`
- **SKIP_GAP_PLAN**: Do not hand-edit production without `/gsd-plan-phase 20 --gaps` tracking (unless user explicitly asks for a quick fix outside GSD)

## Infrastructure State
- Branch: `gsd/phase-20-recipes-service-list-types` (local; may be unpushed)
- No async jobs, no running watchers from this session
- Tests: `pnpm test` → 1020 passed at last gate
- response_language config: German (user-facing GSD copy)

<context>
Session ran `/gsd-execute-phase 20` end-to-end. Wave 0→2 sequential. Verifier returned gaps_found on D-20 only — core RECIPE-01..04 goal otherwise verified. User paused before gap planning.
</context>

<next_action>
Start with: `/gsd-plan-phase 20 --gaps` (creates gap_closure plan for D-20 MANIFEST_HINT on create-git-app). Then `/gsd-execute-phase 20 --gaps-only`.
</next_action>
