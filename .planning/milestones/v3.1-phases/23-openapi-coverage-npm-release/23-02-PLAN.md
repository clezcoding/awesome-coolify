---
phase: 23-openapi-coverage-npm-release
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - docs/coolify_openapi.json
  - docs/coolify_openapi.yaml
  - docs/OPENAPI.md
  - docs/coverage-map.yaml
  - docs/coverage-overrides.yaml
  - docs/COVERAGE.md
  - scripts/lib/openapi-coverage-join.mjs
  - tests/openapi-coverage.test.ts
autonomous: true
requirements:
  - OAPI-01
  - OAPI-02
user_setup: []

must_haves:
  truths:
    - "docs/coolify_openapi.json and .yaml pinned to Coolify tag v4.1.2 per D-07/D-08/D-09"
    - "docs/OPENAPI.md documents upstream URL, tag, fetch date, operation count, refresh steps"
    - "docs/coverage-map.yaml lists every *ActionsCatalog action across 18 MCP tools with client + openapi links per D-01/D-02"
    - "docs/coverage-overrides.yaml seeds SVC-04 logs deferred, execute_command out-of-scope, non-REST tools out-of-scope per D-05"
    - "Regenerated docs/COVERAGE.md includes summary counts for all four buckets and full per-action table"
    - "Vitest validates every catalog action in map, every openapi key in dereferenced spec, every client export in client.ts"
    - "{ statement: 'gap rows exist for honest backlog but deferred/out-of-scope do not fail CI', verification: backstop }"
  artifacts:
    - "docs/OPENAPI.md"
    - "docs/coolify_openapi.json (v4.1.2 refresh)"
    - "docs/coolify_openapi.yaml (v4.1.2 refresh)"
    - "docs/coverage-map.yaml (complete)"
    - "docs/coverage-overrides.yaml (seeded)"
    - "docs/COVERAGE.md (full report)"
  key_links:
    - "coverage-map.yaml actions ↔ src/mcp/tools/*ActionsCatalog"
    - "coverage-map.yaml client ↔ src/api/client.ts exports"
    - "coverage-map.yaml openapi ↔ dereferenced v4.1.2 operations"
    - "OPENAPI.md ↔ committed spec artifacts"
  prohibitions:
    - statement: "Do not float OpenAPI on v4.x or next without phase decision (D-09)"
      status: enforced
      verification: "rg -n 'v4\\.1\\.2' docs/OPENAPI.md returns at least 2 matches"
    - statement: "Do not implement new MCP tools to close gap rows"
      status: enforced
      verification: "git diff --name-only src/mcp/tools/ is empty or docs-only touch"
---

<objective>
Expand coverage map to full MCP surface and pin OpenAPI spec to Coolify v4.1.2 with provenance documentation.

Purpose: Complete OAPI-01/OAPI-02 — every MCP action row mapped, overrides seeded, spec pinned per locked decisions. Generator from Plan 23-01 stays; this plan fills data + validation.

Output: `docs/OPENAPI.md`, refreshed specs, complete `coverage-map.yaml` / `coverage-overrides.yaml`, full `docs/COVERAGE.md`, expanded vitest guards.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md
@.planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md
@.planning/phases/23-openapi-coverage-npm-release/23-PATTERNS.md
@.planning/phases/23-openapi-coverage-npm-release/23-01-PLAN.md
@.planning/spikes/001-coolify-api-surface/README.md
@tests/integration/docs-parity.test.ts
@src/mcp/server.ts
@src/api/client.ts
</context>

## Artifacts this phase produces

- `docs/OPENAPI.md` — provenance doc: upstream `https://raw.githubusercontent.com/coollabsio/coolify/v4.1.2/openapi.yaml`, pin tag, fetch date, operation count, curl refresh + JSON convert steps per D-08.
- `docs/coolify_openapi.yaml` + `docs/coolify_openapi.json` — refreshed from v4.1.2 tag (D-07).
- `docs/coverage-map.yaml` — complete `actions:` array linking all ~115 catalog actions to client exports and OpenAPI METHOD /path keys.
- `docs/coverage-overrides.yaml` — seeded overrides: SVC-04 service/DB logs deferred, execute_command out-of-scope, manifest.* / docs.search / setup.* / recipe.* / instance.* / meta.version action_overrides out-of-scope per RESEARCH Pitfall 3.
- `docs/COVERAGE.md` — full regenerated report with bucket summary header.

<tasks>

<task type="auto">
  <name>Pin OpenAPI v4.1.2 + write OPENAPI.md provenance</name>
  <files>docs/coolify_openapi.yaml, docs/coolify_openapi.json, docs/OPENAPI.md</files>
  <read_first>
    - .planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md (D-07, D-08, D-09)
    - .planning/phases/23-openapi-coverage-npm-release/23-PATTERNS.md (OPENAPI.md analog docs/en/setup.md)
    - .planning/spikes/CONVENTIONS.md (exact tag pin, not v4.x)
    - .planning/spikes/001-coolify-api-surface/README.md (ABSENT endpoints)
    - docs/coolify_openapi.yaml (current committed baseline)
  </read_first>
  <action>
    Fetch Coolify OpenAPI from tag v4.1.2: curl -fsSL https://raw.githubusercontent.com/coollabsio/coolify/v4.1.2/openapi.yaml -o docs/coolify_openapi.yaml. Convert to JSON for docs/coolify_openapi.json (node script or yq — use existing yaml dep). Create docs/OPENAPI.md with sections: Source, Pinned tag v4.1.2, Fetch date (today), Operation count (run indexOpenApiOperations or count paths), Refresh procedure with exact curl + convert commands, Links to committed yaml/json. Do not embed v4.1.2 only in prose — specs must match upstream v4.1.2 content per D-09.
  </action>
  <verify>
    <automated>test -f docs/OPENAPI.md && test -f docs/coolify_openapi.yaml && test -f docs/coolify_openapi.json</automated>
    <automated>rg -n 'v4\.1\.2' docs/OPENAPI.md | wc -l | awk '$1 >= 2'</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'fs'; import {dereference} from '@scalar/openapi-parser'; const r=await dereference(readFileSync('docs/coolify_openapi.json','utf8')); const n=Object.entries(r.schema?.paths??{}).flatMap(([p,i])=>['get','post','put','patch','delete'].filter(m=>i[m]).map(m=>m)); console.log(n.length); if(n.length&lt;130) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/OPENAPI.md exists with upstream URL containing coollabsio/coolify/v4.1.2
    - docs/coolify_openapi.yaml and .json are tracked and non-empty
    - Dereference yields ≥130 operations (baseline ~136)
    - No v4.x or next float references in OPENAPI.md pin section
  </acceptance_criteria>
  <done>OpenAPI artifacts pinned to v4.1.2 with provenance doc</done>
</task>

<task type="auto" tdd="true">
  <name>Complete coverage-map.yaml + seed overrides</name>
  <files>docs/coverage-map.yaml, docs/coverage-overrides.yaml, scripts/lib/openapi-coverage-join.mjs</files>
  <read_first>
    - tests/integration/docs-parity.test.ts (TOOL_ACTIONS inventory)
    - src/mcp/tools/*.ts (*ActionsCatalog exports)
    - src/api/client.ts (grep ^export function names)
    - .planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md (Pattern 2 one-to-many, Pattern 3 overrides)
    - .planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md (D-01, D-02, D-05)
    - .planning/spikes/001-coolify-api-surface/README.md
  </read_first>
  <behavior>
    - Test: every *ActionsCatalog action appears in coverage-map.yaml
    - Test: every openapi key in map exists in dereferenced v4.1.2 spec
    - Test: every client fn in map exists as export in client.ts
    - Override merge: deferred SVC-04 log endpoints do not count as gap
    - action_overrides: non-REST tools classified out-of-scope
  </behavior>
  <action>
    Populate docs/coverage-map.yaml with actions: entries for all MCP tools per docs-parity TOOL_ACTIONS / catalog grep. Link client column to actual src/api/client.ts export names; openapi column to METHOD /path keys from v4.1.2 spec. For orchestration tools (setup, recipe, diagnose.scan, emergency.*), map best-effort client calls or leave openapi empty with matching action_override. Seed docs/coverage-overrides.yaml per D-05: overrides for GET /services/{uuid}/logs and DB logs deferred (SVC-04); execute_command out-of-scope; action_overrides for manifest.*, docs.search, setup.*, recipe.*, instance.*, meta.version. Extend join.mjs if needed for partial-coverage rules (application.create multi-endpoint — bucket covered only when all required ops linked per RESEARCH Pitfall 2). ponytail: manual map maintenance ceiling documented in OPENAPI.md or COVERAGE footer.
  </action>
  <verify>
    <automated>pnpm test tests/openapi-coverage.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -c '^  - action:' docs/coverage-map.yaml | awk '$1 >= 100'
    - rg -n 'SVC-04|execute_command' docs/coverage-overrides.yaml | wc -l | awk '$1 >= 2'
    - rg -q 'action_overrides' docs/coverage-overrides.yaml
    - Vitest map completeness tests pass
  </acceptance_criteria>
  <done>Full join data + overrides seeded; map validation tests pass</done>
</task>

<task type="auto">
  <name>Regenerate full COVERAGE.md + flip remaining tests GREEN</name>
  <files>docs/COVERAGE.md, tests/openapi-coverage.test.ts</files>
  <read_first>
    - scripts/openapi-coverage.mjs
    - scripts/lib/openapi-coverage-render.mjs
    - .planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md (D-03, D-04, D-06)
    - .planning/phases/21-deploy-watch/COVERAGE.md (table format reference)
  </read_first>
  <action>
    Run pnpm run openapi:coverage to regenerate docs/COVERAGE.md with full action table and four-bucket summary counts in header. Commit regenerated file. Flip any remaining it.fails in tests/openapi-coverage.test.ts to bare it. Ensure pnpm test includes --check drift via vitest (D-06 satisfied through required Lint, Test &amp; Build job). Run pnpm run openapi:coverage -- --check to confirm zero drift.
  </action>
  <verify>
    <automated>pnpm run openapi:coverage -- --check</automated>
    <automated>pnpm test tests/openapi-coverage.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - docs/COVERAGE.md contains four bucket names in summary
    - rg -c '^\|' docs/COVERAGE.md | awk '$1 >= 20' (table rows)
    - pnpm test tests/openapi-coverage.test.ts exits 0 with zero it.fails
    - pnpm run openapi:coverage -- --check exits 0
  </acceptance_criteria>
  <done>Full committed gap report; all openapi-coverage tests GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream GitHub → committed spec | Supply-chain: pinned tag fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04 | Tampering | OpenAPI upstream fetch | medium | mitigate | Pin v4.1.2 tag; document provenance in OPENAPI.md; review diff on refresh |
| T-23-05 | Repudiation | gap vs deferred misclassification | medium | mitigate | Versioned coverage-overrides.yaml with reasons (D-05) |
| T-23-02 | Repudiation | stale COVERAGE.md | medium | mitigate | --check in test suite (D-06) |
</threat_model>

<verification>
- pnpm run openapi:coverage -- --check
- pnpm test tests/openapi-coverage.test.ts -x
- rg v4.1.2 docs/OPENAPI.md
</verification>

<success_criteria>
- Full MCP action surface documented in committed COVERAGE.md
- OpenAPI spec pinned v4.1.2 with provenance
- All openapi-coverage vitest GREEN
- Overrides seed known deferrals per D-05
</success_criteria>

<output>
Create `.planning/phases/23-openapi-coverage-npm-release/23-02-SUMMARY.md` when done.
</output>
