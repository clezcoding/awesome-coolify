---
phase: 23-openapi-coverage-npm-release
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/openapi-coverage.test.ts
  - tests/npm-pack-allowlist.test.ts
autonomous: true
requirements:
  - OAPI-01
  - OAPI-02
  - PUB-02
user_setup: []

must_haves:
  truths:
    - "tests/openapi-coverage.test.ts exists with it.fails RED scaffolds for indexOpenApiOperations count, classifyRows buckets, and assertCoverageFresh stale check (OAPI-01, OAPI-02)"
    - "tests/npm-pack-allowlist.test.ts exists with it.fails RED scaffolds for forbidden-prefix absence and allowed dist/LICENSE/README paths (PUB-02, D-13)"
    - "All Wave 0 scaffolds use vitest it.fails so husky pre-commit stays green until Plans 23-01..23-03 flip to bare it"
    - "openapi-coverage.test.ts uses dynamic import of scripts/lib/*.mjs modules where absent (mirrors Phase 22 Wave 0)"
    - "{ statement: 'Wave 0 creates no production generator scripts or committed COVERAGE.md', verification: backstop }"
  artifacts:
    - "tests/openapi-coverage.test.ts (new RED)"
    - "tests/npm-pack-allowlist.test.ts (new RED)"
  key_links:
    - "openapi-coverage.test.ts dynamic import → ../scripts/lib/openapi-coverage-parse.mjs (Plan 23-01)"
    - "openapi-coverage.test.ts dynamic import → ../scripts/lib/openapi-coverage-join.mjs (Plan 23-01)"
    - "npm-pack-allowlist.test.ts execFile npm pack → package.json files allowlist (Plan 23-03)"
  prohibitions:
    - statement: "Do not create scripts/openapi-coverage.mjs or docs/COVERAGE.md in Wave 0 — tests only"
      status: enforced
      verification: "test ! -f scripts/openapi-coverage.mjs && test ! -f docs/COVERAGE.md"
---

<objective>
Wave 0 RED test scaffolds for Phase 23 — OpenAPI coverage generator (parse/join/render/--check) and npm pack tarball allowlist.

Purpose: Lock OAPI-01, OAPI-02, and PUB-02 acceptance as failing `it.fails` tests before implementation so Plans 23-01..23-03 flip them GREEN without rewriting intent. Mirrors Phase 22 Wave 0 pattern.

Output: New `tests/openapi-coverage.test.ts` and `tests/npm-pack-allowlist.test.ts` with `it.fails` scaffolds.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md
@.planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md
@.planning/phases/23-openapi-coverage-npm-release/23-PATTERNS.md
@.planning/phases/23-openapi-coverage-npm-release/23-VALIDATION.md
@tests/release-publish-gate.test.ts
@package.json
</context>

## Artifacts this phase produces

- `tests/openapi-coverage.test.ts` — RED `it.fails` scaffolds: `indexOpenApiOperations` returns ≥136 ops from committed spec; `classifyRows` assigns four buckets per D-04; `assertCoverageFresh` / `--check` contract rejects stale `docs/COVERAGE.md`.
- `tests/npm-pack-allowlist.test.ts` — RED `it.fails` scaffolds: after `pnpm run build`, `npm pack --dry-run --json` lists no forbidden prefixes (`scripts/`, `tests/`, `.planning/`, `.github/`, `skills/`, `docs/coolify_openapi`); allows `dist/`, `LICENSE`, `package.json`, README.

<tasks>

<task type="tdd">
  <name>Task 1: openapi-coverage.test.ts RED scaffolds</name>
  <files>tests/openapi-coverage.test.ts</files>
  <read_first>
    - .planning/phases/23-openapi-coverage-npm-release/23-PATTERNS.md (tests/openapi-coverage.test.ts pattern, import .mjs from tests)
    - .planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md (Pattern 1 dereference, Pattern 3 four buckets, Pattern 4 --check drift)
    - .planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md (D-01, D-02, D-04, D-06)
    - tests/release-publish-gate.test.ts (import ../scripts/lib/*.mjs pattern)
    - docs/coolify_openapi.json (committed spec — 136 ops baseline)
  </read_first>
  <behavior>
    - Dynamic-import indexOpenApiOperations from ../scripts/lib/openapi-coverage-parse.mjs
    - Case op_count: returns array length ≥136 with keys matching METHOD /path shape
    - Dynamic-import classifyRows from ../scripts/lib/openapi-coverage-join.mjs
    - Case buckets: fixture row with linked openapi+client → covered; override deferred → deferred; no link no override → gap; action_override out-of-scope → out-of-scope (D-04)
    - Case drift: import assertCoverageFresh or runCliCheck from ../scripts/openapi-coverage.mjs — when docs/COVERAGE.md missing or stale vs generator output, throws or exits non-zero (D-06)
    - All cases use it.fails until Plan 23-01 implements modules
  </behavior>
  <action>
    Create tests/openapi-coverage.test.ts per 23-PATTERNS.md. Use it.fails for every case. Dynamic-import modules that do not exist yet. Assert three-layer row shape intent: tool.action primary key per D-01/D-02. Do not create scripts/openapi-coverage.mjs or lib modules in Wave 0.
  </action>
  <verify>
    <automated>pnpm test tests/openapi-coverage.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File tests/openapi-coverage.test.ts exists
    - rg -c "it\.fails" tests/openapi-coverage.test.ts | awk '$1 >= 3'
    - rg -n "indexOpenApiOperations|classifyRows|assertCoverageFresh" tests/openapi-coverage.test.ts | wc -l | awk '$1 >= 3'
    - rg -n "covered|deferred|out-of-scope|gap" tests/openapi-coverage.test.ts | wc -l | awk '$1 >= 4'
    - pnpm test tests/openapi-coverage.test.ts exits 0
  </acceptance_criteria>
  <done>openapi-coverage RED scaffolds committed; husky green via it.fails</done>
</task>

<task type="tdd">
  <name>Task 2: npm-pack-allowlist.test.ts RED scaffolds</name>
  <files>tests/npm-pack-allowlist.test.ts</files>
  <read_first>
    - .planning/phases/23-openapi-coverage-npm-release/23-PATTERNS.md (npm-pack-allowlist.test.ts pattern)
    - .planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md (Pattern 5 forbidden prefixes, allowed README/LICENSE)
    - .planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md (D-13, D-14)
    - package.json (files allowlist lines 26-30)
    - .planning/phases/18-ci-cd-hardening/18-04-PLAN.md (inline pack verify precedent)
  </read_first>
  <behavior>
    - beforeAll: pnpm run build (or skip with note if dist missing — scaffold may it.fails on pack)
    - Case forbidden_absent: parse npm pack --dry-run --json; no path matching ^scripts/, ^tests/, ^\.planning/, ^\.github/, ^skills/, ^docs/coolify_openapi
    - Case allowed_present: at least one dist/ path, package.json, LICENSE present
    - Use execFileSync from node:child_process per RESEARCH Pattern 5
    - it.fails until Plan 23-03 implements assertions (scaffold documents expected helper getPackPaths())
  </behavior>
  <action>
    Create tests/npm-pack-allowlist.test.ts with it.fails cases for forbidden-prefix gate and allowed-surface presence per D-13. Export or inline FORBIDDEN_PREFIXES regex array matching RESEARCH Pattern 5. Do not run real publish. Complements existing pnpm run publint — does not replace it (D-14).
  </action>
  <verify>
    <automated>pnpm test tests/npm-pack-allowlist.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File tests/npm-pack-allowlist.test.ts exists
    - rg -c "it\.fails" tests/npm-pack-allowlist.test.ts | awk '$1 >= 2'
    - rg -n "scripts/|\.planning/|coolify_openapi" tests/npm-pack-allowlist.test.ts | wc -l | awk '$1 >= 2'
    - pnpm test tests/npm-pack-allowlist.test.ts exits 0
  </acceptance_criteria>
  <done>npm-pack allowlist RED scaffold committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → npm pack subprocess | Local read-only pack; no publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-00-01 | Information Disclosure | pack test | low | accept | Wave 0 scaffold only; no secrets in test fixtures |
| T-23-SC | Tampering | npm installs | low | accept | No new packages in Wave 0 |
</threat_model>

<verification>
- pnpm test tests/openapi-coverage.test.ts tests/npm-pack-allowlist.test.ts -x
</verification>

<success_criteria>
- All Wave 0 scaffolds use it.fails and exit 0
- No production generator scripts or COVERAGE.md created
- RED tests document OAPI-01/OAPI-02/PUB-02 intent
</success_criteria>

<output>
Create `.planning/phases/23-openapi-coverage-npm-release/23-00-SUMMARY.md` when done.
</output>
