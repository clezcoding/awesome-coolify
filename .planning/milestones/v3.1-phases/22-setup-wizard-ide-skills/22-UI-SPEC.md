---
phase: 22
slug: setup-wizard-ide-skills
status: approved
shadcn_initialized: false
preset: none
created: 2026-07-26
reviewed_at: 2026-07-26
---

# Phase 22 — UI Design Contract

> Visual and interaction contract for Setup Wizard & IDE Skills. Static docs + agent-facing structured responses — no React SPA. Generated by gsd-ui-researcher.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (static HTML + shared.css) |
| Icon library | none (CSS dots/badges; no icon font) |
| Font | Display: Chakra Petch · Body: Plus Jakarta Sans · Mono: JetBrains Mono (`docs/shared.css`) |

**Stack note:** Extend `docs/shared.css` and existing page shells (`mesh-hero`, `glass`, `glass-elevated`, `notice`, `pre`, `btn`). No new CSS framework. Source: existing `docs/install.html` + `docs/index.html`.

---

## Surface Inventory

| Surface | Path / channel | Role |
|---------|----------------|------|
| Install configurator (extend) | `docs/install.html` | MCP client config + **new** skills install block + link to setup docs |
| Landing cross-links | `docs/index.html` | Bento card or nav link to skills + setup docs (mirror install section tone) |
| Setup guide (new) | `docs/en/setup.md` (+ optional `docs/setup.html` shell) | Documents MCP `setup` tool flow, modes, soft-pause, resume — **not** an interactive wizard |
| Agent structured responses | MCP `setup` tool `structuredContent` + `_formattedText` | Soft-pause banners, step progress, recovery copy |
| README install (EN/DE) | `README.md`, `README.de.md` | Skills CLI command + setup pointer; visual/copy parity with docs |
| Skill packs | `skills/coolify-*/SKILL.md` | Markdown-only; monospace for commands; no custom CSS |

**Out of scope:** Full React setup wizard SPA (D-01 locks MCP `setup` as primary entry).

---

## Spacing Scale

Declared values (multiples of 4; align with `shared.css` custom properties):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Eyebrow dot gap, inline icon gaps |
| sm | 8px | `--space-2`; badge row, btn-row gaps |
| md | 16px | `--space-4`; default card padding unit, form field margin-bottom |
| lg | 24px | `--space-6`; section internal gaps, card padding (`--space-6` in `.card`) |
| xl | 32px | `--space-8`; nav-bar margin, output-section margin-top |
| 2xl | 48px | `--space-12`; container vertical padding |
| 3xl | 64px | `--space-16`; page bottom padding, hero breathing room |

Exceptions:

- Touch targets: `.btn` padding `12px 16px` (`--space-3` vertical + `--space-4` horizontal) → min-height 44px with 16px body line — grid-aligned, not `0.7rem/1.2rem`.
- Monospace command rows in `pre`: vertical padding `16px` (`--space-4`).

---

## Typography

Four roles only — inputs, hints, badges, and `pre` use the nearest role token (no extra sizes).

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Label | 14px | 400 | 1.5 |
| Body | 16px | 400 | 1.6 |
| Heading | 24px (`section-title`; clamp 1.35–1.5rem) | 600 | 1.2 |
| Display | 40px (`display-title`; clamp 2–2.5rem hero only) | 600 | 0.95 |

Mono commands (`pre`, CLI snippets, agent status): JetBrains Mono at **Body 16px** — same size, mono family only.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#050505` (`--bg-primary`) + grid/noise overlays | Page background, body |
| Secondary (30%) | `rgba(255,255,255,0.03–0.05)` glass + `#0a0a0a` elevated | Cards, forms, `pre` blocks, client grid |
| Accent (10%) | `#6b16ed` / `#8b3dff` (`--accent`, `--accent-bright`) | Primary buttons, focus rings, badge--accent, hover border glow |
| Destructive | `#ef4444` (new token `--destructive` if needed) | Destructive actions only — **none in Phase 22 setup** |
| Success / info | `#22c55e` (`--success`), gold `#fcd34d` (`--gold`) | Security notices, eyebrow, nav hover |
| Pause / human | Gold border `rgba(252,211,77,0.35)` + bg `rgba(252,211,77,0.08)` | Soft-pause notices (new `.notice--pause`) |

Accent reserved for: primary CTA buttons (`.btn`), primary deeplinks, focus rings on inputs, `badge--accent`, active step indicator in setup docs, copy-button success flash border — **not** body text or large backgrounds.

**60/30/10 rule:** Hero mesh image is decorative (one flourish max); all form/setup content sits on calm glass panels without additional shaders or wave animations on the same page.

### Focal points (per screen)

| Screen | Primary focal | Secondary |
|--------|---------------|-----------|
| `install.html` | **Generate config** CTA (accent `.btn`) | Skills install command block (mono `pre` + Copy) |
| `docs/en/setup.md` | Active step row (`.setup-step--active`) | Resume / preflight CTA in first notice |
| `index.html` cross-link | Install + Skills bento cards (equal weight) | — |

---

## Motion & Interaction

CSS-first; durations ≤ 300ms for UI; `prefers-reduced-motion` already in `shared.css` — preserve and extend.

| Element | Properties | Duration | Easing | Notes |
|---------|------------|----------|--------|-------|
| Input focus | `border-color`, `box-shadow` | 200ms | ease-out | Match existing `input:focus`; never `transition: all` |
| Primary button hover | `background-color`, `transform`, `box-shadow` | 300ms | `cubic-bezier(0.34, 1.56, 0.64, 1)` | Existing `.btn`; add `:active { transform: scale(0.97) }` |
| Secondary button | `border-color`, `background-color` | 200ms | ease-out | No scale on hover for secondary |
| Copy button feedback | `background-color`, `border-color` | 0ms text swap; 160ms | ease-out on bg | Instant label change ("Copied"); optional brief success border |
| Output section reveal | `opacity`, `transform: translateY(8px)` | 250ms | `cubic-bezier(0.23, 1, 0.32, 1)` | On `[hidden]` removal; start `opacity:0; translateY(8px)` — **never** `scale(0)` |
| Card hover (docs only) | `transform`, `box-shadow`, `border-color` | 350ms | spring-ish cubic | Existing `.card:hover`; disable transform on `@media (prefers-reduced-motion: reduce)` |
| Step progress (setup docs) | `width` or `opacity` on indicator | 200ms | ease-out | Occasional-use wizard docs — standard animation OK |
| Eyebrow pulse | `opacity` | 2s | ease-in-out | Existing `pulse-glow`; reduced-motion overrides to 0.01ms |

Popover/tooltip: if added for copy hints, `transform-origin` at trigger edge; 125–200ms ease-out; `scale(0.97)` + opacity from `scale(0.95)` entry — not `scale(0)`.

---

## Docs Components (CSS classes)

Extend `docs/shared.css` — do not duplicate in page inline styles.

| Class | Purpose |
|-------|---------|
| `.notice` | Existing success/security info (green) |
| `.notice--pause` **(new)** | Human-action / soft-pause documentation (gold) |
| `.notice--warning` **(new)** | Optional flags off by default, gh prerequisite |
| `.setup-steps` **(new)** | Ordered list with monospace step labels |
| `.setup-step--active` **(new)** | Accent left border + gold/ accent label |
| `.setup-step--done` **(new)** | Muted + success check via `::before` or text `✓` |
| `.terminal-block` **(new)** | Optional: dark `pre` with header row ("Setup status") for docs — developer-tools vertical |
| `.skills-command` **(new)** | Wrapper for skills CLI `pre` + copy button row (reuse `#copy-btn` pattern) |

**Hero rule:** ONE flourish per page — keep existing mesh hero on `install.html`; setup docs page uses same mesh-hero pattern OR static hero scrim without second animated layer.

---

## Agent-Visible UX Contract (MCP `setup` tool)

Clarity over decoration. Structured fields drive agent UI; `_formattedText` is the human-readable banner.

### Success / in-progress response shape

```json
{
  "ok": true,
  "data": {
    "setup_status": "in_progress" | "complete",
    "mode": "greenfield" | "link-existing",
    "current_step": "gh_preflight" | "repo" | "linkage" | "recipe" | "manifest" | "domains" | "env" | "deploy_watch",
    "steps_completed": ["gh_preflight", "linkage"],
    "steps_remaining": ["recipe", "manifest"],
    "manifest_path": ".coolify/manifest.json",
    "deployment_uuid": "<optional when deploy_and_watch>"
  },
  "_formattedText": "…",
  "_meta": { "truncated": false, "chars": N, "max_chars": 8000 }
}
```

### Soft-pause response shape (`needs_human`)

Use `ok: false` with dedicated code OR `ok: true` with `setup_status: "paused"` — **pick one in implementation; contract prefers:**

```json
{
  "ok": false,
  "error": {
    "code": "COOLIFY_SETUP_PAUSED",
    "message": "GitHub CLI is not authenticated.",
    "recoveryHints": [
      "Install GitHub CLI: https://cli.github.com/ (or brew install gh)",
      "Run: gh auth login",
      "Re-call: setup({ action: \"resume\" }) after auth succeeds"
    ],
    "data": {
      "pause_reason": "gh_unauthenticated" | "gh_missing",
      "resume_action": "resume",
      "resume_params": {}
    }
  },
  "_formattedText": "…"
}
```

**Never** poll-wait inside the tool (D-06). No spinner semantics in MCP — pause is immediate.

### `_formattedText` banner templates (exact copy)

**Paused — gh missing:**

```
⏸ SETUP PAUSED — GitHub CLI not found

Human action required:
1. Install gh: https://cli.github.com/
2. Run: gh auth login
3. Re-call: setup({ action: "resume" })
```

**Paused — gh unauthenticated:**

```
⏸ SETUP PAUSED — GitHub CLI not authenticated

Human action required:
1. Run: gh auth login
2. Re-call: setup({ action: "resume" })
```

**In progress (example):**

```
▶ SETUP IN PROGRESS — greenfield · step: linkage

Completed: gh_preflight
Next: recipe → manifest

Optional (off by default): include_domains, set_env, deploy_and_watch
```

**Complete:**

```
✓ SETUP COMPLETE

Manifest: .coolify/manifest.json
Mode: link-existing | greenfield

Suggested: git push (manual — never auto-pushed)
```

**Deploy watch timeout (when flag enabled):**

```
⚠ DEPLOY WATCH TIMED OUT

deployment_uuid: <uuid>
Status: in_progress (or last known)

Recovery:
• deployment({ action: "get", deployment_uuid: "<uuid>" })
• deployment({ action: "watch", deployment_uuid: "<uuid>", timeout: 300 })
```

Use Unicode symbols sparingly; agents parse `recoveryHints` first. `_formattedText` mirrors hints for humans.

### Step progress semantics

| Step key | Display label | Shown when |
|----------|---------------|------------|
| `gh_preflight` | GitHub CLI preflight | Always first |
| `repo` | GitHub repo (greenfield) | `mode: greenfield` |
| `linkage` | Coolify project / env / server | Both modes |
| `recipe` | App from recipe | Greenfield core |
| `manifest` | Write manifest | Both modes |
| `domains` | Domains | `include_domains: true` |
| `env` | Environment vars | `set_env: true` |
| `deploy_watch` | Deploy + watch | `deploy_and_watch: true` |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (install.html MCP) | **Generate config** (existing — do not change) |
| Primary CTA (skills block) | **Copy install command** |
| Skills install command (canonical) | `npx skills add clezcoding/awesome-coolify -a cursor -a claude-code -a codex` |
| Skills install alt | `npx skills add clezcoding/awesome-coolify --all` (document as optional in hint) |
| Setup docs CTA | **Open setup guide** (links `docs/en/setup.md`) |
| Empty state heading (no MCP config yet) | **Install the MCP server first** |
| Empty state body | Point to `install.html` or deeplink; skills require MCP + Coolify credentials before workflows run. |
| Error state (docs JS / clipboard) | **Could not copy** — Select the command and copy manually. |
| Error state (setup tool generic) | Problem in `error.message` + numbered `recoveryHints` (no generic "Something went wrong") |
| Soft-pause heading | **SETUP PAUSED** — `{reason}` (see banner templates) |
| Destructive confirmation | None in Phase 22 (recipe create has no confirm per established pattern) |
| README skills section title (EN) | **IDE skills (Cursor, Claude Code, Codex)** |
| README setup pointer | After MCP install, run `setup({ action: "preflight" })` or see [Setup guide](docs/en/setup.md) |

**README / docs consistency:** Same command strings everywhere — no divergent repo slug or agent flag spelling. DE README mirrors EN structure; commands stay English.

---

## UI Considerations

Applicable state considerations resolved: 18 covered, 3 backstop, 0 unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | install form (E1) | ✅ covered | `#output-section` stays `[hidden]` until first Generate — no empty snippet panel |
| loading | install form (E1) | ✅ covered | Client-side sync generation — no spinner; output reveals via 250ms opacity/translateY |
| error | install form (E1) | ✅ covered | Clipboard fail → Copywriting **Could not copy** + manual select hint |
| partial | install form (E1) | ✅ covered | Partial form (URL only) still generates snippet with placeholder token |
| overflow | install form (E1) | ✅ covered | `pre` horizontal scroll + `overflow-x: auto` on config output |
| long-text | install form (E1) | ✅ covered | Long URLs/tokens wrap inside `pre`; no ellipsis on secrets |
| overflow | skills block (E2) | ✅ covered | `pre.skills-command` scrolls horizontally; copy row stays fixed |
| long-text | skills block (E2) | ✅ covered | Full CLI command visible via scroll — no truncation |
| empty | setup-steps (E3) | ✅ covered | Static docs always show full step list; optional steps omitted when flags off |
| loading | setup-steps (E3) | ✅ covered | No async fetch on setup docs — static markdown, no skeleton |
| error | setup-steps (E3) | ✅ covered | MCP errors use `error.message` + numbered `recoveryHints` (Copywriting Contract) |
| populated | setup-steps (E3) | ✅ covered | Happy path: all core steps listed with `.setup-step--done` checkmarks |
| partial | setup-steps (E3) | ✅ covered | Agent `steps_completed` / `steps_remaining` arrays mirror partial wizard progress |
| overflow | setup-steps (E3) | 🧪 backstop | `{ statement: "8+ setup steps scroll inside container without layout break", verification: backstop }` |
| zero-one-many | setup-steps (E3) | ✅ covered | Optional steps (domains/env/deploy_watch) hidden when flags false — singular copy per mode |
| unclassified | MCP responses (E4) | ✅ covered | Soft-pause / in-progress / complete / timeout banners defined in Agent-Visible UX Contract |
| unclassified | notice banners (E5) | ✅ covered | `.notice`, `.notice--pause`, `.notice--warning` classes with gold/green semantic borders |
| overflow | copy button (E6) | ✅ covered | Button label swaps in-place; no container growth |
| long-text | copy button (E6) | ✅ covered | "Copied" / "Could not copy" fixed-width labels |
| overflow | empty state (E7) | ✅ covered | Empty-state block fits single glass card |
| long-text | empty state (E7) | ✅ covered | Heading + body under 80 chars; no truncation |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required — static HTML phase |
| third-party | none | not required |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-07-26
