---
phase: 22-setup-wizard-ide-skills
plan: 03
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
files_modified:
  - skills/coolify-deploy/SKILL.md
  - skills/coolify-diagnose/SKILL.md
  - skills/coolify-incident/SKILL.md
  - skills/coolify-setup/SKILL.md
  - src/skills/skills-manifest.test.ts
  - docs/install.html
  - docs/index.html
  - docs/en/setup.md
  - docs/shared.css
  - README.md
  - README.de.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
user_setup: []

must_haves:
  truths:
    - "Four skills exist at skills/coolify-{setup,deploy,diagnose,incident}/SKILL.md discoverable by npx skills add clezcoding/awesome-coolify (SKILL-01, D-14/D-15)"
    - "README.md and README.de.md document npx skills add clezcoding/awesome-coolify -a cursor -a claude-code -a codex and link docs/en/setup.md (D-17)"
    - "docs/install.html includes skills-command block with canonical CLI + copy button; tool count 18 tools (UI-SPEC; setup is 18th registerTool per 22-01)"
    - "docs/en/setup.md documents MCP setup tool preflight/wire/resume, modes, soft-pause .notice--pause, optional flags .notice--warning (not CLI wizard)"
    - "docs/shared.css defines .notice--pause, .notice--warning, .setup-steps, .setup-step--active, .setup-step--done, .skills-command (UI-SPEC)"
    - "coolify-deploy documents deployment.watch primary over application.deploy wait:true, timeout re-watch, COOLIFY_WATCH_TIMEOUT recovery (SKILL-02, D-16)"
    - "coolify-diagnose and coolify-incident document reveal opt-in and confirm gates for destructive ops (SKILL-02)"
    - "All four skills reference recipe actions create-git-app, create-app-db, create-one-click and MCP prompts align with src/mcp/prompts.ts (SKILL-02)"
    - "skills-manifest.test.ts GREEN: frontmatter, deployment.watch, confirm mentions per skill"
    - "docs/index.html links Setup guide and IDE skills install"
    - "{ statement: '8+ setup steps in docs scroll inside .setup-steps without horizontal page break', verification: backstop }"
  artifacts:
    - "skills/coolify-deploy/SKILL.md"
    - "skills/coolify-diagnose/SKILL.md"
    - "skills/coolify-incident/SKILL.md"
    - "docs/en/setup.md"
    - "docs/install.html (skills block)"
    - "docs/shared.css (setup + pause classes)"
    - "README.md / README.de.md (skills section)"
  key_links:
    - "skills action names → src/mcp/tools/*ActionsCatalog strings"
    - "coolify-deploy → deployment.watch timeout 300 pattern from Phase 21"
    - "docs/install.html skills block → same command as README D-17"
  prohibitions:
    - statement: "No hand-maintained per-IDE skill copies as source of truth (D-13)"
      status: enforced
      verification: "rg -l 'coolify-deploy' .cursor/skills .claude/skills 2>/dev/null | wc -l returns 0 new copies"
    - statement: "Skills must not teach application.deploy wait:true as primary monitor"
      status: enforced
      verification: "rg -n 'wait:true' skills/coolify-deploy/SKILL.md includes legacy note only, not primary path"
---

<objective>
Ship remaining three workflow skills, skills manifest test, static docs (install/setup/README), and SKILL-02 safety/watch/recipe coverage.

Purpose: Complete IDE skill distribution and user-facing docs for Cursor, Claude Code, Codex per D-13–D-17 and SKILL-01/02.

Output: `coolify-deploy`, `coolify-diagnose`, `coolify-incident` SKILL.md files; `docs/en/setup.md`; extended install.html/index.html/shared.css; README EN/DE; GREEN skills-manifest.test.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md
@.planning/phases/22-setup-wizard-ide-skills/22-RESEARCH.md
@.planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md
@.planning/phases/22-setup-wizard-ide-skills/22-UI-SPEC.md
@.planning/phases/22-setup-wizard-ide-skills/22-01-PLAN.md
@.planning/phases/22-setup-wizard-ide-skills/22-02-PLAN.md
@src/mcp/prompts.ts
@src/mcp/tools/diagnose.ts
@src/mcp/tools/recipe.ts
@docs/install.html
@docs/en/cloud.md
@README.md
</context>

## Artifacts this phase produces

- `skills/coolify-deploy/SKILL.md` — deploy → deployment.watch workflow from prompts.ts deploy prompt.
- `skills/coolify-diagnose/SKILL.md` — diagnose actions catalog + reveal opt-in.
- `skills/coolify-incident/SKILL.md` — incident workflow + confirm gates on emergency actions.
- `src/skills/skills-manifest.test.ts` — flipped GREEN with SKILL-02 content asserts.
- `docs/en/setup.md` — setup guide markdown.
- `docs/install.html` — skills install block + 18 tools hero.
- `docs/shared.css` — pause/setup/skills CSS classes.
- `docs/index.html` — setup + skills cross-links.
- `README.md`, `README.de.md` — IDE skills install subsection.

<tasks>

<task type="auto">
  <name>Ship coolify-deploy, coolify-diagnose, coolify-incident SKILL.md files</name>
  <files>skills/coolify-deploy/SKILL.md, skills/coolify-diagnose/SKILL.md, skills/coolify-incident/SKILL.md, skills/coolify-setup/SKILL.md</files>
  <read_first>
    - src/mcp/prompts.ts (deploy lines 18-62, diagnose 65-103, incident 154-196, new-project 106-151)
    - src/mcp/tools/diagnose.ts (diagnoseActionsCatalog line 70)
    - src/mcp/tools/recipe.ts (recipeActionsCatalog, create-git-app/create-app-db/create-one-click)
    - .planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md (skill ↔ prompt alignment table)
    - .planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md (D-15, D-16)
    - skills/coolify-setup/SKILL.md (Plan 22-01 — align tone and cross-links)
  </read_first>
  <action>
    Create three SKILL.md files per D-15. coolify-deploy: copy deploy prompt watch-primary steps — application deploy wait false, deployment.watch timeout 300, COOLIFY_WATCH_TIMEOUT re-watch, failed/cancelled surface; note wait:true legacy only. coolify-diagnose: document diagnose tool actions from diagnoseActionsCatalog; reveal opt-in for secrets; confirm for destructive ops. coolify-incident: diagnose → logs → restart → emergency with confirm gates. Each skill includes recipe tool mention (create-git-app, create-app-db, create-one-click) where relevant per SKILL-02. Update coolify-setup after Plan 22-02 wire lands: cross-link sibling skills and document wire action modes/optional flags using live setupActionsCatalog from setup.ts (depends_on 22-02). Action names must match live *ActionsCatalog strings verbatim. Do not flip skills-manifest.test.ts here — Task 2 owns all it.fails→it flips after all four SKILL.md files exist.
  </action>
  <verify>
    <automated>test -f skills/coolify-deploy/SKILL.md && test -f skills/coolify-diagnose/SKILL.md && test -f skills/coolify-incident/SKILL.md && rg -n "deployment\.watch|deployment\(\{ action: \"watch\"" skills/coolify-deploy/SKILL.md && rg -n "confirm|reveal" skills/coolify-diagnose/SKILL.md skills/coolify-incident/SKILL.md && rg -n "create-git-app|create-app-db|create-one-click" skills/</automated>
  </verify>
  <acceptance_criteria>
    - test -f skills/coolify-deploy/SKILL.md && test -f skills/coolify-diagnose/SKILL.md && test -f skills/coolify-incident/SKILL.md
    - rg -n "deployment\.watch|deployment\(\{ action: \"watch\"" skills/coolify-deploy/SKILL.md returns at least 1 match
    - rg -n "confirm|reveal" skills/coolify-diagnose/SKILL.md skills/coolify-incident/SKILL.md returns at least 2 matches
    - rg -n "create-git-app|create-app-db|create-one-click" skills/ returns at least 3 matches across files
    - rg -n "COOLIFY_WATCH_TIMEOUT" skills/coolify-deploy/SKILL.md returns at least 1 match
  </acceptance_criteria>
  <done>SKILL-02 content in all four skills; action names aligned with MCP catalogs</done>
</task>

<task type="auto" tdd="true">
  <name>skills-manifest.test.ts GREEN + SKILL-02 grep coverage</name>
  <files>src/skills/skills-manifest.test.ts</files>
  <read_first>
    - .planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md (skills-manifest.test.ts pattern)
    - .planning/phases/22-setup-wizard-ide-skills/22-VALIDATION.md (SKILL-01/02 test map)
    - skills/coolify-*/SKILL.md (all four after Task 1)
  </read_first>
  <behavior>
    - Each skill dir has SKILL.md with frontmatter name matching directory suffix
    - Each SKILL.md contains deployment.watch or deployment watch wording
    - Each SKILL.md contains confirm (safety gates)
    - coolify-deploy contains recipe or deploy workflow steps
    - Each skill references its MCP prompt analog from src/mcp/prompts.ts (SKILL-02): new-project/setup, deploy, diagnose, incident
  </behavior>
  <action>
    Flip remaining it.fails to bare it in src/skills/skills-manifest.test.ts (coolify-setup already bare it from Plan 22-01 Task 2). Assert four directories exist. Assert frontmatter name matches. Assert deployment.watch and confirm substrings per skill (setup may reference watch via deploy_and_watch flag). Add grep-style asserts or readFileSync checks that each SKILL.md names its prompts.ts analog (new-project for setup, deploy, diagnose, incident). Run green.
  </action>
  <verify>
    <automated>pnpm test -- src/skills/skills-manifest.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -c "it\.fails" src/skills/skills-manifest.test.ts returns 0
    - pnpm test -- src/skills/skills-manifest.test.ts -x green
    - rg -n "new-project" skills/coolify-setup/SKILL.md returns at least 1 match
    - rg -n "deploy" skills/coolify-deploy/SKILL.md returns at least 1 match
    - rg -n "diagnose" skills/coolify-diagnose/SKILL.md returns at least 1 match
    - rg -n "incident" skills/coolify-incident/SKILL.md returns at least 1 match
  </acceptance_criteria>
  <done>SKILL-01 automated layout test GREEN</done>
</task>

<task type="auto">
  <name>Docs: install.html, setup.md, shared.css, index.html, README EN/DE</name>
  <files>docs/install.html, docs/index.html, docs/en/setup.md, docs/shared.css, README.md, README.de.md</files>
  <read_first>
    - docs/install.html (notice block, copy-btn JS pattern lines 27-96)
    - docs/en/cloud.md (markdown structure analog)
    - docs/shared.css (.notice lines 383-395)
    - .planning/phases/22-setup-wizard-ide-skills/22-UI-SPEC.md (Copywriting Contract, Docs Components, setup-steps semantics)
    - .planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md (install.html skills block, README subsection)
    - .planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md (D-17)
    - README.md Install section lines 175-235
  </read_first>
  <action>
    Extend docs/shared.css with .notice--pause, .notice--warning, .setup-steps, .setup-step--active, .setup-step--done, .skills-command per UI-SPEC. Add skills install block to docs/install.html with canonical npx command and copy button reusing existing JS pattern; update hero 17→18 tools (setup registers as 18th registerTool in 22-01; live server.ts has 17 today). Create docs/en/setup.md: overview MCP setup primary entry, prerequisites gh + COOLIFY_URL/TOKEN, modes table, step list with optional flags off by default, soft-pause section referencing COOLIFY_SETUP_PAUSED, skills cross-link. Add index.html bento/nav links to setup.md and install.html#skills. Add README.md subsection IDE skills (Cursor, Claude Code, Codex) with install command and setup guide link; mirror structure in README.de.md with German section title per UI-SPEC. Commands stay English in DE README.
  </action>
  <verify>
    <automated>rg -n "npx skills add clezcoding/awesome-coolify" README.md README.de.md docs/install.html docs/en/setup.md && rg -n "notice--pause|setup-steps|skills-command" docs/shared.css</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "npx skills add clezcoding/awesome-coolify -a cursor -a claude-code -a codex" README.md docs/install.html returns at least 2 matches
    - rg -n "18 tools" docs/install.html returns at least 1 match
    - test -f docs/en/setup.md
    - rg -n "COOLIFY_SETUP_PAUSED|setup\(\{ action" docs/en/setup.md returns at least 2 matches
    - rg -n "\.notice--pause|\.skills-command" docs/shared.css returns at least 2 matches
    - rg -n "setup\.md|Setup guide" docs/index.html README.md returns at least 2 matches
  </acceptance_criteria>
  <done>User-facing install + setup docs match D-17 and UI-SPEC copywriting contract</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → user clipboard | Install command must pin clezcoding/awesome-coolify slug |
| skills → agent behavior | Incorrect action names cause failed MCP calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-07 | Spoofing | skills install typosquat in docs | medium | mitigate | Pin exact repo slug clezcoding/awesome-coolify in all surfaces (D-13) |
| T-22-08 | Tampering | Skill doc drift from MCP schemas | medium | mitigate | skills-manifest.test.ts grep catalog strings; copy from prompts.ts |
| T-22-SC | Tampering | npx skills consumer package | low | accept | Document npx skills@latest consumer-only; not bundled in package.json per RESEARCH audit |
</threat_model>

<verification>
- pnpm test -- src/skills/skills-manifest.test.ts -x
- rg consistency for install command across README + docs
- npm run build
</verification>

<success_criteria>
- SKILL-01: four skills + install docs for Cursor, Claude Code, Codex
- SKILL-02: watch, recipes, prompts, confirm/reveal documented
- UI-SPEC considerations lifted to must_haves and docs CSS
- D-17 README EN/DE complete
</success_criteria>

<output>
Create `.planning/phases/22-setup-wizard-ide-skills/22-03-SUMMARY.md` when done.
</output>
