---
phase: 21-deploy-watch
plan: 01
type: execute
wave: 1
depends_on:
  - 21-00
files_modified:
  - src/utils/deploy-watch-poll.ts
  - src/utils/deploy-watch-poll.test.ts
  - src/utils/errors.ts
  - src/utils/errors.test.ts
autonomous: true
requirements:
  - WATCH-01
user_setup: []

must_haves:
  truths:
    - "src/utils/deploy-watch-poll.ts exports pollDeploymentWithBackoff and WatchPollOutcome discriminated union { kind:'terminal' } | { kind:'timeout', elapsedMs } — never synthesizes status timeout on the deployment object (D-03, D-09)"
    - "Backoff uses Equal Jitter clamped to [minIntervalMs, maxIntervalMs] with defaults 3000 / 30000; random injectable for tests (D-06)"
    - "On isRetryableRateLimit hit (HTTP 429), poller sleeps max(nextBackoff, retryAfterMs) and continues — no hard abort solely for 429 (D-08)"
    - "Terminal detection imports TERMINAL_DEPLOYMENT_STATES from deploy-poll.ts (single source of truth)"
    - "toStructuredError attaches data.retry_after (ms) when HTTP status is 429 by parsing Retry-After header (seconds or HTTP-date) (D-08, RESEARCH A5)"
    - "deploy-watch-poll.test.ts it.fails cases flip to bare it and pass green"
    - "src/utils/deploy-poll.ts remains behaviorally unchanged — wait:true fixed 3s path intact (D-03)"
  artifacts:
    - "src/utils/deploy-watch-poll.ts (new)"
    - "src/utils/deploy-watch-poll.test.ts (modified — GREEN)"
    - "src/utils/errors.ts (modified — 429 Retry-After attach)"
    - "src/utils/errors.test.ts (modified — 429 suite)"
  key_links:
    - "pollDeploymentWithBackoff → fetcher() + sleep(equalJitter) → WatchPollOutcome"
    - "isRetryableRateLimit(err) → toStructuredError 429 data.retry_after → continue poll"
    - "TERMINAL_DEPLOYMENT_STATES import from deploy-poll.ts"
  assumptions:
    - "A-EDGE-01: unclassified WATCH-01 edges for backoff/429/timeout outcome covered by this helper contract"
    - "RESEARCH A1: Equal Jitter formula nextDelayMs(attempt,min,max) = clamp(max(min, floor(exp/2 + random*(exp/2))), max) where exp = min(max, min * 2**attempt)"
    - "RESEARCH A5: shared toStructuredError 429 header parse is in scope for D-08"
  prohibitions:
    - statement: "Do not modify pollDeploymentUntilTerminal implementation or its soft timeout return shape (D-03)"
      status: enforced
      verification: "npx vitest run src/utils/deploy-poll.test.ts green AND rg -n \"status: 'timeout'\" src/utils/deploy-watch-poll.ts returns 0"
    - statement: "No forever loop without timeoutMs check"
      status: enforced
      verification: "rg -n \"timeoutMs|elapsedMs|kind: 'timeout'\" src/utils/deploy-watch-poll.ts returns matches for bounded exit"
    - statement: "No new npm packages"
      status: enforced
      verification: "package.json dependencies unchanged in this plan"
---

<objective>
Ship the watch-only backoff/jitter poll helper and preserve Retry-After on HTTP 429 in the shared error path (WATCH-01 polling core per D-03, D-06, D-08).

Purpose: Give `deployment.watch` (Plan 21-02) a dedicated poller that avoids API storms and does not hard-abort on rate limits, while leaving `pollDeploymentUntilTerminal` untouched for `wait:true`.

Output: New `src/utils/deploy-watch-poll.ts`; green `deploy-watch-poll.test.ts`; `toStructuredError` 429 `retry_after` attach + tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-deploy-watch/21-CONTEXT.md
@.planning/phases/21-deploy-watch/21-RESEARCH.md
@.planning/phases/21-deploy-watch/21-PATTERNS.md
@.planning/phases/21-deploy-watch/21-00-PLAN.md
@src/utils/deploy-poll.ts
@src/utils/errors.ts
@src/utils/errors.test.ts
</context>

## Artifacts this phase produces

- `src/utils/deploy-watch-poll.ts` — `export type WatchPollOutcome`, `export async function pollDeploymentWithBackoff(fetcher, options)` with Equal Jitter, injectable `random`, optional `isRetryableRateLimit`, defaults min 3000 / max 30000 ms.
- `src/utils/deploy-watch-poll.test.ts` — flipped GREEN (bare `it`).
- `src/utils/errors.ts` — 429 path attaches `data.retry_after` (milliseconds) from `Retry-After`.
- `src/utils/errors.test.ts` — 429 Retry-After parse cases (delta-seconds + HTTP-date).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pollDeploymentWithBackoff + flip deploy-watch-poll tests GREEN</name>
  <files>src/utils/deploy-watch-poll.ts, src/utils/deploy-watch-poll.test.ts</files>
  <read_first>
    - src/utils/deploy-poll.ts (TERMINAL_DEPLOYMENT_STATES, sleep loop shape — copy pattern only)
    - src/utils/deploy-watch-poll.test.ts (Wave 0 scaffolds to flip)
    - .planning/phases/21-deploy-watch/21-RESEARCH.md (Pattern 1 Equal Jitter formula, WatchPollOutcome)
    - .planning/phases/21-deploy-watch/21-PATTERNS.md (watch helper shape; anti-pattern soft timeout)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (D-03, D-06, D-08, D-09)
  </read_first>
  <behavior>
    - pollDeploymentWithBackoff loops: fetch → if status in TERMINAL_DEPLOYMENT_STATES return { kind:'terminal', deployment }; if elapsed >= timeoutMs return { kind:'timeout', deployment, elapsedMs }; else sleep(nextDelayMs) and continue
    - nextDelayMs uses Equal Jitter clamped to [minIntervalMs, maxIntervalMs] per RESEARCH A1
    - Defaults: minIntervalMs=3000, maxIntervalMs=30000, random=Math.random
    - On fetcher throw: if isRetryableRateLimit(err) returns non-null, sleep max(nextDelayMs, retryAfterMs ?? 0), increment attempt, continue; otherwise rethrow
    - Never assign deployment.status to the wait:true soft-timeout literal
  </behavior>
  <action>
    Create src/utils/deploy-watch-poll.ts exporting WatchPollOutcome and pollDeploymentWithBackoff per D-03/D-06/D-08/D-09 and RESEARCH Pattern 1. Import TERMINAL_DEPLOYMENT_STATES from ./deploy-poll.js — do not duplicate the terminal list. Implement local sleep via setTimeout Promise. Flip all it.fails in deploy-watch-poll.test.ts to bare it and ensure green under fake timers with injectable random. Do not edit deploy-poll.ts.
  </action>
  <verify>
    <automated>npx vitest run src/utils/deploy-watch-poll.test.ts src/utils/deploy-poll.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "export async function pollDeploymentWithBackoff" src/utils/deploy-watch-poll.ts returns 1
    - rg -n "export type WatchPollOutcome" src/utils/deploy-watch-poll.ts returns 1
    - rg -n "TERMINAL_DEPLOYMENT_STATES" src/utils/deploy-watch-poll.ts returns at least 1
    - rg -n "kind: 'timeout'" src/utils/deploy-watch-poll.ts returns at least 1
    - rg -n "status: 'timeout'|status: \"timeout\"" src/utils/deploy-watch-poll.ts returns 0
    - rg -c "it\.fails" src/utils/deploy-watch-poll.test.ts returns 0
    - npx vitest run src/utils/deploy-watch-poll.test.ts src/utils/deploy-poll.test.ts green
    - npm run build green
  </acceptance_criteria>
  <done>
    - Watch-only poller shipped with backoff/jitter/429 continue
    - Wave 0 poll tests GREEN
    - deploy-poll regression still green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach Retry-After on HTTP 429 in toStructuredError</name>
  <files>src/utils/errors.ts, src/utils/errors.test.ts</files>
  <read_first>
    - src/utils/errors.ts (toStructuredError 409 conflicts attach pattern, statusToCode default → COOLIFY_500)
    - src/utils/errors.test.ts (409 conflicts passthrough suite)
    - .planning/phases/21-deploy-watch/21-PATTERNS.md (errors.ts 429 enhancement)
    - .planning/phases/21-deploy-watch/21-RESEARCH.md (Pitfall 3 ofetch + Retry-After)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (D-08)
  </read_first>
  <behavior>
    - When FetchError/response status === 429, envelope.data.retry_after is a number of milliseconds parsed from Retry-After
    - Delta-seconds header value N → retry_after = N * 1000
    - HTTP-date Retry-After → retry_after = max(0, dateMs - Date.now())
    - Missing Retry-After on 429 → no throw; retry_after may be absent; watch poller still continues via backoff (D-08)
    - statusToCode may still map 429 to COOLIFY_500 — watch poller keys off httpStatus / retry_after, not code alone
  </behavior>
  <action>
    In src/utils/errors.ts toStructuredError path that handles numeric status (mirror 409 conflicts attach): when status === 429, read response.headers.get('retry-after') if available, parse delta-seconds or HTTP-date into milliseconds, merge into envelope.data.retry_after. Add errors.test.ts cases mocking response.status 429 with headers.get. Do not add watch-specific error codes here (Plan 21-02). Do not change statusToCode mapping unless needed for httpStatus passthrough already present.
  </action>
  <verify>
    <automated>npx vitest run src/utils/errors.test.ts -t "429"</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "retry_after|Retry-After|retry-after" src/utils/errors.ts returns at least 2 matches
    - rg -n "429" src/utils/errors.test.ts returns at least 2 matches
    - npx vitest run src/utils/errors.test.ts green
    - npm run build green
  </acceptance_criteria>
  <done>
    - 429 responses expose data.retry_after in ms for watch poller isRetryableRateLimit
    - Existing error tests still pass
  </done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Poll helper → Coolify GET /deployments/{uuid} | Untrusted remote status; helper only classifies known terminal strings |
| HTTP error headers → error.data | Retry-After parsed as delay ms only — not executed as code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Denial of Service | pollDeploymentWithBackoff interval schedule | high | mitigate | Enforce min_interval floor, max_interval cap, Equal Jitter, hard timeoutMs exit — prevents agent-triggered API storms (Pitfall 12, D-06, D-05) |
| T-21-01-02 | Denial of Service | HTTP 429 handling | medium | mitigate | Continue with max(backoff, Retry-After); never tight-loop retry on 429 (D-08) |
| T-21-01-03 | Tampering | Retry-After header parse | low | mitigate | Parse only delta-seconds or HTTP-date into finite ms; clamp negative to 0; ignore malformed |
| T-21-SC | Tampering | npm package installs | low | accept | No new packages per Package Legitimacy Audit |
</threat_model>

<verification>
- npx vitest run src/utils/deploy-watch-poll.test.ts green
- npx vitest run src/utils/deploy-poll.test.ts green
- npx vitest run src/utils/errors.test.ts green
- npm run build green
</verification>

<success_criteria>
- Watch-only backoff poller exists and is tested
- 429 Retry-After preserved for continue policy
- wait:true poller untouched (D-03)
</success_criteria>

<output>
Create `.planning/phases/21-deploy-watch/21-01-SUMMARY.md` when done.
</output>
