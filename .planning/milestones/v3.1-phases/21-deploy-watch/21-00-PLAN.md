---
phase: 21-deploy-watch
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/utils/deploy-watch-poll.test.ts
  - src/mcp/tools/deployment.test.ts
  - tests/mcp/prompts.test.ts
autonomous: true
requirements:
  - WATCH-01
  - WATCH-02
user_setup: []

must_haves:
  truths:
    - "src/utils/deploy-watch-poll.test.ts exists with it.fails RED scaffolds for terminal exit, timeout outcome (kind:timeout without synthesizing status timeout on deployment), Equal Jitter delay bounds in [min,max], and 429 continue + Retry-After (D-03, D-06, D-08, D-09)"
    - "src/mcp/tools/deployment.test.ts gains a describe('deployment watch') block with it.fails RED cases for Zod schema defaults via safeParse().data (timeout 300 / min_interval 3 / max_interval 30 / include_logs false — filled by .default() on the flat schema, same Phase 19 pattern as shared-read-params), finished OK summary, timeout dual-signal isError + COOLIFY_WATCH_TIMEOUT, failed/cancelled isError codes, and min_interval > max_interval reject (D-01, D-04, D-05, D-07, D-09, D-11, D-12)"
    - "tests/mcp/prompts.test.ts deploy-prompt assertions are rewritten as it.fails targeting watch-primary 2–4 steps, timeout re-watch, failed messaging, and wait:true legacy note (D-13, D-14, D-15) — no Future Phase 21 wording required"
    - "All Wave 0 scaffolds use vitest it.fails so husky pre-commit (full vitest) stays green until later waves flip to it"
    - "deploy-watch-poll.test.ts uses dynamic import('./deploy-watch-poll.js') so tsup/build stays green before Plan 21-01 ships the helper"
  artifacts:
    - "src/utils/deploy-watch-poll.test.ts (new RED scaffold)"
    - "src/mcp/tools/deployment.test.ts (modified — watch describe it.fails)"
    - "tests/mcp/prompts.test.ts (modified — deploy prompt it.fails target shape)"
  key_links:
    - "deploy-watch-poll.test.ts dynamic import → ./deploy-watch-poll.js (Plan 21-01 symbol target: pollDeploymentWithBackoff, WatchPollOutcome)"
    - "deployment.test.ts watch describe → handleDeploymentAction / deploymentToolSchema watch action (Plan 21-02)"
    - "prompts.test.ts deploy expectations → registerCoolifyPrompts deploy content (Plan 21-03)"
  assumptions:
    - "EDGE WATCH-01 unclassified ($COVERAGE) → author as D-05..D-12 behaviors: backoff+jitter+bounded timeout, dual-signal timeout, failed/cancelled isError, 429 continue (planner assumption A-EDGE-01)"
    - "EDGE WATCH-02 unclassified ($COVERAGE) → author as D-13..D-15 prompt+README Watch docs; IDE skills deferred D-16/Phase 22 (planner assumption A-EDGE-02)"
    - "RESEARCH A1: Equal Jitter clamped to [min,max] is the Wave 1 formula (Claude discretion within D-06)"
    - "RESEARCH A3/A4: agent-facing timeout/interval params in seconds; timeout Zod bounds min 10 / max 1800 (align wait UX); min_interval floor 1s"
  prohibitions:
    - statement: "Do not modify src/utils/deploy-poll.ts or overload pollDeploymentUntilTerminal for watch (D-03)"
      status: enforced
      verification: "git diff --name-only HEAD -- src/utils/deploy-poll.ts src/utils/deploy-poll.test.ts is empty in this plan"
    - statement: "No forever-blocking watch without timeout; no MCP Tasks / background watcher (REQUIREMENTS out of scope)"
      status: enforced
      verification: "Wave 0 scaffolds assert bounded timeoutMs / COOLIFY_WATCH_TIMEOUT paths only"
    - statement: "No new top-level MCP tool; watch is deployment action only (D-01)"
      status: enforced
      verification: "rg -n \"registerTool\\('watch'\" src/mcp/server.ts returns 0"
    - statement: "No IDE skill packs in this phase (D-16 → Phase 22)"
      status: enforced
      verification: "no new files under .cursor/skills/ or .claude/skills/ for Coolify packs"
    - statement: "No OpenAPI coverage work (Phase 23)"
      status: enforced
      verification: "no new OpenAPI map/tooling files in this plan files_modified"
---

<objective>
Wave 0 RED test scaffolds for Phase 21 — deploy-watch-poll helper, deployment.watch action, and deploy prompt watch guidance.

Purpose: Lock WATCH-01 / WATCH-02 acceptance as failing `it.fails` tests before implementation so Plans 21-01..21-03 flip them GREEN without rewriting intent. Mirrors Phase 20 Wave 0 (`it.fails` + dynamic import keeps husky green).

Output: New `src/utils/deploy-watch-poll.test.ts`; extended `deployment.test.ts` watch describe; updated `tests/mcp/prompts.test.ts` target assertions.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-deploy-watch/21-CONTEXT.md
@.planning/phases/21-deploy-watch/21-RESEARCH.md
@.planning/phases/21-deploy-watch/21-PATTERNS.md
@.planning/phases/21-deploy-watch/21-VALIDATION.md
@src/utils/deploy-poll.test.ts
@src/mcp/tools/deployment.test.ts
@tests/mcp/prompts.test.ts
</context>

## Artifacts this phase produces

- `src/utils/deploy-watch-poll.test.ts` — RED `it.fails` scaffolds (dynamic import of `./deploy-watch-poll.js`) for `pollDeploymentWithBackoff` / `WatchPollOutcome`: terminal finished exit, timeout `kind:'timeout'` without `deployment.status === 'timeout'`, Equal Jitter delays ∈ `[minIntervalMs, maxIntervalMs]` with injectable `random`, 429 continue + Retry-After sleep.
- `src/mcp/tools/deployment.test.ts` — new `describe('deployment watch', …)` with `it.fails` cases for schema defaults/rejects and handler outcomes (finished OK, timeout dual-signal, failed/cancelled errors, include_logs default false).
- `tests/mcp/prompts.test.ts` — deploy prompt test rewritten to `it.fails` targeting watch-primary steps (deploy → watch → timeout re-watch → fail surface; wait:true legacy).

<tasks>

<task type="tdd">
  <name>Task 1: Create deploy-watch-poll.test.ts RED scaffolds with dynamic import + fake timers</name>
  <files>src/utils/deploy-watch-poll.test.ts</files>
  <read_first>
    - src/utils/deploy-poll.test.ts (fake timers, fetcher mock sequences, timeout case)
    - src/utils/deploy-poll.ts (TERMINAL_DEPLOYMENT_STATES export — import target for helper)
    - .planning/phases/21-deploy-watch/21-PATTERNS.md (deploy-watch-poll.test.ts fake-timer scaffold + timeout invert)
    - .planning/phases/21-deploy-watch/21-RESEARCH.md (Pattern 1 Equal Jitter, Pitfall 6 injectable RNG, WatchPollOutcome shape)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (D-03, D-06, D-08, D-09)
  </read_first>
  <behavior>
    - File uses vi.useFakeTimers in beforeEach / useRealTimers in afterEach
    - Each it.fails body starts with `const { pollDeploymentWithBackoff } = await import('./deploy-watch-poll.js')`
    - Case terminal: fetcher returns in_progress then finished → outcome.kind === 'terminal' and deployment.status === 'finished'
    - Case timeout: fetcher always in_progress past timeoutMs → outcome.kind === 'timeout', elapsedMs >= timeoutMs, and deployment.status is NOT the literal timeout string used by wait:true soft body
    - Case jitter bounds: injectable random returns fixed values; assert next delays stay within [minIntervalMs, maxIntervalMs] and grow across attempts toward cap
    - Case 429: fetcher throws rate-limit error once with retryAfterMs; poller continues and eventually reaches terminal (no hard abort)
    - Defaults asserted in options: minIntervalMs 3000, maxIntervalMs 30000 when omitted
  </behavior>
  <action>
    Create src/utils/deploy-watch-poll.test.ts. Import describe/expect/it/vi/beforeEach/afterEach from vitest. Use it.fails (not bare it) for every case so husky stays green while deploy-watch-poll.ts is absent (per D-03 new helper only). Dynamic-import pollDeploymentWithBackoff from ./deploy-watch-poll.js inside each case. Cover terminal exit, timeout outcome without synthesizing wait:true soft timeout status on the deployment object (D-09), Equal Jitter delay band with injectable random (D-06), and 429 continue via isRetryableRateLimit returning { retryAfterMs } (D-08). Do not create deploy-watch-poll.ts. Do not edit deploy-poll.ts.
  </action>
  <verify>
    <automated>npx vitest run src/utils/deploy-watch-poll.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/utils/deploy-watch-poll.test.ts exists
    - rg -c "it\.fails" src/utils/deploy-watch-poll.test.ts returns at least 4
    - rg -c "await import\('./deploy-watch-poll.js'\)" src/utils/deploy-watch-poll.test.ts returns at least 4
    - rg -n "useFakeTimers" src/utils/deploy-watch-poll.test.ts returns at least 1 match
    - rg -n "isRetryableRateLimit|retryAfterMs|429" src/utils/deploy-watch-poll.test.ts returns at least 1 match
    - npx vitest run src/utils/deploy-watch-poll.test.ts exits 0 (it.fails expected-fail contract)
    - git diff --name-only -- src/utils/deploy-poll.ts is empty
  </acceptance_criteria>
  <done>
    - RED scaffolds committed for poll helper covering terminal, timeout dual-path shape, jitter bounds, 429 continue
    - husky/vitest green via it.fails
    - deploy-poll.ts untouched
  </done>
</task>

<task type="tdd">
  <name>Task 2: Add deployment.test.ts watch describe with it.fails RED cases</name>
  <files>src/mcp/tools/deployment.test.ts</files>
  <read_first>
    - src/mcp/tools/deployment.test.ts (vi.mock client, testEnv, schema describe, isDeploymentErrorResult)
    - src/mcp/tools/deployment.ts (current actions list/get/cancel — watch absent)
    - .planning/phases/21-deploy-watch/21-PATTERNS.md (deployment.test.ts watch cases)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (D-01, D-04, D-05, D-07, D-09, D-11, D-12)
    - .planning/phases/21-deploy-watch/21-RESEARCH.md (Pattern 2 error codes, Pattern 3 Zod fields)
  </read_first>
  <behavior>
    - New describe('deployment watch') uses it.fails cases
    - Schema: safeParse action watch with only deployment_uuid succeeds once watch exists; assert result.data.timeout === 300, result.data.min_interval === 3, result.data.max_interval === 30, result.data.include_logs === false from Zod .default() on the flat schema (not handler-applied coalescing) (D-04, D-05, D-07)
    - Schema reject: min_interval greater than max_interval fails refine
    - Schema reject: min_interval less than 1 fails
    - Handler finished: ok structuredContent with summary projection status finished (D-11, D-12)
    - Handler timeout: isError true, code COOLIFY_WATCH_TIMEOUT, error.data has deployment snapshot + timed_out true, recoveryHints mention re-call deployment.watch (D-09, D-10)
    - Handler failed: isError true, code COOLIFY_DEPLOYMENT_FAILED, clear message (D-11)
    - Handler cancelled-by-user: isError true, code COOLIFY_DEPLOYMENT_CANCELLED (D-11)
    - include_logs omitted → logs not required on success payload (D-04)
  </behavior>
  <action>
    Extend src/mcp/tools/deployment.test.ts with describe('deployment watch') and it.fails cases per behavior (D-01 surface on deployment tool). For schema-defaults case: call deploymentToolSchema.safeParse({ action: 'watch', deployment_uuid }) and assert parsed .data carries timeout/min_interval/max_interval/include_logs from Zod .default(300|3|30|false) once Plan 21-02 lands (Wave 0 stays it.fails until then). Keep existing list/get/cancel tests as bare it (still green). Mock fetchDeployment sequences for handler cases. Assert dual-signal via isDeploymentErrorResult / structuredContent.error.code. Do not implement watch in deployment.ts in this plan — scaffolds must remain expected-fail until Plan 21-02.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -c "describe\('deployment watch'" src/mcp/tools/deployment.test.ts returns 1
    - rg -n "COOLIFY_WATCH_TIMEOUT|COOLIFY_DEPLOYMENT_FAILED|COOLIFY_DEPLOYMENT_CANCELLED" src/mcp/tools/deployment.test.ts returns at least 3 matches
    - rg -n "it\.fails" src/mcp/tools/deployment.test.ts returns at least 6
    - npx vitest run src/mcp/tools/deployment.test.ts exits 0
    - Existing describe blocks for list/get/cancel still use bare it and pass
  </acceptance_criteria>
  <done>
    - Watch RED scaffolds present for schema + handler outcomes covering WATCH-01 dual-signal and terminal errors
    - Suite green via it.fails
  </done>
</task>

<task type="tdd">
  <name>Task 3: Rewrite prompts.test.ts deploy assertions as it.fails watch-primary target</name>
  <files>tests/mcp/prompts.test.ts</files>
  <read_first>
    - tests/mcp/prompts.test.ts (current deploy prompt test expecting get-before-watch / Future Phase 21)
    - src/mcp/prompts.ts (current deploy assistant content lines 46-59)
    - .planning/phases/21-deploy-watch/21-PATTERNS.md (prompts.test.ts invert section)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (D-13, D-14, D-15)
  </read_first>
  <behavior>
    - Replace the deploy prompt test that requires deployment.get before deployment.watch and Future Phase 21 wording
    - New it.fails test asserts content contains application.deploy, deployment.watch as primary poll step, timeout re-watch guidance, failed/cancelled clear-error guidance, and wait:true / legacy note (D-14, D-15)
    - Assert content does not require get-before-watch ordering
    - Assert absence of Future Phase 21 do-not-call-watch language once Plan 21-03 lands (scaffold expects that absence so current prompt fails the assertion → it.fails green)
  </behavior>
  <action>
    Edit tests/mcp/prompts.test.ts deploy describe case: convert to it.fails with target assertions for WATCH-02 / D-13..D-15 (watch primary, re-watch on timeout, clear fail messaging, wait:true legacy). Remove assertions that encode get-before-watch ordering or Future Phase 21 forward-ref as success criteria. Do not edit prompts.ts in this plan.
  </action>
  <verify>
    <automated>npx vitest run tests/mcp/prompts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "Future \(Phase 21\)|do not call watch until" tests/mcp/prompts.test.ts returns 0 (old success assertions removed)
    - rg -n "deployment\.watch" tests/mcp/prompts.test.ts returns at least 1
    - rg -n "legacy|wait:\s*true|wait:true" tests/mcp/prompts.test.ts returns at least 1
    - rg -n "it\.fails" tests/mcp/prompts.test.ts returns at least 1 in the deploy test
    - npx vitest run tests/mcp/prompts.test.ts exits 0
  </acceptance_criteria>
  <done>
    - Deploy prompt RED target locked for Plan 21-03
    - Suite green via it.fails
  </done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks → Coolify API | vi.mock isolates fetchDeployment; no outbound HTTPS in Wave 0 |
| RED scaffolds → future handlers | it.fails encodes contracts only; no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-00-01 | Denial of Service | Polling storm scaffolds | medium | mitigate | RED cases encode min/max interval bounds and hard timeout — implemented in 21-01/21-02 (T-21-01) |
| T-21-00-02 | Information Disclosure | Test fixtures | low | accept | Synthetic deployment UUIDs/tokens only; no real credentials |
| T-21-SC | Tampering | npm package installs | low | accept | No new packages — vitest already pinned |
</threat_model>

<verification>
- npx vitest run src/utils/deploy-watch-poll.test.ts exits 0 (it.fails)
- npx vitest run src/mcp/tools/deployment.test.ts exits 0
- npx vitest run tests/mcp/prompts.test.ts exits 0
- src/utils/deploy-poll.ts unchanged
</verification>

<success_criteria>
- WATCH-01 and WATCH-02 have RED it.fails scaffolds ready for Waves 1–3
- Husky-friendly (suite green)
- No production implementation yet
</success_criteria>

<output>
Create `.planning/phases/21-deploy-watch/21-00-SUMMARY.md` when done.
</output>
