---
phase: 21-deploy-watch
plan: 04
type: execute
wave: 4
depends_on:
  - 21-03
files_modified:
  - src/utils/deploy-watch-poll.ts
  - src/utils/deploy-watch-poll.test.ts
  - src/mcp/tools/deployment.test.ts
autonomous: true
gap_closure: true
requirements:
  - WATCH-01
  - WATCH-02
user_setup: []

must_haves:
  truths:
    - "deployment.watch / pollDeploymentWithBackoff returns timeout when bounded timeout elapses — sleep delay never exceeds remaining budget (D-05, ROADMAP SC 1, VERIFICATION truth #2)"
    - "Watch does not block the MCP session beyond configured timeoutMs — both 429 and normal sleep paths clamp to remainingMs (phase goal, VERIFICATION truth #3, CR-01 + WR-01)"
    - "HTTP 429 with Retry-After much larger than timeoutMs still yields kind timeout within ~timeoutMs wall budget under fake timers (regression for CR-01)"
    - "Normal-path backoff sleep is also clamped so effective wait cannot overshoot by a full max_interval (WR-01)"
    - "WATCH-02 remains satisfied — prompts/README untouched by this gap plan"
    - statement: "include_logs true success path returns capped logs and no raw_deployment in watch OK payload (WR-02)"
      verification: backstop
  artifacts:
    - "src/utils/deploy-watch-poll.ts — remainingMs helper; Math.min(computedDelay, remaining) before every sleep (429 + normal)"
    - "src/utils/deploy-watch-poll.test.ts — fake-timer regression: Retry-After >> timeoutMs → timeout within budget"
    - "src/mcp/tools/deployment.test.ts — watch describe include_logs true success assertion (WR-02)"
  key_links:
    - "remainingMs(startTime, timeoutMs) → clamp delayMs on 429 path before sleep"
    - "remainingMs → clamp delayMs on normal in_progress path before sleep"
    - "pollDeploymentWithBackoff timeout outcome → handleDeploymentWatch COOLIFY_WATCH_TIMEOUT dual-signal (unchanged contract)"
  prohibitions:
    - "Do not modify pollDeploymentUntilTerminal or wait:true behavior (D-02, D-03)"
    - "Do not auto-dismiss / remove timeout dual-signal (D-09)"
    - "Do not replan or rewrite 21-00..21-03 PLAN.md files"
    - "Do not change schema defaults for timeout/min/max/include_logs"
    - "Do not add new npm packages"
---

<objective>
Close VERIFICATION blockers for truths #2 and #3 (CR-01 / WR-01): clamp every sleep in `pollDeploymentWithBackoff` to the remaining timeout budget so a large Retry-After (or normal backoff) cannot block the MCP session beyond `timeoutMs`. Also close WR-02 with a cheap `include_logs: true` success-path test.

Purpose: WATCH-01 bounded-timeout guarantee was incomplete — Pre-Sleep elapsed check exists but `await sleep(delayMs)` itself could exceed the budget (e.g. Retry-After 3600s with timeout 300s). Phase goal and D-05 require a hard upper bound on total block time.

Output: `remainingMs` + dual-path sleep clamp in `deploy-watch-poll.ts`; regression + include_logs tests green. WATCH-02 docs left untouched (already satisfied).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-deploy-watch/21-CONTEXT.md
@.planning/phases/21-deploy-watch/21-RESEARCH.md
@.planning/phases/21-deploy-watch/21-VERIFICATION.md
@.planning/phases/21-deploy-watch/21-REVIEW.md
@.planning/phases/21-deploy-watch/21-01-SUMMARY.md
@.planning/phases/21-deploy-watch/21-02-SUMMARY.md
@src/utils/deploy-watch-poll.ts
@src/utils/deploy-watch-poll.test.ts
@src/mcp/tools/deployment.ts
@src/mcp/tools/deployment.test.ts
</context>

## Artifacts this phase produces

- `src/utils/deploy-watch-poll.ts` — Gap closure only: add `remainingMs(startTime, timeoutMs)` returning `timeoutMs - (Date.now() - startTime)`. Before every `sleep()` (429 path ~lines 63-66 and normal path ~lines 85-86): if remaining ≤ 0 return `{ kind: 'timeout', deployment, elapsedMs }`; else `delayMs = Math.min(computedDelay, remaining)` then sleep. Computed delay on 429 stays `Math.max(backoffMs, retryAfterMs ?? 0)`; on normal path stays `nextDelayMs(...)`. Do not change Equal-Jitter formula, terminal detection, or fetcher contract.
- `src/utils/deploy-watch-poll.test.ts` — New fake-timer `it`: fetcher always rejects with retryable 429 and `retryAfterMs` much larger than `timeoutMs` (e.g. 3_600_000 vs timeoutMs 5_000); after advancing timers by ~timeoutMs (+ small slack), outcome is `kind: 'timeout'` and no single scheduled sleep delay exceeds remaining budget at schedule time (spy `setTimeout` delays ≤ timeoutMs). Title must reference Retry-After / remaining clamp / CR-01 language.
- `src/mcp/tools/deployment.test.ts` — New `it` under `describe('deployment watch')`: mock `fetchDeployment` with terminal `finished` plus a long `logs` string; call `handleDeploymentAction` with `include_logs: true`; assert OK, `data.logs` present and capped (length ≤ max_chars or projection cap), and payload has no `raw_deployment` property (WR-02).

## Multi-Source Coverage Audit (gap_closure scope)

| SOURCE | ID | Item | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Monitor deploy to terminal without blocking MCP session / storming API | 21-00..03 + **21-04** | COVERED | Gap restores hard timeout bound (truths #2/#3) |
| REQ | WATCH-01 | watch with backoff + bounded timeout | 21-00..02 + **21-04** | COVERED | CR-01 sleep clamp closes blocked bounded-timeout guarantee |
| REQ | WATCH-02 | Prompt/README watch docs | 21-03 | COVERED | Already satisfied; listed for phase continuity; untouched |
| CONTEXT | D-05 | Default timeout 300s / bounded timeout | **21-04** | COVERED | Clamp enforces timeout as hard total-block ceiling |
| CONTEXT | D-08 | 429 honor Retry-After, continue poll | 21-01 + **21-04** | COVERED | Still honor Retry-After but never beyond remainingMs |
| RESEARCH | — | Pitfall 12 / hard timeout | **21-04** | COVERED | remainingMs clamp is the missing hard bound |
| REVIEW | CR-01 | Sleep not clamped to remaining | **21-04** | COVERED | Primary blocker |
| REVIEW | WR-01 | Normal path +max_interval overshoot | **21-04** | COVERED | Same clamp on normal sleep |
| REVIEW | WR-02 | include_logs true success test | **21-04** | COVERED | Cheap secondary test |
| REVIEW | IN-01 / IN-02 | Jitter dangling promise / empty 429 snapshot | — | EXCLUDED | Info only; not VERIFICATION gaps |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clamp sleep to remaining timeout budget on 429 and normal paths (CR-01, WR-01, D-05)</name>
  <files>src/utils/deploy-watch-poll.ts</files>
  <read_first>
    - src/utils/deploy-watch-poll.ts — full file; 429 sleep at ~63-66 (`delayMs = Math.max(backoffMs, retryAfterMs)` then sleep); normal sleep at ~85-86 (`delayMs = nextDelayMs(...)` then sleep); existing pre-sleep elapsed checks at ~55-60 and ~77-82
    - .planning/phases/21-deploy-watch/21-REVIEW.md — CR-01 Fix block (remainingMs helper + Math.min clamp on both paths); WR-01 same clamp on normal path
    - .planning/phases/21-deploy-watch/21-VERIFICATION.md — gaps[0] and gaps[1] missing items (exact closure criteria)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md — D-05 bounded timeout; D-08 429 Retry-After continue (must keep continue-after-429, only bound sleep)
    - src/utils/deploy-watch-poll.test.ts — existing fake-timer + 429 continue patterns to keep green
  </read_first>
  <behavior>
    - When isRetryableRateLimit returns retryAfterMs much larger than timeoutMs, pollDeploymentWithBackoff resolves with kind timeout and elapsedMs approximately ≤ timeoutMs (fake timers)
    - When in_progress polls continue near timeout, normal-path sleep delay passed to sleep/setTimeout is ≤ remainingMs (no full maxInterval overshoot past timeout)
    - Terminal finished/failed still return kind terminal unchanged
    - Non-429 errors still rethrow unchanged
    - Existing Equal-Jitter bounds tests remain green
  </behavior>
  <action>
    Close VERIFICATION truths #2 and #3 / CR-01 + WR-01 in pollDeploymentWithBackoff only (per D-05 bounded timeout as hard total-block ceiling).

    1. Add a small helper remainingMs(startTime, timeoutMs) that returns timeoutMs minus elapsed wall time since startTime (Date.now() - startTime). Keep it module-local (not exported) unless a test needs export — prefer testing via behavior.

    2. On the 429 continue path (after the existing elapsed ≥ timeoutMs timeout return): compute remaining = remainingMs(startTime, options.timeoutMs). If remaining ≤ 0, return timeout outcome with current deployment + elapsedMs. Else compute backoffMs via nextDelayMs and computedDelay = Math.max(backoffMs, rateLimitInfo.retryAfterMs ?? 0), then delayMs = Math.min(computedDelay, remaining), then await sleep(delayMs). Per CR-01 / D-05 / D-08 (still continue after 429; never sleep past budget).

    3. On the normal in_progress path (after terminal check and existing elapsed timeout return): same remaining ≤ 0 → timeout; else delayMs = Math.min(nextDelayMs(...), remaining); await sleep(delayMs). Per WR-01.

    4. Do not change nextDelayMs Equal-Jitter math, TERMINAL_DEPLOYMENT_STATES usage, fetcher contract, or throw path for non-retryable errors. Do not edit deploy-poll.ts / application wait:true (D-02, D-03). Do not edit prompts or README (WATCH-02 already done).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/utils/deploy-watch-poll.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "remainingMs" src/utils/deploy-watch-poll.ts` returns ≥1 match defining or calling the helper
    - Both sleep call sites compute delay with Math.min against remaining (429 path and normal path) — `rg -n "Math\\.min\\(" src/utils/deploy-watch-poll.ts` shows clamp usage near both sleep awaits
    - No sleep of unclamped Math.max(backoff, retryAfter) without a remaining budget min on the 429 path
    - `pnpm exec vitest run src/utils/deploy-watch-poll.test.ts` green after Task 2 adds the regression (Task 1 may land with existing 5 tests green first)
  </acceptance_criteria>
  <done>Sleep on 429 and normal paths cannot exceed remaining timeout budget; truths #2/#3 / CR-01 / WR-01 closable once regression test lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression tests for Retry-After clamp (CR-01) and include_logs success (WR-02)</name>
  <files>src/utils/deploy-watch-poll.test.ts, src/mcp/tools/deployment.test.ts</files>
  <read_first>
    - src/utils/deploy-watch-poll.test.ts — existing fake timers beforeEach/afterEach; continues-polling-after-429 it (~113-155) as pattern for isRetryableRateLimit; setTimeout spy pattern in jitter/defaults tests
    - src/utils/deploy-watch-poll.ts — Task 1 remainingMs clamp (confirm both paths)
    - src/mcp/tools/deployment.test.ts — describe('deployment watch') (~383+); returns OK summary when deployment finishes (~458-482) as sibling to mirror; handleDeploymentAction / isDeploymentErrorResult / fetchDeployment mock imports at file top
    - src/mcp/tools/deployment.ts — handleDeploymentWatch include_logs branch (~363-368) using projectDeploymentFull for capped logs
    - .planning/phases/21-deploy-watch/21-VERIFICATION.md — gaps[0].missing regression test item; WR-02 note under artifacts
    - .planning/phases/21-deploy-watch/21-REVIEW.md — WR-02 Fix: mock logs, assert capped logs, no raw_deployment
  </read_first>
  <behavior>
    - New poll helper it: constant 429 with retryAfterMs e.g. 3_600_000, timeoutMs e.g. 5_000, min/max small; after advanceTimersByTimeAsync covering timeoutMs (+ small slack ≤ one tick), outcome.kind is timeout; every positive setTimeout delay scheduled during the run is ≤ timeoutMs
    - New deployment watch it: fetchDeployment resolves finished with a long logs string; watch with include_logs true and a small max_chars; result ok; data.logs is a string with length ≤ max_chars (or documented projection cap); data/result has no raw_deployment property
    - Existing watch and poll helper tests remain green
  </behavior>
  <action>
    1. In deploy-watch-poll.test.ts add one vitest it under the pollDeploymentWithBackoff describe. Use vi.useFakeTimers (already in beforeEach). Fetcher always rejects with an error that isRetryableRateLimit maps to { retryAfterMs: 3_600_000 } (or similar >> timeout). Call pollDeploymentWithBackoff with timeoutMs 5000, deterministic random, and that isRetryableRateLimit. Spy setTimeout. Advance fake time by timeoutMs plus small slack (e.g. +100ms loops) until the promise settles. Assert outcome.kind === 'timeout'. Assert every captured positive setTimeout delay is ≤ timeoutMs (proves clamp; unclamped Retry-After would schedule 3600000). Title must clearly reference Retry-After exceeding timeout / remaining clamp / CR-01. Prefer awaiting the promise (avoid dangling void promise — closes IN-01 style smell for this new test).

    2. In deployment.test.ts under describe('deployment watch'), add one it for include_logs true success (WR-02). Mock fetchDeployment to resolve a finished deployment including a long logs field (string longer than max_chars). Call handleDeploymentAction with action watch, deployment_uuid, include_logs true, and a small max_chars (e.g. 50). Assert not error, result.ok, typeof data.logs === 'string', data.logs.length ≤ max_chars (or equal to capped projection), and that the success data object does not have property raw_deployment. Do not weaken existing finished/timeout/failed/cancelled tests.

    3. Do not edit prompts, README, errors.ts, or deploy-poll.ts.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/utils/deploy-watch-poll.test.ts src/mcp/tools/deployment.test.ts -t "watch|pollDeploymentWithBackoff|Retry-After|include_logs|remaining|clamp"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Retry-After|remaining|clamp|CR-01" src/utils/deploy-watch-poll.test.ts` shows the new regression it title/body
    - New poll test asserts kind timeout when retryAfterMs >> timeoutMs under fake timers
    - `rg -n "include_logs:\\s*true" src/mcp/tools/deployment.test.ts` shows a success-path watch test (not only schema default false)
    - New watch it asserts logs present/capped and absence of raw_deployment on success payload
    - `pnpm exec vitest run src/utils/deploy-watch-poll.test.ts` green
    - `pnpm exec vitest run src/mcp/tools/deployment.test.ts -t "watch"` green
  </acceptance_criteria>
  <done>CR-01 regression locked; WR-02 include_logs success path covered; WATCH-01 bounded timeout re-verifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Agent → deployment.watch MCP tool | Untrusted timeout / interval / Retry-After-influenced sleep scheduling inside poll helper |
| Poll helper → Coolify API | 429 Retry-After values are server-controlled and must not dictate unbounded local sleep |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-21-04-01 | Denial of Service | pollDeploymentWithBackoff sleep (429 Retry-After) | high | mitigate | Clamp every sleep to remainingMs so hostile/large Retry-After cannot hang MCP session beyond timeoutMs (CR-01) |
| T-21-04-02 | Denial of Service | pollDeploymentWithBackoff sleep (normal backoff) | medium | mitigate | Same remainingMs clamp on normal path (WR-01) prevents +max_interval overshoot past budget |
| T-21-04-03 | Information Disclosure | include_logs watch success | low | accept | Logs already capped via projectDeploymentFull + max_chars; WR-02 only adds test — no new exposure surface |
| T-21-04-04 | Tampering | timeout / interval agent params | low | accept | Zod bounds already enforce timeout 10–1800 and min≤max; gap does not widen param surface |
| T-21-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this gap plan — N/A; if any install appears, block and run legitimacy gate |
</threat_model>

<verification>
- `pnpm exec vitest run src/utils/deploy-watch-poll.test.ts` green including Retry-After >> timeout regression
- `pnpm exec vitest run src/mcp/tools/deployment.test.ts -t "watch"` green including include_logs true success
- `pnpm exec vitest run src/utils/deploy-poll.test.ts` green (wait:true regression untouched)
- Re-run phase verifier / `/gsd-verify-work` — truths #2 and #3 must flip to VERIFIED; WATCH-01 unblocked
</verification>

<success_criteria>
- VERIFICATION truths #2 and #3 satisfied (bounded timeout hard ceiling on total block time)
- CR-01 and WR-01 closed via remainingMs clamp on both sleep paths
- WR-02 include_logs success test present and green
- WATCH-02 unchanged and still satisfied
- Phase 21 ready for re-verification (not marked complete until verifier passes)
</success_criteria>

<output>
Create `.planning/phases/21-deploy-watch/21-04-SUMMARY.md` when done
</output>
