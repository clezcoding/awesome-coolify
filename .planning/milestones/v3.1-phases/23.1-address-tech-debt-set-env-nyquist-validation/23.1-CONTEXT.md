# Phase 23.1: Address tech debt: set_env + Nyquist validation - Context

**Gathered:** 2026-07-27
**Status:** Ready for planning

<domain>
## Phase Boundary

Close two urgent v3.1 tech-debt items before milestone close:

1. **`setup.wire(set_env:true)`** — today a ponytail no-op in `setup.ts`; wire to real env sync via existing `application` `envs:sync` handler and new setup params.
2. **Nyquist validation for Phases 19–23** — all five v3.1 phases currently `status: draft` / NOT-VALIDATED per `.planning/v3.1-MILESTONE-AUDIT.md`; reconcile via `/gsd-validate-phase` with **100% REQ-row coverage** (every requirement ID green in per-phase VALIDATION.md).

Out of scope: other audit items (WR-06 domains false progress, CLI setup-wizard D-03, npm Version Packages publish, OpenAPI gap backlog, Phase 19 DX nits). No new MCP domain tools. No OpenAPI coverage expansion.

</domain>

<decisions>
## Implementation Decisions

### set_env — Env source & schema
- **D-01:** When `set_env: true`, require exactly one of `env_file` (local path) or `env_content` (inline `.env` text) on `setup.wire` — same mutual-exclusion contract as `application` `envs:sync`. No auto-detect of workspace `.env` without explicit param. — **Reversibility:** costly — published flat schema + skill/docs contract.
- **D-02:** Add `env_file` and `env_content` to `setupActionSchema` wire fields and `setupActionsCatalog` wire action required/optional field lists (required when `set_env: true`; omitted when `set_env` false/off).

### set_env — Modes & link-existing
- **D-03:** `set_env` runs in **both** `greenfield` and `link-existing` when `application_uuid` is known — fixes WR-07 (optional flags accepted on link-existing but never executed). Reuse `assertLinkExistingOptionalFlags` guard (already requires `application_uuid` for optional post-wire flags). — **Reversibility:** costly — behavior change vs current greenfield-only implementation.

### set_env — Safety & apply policy
- **D-04:** When `set_env: true`, apply env sync directly (not dry-run-only) with **`confirm: true` implicit** — user/agent already opted in via flag; no extra soft-pause round for confirm. — **Reversibility:** one-way — safety contract for setup apply path.
- **D-05:** Default **`conflict_policy: 'abort'`** on setup-initiated env sync — on remote/local value conflicts, stop and surface conflict payload + recovery hints; do not silently overwrite or keep remote. Agent must ask human and retry with explicit policy if needed. — **Reversibility:** costly — default behavior for setup path.
- **D-06:** Delegate implementation to existing `handleApplicationAction` `envs:sync` (or shared internal helper extracted from it) — do not duplicate Coolify env API calls in `setup.ts`. Pass `uuid: application_uuid`, resolved env source, `dry_run: false`, `confirm: true`, `conflict_policy: 'abort'` unless planner adds optional override param (default locked to abort).

### set_env — Step reporting
- **D-07:** Push `'env'` to `steps_completed` only after successful `envs:sync` apply — never on no-op or validation skip (aligns with WR-06 fix spirit for domains, but WR-06 itself is deferred per D-12).

### Nyquist — Scope
- **D-08:** Run full Nyquist reconciliation for **all five v3.1 phases: 19, 20, 21, 22, 23** — not a subset. Milestone-close gate. — **Reversibility:** costly — milestone ship criteria.

### Nyquist — Depth & coverage
- **D-09:** Use **`/gsd-validate-phase {N}`** per phase (19 through 23) — fill per-task verification maps, set `status: validated`, and `nyquist_compliant: true` only when earned. — **Reversibility:** costly — validation artifacts across five phase dirs.
- **D-10:** **100% REQ-row coverage** — every requirement ID mapped in each phase's VALIDATION.md must have an existing automated test (green) or a new test added in this phase; no documented gaps, no "pending" rows at close. Includes known gaps such as Phase 19 prompt tests and Phase 22 `set_env` tests. — **Reversibility:** one-way — ship gate quality bar.

### Scope boundary
- **D-11:** **Strict scope:** only `set_env` implementation + Nyquist 19–23. Defer all other v3.1-MILESTONE-AUDIT tech-debt items (see `<deferred>`). — **Reversibility:** reversible — backlog items unchanged.

### Claude's Discretion
- Exact Zod field names (`env_file` / `env_content` vs shorter aliases) — must match `application` `envs:sync` names.
- Whether to extract a shared `syncApplicationEnv(...)` helper vs inline `handleApplicationAction` call from setup.
- Plan wave split (set_env first vs Nyquist-first vs interleaved).
- Optional `prune` exposure on setup wire (default off; not discussed — omit unless planner finds REQ tie-in).
- Order of `/gsd-validate-phase` runs (19→23 sequential recommended).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Milestone audit & roadmap
- `.planning/v3.1-MILESTONE-AUDIT.md` — set_env no-op evidence, Nyquist NOT-VALIDATED table (phases 19–23), tech-debt inventory
- `.planning/ROADMAP.md` — Phase 23.1 insertion (URGENT), depends on Phase 23
- `.planning/REQUIREMENTS.md` — SETUP-02 (setup wizard manifest linkage; set_env completes optional env path)
- `.planning/STATE.md` — Phase 23.1 inserted after Phase 23 ship
- `.planning/config.json` — `workflow.nyquist_validation: true`

### Prior phase context
- `.planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md` — D-10 optional flags default off; D-11 deploy_and_watch
- `.planning/phases/22-setup-wizard-ide-skills/22-REVIEW.md` — WR-06 (domains false progress), WR-07 (link-existing optional flags)
- `.planning/phases/22-setup-wizard-ide-skills/22-VALIDATION.md` — draft; nyquist_compliant false
- `.planning/phases/19-dx-schemas-mcp-prompts/19-VALIDATION.md` — draft; prompt test gaps
- `.planning/phases/20-recipes-service-list-types/20-VALIDATION.md` — draft
- `.planning/phases/21-deploy-watch/21-VALIDATION.md` — draft
- `.planning/phases/23-openapi-coverage-npm-release/23-VALIDATION.md` — draft

### Implementation sources
- `src/mcp/tools/setup.ts` — `set_env` ponytail no-op (~L737–740); `runOptionalGreenfieldSteps`; `assertLinkExistingOptionalFlags`
- `src/mcp/tools/application.ts` — `envs:sync` action (env_file/env_content, confirm, conflict_policy, prune)
- `src/mcp/tools/setup.test.ts` — setup tests (no set_env coverage yet)
- `skills/coolify-setup/SKILL.md` — documents `set_env` for greenfield and link-existing

### Docs
- `docs/en/setup.md` — setup wizard user docs (update when set_env ships)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `handleApplicationAction` `envs:sync` — full sync pipeline: parse `.env`, conflict detection, dry_run diff, apply, rollback hints (`application.ts`)
- `assertLinkExistingOptionalFlags` — already gates optional post-wire flags on `application_uuid` for link-existing
- `runOptionalGreenfieldSteps` — hosts `include_domains`, `set_env`, `deploy_and_watch`; extend for link-existing path or shared helper
- Phase 22 setup tests + gh-preflight tests — base for new set_env integration tests

### Established Patterns
- Optional setup flags default **off** (D-10 Phase 22) — `set_env` remains opt-in
- `confirm:true` required for destructive/apply paths on application tool — setup bypasses explicit confirm param but must pass `confirm: true` internally (D-04)
- Nyquist VALIDATION.md frontmatter: `status`, `nyquist_compliant`, per-task map with vitest commands
- Flat schema field names aligned across tools (Phase 19 DX-02)

### Integration Points
- `setup.wire` → `handleApplicationAction({ action: 'envs:sync', uuid: application_uuid, ... })` after resource UUID known
- Greenfield: `application_uuid` from recipe/wire result resource
- Link-existing: `application_uuid` from wire params
- Skills + `docs/en/setup.md` — document new required params when `set_env: true`
- Five `*-VALIDATION.md` files under `.planning/phases/` — target of validate-phase reconciliation

</code_context>

<specifics>
## Specific Ideas

- User locked Batch 1: explicit env params, both modes, implicit confirm on apply, abort on conflict.
- User locked Batch 2: all phases 19–23 Nyquist, full validate-phase per phase, strict scope, **100% REQ coverage** (no partial maps).

</specifics>

<deferred>
## Deferred Ideas

Per D-11 strict scope — from `.planning/v3.1-MILESTONE-AUDIT.md`, not Phase 23.1:

- **WR-06** — `include_domains: true` marks step complete without domains when list empty
- **D-03 CLI** — `src/cli/setup-wizard.ts` deferred wrapper
- **Phase 19 nits** — logs max_chars WR-03, description `\n` spacing WR-04, manifest recovery WR-05
- **Phase 20** — README list-types row, optional createPublicApplication test
- **Phase 21** — timeout payload edge cases (info-only)
- **Phase 23** — Version Packages npm @1.0.0 publish, 57 OpenAPI gap backlog
- **Auto-detect `.env`** — user chose explicit params only (D-01)

</deferred>

---

*Phase: 23.1-address-tech-debt-set-env-nyquist-validation*
*Context gathered: 2026-07-27*
