---
phase: 23.1-address-tech-debt-set-env-nyquist-validation
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/setup.ts
  - src/mcp/tools/setup.test.ts
autonomous: true
requirements:
  - SETUP-02
user_setup: []

must_haves:
  truths:
    - "setup.wire(set_env:true) with env_content XOR env_file delegates to handleApplicationAction envs:sync and pushes 'env' only after success (D-01, D-06, D-07)"
    - "set_env runs on both greenfield and link-existing when application resource UUID known (D-03)"
    - "envs:sync called with dry_run:false, confirm:true, conflict_policy:'abort' (D-04, D-05)"
    - "set_env:true without exactly one of env_file/env_content fails Zod with COOLIFY_VALIDATION_ERROR before wire side-effects (D-01, D-02)"
    - "set_env on non-application resource (e.g. create-one-click service) throws COOLIFY_VALIDATION_ERROR and does not push 'env'"
  artifacts:
    - path: src/mcp/tools/setup.ts
      provides: "env_file/env_content schema fields, extraRefine XOR, runOptionalWireSteps envs:sync delegation"
      contains: "env_file"
    - path: src/mcp/tools/setup.test.ts
      provides: "describe('set_env') coverage for link-existing, greenfield, XOR, conflict abort, non-application"
      contains: "set_env"
  key_links:
    - from: "runOptionalWireSteps set_env branch"
      to: "handleApplicationAction({ action: 'envs:sync', ... })"
      via: "inline call matching deploy_and_watch pattern"
    - from: "setupActionSchema extraRefine"
      to: "env_file XOR env_content when set_env===true"
      via: "createFlatActionSchema 5th argument"
---

<objective>
Tracer + expansion: make `setup.wire(set_env:true)` a real env sync path via existing `application` `envs:sync`, with schema XOR and green tests covering SETUP-02 optional env path (D-01–D-07).

Purpose: Close milestone tech-debt no-op before Nyquist wave; Phase 22 VALIDATION set_env gap needs these tests first.

Output: Working set_env delegation in `setup.ts`, green `describe('set_env')` in `setup.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-CONTEXT.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-RESEARCH.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-PATTERNS.md
@src/mcp/tools/setup.ts
@src/mcp/tools/setup.test.ts
@src/mcp/tools/application.ts
@src/mcp/tools/shared-read-params.ts
</context>

## Artifacts this phase produces

- `src/mcp/tools/setup.ts` — `env_file`/`env_content` on wire/resume schema; `extraRefine` XOR when `set_env:true`; `runOptionalWireSteps` delegates to `handleApplicationAction` `envs:sync`.
- `src/mcp/tools/setup.test.ts` — `describe('set_env')` (link-existing happy path, greenfield, XOR validation, conflict abort propagation, non-application rejection).
- (later plans) Nyquist-validated `19`–`23` VALIDATION.md files; docs/skills param docs.

<tasks>

<task type="tracer">
  <name>End-to-end set_env → envs:sync on link-existing</name>
  <reversibility rating="one-way">D-04 locks implicit confirm:true on setup apply path — changing later breaks agent safety contract for setup wire.</reversibility>
  <files>src/mcp/tools/setup.ts, src/mcp/tools/setup.test.ts</files>
  <read_first>
    - .planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-CONTEXT.md (D-01 through D-07)
    - .planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-PATTERNS.md (schema XOR + delegation blocks)
    - src/mcp/tools/setup.ts (setupActionSchema ~L66-203; runOptionalWireSteps set_env no-op ~L737-740; deploy_and_watch ~L742-779)
    - src/mcp/tools/application.ts (env_file/env_content fields ~L418-425; envs:sync XOR ~L754-773)
    - src/mcp/tools/setup.test.ts (deploy_and_watch describe ~L315-382; link-existing describe ~L146-214; handleApplicationAction mock)
    - src/mcp/tools/shared-read-params.ts (createFlatActionSchema extraRefine 5th param)
  </read_first>
  <behavior>
    - link-existing wire with set_env:true + env_content:'FOO=bar' + application_uuid calls handleApplicationAction with action:'envs:sync', uuid:application_uuid, env_content:'FOO=bar', dry_run:false, confirm:true, conflict_policy:'abort'
    - on successful syncResult (not isApplicationErrorResult), result.data.steps_completed contains 'env'
    - set_env:true with neither env_file nor env_content fails schema/parse before handler sync (COOLIFY_VALIDATION_ERROR)
  </behavior>
  <action>
    Per D-01/D-02: add optional Zod fields env_file and env_content to setupActionSchema shape (same describe strings as application.ts), append 'env_file' and 'env_content' to wire and resume actionAllowedFields arrays. Pass createFlatActionSchema 5th-arg extraRefine that when action is wire or resume and set_env===true requires exactly one of non-empty env_file or env_content (message and params.code COOLIFY_VALIDATION_ERROR per 23.1-PATTERNS.md / D-01). Update setupActionsCatalog string so wire documents set_env with env_file?/env_content? (mirror application envs:sync catalog style). Per D-03/D-04/D-05/D-06/D-07: replace the set_env branch inside runOptionalWireSteps — if resource.type !== 'application' throw CoolifyApiError COOLIFY_VALIDATION_ERROR that set_env requires an application resource; else await handleApplicationAction with action:'envs:sync', uuid:resource.uuid, env_file or env_content from parsed, dry_run:false, confirm:true, conflict_policy:'abort', instance:parsed.instance; on isApplicationErrorResult call rethrowHandlerError; only then stepsCompleted.push('env'). Inline call only (no new syncApplicationEnv helper). Do not expose prune on setup. Add describe('set_env') with at least one GREEN it: link-existing wire (skip_gh:true, application_uuid, project/environment/server UUIDs, set_env:true, env_content:'FOO=bar') asserting handleApplicationAction call shape and steps_completed includes 'env'. Reuse existing vi.mock('./application.js') and mockLinkageFetch patterns.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/setup.test.ts -t set_env -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "env_file:" src/mcp/tools/setup.ts returns at least 1
    - rg -n "env_content:" src/mcp/tools/setup.ts returns at least 1
    - rg -n "conflict_policy: 'abort'" src/mcp/tools/setup.ts returns at least 1
    - rg -n "action: 'envs:sync'" src/mcp/tools/setup.ts returns at least 1
    - rg -n "describe\\('set_env'" src/mcp/tools/setup.test.ts returns 1
    - npx vitest run src/mcp/tools/setup.test.ts -t set_env exits 0
  </acceptance_criteria>
  <done>link-existing set_env path syncs via envs:sync with locked apply params; 'env' step only after success; XOR enforced at schema; at least one green set_env test.</done>
</task>

<task type="auto" tdd="true">
  <name>Expand set_env tests: greenfield, XOR, conflict, non-app</name>
  <reversibility rating="costly">D-01/D-03/D-05 lock published schema + both-mode behavior + abort default — undo needs skill/docs + agent contract updates.</reversibility>
  <files>src/mcp/tools/setup.ts, src/mcp/tools/setup.test.ts</files>
  <read_first>
    - src/mcp/tools/setup.test.ts (describe set_env from tracer task; greenfield wire helpers ~L216-280)
    - src/mcp/tools/setup.ts (runOptionalWireSteps set_env + deploy_and_watch guards)
    - .planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-RESEARCH.md (Pitfall 1 non-application; Code Examples mock)
  </read_first>
  <behavior>
    - greenfield create-git-app + set_env:true + env_content delegates envs:sync with uuid from recipe application_uuid and steps_completed contains 'env'
    - set_env:true with both env_file and env_content fails validation (XOR)
    - when handleApplicationAction returns isError with COOLIFY_CONFIRM_REQUIRED (conflict abort payload), setup result is error and steps_completed does not include 'env'
    - greenfield recipe_type create-one-click yielding service resource + set_env:true returns/throws COOLIFY_VALIDATION_ERROR and does not call envs:sync successfully / does not push 'env'
    - resume path with set_env:true + env_content also delegates (same apply params) when resource known
  </behavior>
  <action>
    Per D-03/D-05/D-07 and RESEARCH pitfalls: expand describe('set_env') with GREEN tests for (1) greenfield create-git-app wire + set_env + env_content asserting envs:sync uuid and steps_completed includes env; (2) XOR both-or-neither rejected at schema/handler boundary with COOLIFY_VALIDATION_ERROR; (3) mock handleApplicationAction resolving isError structuredContent.error.code COOLIFY_CONFIRM_REQUIRED — assert setup error path and 'env' absent from steps_completed; (4) create-one-click / service-type resource rejects set_env without marking env complete; (5) optional resume with set_env:true + env_content if resume shares runOptionalWireSteps. Keep mocks local to describe; do not edit application.ts. After tests green, run graphify update . if graphify enabled in this repo (skip if disabled).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/setup.test.ts -t set_env -x</automated>
    <automated>npx vitest run src/mcp/tools/setup.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "create-one-click|non-application|service resource|requires an application" src/mcp/tools/setup.test.ts returns at least 1
    - rg -n "COOLIFY_CONFIRM_REQUIRED" src/mcp/tools/setup.test.ts returns at least 1
    - rg -n "env_file" src/mcp/tools/setup.test.ts returns at least 1
    - npx vitest run src/mcp/tools/setup.test.ts -t set_env exits 0
  </acceptance_criteria>
  <done>Full set_env describe covers greenfield, XOR, conflict abort, non-application; SETUP-02 optional env path testable for Nyquist Phase 22.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → setup.wire | Untrusted MCP args (set_env, env_file path, env_content body) |
| setup → application envs:sync | Trusted internal delegation; must pass confirm + conflict_policy |
| env_file → filesystem | Path must stay inside application allowlist (reuse readBoundedEnvFile) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23.1-01 | Tampering | setup.ts env_file | high | mitigate | Delegate to envs:sync readBoundedEnvFile allowlist — never open paths in setup.ts |
| T-23.1-02 | Information Disclosure | env sync diff / errors | medium | mitigate | Propagate masked conflict payload via rethrowHandlerError; do not log raw secrets in setup |
| T-23.1-03 | Tampering | conflict overwrite | high | mitigate | Hard-code conflict_policy:'abort' + confirm:true on setup path (D-04/D-05) |
| T-23.1-04 | Elevation of Privilege | agent skips human on conflicts | medium | mitigate | Surface COOLIFY_CONFIRM_REQUIRED; never swallow sync errors into steps_completed |
| T-23.1-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
npx vitest run src/mcp/tools/setup.test.ts -t set_env exits 0; full setup.test.ts green; set_env no longer pushes env without sync.
</verification>

<success_criteria>
SETUP-02 optional env path works end-to-end for greenfield and link-existing with locked apply/abort contract and green set_env tests.
</success_criteria>

<output>
Create `.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-01-SUMMARY.md` when done
</output>
