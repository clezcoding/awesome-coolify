---
context: phase
phase: 23.1-address-tech-debt-set-env-nyquist-validation
task: 0
total_tasks: 0
status: verification_gaps
last_updated: 2026-07-27T05:26:23.122Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through verification failure — not predictions.

- [ ] **CONFLICT_POLICY_ABORT_PARTIAL_APPLY** — `setup.ts` passes explicit `conflict_policy: 'abort'` to `envs:sync`. `validateSyncConflictPolicy` only throws `COOLIFY_CONFIRM_REQUIRED` when `conflict_policy` is **undefined** (`application.ts:2376`). With explicit `'abort'`, sync partial-applies non-conflicting keys, returns success + `aborted[]`, and setup pushes `'env'` to `steps_completed`. D-05/D-07 require stop + human resolution. **Mitigation:** omit `conflict_policy` on setup delegation (CR-01 preferred) OR inspect sync result `aborted`/`conflicts` and fail before marking env complete.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| conflict_policy abort on setup path | Passing explicit `conflict_policy: 'abort'` causes silent partial apply; docs/skill claim COOLIFY_CONFIRM_REQUIRED stop behavior production never emits | blocking | Gap plan must omit conflict_policy OR fail on aborted[]; conflict test must assert production path not mocked error path |
| test mocks wrong production path | `setup.test.ts` conflict test mocks COOLIFY_CONFIRM_REQUIRED throw — path never hit with explicit abort | advisory | Replace with integration test using success response containing aborted[] or real handleApplicationAction fixture |
| docs claim stop-on-conflict | `docs/en/setup.md` + `coolify-setup/SKILL.md` say setup stops on conflicts | advisory | Align docs with code after gap fix — or fix code to match docs |

<current_state>
Phase 23.1 execution **complete** (4/4 plans, 3 waves). Verification **gaps_found** (12/14 truths). Branch `gsd/phase-23.1-address-tech-debt-set-env-nyquist-validation`. Tests 1112 green. Phase NOT marked complete — milestone close blocked on SETUP-02 conflict safety.
</current_state>

<completed_work>

**Plans executed this session:**

| Plan | What shipped | Head commit |
|------|--------------|-------------|
| 23.1-01 | `setup.wire(set_env:true)` → `application` `envs:sync`; XOR schema; 7 tests | `b59b40a` |
| 23.1-02 | Nyquist validated phases 19–21 (`nyquist_compliant: true`) | `a9f9c26` |
| 23.1-04 | `docs/en/setup.md` + `coolify-setup` skill set_env docs | `a272bb9` |
| 23.1-03 | Nyquist validated phases 22–23; inventory 19–23 complete (5/5) | `3ea5fe2` |

**Verification + review (uncommitted):**
- `23.1-VERIFICATION.md` — gaps_found 12/14
- `23.1-REVIEW.md` — 1 critical, 2 warnings, 2 info
</completed_work>

<remaining_work>

1. **Gap closure** — fix D-05/D-07 conflict behavior in `src/mcp/tools/setup.ts` (~L770-780)
2. **Gap closure** — fix conflict test in `setup.test.ts` to match production path
3. **Gap closure** — align docs/skill if behavior changes
4. **Re-verify** — `/gsd-execute-phase 23.1 --gaps-only` after gap plan ships
5. **Milestone** — `/gsd-complete-milestone v3.1` only after phase 23.1 passes verification
</remaining_work>

<decisions_made>

- Sequential execution on main checkout (`USE_WORKTREES=false`, cursor runtime)
- Nyquist 19–23 validated via doc reconciliation — no new tests needed for phases 19–21
- Phase completion withheld — verifier found real behavioral gap, not doc drift
</decisions_made>

<blockers>

- **D-05/D-07 conflict stop** — `conflict_policy: 'abort'` partial-applies; `'env'` marked complete incorrectly
- **Uncommitted artifacts** — REVIEW.md, VERIFICATION.md, modified 23.1-02-SUMMARY.md
</blockers>

## Required Reading (in order)

1. `.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-VERIFICATION.md` — gap details + evidence
2. `.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-REVIEW.md` — CR-01 fix recommendation
3. `src/mcp/tools/setup.ts` — `runOptionalWireSteps` set_env branch ~L757-780
4. `src/mcp/tools/application.ts` — `validateSyncConflictPolicy` ~L2376
5. `.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-CONTEXT.md` — D-04/D-05 locked decisions

## Infrastructure State

- Branch: `gsd/phase-23.1-address-tech-debt-set-env-nyquist-validation`
- Tests: 1112 passed (last run post wave 3)
- No async jobs running
- No background servers

<context>
All planned work shipped. Single blocker before milestone close: set_env conflict path does not match locked D-05 contract. Code review and verifier agree on root cause and fix (omit conflict_policy or inspect aborted[]). Nyquist inventory done — only behavioral gap remains.
</context>

<next_action>
Start with: `/gsd-plan-phase 23.1 --gaps` — creates gap-closure plan from VERIFICATION.md. Then `/gsd-execute-phase 23.1 --gaps-only`.
</next_action>
