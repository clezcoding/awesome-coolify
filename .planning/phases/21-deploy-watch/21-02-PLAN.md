---
phase: 21-deploy-watch
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/utils/errors.ts
  - src/utils/errors.test.ts
  - src/mcp/tools/deployment.ts
  - src/mcp/tools/deployment.test.ts
autonomous: true
requirements:
  - WATCH-01
user_setup: []

must_haves:
  truths:
    - "deployment tool actionsCatalog and createFlatActionSchema include watch with params deployment_uuid (required), timeout?, min_interval?, max_interval?, include_logs?, plus shared read/routing fields (D-01, D-04, D-07)"
    - "Default timeout 300 seconds when omitted; min_interval default 3; max_interval default 30; include_logs default false (D-04, D-05, D-06)"
    - "Zod rejects min_interval < 1 and min_interval > max_interval when both set; timeout bounds min 10 max 1800 seconds (RESEARCH A4)"
    - "handleDeploymentWatch calls pollDeploymentWithBackoff (not pollDeploymentUntilTerminal) with fetchDeployment fetcher (D-03)"
    - "Terminal finished → ok buildReadResponse with projectDeploymentSummary; logs only when include_logs true via capped full projection (D-11, D-12, D-04)"
    - "Timeout → throw CoolifyApiError COOLIFY_WATCH_TIMEOUT with isError dual-signal via wrapMcpError; error.data includes deployment summary, timed_out true, elapsed_seconds; recoveryHints tell agent to re-call deployment.watch same uuid (D-09, D-10)"
    - "Terminal failed → COOLIFY_DEPLOYMENT_FAILED isError with clear user-facing message; cancelled-by-user → COOLIFY_DEPLOYMENT_CANCELLED (D-11)"
    - "application.deploy wait:true path unchanged (D-02)"
    - "deployment.test.ts watch describe flipped from it.fails to bare it and green"
  artifacts:
    - "src/utils/errors.ts (modified — new CoolifyErrorCode + RECOVERY_HINTS)"
    - "src/utils/errors.test.ts (modified — new code hint smoke if needed)"
    - "src/mcp/tools/deployment.ts (modified — watch schema/catalog/handler)"
    - "src/mcp/tools/deployment.test.ts (modified — watch GREEN)"
  key_links:
    - "handleDeploymentAction switch watch → handleDeploymentWatch → pollDeploymentWithBackoff → CoolifyApiError | buildReadResponse"
    - "wrapMcpError → MCP isError true + structuredContent.error dual-signal"
    - "isRetryableRateLimit in watch handler uses toStructuredError httpStatus 429 + data.retry_after from Plan 21-01"
  assumptions:
    - "RESEARCH A2: new codes COOLIFY_WATCH_TIMEOUT / COOLIFY_DEPLOYMENT_FAILED / COOLIFY_DEPLOYMENT_CANCELLED (not reuse COOLIFY_TIMEOUT soft path)"
    - "A-EDGE-01 remainder: finished/failed/cancelled/timeout agent-visible outcomes covered here"
  prohibitions:
    - statement: "Do not return ok:true soft body on watch timeout (no wait:true status timeout success) (D-09)"
      status: enforced
      verification: "deployment watch timeout test asserts isDeploymentErrorResult and code COOLIFY_WATCH_TIMEOUT"
    - statement: "Do not return quiet ok on failed/cancelled (D-11)"
      status: enforced
      verification: "failed/cancelled tests assert isError + matching codes"
    - statement: "Do not call or modify pollDeploymentUntilTerminal from watch handler (D-03)"
      status: enforced
      verification: "rg -n \"pollDeploymentUntilTerminal\" src/mcp/tools/deployment.ts returns 0"
    - statement: "No new top-level registerTool for watch (D-01)"
      status: enforced
      verification: "rg -n \"registerTool\\('watch'\" src/mcp/server.ts returns 0"
    - statement: "Do not change application.deploy wait:true behavior (D-02)"
      status: enforced
      verification: "no edits to application.ts wait path in this plan files_modified"
---

<objective>
Wire `deployment.watch` action with dual-signal timeout/failure envelopes and register it on the existing deployment tool (WATCH-01 complete for runtime behavior per D-01..D-12).

Purpose: Agents poll to terminal status with backoff without forever-blocking or treating timeout/failure as success.

Output: Error codes + hints; `handleDeploymentWatch` in `deployment.ts`; green watch tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-deploy-watch/21-CONTEXT.md
@.planning/phases/21-deploy-watch/21-RESEARCH.md
@.planning/phases/21-deploy-watch/21-PATTERNS.md
@.planning/phases/21-deploy-watch/21-01-PLAN.md
@src/mcp/tools/deployment.ts
@src/mcp/tools/application.ts
@src/utils/deploy-watch-poll.ts
@src/utils/errors.ts
</context>

## Artifacts this phase produces

- `CoolifyErrorCode` additions: `COOLIFY_WATCH_TIMEOUT`, `COOLIFY_DEPLOYMENT_FAILED`, `COOLIFY_DEPLOYMENT_CANCELLED` + `RECOVERY_HINTS` entries (timeout → re-call `deployment.watch`; fail/cancel → surface logs via `deployment.get` / `include_logs`).
- `src/mcp/tools/deployment.ts` — `deploymentActionsCatalog` includes `watch(...)`; schema fields `timeout` / `min_interval` / `max_interval` / `include_logs`; `handleDeploymentWatch`; switch case `watch`.
- `src/mcp/tools/deployment.test.ts` — watch describe GREEN.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add watch error codes and RECOVERY_HINTS</name>
  <files>src/utils/errors.ts, src/utils/errors.test.ts</files>
  <read_first>
    - src/utils/errors.ts (CoolifyErrorCode union, RECOVERY_HINTS Record completeness)
    - src/utils/errors.test.ts (existing code/hint patterns)
    - .planning/phases/21-deploy-watch/21-RESEARCH.md (Pattern 2 recommended codes)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (D-09, D-10, D-11)
  </read_first>
  <behavior>
    - CoolifyErrorCode includes COOLIFY_WATCH_TIMEOUT, COOLIFY_DEPLOYMENT_FAILED, COOLIFY_DEPLOYMENT_CANCELLED
    - RECOVERY_HINTS.COOLIFY_WATCH_TIMEOUT mentions re-calling deployment.watch with the same deployment_uuid and optional higher timeout (D-10)
    - RECOVERY_HINTS for FAILED/CANCELLED tell agent to surface the error to the user and optionally fetch logs via deployment.get or include_logs (D-11)
    - TypeScript RECOVERY_HINTS Record keyed by CoolifyErrorCode remains exhaustive (compile-time)
  </behavior>
  <action>
    Extend CoolifyErrorCode and RECOVERY_HINTS in src/utils/errors.ts per D-09/D-10/D-11 and RESEARCH A2. Add a small errors.test.ts assertion that the three new keys exist on RECOVERY_HINTS with non-empty hint arrays. Do not wire deployment.ts in this task.
  </action>
  <verify>
    <automated>npx vitest run src/utils/errors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "COOLIFY_WATCH_TIMEOUT|COOLIFY_DEPLOYMENT_FAILED|COOLIFY_DEPLOYMENT_CANCELLED" src/utils/errors.ts returns at least 6 matches (union + hints)
    - rg -n "deployment\.watch" src/utils/errors.ts returns at least 1 match inside WATCH_TIMEOUT hints
    - npx vitest run src/utils/errors.test.ts green
    - npm run build green
  </acceptance_criteria>
  <done>
    - New codes + recovery hints compile and are tested
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deployment.watch action + flip watch tests GREEN</name>
  <files>src/mcp/tools/deployment.ts, src/mcp/tools/deployment.test.ts</files>
  <read_first>
    - src/mcp/tools/deployment.ts (catalog, schema, handleDeploymentGet projections, handleDeploymentAction switch)
    - src/mcp/tools/application.ts (wait path contrast — do not copy soft timeout OK)
    - src/utils/deploy-watch-poll.ts (pollDeploymentWithBackoff API from Plan 21-01)
    - src/utils/errors.ts (CoolifyApiError + new codes)
    - src/mcp/tools/deployment.test.ts (Wave 0 watch it.fails)
    - .planning/phases/21-deploy-watch/21-PATTERNS.md (deployment.ts watch wiring)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (D-01..D-12)
  </read_first>
  <behavior>
    - deploymentActionsCatalog includes watch(deployment_uuid, timeout?, min_interval?, max_interval?, include_logs?, format?, max_chars?, instance?)
    - Schema action enum includes watch; required deployment_uuid; optional timeout (int 10..1800), min_interval (>=1), max_interval (>=1), include_logs boolean; refine min_interval <= max_interval
    - Defaults applied in handler when omitted: timeout 300, min_interval 3, max_interval 30, include_logs false (D-04, D-05, D-06)
    - handleDeploymentWatch: resolveRoutingEnv → build fetcher via fetchDeployment → pollDeploymentWithBackoff with isRetryableRateLimit reading toStructuredError for httpStatus 429 / data.retry_after → map outcomes
    - finished → projectDeploymentSummary (+ capped logs if include_logs) → buildReadResponse ok
    - timeout → throw CoolifyApiError COOLIFY_WATCH_TIMEOUT with data { deployment, timed_out:true, elapsed_seconds }
    - failed / cancelled-by-user → throw matching CoolifyApiError with clear message including status
    - Outer handleDeploymentAction catch still wrapMcpError (dual-signal)
  </behavior>
  <action>
    Update src/mcp/tools/deployment.ts per D-01..D-12: extend catalog + createFlatActionSchema for watch fields and refine; add handleDeploymentWatch using pollDeploymentWithBackoff only (never pollDeploymentUntilTerminal). Convert seconds to ms for helper. Wire isRetryableRateLimit using toStructuredError / CoolifyApiError httpStatus 429 and retry_after from Plan 21-01. Flip deployment.test.ts watch it.fails → it and adjust mocks so all watch cases green. Do not edit application.ts. Do not register a new top-level tool in server.ts (existing deployment registration picks up schema/catalog).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts -t "watch"</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "'watch'" src/mcp/tools/deployment.ts returns matches in schema actions, allowed/required maps, catalog, and switch
    - rg -n "pollDeploymentWithBackoff" src/mcp/tools/deployment.ts returns at least 2 (import + call)
    - rg -n "pollDeploymentUntilTerminal" src/mcp/tools/deployment.ts returns 0
    - rg -n "COOLIFY_WATCH_TIMEOUT|COOLIFY_DEPLOYMENT_FAILED|COOLIFY_DEPLOYMENT_CANCELLED" src/mcp/tools/deployment.ts returns at least 3
    - rg -c "it\.fails" src/mcp/tools/deployment.test.ts returns 0 for watch cases (no it.fails remaining in watch describe)
    - npx vitest run src/mcp/tools/deployment.test.ts green
    - npx vitest run src/utils/deploy-poll.test.ts green
    - npm run build green
  </acceptance_criteria>
  <done>
    - deployment.watch callable with backoff + dual-signal outcomes
    - WATCH-01 runtime behavior complete
    - wait:true helper still untouched
  </done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → deployment.watch inputSchema | Untrusted agent input; Zod validates uuid + bounds before poll |
| Watch handler → Coolify API | Bearer via resolveRoutingEnv; same trust as deployment.get |
| Error/log payload → agent | Summary projection + optional capped logs; redactSecrets via wrapMcpError |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02 | Denial of Service | deployment.watch unbounded poll | high | mitigate | Zod + handler enforce timeout default 300s (max 1800), min_interval >=1, max_interval >= min; helper caps backoff at 30s default (D-05, D-06, D-07) |
| T-21-02-02 | Information Disclosure | include_logs / error.data deployment snapshot | medium | mitigate | Default include_logs false; use projectDeploymentSummary / truncateLogs / max_chars; wrapMcpError redactSecrets (D-04, D-12) |
| T-21-02-03 | Tampering | Deployment log text in agent context | medium | mitigate | Treat logs as untrusted data; cap size; do not execute log contents |
| T-21-02-04 | Elevation of Privilege | instance routing | low | mitigate | resolveRoutingEnv / soft instance only — no cross-instance credential merge beyond existing Phase 20 patterns |
| T-21-SC | Tampering | npm package installs | low | accept | No new packages |
</threat_model>

<verification>
- npx vitest run src/mcp/tools/deployment.test.ts green
- npx vitest run src/utils/deploy-watch-poll.test.ts green
- npx vitest run src/utils/deploy-poll.test.ts green
- npm run build green
</verification>

<success_criteria>
- Agent can call deployment.watch and receive terminal OK or dual-signal errors
- Backoff helper used exclusively for watch
- application.deploy wait:true unchanged (D-02)
</success_criteria>

<output>
Create `.planning/phases/21-deploy-watch/21-02-SUMMARY.md` when done.
</output>
