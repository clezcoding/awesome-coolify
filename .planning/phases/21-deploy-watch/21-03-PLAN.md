---
phase: 21-deploy-watch
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - src/mcp/prompts.ts
  - tests/mcp/prompts.test.ts
  - README.md
  - README.de.md
autonomous: true
requirements:
  - WATCH-02
user_setup: []

must_haves:
  truths:
    - "MCP prompt deploy documents 2–4 concrete steps: application.deploy wait false → capture deployment_uuid → deployment.watch → on timeout re-watch → on failed/cancelled surface clear error (D-13, D-14)"
    - "Deploy prompt marks application.deploy wait:true as legacy/back-compat and recommends watch (D-15)"
    - "Deploy prompt no longer contains Future Phase 21 or do-not-call-watch-until-exists forward-ref language"
    - "README.md has a short Watch section covering timeout default 300s, backoff band, timeout→re-watch, failed/cancelled clear error, and wait:true legacy (D-13, D-15)"
    - "README.de.md has the parallel German Watch section with the same behaviors (D-13)"
    - "deployment table in README EN/DE lists watch action row"
    - "tests/mcp/prompts.test.ts deploy case flipped from it.fails to bare it and green"
    - "Phase 22 IDE skill packs remain out of this phase but D-16 obligation is noted in README Watch section or deploy prompt footnote so SKILL-02 consumers know watch must be documented later (D-16)"
  artifacts:
    - "src/mcp/prompts.ts (modified — deploy prompt watch-primary)"
    - "tests/mcp/prompts.test.ts (modified — GREEN)"
    - "README.md (modified — Watch section + deployment table)"
    - "README.de.md (modified — Watch Abschnitt + deployment table)"
  key_links:
    - "registerCoolifyPrompts deploy assistant content → deployment.watch steps"
    - "README Watch section → same recovery guidance as prompt (timeout re-watch, clear fail)"
  assumptions:
    - "A-EDGE-02: unclassified WATCH-02 → prompt + bilingual README satisfy skill-and/or-prompt requirement; IDE packs deferred D-16"
    - "Claude discretion: exact EN/DE wording and README section placement near deployment tool docs"
  prohibitions:
    - statement: "No IDE skill pack files in this phase (D-16 deferred to Phase 22 SKILL-01/SKILL-02)"
      status: enforced
      verification: "files_modified contains no new .cursor/skills or .claude/skills Coolify pack paths"
    - statement: "No long playbook — keep Phase 19 short-prompt style 2–4 steps (D-14)"
      status: enforced
      verification: "deploy assistant content stays concise; prompts.test.ts does not require a long runbook"
    - statement: "No OpenAPI coverage docs (Phase 23)"
      status: enforced
      verification: "no OpenAPI map artifacts added"
    - statement: "Do not document forever-blocking watch without timeout"
      status: enforced
      verification: "Watch docs mention bounded timeout and re-watch recovery"
---

<objective>
Document `deployment.watch` for agents via the `deploy` MCP prompt and bilingual README Watch sections (WATCH-02 per D-13..D-16).

Purpose: Agents stop manual `deployment.get` loops and use bounded watch with clear timeout/failure recovery; `wait:true` marked legacy.

Output: Updated `src/mcp/prompts.ts`; green `tests/mcp/prompts.test.ts`; README.md + README.de.md Watch docs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-deploy-watch/21-CONTEXT.md
@.planning/phases/21-deploy-watch/21-RESEARCH.md
@.planning/phases/21-deploy-watch/21-PATTERNS.md
@.planning/phases/21-deploy-watch/21-02-PLAN.md
@src/mcp/prompts.ts
@tests/mcp/prompts.test.ts
@README.md
@README.de.md
</context>

## Artifacts this phase produces

- `src/mcp/prompts.ts` — deploy prompt assistant content rewritten to watch-primary 2–4 steps + legacy wait:true note (keeps `manifestSoftNote` / `optionalInstanceSuffix` helpers).
- `tests/mcp/prompts.test.ts` — deploy assertions GREEN.
- `README.md` — `watch` row in deployment table + short Watch section (timeout 300s, backoff, re-watch, clear fail, wait:true legacy; D-16 Phase 22 note).
- `README.de.md` — parallel German Watch docs.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite deploy MCP prompt for watch-primary flow + flip prompts tests GREEN</name>
  <files>src/mcp/prompts.ts, tests/mcp/prompts.test.ts</files>
  <read_first>
    - src/mcp/prompts.ts (deploy prompt content, helpers manifestSoftNote / optionalInstanceSuffix)
    - tests/mcp/prompts.test.ts (Wave 0 it.fails target assertions)
    - .planning/phases/21-deploy-watch/21-PATTERNS.md (deploy prompt target shape)
    - .planning/phases/21-deploy-watch/21-RESEARCH.md (Deploy prompt target shape WATCH-02)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (D-13, D-14, D-15)
  </read_first>
  <behavior>
    - Deploy assistant content steps: (1) resolve uuid + application.deploy wait false (2) deployment.watch with timeout optional default 300 (3) on watch timeout error re-call deployment.watch same uuid (4) on failed/cancelled surface clear error to user — do not treat as success
    - Explicit note that application.deploy wait:true is legacy; prefer watch (D-15)
    - Remove Future Phase 21 / do-not-call-watch forward-ref
    - Keep force/instance interpolation and soft manifest note helpers
    - prompts.test.ts deploy case uses bare it and asserts watch primary, re-watch, fail messaging, legacy wait note
  </behavior>
  <action>
    Update the deploy prompt assistant content in src/mcp/prompts.ts to the D-14 2–4 step watch-primary workflow (per RESEARCH target shape). Preserve registerPrompt structure and helpers. Flip tests/mcp/prompts.test.ts deploy it.fails to bare it matching the new content. Do not add IDE skill pack files (D-16).
  </action>
  <verify>
    <automated>npx vitest run tests/mcp/prompts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "deployment\.watch" src/mcp/prompts.ts returns at least 1
    - rg -n "Future \(Phase 21\)|do not call watch until" src/mcp/prompts.ts returns 0
    - rg -n "legacy|wait:\s*true|wait:true" src/mcp/prompts.ts returns at least 1
    - rg -c "it\.fails" tests/mcp/prompts.test.ts returns 0
    - npx vitest run tests/mcp/prompts.test.ts green
    - npm run build green
  </acceptance_criteria>
  <done>
    - Deploy prompt documents watch usage with timeout/recovery (WATCH-02)
    - Prompt tests green
  </done>
</task>

<task type="auto">
  <name>Task 2: Add README.md and README.de.md Watch sections + deployment.watch table rows</name>
  <files>README.md, README.de.md</files>
  <read_first>
    - README.md (deployment table ~lines 384-390; wait:true callout examples ~99, 330, 644)
    - README.de.md (parallel deployment section)
    - .planning/phases/21-deploy-watch/21-PATTERNS.md (README Watch docs)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (D-13, D-15, D-16)
  </read_first>
  <behavior>
    - Both READMEs add watch row to deployment actions table
    - Both READMEs add a short Watch section: default timeout 300s, interval band start 3s cap 30s with backoff+jitter, timeout → re-call deployment.watch, failed/cancelled → clear error (not quiet success), wait:true legacy/back-compat recommend watch
    - Brief note that IDE skill packs (Phase 22) must document deployment.watch timeout/non-forever/recovery per D-16 / SKILL-02 — without implementing those packs now
    - Adjust any primary happy-path examples that present wait:true as the recommended path so they recommend watch instead (D-15); legacy mention OK
  </behavior>
  <action>
    Edit README.md and README.de.md per D-13/D-15/D-16: add watch to deployment tables, add concise Watch sections (EN + DE), steer examples from wait:true-as-primary toward deployment.watch, leave a one-line Phase 22 skills obligation note. No OpenAPI docs. No new skill pack directories.
  </action>
  <verify>
    <automated>rg -n "deployment\.watch|Watch" README.md README.de.md | head -40</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "`watch`|watch" README.md returns matches in deployment table and Watch section
    - rg -n "watch|Watch|Beobachten" README.de.md returns matches in deployment table and Watch section
    - rg -n "300" README.md returns at least 1 match near Watch timeout guidance
    - rg -n "legacy|wait:\s*true|wait:true|Kompatibilität" README.md README.de.md returns at least 1 match each file family for legacy guidance
    - rg -n "Phase 22|SKILL" README.md returns at least 1 match for D-16 obligation note
    - test -f README.md -a -f README.de.md
  </acceptance_criteria>
  <done>
    - Bilingual Watch docs ship WATCH-02 human-facing guidance
    - D-16 Phase 22 obligation noted without implementing skill packs
  </done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt/README text → agent behavior | Documentation steers tool calls; no new auth boundary |
| Docs → user | Public repo docs must not invent forever-block instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Denial of Service | Docs encouraging tight poll loops | medium | mitigate | Watch section + prompt mandate bounded timeout, backoff, and re-watch — forbid documenting forever watch without timeout |
| T-21-03-02 | Information Disclosure | README examples | low | accept | Examples use placeholder UUIDs only; no real tokens |
| T-21-SC | Tampering | npm package installs | low | accept | Docs-only plan — no packages |
</threat_model>

<verification>
- npx vitest run tests/mcp/prompts.test.ts green
- rg confirms Watch sections + watch table rows in README.md and README.de.md
- npm run build green
</verification>

<success_criteria>
- WATCH-02 satisfied via prompt + bilingual README
- wait:true documented as legacy; watch recommended (D-15)
- No IDE skill packs; D-16 noted for Phase 22
</success_criteria>

<output>
Create `.planning/phases/21-deploy-watch/21-03-SUMMARY.md` when done.
</output>
