---
phase: 27-branding-docs-stale-fix
plan: 01
type: execute
wave: 1
depends_on:
  - "27-00"
files_modified:
  - scripts/generate-mcp-icon-data.mjs
  - src/mcp/mcp-icon-data.ts
  - src/mcp/server-icons.ts
  - src/mcp/server.ts
  - package.json
  - src/mcp/server.test.ts
  - src/mcp/server-icons.test.ts
autonomous: false
requirements:
  - BRND-01
user_setup: []

must_haves:
  truths:
    - "npm run build generates mcp-icon-data.ts and bundles data URI into dist/ for npx path (BRND-01, D-01)"
    - "initialize serverInfo.icons has data URI entry first plus ≥2 jsDelivr CDN PNG entries with mimeType and sizes (BRND-01, D-01, D-02)"
    - "McpServer version field equals readPackageVersion() matching package.json 1.0.1 (D-08)"
    - "Wave 0 it.fails scaffolds flip GREEN; npm test -- src/mcp/server.test.ts src/mcp/server-icons.test.ts -x passes"
    - "Empty-state backstop: data URI never read from docs/assets at runtime — only embedded constant (UI-SPEC error: initialize missing data URI)"
    - "icons[] length ≥3 documented in tests (UI-SPEC zero-one-many)"
  artifacts:
    - path: scripts/generate-mcp-icon-data.mjs
      provides: "PNG to base64 module generator"
    - path: src/mcp/mcp-icon-data.ts
      provides: "MCP_ICON_192_BASE64 constant"
      exports: ["MCP_ICON_192_BASE64"]
    - path: src/mcp/server-icons.ts
      provides: "buildMcpServerIcons()"
      exports: ["buildMcpServerIcons"]
    - path: src/mcp/server.ts
      provides: "icons + version wire-up"
      contains: "buildMcpServerIcons"
  key_links:
    - from: "package.json build script"
      to: "scripts/generate-mcp-icon-data.mjs"
      via: "pre-tsup generation"
    - from: "buildMcpServerIcons"
      to: "McpServer constructor icons"
      via: "createAndConnectServer"
    - from: "readPackageVersion"
      to: "McpServer constructor version"
      via: "import from package-version.js"
  prohibitions:
    - statement: "Do not use runtime readFileSync on docs/assets for data URI (npm tarball excludes docs/)"
      status: enforced
      verification: "rg -n 'readFileSync.*docs/assets' src/mcp/server-icons.ts src/mcp/server.ts | grep -v '^#' | wc -l is 0"
    - statement: "Do not ship data-URI-only or CDN-only icons array (D-01)"
      status: enforced
      verification: "server-icons.test.ts asserts both data and CDN entries"
    - statement: "Do not embed favicon-512 in data URI (~788 KB)"
      status: enforced
      verification: "rg -n 'favicon-512' src/mcp/ | grep -v '^#' | wc -l is 0"
---

<objective>
Tracer vertical slice: build-time icon embed → buildMcpServerIcons → server.ts initialize payload with readPackageVersion — one happy path, tests GREEN.

Purpose: Prove npm-safe dual-icon architecture before maintainer verify and docs copy in Plans 27-02/27-03.

Output: `generate-mcp-icon-data.mjs`, `mcp-icon-data.ts`, `server-icons.ts`, updated `server.ts`, green branding tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-branding-docs-stale-fix/27-CONTEXT.md
@.planning/phases/27-branding-docs-stale-fix/27-RESEARCH.md
@.planning/phases/27-branding-docs-stale-fix/27-PATTERNS.md
@.planning/phases/27-branding-docs-stale-fix/27-UI-SPEC.md
@.planning/phases/27-branding-docs-stale-fix/27-00-SUMMARY.md
@src/mcp/server.ts
@src/mcp/tools/meta.ts
@src/utils/package-version.ts
@docs/assets/mcp-icon-192.png
</context>

## Artifacts this phase produces

- `scripts/generate-mcp-icon-data.mjs` — reads `docs/assets/mcp-icon-192.png`, writes `src/mcp/mcp-icon-data.ts`.
- `src/mcp/server-icons.ts` — `buildMcpServerIcons()` per UI-SPEC icon contract table (V1 baseline).
- `src/mcp/server.ts` — `version: readPackageVersion()`, `icons: buildMcpServerIcons()`.
- `package.json` — `"build": "node scripts/generate-mcp-icon-data.mjs && tsup"`.
- Green Wave 0 tests.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>D-01: Advertise both data URI and multi-size CDN PNG entries</name>
  <decision>D-01: Advertise both data URI and multi-size CDN PNG entries in serverInfo.icons</decision>
  <context>Locked decision with costly reversibility — published initialize payload / client display contract. Phase 16 shipped CDN-only; Phase 27 adds build-time embedded data URI as primary (D-02) while keeping jsDelivr entries. Bundle grows ~42 KB base64 in dist.</context>
  <options>
    <option id="proceed-d01">
      <name>Proceed with dual icons[] (data URI + CDN multi-size)</name>
      <pros>Matches locked D-01/D-02; npm-safe embed; spec-compliant workarounds</pros>
      <cons>~42 KB larger dist bundle; initialize payload larger</cons>
    </option>
    <option id="defer-d01">
      <name>Defer dual strategy</name>
      <pros>Smaller bundle</pros>
      <cons>Violates locked D-01 — not valid without user re-discuss</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-d01</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end dual icons build pipeline — V1 baseline</name>
  <reversibility rating="costly">D-01 dual icons[] changes published initialize contract; build embeds ~42 KB base64 in dist.</reversibility>
  <files>scripts/generate-mcp-icon-data.mjs, src/mcp/mcp-icon-data.ts, src/mcp/server-icons.ts, src/mcp/server.ts, package.json, src/mcp/server-icons.test.ts</files>
  <read_first>
    - .planning/phases/27-branding-docs-stale-fix/27-PATTERNS.md (server-icons.ts, generate script, server.ts target)
    - .planning/phases/27-branding-docs-stale-fix/27-UI-SPEC.md (MCP Icon Contract table — V1 default)
    - scripts/changeset-emit-new-tag.mjs (script header/fs pattern)
    - src/mcp/server.ts (createAndConnectServer lines 792-817)
    - package.json (files allowlist, build script)
  </read_first>
  <behavior>
    - npm run build succeeds and dist/index.js contains embedded base64 substring
    - buildMcpServerIcons returns 3 entries: data URI 192x192 first, favicon-32 CDN 32x32, mcp-icon-192 CDN 192x192
    - server.ts uses icons: buildMcpServerIcons() not inline array
    - server-icons.test.ts Wave 0 it.fails cases flip to it and pass
  </behavior>
  <action>
    Per D-01/D-02: create scripts/generate-mcp-icon-data.mjs following PATTERNS.md — read docs/assets/mcp-icon-192.png, write src/mcp/mcp-icon-data.ts exporting MCP_ICON_192_BASE64. Prepend generator to package.json build script before tsup. Create src/mcp/server-icons.ts with buildMcpServerIcons() returning UI-SPEC V1 table: entry 0 data:image/png;base64,${MCP_ICON_192_BASE64} sizes 192x192; entry 1 jsDelivr favicon-32.png sizes 32x32; entry 2 jsDelivr mcp-icon-192.png sizes 192x192 — CDN base URL https://cdn.jsdelivr.net/gh/clezcoding/awesome-coolify@main/docs/assets. Update server.ts: import buildMcpServerIcons, replace inline icons array with icons: buildMcpServerIcons(). Do NOT change version field yet (D-08 in next task). Run npm run build then commit generated mcp-icon-data.ts. Flip server-icons.test.ts it.fails to it. Do not touch registerCoolifyTools or tool registrations.
  </action>
  <acceptance_criteria>
    - npm run build exits 0
    - src/mcp/server-icons.ts and mcp-icon-data.ts exist
    - server.ts constructor uses buildMcpServerIcons()
    - npm test -- src/mcp/server-icons.test.ts -x passes all cases
    - rg finds no readFileSync on docs/assets in src/mcp/
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test -- src/mcp/server-icons.test.ts -x</automated>
  </verify>
  <done>V1 dual icons pipeline works end-to-end; server-icons tests green</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>D-08: Align serverInfo.version with package.json</name>
  <decision>D-08: Align serverInfo.version with package.json via readPackageVersion()</decision>
  <context>Costly reversibility — changes published serverInfo.version from literal 0.1.0 to dynamic 1.0.1. meta.version already uses readPackageVersion (Phase 24); this closes handshake drift only.</context>
  <options>
    <option id="proceed-d08">
      <name>Wire readPackageVersion() in McpServer constructor</name>
      <pros>Single source of truth; matches meta.version; fixes D-08</pros>
      <cons>Clients caching version string see 1.0.1 not 0.1.0</cons>
    </option>
    <option id="keep-literal">
      <name>Keep literal 0.1.0</name>
      <pros>No client-visible version change</pros>
      <cons>Violates locked D-08</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-d08</resume-signal>
</task>

<task type="auto">
  <name>D-08 version parity + flip server.test.ts GREEN</name>
  <reversibility rating="costly">serverInfo.version published field changes to package.json semver.</reversibility>
  <files>src/mcp/server.ts, src/mcp/server.test.ts</files>
  <read_first>
    - src/mcp/tools/meta.ts (readPackageVersion import pattern)
    - src/mcp/server.test.ts (Wave 0 it.fails branding block)
    - src/utils/package-version.ts
  </read_first>
  <action>
    Per D-08: import readPackageVersion from ../utils/package-version.js in server.ts; set version: readPackageVersion() in McpServer constructor. Flip all Wave 0 server.test.ts it.fails branding cases to it. Update legacy jsDelivr inline assertion to expect buildMcpServerIcons() call instead of inline URL if constructor block no longer contains raw jsDelivr string. Add test that readPackageVersion() output equals package.json version field. Run full branding describe block.
  </action>
  <acceptance_criteria>
    - server.ts contains version: readPackageVersion() and no version: '0.1.0'
    - npm test -- src/mcp/server.test.ts -x passes McpServer branding metadata describe
    - readPackageVersion() returns 1.0.1 from package.json
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/mcp/server.test.ts -x</automated>
  </verify>
  <done>serverInfo.version parity green; all Wave 0 server branding tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script → PNG asset | Reads trusted repo asset only |
| initialize → MCP client | serverInfo.icons and version are server-originated constants |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | icons[] src | medium | mitigate | PNG-only assets; no SVG; jsDelivr from own repo path only |
| T-27-02 | Spoofing | CDN icon URLs | medium | mitigate | Hardcode clezcoding/awesome-coolify@main/docs/assets base |
| T-27-03 | Denial of Service | data URI size | low | accept | ~42 KB acceptable per RESEARCH A3; exclude favicon-512 |
| T-27-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this phase — N/A |
</threat_model>

<verification>
npm run build && npm test -- src/mcp/server.test.ts src/mcp/server-icons.test.ts -x && node -e "const fs=require('fs'); const s=fs.readFileSync('dist/index.js','utf8'); if(!s.includes('data:image/png;base64,')) process.exit(1)" && graphify update .
</verification>

<success_criteria>
BRND-01 satisfied: dual icons in initialize payload, build embeds data URI for npm path, D-08 version parity, automated tests green.
</success_criteria>

<output>
Create `.planning/phases/27-branding-docs-stale-fix/27-01-SUMMARY.md` when done
</output>
