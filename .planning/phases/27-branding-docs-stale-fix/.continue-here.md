---
context: phase
phase: 27-branding-docs-stale-fix
task: 0
total_tasks: 4
status: planned
last_updated: 2026-07-28T04:23:00.996Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Planner model slug — `cursor-grok-4.5-high` from config invalid on Cursor; spawn without explicit model or use allowed slug (e.g. `composer-2.5`)

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| UI gate false-positive | Branding/docs phase triggered `ui-plan-gate` block | advisory | Use `--skip-ui` for non-React phases OR run `/gsd-ui-phase` first (done this session) |
| Weak verify awk | `awk '$1>=4'` passes on zero matches | blocking | Plan 27-02 fixed to `test "$(rg … \| grep -cv '^#' \|\| true)" -ge 4` — do not regress |

<current_state>
Phase 27 planning **complete**. Execution **not started**.

- 4 plans written + plan-checker passed (1 revision)
- Artifacts: `27-RESEARCH.md`, `27-UI-SPEC.md`, `27-PATTERNS.md`, `27-VALIDATION.md`, `27-00`…`27-03-PLAN.md`
- Committed: `2ebe739` docs(27): create phase plan
- STATE.md: Plan not started, status ready to execute
</current_state>

<completed_work>

**This session (planning):**
- `/gsd-plan-phase 27` — research, UI-SPEC, pattern map, 4 plans, verification
- Research: npm pack has no `docs/assets/` → build-time data URI embed
- UI-SPEC: branding/docs contract (no shadcn); approved with Dimension 2 FLAG
- Plans: Wave 0 RED → Wave 1 tracer icons+version → Wave 2 verify doc → Wave 3 DOC-01 sweep
- Checker revision: RESEARCH open questions resolved, 27-02 verify command fixed

**Prior session:**
- Phase 26 shipped (PR #100)
- Phase 27 CONTEXT gathered via discuss-phase
</completed_work>

<remaining_work>

**Execute all 4 plans (in order):**

| Plan | Wave | Focus |
|------|------|-------|
| 27-00 | 0 | RED `it.fails` scaffolds — server.test, server-icons.test, doc-version-parity |
| 27-01 | 1 | Tracer: `generate-mcp-icon-data.mjs`, `buildMcpServerIcons()`, `readPackageVersion()` — checkpoints D-01, D-08 |
| 27-02 | 2 | Refresh `cursor-icon-verify.md`; human verify dist/ + npx; up to 4 icon variants (D-03) |
| 27-03 | 3 | DOC-01: PROJECT opener, README EN/DE, docs sweep — no CHANGELOG rewrite (D-09) |

**Requirements:** BRND-01, BRND-02, DOC-01 — all mapped in plans.
</remaining_work>

<decisions_made>

- Research-first (user choice) — data URI build embed + CDN multi-size confirmed
- UI-SPEC path (user choice) — adapted for MCP metadata + docs, not web UI
- Data URI primary, CDN secondary (CONTEXT D-02)
- Max 4 icon experiments then accept client limitation (D-03/D-05)
- Commit generated `mcp-icon-data.ts` via build hook (RESEARCH resolved)
</decisions_made>

<blockers>
- None — ready for `/gsd-execute-phase 27`
</blockers>

## Required Reading (in order)

1. `.planning/phases/27-branding-docs-stale-fix/27-CONTEXT.md` — locked D-01–D-09
2. `.planning/phases/27-branding-docs-stale-fix/27-00-PLAN.md` — first execute target
3. `.planning/phases/27-branding-docs-stale-fix/27-RESEARCH.md` — icon/npm constraints
4. `.planning/phases/27-branding-docs-stale-fix/27-UI-SPEC.md` — copy/icon wire contract

## Infrastructure State

- No background servers/watchers running
- No async jobs
- Uncommitted local (not in handoff commit): `.planning/config.json` (`firecrawl: true`), `CURSOR-SETUP-GUIDE.md.draft.md`

<context>
v3.2 final phase before milestone close. Scope: MCP `initialize` icons (data URI + CDN), version `1.0.1` parity, stale "pending Version Packages" docs fix. Human Cursor UI verify is expected in 27-02 — not a blocker for starting execute.
</context>

<next_action>
Start with: `/gsd-execute-phase 27` — Plan 27-00 Task 1 (RED scaffolds in `src/mcp/server.test.ts`)
</next_action>
