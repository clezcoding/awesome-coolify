---
phase: 27-branding-docs-stale-fix
plan: 02
type: execute
wave: 2
depends_on:
  - "27-01"
files_modified:
  - docs/assets/cursor-icon-verify.md
  - docs/assets/cursor-icon-verify.png
  - src/mcp/server-icons.ts
autonomous: false
requirements:
  - BRND-02
user_setup:
  - service: cursor-ide
    why: "BRND-02 maintainer MCP list UI verify for dist/ and npx paths"
    dashboard_config:
      - task: "Configure .cursor/mcp.json with local dist/ entry"
        location: "Project .cursor/mcp.json — node dist/index.js after npm run build"
      - task: "Configure npx awesome-coolify-mcp@1.0.1 entry"
        location: "Project .cursor/mcp.json — npx path for npm verify"

must_haves:
  truths:
    - "cursor-icon-verify.md documents Path A local dist/ and Path B npx awesome-coolify-mcp@1.0.1 with UI observations and initialize JSON excerpts (BRND-02, D-04, D-06)"
    - "Verify doc records D-03 variant tested (V1–V4) and stops after budget if Cursor shows letter fallback (D-03, D-05)"
    - "Conclusion table covers data URI, CDN PNGs, version parity, jsDelivr 200, tools visible, custom icon per path (UI-SPEC verify doc layout)"
    - "Phase passes with documented client limitation + screenshot + initialize dump if UI omits custom icon (D-05)"
    - "Partial verify backstop: phase incomplete if either dist/ or npx path section missing (UI-SPEC partial state)"
    - "Screenshot committed or metadata notes pending capture (UI-SPEC empty screenshot state)"
    - "Sample JSON uses version 1.0.1 and icons[] placeholder not full ~42 KB base64 inline (UI-SPEC overflow backstop)"
  artifacts:
    - path: docs/assets/cursor-icon-verify.md
      provides: "BRND-02 maintainer verify record refreshed for v3.2"
    - path: docs/assets/cursor-icon-verify.png
      provides: "Cursor MCP list UI screenshot"
  key_links:
    - from: "cursor-icon-verify.md Path A"
      to: "dist/index.js initialize"
      via: "stdio dump procedure"
    - from: "cursor-icon-verify.md Path B"
      to: "npx awesome-coolify-mcp@1.0.1"
      via: "stdio dump procedure"
  prohibitions:
    - statement: "Do not create parallel v3.2-only verify doc (D-06)"
      status: enforced
      verification: "ls docs/assets/*icon-verify* | wc -l is 1"
    - statement: "Do not exceed four icon experiment variants (D-03)"
      status: enforced
      verification: "verify doc Variant tested section lists at most V4"
---

<objective>
BRND-02 maintainer re-verify gate: document dist/ and npx paths in cursor-icon-verify.md with evidence; accept client limitation if Cursor UI still shows letter fallback.

Purpose: Close BRND-02 with Phase 16 D-09 precedent refreshed for v3.2 workarounds.

Output: Updated `cursor-icon-verify.md`, screenshot, optional V2–V4 icon tweaks within D-03 budget.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-branding-docs-stale-fix/27-CONTEXT.md
@.planning/phases/27-branding-docs-stale-fix/27-UI-SPEC.md
@.planning/phases/27-branding-docs-stale-fix/27-01-SUMMARY.md
@docs/assets/cursor-icon-verify.md
@src/mcp/server-icons.ts
</context>

## Artifacts this phase produces

- `docs/assets/cursor-icon-verify.md` — v3.2 refresh with both verify paths, conclusion table, forum evidence.
- `docs/assets/cursor-icon-verify.png` — MCP list UI capture (custom icon or A fallback).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>D-04: Maintainer re-verify both dist/ and npx paths</name>
  <decision>D-04: Maintainer re-verify both dist/ and npx awesome-coolify-mcp paths</decision>
  <context>Costly reversibility — BRND-02 success criterion requires both paths documented before phase close. Maintainer must have Cursor access and ability to run npm run build + npx.</context>
  <options>
    <option id="proceed-d04">
      <name>Proceed with dual-path verify (dist/ + npx)</name>
      <pros>Fulfills locked D-04; matches Phase 16 precedent extended</pros>
      <cons>Manual maintainer time; requires Cursor IDE</cons>
    </option>
    <option id="single-path">
      <name>Verify dist/ only</name>
      <pros>Faster</pros>
      <cons>Violates locked D-04</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-d04</resume-signal>
</task>

<task type="auto">
  <name>Refresh cursor-icon-verify.md structure + initialize samples</name>
  <files>docs/assets/cursor-icon-verify.md</files>
  <read_first>
    - docs/assets/cursor-icon-verify.md (existing Phase 16 template)
    - .planning/phases/27-branding-docs-stale-fix/27-UI-SPEC.md (Verify Doc Layout Contract)
    - .planning/milestones/v3.0-phases/16-coolify-cloud-server-branding/16-CONTEXT.md (D-09 precedent)
    - src/mcp/server-icons.ts (current V1 icon shape)
  </read_first>
  <action>
    Per D-06: rewrite docs/assets/cursor-icon-verify.md in place — do not fork new file. Update H1 to MCP icon verify — v3.2 workarounds (Phase 27). Add sections per UI-SPEC order: metadata (date, outcome placeholder, screenshot link), Variant tested (V1 baseline from Plan 27-01), Path A local dist/ (mcp.json node dist/index.js, UI observation template, initialize JSON excerpt with version 1.0.1 and icons[] showing data URI placeholder data:image/png;base64,{…} plus CDN URLs — never paste full base64), Path B npm npx awesome-coolify-mcp@1.0.1 (same structure), Server emits icons correctly full serverInfo sample, CDN asset reachable curl -I blocks for favicon-32 and mcp-icon-192, Spec/SDK alignment paragraph, Cursor client evidence forum links, Conclusion table per UI-SPEC. Include stdio initialize dump bash procedure from RESEARCH.md. Update sample JSON version from stale handshake semver to 1.0.1.
  </action>
  <acceptance_criteria>
    - cursor-icon-verify.md has Path A and Path B subsections
    - Conclusion table has rows for data URI, CDN, version, jsDelivr, tools visible, custom icon per path
    - Sample JSON version is 1.0.1
    - No full ~42 KB base64 pasted inline
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'Path A|Path B|1\.0\.1|data:image/png;base64,\{…\}' docs/assets/cursor-icon-verify.md | grep -v '^#' | wc -l | awk '$1>=4'</automated>
  </verify>
  <done>Verify doc template ready for maintainer evidence fill-in</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>BRND-02: Cursor MCP list verify dist/ + npx paths</name>
  <what-built>BRND-02 maintainer verify: Cursor MCP list for dist/ and npx paths; optional D-03 icon variants V2–V4 if V1 UI fails</what-built>
  <how-to-verify>
    1. npm run build
    2. Path A: Point Cursor .cursor/mcp.json at node dist/index.js; open MCP server list; note icon (custom or letter A fallback); capture screenshot to docs/assets/cursor-icon-verify.png
    3. Run stdio initialize dump from verify doc; paste excerpt into Path A section
    4. Path B: Switch mcp.json to npx awesome-coolify-mcp@1.0.1; repeat UI observation + dump; paste into Path B section
    5. curl -I both jsDelivr URLs; record 200 in CDN section
    6. If custom icon not rendered after V1: try V2 (theme dark on data URI), V3 (CDN first), V4 (drop favicon-32) — max 4 variants per D-03; update server-icons.ts and rebuild between attempts
    7. Set Outcome line per D-05: Client limitation (server correct) OR Custom icon rendered
    8. Fill Conclusion table checkmarks
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → Cursor IDE | Manual UI observation; not automatable |
| verify doc → public readers | Evidence doc is trusted maintainer record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-02-01 | Repudiation | verify evidence | low | mitigate | Screenshot + initialize JSON dump both paths |
| T-27-02-02 | Information Disclosure | initialize dump in doc | low | accept | Truncate base64 placeholder per UI-SPEC |
</threat_model>

<verification>
docs/assets/cursor-icon-verify.md complete with both paths filled; screenshot present; D-03 budget respected (≤4 variants documented).
</verification>

<success_criteria>
BRND-02 satisfied: maintainer documented dist/ and npx outcomes; client limitation accepted with evidence if UI omits icon.
</success_criteria>

<output>
Create `.planning/phases/27-branding-docs-stale-fix/27-02-SUMMARY.md` when done
</output>
