---
phase: 29-drift-heal
plan: 03
type: execute
wave: 3
depends_on:
  - "29-01"
  - "29-02"
files_modified:
  - src/mcp/capabilities.ts
  - src/mcp/tools/system.test.ts
  - docs/coverage-map.yaml
  - docs/COVERAGE.md
  - README.md
  - README.de.md
autonomous: true
requirements:
  - DRIFT-01
  - DRIFT-02
  - DRIFT-03
user_setup: []

must_haves:
  truths:
    - "system.version exposes `manifest_audit` and `envs_promote` capability keys (D-15)."
    - "docs/COVERAGE.md records manifest.audit and application.envs:promote as MCP composites over existing Coolify APIs, not new REST endpoints (D-14, D-15)."
    - "README EN and README DE document manifest.audit, env.promote naming, safety defaults, and remediation hints consistently (D-15)."
    - "No docs claim auto-heal, cross-instance promotion, new drift/heal tool, or Coolify-native promote endpoint (D-01, D-02, D-11, D-14, D-16)."
    - "Targeted manifest, application, system, coverage, and docs parity checks pass before phase verification."
  artifacts:
    - path: src/mcp/capabilities.ts
      provides: "manifest_audit and envs_promote capabilities"
      contains: "manifest_audit"
    - path: src/mcp/tools/system.test.ts
      provides: "GREEN capability assertions from Wave 0"
      contains: "envs_promote"
    - path: docs/coverage-map.yaml
      provides: "source map rows for manifest.audit and application.envs:promote"
      contains: "application.envs:promote"
    - path: docs/COVERAGE.md
      provides: "generated OpenAPI coverage report"
      contains: "manifest.audit"
    - path: README.md
      provides: "English discoverability and safety docs"
      contains: "manifest.audit"
    - path: README.de.md
      provides: "German discoverability and safety docs"
      contains: "manifest.audit"
  key_links:
    - from: "system.version"
      to: "COOLIFY_412_CAPABILITIES"
      via: "capability map consumed by system tool"
    - from: "docs/coverage-map.yaml"
      to: "docs/COVERAGE.md"
      via: "npm run openapi:coverage"
    - from: "README.md / README.de.md"
      to: "implemented action names"
      via: "literal manifest.audit and application envs:promote docs"
---

<objective>
Publish Phase 29 discoverability: capabilities, OpenAPI coverage metadata, and README EN/DE docs.

Purpose: Make new drift-heal actions visible to agents and users with accurate safety wording and no nonexistent endpoint claims.

Output: capability keys, regenerated coverage report, bilingual docs, final targeted test pass.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-drift-heal/29-CONTEXT.md
@.planning/phases/29-drift-heal/29-RESEARCH.md
@.planning/phases/29-drift-heal/29-PATTERNS.md
@.planning/phases/29-drift-heal/29-VALIDATION.md
@.planning/phases/29-drift-heal/29-01-SUMMARY.md
@.planning/phases/29-drift-heal/29-02-SUMMARY.md
@.cursor/skills/spike-findings-awesome-coolify/SKILL.md
@src/mcp/capabilities.ts
@src/mcp/tools/system.ts
@src/mcp/tools/system.test.ts
@docs/coverage-map.yaml
@docs/COVERAGE.md
@README.md
@README.de.md
@tests/integration/docs-parity.test.ts
</context>

## Artifacts this phase produces

- `src/mcp/capabilities.ts` — `manifest_audit` and `envs_promote` keys.
- `src/mcp/tools/system.test.ts` — GREEN capability assertions.
- `docs/coverage-map.yaml` — coverage rows mapping new actions to existing client/API calls.
- `docs/COVERAGE.md` — regenerated coverage report.
- `README.md` / `README.de.md` — bilingual docs for Drift & Heal actions and safety posture.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish capability keys and coverage rows</name>
  <files>src/mcp/capabilities.ts, src/mcp/tools/system.test.ts, docs/coverage-map.yaml, docs/COVERAGE.md</files>
  <read_first>
    - .planning/phases/29-drift-heal/29-CONTEXT.md (D-14, D-15)
    - .planning/phases/29-drift-heal/29-RESEARCH.md (Standard Stack, Validation Architecture)
    - .planning/phases/29-drift-heal/29-PATTERNS.md (capabilities.ts, system.test.ts, coverage-map analogs)
    - src/mcp/capabilities.ts (existing 4.1.2 capability entries)
    - src/mcp/tools/system.test.ts (capability key assertion from Wave 0)
    - docs/coverage-map.yaml (manifest.diff and application.envs:sync rows)
    - docs/COVERAGE.md (generated output expectations)
  </read_first>
  <behavior>
    - `system.version` includes `manifest_audit` and `envs_promote` keys with supported true, coolify_min_version 4.1.2, and notes stating MCP composite behavior (D-15).
    - Capability count assertions from Wave 0 turn GREEN without deleting older capability keys.
    - coverage-map has `manifest.audit` row mapped to existing live read client calls used by audit.
    - coverage-map has `application.envs:promote` row mapped to existing env read/update/create client calls used by promote.
    - `docs/COVERAGE.md` is regenerated from the map and includes both new action rows.
  </behavior>
  <action>
    Add `manifest_audit` and `envs_promote` to `src/mcp/capabilities.ts` following the existing capability object style. Mark both supported on Coolify 4.1.2 and describe them as MCP composites over existing API calls, not Coolify-native REST endpoints. Flip the Wave 0 system capability `it.fails` to `it()` and update expected key count without removing existing keys. Add rows to `docs/coverage-map.yaml`: `manifest.audit` using the manifest live read client calls, and `application.envs:promote` using env read, bulk update, and create client calls. Run `npm run openapi:coverage` so `docs/COVERAGE.md` is generated from the map.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/system.test.ts -t capabilities -x && npm run openapi:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/capabilities.ts` contains `manifest_audit` and `envs_promote`.
    - `src/mcp/tools/system.test.ts` capability assertion is GREEN.
    - `docs/coverage-map.yaml` contains rows for `manifest.audit` and `application.envs:promote`.
    - `docs/COVERAGE.md` contains generated entries for `manifest.audit` and `application.envs:promote`.
    - Coverage notes do not claim a Coolify-native env promote endpoint.
  </acceptance_criteria>
  <done>Agents can discover Phase 29 capabilities via system.version and coverage docs.</done>
</task>

<task type="auto">
  <name>Task 2: Document Drift & Heal actions in README EN/DE</name>
  <files>README.md, README.de.md</files>
  <read_first>
    - .planning/phases/29-drift-heal/29-CONTEXT.md (D-01, D-02, D-03, D-05, D-06, D-09, D-10, D-11, D-15, D-16)
    - .planning/phases/29-drift-heal/29-RESEARCH.md (Summary, Anti-Patterns to Avoid)
    - README.md (current tool/action tables and safety section)
    - README.de.md (German parity section)
    - tests/integration/docs-parity.test.ts (literal parity constraints)
  </read_first>
  <action>
    Update README EN and DE in the same sections that list agent intelligence actions and safety behavior. Document `manifest.audit` on the existing manifest tool, preserving `manifest.diff` as raw structural diff. Document product wording `env.promote` as implemented action `application.envs:promote`. Include safety notes: audit is read-only/advisory, promote previews by default, apply needs `confirm:true`, conflict_policy defaults to `keep_remote`, values are masked by default, and calls stay within one instance. Mention remediation hints as structured follow-up tool/action guidance. Keep wording aligned between English and German. Do not add docs for a new drift/heal tool, cross-instance promote, auto-heal, or nonexistent Coolify promote endpoint.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/docs-parity.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - README.md and README.de.md both mention `manifest.audit`.
    - README.md and README.de.md both mention `application.envs:promote` or explain `env.promote` as that implementation action.
    - Both READMEs state audit read-only/advisory and promote preview/confirm/keep_remote safety.
    - Docs parity test passes.
  </acceptance_criteria>
  <done>Bilingual docs accurately expose Drift & Heal capabilities and safety defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 29 targeted verification</name>
  <files>src/mcp/tools/manifest.test.ts, src/mcp/tools/application.test.ts, src/mcp/tools/system.test.ts, tests/integration/docs-parity.test.ts</files>
  <read_first>
    - .planning/phases/29-drift-heal/29-VALIDATION.md (Sampling Rate and Per-Task Verification Map)
    - .planning/phases/29-drift-heal/29-01-SUMMARY.md (manifest.audit shipped details)
    - .planning/phases/29-drift-heal/29-02-SUMMARY.md (envs:promote shipped details)
    - src/mcp/tools/manifest.test.ts
    - src/mcp/tools/application.test.ts
    - src/mcp/tools/system.test.ts
  </read_first>
  <action>
    Run the targeted Phase 29 verification suite after docs/capabilities are in place. Fix only issues directly caused by Phase 29 edits. If a failure exposes an unrelated pre-existing problem, document it in the plan summary and do not broaden scope. Ensure no Wave 0 `it.fails` remains for Phase 29 behaviors after all implementation plans complete.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/manifest.test.ts src/mcp/tools/application.test.ts src/mcp/tools/system.test.ts tests/integration/docs-parity.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Targeted Phase 29 suite is green.
    - Phase 29 `it.fails` scaffolds for audit, promote, and capabilities are flipped to normal passing tests.
    - No new external package dependency was added.
  </acceptance_criteria>
  <done>Phase 29 is ready for `/gsd-verify-work` with targeted automated coverage green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| system.version → agent capability discovery | Agents decide available actions from capability metadata |
| coverage/docs → user/agent expectations | Public docs must not claim nonexistent endpoints or unsafe behavior |
| README examples → later tool calls | Example wording may influence confirm/reveal behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Information Disclosure | README/envs:promote docs | high | mitigate | Docs must state env values are masked by default and reveal follows existing policy |
| T-29-02 | Tampering | README/promote apply docs | high | mitigate | Docs must state preview default, `confirm:true`, and `keep_remote` default |
| T-29-03 | Spoofing | capability/docs instance wording | high | mitigate | Docs must state single-instance per call and explicit target_uuid; no cross-instance fan-out |
| T-29-04 | Elevation of Privilege | audit docs | high | mitigate | Docs and capability note must state audit is read-only/advisory and hints do not auto-mutate |
| T-29-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
`npx vitest run src/mcp/tools/manifest.test.ts src/mcp/tools/application.test.ts src/mcp/tools/system.test.ts tests/integration/docs-parity.test.ts -x` green and `npm run openapi:coverage` completes with updated docs/COVERAGE.md.
</verification>

<success_criteria>
- DRIFT-01/02/03 are discoverable through capabilities, coverage, and README EN/DE.
- Security language matches D-06, D-09, D-10, D-11, D-16.
- No docs or metadata advertise stubs, new top-level drift/heal tool, auto-heal, cross-instance promote, or nonexistent Coolify endpoints.
</success_criteria>

<output>
Create `.planning/phases/29-drift-heal/29-03-SUMMARY.md` when done.
</output>
