---
phase: 17-local-manifest-sync
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/utils/manifest.test.ts
  - src/mcp/tools/manifest.test.ts
  - src/utils/project-root.test.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Vitest scaffolds for manifest utility, manifest MCP tool, and project-root resolver exist and fail predictably before implementation"
    - "Husky pre-commit stays green because RED scaffolds use it.fails / dynamic-import patterns from Phase 15/16"
  artifacts:
    - src/utils/manifest.test.ts
    - src/mcp/tools/manifest.test.ts
    - src/utils/project-root.test.ts
  key_links:
    - "manifest.test.ts → src/utils/manifest.ts (dynamic import until 17-01 ships)"
    - "manifest tool test → src/mcp/tools/manifest.ts (dynamic import until 17-02 ships)"
  prohibitions:
    - statement: "No real .coolify/manifest.json is written outside a tmp sandbox"
      status: pending
      verification: backstop
    - statement: "No live Coolify API calls in Wave 0 scaffolds"
      status: pending
      verification: backstop
---

<objective>
Seed Wave 0 RED test scaffolds for the manifest utility, manifest MCP tool, and project-root resolver so that Plans 17-01 and 17-02 flip them GREEN without rewriting tests.

Purpose: Husky pre-commit blocks test-only RED commits — Phase 15/16 established the it.fails + dynamic-import pattern to keep pre-commit green while signalling unfinished implementations. Scaffolds encode the contract before code lands.
Output: Three failing-on-purpose test files that pin the public surface of ManifestManager, handleManifestAction, and resolveProjectRoot.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-local-manifest-sync/17-CONTEXT.md
@.planning/phases/17-local-manifest-sync/17-RESEARCH.md
@.planning/phases/17-local-manifest-sync/17-PATTERNS.md
@.planning/phases/17-local-manifest-sync/17-VALIDATION.md
@src/utils/instance-registry.ts
@src/utils/instance-registry.test.ts
@src/mcp/tools/instance.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold manifest.test.ts (utility) with it.fails RED cases</name>
  <files>src/utils/manifest.test.ts</files>
  <read_first>
    - src/utils/instance-registry.test.ts (vitest isolation + tmp sandbox pattern)
    - src/utils/errors.ts (CoolifyApiError, RECOVERY_HINTS)
    - .planning/phases/17-local-manifest-sync/17-PATTERNS.md (manifest.ts schema excerpt)
    - .planning/phases/17-local-manifest-sync/17-CONTEXT.md (D-05, D-06, D-07, D-10, D-11)
  </read_first>
  <behavior>
    - Test 1: ManifestManager.load() on missing file returns empty manifest with version "1.0.0", projects [], servers []
    - Test 2: ManifestManager.upsert() adds a resource under nested project/environment, creating project/environment when absent
    - Test 3: ManifestManager.remove(uuid) deletes a resource by UUID and leaves siblings intact
    - Test 4: First successful write creates .coolify/manifest.json and appends ".coolify/" to workspace .gitignore (MAN-02)
    - Test 5: Second write does NOT duplicate the .gitignore entry
    - Test 6: ManifestManager.hasUuid(uuid) returns true when UUID is present, false otherwise
    - Test 7: Atomic write leaves no .tmp files behind on success
    - Test 8: ManifestManager.autoUpsert() is callable with the documented resource shape (best-effort, never throws on disk errors)
  </behavior>
  <action>
    Create src/utils/manifest.test.ts using vitest. Use beforeEach/afterEach mkdtempSync/rmSync tmp workspace (mirror instance-registry.test.ts). Use dynamic import for src/utils/manifest.ts so the file does not need to exist yet: `const { ManifestManager } = await import('../../utils/manifest.js')` inside it blocks. Wrap every test in it.fails(...) so husky pre-commit sees green until 17-01 flips them to it. Assert exact field names from PATTERNS.md (manifestResourceSchema, manifestProjectSchema, manifestEnvironmentSchema, manifestServerSchema, manifestSchema). Use COOLIFY_MCP_TEST_WORKSPACE env var as the project-root override seam (mirror COOLIFY_MCP_TEST_REGISTRY_DIR from instance-registry). Do NOT import nonexistent modules at top-level — only dynamic imports. Do NOT call the live Coolify API.
  </action>
  <verify>
    <automated>pnpm test -- src/utils/manifest.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - File src/utils/manifest.test.ts exists
    - All 8 cases use it.fails and fail when run (proving the contract is encoded)
    - `pnpm test -- src/utils/manifest.test.ts --run` exits 0 because it.fails negates the failure
    - No top-level import of src/utils/manifest.ts (dynamic import only)
    - No real .coolify/ directory created outside tmp sandbox
  </acceptance_criteria>
  <done>8 it.fails cases committed; suite green via negation; manifest utility contract pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold manifest.test.ts (MCP tool) with it.fails RED cases</name>
  <files>src/mcp/tools/manifest.test.ts</files>
  <read_first>
    - src/mcp/tools/instance.test.ts (action-schema + handler dispatch test pattern)
    - src/mcp/tools/instance.ts (parseWithInstanceRouting, action discriminatedUnion)
    - .planning/phases/17-local-manifest-sync/17-CONTEXT.md (D-02, D-03, D-04, D-12, D-13, D-14, D-15)
    - .planning/phases/17-local-manifest-sync/17-PATTERNS.md (manifest tool action schemas)
  </read_first>
  <behavior>
    - Test 1: handleManifestAction({action:'get'}) returns buildReadResponse envelope with manifest data
    - Test 2: handleManifestAction({action:'upsert', resource:{...}}) upserts and returns the upserted entry
    - Test 3: handleManifestAction({action:'set', manifest:{...}}) replaces the whole manifest and validates via Zod
    - Test 4: handleManifestAction({action:'remove', uuid:'...'}) removes the entry and returns {removed:true}
    - Test 5: handleManifestAction({action:'clear', confirm:false}) throws COOLIFY_422; with confirm:true returns {cleared:true}
    - Test 6: handleManifestAction({action:'sync'}) without creds returns COOLIFY_NO_INSTANCE envelope (D-04)
    - Test 7: handleManifestAction({action:'sync', instance:'prod', dry_run:true}) returns planned diff without writing
    - Test 8: handleManifestAction({action:'sync', instance:'prod', confirm:true, prune:true}) prunes orphans and returns sync report
    - Test 9: handleManifestAction({action:'diff', instance:'prod'}) returns always non-destructive report
    - Test 10: 404 on a manifest-cached UUID surfaces recovery hints pointing to manifest.sync / manifest.diff (MAN-04)
  </behavior>
  <action>
    Create src/mcp/tools/manifest.test.ts using vitest. Use dynamic import for src/mcp/tools/manifest.js. Wrap every case in it.fails. Use the same tmp workspace seam (COOLIFY_MCP_TEST_WORKSPACE). For sync/diff tests, mock the Coolify client (mirror instance.test.ts mock pattern — do NOT hit live API). Assert action literals exactly: 'get','upsert','set','remove','sync','diff','clear'. Confirm that 'clear' and 'sync prune' require confirm:true (D-02, D-13). For MAN-04 test, simulate a 404 CoolifyApiError carrying a UUID that exists in the manifest and assert recoveryHints contains the manifest.sync / manifest.diff hint literal (D-15). No top-level imports of nonexistent modules.
  </action>
  <verify>
    <automated>pnpm test -- src/mcp/tools/manifest.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - File src/mcp/tools/manifest.test.ts exists
    - 10 it.fails cases covering all 7 actions plus MAN-04 404 hint
    - `pnpm test -- src/mcp/tools/manifest.test.ts --run` exits 0 (it.fails negation)
    - No live Coolify API calls — all client interactions mocked
  </acceptance_criteria>
  <done>10 it.fails cases committed; tool action contract pinned; MAN-03/MAN-04 testable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold project-root.test.ts with it.fails RED cases</name>
  <files>src/utils/project-root.test.ts</files>
  <read_first>
    - src/utils/instance-registry.test.ts (tmp sandbox pattern)
    - .planning/research/PITFALLS.md (project-root resolution pitfall)
    - .planning/phases/17-local-manifest-sync/17-RESEARCH.md (Pattern 1: Project-Root Resolution)
  </read_first>
  <behavior>
    - Test 1: resolveProjectRoot walks up from nested subdir and returns the dir containing .git
    - Test 2: resolveProjectRoot walks up to a dir containing package.json when no .git exists
    - Test 3: resolveProjectRoot returns an existing .coolify/ parent when neither .git nor package.json found above
    - Test 4: resolveProjectRoot falls back to startDir when no marker found
    - Test 5: COOLIFY_MCP_TEST_WORKSPACE env var overrides the start dir (test seam)
  </behavior>
  <action>
    Create src/utils/project-root.test.ts using vitest. Use mkdtempSync to build a fake workspace tree with nested subdirectories and create .git / package.json / .coolify markers per test. Dynamic import src/utils/project-root.js. Wrap every case in it.fails. No top-level imports of nonexistent modules. Tests must not touch the real repository root.
  </action>
  <verify>
    <automated>pnpm test -- src/utils/project-root.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - File src/utils/project-root.test.ts exists
    - 5 it.fails cases committed
    - `pnpm test -- src/utils/project-root.test.ts --run` exits 0 (it.fails negation)
    - No real repository paths mutated
  </acceptance_criteria>
  <done>5 it.fails cases committed; project-root resolver contract pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test sandbox → host fs | RED scaffolds must not escape the tmp workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-00-01 | Tampering | tmp workspace cleanup | low | mitigate | Use mkdtempSync + rmSync recursive in afterEach; never write to process.cwd() directly |
| T-17-00-02 | Information Disclosure | test fixtures with real UUIDs | low | accept | Use deterministic fake UUIDs (e.g., 00000000-0000-4000-8000-000000000001) — no production data |
| T-17-00-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed in Wave 0 — zod/ofetch already verified by 17-RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
- `pnpm test --run` exits 0 across the full suite (it.fails negation keeps husky green)
- All three scaffold files importable without TypeScript errors at the dynamic-import call sites
- No real .coolify/ or .gitignore modifications outside tmp sandbox
</verification>

<success_criteria>
- 3 RED scaffold files committed (manifest.test.ts utility, manifest.test.ts tool, project-root.test.ts)
- 23 total it.fails cases (8 + 10 + 5)
- Husky pre-commit green
- Public surface contract pinned for Plans 17-01 and 17-02
</success_criteria>

<output>
Create `.planning/phases/17-local-manifest-sync/17-00-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/utils/manifest.test.ts` — RED test scaffold (Wave 0)
- `src/mcp/tools/manifest.test.ts` — RED test scaffold (Wave 0)
- `src/utils/project-root.test.ts` — RED test scaffold (Wave 0)
