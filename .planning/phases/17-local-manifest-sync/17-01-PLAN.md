---
phase: 17-local-manifest-sync
plan: 01
type: execute
wave: 1
depends_on:
  - 17-00
files_modified:
  - src/utils/manifest.ts
  - src/utils/project-root.ts
  - .coolify-manifest.example.json
autonomous: true
requirements:
  - MAN-01
  - MAN-02
user_setup: []

must_haves:
  truths:
    - "ManifestManager reads and writes .coolify/manifest.json storing project, environment, server, and resource UUIDs plus domains (MAN-01)"
    - "First successful manifest write auto-appends .coolify/ to the workspace .gitignore if not already present (MAN-02)"
    - "Manifest is treated as cache — no tokens or secrets stored in the file"
    - "Manifest writes are atomic (temp file + rename) — interrupted or parallel writes do not corrupt the manifest (MAN-03 concurrency probe)"
    - "Project root is resolved by walking up from process.cwd() to find .git / package.json / .coolify — never relative to raw cwd"
    - "Manifest path is confined to the resolved workspace root — no path escape outside the project"
  artifacts:
    - src/utils/manifest.ts
    - src/utils/project-root.ts
    - .coolify-manifest.example.json
  key_links:
    - "manifest.ts → project-root.ts (resolveProjectRoot for .coolify/manifest.json path)"
    - "manifest.ts → errors.ts (CoolifyApiError, RECOVERY_HINTS)"
    - "manifest.ts → zod (schema validation)"
  prohibitions:
    - statement: "No API tokens, bearer tokens, or secret values stored in .coolify/manifest.json"
      status: pending
      verification: backstop
    - statement: "No treating the manifest as source of truth — sync/diff reconciliation lives in the MCP tool layer, not the utility"
      status: pending
      verification: backstop
    - statement: "No path escape outside the resolved workspace project root"
      status: pending
      verification: backstop
  assumptions:
    - statement: "ManifestManager.withWriteLock serializes full load-modify-save — concurrent upsert/remove calls do not interleave and lose updates (MAN-03 concurrency probe)"
      verification: backstop
    - statement: "Atomic write (temp file + rename) guarantees no partial manifest is ever observable on disk — interrupted writes leave the previous file intact (MAN-03 concurrency probe)"
      verification: backstop
---

<objective>
Build the ManifestManager utility and project-root resolver that underpin the manifest cache. Covers MAN-01 (read/write manifest) and MAN-02 (auto-append .gitignore on first write).

Purpose: The manifest is a local cache/index — never a source of truth. This plan delivers the file I/O layer with atomic writes, project-root resolution, gitignore safety, and the hooks API that the MCP tool (17-02) and mutation handlers (17-03) will call.
Output: src/utils/manifest.ts, src/utils/project-root.ts, committed example template .coolify-manifest.example.json. Flips Wave 0 it.fails scaffolds (manifest.test.ts, project-root.test.ts) to GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-local-manifest-sync/17-CONTEXT.md
@.planning/phases/17-local-manifest-sync/17-RESEARCH.md
@.planning/phases/17-local-manifest-sync/17-PATTERNS.md
@.planning/phases/17-local-manifest-sync/17-00-PLAN.md
@src/utils/instance-registry.ts
@src/utils/errors.ts
@src/utils/manifest.test.ts
@src/utils/project-root.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement resolveProjectRoot + ManifestManager core (load, save, schema, atomic write, gitignore)</name>
  <files>src/utils/project-root.ts, src/utils/manifest.ts</files>
  <read_first>
    - src/utils/instance-registry.ts (atomic write, withWriteLock, Zod schema, chmod patterns)
    - src/utils/errors.ts (CoolifyApiError, RECOVERY_HINTS)
    - src/utils/manifest.test.ts (contract — flip it.fails to it)
    - src/utils/project-root.test.ts (contract — flip it.fails to it)
    - .planning/phases/17-local-manifest-sync/17-PATTERNS.md (schema + atomic write excerpts)
    - .planning/phases/17-local-manifest-sync/17-RESEARCH.md (Pattern 1, 2, 3)
    - .planning/phases/17-local-manifest-sync/17-CONTEXT.md (D-05, D-06, D-07, D-10)
  </read_first>
  <behavior>
    - resolveProjectRoot walks up from startDir (default process.cwd() or COOLIFY_MCP_TEST_WORKSPACE) checking for .git, package.json, .coolify in that order; returns startDir fallback
    - ManifestManager.load() returns parsed+validated manifest or empty manifest ({version:'1.0.0', updatedAt: new Date().toISOString(), projects:[], servers:[]}) when file missing
    - ManifestManager.save(manifest) writes atomically (temp file + rename) and on first write calls ensureGitignore(projectRoot)
    - ensureGitignore appends ".coolify/" exactly once — idempotent across repeated writes
    - manifestSchema rejects unknown keys via .strict() on every nested object
    - manifestResourceSchema.type enum includes 'application','service','database' (extendable)
    - ManifestManager.hasUuid(uuid) returns true if any project/environment/resource/server UUID matches
    - ManifestManager.withWriteLock serializes concurrent saves — no interleaving
  </behavior>
  <action>
    Create src/utils/project-root.ts exporting resolveProjectRoot(startDir?: string): string. Use existsSync + dirname loop from RESEARCH Pattern 1. Honor COOLIFY_MCP_TEST_WORKSPACE override (when set, return it directly — test seam). Create src/utils/manifest.ts exporting manifestResourceSchema, manifestEnvironmentSchema, manifestProjectSchema, manifestServerSchema, manifestSchema, type Manifest, and ManifestManager class. Mirror instance-registry.ts: private static writeLock + withWriteLock, private executeSave using tmp file `${path}.tmp.${process.pid}.${Date.now()}` + renameSync + unlinkSync cleanup on error. Manifest path = join(resolveProjectRoot(), '.coolify', 'manifest.json'). Implement load(), save(manifest), upsert({resource, projectUuid?, projectName?, environmentUuid?, environmentName?}), remove(uuid), hasUuid(uuid), clear(). upsert nests resource under project/environment — create project/environment when absent, match by UUID when present. Do NOT store tokens. Do NOT add sync/diff logic here (lives in 17-02 tool layer). Do NOT call the Coolify API. Flip it.fails → it in manifest.test.ts and project-root.test.ts where the contract is now satisfied.
  </action>
  <verify>
    <automated>pnpm test -- src/utils/manifest.test.ts src/utils/project-root.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/project-root.ts and src/utils/manifest.ts exist
    - manifest.test.ts and project-root.test.ts flip from it.fails to it and pass
    - `pnpm test -- src/utils/manifest.test.ts src/utils/project-root.test.ts --run` exits 0
    - manifestSchema is .strict() on all nested objects (unknown keys rejected)
    - No token/secret fields in any manifest schema
    - Atomic write leaves no .tmp files on success (assert via tmp sandbox test)
    - ensureGitignore is idempotent — second call does not duplicate ".coolify/"
  </acceptance_criteria>
  <done>ManifestManager + resolveProjectRoot shipped; MAN-01 + MAN-02 utility contract GREEN; no tokens in schema.</done>
</task>

<task type="auto">
  <name>Task 2: Add autoUpsert/autoRemove hooks API + committed example template</name>
  <files>src/utils/manifest.ts, .coolify-manifest.example.json</files>
  <read_first>
    - src/utils/manifest.ts (Task 1 output)
    - .planning/phases/17-local-manifest-sync/17-CONTEXT.md (D-09, D-11, D-08)
    - .planning/phases/17-local-manifest-sync/17-PATTERNS.md (Auto-Upsert Hook pattern)
    - src/mcp/tools/application.ts (mutation success sites — read only, do NOT modify in this plan)
  </read_first>
  <action>
    Add to ManifestManager: static async autoUpsert(entry: { uuid, type, name, domains?, projectUuid?, projectName?, environmentUuid?, environmentName? }): Promise<void> and static async autoRemove(uuid: string): Promise<void>. Both wrap upsert/remove in try/catch and NEVER throw — disk/permission failures are swallowed silently (D-11 best-effort). The caller (mutation handler in 17-03) is responsible for surfacing _meta.manifestWarning. Add a committed example template at repo root .coolify-manifest.example.json showing one project with one environment containing one application resource, one server, and the optional instance slug field — no secrets, no real UUIDs (use 00000000-0000-4000-8000-000000000001 style placeholders). Document in a top-of-file comment that the live .coolify/manifest.json is gitignored and this example is the only manifest file safe to commit.
  </action>
  <verify>
    <automated>pnpm test -- src/utils/manifest.test.ts --run && node -e "JSON.parse(require('fs').readFileSync('.coolify-manifest.example.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - ManifestManager.autoUpsert and autoRemove exported and never throw on disk errors
    - .coolify-manifest.example.json exists at repo root, parses as valid JSON, contains no token/secret fields
    - Example file documents that live manifest is gitignored
    - manifest.test.ts autoUpsert case passes
  </acceptance_criteria>
  <done>Hooks API ready for 17-03 wiring; example template committed; D-08/D-09/D-11 satisfied at utility layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace fs → manifest file | Local file I/O; project-root resolver must confine writes to workspace |
| manifest cache → git | .gitignore must prevent committing environment-specific UUIDs/domains |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01-01 | Tampering / Path Traversal | resolveProjectRoot + manifest path | high | mitigate | Walk up to nearest .git/.package.json/.coolify marker; refuse to write outside resolved root; COOLIFY_MCP_TEST_WORKSPACE override only honored in test env |
| T-17-01-02 | Information Disclosure | manifest storing tokens | high | mitigate | Schema has no token/secret fields; .strict() rejects unknown keys; autoUpsert input shape excludes credentials |
| T-17-01-03 | Tampering | concurrent manifest writes | medium | mitigate | withWriteLock serializes full load-modify-save; atomic temp+rename prevents partial writes |
| T-17-01-04 | Information Disclosure | .coolify/ committed to git | high | mitigate | ensureGitignore appends ".coolify/" on first write; idempotent; committed example uses .coolify-manifest.example.json (outside gitignored dir) |
| T-17-01-SC | Tampering | npm installs | high | accept | No new packages — zod/ofetch already approved in 17-RESEARCH.md Package Legitimacy Audit |

</threat_model>

<verification>
- `pnpm test -- src/utils/manifest.test.ts src/utils/project-root.test.ts --run` exits 0
- `pnpm test --run` full suite still green (no regressions)
- .coolify-manifest.example.json parses as valid JSON
- No token/secret fields in any exported schema
</verification>

<success_criteria>
- MAN-01 covered: ManifestManager load/save/upsert/remove/hasUuid/clear operational
- MAN-02 covered: ensureGitignore auto-appends .coolify/ on first write, idempotent
- MAN-03 concurrency assumption documented: withWriteLock + atomic write
- MAN-04 utility hook (hasUuid) ready for 17-02 404 hint integration
- D-05, D-06, D-07, D-08, D-10, D-11 satisfied at utility layer
</success_criteria>

<output>
Create `.planning/phases/17-local-manifest-sync/17-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/utils/manifest.ts` — ManifestManager (load, save, upsert, remove, hasUuid, clear, autoUpsert, autoRemove), Zod schemas (manifestResourceSchema, manifestEnvironmentSchema, manifestProjectSchema, manifestServerSchema, manifestSchema), type Manifest
- `src/utils/project-root.ts` — resolveProjectRoot(startDir?: string)
- `.coolify-manifest.example.json` — committed example template (no secrets)
