---
phase: 01-foundation-multi-instance-auth
plan: 05
type: execute
wave: 5
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - .cursor/mcp.json
  - docs/mcp.example.json
  - package.json
  - README.md
autonomous: false
requirements:
  - DIST-03
user_setup:
  - service: cursor
    why: Manual MCP stdio handshake verification
    dashboard_config:
      - task: Reload MCP servers after adding config
        location: Cursor → Settings → MCP → Reload
  - service: coolify
    why: Live health round-trip during handshake test
    env_vars:
      - name: COOLIFY_URL
        source: Coolify instance URL
      - name: COOLIFY_TOKEN
        source: Coolify UI → Keys & Tokens
must_haves:
  truths:
    - npm run build produces runnable dist/index.js with shebang or node invocation
    - .cursor/mcp.json example uses command node args absolute path to dist/index.js with env COOLIFY_URL COOLIFY_TOKEN
    - MCP stdio handshake completes in Cursor when config applied
    - Invoking system action health returns connected true in live client
  prohibitions:
    - mcp.json example must not contain real production tokens — use placeholder YOUR_COOLIFY_API_TOKEN
  artifacts:
    - .cursor/mcp.json
    - docs/mcp.example.json
    - dist/index.js
  key_links:
    - mcp.json command node args points to dist/index.js built by npm run build
    - env block passes COOLIFY_URL and COOLIFY_TOKEN to server process per D-02
---

## Phase Goal

**As an** AI agent operator, **I want** MCP stdio working in Cursor, **so that** I can invoke Coolify tools from my IDE.

> **v2-deferred (D-04/D-05/D-06/D-07):** CTX-04, CTX-05, CTX-06, CTX-07 not in scope. Multi-instance mcp.json pattern deferred v2.

<objective>
Prove DIST-03: local MCP config → built entry → stdio handshake → live system health round-trip in Cursor (Claude Desktop optional).

Purpose: Close Phase 1 with demonstrable agent-ready MCP server.
Output: mcp.json example, production build, human-verified handshake.
</objective>

<execution_context>
@/Users/puzzless/gstack/.cursor/gsd-core/workflows/execute-plan.md
@/Users/puzzless/gstack/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md
@.planning/phases/01-foundation-multi-instance-auth/01-RESEARCH.md
@README.md
@.planning/phases/01-foundation-multi-instance-auth/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build and entrypoint hardening</name>
  <files>package.json, src/index.ts</files>
  <read_first>
    - package.json
    - src/index.ts
    - tsup.config.ts
    - .planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md (D-16)
  </read_first>
  <acceptance_criteria>
    - npm run build exits 0
    - test -f dist/index.js
    - dist/index.js is valid ESM executable via node dist/index.js
    - package.json files field includes dist if added
    - COOLIFY_URL=x COOLIFY_TOKEN=y timeout 2 node dist/index.js does not print token to stderr
  </acceptance_criteria>
  <action>
    Run production build via tsup. Verify dist/index.js starts MCP server on stdio without writing to stdout except MCP protocol — all diagnostics to stderr per D-18. Add package.json prepublishOnly or prepare script running build if missing. Ensure bin coolify-mcp points to dist/index.js for future DIST-01. No npm publish in this plan per D-16.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f dist/index.js &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Production dist/index.js builds and starts without leaking env secrets to stderr.</done>
</task>

<task type="auto">
  <name>Task 2: Local MCP client config examples</name>
  <files>.cursor/mcp.json, docs/mcp.example.json</files>
  <read_first>
    - README.md (Local development mcp.json section lines 206-216)
    - .planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md (D-02)
  </read_first>
  <acceptance_criteria>
    - docs/mcp.example.json contains mcpServers.coolify with command node args array including dist/index.js path placeholder
    - docs/mcp.example.json env block includes COOLIFY_URL and COOLIFY_TOKEN placeholder values
    - .cursor/mcp.json follows same structure with relative or documented absolute path pattern
    - Example uses placeholder YOUR_COOLIFY_API_TOKEN not real secret
    - JSON files parse valid via node -e "JSON.parse(...)"
  </acceptance_criteria>
  <action>
    Create docs/mcp.example.json as copy-paste template per README Local development section — command node, args pointing to dist/index.js, env with COOLIFY_URL COOLIFY_TOKEN COOLIFY_MCP_LOG optional. Create or update .cursor/mcp.json for project-local dev with same structure. Document that instances.json is v2-deferred per D-04. Use placeholder token string not real credentials.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('docs/mcp.example.json','utf8')); console.log('ok')"</automated>
  </verify>
  <done>MCP config examples exist and parse; ready for Cursor reload.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live MCP stdio handshake in Cursor</name>
  <files>.cursor/mcp.json</files>
  <read_first>
    - .cursor/mcp.json
    - .planning/phases/01-foundation-multi-instance-auth/01-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <acceptance_criteria>
    - Cursor MCP panel shows coolify server connected without error
    - Invoking tool system with input action health returns response containing connected true
    - Response does not contain user's COOLIFY_TOKEN value
    - Claude Desktop handshake optional — Cursor success satisfies DIST-03 minimum
  </acceptance_criteria>
  <what-built>Full P1 MCP server with system and meta tools, local mcp.json config, production build</what-built>
  <how-to-verify>
    1. Set real COOLIFY_URL and COOLIFY_TOKEN in .cursor/mcp.json env block
    2. Run npm run build
    3. Reload Cursor MCP servers (Settings → MCP → Reload)
    4. Confirm coolify server shows connected
    5. Invoke system tool with { "action": "health" }
    6. Verify response JSON has connected: true and host field
    7. Confirm response text does not contain your API token
    8. Optionally invoke meta { "action": "version" } and confirm mcpVersion returned
  </how-to-verify>
  <resume-signal>Type "approved" or describe handshake issues</resume-signal>
  <action>
    All automation complete before this checkpoint. Human performs live handshake verification per 01-VALIDATION.md Manual-Only table. Executor waits for approved signal before marking plan complete.
  </action>
  <verify>
    <automated>MISSING — human checkpoint; DIST-03 requires live MCP client</automated>
    <human-check>Cursor MCP handshake + system health invoke per steps above</human-check>
  </verify>
  <done>Human confirms DIST-03: MCP stdio works in Cursor with real Coolify round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcp.json → process env | Tokens stored in IDE config file on disk |
| MCP client → server | Live tool invocation over stdio |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Information Disclosure | mcp.json | high | mitigate | Example uses placeholder; document gitignore for local config with real tokens |
| T-01-03 | Information Disclosure | live response | high | mitigate | D-21 host-only responses; human verifies no token in step 7 |
| T-01-05 | Information Disclosure | TLS | medium | mitigate | Document COOLIFY_VERIFY_SSL=false for homelab self-signed |

</threat_model>

## Artifacts this phase produces

| Symbol | Location | Description |
|--------|----------|-------------|
| MCP config example | docs/mcp.example.json | Copy-paste Cursor/Claude Desktop template |
| Project MCP config | .cursor/mcp.json | Local dev server entry |
| Production bundle | dist/index.js | Built MCP server entrypoint |

<verification>
- npm run build && npx vitest run exits 0
- Human checkpoint approved for Cursor handshake
</verification>

<success_criteria>
- DIST-03 satisfied via local mcp.json + node dist/index.js + live health invoke
- Phase 1 foundation complete and ready for Phase 2 discovery tools
</success_criteria>

<output>
Create `.planning/phases/01-foundation-multi-instance-auth/01-05-SUMMARY.md` when done
</output>
