---
phase: 01-foundation-multi-instance-auth
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/mcp/tools/system.ts
  - src/mcp/tools/system.test.ts
  - src/mcp/tools/meta.ts
  - src/mcp/tools/meta.test.ts
  - src/mcp/server.ts
  - src/mcp/server.test.ts
  - src/api/client.ts
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - CTX-03
  - DX-01
  - DX-02
must_haves:
  truths:
    - system tool accepts action health version verify via discriminatedUnion per D-15 D-17
    - system action version returns Coolify API version string from GET /version
    - system action verify returns connected true host and coolifyVersion after auth check
    - meta tool accepts action version and returns mcpVersion matching package.json version
    - Only system and meta tools registered per D-14
    - Invalid action input rejected by Zod before handler dispatch
  prohibitions:
    - Tool responses must never include COOLIFY_TOKEN or raw Bearer value
    - meta version response must not expose env vars
  artifacts:
    - src/mcp/tools/system.ts
    - src/mcp/tools/meta.ts
    - src/mcp/server.ts
    - systemActionSchema
    - metaActionSchema
  key_links:
    - src/mcp/server.ts registers system and meta tools with Zod inputSchema
    - system version action calls createCoolifyClient get /version endpoint
    - meta version reads version from package.json at build time or import
---

## Phase Goal

**As an** AI agent operator, **I want** system and meta version tools with Zod validation, **so that** I can verify Coolify and MCP versions before ops.

> **v2-deferred (D-04/D-05/D-06/D-07):** CTX-04, CTX-05, CTX-06, CTX-07 — multi-instance and per-request override not in P1.

<objective>
Complete P1 tool surface: system health/version/verify + meta version with full Zod discriminatedUnion schemas (DX-02) and action-based registration (DX-01).

Purpose: Satisfy CTX-01 through CTX-03 for single-instance foundation.
Output: Full systemActionSchema, metaActionSchema, registered tools with tests.
</objective>

<execution_context>
@/Users/puzzless/gstack/.cursor/gsd-core/workflows/execute-plan.md
@/Users/puzzless/gstack/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md
@.planning/phases/01-foundation-multi-instance-auth/01-RESEARCH.md
@.planning/phases/01-foundation-multi-instance-auth/01-PATTERNS.md
@.planning/phases/01-foundation-multi-instance-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-multi-instance-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-multi-instance-auth/01-03-SUMMARY.md
@.cursor/skills/spike-findings-awesome-coolify/references/mcp-sdk-patterns.md
@.cursor/skills/spike-findings-awesome-coolify/references/coolify-api.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend system tool with version and verify actions</name>
  <files>src/mcp/tools/system.ts, src/mcp/tools/system.test.ts, src/api/client.ts</files>
  <behavior>
    - systemActionSchema discriminatedUnion includes action literals health version verify
    - action version returns object with version string from mocked GET /version response
    - action verify returns connected true host coolifyVersion without token field
    - action health unchanged from 01-01 behavior
    - Invalid action foo fails Zod parse before handler
  </behavior>
  <read_first>
    - src/mcp/tools/system.ts (modify)
    - src/api/client.ts (modify)
    - .planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md (D-15, D-17, D-21, D-23)
    - .cursor/skills/spike-findings-awesome-coolify/references/coolify-api.md (health and version endpoints)
  </read_first>
  <acceptance_criteria>
    - systemActionSchema is z.discriminatedUnion action with branches health version verify per D-15
    - createCoolifyClient exports fetchVersion calling GET /version on api/v1 base
    - handleSystemAction dispatches version returning coolify version field from API
    - handleSystemAction dispatches verify combining health or version check with connected true and host only per D-21
    - npx vitest run src/mcp/tools/system.test.ts exits 0
    - CTX-02 satisfied by version action test
  </acceptance_criteria>
  <action>
    Extend systemActionSchema with version and verify branches per D-17 discriminatedUnion. Add fetchVersion to src/api/client.ts for GET /version requiring Bearer per D-23. Implement verify action as authenticated connection check returning connected host coolifyVersion — maps to CTX-01 verify semantics for single instance. Import logger from src/utils/logger.ts for debug path+status logging. Update system.test.ts with mocked responses for all three actions.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/system.test.ts</automated>
  </verify>
  <done>System tool supports health version verify; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Meta tool for MCP server version</name>
  <files>src/mcp/tools/meta.ts, src/mcp/tools/meta.test.ts</files>
  <behavior>
    - metaActionSchema discriminatedUnion with single branch action version
    - handleMetaAction version returns mcpVersion string 0.1.0 matching package.json
    - Response contains no env or token fields
  </behavior>
  <read_first>
    - src/mcp/tools/meta.ts (create)
    - package.json
    - .planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md (D-14, D-15)
    - .planning/phases/01-foundation-multi-instance-auth/01-RESEARCH.md (meta tool recommendation)
  </read_first>
  <acceptance_criteria>
    - src/mcp/tools/meta.ts exports metaActionSchema and handleMetaAction
    - meta action version returns object with mcpVersion field from package.json version 0.1.0
    - npx vitest run src/mcp/tools/meta.test.ts exits 0
    - CTX-03 satisfied by meta version test
  </acceptance_criteria>
  <action>
    Create src/mcp/tools/meta.ts as separate domain tool per RESEARCH.md discretion — metaActionSchema with action version literal only. handleMetaAction reads version from package.json via import assert or readFileSync at module init. Return mcpVersion and serverName coolify-mcp. No Coolify API call. Write meta.test.ts asserting mcpVersion value and absence of token keys in response JSON.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/meta.test.ts</automated>
  </verify>
  <done>Meta tool returns MCP server version; CTX-03 covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register system and meta tools with outputSchema</name>
  <files>src/mcp/server.ts, src/mcp/server.test.ts</files>
  <behavior>
    - createAndConnectServer registers exactly two tools system and meta per D-14
    - system tool inputSchema is systemActionSchema discriminatedUnion
    - meta tool inputSchema is metaActionSchema discriminatedUnion
    - Tools use outputSchema with ok boolean and data optional per spike pattern
    - Zod rejects { action: 'invalid' } at registration validation layer
  </behavior>
  <read_first>
    - src/mcp/server.ts (modify)
    - src/mcp/tools/system.ts
    - src/mcp/tools/meta.ts
    - .cursor/skills/spike-findings-awesome-coolify/references/mcp-sdk-patterns.md (registerTool pattern lines 28-60)
  </read_first>
  <acceptance_criteria>
    - src/mcp/server.ts registers tool names system and meta only per D-14 DX-01
    - Both tools use Zod discriminatedUnion inputSchema per DX-02
    - Both tools include outputSchema with ok z.boolean data z.unknown optional per spike
    - Successful responses include structuredContent ok true per D-12
    - npx vitest run src/mcp/server.test.ts exits 0
    - grep -c "registerTool" src/mcp/server.ts returns 2
  </acceptance_criteria>
  <action>
    Update createAndConnectServer in src/mcp/server.ts to register meta tool alongside system. Wire inputSchema from exported schemas. Add outputSchema and structuredContent on success and error paths per spike two-layer pattern. Write server.test.ts asserting registered tool count is 2 and schema rejects unknown action via Zod safeParse. Ensure no instance application or other domain tool stubs per D-14.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/server.test.ts &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Server registers system+meta only; full test suite green; DX-01 DX-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool args → Zod | Agent-supplied action parameter |
| Tool response → agent | Version strings and host only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | Zod inputSchema | high | mitigate | discriminatedUnion rejects unknown actions per DX-02 |
| T-01-02 | Information Disclosure | verify response | high | mitigate | Return host only never token per D-21 |
| T-01-03 | Information Disclosure | version errors | high | mitigate | wrapMcpError with redactSecrets from 01-03 |

</threat_model>

## Artifacts this phase produces

| Symbol | Location | Description |
|--------|----------|-------------|
| `systemActionSchema` | src/mcp/tools/system.ts | Zod union health\|version\|verify |
| `metaActionSchema` | src/mcp/tools/meta.ts | Zod union version |
| `handleSystemAction` | src/mcp/tools/system.ts | Full system dispatch |
| `handleMetaAction` | src/mcp/tools/meta.ts | Meta dispatch |
| `fetchVersion` | src/api/client.ts | Authenticated version endpoint |
| `createAndConnectServer` | src/mcp/server.ts | Registers system + meta only |

<verification>
- npx vitest run exits 0
- Only 2 tools registered in server.ts
- All system actions have test coverage
</verification>

<success_criteria>
- CTX-01, CTX-02, CTX-03, DX-01, DX-02 satisfied for P1 single-instance scope
</success_criteria>

<output>
Create `.planning/phases/01-foundation-multi-instance-auth/01-04-SUMMARY.md` when done
</output>
