---
phase: 01-foundation-multi-instance-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/utils/errors.ts
  - src/utils/errors.test.ts
  - src/api/client.ts
  - src/api/client.test.ts
  - src/mcp/tools/system.ts
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
must_haves:
  truths:
    - mapApiError maps HTTP 401 to code COOLIFY_401 with recoveryHints array
    - mapApiError maps HTTP 404 to COOLIFY_404, 422 to COOLIFY_422, 500 to COOLIFY_500
    - mapApiError maps network failures to COOLIFY_NETWORK and timeouts to COOLIFY_TIMEOUT
    - createCoolifyClient retries 429 and 5xx up to 3 times with delays 1s 2s 4s
    - Tool failures return isError true with JSON envelope in content text per D-12
  prohibitions:
    - Error envelope message and recoveryHints must not contain raw Bearer token value
  artifacts:
    - src/utils/errors.ts
    - src/api/client.ts
  key_links:
    - createCoolifyClient onResponseError calls mapApiError from src/utils/errors.ts
    - handleSystemAction catch block uses wrapMcpError for MCP isError response
---

## Phase Goal

**As an** AI agent operator, **I want** structured API errors with retry, **so that** transient failures recover without manual intervention.

> **v2-deferred (D-04/D-05/D-06/D-07):** CTX-04, CTX-05, CTX-06, CTX-07 not in scope.

<objective>
Implement structured error envelope (ERR-01, ERR-02) and transient retry with exponential backoff (ERR-03) in CoolifyClient; wire two-layer MCP error responses per D-08–D-12.

Purpose: Every Phase 2+ tool inherits consistent error recovery.
Output: mapApiError, wrapMcpError, retry-enabled createCoolifyClient.
</objective>

<execution_context>
@/Users/puzzless/gstack/.cursor/gsd-core/workflows/execute-plan.md
@/Users/puzzless/gstack/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md
@.planning/phases/01-foundation-multi-instance-auth/01-RESEARCH.md
@.planning/phases/01-foundation-multi-instance-auth/01-PATTERNS.md
@.planning/phases/01-foundation-multi-instance-auth/01-01-SUMMARY.md
@README.md
@.cursor/skills/spike-findings-awesome-coolify/references/coolify-api.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Structured error envelope and mapping tests</name>
  <files>src/utils/errors.ts, src/utils/errors.test.ts</files>
  <behavior>
    - mapApiError with status 401 returns code COOLIFY_401, httpStatus 401, recoveryHints array length >= 1
    - mapApiError with status 404 returns code COOLIFY_404
    - mapApiError with status 422 returns code COOLIFY_422
    - mapApiError with status 500 returns code COOLIFY_500
    - mapApiError with FetchError network code ECONNREFUSED returns code COOLIFY_NETWORK
    - mapApiError with AbortError returns code COOLIFY_TIMEOUT
    - wrapMcpError returns object with isError true and content[0].text parseable as JSON containing code field
    - toStructuredError never includes substring from Authorization header in output
  </behavior>
  <read_first>
    - src/utils/errors.ts (create)
    - .planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md (D-08, D-09, D-10, D-12)
    - .planning/phases/01-foundation-multi-instance-auth/01-PATTERNS.md (errors.ts pattern)
    - README.md (structured error example section)
  </read_first>
  <acceptance_criteria>
    - src/utils/errors.ts exports CoolifyErrorEnvelope interface with fields code message recoveryHints httpStatus optional
    - src/utils/errors.ts exports mapApiError accepting response status or Error and returning CoolifyErrorEnvelope
    - mapApiError codes include exactly COOLIFY_401 COOLIFY_404 COOLIFY_422 COOLIFY_500 COOLIFY_NETWORK COOLIFY_TIMEOUT per D-09
    - recoveryHints entries are English actionable strings per D-10 example mentioning Keys & Tokens for 401
    - src/utils/errors.ts exports wrapMcpError and toStructuredError per 01-PATTERNS.md shared pattern
    - npx vitest run src/utils/errors.test.ts exits 0
  </acceptance_criteria>
  <action>
    Create src/utils/errors.ts with CoolifyErrorEnvelope type and mapApiError mapping HTTP status and ofetch/Fetch error types to D-09 codes. Include English recoveryHints per D-10 — 401 hint references Coolify UI Keys & Tokens. Implement wrapMcpError setting isError true and JSON envelope in content text per D-12. Write src/utils/errors.test.ts first covering all status codes and network/timeout paths. Strip any Authorization values from error messages before return.
  </action>
  <verify>
    <automated>npx vitest run src/utils/errors.test.ts</automated>
  </verify>
  <done>All error mapping tests green; envelope shape matches D-08 fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retry-enabled client with error integration</name>
  <files>src/api/client.ts, src/api/client.test.ts</files>
  <behavior>
    - createCoolifyClient configured with retry 3 and retryStatusCodes including 429 500 502 503 504
    - Mock 503 then 200: client makes 2 requests before success
    - Mock 503 three times: client throws after 3 retry attempts
    - retryDelay sequence produces 1000ms 2000ms 4000ms cumulative spacing per D-11
    - COOLIFY_VERIFY_SSL false configures https Agent rejectUnauthorized false
  </behavior>
  <read_first>
    - src/api/client.ts (modify)
    - src/utils/errors.ts
    - .planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md (D-11, D-22)
    - .planning/phases/01-foundation-multi-instance-auth/01-RESEARCH.md (client pattern lines 232-260)
    - .planning/phases/01-foundation-multi-instance-auth/01-PATTERNS.md (client.ts pattern)
  </read_first>
  <acceptance_criteria>
    - src/api/client.ts createCoolifyClient sets retry 3 retryDelay 1000 retryStatusCodes 429 500 502 503 504 per D-11
    - onResponseError hook throws or returns error mapped via mapApiError
    - onRequestError hook handles network errors as COOLIFY_NETWORK
    - npx vitest run src/api/client.test.ts exits 0
    - grep -c 'retry: 3' src/api/client.ts returns >= 1
  </acceptance_criteria>
  <action>
    Extend createCoolifyClient from 01-01 with ofetch retry config per D-11 — max 3 attempts, exponential backoff 1s 2s 4s, retry on 429 5xx and network errors. Wire onResponseError and onRequestError to mapApiError. Add COOLIFY_VERIFY_SSL agent handling per RESEARCH.md Pitfall 2. Write client.test.ts with vi.fn mock fetch or ofetch mock verifying retry count and SSL agent flag.
  </action>
  <verify>
    <automated>npx vitest run src/api/client.test.ts</automated>
  </verify>
  <done>Client retries transient failures; tests verify backoff and max attempts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire two-layer errors into system tool handler</name>
  <files>src/mcp/tools/system.ts</files>
  <read_first>
    - src/mcp/tools/system.ts (modify)
    - src/utils/errors.ts
    - .cursor/skills/spike-findings-awesome-coolify/references/mcp-sdk-patterns.md (two-layer error lines 80-88)
  </read_first>
  <acceptance_criteria>
    - handleSystemAction catch block returns wrapMcpError result with isError true on API failure
    - Successful health response still returns connected true without error envelope
    - npx vitest run exits 0 for full suite
    - src/mcp/tools/system.ts imports wrapMcpError from src/utils/errors.ts
  </acceptance_criteria>
  <action>
    Update handleSystemAction in src/mcp/tools/system.ts to catch errors from fetchHealth and return wrapMcpError envelope per D-12 two-layer pattern. Preserve successful health path unchanged. Add test case in system.test.ts asserting 401 response yields isError true and content text contains COOLIFY_401.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>System tool returns structured MCP errors on API failure; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coolify API → error mapper | Raw HTTP responses may contain sensitive headers |
| Error envelope → MCP client | Error JSON crosses to agent context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information Disclosure | mapApiError | high | mitigate | Strip Bearer tokens from message before envelope return; plan 01-04 adds redactSecrets |
| T-01-02 | Information Disclosure | wrapMcpError | high | mitigate | Never include request headers in content text |
| T-01-04 | Tampering | retry loop | medium | accept | Retry only on safe GET health/version endpoints in P1 |
| T-01-06 | Denial of Service | retry | low | accept | Max 3 retries caps amplification per D-11 |

</threat_model>

## Artifacts this phase produces

| Symbol | Location | Description |
|--------|----------|-------------|
| `CoolifyErrorEnvelope` | src/utils/errors.ts | Structured error interface |
| `mapApiError` | src/utils/errors.ts | HTTP/network error → envelope mapper |
| `toStructuredError` | src/utils/errors.ts | Unknown error → envelope |
| `wrapMcpError` | src/utils/errors.ts | MCP isError response builder |
| Retry config | src/api/client.ts | ofetch retry 3x exponential backoff |

<verification>
- npx vitest run src/utils/errors.test.ts src/api/client.test.ts exits 0
- Error codes COOLIFY_401 through COOLIFY_TIMEOUT all have test coverage
</verification>

<success_criteria>
- ERR-01, ERR-02, ERR-03 satisfied
- API failures return parseable JSON with code message recoveryHints httpStatus
</success_criteria>

<output>
Create `.planning/phases/01-foundation-multi-instance-auth/01-02-SUMMARY.md` when done
</output>
