---
phase: 01-foundation-multi-instance-auth
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/utils/redact.ts
  - src/utils/redact.test.ts
  - src/utils/logger.ts
  - src/api/client.ts
  - src/utils/errors.ts
autonomous: true
requirements:
  - CTX-01
must_haves:
  truths:
    - redactSecrets replaces Bearer token sequences with Bearer followed by masked placeholder
    - redactSecrets replaces token api_key password secret key=value patterns with masked values
    - logger.debug and logger.info write only to stderr never stdout
    - logger respects COOLIFY_MCP_LOG level filtering per D-18
    - Client onRequestError and onResponseError pass messages through redactSecrets
  prohibitions:
    - Raw COOLIFY_TOKEN value must never appear in stderr after redactSecrets applied
    - Request/response bodies must not be logged per D-20
  artifacts:
    - src/utils/redact.ts
    - src/utils/logger.ts
  key_links:
    - src/api/client.ts hooks call redactSecrets on error messages
    - wrapMcpError in src/utils/errors.ts applies redactSecrets before envelope return
---

## Phase Goal

**As an** AI agent operator, **I want** secrets redacted in logs, **so that** tokens never leak to IDE output.

> **v2-deferred (D-04/D-05/D-06/D-07):** CTX-04, CTX-05, CTX-06, CTX-07 not in scope.

<objective>
Implement aggressive secret redaction (D-19) and stderr-only logging (D-18, D-20); wire into HTTP client and tool handlers.

Purpose: Close high-severity token leakage threats T-01-02, T-01-03 before DIST-03 handshake.
Output: redactSecrets, logger utilities integrated across client and tools.
</objective>

<execution_context>
@/Users/puzzless/gstack/.cursor/gsd-core/workflows/execute-plan.md
@/Users/puzzless/gstack/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md
@.planning/phases/01-foundation-multi-instance-auth/01-RESEARCH.md
@.planning/phases/01-foundation-multi-instance-auth/01-PATTERNS.md
@.planning/phases/01-foundation-multi-instance-auth/01-01-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Secret redaction utility</name>
  <files>src/utils/redact.ts, src/utils/redact.test.ts</files>
  <behavior>
    - redactSecrets on input Bearer eyJhbGciOiJIUzI1NiJ9.abc returns string containing Bearer *** and not containing eyJ
    - redactSecrets on token=secret123 returns token=***
    - redactSecrets on api_key=abc password=xyz secret=foo returns all values masked
    - redactSecrets on plain text without secrets returns unchanged text
  </behavior>
  <read_first>
    - src/utils/redact.ts (create)
    - .planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md (D-19)
    - .planning/phases/01-foundation-multi-instance-auth/01-PATTERNS.md (redact.ts pattern lines 203-208)
    - .planning/phases/01-foundation-multi-instance-auth/01-RESEARCH.md (Pitfall 1)
  </read_first>
  <acceptance_criteria>
    - src/utils/redact.ts exports function redactSecrets(text string) string
    - redactSecrets masks Bearer JWT-like sequences per D-19
    - redactSecrets masks token api_key password secret key=value patterns case insensitive
    - npx vitest run src/utils/redact.test.ts exits 0
    - Test asserts output for Bearer input does not contain original token substring
  </acceptance_criteria>
  <action>
    Create src/utils/redact.ts with exported redactSecrets function per 01-PATTERNS.md regex patterns for Bearer prefix and token api_key password secret equals patterns. Write src/utils/redact.test.ts with cases for Bearer JWT, query param secrets, and clean passthrough. Apply redaction to error message strings only — not to successful tool response data in this task.
  </action>
  <verify>
    <automated>npx vitest run src/utils/redact.test.ts</automated>
  </verify>
  <done>redactSecrets tests green; all D-19 patterns covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stderr-only logger with level gating</name>
  <files>src/utils/logger.ts, src/utils/logger.test.ts</files>
  <behavior>
    - logger.debug writes to console.error only when COOLIFY_MCP_LOG is debug
    - logger.info writes when COOLIFY_MCP_LOG is debug or info
    - logger.error always writes to console.error
    - logger methods never call console.log
  </behavior>
  <read_first>
    - src/utils/logger.ts (create)
    - src/config/env.ts
    - .planning/phases/01-foundation-multi-instance-auth/01-CONTEXT.md (D-18, D-20)
    - .planning/phases/01-foundation-multi-instance-auth/01-PATTERNS.md (logger.ts pattern)
  </read_first>
  <acceptance_criteria>
    - src/utils/logger.ts exports logger object with debug info error methods
    - All logger methods use console.error not console.log per D-18
    - logger.debug logs HTTP path and status only per D-20 — no body content
    - Logger reads COOLIFY_MCP_LOG from loadEnv result
  </acceptance_criteria>
  <action>
    Create src/utils/logger.ts per 01-PATTERNS.md pattern — stderr-only console.error with level gating from env.COOLIFY_MCP_LOG. debug level logs request path and HTTP status integer only per D-20. Import env via loadEnv or accept EnvConfig parameter to avoid circular imports. Add unit test mocking console.error spy verifying log level filtering.
  </action>
  <verify>
    <automated>npx vitest run src/utils/</automated>
  </verify>
  <done>Logger writes stderr only; debug logs path+status without bodies.</done>
</task>

<task type="auto">
  <name>Task 3: Wire redaction into client and error envelope</name>
  <files>src/api/client.ts, src/utils/errors.ts</files>
  <read_first>
    - src/api/client.ts (modify)
    - src/utils/errors.ts (modify)
    - src/utils/redact.ts
    - src/utils/logger.ts
  </read_first>
  <acceptance_criteria>
    - src/api/client.ts onRequestError applies redactSecrets to error.message before rethrow
    - src/api/client.ts onResponseError applies redactSecrets to error context strings
    - wrapMcpError in src/utils/errors.ts applies redactSecrets to envelope message field
    - npx vitest run exits 0
    - errors.test.ts asserts envelope message does not contain injected token fixture substring
  </acceptance_criteria>
  <action>
    Import redactSecrets into src/api/client.ts hooks — sweep all error message strings before mapApiError. Update wrapMcpError in src/utils/errors.ts to redact envelope message and recoveryHints strings. Add redaction test case in errors.test.ts with token embedded in source error message. Defer system tool logger wiring to plan 01-04.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Client and error envelope use redaction; no token leakage in error paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP errors → stderr | Error messages may echo Authorization headers |
| Debug logs → IDE | stderr visible in Cursor terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Information Disclosure | logger | high | mitigate | stderr only; path+status only per D-20; redactSecrets on all log strings |
| T-01-03 | Information Disclosure | redactSecrets | high | mitigate | Mask Bearer token api_key password secret per D-19 |
| T-01-04 | Tampering | log injection | low | accept | Logs are diagnostic only; no eval of log content |

</threat_model>

## Artifacts this phase produces

| Symbol | Location | Description |
|--------|----------|-------------|
| `redactSecrets` | src/utils/redact.ts | Secret pattern masking utility |
| `logger` | src/utils/logger.ts | stderr-only level-gated logger |

<verification>
- npx vitest run src/utils/redact.test.ts exits 0
- No test output contains hardcoded test token literal from fixtures after redaction
</verification>

<success_criteria>
- D-18, D-19, D-20 implemented
- Token leakage threat T-01-02 mitigated for P1 paths
</success_criteria>

<output>
Create `.planning/phases/01-foundation-multi-instance-auth/01-03-SUMMARY.md` when done
</output>
