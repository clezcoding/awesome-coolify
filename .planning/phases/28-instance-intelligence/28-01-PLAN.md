---
phase: 28-instance-intelligence
plan: 01
type: execute
wave: 1
depends_on:
  - "28-00"
files_modified:
  - src/utils/resource-graph.ts
  - src/utils/resource-graph.test.ts
  - src/mcp/tools/intelligence.ts
  - src/mcp/tools/intelligence.test.ts
  - src/mcp/server.ts
autonomous: true
requirements:
  - GRAPH-01
user_setup: []

must_haves:
  truths:
    - "Agent calls intelligence.graph and receives nodes+edges built from live Coolify UUID fields (GRAPH-01, D-01, D-02, D-07)"
    - "Primary edges only from database_uuid, application_uuid on flat /resources and service nested applications/databases via fetchService (D-07, D-08)"
    - "intelligence tool registered in server.ts with actionsCatalog listing scorecard/graph/impact/janitor/cleanup (D-01, D-02, D-03)"
    - "Optional instance routing on graph via parseWithInstanceRouting (D-16)"
    - "Wave 0 graph + resource-graph it.fails flipped to passing it()"
  artifacts:
    - path: src/utils/resource-graph.ts
      provides: "buildGraph, edgesFromFlatResources, edgesFromServiceRecord, findDependents"
      exports: ["buildGraph", "edgesFromFlatResources", "findDependents", "findOrphans"]
    - path: src/mcp/tools/intelligence.ts
      provides: "intelligenceActionSchema, handleIntelligenceAction, handleIntelligenceGraph"
      contains: "handleIntelligenceGraph"
    - path: src/mcp/server.ts
      provides: "registerTool intelligence"
      contains: "handleIntelligenceAction"
  key_links:
    - from: "handleIntelligenceGraph"
      to: "buildGraph / edgesFromFlatResources"
      via: "fetchResources + bounded fetchService enrichment"
    - from: "server.registerTool('intelligence')"
      to: "handleIntelligenceAction"
      via: "withInstanceRoutingSchema(intelligenceActionSchema)"
  prohibitions:
    - statement: "Do not invent graph edges from resource name or environment-variable string matching (D-08)"
      status: enforced
      verification: "rg -n \"env.*match|fuzzy|nameEquals\" src/utils/resource-graph.ts src/mcp/tools/intelligence.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
    - statement: "Do not fold scorecard/graph into diagnose or resource tools (D-01, D-03)"
      status: enforced
      verification: "rg -n \"scorecard|action: 'graph'\" src/mcp/tools/diagnose.ts src/mcp/tools/resource.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
    - statement: "Do not implement full scorecard/impact/janitor/cleanup happy paths in this plan"
      status: enforced
      verification: "npx vitest run src/mcp/tools/intelligence.test.ts -t scorecard -x ; test exit non-zero OR remaining it.fails still present for scorecard"
---

<objective>
Tracer vertical slice: new `intelligence` MCP tool end-to-end for `graph` — resource-graph util, schema/catalog, handler, server registration, Wave 0 graph tests GREEN.

Purpose: Prove composite intelligence architecture and UUID-only graph semantics (D-07/D-08) before scorecard/impact/janitor/cleanup expansion.

Output: `resource-graph.ts`, `intelligence.ts` (graph path), server registration, green graph tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-instance-intelligence/28-CONTEXT.md
@.planning/phases/28-instance-intelligence/28-RESEARCH.md
@.planning/phases/28-instance-intelligence/28-PATTERNS.md
@.planning/phases/28-instance-intelligence/28-00-SUMMARY.md
@src/mcp/tools/manifest.ts
@src/mcp/tools/diagnose.ts
@src/mcp/tools/database.ts
@src/mcp/tools/service.ts
@src/mcp/tools/shared-read-params.ts
@src/mcp/server.ts
</context>

## Artifacts this phase produces

- `src/utils/resource-graph.ts` — flat UUID edges, service nested edges, buildGraph, findDependents, findOrphans.
- `src/mcp/tools/intelligence.ts` — createFlatActionSchema for five actions (D-02), handleIntelligenceGraph, routing switch.
- `src/mcp/server.ts` — registerTool('intelligence', ...).
- Green graph / resource-graph tests from Wave 0.

<tasks>

<task type="tracer">
  <name>End-to-end intelligence.graph — UUID edges through util, tool, server</name>
  <reversibility rating="costly">D-01 new intelligence domain tool; D-02 published action names; D-07/D-08 graph edge semantics — agent catalog and graph contract become costly to flip.</reversibility>
  <files>src/utils/resource-graph.ts, src/utils/resource-graph.test.ts, src/mcp/tools/intelligence.ts, src/mcp/tools/intelligence.test.ts, src/mcp/server.ts</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-CONTEXT.md (D-01, D-02, D-03, D-07, D-08, D-16, D-18)
    - .planning/phases/28-instance-intelligence/28-RESEARCH.md (Graph Edge Inventory, Patterns 1 and 3, Pitfall 1)
    - .planning/phases/28-instance-intelligence/28-PATTERNS.md (intelligence.ts + resource-graph + server registration)
    - src/mcp/tools/manifest.ts (actionsCatalog + createFlatActionSchema shell)
    - src/mcp/tools/diagnose.ts (parseWithInstanceRouting + wrapMcpError switch)
    - src/mcp/tools/database.ts (database_uuid dependents filter ~1481–1488)
    - src/mcp/tools/application.ts (application_uuid dependents ~2101–2108)
    - src/mcp/tools/service.ts (fetchService nested applications/databases ~1399–1417; mapNestedChildResources)
    - src/mcp/server.ts (diagnose/manifest registerTool blocks)
  </read_first>
  <action>
    Implement production-quality tracer path per D-01/D-02/D-03: new domain tool `intelligence` (do not add graph to diagnose or resource). Create `src/utils/resource-graph.ts` exporting edgesFromFlatResources (database_uuid + application_uuid child→parent edges only), edgesFromServiceRecord / async enrichServices using fetchService nested applications[] and databases[] (relation service_child), buildGraph({nodes, edges, meta.services_enriched}), findDependents (reverse BFS), findOrphans (zero inbound). Cap service enrichment concurrency (e.g. 5). Create `src/mcp/tools/intelligence.ts` with intelligenceActionsCatalog listing scorecard · graph · impact · janitor · cleanup, intelligenceSafetyFooter (cleanup confirm + advisory impact), intelligenceActionSchema via createFlatActionSchema with optional instance + shared read params; impact requires uuid+type; cleanup requires targets+confirm. Implement handleIntelligenceAction with parseWithInstanceRouting + resolveRoutingEnv (D-16) and switch. Implement handleIntelligenceGraph: fetchResources, build flat edges, enrich services, return buildReadResponse-shaped data with nodes/edges/meta. For scorecard/impact/janitor/cleanup switch arms only, throw CoolifyApiError COOLIFY_NOT_IMPLEMENTED naming the pending plan so Wave 0 RED cases stay red (D-18: no fake Coolify data). Register tool in server.ts mirroring diagnose (composeToolDescription, withInstanceRoutingSchema, isIntelligenceErrorResult). Flip Wave 0 graph + resource-graph it.fails to it(). ESM `.js` imports. No new npm packages.
  </action>
  <verify>
    <automated>npx vitest run src/utils/resource-graph.test.ts src/mcp/tools/intelligence.test.ts -t graph -x</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/resource-graph.ts exports buildGraph and edgesFromFlatResources
    - src/mcp/tools/intelligence.ts exports handleIntelligenceAction and intelligenceActionSchema
    - src/mcp/server.ts registers tool name intelligence
    - Graph unit tests pass; scorecard/cleanup Wave 0 cases remain it.fails or still failing until later plans
    - Graph edges use only database_uuid, application_uuid, and service nested children (D-07, D-08, GRAPH-01)
  </acceptance_criteria>
  <done>Agent can call intelligence.graph end-to-end with UUID-only edges; tool registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden graph enrichment meta + empty-service pitfall coverage</name>
  <files>src/utils/resource-graph.ts, src/mcp/tools/intelligence.ts, src/mcp/tools/intelligence.test.ts</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-RESEARCH.md (Pitfall 1 Empty service subgraph)
    - src/mcp/tools/service.ts (mapNestedChildResources)
    - src/mcp/tools/intelligence.test.ts (graph describe from Wave 0)
  </read_first>
  <behavior>
    - When flat resources include type service, fetchService is called and nested children appear as edges
    - meta.services_enriched equals number of successful service fetches
    - Missing nested arrays yield service node without inventing children
  </behavior>
  <action>
    Ensure handleIntelligenceGraph documents services_enriched in meta and bounds concurrency. Add/green tests covering service enrichment happy path and empty nested arrays. Keep D-08: do not add secondary hint edges. Soft-fail individual fetchService errors into meta without failing whole graph (D-17 spirit for composite reads).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/intelligence.test.ts src/utils/resource-graph.test.ts -t graph -x</automated>
  </verify>
  <acceptance_criteria>
    - Service enrichment test green
    - meta.services_enriched present on graph response
    - No name/env heuristic edge code paths
  </acceptance_criteria>
  <done>Service subgraph enrichment proven; Pitfall 1 covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP args → graph builder | uuid/type filters untrusted |
| Coolify /resources + /services → edges | Only explicit UUID fields trusted as links |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-04 | Spoofing | resource-graph edges | medium | mitigate | UUID-only primary edges (D-08); no env/name heuristics — enforced in tasks |
| T-28-05 | Info disclosure | graph node payloads | low | mitigate | Project only uuid/type/name/status/project/environment fields; reuse redactSecrets on errors |
| T-28-01 | Tampering | cleanup (not in this plan) | high | mitigate | Deferred to Plan 28-04; tracer does not expose mutating happy path |
| T-28-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
`npx vitest run src/utils/resource-graph.test.ts src/mcp/tools/intelligence.test.ts -t graph -x` green; server registers intelligence; diagnose/resource catalogs unchanged.
</verification>

<success_criteria>
- GRAPH-01 observable via intelligence.graph
- D-01/D-02/D-03/D-07/D-08/D-16 satisfied for tracer path
- Architecture proven for expansion plans
</success_criteria>

<output>
Create `.planning/phases/28-instance-intelligence/28-01-SUMMARY.md` when done
</output>
