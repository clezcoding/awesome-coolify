---
phase: 28-instance-intelligence
plan: 03
type: execute
wave: 3
depends_on:
  - "28-01"
  - "28-02"
files_modified:
  - src/utils/resource-graph.ts
  - src/mcp/tools/intelligence.ts
  - src/mcp/tools/intelligence.test.ts
autonomous: true
requirements:
  - GRAPH-02
  - JANI-01
user_setup: []

must_haves:
  truths:
    - "Agent calls intelligence.impact with uuid+type+intent and receives direct_dependents then transitive_dependents within max_depth (default 3) with advisory true (GRAPH-02, D-09, D-10)"
    - "impact does not call delete/restart handlers — advisory preflight only (D-10, D-15)"
    - "Agent calls intelligence.janitor and receives stopped/long_exited/orphan candidates with safe cleanup suggestions and preview_only true (JANI-01, D-11, D-12)"
    - "stopped_days default 7 with optional override; long_exited uses updated_at clock (D-12)"
    - "Orphan detection uses graph inbound degree only — UUID edges (D-08, D-12)"
  artifacts:
    - path: src/mcp/tools/intelligence.ts
      provides: "handleIntelligenceImpact + handleIntelligenceJanitor"
      contains: "handleIntelligenceImpact"
    - path: src/utils/resource-graph.ts
      provides: "findDependents + findOrphans + findJanitorCandidates helpers"
      exports: ["findDependents", "findOrphans"]
  key_links:
    - from: "handleIntelligenceImpact"
      to: "findDependents"
      via: "reverse BFS on buildGraph edges"
    - from: "handleIntelligenceJanitor"
      to: "findOrphans + status filters"
      via: "graph in-degree + exited/stopped + updated_at threshold"
  prohibitions:
    - statement: "Do not mutate resources from impact or janitor handlers (D-10, D-11, D-15)"
      status: enforced
      verification: "rg -n \"handleApplicationAction|handleServiceAction|handleDatabaseAction|deleteApplication|deleteService|deleteDatabase\" src/mcp/tools/intelligence.ts | grep -v '^#' | grep -v cleanup | grep -v handleIntelligenceCleanup | wc -l | awk '{exit ($1==0)?0:1}'"
    - statement: "Do not derive orphan edges from environment variable values (D-08)"
      status: enforced
      verification: "rg -ni \"process\\.env|env_var|fuzzy\" src/mcp/tools/intelligence.ts src/utils/resource-graph.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
---

<objective>
Expand intelligence with advisory `impact` (GRAPH-02) and read-only `janitor` (JANI-01) on the shared resource graph.

Purpose: Preflight blast-radius queries and safe cleanup candidate listing without mutations (D-10, D-11, D-15).

Output: handleIntelligenceImpact, handleIntelligenceJanitor, green impact/janitor tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-instance-intelligence/28-CONTEXT.md
@.planning/phases/28-instance-intelligence/28-RESEARCH.md
@.planning/phases/28-instance-intelligence/28-PATTERNS.md
@.planning/phases/28-instance-intelligence/28-01-SUMMARY.md
@.planning/phases/28-instance-intelligence/28-02-SUMMARY.md
@src/mcp/tools/intelligence.ts
@src/utils/resource-graph.ts
@src/utils/issue-classifier.ts
@src/utils/diagnose-hints.ts
</context>

## Artifacts this phase produces

- Impact response: target, intent, direct_dependents, transitive_dependents, depth_cap, advisory, suggested_preflight.
- Janitor response: candidates[] with reason/suggestion/safe_to_delete/preview_only + coverage_note for UUID-only orphan limits.
- Green impact + janitor Wave 0 tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Advisory impact analysis with depth cap</name>
  <reversibility rating="costly">D-09/D-10 impact contract (direct then transitive, advisory) becomes agent preflight habit.</reversibility>
  <files>src/utils/resource-graph.ts, src/mcp/tools/intelligence.ts, src/mcp/tools/intelligence.test.ts</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-CONTEXT.md (D-09, D-10, D-15)
    - .planning/phases/28-instance-intelligence/28-RESEARCH.md (Pattern 4 Impact, Pitfall 4 restart)
    - .planning/phases/28-instance-intelligence/28-PATTERNS.md (findDependents)
    - src/utils/resource-graph.ts (buildGraph from Plan 28-01)
  </read_first>
  <behavior>
    - impact requires uuid + type; intent defaults to delete; max_depth defaults to 3
    - response lists direct_dependents first, then transitive_dependents within depth cap
    - advisory true; suggested_preflight includes domain delete_preview tool/action hints
    - intent restart returns same dependents with impact_level tagging (outage|degraded) documenting advisory subset vs delete (D-09 discretion)
    - no domain delete/restart client calls from impact handler (D-10)
  </behavior>
  <action>
    Implement findDependents reverse BFS on graph edges (child depends on parent → removing parent breaks children). Scope traversal to same environment/project when environment_id/project_uuid present on nodes. Replace impact COOLIFY_NOT_IMPLEMENTED with handleIntelligenceImpact. Default max_depth 3, intent delete. For intent restart, include dependents but tag database_uuid-only links as degraded vs outage where applicable (document in response). Flip Wave 0 impact it.fails to it(). Do not perform mutations (D-10, D-15).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/intelligence.test.ts -t impact -x</automated>
  </verify>
  <acceptance_criteria>
    - Impact returns direct then transitive within depth cap (GRAPH-02, D-09)
    - advisory true present (D-10)
    - Impact tests green; no delete client invocations in impact tests
  </acceptance_criteria>
  <done>GRAPH-02 impact advisory preflight works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read-only janitor candidates + suggestions</name>
  <reversibility rating="costly">D-11 janitor read-only semantics and D-12 criteria become agent-facing.</reversibility>
  <files>src/utils/resource-graph.ts, src/mcp/tools/intelligence.ts, src/mcp/tools/intelligence.test.ts</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-CONTEXT.md (D-11, D-12, D-15)
    - .planning/phases/28-instance-intelligence/28-RESEARCH.md (Pattern 5 Janitor, Pitfall 5 orphan false positives)
    - src/utils/issue-classifier.ts (exited/stopped statusStartsWith ~99–117)
    - src/utils/diagnose-hints.ts (FollowUpHint shape)
  </read_first>
  <behavior>
    - janitor unions stopped/exited, long_exited (updated_at older than stopped_days default 7), orphan (zero inbound dependents); dedupe by uuid
    - each candidate has reason, suggestion {tool,action,args,label}, safe_to_delete false when dependents exist, preview_only true
    - response states preview / no mutation posture (D-11)
    - coverage_note documents UUID-only orphan limitation (Pitfall 5 / D-08)
  </behavior>
  <action>
    Implement handleIntelligenceJanitor as read-only (D-11). Criteria per D-12: status starts with exited or stopped; long_exited via updated_at vs stopped_days (default 7, optional override); orphan via findOrphans on UUID graph only. Suggestions point to application/service/database delete_preview (or intelligence.cleanup after confirm) — never auto-execute deletes (D-15). Flip Wave 0 janitor it.fails to it(). Keep cleanup arm unimplemented until Plan 28-04.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/intelligence.test.ts -t janitor -x</automated>
  </verify>
  <acceptance_criteria>
    - Janitor lists stopped/long_exited/orphan with suggestions (JANI-01, D-11, D-12)
    - preview_only true on candidates; no mutations
    - Janitor tests green
  </acceptance_criteria>
  <done>JANI-01 read-only janitor complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Impact uuid input → graph traversal | Untrusted UUID; validate via Zod uuid() |
| Janitor suggestions → agent | Advisory only; mutations gated in Plan 28-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-04 | Spoofing | orphan detection | medium | mitigate | UUID-only in-degree (D-08); coverage_note for env-linked DBs |
| T-28-07 | Tampering | impact advice | medium | mitigate | Mark advisory true; callers must use domain tools (D-10) |
| T-28-01 | Tampering | janitor→cleanup | high | mitigate | Janitor read-only (D-11/D-15); destructive path only in Plan 28-04 with confirm |
| T-28-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
`npx vitest run src/mcp/tools/intelligence.test.ts -t "impact|janitor" -x` green; scorecard+graph still green.
</verification>

<success_criteria>
- GRAPH-02 and JANI-01 satisfied
- D-09–D-12, D-15 (reads) satisfied
- No destructive mutations from this plan
</success_criteria>

<output>
Create `.planning/phases/28-instance-intelligence/28-03-SUMMARY.md` when done
</output>
