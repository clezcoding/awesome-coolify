---
phase: 28-instance-intelligence
plan: 02
type: execute
wave: 2
depends_on:
  - "28-01"
files_modified:
  - src/mcp/tools/intelligence.ts
  - src/mcp/tools/intelligence.test.ts
autonomous: true
requirements:
  - INTEL-01
  - INTEL-02
user_setup: []

must_haves:
  truths:
    - "Agent calls intelligence.scorecard and receives overall severity plus per-factor breakdown for deployments, backups, exited_resources, diagnose_scan (INTEL-01, D-04)"
    - "findings[] entries carry severity critical|high|info and structured FollowUpHint recovery hints (INTEL-02, D-05)"
    - "Scoring is deterministic rule-based with transparent score_breakdown — no ML (D-06)"
    - "One factor failure returns soft partial with failed flag on that factor while siblings succeed (D-17)"
    - "Deployment/backup factors sample with caps (50) and concurrency (5); partial true when capped"
  artifacts:
    - path: src/mcp/tools/intelligence.ts
      provides: "handleIntelligenceScorecard + factor collectors"
      contains: "handleIntelligenceScorecard"
  key_links:
    - from: "handleIntelligenceScorecard"
      to: "classifyIssues + generateHints"
      via: "diagnose_scan + exited factor + findings"
    - from: "deployments factor"
      to: "fetchAppDeployments"
      via: "bounded per-app sample"
    - from: "backups factor"
      to: "fetchDatabaseBackups"
      via: "bounded per-db sample"
  prohibitions:
    - statement: "Do not call handleDiagnoseAction internally for scan — use classifyIssues directly"
      status: enforced
      verification: "rg -n \"handleDiagnoseAction\" src/mcp/tools/intelligence.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
    - statement: "Do not add statistical/ML anomaly detection"
      status: enforced
      verification: "rg -ni \"tensorflow|anomaly|stddev|zscore\" src/mcp/tools/intelligence.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
---

<objective>
Expand intelligence tool with composite `scorecard` — four factors, severity rollup, structured findings/hints, soft partials (INTEL-01, INTEL-02).

Purpose: Deliver instance health assessment on existing Coolify 4.1.x reads only (D-04, D-18).

Output: handleIntelligenceScorecard + green scorecard/findings/partial tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-instance-intelligence/28-CONTEXT.md
@.planning/phases/28-instance-intelligence/28-RESEARCH.md
@.planning/phases/28-instance-intelligence/28-PATTERNS.md
@.planning/phases/28-instance-intelligence/28-01-SUMMARY.md
@src/mcp/tools/intelligence.ts
@src/mcp/tools/diagnose.ts
@src/utils/issue-classifier.ts
@src/utils/diagnose-hints.ts
@src/api/client.ts
@tests/fixtures/coolify-mixed-health.ts
</context>

## Artifacts this phase produces

- `src/mcp/tools/intelligence.ts` — handleIntelligenceScorecard with Promise.allSettled factors.
- Green intelligence.test.ts scorecard / findings / partial describes.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scorecard composite factors + severity rollup</name>
  <reversibility rating="costly">D-04 factor names and D-05 envelope become agent-facing contracts.</reversibility>
  <files>src/mcp/tools/intelligence.ts, src/mcp/tools/intelligence.test.ts</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-CONTEXT.md (D-04, D-05, D-06, D-17, D-18)
    - .planning/phases/28-instance-intelligence/28-RESEARCH.md (Pattern 2 Scorecard, Pitfall 2 N+1)
    - .planning/phases/28-instance-intelligence/28-PATTERNS.md (soft partial + classifyIssues)
    - src/mcp/tools/diagnose.ts (scan projection + soft partial ~546–559, ~710–743)
    - src/utils/issue-classifier.ts (classifyIssues + exited/stopped mapping)
    - src/utils/diagnose-hints.ts (FollowUpHint / generateHints)
    - src/api/client.ts (fetchAppDeployments, fetchDatabaseBackups signatures)
  </read_first>
  <behavior>
    - scorecard returns factors.deployments, factors.backups, factors.exited_resources, factors.diagnose_scan
    - overall severity is max of factor severities using critical > high > info > ok
    - transparent numeric score with score_breakdown object (deductive: start 100, −30 critical, −15 high, −5 info, floor 0) per D-06 discretion
    - deployments: sample up to 50 apps, concurrency 5; high if latest failed or stuck in_progress >24h; info if older failures in 7d; set partial+sampled_count when capped
    - backups: sample up to 50 databases, concurrency 5; high if zero enabled schedules; info if schedule disabled
    - exited_resources: count status starting with exited or stopped → info severity findings
    - diagnose_scan: classifyIssues(servers, resources) bucket counts + top issues as findings
  </behavior>
  <action>
    Replace scorecard COOLIFY_NOT_IMPLEMENTED arm with handleIntelligenceScorecard. Fetch servers+resources once; run factor collectors via Promise.allSettled (D-17). Prefer classifyIssues() and generateHints() — do not call handleDiagnoseAction. Omit any factor that lacks a Coolify endpoint rather than inventing data (D-18). Map findings to severity tags critical|high|info with structured hint objects (tool/action/args/label), not free-text-only (D-05, INTEL-02). Flip Wave 0 scorecard it.fails to it(). Keep graph path unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/intelligence.test.ts -t scorecard -x</automated>
  </verify>
  <acceptance_criteria>
    - Four minimum factors present on happy-path scorecard (INTEL-01, D-04)
    - Overall severity + score_breakdown present (D-05, D-06)
    - Scorecard honors optional `instance` routing via parseWithInstanceRouting (D-16)
    - Scorecard tests green
  </acceptance_criteria>
  <done>Scorecard composite read returns factor breakdown and severity rollup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Findings hints + soft partial factor failure</name>
  <files>src/mcp/tools/intelligence.ts, src/mcp/tools/intelligence.test.ts</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-CONTEXT.md (D-05, D-17)
    - src/mcp/tools/diagnose.test.ts (soft partial diagnose_failed pattern ~1209–1233)
    - src/utils/diagnose-hints.ts
  </read_first>
  <behavior>
    - findings[] include severity and FollowUpHint-shaped recovery hints (INTEL-02, D-05)
    - when deployments collector rejects, factors.deployments.failed has code/message and other factors still populate (D-17)
  </behavior>
  <action>
    Ensure findings aggregation merges factor findings with generateHints where status-based. Implement toFactorError helper for rejected settlements. Green findings + partial Wave 0 tests. Do not fail entire scorecard when a single factor rejects.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/intelligence.test.ts -t "findings|partial" -x</automated>
  </verify>
  <acceptance_criteria>
    - findings tests assert severity + hint.tool/action shape (INTEL-02)
    - partial test asserts sibling factors survive one rejection (D-17)
  </acceptance_criteria>
  <done>INTEL-02 hints + D-17 soft partials green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coolify deployment/backup payloads → scorecard | Untrusted strings; project/redact before agent output |
| MCP format/max_chars → formatter | Bound output size via existing buildReadResponse |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-05 | Info disclosure | scorecard findings | low | mitigate | Reuse redactSecrets / existing error wrapping on factor failures |
| T-28-06 | Denial of service | N+1 scorecard fetches | medium | mitigate | Cap 50 apps/dbs + concurrency 5 + partial flag (Pitfall 2) |
| T-28-01 | Tampering | cleanup | high | mitigate | Scorecard remains read-only (D-15); no mutations in this plan |
| T-28-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
`npx vitest run src/mcp/tools/intelligence.test.ts -t "scorecard|findings|partial" -x` green; graph tests still green.
</verification>

<success_criteria>
- INTEL-01 and INTEL-02 satisfied
- D-04, D-05, D-06, D-17 implemented
- Read-only posture preserved (D-15)
</success_criteria>

<output>
Create `.planning/phases/28-instance-intelligence/28-02-SUMMARY.md` when done
</output>
