---
phase: 28-instance-intelligence
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/tools/intelligence.test.ts
  - src/utils/resource-graph.test.ts
  - src/mcp/tools/system.test.ts
  - docs/coverage-map.yaml
autonomous: true
requirements:
  - INTEL-01
  - INTEL-02
  - GRAPH-01
  - GRAPH-02
  - JANI-01
  - JANI-02
user_setup: []

must_haves:
  truths:
    - "intelligence.test.ts has it.fails scaffolds for scorecard factors+severityity+findings (INTEL-01, INTEL-02, D-04, D-05, D-17)"
    - "intelligence.test.ts has it.fails scaffolds for graph UUID edges + service nested enrichment (GRAPH-01, D-07, D-08)"
    - "intelligence.test.ts has it.fails scaffolds for impact direct-then-transitive depth cap (GRAPH-02, D-09, D-10)"
    - "intelligence.test.ts has it.fails scaffolds for janitor stopped/orphan/long-exited suggestions (JANI-01, D-11, D-12)"
    - "intelligence.test.ts has it.fails scaffolds for cleanup confirm gate + SAF-02 defaults (JANI-02, D-13, D-14, T-28-01, T-28-02)"
    - "resource-graph.test.ts has it.fails scaffolds for edgesFromFlatResources + findDependents"
    - "system.test.ts has it.fails scaffold expecting eleven capability keys including five intelligence_* keys (D-19)"
    - "docs/coverage-map.yaml has intelligence.scorecard/graph/impact/janitor/cleanup rows (D-19)"
    - "All new RED cases use vitest it.fails so husky pre-commit stays green"
  artifacts:
    - path: src/mcp/tools/intelligence.test.ts
      provides: "describe blocks it.fails for all five intelligence actions"
      contains: "scorecard"
    - path: src/utils/resource-graph.test.ts
      provides: "graph helper it.fails scaffolds"
      contains: "edgesFromFlatResources"
    - path: docs/coverage-map.yaml
      provides: "intelligence.* coverage rows"
      contains: "intelligence.scorecard"
  key_links:
    - from: "intelligence.test.ts"
      to: "handleIntelligenceAction (Plans 28-01..28-04)"
      via: "vi.mock api/client + domain delete handlers"
    - from: "resource-graph.test.ts"
      to: "src/utils/resource-graph.ts (Plan 28-01)"
      via: "dynamic import or direct import once module exists"
  prohibitions:
    - statement: "Do not implement production intelligence.ts, resource-graph.ts, server registration, or capabilities in Wave 0 — scaffolds only"
      status: enforced
      verification: "git diff --name-only -- src/mcp/tools/intelligence.ts src/utils/resource-graph.ts src/mcp/server.ts src/mcp/capabilities.ts is empty after this plan"
    - statement: "Do not edit README EN/DE in Wave 0 (Plan 28-04)"
      status: enforced
      verification: "git diff --name-only -- README.md README.de.md is empty after this plan"
---

<objective>
Wave 0 Nyquist RED scaffolds for Phase 28 — lock INTEL/GRAPH/JANI acceptance as `it.fails` tests before implementation.

Purpose: Plans 28-01..28-04 flip scaffolds GREEN without rewriting intent. Mirrors Phase 24–26 Wave 0 pattern.

Output: intelligence + resource-graph + system test RED targets; coverage-map rows; production handlers untouched.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-instance-intelligence/28-CONTEXT.md
@.planning/phases/28-instance-intelligence/28-RESEARCH.md
@.planning/phases/28-instance-intelligence/28-PATTERNS.md
@.planning/phases/28-instance-intelligence/28-VALIDATION.md
@src/mcp/tools/diagnose.test.ts
@src/mcp/tools/database.test.ts
@src/mcp/tools/application.test.ts
@src/mcp/tools/system.test.ts
@docs/coverage-map.yaml
@tests/fixtures/coolify-mixed-health.ts
</context>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|-----|---------------------|------|--------|-------|
| GOAL | — | Assess health, map deps, safe cleanup candidates | 01–04 | COVERED | ROADMAP success criteria 1–5 |
| REQ | INTEL-01 | Scorecard factor breakdown | 00 RED, 02 GREEN | COVERED | |
| REQ | INTEL-02 | Severity + structured recovery hints | 00 RED, 02 GREEN | COVERED | |
| REQ | GRAPH-01 | Live dependency graph | 00 RED, 01 GREEN | COVERED | Tracer |
| REQ | GRAPH-02 | Impact before delete/restart | 00 RED, 03 GREEN | COVERED | |
| REQ | JANI-01 | Orphan/stopped/long-exited list | 00 RED, 03 GREEN | COVERED | |
| REQ | JANI-02 | Cleanup confirm gate SAF | 00 RED, 04 GREEN | COVERED | D-13 one-way checkpoint |
| RESEARCH | — | resource-graph.ts extract | 00 RED, 01 GREEN | COVERED | |
| RESEARCH | — | Soft partials + sample caps | 02 | COVERED | D-17 |
| RESEARCH | — | UUID-only edges; service nested fetch | 01 | COVERED | D-07/D-08 |
| RESEARCH | — | Reuse domain delete handlers | 04 | COVERED | D-14 |
| CONTEXT | D-01..D-03 | New intelligence tool + 5 actions | 01 | COVERED | |
| CONTEXT | D-04..D-06 | Scorecard composite / rule-based | 02 | COVERED | |
| CONTEXT | D-07..D-10 | Graph + advisory impact | 01, 03 | COVERED | |
| CONTEXT | D-11..D-15 | Janitor read-only + cleanup confirm | 03, 04 | COVERED | |
| CONTEXT | D-16..D-19 | Routing, soft partials, no stubs, caps/docs | 01–04 | COVERED | |
| CONTEXT | Deferred | Drift/Guard/Playbooks/ML/fan-out | — | EXCLUDED | Deferred Ideas |

## Artifacts this phase produces

- `src/mcp/tools/intelligence.test.ts` — `it.fails` blocks for scorecard, graph, impact, janitor, cleanup, soft partial.
- `src/utils/resource-graph.test.ts` — `it.fails` for flat edges + dependents BFS.
- `src/mcp/tools/system.test.ts` — eleven-key capabilities `it.fails` scaffold (D-19).
- `docs/coverage-map.yaml` — five `intelligence.*` MCP-composite rows.

<tasks>

<task type="tdd">
  <name>Task 1: RED scaffolds for intelligence actions + resource-graph</name>
  <files>src/mcp/tools/intelligence.test.ts, src/utils/resource-graph.test.ts</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-CONTEXT.md (D-01–D-15)
    - .planning/phases/28-instance-intelligence/28-RESEARCH.md (Validation Architecture + Patterns 2–5)
    - .planning/phases/28-instance-intelligence/28-PATTERNS.md (intelligence.test.ts + resource-graph analogs)
    - .planning/phases/28-instance-intelligence/28-VALIDATION.md (Wave 0 checklist)
    - src/mcp/tools/diagnose.test.ts (vi.mock client + soft partial pattern)
    - src/mcp/tools/database.test.ts (delete_preview database_uuid dependents ~1004–1027)
    - src/mcp/tools/application.test.ts (confirm gate + SAF-02 ~2563–2594)
    - tests/fixtures/coolify-mixed-health.ts (exited status shapes)
  </read_first>
  <behavior>
    - scorecard: returns factors deployments/backups/exited_resources/diagnose_scan + overall severity + findings[] with severity + FollowUpHint-shaped recovery hints (INTEL-01, INTEL-02, D-04, D-05)
    - scorecard soft partial: one factor reject leaves siblings present with failed flag on the rejected factor (D-17)
    - graph: edges from database_uuid and application_uuid on flat resources; service→child via fetchService nested applications/databases; no fuzzy name edges (GRAPH-01, D-07, D-08)
    - impact: given uuid+type+intent, returns direct_dependents then transitive_dependents within max_depth default 3; advisory true; does not mutate (GRAPH-02, D-09, D-10)
    - janitor: lists stopped/exited, long_exited (stopped_days default 7), orphan (zero inbound dependents); each candidate has suggestion FollowUpHint + preview_only true (JANI-01, D-11, D-12)
    - cleanup without confirm true → structuredContent.error.code COOLIFY_CONFIRM_REQUIRED and domain delete clients not called (JANI-02, D-13, T-28-01)
    - cleanup with confirm true and no delete_volumes/delete_configurations flags → delete handlers receive both flags false (SAF-02, D-14, T-28-02)
    - resource-graph: edgesFromFlatResources emits database_uuid + application_uuid edges; findDependents returns direct then transitive up to depth cap
  </behavior>
  <action>
    Create co-located `src/mcp/tools/intelligence.test.ts` mirroring diagnose.test.ts harness (vi.mock `../../api/client.js` for fetchResources/fetchServers/fetchService/fetchAppDeployments/fetchDatabaseBackups; mock application/service/database delete handlers for cleanup). Use `it.fails` for every behavior above so pre-commit stays green. Group describes: scorecard, findings, partial, graph, impact, janitor, cleanup. Create `src/utils/resource-graph.test.ts` with `it.fails` for edgesFromFlatResources + findDependents using inline fixture resources (child with database_uuid, child with application_uuid). Do not create production modules in this plan. Seed fixtures from coolify-mixed-health exited shapes where useful. Cite D-04–D-14 and INTEL/GRAPH/JANI reqs in describe titles or comments for traceability.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/intelligence.test.ts src/utils/resource-graph.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - intelligence.test.ts exists and contains it.fails cases matching INTEL-01, INTEL-02, GRAPH-01, GRAPH-02, JANI-01, JANI-02 behaviors
    - resource-graph.test.ts exists with it.fails for flat edges + dependents
    - `npx vitest run src/mcp/tools/intelligence.test.ts src/utils/resource-graph.test.ts -x` exits 0 (it.fails expected)
    - No new src/mcp/tools/intelligence.ts or src/utils/resource-graph.ts production file from this plan
  </acceptance_criteria>
  <done>Wave 0 RED scaffolds lock all six requirement behaviors; production code untouched.</done>
</task>

<task type="tdd">
  <name>Task 2: RED scaffolds for capabilities + coverage-map rows</name>
  <files>src/mcp/tools/system.test.ts, docs/coverage-map.yaml</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-CONTEXT.md (D-19)
    - .planning/phases/28-instance-intelligence/28-PATTERNS.md (capabilities + coverage-map analogs)
    - src/mcp/tools/system.test.ts (CAPABILITY_KEYS ~140–179)
    - docs/coverage-map.yaml (diagnose.logs row)
    - src/mcp/capabilities.ts (diagnose_logs entry)
  </read_first>
  <behavior>
    - system.version capabilities expects eleven sorted keys including intelligence_scorecard, intelligence_graph, intelligence_impact, intelligence_janitor, intelligence_cleanup (D-19)
    - each intelligence_* entry shape: supported true, coolify_min_version 4.1.2, note string
    - coverage-map.yaml has rows for intelligence.scorecard, intelligence.graph, intelligence.impact, intelligence.janitor, intelligence.cleanup with composed client arrays
  </behavior>
  <action>
    Add an `it.fails` (or convert existing six-key test via a parallel it.fails describe) in system.test.ts locking eleven capability keys per D-19 / RESEARCH Open Questions recommendation — do not change capabilities.ts production yet. Append five intelligence.* rows to docs/coverage-map.yaml following diagnose.logs MCP-composite style (client lists fetchResources/fetchServers/fetchService/fetchAppDeployments/fetchDatabaseBackups; cleanup row also lists domain delete client fns). Do not regenerate COVERAGE.md here. Do not edit README.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/system.test.ts -x && test "$(rg -c "intelligence\.(scorecard|graph|impact|janitor|cleanup)" docs/coverage-map.yaml | head -1)" != "" && rg -n "intelligence\.(scorecard|graph|impact|janitor|cleanup)" docs/coverage-map.yaml | grep -v '^#' | wc -l | awk 'BEGIN{ok=0} {if($1>=5) ok=1} END{exit ok?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - system.test.ts has it.fails (or failing-until-green) assertion for eleven keys including five intelligence_* names
    - docs/coverage-map.yaml contains all five intelligence.* action rows
    - Production capabilities.ts unchanged in this plan
  </acceptance_criteria>
  <done>Capability + coverage RED targets ready for Plan 28-04 / openapi refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → intelligence tool | Untrusted agent args (uuid, targets, confirm) |
| Coolify API → MCP | Trusted-enough inventory; still validate UUID shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | intelligence.cleanup | high | mitigate | Wave 0 RED locks COOLIFY_CONFIRM_REQUIRED; Plan 28-04 implements gate (D-13) |
| T-28-02 | Destruction | cleanup delete flags | high | mitigate | Wave 0 RED locks SAF-02 defaults false; Plan 28-04 implements |
| T-28-03 | Elevation | cleanup targets | high | mitigate | Wave 0 RED requires explicit targets[]; Plan 28-04 enforces |
| T-28-04 | Spoofing | graph edges | medium | mitigate | Wave 0 RED asserts UUID field edges only (D-08); Plan 28-01 implements |
| T-28-SC | Tampering | npm installs | low | accept | No new packages this phase |
</threat_model>

<verification>
Wave 0 complete when all it.fails scaffolds exist, vitest exits 0, and production intelligence/resource-graph/server/capabilities files are untouched.
</verification>

<success_criteria>
- Six REQs have RED test targets
- Coverage-map + capability RED targets present
- Husky/pre-commit green via it.fails
</success_criteria>

<output>
Create `.planning/phases/28-instance-intelligence/28-00-SUMMARY.md` when done
</output>
