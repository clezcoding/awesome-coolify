---
phase: 28-instance-intelligence
plan: 04
type: execute
wave: 4
depends_on:
  - "28-03"
files_modified:
  - src/mcp/tools/intelligence.ts
  - src/mcp/tools/intelligence.test.ts
  - src/mcp/capabilities.ts
  - src/mcp/tools/system.test.ts
  - docs/coverage-map.yaml
  - README.md
  - README.de.md
autonomous: false
requirements:
  - JANI-02
user_setup: []

must_haves:
  truths:
    - "intelligence.cleanup without confirm true returns COOLIFY_CONFIRM_REQUIRED and does not call domain delete handlers (JANI-02, D-13, T-28-01)"
    - "cleanup requires explicit targets[] of {type,uuid}; batch confirm processes each target with per-item results (D-13, T-28-03)"
    - "delete_volumes and delete_configurations default false when omitted (SAF-02, D-14, T-28-02)"
    - "cleanup reuses handleApplicationAction/handleServiceAction/handleDatabaseAction delete paths — no parallel HTTP delete client (D-14)"
    - "Five intelligence_* capability keys on system.version; README EN+DE note; coverage-map rows green (D-19)"
    - "Scorecard/graph/janitor remain non-mutating (D-15)"
  artifacts:
    - path: src/mcp/tools/intelligence.ts
      provides: "handleIntelligenceCleanup with confirm gate + SAF-02 defaults"
      contains: "handleIntelligenceCleanup"
    - path: src/mcp/capabilities.ts
      provides: "intelligence_scorecard/graph/impact/janitor/cleanup keys"
      contains: "intelligence_scorecard"
    - path: README.md
      provides: "Instance intelligence shipped note"
      contains: "intelligence.scorecard"
  key_links:
    - from: "handleIntelligenceCleanup"
      to: "handleApplicationAction / handleServiceAction / handleDatabaseAction"
      via: "action delete + confirm true + SAF-02 flags"
    - from: "system.version"
      to: "COOLIFY_412_CAPABILITIES"
      via: "five intelligence_* keys"
  prohibitions:
    - statement: "Do not add an unbounded fleet-wide cleanup action without explicit targets"
      status: enforced
      verification: "rg -n \"targets\" src/mcp/tools/intelligence.ts | grep -v '^#' | wc -l | awk '{exit ($1>=1)?0:1}'"
    - statement: "Do not implement raw deleteApplication/deleteService/deleteDatabase HTTP calls inside intelligence.ts"
      status: enforced
      verification: "rg -n \"deleteApplication|deleteService|deleteDatabase\" src/mcp/tools/intelligence.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
---

<objective>
Ship confirm-gated `intelligence.cleanup` (JANI-02 / D-13 / D-14) plus capability flags and README EN+DE parity (D-19).

Purpose: Destructive janitor mutations only behind published SAF confirm + safe-delete defaults; discoverability via system.version and docs.

Output: handleIntelligenceCleanup, capabilities, README notes, green cleanup + system tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-instance-intelligence/28-CONTEXT.md
@.planning/phases/28-instance-intelligence/28-RESEARCH.md
@.planning/phases/28-instance-intelligence/28-PATTERNS.md
@.planning/phases/28-instance-intelligence/28-03-SUMMARY.md
@src/mcp/tools/intelligence.ts
@src/mcp/tools/application.ts
@src/mcp/tools/service.ts
@src/mcp/tools/database.ts
@src/mcp/tools/emergency.ts
@src/mcp/capabilities.ts
@src/mcp/tools/system.test.ts
@README.md
@README.de.md
</context>

## Artifacts this phase produces

- Confirm-gated cleanup with per-item batch results and SAF-02 defaults.
- `intelligence_*` capability keys (coolify_min_version 4.1.2, MCP composite notes).
- README.md / README.de.md Instance intelligence section.
- Green cleanup + eleven-key system.version tests; coverage-map refreshed if needed.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm D-13 one-way cleanup confirm gate contract</name>
  <decision>Lock intelligence.cleanup as a published one-way SAF confirm contract: mutations require confirm:true; without it return COOLIFY_CONFIRM_REQUIRED (D-13). Undoing later breaks agent safety habits and SAF-01 parity.</decision>
  <context>
    CONTEXT.md already chose D-13 (reversibility: one-way). This checkpoint is the reversibility gate before implementing the wire contract. Prefer option confirm-required to honor locked decisions; soft-warn rejects SAF-01/D-13.
  </context>
  <options>
    <option id="confirm-required">
      <name>Require confirm:true (D-13 locked)</name>
      <pros>Matches SAF-01 / Phase 10; COOLIFY_CONFIRM_REQUIRED + recovery hints; agents cannot mute destruction</pros>
      <cons>One-way published contract — changing later breaks agent playbooks</cons>
    </option>
    <option id="soft-warn">
      <name>Warn-only without hard confirm gate</name>
      <pros>Slightly easier agent UX</pros>
      <cons>Rejects D-13 and SAF-01; high-severity destruction risk — do not choose</cons>
    </option>
  </options>
  <resume-signal>Select: confirm-required (expected) or soft-warn</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cleanup confirm gate + SAF-02 + domain delete reuse</name>
  <reversibility rating="one-way">D-13 confirm:true gate is a published safety contract; reversing breaks SAF-01 agent expectations.</reversibility>
  <files>src/mcp/tools/intelligence.ts, src/mcp/tools/intelligence.test.ts</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-CONTEXT.md (D-13, D-14, D-15)
    - .planning/phases/28-instance-intelligence/28-RESEARCH.md (Pattern 5 Cleanup, Pitfall 3 SAF-02)
    - .planning/phases/28-instance-intelligence/28-PATTERNS.md (validateConfirmGate + SAF-02 defaults + handler reuse)
    - src/mcp/tools/emergency.ts (validateConfirmGate ~181–196)
    - src/mcp/tools/application.ts (validateDeleteConfirm ~1180–1194; SAF-02 defaults ~2049–2054)
    - src/mcp/tools/service.ts / database.ts (delete handler entrypoints)
  </read_first>
  <behavior>
    - cleanup with confirm !== true throws/returns COOLIFY_CONFIRM_REQUIRED with RECOVERY_HINTS; delete handlers not called (JANI-02, D-13, T-28-01)
    - cleanup requires non-empty targets array of {type: application|service|database, uuid}; reject missing targets via schema/validation (T-28-03)
    - with confirm true, sequentially call matching handle*Action({action:'delete', uuid, confirm:true, delete_volumes, delete_configurations}) per target
    - omit delete_volumes/delete_configurations → pass false/false (D-14, SAF-02, T-28-02)
    - return results[] per uuid with ok/deleted/error for partial batch failure (discretion A4)
  </behavior>
  <action>
    After checkpoint resolves to confirm-required per D-13: implement handleIntelligenceCleanup. Gate with emergency-style validateConfirmGate or equivalent requiring confirm === true before any mutation. Apply delete_volumes = parsed.delete_volumes ?? false and delete_configurations = parsed.delete_configurations ?? false (D-14). Reuse exported handleApplicationAction / handleServiceAction / handleDatabaseAction — do not import raw delete* HTTP client fns into intelligence.ts (D-14). Prefer janitor preview before cleanup in safety footer / recovery hints (D-13). Enforce explicit targets only (no fleet-wide wipe action). Flip Wave 0 cleanup it.fails to it(). Scorecard/graph/impact/janitor stay read-only (D-15).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/intelligence.test.ts -t cleanup -x</automated>
  </verify>
  <acceptance_criteria>
    - Confirm-false test expects COOLIFY_CONFIRM_REQUIRED and zero delete handler calls (JANI-02, D-13, T-28-01)
    - Confirm-true without volume flags expects delete_volumes false and delete_configurations false forwarded (D-14, T-28-02)
    - Targets required; per-item results array present (T-28-03)
  </acceptance_criteria>
  <done>JANI-02 cleanup confirm gate + SAF-02 + handler reuse shipped.</done>
</task>

<task type="auto">
  <name>Task 3: Capabilities, coverage-map, README EN+DE (D-19)</name>
  <reversibility rating="costly">D-19 capability key names become system.version discovery surface.</reversibility>
  <files>src/mcp/capabilities.ts, src/mcp/tools/system.test.ts, docs/coverage-map.yaml, README.md, README.de.md</files>
  <read_first>
    - .planning/phases/28-instance-intelligence/28-CONTEXT.md (D-19)
    - .planning/phases/28-instance-intelligence/28-PATTERNS.md (capabilities + README analogs)
    - src/mcp/capabilities.ts (diagnose_logs entry)
    - src/mcp/tools/system.test.ts (CAPABILITY_KEYS)
    - README.md (capability table + diagnose.logs blockquote ~731–749)
    - README.de.md (matching DE section)
  </read_first>
  <action>
    Add five keys to COOLIFY_412_CAPABILITIES per D-19 discretion: intelligence_scorecard, intelligence_graph, intelligence_impact, intelligence_janitor, intelligence_cleanup — each supported true, coolify_min_version 4.1.2, note stating MCP composite on Coolify 4.1.x reads. Flip system.test.ts eleven-key assertions from it.fails to it(); update CAPABILITY_KEYS array. Ensure docs/coverage-map.yaml intelligence.* rows match client lists; run npm run openapi:coverage if project convention requires regen. Add README.md and README.de.md Instance intelligence rows/notes listing scorecard/graph/impact/janitor/cleanup and pointing at system.version capability keys — mirror diagnose.logs EN/DE structure. Do not invent Coolify REST paths.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/system.test.ts src/mcp/tools/intelligence.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - system.version exposes eleven capability keys including five intelligence_* (D-19)
    - README.md and README.de.md mention intelligence.scorecard (and sibling actions)
    - Full intelligence + system unit tests green
  </acceptance_criteria>
  <done>D-19 discoverability complete; Phase 28 surface documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → cleanup | Untrusted confirm/targets/delete flags — hard gate before mutation |
| intelligence.cleanup → domain delete handlers | Trusted internal reuse with SAF-02 defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | intelligence.cleanup confirm | high | mitigate | confirm === true required else COOLIFY_CONFIRM_REQUIRED (D-13); locked by checkpoint |
| T-28-02 | Destruction | delete_volumes / delete_configurations | high | mitigate | Default both false (SAF-02, D-14); tested |
| T-28-03 | Elevation | mass delete | high | mitigate | Explicit targets[] required; no fleet-wide wipe action |
| T-28-08 | Repudiation | batch cleanup results | low | mitigate | Per-item results[] with ok/error for auditability |
| T-28-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
`npx vitest run src/mcp/tools/intelligence.test.ts src/mcp/tools/system.test.ts -x` green; README EN+DE updated; confirm gate blocks unconfirmed cleanup.
</verification>

<success_criteria>
- JANI-02 satisfied with high-severity threats mitigated
- D-13 checkpoint resolved; D-14/D-15/D-19 done
- Phase 28 requirements INTEL/GRAPH/JANI all green across plans
</success_criteria>

<output>
Create `.planning/phases/28-instance-intelligence/28-04-SUMMARY.md` when done
</output>
