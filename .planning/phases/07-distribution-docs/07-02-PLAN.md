---
phase: 07-distribution-docs
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - README.md
  - README.de.md
  - docs/mcp.example.json
autonomous: true
requirements: [DIST-02]

must_haves:
  truths:
    - "README.md and README.de.md have identical H2 section structure (count + order + canonical EN↔DE slug map) per D-11 — translated German headings accepted, literal H2 string equality NOT required"
    - "Both READMEs contain a tool reference table covering all 10 registered tools AND all 32 action literals from src/mcp/tools/*.ts (D-09)"
    - "Both READMEs offer three install paths: deeplink, GitHub Pages configurator, manual config paste"
    - "README.md contains `docs/install.html` (Pages configurator link), `cursor://` (deeplink), and `vscode:mcp/install` (VS Code deeplink) per D-15/D-23; README.de.md mirrors at minimum `docs/install.html` per D-11"
    - "Both READMEs have a dedicated Safety section (## Safety EN / ## Sicherheit DE) covering emergency confirm and default masking / reveal opt-in"
    - "Neither README links to .planning/ or spike-findings or any internal planning file"
    - "Neither README markets Multi-Instance or documents instances.json as shipped"
    - "Neither README contains stale placeholders YOUR_ORG, 'LICENSE to be added', or the old coolify-mcp name"
    - "Both READMEs reference CONTRIBUTING.md for the maintainer publish workflow (no full publish steps inlined in README)"
    - "docs/mcp.example.json uses awesome-coolify-mcp via npx -y (not node dist/index.js)"
    - "vitest run tests/integration/docs-parity.test.ts is GREEN"
  artifacts:
    - README.md
    - README.de.md
    - docs/mcp.example.json
  key_links:
    - "README.md → docs/install.html (Pages configurator)"
    - "README.md → docs/mcp.example.json (manual paste template)"
    - "README.md → CONTRIBUTING.md (maintainer publish workflow)"
    - "README action table → src/mcp/tools/*.ts action literals (source of truth)"
---

## Phase Goal

**As a** community user, **I want to** install the MCP via `npx -y awesome-coolify-mcp` and read a complete GitHub README, **so that** I can adopt the MCP without reading source code.

<!-- gsd-write-continue -->

<objective>
Rewrite README.md (EN) and README.de.md (DE) with full structural parity (D-11), accurate action-based tool reference for all 10 registered tools (D-09), three equal install paths (D-15), dedicated Safety section (D-17), minimal v2 note (D-12), and no internal planning links (D-13). Fix stale Phase-1-only claims (D-14). Update docs/mcp.example.json to awesome-coolify-mcp / npx. Turn the Wave 0 docs-parity test GREEN.

Purpose: DIST-02 requires a community README that lets users adopt the MCP without reading source. The current README is stale (Phase-1-only tool table, multi-instance-as-v2, .planning links, YOUR_ORG placeholder, "LICENSE to be added") and ships the wrong package name.

Output: Rewritten `README.md`, `README.de.md`, `docs/mcp.example.json`; docs-parity test GREEN.
</objective>

<execution_context>
@/Users/puzzless/gstack/.cursor/gsd-core/workflows/execute-plan.md
@/Users/puzzless/gstack/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distribution-docs/07-CONTEXT.md
@.planning/phases/07-distribution-docs/07-RESEARCH.md
@.planning/phases/07-distribution-docs/07-VALIDATION.md
@.planning/phases/07-distribution-docs/07-01-SUMMARY.md
@README.md
@README.de.md
@docs/mcp.example.json
@src/mcp/server.ts
@src/mcp/tools/system.ts
@src/mcp/tools/meta.ts
@src/mcp/tools/resource.ts
@src/mcp/tools/diagnose.ts
@src/mcp/tools/application.ts
@src/mcp/tools/deployment.ts
@src/mcp/tools/service.ts
@src/mcp/tools/database.ts
@src/mcp/tools/docs.ts
@src/mcp/tools/emergency.ts
@src/config/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md (EN) with full tool table + 3 install paths + Safety section (D-09, D-14, D-15, D-17)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current stale state — Phase-1-only tool table, multi-instance-as-v2, YOUR_ORG, "LICENSE to be added", .planning links)
    - src/mcp/server.ts (10 registered tools and their descriptions)
    - src/mcp/tools/system.ts, meta.ts, resource.ts, diagnose.ts, application.ts, deployment.ts, service.ts, database.ts, docs.ts, emergency.ts (action literals — source of truth for the tool table per D-09)
    - src/config/env.ts (COOLIFY_ENV_KEYS + formatEnvLoadHint — env contract to document)
    - .planning/phases/07-distribution-docs/07-RESEARCH.md (Complete Action-Based Tool Action Inventory table + Multi-Client Matrix Configuration Blocks)
    - .planning/phases/07-distribution-docs/07-CONTEXT.md (D-09 through D-23 locked decisions)
    - docs/mcp.example.json (current stale Cursor-only example)
    - LICENSE (verify MIT + clezcoding 2026 before referencing "MIT — see LICENSE")
  </read_first>
  <action>
    Rewrite README.md from scratch with the following H2 section order (exact titles — README.de.md must match per D-11):
    1. `## Why awesome-coolify-mcp?` — rebrand from coolify-mcp; keep the "three overlapping MCP implementations" framing; drop "Multi-instance (v2)" bullet per D-10; add "Action-based tools across 10 domains" bullet
    2. `## Quick start` — prerequisites (Node 20+, Coolify 4.1.x + API token); `npx -y awesome-coolify-mcp` as primary invoke per D-16; short safety warning per D-17 ("Emergency actions require confirm:true; secrets masked by default")
    3. `## Install` — three equal subsections per D-15:
       - `### One-click (deeplink)` — Cursor deeplink badge (placeholder env OK per D-23) + VS Code `vscode:mcp/install` badge; link to GitHub Pages configurator for filling secrets first
       - `### Configurator (GitHub Pages)` — link to `docs/install.html` (Pages configurator); short note: tokens stay in your browser, never posted to a backend per D-19; explain how to obtain COOLIFY_URL (your Coolify base URL) and COOLIFY_TOKEN (Coolify UI → Keys & Tokens → API Token)
       - `### Manual` — paste JSON/TOML/YAML into client config; show Cursor mcpServers example using `npx -y awesome-coolify-mcp`; link to `docs/mcp.example.json`
       - D-18 note (one short line near the Claude Desktop mention in the Clients subsection or here in Install): "D-18: No Claude Desktop `.mcpb` packaging in Phase 7 — Claude Desktop = manual JSON paste or Pages configurator output only."
    4. `## Clients` — short per D-20: top clients (Cursor, VS Code / GitHub Copilot, Claude Desktop, Claude Code, Windsurf) with badges + one-line each; link to `docs/install.html` for the full 15+ client matrix per D-21
    5. `## Environment variables` — table of COOLIFY_URL (required), COOLIFY_TOKEN (required), COOLIFY_VERIFY_SSL (default true), COOLIFY_MCP_LOG (debug/info/error, default info) from src/config/env.ts
    6. `## Tools (action-based)` — intro sentence; then the COMPLETE action table per D-09 with one row per (tool, action) pair from `src/mcp/tools/*.ts`. Use the inventory in 07-RESEARCH.md § Complete Action-Based Tool Action Inventory as the source of truth, cross-checked against the Zod `z.literal('...')` action literals in `src/mcp/tools/*.ts`. Columns: Tool | Action | Purpose. Cover all 10 tools AND all 32 action literals: system (health, version, verify, infrastructure_overview), meta (version), resource (list, find), diagnose (app, server, scan), application (start, stop, restart, deploy, logs, get), deployment (list, get, cancel), service (start, stop, restart, deploy, get), database (start, stop, restart, get), docs (search), emergency (stop_all, redeploy_project, restart_project). Mark emergency actions as "requires confirm:true". Every action literal must appear verbatim in the README body (the docs-parity test greps for each literal).
    7. `## Safety` — dedicated section per D-17: emergency actions (stop_all, redeploy_project, restart_project) require explicit `confirm: true`; call without confirm first to preview would_affect; default masking of sensitive keys (password/token/secret/private/env) as `***`; `reveal: true` opt-in for plaintext; log line content is NOT masked — warn agents not to persist logs
    8. `## Structured errors` — table of COOLIFY_401/404/422/500/NETWORK/TIMEOUT with meaning; note transient retry with exponential backoff (1s→2s→4s, 3×)
    9. `## Not in v1` — one sentence per D-12: "Create/delete CRUD, multi-instance runtime, env-var sync, and full parity with legacy MCPs arrive in v2." Do NOT link .planning/ per D-13
    10. `## Development` — `npm install`, `npm run build`, `npm test`, `npm run dev`; note logs go to stderr only; pointer to `CONTRIBUTING.md` for maintainer publish workflow (`npm run build` → `npm pack --dry-run` → `npm publish --access public`) per D-01 — do NOT inline the full publish steps here, just link CONTRIBUTING.md
    11. `## License` — `MIT — see LICENSE` (LICENSE exists; remove "to be added" placeholder per D-14)
    Branding: title `awesome-coolify-mcp`; logo alt `awesome-coolify-mcp logo`; keep docs/assets/logo.png and social-preview.png references. Remove all `.planning/` and `spike-findings` links per D-13. Remove `YOUR_ORG` per D-14. Remove "Multi-Instance (v2)" marketing per D-10. Do NOT document instances.json per D-10.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && node -e "
const fs=require('fs');
const r=fs.readFileSync('README.md','utf8');
const h2=r.split('\n').filter(l=>l.startsWith('## '));
const assert=(cond,msg)=>{if(!cond){console.error('FAIL: '+msg);process.exit(1)}};
assert(h2.length===11,'H2 count === 11, got '+h2.length);
const toolActions={system:['health','version','verify','infrastructure_overview'],meta:['version'],resource:['list','find'],diagnose:['app','server','scan'],application:['start','stop','restart','deploy','logs','get'],deployment:['list','get','cancel'],service:['start','stop','restart','deploy','get'],database:['start','stop','restart','get'],docs:['search'],emergency:['stop_all','redeploy_project','restart_project']};
let totalActions=0;
for(const[tool,actions]of Object.entries(toolActions)){
  assert(r.includes(tool),'tool '+tool+' present');
  for(const a of actions){assert(r.includes(a),'action '+tool+'.'+a+' present'); totalActions++}
}
assert(totalActions===32,'total action literals === 32, got '+totalActions);
assert((r.match(/awesome-coolify-mcp/g)||[]).length>=10,'awesome-coolify-mcp count >= 10');
assert((r.match(/(?<![\w-])coolify-mcp(?![\w-])/g)||[]).length===0,'old name coolify-mcp count === 0 (word-boundary regex, does not match substring inside awesome-coolify-mcp)');
assert((r.match(/\.planning\//g)||[]).length===0,'.planning/ count === 0');
assert((r.match(/YOUR_ORG/g)||[]).length===0,'YOUR_ORG count === 0');
assert((r.match(/instances\.json/g)||[]).length===0,'instances.json count === 0');
assert(r.includes('## Safety'),'Safety H2 present');
assert(r.includes('confirm')&&r.includes('reveal'),'confirm + reveal present');
assert(/### One-click|### One-Click/.test(r),'EN Install: One-click subsection present');
assert(/### Configurator/.test(r),'EN Install: Configurator subsection present');
assert(/### Manual/.test(r),'EN Install: Manual subsection present');
assert(r.includes('CONTRIBUTING.md'),'EN references CONTRIBUTING.md for publish workflow');
assert(r.includes('docs/install.html'),'EN D-15: docs/install.html configurator link present');
assert(/cursor:\/\//.test(r),'EN D-15/D-23: cursor:// deeplink present');
assert(/vscode:mcp\/install/.test(r),'EN D-15/D-23: vscode:mcp/install deeplink present');
console.log('README.md OK');
"</automated>
  </verify>
  <done>
    - README.md has exactly 11 H2 sections in the order listed
    - `awesome-coolify-mcp` appears ≥10 times; `coolify-mcp` (old name, word-boundary regex `/(?<![\w-])coolify-mcp(?![\w-])/g`) appears 0 times
    - `.planning/` link count is 0
    - `YOUR_ORG` count is 0
    - `instances.json` count is 0 (not documented as shipped)
    - All 10 tool names AND all 32 action literals appear in the README body
    - Three install subsections present (### One-click / ### Configurator / ### Manual)
    - Safety section (`## Safety`) covers confirm and reveal
    - References `CONTRIBUTING.md` for the maintainer publish workflow
    - LICENSE referenced as existing (no "to be added")
    - `docs/install.html` configurator link present (D-15)
    - `cursor://` deeplink present (D-15/D-23)
    - `vscode:mcp/install` deeplink present (D-15/D-23)
    - Verify command prints `README.md OK` (exits non-zero on any assertion failure)
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.de.md (DE) with full parity to README.md (D-11)</name>
  <files>README.de.md</files>
  <read_first>
    - README.md (after Task 1 — exact H2 section structure and tool table to mirror)
    - README.de.md (current stale DE state)
    - src/mcp/tools/*.ts (action literals — same source of truth as EN)
    - .planning/phases/07-distribution-docs/07-CONTEXT.md (D-11 parity decision)
  </read_first>
  <action>
    Rewrite README.de.md to mirror README.md section-by-section and table-by-table per D-11 (structural parity, not literal H2 string equality — translated German headings are expected). Use the identical 11 H2 section titles translated to German (canonical EN↔DE map):
    1. `## Warum awesome-coolify-mcp?`
    2. `## Schnellstart`
    3. `## Installation` — three subsections `### One-Click (Deeplink)`, `### Konfigurator (GitHub Pages)`, `### Manuell`
    4. `## Clients`
    5. `## Umgebungsvariablen`
    6. `## Tools (aktionsbasiert)` — identical tool table with all 10 tools and every one of the 32 action literals; German one-line purposes
    7. `## Sicherheit` — confirm + reveal opt-in + log line content warning
    8. `## Strukturierte Fehler` — COOLIFY_401/404/422/500/NETWORK/TIMEOUT table
    9. `## Nicht in v1` — one sentence per D-12
    10. `## Entwicklung` — pointer to `CONTRIBUTING.md` for maintainer publish workflow (do NOT inline full publish steps)
    11. `## Lizenz` — `MIT — siehe LICENSE`
    Keep branding identical (logo, social-preview, badges). Keep `awesome-coolify-mcp` package name and `npx -y awesome-coolify-mcp` command UNCHANGED in German text (technical identifiers stay English). Keep all `.planning/` and `spike-findings` links absent per D-13. Keep `YOUR_ORG` and `instances.json` absent. Translate prose to German; keep code blocks, JSON, TOML, YAML, and CLI commands identical to EN. The docs-parity test (Wave 0 scaffold from Plan 01) compares H2 structure via a canonical-slug map (NOT literal EN strings in the DE file) and asserts every action literal is present — both must pass.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && node -e "
const fs=require('fs');
const en=fs.readFileSync('README.md','utf8');
const de=fs.readFileSync('README.de.md','utf8');
const enH2=en.split('\n').filter(l=>l.startsWith('## '));
const deH2=de.split('\n').filter(l=>l.startsWith('## '));
const assert=(cond,msg)=>{if(!cond){console.error('FAIL: '+msg);process.exit(1)}};
assert(deH2.length===11,'DE H2 count === 11, got '+deH2.length);
assert(enH2.length===deH2.length,'EN/DE H2 count parity, en='+enH2.length+' de='+deH2.length);
const canonical=[
  ['Why awesome-coolify-mcp?','Warum awesome-coolify-mcp?'],
  ['Quick start','Schnellstart'],
  ['Install','Installation'],
  ['Clients','Clients'],
  ['Environment variables','Umgebungsvariablen'],
  ['Tools (action-based)','Tools (aktionsbasiert)'],
  ['Safety','Sicherheit'],
  ['Structured errors','Strukturierte Fehler'],
  ['Not in v1','Nicht in v1'],
  ['Development','Entwicklung'],
  ['License','Lizenz']
];
for(let i=0;i<canonical.length;i++){
  assert(enH2[i]==='## '+canonical[i][0],'EN H2['+i+'] === ## '+canonical[i][0]+', got '+enH2[i]);
  assert(deH2[i]==='## '+canonical[i][1],'DE H2['+i+'] === ## '+canonical[i][1]+', got '+deH2[i]);
}
const toolActions={system:['health','version','verify','infrastructure_overview'],meta:['version'],resource:['list','find'],diagnose:['app','server','scan'],application:['start','stop','restart','deploy','logs','get'],deployment:['list','get','cancel'],service:['start','stop','restart','deploy','get'],database:['start','stop','restart','get'],docs:['search'],emergency:['stop_all','redeploy_project','restart_project']};
let totalActions=0;
for(const[tool,actions]of Object.entries(toolActions)){
  assert(de.includes(tool),'DE tool '+tool+' present');
  for(const a of actions){assert(de.includes(a),'DE action '+tool+'.'+a+' present'); totalActions++}
}
assert(totalActions===32,'DE total action literals === 32, got '+totalActions);
assert((de.match(/(?<![\w-])coolify-mcp(?![\w-])/g)||[]).length===0,'DE old name count === 0 (word-boundary regex, does not match substring inside awesome-coolify-mcp)');
assert((de.match(/\.planning\//g)||[]).length===0,'DE .planning/ count === 0');
assert((de.match(/YOUR_ORG/g)||[]).length===0,'DE YOUR_ORG count === 0');
assert((de.match(/instances\.json/g)||[]).length===0,'DE instances.json count === 0');
assert(de.includes('## Sicherheit'),'DE Sicherheit H2 present');
assert(de.includes('confirm')&&de.includes('reveal'),'DE confirm + reveal present');
assert(/### One-Click|### One-click/.test(de),'DE Installation: One-Click subsection present');
assert(/### Konfigurator/.test(de),'DE Installation: Konfigurator subsection present');
assert(/### Manuell/.test(de),'DE Installation: Manuell subsection present');
assert(de.includes('CONTRIBUTING.md'),'DE references CONTRIBUTING.md for publish workflow');
assert(de.includes('docs/install.html'),'DE D-15: docs/install.html configurator link present (mirrored from EN)');
console.log('README.de.md OK');
"</automated>
  </verify>
  <done>
    - README.de.md has exactly 11 H2 sections matching the canonical EN↔DE title map position-by-position
    - EN and DE H2 counts are equal (11 each)
    - All 10 tool names AND all 32 action literals present in the DE body
    - `coolify-mcp` (old name, word-boundary regex `/(?<![\w-])coolify-mcp(?![\w-])/g`) count is 0 in DE
    - `.planning/` link count is 0 in DE
    - `YOUR_ORG` count is 0 in DE
    - `instances.json` count is 0 in DE
    - Three install subsections present in DE (### One-Click / ### Konfigurator / ### Manuell)
    - Safety section (`## Sicherheit`) in DE covers confirm and reveal
    - References `CONTRIBUTING.md` for the maintainer publish workflow
    - `docs/install.html` configurator link present in DE (mirrored from EN per D-11/D-15)
    - Verify command prints `README.de.md OK` (exits non-zero on any assertion failure)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update docs/mcp.example.json to awesome-coolify-mcp / npx; turn docs-parity test GREEN</name>
  <files>docs/mcp.example.json</files>
  <read_first>
    - docs/mcp.example.json (current stale Cursor-only example using node dist/index.js)
    - README.md (after Task 1 — Manual install section references this file)
    - package.json (after Plan 01 — bin name awesome-coolify-mcp)
    - tests/integration/docs-parity.test.ts (Wave 0 scaffold from Plan 01 — must turn GREEN)
  </read_first>
  <action>
    Rewrite `docs/mcp.example.json` to use the published package via npx:
    - `command`: `npx`
    - `args`: `["-y", "awesome-coolify-mcp"]`
    - `env`: `COOLIFY_URL` (placeholder `https://coolify.example.com`), `COOLIFY_TOKEN` (placeholder `YOUR_COOLIFY_API_TOKEN`), `COOLIFY_VERIFY_SSL` (`"true"`), `COOLIFY_MCP_LOG` (`"info"`)
    - Keep the Cursor `mcpServers` shape (single-server example) — multi-client examples live in docs/install.html (Plan 03)
    - Rename the server key from `coolify` to `awesome-coolify-mcp` for consistency with the package name
    Then run `npx vitest run tests/integration/docs-parity.test.ts` and confirm GREEN. If any test fails, fix the README content (not the test) — the test is the spec. Per D-13, ensure docs/mcp.example.json contains no `.planning/` references.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && node -e "const c=require('./docs/mcp.example.json'); const s=c.mcpServers['awesome-coolify-mcp']; console.assert(s && s.command==='npx','command'); console.assert(JSON.stringify(s.args)===JSON.stringify(['-y','awesome-coolify-mcp']),'args'); console.assert(s.env.COOLIFY_URL,'url'); console.assert(s.env.COOLIFY_TOKEN,'token'); console.log('mcp.example.json OK')" && npx vitest run tests/integration/docs-parity.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - docs/mcp.example.json uses `npx -y awesome-coolify-mcp` (not `node dist/index.js`)
    - Server key is `awesome-coolify-mcp`
    - env block has COOLIFY_URL, COOLIFY_TOKEN, COOLIFY_VERIFY_SSL, COOLIFY_MCP_LOG
    - `npx vitest run tests/integration/docs-parity.test.ts` exits 0 (GREEN)
    - No `.planning/` references in docs/mcp.example.json
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub README → community reader | Public doc; users copy-paste config snippets and run npx |
| docs/mcp.example.json → user config | Template users paste into their client config; placeholder secrets must never be real |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Information Disclosure | README links to .planning/ | high | mitigate | Remove all `.planning/` and `spike-findings` links from README.md and README.de.md per D-13; docs-parity test negative-greps for `.planning/` and `spike-findings` — keeps internal planning docs out of community-facing README. |
| T-07-05 | Spoofing | Stale package name `coolify-mcp` in README | high | mitigate | Replace all `coolify-mcp` references with `awesome-coolify-mcp` per D-02; docs-parity test negative-greps for the old name — users running `npx coolify-mcp` would hit a typosquatter or 404. |
| T-07-06 | Information Disclosure | `instances.json` documented as shipped | medium | mitigate | Do NOT document `instances.json` as a v1 feature per D-10; docs-parity test negative-greps for `instances.json` — prevents users expecting multi-instance runtime that does not exist in v1 code. |
</threat_model>

<verification>
- `npx vitest run tests/integration/docs-parity.test.ts` is GREEN (all 6 tests pass — structural parity via canonical-slug map, full 32-action inventory, locale-aware Safety section, no .planning links, no stale placeholders)
- README.md and README.de.md have matching H2 section counts (11 each) and canonical EN↔DE title parity position-by-position
- Both READMEs contain all 10 tool names AND all 32 action literals
- Neither README contains `.planning/`, `YOUR_ORG`, `instances.json`, or the old `coolify-mcp` name
- Both READMEs reference CONTRIBUTING.md for the publish workflow
- README.md contains `docs/install.html`, `cursor://`, and `vscode:mcp/install` (D-15/D-23); README.de.md contains at minimum `docs/install.html` (D-11 mirror)
- docs/mcp.example.json uses `npx -y awesome-coolify-mcp`
- `npm run build && npm test` still green (no source code touched)
</verification>

<success_criteria>
- README.md and README.de.md are rewritten with full structural parity via canonical-slug map (D-11)
- All 10 registered tools with all 32 action literals are documented (D-09)
- Three install paths (deeplink, Pages configurator, manual) are first-class (D-15); README.md contains `docs/install.html` + `cursor://` + `vscode:mcp/install` deeplink/configurator links, README.de.md mirrors `docs/install.html`
- Dedicated Safety section (## Safety / ## Sicherheit) covers confirm and reveal opt-in (D-17)
- No `.planning/` or spike-findings links in either README (D-13)
- No Multi-Instance marketing; no instances.json as shipped (D-10)
- No stale placeholders (YOUR_ORG, "LICENSE to be added") (D-14)
- Both READMEs reference CONTRIBUTING.md for maintainer publish workflow (D-01)
- docs/mcp.example.json updated to awesome-coolify-mcp / npx
- Wave 0 docs-parity test is GREEN
</success_criteria>

<output>
Create `.planning/phases/07-distribution-docs/07-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `README.md` — rewritten community README in English with 11 H2 sections (Why, Quick start, Install, Clients, Environment variables, Tools, Safety, Structured errors, Not in v1, Development, License), full 10-tool / 32-action table, 3 install paths, dedicated Safety section, pointer to CONTRIBUTING.md
- `README.de.md` — German translation with full structural parity to README.md per D-11 (canonical-slug map, translated H2 headings accepted)
- `docs/mcp.example.json` — updated Cursor-shaped example using `npx -y awesome-coolify-mcp` with server key `awesome-coolify-mcp`
- `tests/integration/docs-parity.test.ts` — Wave 0 scaffold turns GREEN (test file itself is owned by Plan 01; Plan 02 makes its assertions pass by rewriting both READMEs)
