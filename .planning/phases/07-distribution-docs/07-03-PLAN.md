---
phase: 07-distribution-docs
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - docs/index.html
  - docs/install.html
  - tests/integration/install-configurator.test.ts
autonomous: true
requirements: [DIST-02]

must_haves:
  truths:
    - "docs/install.html is a static client-side HTML form that takes COOLIFY_URL + COOLIFY_TOKEN + client selector and emits a per-client config snippet"
    - "docs/install.html supports at least 15 MCP clients with per-client format adapters (Cursor mcpServers, VS Code servers+inputs, Claude Desktop mcpServers, Claude Code stdio, Windsurf mcpServers, OpenCode type:local+command array, Codex TOML, Gemini mcpServers, Antigravity mcpServers, OpenClaw mcp/servers, Hermes YAML mcp_servers:, Kimi mcpServers, Cline mcpServers, Goose YAML mcpServers:, LM Studio mcpServers)"
    - "docs/install.html source contains static D-22 adapter markers proving multi-format emission (BLOCKER — not just client name substrings): Cursor `mcpServers` object shape, VS Code `\"inputs\"` + `${input:` substitution, OpenCode/Kilo `\"type\": \"local\"`, Codex `[mcp_servers.` TOML section, Hermes YAML `mcp_servers:` block (snake_case, Hermes-only), Goose YAML `mcpServers:` block (camelCase, Goose-only), OpenClaw `mcp.servers`/`mcp/servers` shape — all 7 asserted separately by install-configurator.test.ts Test 10 and Task 1 verify (Hermes and Goose keys MUST NOT share a single regex; each key is asserted independently)"
    - "docs/install.html performs no backend POST/fetch — all generation happens in-browser; tokens never leave the browser per D-19"
    - "docs/install.html loads no external scripts — no `<script src=` patterns (T-07-09 mitigation); all JS inline"
    - "docs/install.html generates Cursor deeplink (cursor://anysphere.cursor-deeplink/mcp/install?name=...&config=base64) and VS Code vscode:mcp/install badge client-side"
    - "docs/index.html is a landing page that links to the three install paths (deeplink, configurator, manual) per D-15; copy mirrors post-Plan-02 README"
    - "No community doc (docs/*.html) links to .planning/ or spike-findings per D-13"
    - "tests/integration/install-configurator.test.ts validates configurator HTML files exist, contain no .planning links, contain no external fetch/POST patterns, and contain no `<script src=` patterns"
  artifacts:
    - docs/index.html
    - docs/install.html
    - tests/integration/install-configurator.test.ts
  key_links:
    - "README.md (post-Plan-02) → docs/install.html (Pages configurator link from Plan 02)"
    - "docs/install.html → per-client config snippet generators (15+ adapters)"
    - "docs/install.html → Cursor deeplink + VS Code vscode:mcp/install badge generators"
---

## Phase Goal

**As a** community user, **I want to** install the MCP via `npx -y awesome-coolify-mcp` and read a complete GitHub README, **so that** I can adopt the MCP without reading source code.

<!-- gsd-write-continue -->

<objective>
Build a static client-side GitHub Pages configurator at `docs/install.html` that generates per-client MCP config snippets (JSON/TOML/YAML) and deeplinks for 15+ MCP-capable clients, plus a landing page at `docs/index.html` linking the three install paths. No backend; tokens never leave the browser (D-19). Per-client format adapters per D-22. Broad client matrix per D-21.

Purpose: DIST-02 requires users to adopt the MCP without reading source. The README (Plan 02) links to this configurator for the full 15+ client matrix and for filling secrets before generating deeplinks (D-23). Three equal install paths (D-15) need the Pages configurator to be first-class.

Output: `docs/index.html`, `docs/install.html`, `tests/integration/install-configurator.test.ts`.
</objective>

<execution_context>
@/Users/puzzless/gstack/.cursor/gsd-core/workflows/execute-plan.md
@/Users/puzzless/gstack/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distribution-docs/07-CONTEXT.md
@.planning/phases/07-distribution-docs/07-RESEARCH.md
@.planning/phases/07-distribution-docs/07-VALIDATION.md
@.planning/phases/07-distribution-docs/07-01-SUMMARY.md
@.planning/phases/07-distribution-docs/07-02-SUMMARY.md
@README.md
@README.de.md
@docs/mcp.example.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/install.html — client-side configurator with 15+ per-client format adapters (D-19, D-21, D-22)</name>
  <files>docs/install.html</files>
  <read_first>
    - .planning/phases/07-distribution-docs/07-RESEARCH.md (§ Multi-Client Matrix Configuration Blocks — verified JSON/TOML/YAML shapes for all 15+ clients; § Code Examples → Safe browser-based Cursor deeplink generation)
    - .planning/phases/07-distribution-docs/07-CONTEXT.md (D-19 client-side only, D-21 broad matrix, D-22 per-client adapters, D-23 deeplink badges)
    - docs/mcp.example.json (after Plan 02 — Cursor shape reference)
    - README.md (after Plan 02 — link target; mirror the post-Plan-02 README install/clients wording so the Pages configurator landing copy stays consistent with the README)
  </read_first>
  <action>
    Create `docs/install.html` as a single static HTML file with inline CSS and vanilla JS (no build step, no external frameworks, no fetch/XHR). Structure:
    - Form inputs: `COOLIFY_URL` (text, placeholder `https://coolify.example.com`), `COOLIFY_TOKEN` (password, placeholder), client selector (dropdown with 15+ clients), optional `COOLIFY_VERIFY_SSL` (checkbox default true), optional `COOLIFY_MCP_LOG` (select debug/info/error default info)
    - Client dropdown options (per D-21): Cursor, VS Code / GitHub Copilot, Claude Desktop, Claude Code, Windsurf, OpenCode, Codex CLI, Gemini CLI, Google Antigravity, OpenClaw, Hermes Agent, Kimi Code, Kilo Code, Cline, Goose, LM Studio
    - D-18 note near the Claude Desktop entry in the client dropdown/help text: "D-18: No Claude Desktop .mcpb packaging in Phase 7 — Claude Desktop = manual JSON paste or Pages configurator output only." (Do NOT emit a `.mcpb` bundle or document one.)
    - On submit (client-side only — `addEventListener('submit', e => e.preventDefault())`), generate a config snippet in the per-client format per D-22:
      - Cursor / Claude Desktop / Claude Code / Gemini / Antigravity / Kimi / Cline / LM Studio: JSON `mcpServers` shape with `command: npx`, `args: ["-y", "awesome-coolify-mcp"]`, `env: {COOLIFY_URL, COOLIFY_TOKEN, ...}`
      - VS Code / GitHub Copilot: JSON with `inputs` (promptString for coolify_url, password promptString for coolify_token) + `servers` block using `${input:coolify_url}` / `${input:coolify_token}` per D-22
      - OpenCode / Kilo Code: JSON `mcp` block with `type: local`, `command: ["npx", "-y", "awesome-coolify-mcp"]`, `enabled: true`, `environment: {...}`
      - Codex CLI: TOML `[mcp_servers.awesome-coolify-mcp]` with `command = "npx"`, `args = ["-y", "awesome-coolify-mcp"]`, `enabled = true`, and `[mcp_servers.awesome-coolify-mcp.env]` block
      - Hermes Agent: YAML `mcp_servers:` (snake_case — Hermes-only key) with `command: "npx"`, `args: ["-y", "awesome-coolify-mcp"]`, `env:` block
      - Goose: YAML `mcpServers:` (camelCase — Goose-only key, NOT the same as Hermes) with `command: "npx"`, `args: ["-y", "awesome-coolify-mcp"]`, `env:` block. Per RESEARCH.md § Multi-Client Matrix → Goose (`~/.config/goose/config.yaml`), Goose uses `mcpServers:` (camelCase); Hermes uses `mcp_servers:` (snake_case). The two YAML keys MUST be emitted as separate literals and asserted by separate regexes — do NOT collapse them into a single `mcp_servers:` regex that would only catch Hermes.
      - OpenClaw: JSON `mcp.servers` with `command: ["npx", "-y", "awesome-coolify-mcp"]`, `environment: {...}` (and display the `openclaw mcp set` CLI snippet as an alternative)
    - Deeplink generators (separate output blocks):
      - Cursor deeplink: `cursor://anysphere.cursor-deeplink/mcp/install?name=${encodeURIComponent('awesome-coolify-mcp')}&config=${btoa(JSON.stringify({ 'awesome-coolify-mcp': { command: 'npx', args: ['-y', 'awesome-coolify-mcp'], env: envVars } }))}` — use the verified shape from 07-RESEARCH.md § Code Examples
      - VS Code: `vscode:mcp/install?config=${encodeURIComponent(JSON.stringify({ inputs: [...], servers: {...} }))}` (or `vscode.dev/redirect/?url=...` per current VS Code MCP docs — use the verified form from 07-RESEARCH.md)
    - Output area: `<pre><code>` block with the generated snippet; copy-to-clipboard button; separate deeplink output blocks for Cursor and VS Code
    - Security notice near the form: "All generation happens in your browser. Your URL and token never leave this page." per D-19
    - CRITICAL: no `fetch(`, no `XMLHttpRequest`, no `<form action>`, no external script src, no analytics, no third-party CDNs. All JS inline. Per D-19, tokens must not appear in shareable URLs (deeplinks use placeholder env or the form values only when the user explicitly clicks "generate deeplink with my values" — default uses placeholders per D-23).
    - Link back to README.md and to docs/index.html
    - No `.planning/` or `spike-findings` references per D-13
    - CRITICAL — D-22 adapter markers (BLOCKER): the HTML source MUST contain static markers proving per-client multi-format emission (not just client name substrings). The install-configurator.test.ts Test 10 and the Task 1 verify both grep for these literals, so each adapter generator must emit the literal marker into the source:
      - Cursor generator: emit a `mcpServers` object shape (e.g. `const cursorConfig = { mcpServers: { ... } }`)
      - VS Code generator: emit `"inputs"` array AND `${input:coolify_url}` / `${input:coolify_token}` substitution tokens
      - OpenCode/Kilo generator: emit `"type": "local"` with a `command` array
      - Codex generator: emit a TOML `[mcp_servers.awesome-coolify-mcp]` section literal
      - Hermes generator: emit a YAML `mcp_servers:` (snake_case) block literal — Hermes-only key
      - Goose generator: emit a YAML `mcpServers:` (camelCase) block literal — Goose-only key, distinct from Hermes. The Hermes `mcp_servers:` and Goose `mcpServers:` literals MUST both appear in source as separate, independently-greppable tokens (do NOT share a single regex).
      - OpenClaw generator: emit a `mcp.servers` (or `mcp/servers`) shape literal
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && node -e "
const fs=require('fs');
const r=fs.readFileSync('docs/install.html','utf8');
const assert=(cond,msg)=>{if(!cond){console.error('FAIL: '+msg);process.exit(1)}};
assert(r.length>0,'install.html non-empty');
const clients=['Cursor','VS Code','Claude Desktop','Claude Code','Windsurf','OpenCode','Codex CLI','Gemini CLI','Antigravity','OpenClaw','Hermes','Kimi','Kilo','Cline','Goose','LM Studio'];
const present=clients.filter(c=>r.includes(c));
assert(present.length>=15,'client count >= 15, got '+present.length+' ('+present.join(', ')+')');
assert((r.match(/fetch\(|XMLHttpRequest|<form[^>]+action/g)||[]).length===0,'no fetch/XHR/form-action (D-19)');
assert((r.match(/<script\s+src=/g)||[]).length===0,'no external <script src> (T-07-09)');
assert((r.match(/\.planning\/|spike-findings/g)||[]).length===0,'no .planning/ or spike-findings links (D-13)');
assert(r.includes('npx -y awesome-coolify-mcp'),'npx command present');
assert(r.includes('cursor://anysphere'),'Cursor deeplink protocol present');
assert(/vscode:mcp\/install|vscode\.dev\/redirect/.test(r),'VS Code install URL present');
assert(/browser/i.test(r)&&/never leave/i.test(r),'security notice present');
console.log('--- D-22 per-client format adapter markers (BLOCKER: proves multi-format emission, not just client name substrings):');
const adapterMarkers=[
  {label:'Cursor mcpServers object shape',re:/mcpServers\s*[:=]/},
  {label:'VS Code inputs + ${input:}',re:/["']inputs["'].*\$\{input:/s},
  {label:'OpenCode/Kilo type:local',re:/["']type["']\s*:\s*["']local["']/},
  {label:'Codex TOML [mcp_servers.',re:/\[mcp_servers\./},
  {label:'Hermes YAML mcp_servers: (snake_case, Hermes-only)',re:/mcp_servers\s*:/},
  {label:'Goose YAML mcpServers: (camelCase, Goose-only)',re:/mcpServers\s*:/},
  {label:'OpenClaw mcp.servers shape',re:/mcp\.servers|mcp\/servers/},
];
for(const m of adapterMarkers){
  assert(m.re.test(r),'D-22 adapter marker missing: '+m.label);
  console.log('  OK: '+m.label);
}
// CRITICAL — Hermes (mcp_servers:) and Goose (mcpServers:) MUST both be present as separate literals.
assert(/mcp_servers\s*:/.test(r),'Hermes YAML mcp_servers: (snake_case) MUST be present as a separate literal');
assert(/mcpServers\s*:/.test(r),'Goose YAML mcpServers: (camelCase) MUST be present as a separate literal — a single mcp_servers: regex is NOT sufficient (it would miss Goose)');
console.log('install.html OK');
"</automated>
  </verify>
  <done>
    - docs/install.html exists as a single static HTML file with inline CSS+JS
    - Client dropdown lists ≥15 distinct clients
    - No `fetch(`, `XMLHttpRequest`, or `<form action>` patterns (client-side only per D-19)
    - No `.planning/` or `spike-findings` references per D-13
    - `npx -y awesome-coolify-mcp` appears in the generated snippets
    - Cursor deeplink protocol `cursor://anysphere` present
    - VS Code `vscode:mcp/install` (or `vscode.dev/redirect`) present
    - Security notice about client-side only is visible
    - D-22 per-client format adapter markers present in source (BLOCKER — proves multi-format emission, not just client name substrings):
      - Cursor: `mcpServers` object shape (`/mcpServers\s*[:=]/`)
      - VS Code: `"inputs"` AND `${input:` (`/["']inputs["'].*\$\{input:/s`)
      - OpenCode/Kilo: `"type": "local"` (`/["']type["']\s*:\s*["']local["']/`)
      - Codex: `[mcp_servers.` TOML section (`/\[mcp_servers\./`)
      - Hermes: YAML `mcp_servers:` snake_case (`/mcp_servers\s*:/`) — Hermes-only key, asserted independently
      - Goose: YAML `mcpServers:` camelCase (`/mcpServers\s*:/`) — Goose-only key, asserted independently (NOT collapsed into the Hermes regex)
      - OpenClaw: `mcp.servers` or `mcp/servers` shape (`/mcp\.servers|mcp\/servers/`)
    - D-18 note present near Claude Desktop entry: "No Claude Desktop .mcpb packaging in Phase 7 — Claude Desktop = manual JSON / Pages configurator output only."
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/index.html — landing page with three install paths (D-15)</name>
  <files>docs/index.html</files>
  <read_first>
    - docs/install.html (after Task 1 — link target)
    - README.md (after Plan 02 — three install paths wording to mirror)
    - .planning/phases/07-distribution-docs/07-CONTEXT.md (D-15 three equal paths, D-23 badges)
    - .planning/phases/07-distribution-docs/07-RESEARCH.md (§ Multi-Client Matrix Configuration Blocks)
  </read_first>
  <action>
    Create `docs/index.html` as a single static HTML landing page (inline CSS, no JS framework, no external fetch). Structure:
    - Hero: title `awesome-coolify-mcp`, one-line tagline, badges (Coolify API 4.1.x, MCP action-based, MIT)
    - Section `## Install` with three equal cards per D-15:
      1. **One-click (deeplink)** — buttons/links that generate Cursor deeplink and VS Code `vscode:mcp/install` link client-side (link to docs/install.html for filling secrets first); placeholder env per D-23
      2. **Configurator (GitHub Pages)** — big CTA button linking to `docs/install.html`; short note: client-side only, tokens never posted to a backend per D-19; how to obtain COOLIFY_URL (Coolify base URL) and COOLIFY_TOKEN (Coolify UI → Keys & Tokens → API Token)
      3. **Manual** — show a Cursor `mcpServers` JSON snippet inline using `npx -y awesome-coolify-mcp`; link to `docs/mcp.example.json` and to `docs/install.html` for other clients
    - Section `## Clients` — grid of 15+ client badges with one-line each; link each to `docs/install.html#client=<slug>` so the configurator preselects that client
    - Footer: link to README.md, LICENSE, GitHub repo (clezcoding/awesome-coolify-mcp)
    - CRITICAL: no `fetch(`, no `XMLHttpRequest`, no external script src, no analytics. All JS inline and only for deeplink generation (reuse the same btoa pattern from docs/install.html Task 1).
    - No `.planning/` or `spike-findings` references per D-13
    - No `instances.json` mentions per D-10
    - Branding: `awesome-coolify-mcp` (not `coolify-mcp`)
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && node -e "
const fs=require('fs');
const r=fs.readFileSync('docs/index.html','utf8');
const assert=(cond,msg)=>{if(!cond){console.error('FAIL: '+msg);process.exit(1)}};
assert(r.length>0,'index.html non-empty');
assert(r.includes('One-click')||r.includes('One-Click'),'One-click path present');
assert(r.includes('Configurator'),'Configurator path present');
assert(r.includes('Manual'),'Manual path present');
assert(r.includes('install.html'),'link to install.html present');
assert((r.match(/fetch\(|XMLHttpRequest/g)||[]).length===0,'no fetch/XHR (D-19)');
assert((r.match(/<script\s+src=/g)||[]).length===0,'no external <script src> (T-07-09)');
assert((r.match(/\.planning\/|spike-findings/g)||[]).length===0,'no .planning/ or spike-findings links (D-13)');
assert((r.match(/(?<![\w-])coolify-mcp(?![\w-])/g)||[]).length===0,'no old coolify-mcp name (word-boundary regex, does not match substring inside awesome-coolify-mcp)');
assert((r.match(/instances\.json/g)||[]).length===0,'no instances.json (D-10)');
console.log('index.html OK');
"</automated>
  </verify>
  <done>
    - docs/index.html exists as a single static HTML file
    - Three install path cards present (One-click, Configurator, Manual)
    - Links to docs/install.html present
    - No `fetch(`/`XMLHttpRequest` (client-side only)
    - No `.planning/` or `spike-findings` references per D-13
    - No old `coolify-mcp` name (word-boundary regex `/(?<![\w-])coolify-mcp(?![\w-])/g`)
    - No `instances.json` per D-10
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/integration/install-configurator.test.ts — validate configurator HTML (D-13, D-19)</name>
  <files>tests/integration/install-configurator.test.ts</files>
  <read_first>
    - tests/integration/docs-parity.test.ts (after Plan 01 — Vitest file-read pattern to mirror)
    - tests/integration/mcp-schema-validation.test.ts (Vitest + beforeAll/afterAll pattern)
    - docs/install.html (after Task 1)
    - docs/index.html (after Task 2)
  </read_first>
  <behavior>
    - Test 1: docs/install.html and docs/index.html both exist and are non-empty
    - Test 2: docs/install.html contains all 15+ client names (Cursor, VS Code, Claude Desktop, Claude Code, Windsurf, OpenCode, Codex CLI, Gemini CLI, Antigravity, OpenClaw, Hermes, Kimi, Kilo, Cline, Goose, LM Studio)
    - Test 3: docs/install.html contains the Cursor deeplink protocol `cursor://anysphere` and the VS Code `vscode:mcp/install` (or `vscode.dev/redirect`) pattern
    - Test 4: docs/install.html contains NO `fetch(`, `XMLHttpRequest`, or `<form ... action=` patterns (client-side only per D-19)
    - Test 5: docs/install.html and docs/index.html contain NO `<script src=` patterns (no external script loading — threat T-07-09 mitigation)
    - Test 6: docs/install.html and docs/index.html contain NO `.planning/` or `spike-findings` references per D-13
    - Test 7: docs/install.html and docs/index.html contain NO `instances.json` references per D-10
    - Test 8: docs/install.html and docs/index.html contain NO old `coolify-mcp` package name (only `awesome-coolify-mcp` allowed). The negative grep for the old name MUST use a word-boundary regex that does NOT match the `coolify-mcp` substring inside `awesome-coolify-mcp`: `/(?<![\w-])coolify-mcp(?![\w-])/g`. Sanity check: `'awesome-coolify-mcp'.match(/(?<![\w-])coolify-mcp(?![\w-])/g)` → `null`; `'coolify-mcp'.match(/(?<![\w-])coolify-mcp(?![\w-])/g)` → `['coolify-mcp']`. A plain `/coolify-mcp/g` regex is FORBIDDEN — it matches the substring inside `awesome-coolify-mcp` and would fail on correct content. Use `expect(content).not.toMatch(/(?<![\w-])coolify-mcp(?![\w-])/)` for both HTML files.
    - Test 9: docs/install.html contains the security notice about client-side only (presence of "browser" and "never leave" or equivalent)
    - Test 10 (D-22 adapter markers — BLOCKER): docs/install.html source contains static literals proving per-client multi-format emission (NOT just client name substrings). Assert ALL of the following markers present via `expect(content).toMatch(/pattern/)` — Hermes and Goose keys are asserted SEPARATELY (do NOT share a single `mcp_servers:` regex):
      - Cursor: `/mcpServers\s*[:=]/` (mcpServers object shape)
      - VS Code: `/["']inputs["'].*\$\{input:/s` (`"inputs"` AND `${input:` substitution)
      - OpenCode/Kilo: `/["']type["']\s*:\s*["']local["']/` (`"type": "local"`)
      - Codex: `/\[mcp_servers\./` (TOML section `[mcp_servers.<name>]`)
      - Hermes: `/mcp_servers\s*:/` (YAML `mcp_servers:` snake_case block — Hermes-only key)
      - Goose: `/mcpServers\s*:/` (YAML `mcpServers:` camelCase block — Goose-only key, distinct from Hermes; a single `mcp_servers:` regex is FORBIDDEN because it would only catch Hermes and miss Goose)
      - OpenClaw: `/mcp\.servers|mcp\/servers/` (mcp.servers or mcp/servers shape)
    - Test 11 (D-18): docs/install.html source contains the D-18 disclaimer literal "No Claude Desktop .mcpb packaging" (or equivalent wording asserting no `.mcpb` bundle in Phase 7) AND does NOT contain a `.mcpb` file/bundle generation path (negative grep: no `mcpb` bundle emission, no `claude-desktop-extension` references).
  </behavior>
  <action>
    Create `tests/integration/install-configurator.test.ts` as a Vitest spec that reads `docs/install.html` and `docs/index.html` via `node:fs.readFileSync` and asserts the 11 behaviors above. Use `describe('install configurator (Pages)', ...)` with one `it(...)` per behavior. Use `import { readFileSync } from 'node:fs'` and `import { resolve } from 'node:path'` per the docs-parity.test.ts pattern. Tests MUST pass GREEN against the HTML files produced by Tasks 1 and 2. Reference D-13, D-18, D-19, D-21, D-22, D-10, and T-07-09 in test names. For negative greps, use `expect(content).not.toMatch(/pattern/)`. For Test 5 (T-07-09), use `expect(content).not.toMatch(/<script\s+src=/i)` to catch any external script loading (case-insensitive to catch `<SCRIPT SRC=`). For Test 8 (old package name), use the word-boundary regex `expect(content).not.toMatch(/(?<![\w-])coolify-mcp(?![\w-])/)` for both HTML files — a plain `/coolify-mcp/g` is FORBIDDEN because it matches the substring inside `awesome-coolify-mcp`. For Test 10 (D-22 adapter markers — BLOCKER), assert each of the 7 marker regexes matches the install.html source via `expect(installHtml).toMatch(/pattern/)`; this proves the configurator emits per-client formats (Cursor mcpServers, VS Code inputs+${input:}, OpenCode/Kilo type:local, Codex TOML, Hermes mcp_servers: snake_case, Goose mcpServers: camelCase, OpenClaw mcp.servers) rather than a single JSON for all clients. CRITICAL: Hermes (`mcp_servers:`) and Goose (`mcpServers:`) MUST be asserted by SEPARATE regexes — a single `/mcp_servers\s*:/` regex is FORBIDDEN because it would only catch Hermes and silently miss Goose. For Test 11 (D-18), assert `expect(installHtml).toMatch(/No Claude Desktop .mcpb packaging/i)` (or equivalent D-18 wording) AND `expect(installHtml).not.toMatch(/\.mcpb\b/)` to confirm no `.mcpb` bundle path is emitted.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run tests/integration/install-configurator.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>
    - File `tests/integration/install-configurator.test.ts` exists with 11 `it(...)` blocks
    - `npx vitest run tests/integration/install-configurator.test.ts` exits 0 (GREEN)
    - All 11 behaviors asserted; tests reference D-13, D-18, D-19, D-21, D-22, D-10, T-07-09
    - Test 10 asserts all 7 D-22 per-client adapter markers present in install.html source (Cursor mcpServers, VS Code inputs+${input:}, OpenCode/Kilo type:local, Codex [mcp_servers., Hermes mcp_servers: snake_case, Goose mcpServers: camelCase, OpenClaw mcp.servers/mcp/servers) — Hermes and Goose asserted by SEPARATE regexes
    - Test 11 asserts D-18 disclaimer present and no `.mcpb` bundle path emitted
    - No test skipped or `.todo`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → docs/install.html | User enters COOLIFY_TOKEN into a web form; trust boundary is the browser DOM |
| docs/install.html → user clipboard | Generated config snippet with secrets is copied; user pastes into client config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Information Disclosure | Configurator posts secrets to backend | critical | mitigate | docs/install.html uses NO `fetch(`/`XMLHttpRequest`/`<form action>`; all generation is inline vanilla JS in the browser; install-configurator.test.ts negative-greps for these patterns. Per D-19. |
| T-07-08 | Information Disclosure | Secrets in shareable deeplink URLs | high | mitigate | Cursor deeplink defaults to placeholder env per D-23; only when the user explicitly clicks "generate deeplink with my values" are real tokens encoded into the URL — and the URL is meant to be opened locally, not shared. Document this in the security notice on the page. |
| T-07-09 | Spoofing | Configurator loads external script | high | mitigate | No external `<script src>`, no CDNs, no analytics; all JS inline. install-configurator.test.ts greps for `<script src=` and fails if found. |
| T-07-10 | Information Disclosure | docs/*.html links to .planning/ | medium | mitigate | No `.planning/` or `spike-findings` references in any community doc per D-13; install-configurator.test.ts negative-greps. |
</threat_model>

<verification>
- `npx vitest run tests/integration/install-configurator.test.ts` is GREEN (11 tests — incl. Test 10 D-22 adapter markers BLOCKER with Hermes mcp_servers: and Goose mcpServers: asserted separately, and Test 11 D-18 .mcpb disclaimer)
- `npx vitest run tests/integration/docs-parity.test.ts` remains GREEN (no regression from Plan 02)
- `npm run build && npm test` green (no source code touched)
- docs/install.html and docs/index.html exist as single static files with inline CSS+JS
- No `fetch(`/`XMLHttpRequest`/`<form action>`/external `<script src>` in either HTML file
- No `.planning/`/`spike-findings`/`instances.json`/old `coolify-mcp` name in either HTML file
- Cursor deeplink and VS Code install URL generators present
- D-22 per-client adapter markers present in install.html source (Cursor mcpServers, VS Code inputs+${input:}, OpenCode/Kilo type:local, Codex [mcp_servers., Hermes mcp_servers: snake_case, Goose mcpServers: camelCase, OpenClaw mcp.servers/mcp/servers) — Hermes and Goose keys asserted by separate regexes
- D-18 disclaimer present in install.html: no Claude Desktop .mcpb packaging in Phase 7
</verification>

<success_criteria>
- docs/install.html is a client-side-only configurator supporting 15+ MCP clients with per-client format adapters (D-21, D-22)
- docs/install.html source contains all 7 D-22 adapter markers proving multi-format emission (BLOCKER — verified by Test 10); Hermes `mcp_servers:` (snake_case) and Goose `mcpServers:` (camelCase) are asserted by SEPARATE regexes (a single `mcp_servers:` regex is FORBIDDEN — it would miss Goose)
- docs/install.html surfaces the D-18 disclaimer: no Claude Desktop `.mcpb` packaging in Phase 7 — Claude Desktop = manual JSON / Pages configurator output only (verified by Test 11)
- docs/index.html is a landing page with three equal install paths (D-15), copy consistent with post-Plan-02 README
- No backend POST, no external scripts, no analytics (D-19, T-07-09)
- Cursor deeplink + VS Code vscode:mcp/install badge generators present (D-23)
- No `.planning/` or spike-findings links in community docs (D-13)
- No `instances.json` documentation (D-10)
- No old `coolify-mcp` name in community docs (D-02)
- install-configurator.test.ts is GREEN with 11 assertions (incl. T-07-09 `<script src=` negative grep, Test 10 D-22 adapter markers with Hermes/Goose split, and Test 11 D-18 .mcpb disclaimer)
</success_criteria>

<output>
Create `.planning/phases/07-distribution-docs/07-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `docs/install.html` — static client-side configurator: form (COOLIFY_URL, COOLIFY_TOKEN, client selector, optional SSL/log) + 15+ per-client format adapters (Cursor/VS Code/Claude Desktop/Claude Code/Windsurf/OpenCode/Codex TOML/Gemini/Antigravity/OpenClaw/Hermes YAML/Kimi/Kilo/Cline/Goose YAML/LM Studio) + Cursor deeplink generator + VS Code vscode:mcp/install badge generator + copy-to-clipboard + security notice
- `docs/index.html` — static landing page with three equal install path cards (One-click deeplink, Pages Configurator, Manual) + 15+ client badge grid linking to docs/install.html with preselected client; landing copy mirrors post-Plan-02 README
- `tests/integration/install-configurator.test.ts` — Vitest spec with 11 assertions validating configurator HTML existence, 15+ client coverage, deeplink protocols, no-backend POST, no external `<script src>` (T-07-09), no .planning links, no instances.json, no old package name, security notice present, Test 10 D-22 per-client adapter markers (Cursor mcpServers, VS Code inputs+${input:}, OpenCode/Kilo type:local, Codex [mcp_servers., Hermes mcp_servers: snake_case, Goose mcpServers: camelCase, OpenClaw mcp.servers/mcp/servers — Hermes and Goose asserted by separate regexes), and Test 11 D-18 .mcpb disclaimer
