---
context: phase
phase: 06-bulk-emergency-safety
task: 12
total_tasks: 14
status: human_verification_pending
last_updated: 2026-07-16T05:29:42.988Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through failure this session. Acknowledge before proceeding.

- [ ] **CONSTRAINT: Phase branch base** — `origin/main` only has Phase 4. Phase 6 branch MUST fork from `gsd/phase-05-logs-service-db-ops`, not main. — Verify `git merge-base HEAD gsd/phase-05-logs-service-db-ops` before any new phase work.
- [ ] **CONSTRAINT: Intra-wave server.ts overlap** — Plans 06-01 and 06-02 both touch `src/mcp/server.ts`. Never parallelize them. — Wave 1 must stay sequential.
- [ ] **CONSTRAINT: Planning gitignore** — `.planning/` is gitignored. SUMMARY/VERIFICATION/UAT commits need `git add -f`. — Check `git check-ignore` before assuming artifacts are tracked.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong branch base | Phase 06 branch created off `origin/main` first — missing Phase 5 code (logs, service/DB lifecycle) | blocking | Always fork phase-N branch from phase-(N-1) completion branch when main lags |
| Parallel server.ts edits | 06-01 + 06-02 share `src/mcp/server.ts` | blocking | Pre-spawn `files_modified` overlap check in execute-phase; force sequential |
| Untracked planning artifacts | gsd-tools commit skips `.planning/` due to gitignore | advisory | `git add -f .planning/phases/...` for SUMMARY/VERIFICATION/UAT |
| STATE/verification drift | STATE.md says Phase 7 ready; `verification.status` still `human_needed` | advisory | Do not start Phase 7 until `/gsd-verify-work 6` passes all UAT items |

<current_state>
Phase 6 code execution **complete** (3/3 plans, 459 tests green). Branch `gsd/phase-06-bulk-emergency-safety` at `5dac14d`. Formal verification status: **`human_needed`** — 3 manual UAT items in `06-UAT.md` still `[pending]`. STATE.md already advanced to Phase 7 ready (drift — reconcile after UAT).
</current_state>

<completed_work>

**Phase 6 plans (all done):**

| Plan | Deliverable | Key commit |
|------|-------------|------------|
| 06-01 | `emergency` tool, `COOLIFY_CONFIRM_REQUIRED`, confirm preview | `3c00d28`–`48ac342` |
| 06-02 | `reveal` opt-in on full projections (OUT-02) | `a062fc3`–`346ccb0` |
| 06-03 | `emergency-safety-flow.test.ts` (27 tests), VALIDATION sign-off | `c3f4216`–`83726d8` |

**Verification:** `06-VERIFICATION.md` — 38/38 must-haves automated (`6902235`)

**UAT file:** `06-UAT.md` created (`5dac14d`) — 0/3 tests run
</completed_work>

<remaining_work>

1. **UAT 1** — MCP stdio E2E: emergency + reveal over real transport
2. **UAT 2** — Live emergency on **non-prod** project only (confirm preview + confirm:true)
3. **UAT 3** — Live reveal on app with real secrets (`***` default vs plaintext)
4. After UAT pass → mark Phase 6 formally complete, then Phase 7 (Distribution & Docs)
5. **Optional cleanup** — uncommitted WIP unrelated to Phase 6 (see below)
</remaining_work>

<decisions_made>

- Emergency tool isolated from `system`/`application` (D-01) — dedicated danger zone
- `confirm: true` boolean gate with preview block before any EMG mutation (OUT-07)
- `reveal` MCP-side only; errors still redacted regardless of reveal (D-11)
- Integration tests handler-level only; MCP stdio E2E MANUAL-ONLY (P1–P5 precedent)
- Phase 6 branch based on phase-05 branch, not main
</decisions_made>

<blockers>

- **Human UAT (3 items)** — blocking formal phase sign-off. No code blockers.
- **STATE/verification mismatch** — STATE says Phase 7 ready; verifier says human_needed. Resolve via verify-work.
</blockers>

## Required Reading (in order)

1. `.planning/phases/06-bulk-emergency-safety/06-UAT.md` — pending manual tests
2. `.planning/phases/06-bulk-emergency-safety/06-VERIFICATION.md` — what passed automated
3. `.planning/phases/06-bulk-emergency-safety/06-03-SUMMARY.md` — integration sign-off details
4. `.planning/STATE.md` — note Phase 7 ready vs UAT pending drift

## Infrastructure State

- **Branch:** `gsd/phase-06-bulk-emergency-safety` (from phase-05 HEAD `43c35b3`)
- **Tests:** 459/459 green (last run during execute-phase)
- **Build:** green (`npm run build`)
- **Coolify target:** https://puzzlesstool.online (used in prior phases UAT)
- **No background processes** running from this session

## Uncommitted WIP (separate from Phase 6)

Not touched by phase 6 executors — decide commit/discard/stash:

- `src/config/env.ts` — dotenv merge + `formatEnvLoadHint`
- `src/config/env.test.ts`, `src/index.ts`
- `scripts/run-mcp.mjs` — `npm start` MCP launcher
- `README.md`, `README.de.md`, `docs/mcp.example.json`
- `package.json` / `package-lock.json` — zod 4.x + start scripts

<context>
Phase 6 execute-phase finished in one session. All code shipped. Pausing before live UAT — operator must run destructive emergency tests on non-prod only. After UAT, Phase 7 (npm publish, GitHub README, Cursor setup) is next per ROADMAP.
</context>

<next_action>
Start with: `/gsd-verify-work 6` — walk through 3 UAT items in `06-UAT.md` against live Coolify. Do NOT start Phase 7 until verification status is `passed`.
</next_action>
