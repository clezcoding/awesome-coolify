---
phase: 11-service-database-crud
plan: 02
type: execute
wave: 2
depends_on:
  - 11-00
  - 11-01
files_modified:
  - src/mcp/tools/service.ts
autonomous: true
requirements:
  - SVC-06
  - SVC-07
  - SVC-10
must_haves:
  truths:
    - "serviceActionSchema discriminatedUnion includes a 'create' branch accepting one-click `type` XOR compose input (`compose` inline OR `compose_file` local path) per D-01, D-04"
    - "create schema requires server_uuid (D-03); project_uuid XOR project_name; environment_name or environment_uuid; no silent production default — missing -> COOLIFY_VALIDATION_ERROR with ask-human hint"
    - "create schema superRefine: exactly one of (type) OR (compose|compose_file) must be present; both or neither -> COOLIFY_VALIDATION_ERROR (D-01 XOR)"
    - "create schema superRefine: compose_file XOR compose — exactly one when compose path chosen (D-04); both -> COOLIFY_VALIDATION_ERROR"
    - "create schema: instant_deploy defaults to true (D-11 override vs Phase 10 apps default false)"
    - "create schema: force_domain_override defaults false (D-19); optional urls[] of { name, url } (D-02)"
    - "handler reads compose_file from disk when compose_file is set; encodeCompose the YAML to base64 before calling createService; agent never sees base64 in input or response (D-05, D-06)"
    - "handler validates compose via validateCompose before API call (D-07); invalid -> COOLIFY_VALIDATION_ERROR"
    - "handler resolves project_uuid from project_name via fetchProjects + resolveProjectUuid when only project_name is given (D-03)"
    - "HTTP 409 from createService maps to COOLIFY_409 with data.conflicts passthrough and force_domain_override:true recovery hint (D-19, SVC-10, mirrors Phase 10 APP-21)"
    - "force_domain_override:true on a non-conflicting payload passes the flag through to createService body and returns success (no COOLIFY_409) — SVC-10 happy-path override"
    - "instant_deploy fire-and-forget: return service UUID + deploy queued status; no wait/poll (D-11); on queue failure return soft success ok:true with deploy.status='failed_to_queue' + recovery hints — no auto-rollback/delete (D-13)"
    - "create response includes follow-up hints for service.get and service.deploy/start"
    - "D-06: create success response is routed through projectServiceCompose so the agent sees `compose` as decoded YAML and never sees `docker_compose_raw` base64"
    - "D-06: existing service.get handler is updated to route its fetchService result through projectServiceCompose so service.get responses also expose decoded `compose` and strip `docker_compose_raw`"
    - "Zod validates create payload before any API call (SAF-03); malformed payloads never reach createService"
    - "npx vitest run src/mcp/tools/service.test.ts -t 'service create' is GREEN — 11-00 RED scaffolds flipped green"
    - "D-14: extend existing `service` tool only — no new MCP tool; create lands on the existing `service` action surface"
  artifacts:
    - "src/mcp/tools/service.ts (extended with createActionSchema + handleServiceCreate)"
    - "D-14: extend existing `service` tool — no new MCP tool registered; create/update/delete/delete_preview land on the existing `service` tool surface"
  key_links:
    - "Imports createService from ../../api/client.js (added in 11-01)"
    - "Imports encodeCompose + validateCompose + projectServiceCompose from ../../utils/yaml-validator.js (added in 11-01)"
    - "Imports COOLIFY_VALIDATION_ERROR + RECOVERY_HINTS + CoolifyApiError + wrapMcpError from ../../utils/errors.js (Phase 10 enriched)"
    - "Imports resolveProjectUuid from ../../utils/project-lookup.js (existing Phase 9 util) for project_name -> project_uuid resolution"
    - "Imports triggerServiceStart (existing) for the instant_deploy fire-and-forget path"
    - "D-06: projectServiceCompose is called on the create 201 response AND on the existing service.get handler's fetchService result — strips docker_compose_raw, exposes decoded YAML as `compose`"
---

<objective>
Wave 2 service create handler: one-click + custom compose XOR, transparent base64, instant_deploy default true, 409 force_domain_override.

Purpose: Implement the `create` action on the `service` tool. Single POST /services route dispatched by `type` (one-click) XOR compose input. Transparent base64 encode of YAML at MCP boundary (agent never sees base64). instant_deploy defaults true (D-11 override). 409 domain conflict maps to COOLIFY_409 with force_domain_override hint (SVC-10, mirrors Phase 10 APP-21). Soft success on deploy queue failure (D-13).

Output: `createActionSchema` + `handleServiceCreate` wired into `handleServiceAction` switch. Flips the `service create` RED scaffolds from 11-00 to GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-service-database-crud/11-CONTEXT.md
@.planning/phases/11-service-database-crud/11-RESEARCH.md
@.planning/phases/11-service-database-crud/11-PATTERNS.md
@.planning/phases/11-service-database-crud/11-00-SUMMARY.md
@.planning/phases/11-service-database-crud/11-01-SUMMARY.md
@.planning/phases/10-application-crud-safety/10-02-PLAN.md
@src/mcp/tools/service.ts
@src/mcp/tools/application.ts
@src/mcp/tools/private_key.ts
@src/utils/yaml-validator.ts
@src/utils/project-lookup.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add createActionSchema — one-click type XOR compose (D-01, D-02, D-03, D-04, D-11, D-19, SAF-03)</name>
  <files>src/mcp/tools/service.ts</files>
  <read_first>
    - src/mcp/tools/service.ts (lines 1-40 for imports; lines 37-78 for requireServiceMutationIdentifier pattern; lines 140-150 for serviceActionSchema discriminatedUnion)
    - src/mcp/tools/private_key.ts (lines 72-84 for XOR superRefine pattern — mirror for compose_file XOR compose)
    - src/mcp/tools/application.ts (lines 238-249 for createActionSchema discriminatedUnion pattern; lines 395-401 for project/env superRefine)
    - src/mcp/tools/project.ts (lines 84-97 for .strict() + throwValidationError pattern)
    - docs/coolify_openapi.yaml (lines 6870-6932 for POST /services required+optional fields)
    - .planning/phases/11-service-database-crud/11-CONTEXT.md (D-01..D-07, D-11, D-19)
  </read_first>
  <behavior>
    - createActionSchema = z.object({ action:z.literal('create'), server_uuid:z.string(), project_uuid?:z.string(), project_name?:z.string(), environment_name?:z.string(), environment_uuid?:z.string(), name?:z.string(), description?:z.string(), type?:z.string().describe('One-click service type, e.g. actualbudget, calibre-web, gitea-with-mysql'), compose?:z.string().describe('Inline Docker Compose YAML — MCP base64-encodes for the API'), compose_file?:z.string().describe('Local path to a docker-compose.yml file — MCP reads and base64-encodes for the API'), urls?:z.array(z.object({ name:z.string(), url:z.string() })).optional(), instant_deploy:z.boolean().default(true) (D-11), force_domain_override:z.boolean().default(false) (D-19), ...mutationResponseParamsSchema }).strict()
    - superRefine: exactly one of (type) OR (compose|compose_file) must be present; both or neither -> COOLIFY_VALIDATION_ERROR issue (D-01 XOR)
    - superRefine: if compose path chosen, exactly one of compose or compose_file; both -> COOLIFY_VALIDATION_ERROR (D-04)
    - superRefine: at least one of project_uuid or project_name -> else COOLIFY_VALIDATION_ERROR (D-03)
    - superRefine: at least one of environment_name or environment_uuid -> else COOLIFY_VALIDATION_ERROR (D-03)
    - All superRefine issues use `params: { code: 'COOLIFY_VALIDATION_ERROR' }` so throwValidationError maps to the new error code (Phase 10 pattern)
    - Add 'create' branch to serviceActionSchema discriminatedUnion
  </behavior>
  <action>
    Add `createActionSchema` as a `z.object({...}).strict()` with the fields listed above. Apply the XOR + project/env superRefines via shared helpers `requireServiceCreateSource(data, ctx)` (type XOR compose) and `requireProjectAndEnvironment(data, ctx)` (reuse from application.ts or copy-paste per D-17 discretion — copy-paste to avoid cross-file refactor risk in this plan).

    Use `params: { code: 'COOLIFY_VALIDATION_ERROR' }` on every superRefine issue so `throwValidationError` (existing pattern from project.ts) maps it to the new error code from Phase 10.

    Add the `createActionSchema` to the `serviceActionSchema` discriminatedUnion array (currently 5 entries — get/start/stop/restart/deploy — add create as the 6th).

    Do NOT implement the handler in this task — only the schema. The handler lands in Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/service.test.ts -t "service create"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createActionSchema" src/mcp/tools/service.ts` returns at least 2 (definition + inclusion in discriminatedUnion)
    - `grep -c "COOLIFY_VALIDATION_ERROR" src/mcp/tools/service.ts` returns at least 4 (type/compose XOR + compose/compose_file XOR + project refine + env refine)
    - `serviceActionSchema.safeParse({ action:'create', type:'actualbudget', server_uuid:'s', project_uuid:'p', environment_name:'production' }).success === true`
    - `serviceActionSchema.safeParse({ action:'create', compose:'services:\\n  redis:\\n    image: redis:7', server_uuid:'s', project_uuid:'p', environment_name:'production' }).success === true`
    - `serviceActionSchema.safeParse({ action:'create', server_uuid:'s', project_uuid:'p', environment_name:'production' }).success === false` (no source — D-01 XOR)
    - `serviceActionSchema.safeParse({ action:'create', type:'actualbudget', compose:'x: y', server_uuid:'s', project_uuid:'p', environment_name:'production' }).success === false` (both sources — D-01 XOR)
    - `serviceActionSchema.safeParse({ action:'create', type:'actualbudget', server_uuid:'s', environment_name:'production' }).success === false` (no project — D-03)
    - `serviceActionSchema.safeParse({ action:'create', type:'actualbudget', server_uuid:'s', project_uuid:'p' }).success === false` (no environment — D-03)
    - Schema-acceptance tests in `service create` describe block flip GREEN; handler-call tests still RED until Task 2
  </acceptance_criteria>
  <done>
    - createActionSchema committed; schema-level RED tests (D-01 XOR, D-03, D-04, SAF-03) flip GREEN; handler-call tests still RED until Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement handleServiceCreate — dispatcher, compose encode, instant_deploy fire-and-forget, 409 mapping (SVC-06, SVC-07, SVC-10, D-01..D-07, D-11, D-13, D-19)</name>
  <files>src/mcp/tools/service.ts</files>
  <read_first>
    - src/mcp/tools/service.ts (after Task 1 — createActionSchema in place; lines 262-332 for handleServiceMutation/handleServiceDeploy fire-and-forget patterns; lines 334-394 for handleServiceAction switch)
    - src/mcp/tools/application.ts (lines 628-693 for handleApplicationDeploy fire-and-forget + extractDeploymentUuid pattern; lines 800-866 for handleApplicationAction switch + 409 hint enrichment)
    - src/utils/yaml-validator.ts (encodeCompose, decodeCompose, validateCompose signatures from 11-01)
    - src/utils/project-lookup.ts (resolveProjectUuid signature for project_name -> project_uuid)
    - src/utils/errors.ts (Phase 10 — COOLIFY_VALIDATION_ERROR + 409 conflicts passthrough already present)
    - .planning/phases/11-service-database-crud/11-CONTEXT.md (D-01..D-13, D-19)
  </read_first>
  <behavior>
    - handleServiceCreate(parsed, env) dispatches by source: if parsed.type set -> one-click path; if parsed.compose or parsed.compose_file set -> custom compose path
    - Custom compose path: if compose_file set, read file via fs.promises.readFile(compose_file, 'utf8') with a 1 MiB size cap; else use parsed.compose; validate via validateCompose; on invalid -> throw CoolifyApiError code COOLIFY_VALIDATION_ERROR with the validate error message
    - Encode the YAML via encodeCompose to base64; build API body with docker_compose_raw = base64 (D-05); agent never sees base64
    - One-click path: build API body with type = parsed.type; no docker_compose_raw
    - Both paths: include server_uuid, project_uuid (resolved from project_name if needed), environment_name or environment_uuid, name, description, urls, instant_deploy (default true per D-11), force_domain_override (default false per D-19)
    - Resolve project_uuid from project_name via fetchProjects + resolveProjectUuid (existing Phase 9 util) when only project_name is given (D-03)
    - Call createService(env.COOLIFY_URL, env.COOLIFY_TOKEN, body, env.COOLIFY_VERIFY_SSL)
    - On HTTP 409: the Phase 10 toStructuredError enrichment already attached data.conflicts; handler catches CoolifyApiError, checks error.envelope.data?.conflicts, and if present appends "Retry with force_domain_override: true on the same create call to override the domain conflict." to recoveryHints before re-throwing (D-19, SVC-10)
    - On 201 success: extract service UUID from response; route the raw response through `projectServiceCompose(raw)` so `compose` is decoded YAML and `docker_compose_raw` is stripped (D-06); if instant_deploy is true (default), fire-and-forget triggerServiceStart (no wait/poll per D-11); catch start-queue failure and return soft success ok:true with start.status='failed_to_queue' + recovery hints (D-13); no auto-rollback/delete
    - On instant_deploy success: include follow-up hints — service.get to inspect deployed compose, service.deploy to re-deploy, service.start/stop for lifecycle
    - D-06 service.get update: locate the existing `handleServiceGet` (or the get branch of `handleServiceAction`) and route its `fetchService(...)` raw result through `projectServiceCompose(raw)` before `sanitizeFullProjection` / `buildReadResponse` so service.get responses also expose decoded `compose` and strip `docker_compose_raw`. This is a small surgical edit to the existing handler — no schema change.
    - Wire 'create' case into handleServiceAction switch (before the default)
  </behavior>
  <action>
    Implement `handleServiceCreate(parsed, env)`:

    1. Resolve project_uuid: if `parsed.project_uuid` use it; else if `parsed.project_name` call `resolveProjectUuid(undefined, parsed.project_name, env)` (existing util from project-lookup.ts); else Zod superRefine already rejected (D-03).
    2. Resolve compose content if custom path:
       - If `parsed.compose_file` is set: `const composeYaml = await fs.promises.readFile(parsed.compose_file, 'utf8');` — wrap in try/catch; on read error (ENOENT, EACCES) throw CoolifyApiError code COOLIFY_VALIDATION_ERROR with message `Cannot read compose_file at ${path}: ${error.message}`. Enforce a 1 MiB size cap: if `Buffer.byteLength(composeYaml) > 1024 * 1024` throw COOLIFY_VALIDATION_ERROR with `compose_file exceeds 1 MiB limit`.
       - Else if `parsed.compose` is set: `const composeYaml = parsed.compose;`
       - Else (one-click path): composeYaml is undefined.
    3. If composeYaml is set: `const validation = validateCompose(composeYaml); if (!validation.ok) throw CoolifyApiError({ code:'COOLIFY_VALIDATION_ERROR', message:`Invalid compose YAML: ${validation.error}`, recoveryHints: RECOVERY_HINTS.COOLIFY_VALIDATION_ERROR });` (D-07). Then `const docker_compose_raw = encodeCompose(composeYaml);` (D-05).
    4. Build the curated body object: include `server_uuid`, `project_uuid`, `environment_name` (or `environment_uuid` if that's what the caller supplied), `name`, `description`, `urls`, `instant_deploy`, `force_domain_override`. For one-click path also include `type`. For custom compose path also include `docker_compose_raw`. Omit undefined fields (don't send null).
    5. Call `await createService(env.COOLIFY_URL, env.COOLIFY_TOKEN, body, env.COOLIFY_VERIFY_SSL)`.
    6. Extract service UUID from the 201 response (`response.uuid`). Route the raw 201 response through `projectServiceCompose(raw)` so the agent sees `compose` as decoded YAML and never sees `docker_compose_raw` base64 (D-06).
    7. If `parsed.instant_deploy === true` (default):
       - Call `await triggerServiceStart(env.COOLIFY_URL, env.COOLIFY_TOKEN, serviceUuid, env.COOLIFY_VERIFY_SSL)` (fire-and-forget — service start maps to deploy for compose services per Phase 5 finding).
       - Wrap in try/catch. On success: return `{ uuid: serviceUuid, deploy: { status: 'queued' }, hints: ['Use service.get with uuid to inspect deployed compose', 'Use service.deploy to re-deploy', 'Use service.stop or service.start for lifecycle'] }` via buildReadResponse.
       - On catch (start failed to queue): return soft success `{ ok: true, uuid: serviceUuid, deploy: { status: 'failed_to_queue', error: <message> }, recoveryHints: [...] }` — DO NOT re-throw, DO NOT delete the created service (D-13).
    8. If `parsed.instant_deploy === false`: return `{ uuid: serviceUuid, deploy: { status: 'not_triggered' }, hints: ['Use service.start or service.deploy to start the service'] }`.
    9. Wire `case 'create': return await handleServiceCreate(parsed, env);` into `handleServiceAction` switch (before the `default`).
    10. The 409 mapping is automatic: Phase 10's `toStructuredError` already enriches the envelope with `data.conflicts`. To add the force_domain_override hint, catch CoolifyApiError in the handler around the createService call, check `error.envelope.data?.conflicts`, and if present, append the override hint to `error.envelope.recoveryHints` before re-throwing (mirrors 10-02 create handler).

    11. D-06 service.get wiring: locate the existing `service.get` handler (the `case 'get'` branch in `handleServiceAction` or the dedicated `handleServiceGet` function — read service.ts to find which shape is in use). After `fetchService(...)` returns the raw service object, route it through `projectServiceCompose(raw)` BEFORE any `sanitizeFullProjection` / `buildReadResponse` call. This ensures existing service.get responses expose decoded YAML in `compose` and strip `docker_compose_raw` (D-06). This is a surgical edit — no schema change, no new test files. Add `projectServiceCompose` to the existing `import { encodeCompose, validateCompose } from '../../utils/yaml-validator.js'` statement (extend to `import { encodeCompose, validateCompose, projectServiceCompose } from '../../utils/yaml-validator.js'`).

    Use the existing `wrapMcpError` outer wrapper in `handleServiceAction` for all error propagation. Do NOT add a new try/catch around the create POST itself beyond the 409-hint enrichment — let the client's `withMappedErrors` + the outer wrapper handle non-409 errors.

    Import `fs.promises` from `node:fs` at the top of service.ts for the compose_file read.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/service.test.ts -t "service create"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleServiceCreate" src/mcp/tools/service.ts` returns at least 2 (definition + switch case)
    - `grep -c "case 'create'" src/mcp/tools/service.ts` returns 1
    - `grep -c "encodeCompose" src/mcp/tools/service.ts` returns at least 1
    - `grep -c "validateCompose" src/mcp/tools/service.ts` returns at least 1
    - `grep -c "projectServiceCompose" src/mcp/tools/service.ts` returns at least 2 (import + at least one call site — create response and/or service.get)
    - `npx vitest run src/mcp/tools/service.test.ts -t "service create"` is GREEN — all create tests pass
    - `npx vitest run src/mcp/tools/service.test.ts -t "service get"` is GREEN — D-06 decode assertion passes (compose is decoded YAML, docker_compose_raw stripped)
    - SVC-06: create with type='actualbudget' -> createService called with type in body; response includes service UUID
    - SVC-07 (inline): create with compose string -> createService called with docker_compose_raw = base64 of compose; agent never sees base64 in input or response
    - SVC-07 (file): create with compose_file path -> handler reads file, encodes, calls createService with docker_compose_raw
    - SVC-07 (XOR): create with both type and compose -> Zod rejects (Task 1 schema); create with neither -> Zod rejects
    - D-11: create without instant_deploy -> createService called with instant_deploy=true (default)
    - SVC-10 (409 path): 409 from createService -> COOLIFY_409 error with data.conflicts + force_domain_override recovery hint
    - SVC-10 (happy-path override): create with force_domain_override:true and a non-conflicting payload -> createService called with force_domain_override:true in the body; handler returns success with service UUID (no COOLIFY_409)
    - D-13: instant_deploy start-queue failure -> ok:true, uuid returned, deploy.status='failed_to_queue', no deleteService call
    - D-06: create response routes through projectServiceCompose -> `compose` is decoded YAML, `docker_compose_raw` is absent from the agent-visible response
    - D-06: service.get response routes through projectServiceCompose -> `compose` is decoded YAML, `docker_compose_raw` is absent
    - SAF-03: missing required fields -> COOLIFY_VALIDATION_ERROR before any createService call (vi.fn() not-called assertion)
    - D-07: invalid compose YAML -> COOLIFY_VALIDATION_ERROR before any createService call
    - `npm run build` is green
  </acceptance_criteria>
  <done>
    - Create handler committed; all `service create` RED scaffolds from 11-00 flipped GREEN; build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> MCP create handler | Untrusted input crosses here; Zod (SAF-03) is the first gate before any API call |
| MCP handler -> local filesystem (compose_file) | Handler reads a local path supplied by the agent; path traversal risk mitigated by reading via fs.readFile (no shell), and the path is only read — never written or executed |
| MCP handler -> Coolify POST /services | Existing bearer auth + retry + error mapping |
| Coolify 409 -> MCP error envelope | Conflicts array passthrough from Phase 10; recovery hint adds force_domain_override |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02-01 | Tampering | create payload validation | high | mitigate | Zod .strict() + superRefine for type/compose XOR, compose/compose_file XOR, project/env required — rejects before any client call (SAF-03). |
| T-11-02-02 | Information Disclosure | instant_deploy soft-success response | low | accept | Soft-success returns service UUID + deploy status only — no secrets. |
| T-11-02-03 | Tampering | compose_file path traversal | high | mitigate | Handler reads compose_file via `fs.promises.readFile(path, 'utf8')` — no shell, no glob, no execution. Path is only read. Reject empty result. Validate via validateCompose before encoding. |
| T-11-02-04 | Information Disclosure | base64 compose in API body | low | accept | Base64 is encoding not encryption; compose YAML is not secret. Agent never sees base64 (D-05/D-06). |
| T-11-02-05 | Denial of Service | huge compose_file | medium | mitigate | Handler reads file with a size cap (e.g. 1 MiB); reject with COOLIFY_VALIDATION_ERROR if file exceeds cap. Document the limit in the field description. |
| T-11-02-06 | Spoofing | project_uuid resolution from name | medium | mitigate | resolveProjectUuid uses fetchProjects + name match; multi-match -> COOLIFY_AMBIGUOUS_MATCH (existing Phase 9 pattern, D-18 spirit). |
| T-11-SC | Tampering | npm installs | high | mitigate | No new packages in this plan — reuses yaml (installed in 11-01, audited [OK]) + zod/ofetch already pinned. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/service.test.ts -t "service create"` — GREEN
- `npm run build` (tsup) — green
- `grep -c "handleServiceCreate" src/mcp/tools/service.ts` — at least 2
- `grep -c "case 'create'" src/mcp/tools/service.ts` — 1
- `grep -c "encodeCompose" src/mcp/tools/service.ts` — at least 1
- `grep -c "validateCompose" src/mcp/tools/service.ts` — at least 1
- Existing `service get`/`start`/`stop`/`restart`/`deploy` tests still GREEN (no regression)
</verification>

<success_criteria>
- One-click `type` and custom `compose`/`compose_file` create paths both work (SVC-06, SVC-07)
- Transparent base64 encode of compose YAML — agent never sees base64 in input or response (D-05, D-06)
- D-06: service.get and create responses expose decoded YAML in `compose` and strip `docker_compose_raw` via projectServiceCompose
- Light YAML validation before API call (D-07)
- instant_deploy defaults true, fire-and-forget, soft success on queue failure (D-11, D-13)
- 409 domain conflicts map to COOLIFY_409 with force_domain_override hint (SVC-10, D-19)
- force_domain_override:true on a non-conflicting create passes the flag to the API body and returns success (SVC-10 happy-path override)
- Zod rejects malformed payloads before any API call (SAF-03, D-01 XOR, D-03, D-04)
- 11-00 `service create` RED scaffolds flipped GREEN
- No regression in existing service actions
</success_criteria>

<output>
Create `.planning/phases/11-service-database-crud/11-02-SUMMARY.md` when done.
</output>
