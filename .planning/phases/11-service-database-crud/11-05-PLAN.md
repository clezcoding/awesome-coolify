---
phase: 11-service-database-crud
plan: 05
type: execute
wave: 3
depends_on:
  - 11-03
files_modified:
  - src/mcp/tools/database.ts
autonomous: true
requirements:
  - DB-02
  - DB-03
must_haves:
  truths:
    - "databaseActionSchema discriminatedUnion includes an 'update' branch accepting uuid|name identity (D-18) with curated fields: name, description, image, is_public, public_port, public_port_timeout, limits_*, engine-specific credential fields (postgres_user/postgres_password/postgres_db, mysql_*, mariadb_*, redis_password, clickhouse_admin_*, dragonfly_password, keydb_password), confirm:boolean default false (D-12 update-path gate per planner discretion)"
    - "update schema uses .strict() and rejects unknown fields; SAF-03: Zod validates update payload before any API call"
    - "update schema D-12 update-path confirm gate: if is_public === true && confirm !== true -> COOLIFY_CONFIRM_REQUIRED issue at schema level (mirror D-12 create-path gate for safety consistency — planner discretion exercised)"
    - "update handler resolves identity via resolveDatabaseUuid (uuid|name, multi-match -> COOLIFY_AMBIGUOUS_MATCH per D-18)"
    - "update handler calls updateDatabase(url, token, uuid, curatedPayload, verifySsl) — PATCH /databases/{uuid}"
    - "update response masks engine-specific password fields and connection strings via sanitizeFullProjection unless reveal:true (SAF-04, D-10)"
    - "databaseActionSchema discriminatedUnion includes a 'delete' branch accepting uuid|name identity (D-18), confirm:boolean default false (D-15, SAF-01), delete_volumes:boolean default false (D-16, SAF-02), delete_configurations:boolean default false, docker_cleanup:boolean default false, delete_connected_networks:boolean default false"
    - "databaseActionSchema discriminatedUnion includes a 'delete_preview' branch accepting uuid|name identity (D-18) — no confirm field, no destructive flags"
    - "delete handler: confirm=false -> throws CoolifyApiError code COOLIFY_CONFIRM_REQUIRED; deleteDatabase NOT called (SAF-01, D-15)"
    - "delete handler: confirm=true -> calls deleteDatabase(url, token, uuid, { delete_volumes, delete_configurations, docker_cleanup, delete_connected_networks }, verifySsl) with all four flags defaulting to false (SAF-02, D-16)"
    - "delete_preview handler: returns { uuid, child_resources?, would_delete:true } WITHOUT calling deleteDatabase — Phase 8/9/10 parity (D-15)"
    - "npx vitest run src/mcp/tools/database.test.ts -t 'database (update|delete|delete_preview)' is GREEN"
  artifacts:
    - "src/mcp/tools/database.ts (extended with updateDatabaseSchema + deleteActionSchema + deletePreviewActionSchema + handleDatabaseUpdate + handleDatabaseDelete + handleDatabaseDeletePreview)"
  key_links:
    - "Imports updateDatabase + deleteDatabase + fetchDatabase + fetchResources from ../../api/client.js (added in 11-01)"
    - "Imports sanitizeFullProjection from ../../utils/projections.js for SAF-04 masking"
    - "Imports resolveDatabaseUuid (existing in database.ts) for uuid|name resolution (D-18)"
    - "Imports RECOVERY_HINTS + CoolifyApiError + wrapMcpError from ../../utils/errors.js (Phase 10 enriched)"
    - "Reuses Phase 8/9/10 validateDeleteConfirm pattern — copy into database.ts (per D-17 discretion — copy-paste to avoid cross-file refactor risk)"
---

<objective>
Wave 3 database update + delete + delete_preview handler: curated PATCH, public access confirm gate, safe defaults, delete_preview.

Purpose: Implement the `update`, `delete`, and `delete_preview` actions on the `database` tool. Curated PATCH body (DB-02), public access confirm gate on update (D-12 update-path — planner discretion exercised for safety consistency), confirm gate (SAF-01, D-15), safe delete defaults (SAF-02, D-16), delete_preview two-stage model (Phase 8/9/10 parity, D-15). Masked responses unless reveal:true (SAF-04, D-10).

Output: `updateDatabaseSchema` + `deleteActionSchema` + `deletePreviewActionSchema` + `handleDatabaseUpdate` + `handleDatabaseDelete` + `handleDatabaseDeletePreview` wired into `handleDatabaseAction` switch. Flips the `database update`, `database delete`, and `database delete_preview` RED scaffolds from 11-00 to GREEN. Phase 11 complete.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-service-database-crud/11-CONTEXT.md
@.planning/phases/11-service-database-crud/11-RESEARCH.md
@.planning/phases/11-service-database-crud/11-PATTERNS.md
@.planning/phases/11-service-database-crud/11-03-SUMMARY.md
@.planning/phases/10-application-crud-safety/10-03-PLAN.md
@.planning/phases/10-application-crud-safety/10-04-PLAN.md
@src/mcp/tools/database.ts
@src/mcp/tools/application.ts
@src/mcp/tools/server.ts
@src/mcp/tools/project.ts
@src/utils/projections.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add updateDatabaseSchema + deleteActionSchema + deletePreviewActionSchema (D-12, D-15, D-16, D-18, DB-02, DB-03, SAF-01, SAF-02, SAF-03, SAF-04)</name>
  <files>src/mcp/tools/database.ts</files>
  <read_first>
    - src/mcp/tools/database.ts (after 11-03 — createDatabaseSchema in place; lines 37-79 for requireDatabaseIdentifier pattern; lines 117-134 for databaseActionSchema discriminatedUnion)
    - src/mcp/tools/application.ts (lines 87-100 for requireMutationIdentifier; lines 238-249 for applicationActionSchema discriminatedUnion)
    - src/mcp/tools/server.ts (lines 99-121 for deleteActionSchema + deletePreviewActionSchema pattern with confirm + delete_volumes defaults)
    - src/mcp/tools/project.ts (lines 99-133 for update + delete + delete_preview schema patterns)
    - docs/coolify_openapi.yaml (lines 2808-2900 for PATCH /databases/{uuid} field list; 2744-2807 for DELETE /databases/{uuid} query params)
    - .planning/phases/11-service-database-crud/11-CONTEXT.md (D-12, D-15..D-18)
  </read_first>
  <behavior>
    - updateDatabaseSchema = z.object({ action:z.literal('update'), uuid?:string, name?:string, ...curatedFields, confirm:z.boolean().default(false), reveal:z.boolean().default(false), ...mutationResponseParamsSchema }).strict().superRefine(requireDatabaseIdentifier('update'))
    - Curated update fields (OpenAPI PATCH /databases/{uuid}): name, description, image, is_public, public_port, public_port_timeout, limits_memory, limits_memory_swap, limits_memory_swappiness, limits_memory_reservation, limits_cpus, limits_cpuset, limits_cpu_shares, plus engine-specific credential fields (postgres_user/postgres_password/postgres_db, mysql_user/mysql_password/mysql_root_password/mysql_database, mariadb_*, redis_password, clickhouse_admin_user/clickhouse_admin_password, dragonfly_password, keydb_password)
    - D-12 update-path confirm gate (planner discretion): superRefine — if is_public === true && confirm !== true -> add COOLIFY_CONFIRM_REQUIRED issue (mirror create-path gate for safety consistency)
    - deleteActionSchema = z.object({ action:z.literal('delete'), uuid?:string, name?:string, confirm:z.boolean().default(false), delete_volumes:z.boolean().default(false), delete_configurations:z.boolean().default(false), docker_cleanup:z.boolean().default(false), delete_connected_networks:z.boolean().default(false), ...mutationResponseParamsSchema }).strict().superRefine(requireDatabaseIdentifier('delete'))
    - deletePreviewActionSchema = z.object({ action:z.literal('delete_preview'), uuid?:string, name?:string, ...mutationResponseParamsSchema }).strict().superRefine(requireDatabaseIdentifier('delete_preview'))
    - All three schemas accept uuid|name identity (D-18)
    - delete schema: confirm defaults false (SAF-01, D-15); all four destructive flags default false (SAF-02, D-16) — overrides Coolify's API-side default of true
    - delete_preview schema: no confirm field, no destructive flags (D-15 — preview is non-destructive)
    - Add 'update', 'delete', 'delete_preview' branches to databaseActionSchema discriminatedUnion (now 8 entries — get/start/stop/restart/create/update/delete/delete_preview)
  </behavior>
  <action>
    Add `updateDatabaseSchema`, `deleteActionSchema`, and `deletePreviewActionSchema` as `z.object({...}).strict()` with the fields listed above. Reuse the existing `requireDatabaseIdentifier(schema, 'update'|'delete'|'delete_preview')` helper for the uuid|name superRefine (D-18).

    For `updateDatabaseSchema`, add the D-12 update-path confirm gate via a `requireConfirmForPublicAccess(data, ctx)` helper (same as create-path gate in 11-03) that adds a COOLIFY_CONFIRM_REQUIRED issue when `is_public === true && confirm !== true`. Use `params: { code: 'COOLIFY_CONFIRM_REQUIRED' }` so throwValidationError maps it correctly.

    Critical: the four destructive flags (delete_volumes, delete_configurations, docker_cleanup, delete_connected_networks) MUST default to `false` (SAF-02, D-16). The Coolify API defaults them to `true`, so the MCP handler must explicitly pass `false` when the caller omits them — the Zod default handles this at parse time.

    Add all three schemas to the `databaseActionSchema` discriminatedUnion array (now 8 entries).

    Do NOT implement the handlers in this task — only the schemas.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/database.test.ts -t "database (update|delete|delete_preview)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "updateDatabaseSchema" src/mcp/tools/database.ts` returns at least 2 (definition + inclusion in discriminatedUnion)
    - `grep -c "deleteActionSchema" src/mcp/tools/database.ts` returns at least 2
    - `grep -c "deletePreviewActionSchema" src/mcp/tools/database.ts` returns at least 2
    - `grep -c "delete_volumes" src/mcp/tools/database.ts` returns at least 1
    - `grep -c "COOLIFY_CONFIRM_REQUIRED" src/mcp/tools/database.ts` returns at least 3 (create gate from 11-03 + update gate + handler throw in Task 2)
    - `databaseActionSchema.safeParse({ action:'update', uuid:'x', name:'new-name' }).success === true`
    - `databaseActionSchema.safeParse({ action:'update', uuid:'x', is_public:true, public_port:5432 }).success === false` (D-12 update-path gate)
    - `databaseActionSchema.safeParse({ action:'update', uuid:'x', is_public:true, public_port:5432, confirm:true }).success === true`
    - `databaseActionSchema.safeParse({ action:'update', name:'new-name' }).success === false` (no identity — D-18)
    - `databaseActionSchema.safeParse({ action:'delete', uuid:'x', confirm:true }).success === true`
    - `databaseActionSchema.safeParse({ action:'delete', uuid:'x' }).success === true` (confirm defaults false)
    - `databaseActionSchema.safeParse({ action:'delete', confirm:true }).success === false` (no identity — D-18)
    - `databaseActionSchema.safeParse({ action:'delete_preview', uuid:'x' }).success === true`
    - Schema-acceptance tests in `database update`, `database delete`, `database delete_preview` describe blocks flip GREEN; handler-call tests still RED until Task 2
  </acceptance_criteria>
  <done>
    - update + delete + delete_preview schemas committed; schema-level tests GREEN; handler-call tests still RED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement handleDatabaseUpdate + handleDatabaseDelete + handleDatabaseDeletePreview (DB-02, DB-03, DB-04 update path, D-10, D-12, D-15..D-18, SAF-01, SAF-02, SAF-03, SAF-04)</name>
  <files>src/mcp/tools/database.ts</files>
  <read_first>
    - src/mcp/tools/database.ts (after Task 1 — update + delete + delete_preview schemas in place; lines 175-232 for resolveDatabaseUuid; lines 276-336 for handleDatabaseAction switch)
    - src/mcp/tools/application.ts (10-03 Task 2 for update handler pattern; 10-04 Task 2 for delete + delete_preview handler pattern)
    - src/mcp/tools/server.ts (lines 254-268 for validateDeleteConfirm pattern; lines 432-488 for delete + delete_preview handler)
    - src/mcp/tools/project.ts (lines 221-235 for validateDeleteConfirm; lines 471-543 for delete + delete_preview handler)
    - src/utils/projections.ts (sanitizeFullProjection signature for SAF-04 masking)
    - .planning/phases/11-service-database-crud/11-CONTEXT.md (D-10, D-12, D-15..D-18)
  </read_first>
  <behavior>
    - handleDatabaseUpdate(parsed, env):
      - resolve uuid via resolveDatabaseUuid({ uuid, name }, env) (D-18)
      - D-12 update-path confirm gate is enforced at schema level (Task 1 superRefine) — handler does not need to re-check; if parsed passed schema, is_public:true implies confirm:true was supplied
      - build curated PATCH payload from parsed fields (name, description, image, is_public, public_port, public_port_timeout, limits_*, engine-specific credential fields); omit undefined fields
      - call updateDatabase(env.COOLIFY_URL, env.COOLIFY_TOKEN, uuid, payload, env.COOLIFY_VERIFY_SSL)
      - on 200 success: fetch updated database via fetchDatabase; route through sanitizeFullProjection(raw, parsed.reveal) so engine-specific password fields and connection strings are masked as '***' unless reveal:true (D-10, SAF-04); return via buildReadResponse
    - handleDatabaseDelete(parsed, env):
      - resolve uuid via resolveDatabaseUuid({ uuid, name }, env) (D-18)
      - validateDeleteConfirm(parsed.confirm, uuid) — throws COOLIFY_CONFIRM_REQUIRED if confirm is false (SAF-01, D-15)
      - call deleteDatabase(env.COOLIFY_URL, env.COOLIFY_TOKEN, uuid, { delete_volumes: parsed.delete_volumes, delete_configurations: parsed.delete_configurations, docker_cleanup: parsed.docker_cleanup, delete_connected_networks: parsed.delete_connected_networks }, env.COOLIFY_VERIFY_SSL) — all four flags default false (SAF-02, D-16)
      - return { ok:true, uuid, deleted:true, delete_volumes: parsed.delete_volumes, delete_configurations: parsed.delete_configurations } via buildReadResponse
    - handleDatabaseDeletePreview(parsed, env):
      - resolve uuid via resolveDatabaseUuid (D-18)
      - fetch child resources: fetchResources filtered to records where database_uuid === uuid OR (Coolify 4.1.x shape) records in the same project+environment that depend on this database — list as { uuid, name?, type? }[] (D-15 — Phase 8/9/10 parity)
      - return { uuid, child_resources, would_delete:true, warning? } — warning string when child_resources.length > 0; deleteDatabase NOT called
    - Wire 'update', 'delete', 'delete_preview' cases into handleDatabaseAction switch
  </behavior>
  <action>
    Implement `validateDeleteConfirm(confirm, uuid)` in database.ts (copy the Phase 8/9/10 pattern — server.ts:254-268 / project.ts:221-235 / application.ts). Per D-17 discretion, copy-paste is acceptable for the canonical SAF reference — choose copy-paste to avoid cross-file refactor risk in this plan.

    Implement `handleDatabaseUpdate(parsed, env)`:
    1. `const uuid = await resolveDatabaseUuid({ uuid: parsed.uuid, name: parsed.name }, env);` (D-18)
    2. Build the curated payload: iterate the curated field list from the schema; for each field, if `parsed[field] !== undefined`, add to payload. Include engine-specific credential fields (postgres_user, postgres_password, postgres_db, mysql_*, mariadb_*, redis_password, clickhouse_admin_user, clickhouse_admin_password, dragonfly_password, keydb_password) when set. Omit `action`, `uuid`, `name` (identity), `confirm`, `format`, `max_chars`, `reveal` from the payload (those are MCP-layer concerns).
    3. `await updateDatabase(env.COOLIFY_URL, env.COOLIFY_TOKEN, uuid, payload, env.COOLIFY_VERIFY_SSL);`
    4. On success: fetch updated state via `fetchDatabase(env.COOLIFY_URL, env.COOLIFY_TOKEN, uuid, env.COOLIFY_VERIFY_SSL)`. Route through `sanitizeFullProjection(raw, parsed.reveal)` (D-10, SAF-04). Return via `buildReadResponse(data, { format: parsed.format, max_chars: parsed.max_chars })`.
    5. The D-12 update-path confirm gate is enforced at schema level (Task 1 superRefine) so the handler does not need to re-check — if parsed passed schema, is_public:true implies confirm:true was supplied.

    Implement `handleDatabaseDelete(parsed, env)`:
    1. `const uuid = await resolveDatabaseUuid({ uuid: parsed.uuid, name: parsed.name }, env);`
    2. `validateDeleteConfirm(parsed.confirm, uuid);` — throws COOLIFY_CONFIRM_REQUIRED if confirm is false (SAF-01, D-15).
    3. `await deleteDatabase(env.COOLIFY_URL, env.COOLIFY_TOKEN, uuid, { delete_volumes: parsed.delete_volumes, delete_configurations: parsed.delete_configurations, docker_cleanup: parsed.docker_cleanup, delete_connected_networks: parsed.delete_connected_networks }, env.COOLIFY_VERIFY_SSL);` — all four flags default false via Zod (SAF-02, D-16).
    4. Return `buildReadResponse({ ok:true, uuid, deleted:true, delete_volumes: parsed.delete_volumes, delete_configurations: parsed.delete_configurations }, { format: parsed.format, max_chars: parsed.max_chars })`.

    Implement `handleDatabaseDeletePreview(parsed, env)`:
    1. `const uuid = await resolveDatabaseUuid({ uuid: parsed.uuid, name: parsed.name }, env);`
    2. `const resources = await fetchResources(env.COOLIFY_URL, env.COOLIFY_TOKEN, env.COOLIFY_VERIFY_SSL);`
    3. Filter resources to children of this database — records where `database_uuid === uuid` OR (for Coolify 4.1.x shape) records in the same project+environment that depend on this database. Map to `{ uuid, name?, type? }[]`. (If the API does not expose a clean parent-child link, return an empty child_resources array with would_delete:true — the preview is non-blocking per D-15 discretion.)
    4. Build response `{ uuid, child_resources, would_delete: true }`. If `child_resources.length > 0`, add `warning: 'Database has child resources that will also be removed or orphaned — review child_resources before confirming delete.'` (Phase 8/9/10 pattern).
    5. Return via `buildReadResponse(response, { format: parsed.format, max_chars: parsed.max_chars })`. Do NOT call deleteDatabase.

    Wire `case 'update': return await handleDatabaseUpdate(parsed, env);`, `case 'delete': return await handleDatabaseDelete(parsed, env);`, and `case 'delete_preview': return await handleDatabaseDeletePreview(parsed, env);` into `handleDatabaseAction` switch (before the `default`).

    The outer `wrapMcpError` in `handleDatabaseAction` handles all error propagation — no new try/catch needed in the handlers themselves (validateDeleteConfirm throws, which the outer wrapper catches).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/database.test.ts -t "database (update|delete|delete_preview)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleDatabaseUpdate" src/mcp/tools/database.ts` returns at least 2 (definition + switch case)
    - `grep -c "handleDatabaseDelete" src/mcp/tools/database.ts` returns at least 3 (definition + 2 switch cases — delete + delete_preview share the prefix)
    - `grep -c "handleDatabaseDeletePreview" src/mcp/tools/database.ts` returns at least 2
    - `grep -c "case 'update'" src/mcp/tools/database.ts` returns 1
    - `grep -c "case 'delete'" src/mcp/tools/database.ts` returns 1
    - `grep -c "case 'delete_preview'" src/mcp/tools/database.ts` returns 1
    - `grep -c "validateDeleteConfirm" src/mcp/tools/database.ts` returns at least 2 (definition + call)
    - `npx vitest run src/mcp/tools/database.test.ts -t "database update"` is GREEN
    - `npx vitest run src/mcp/tools/database.test.ts -t "database delete"` is GREEN
    - `npx vitest run src/mcp/tools/database.test.ts -t "database delete_preview"` is GREEN
    - DB-02: update with name + is_public:false + public_port -> updateDatabase called with curated payload; response reflects changed fields
    - DB-04 (update-path confirm gate): update with is_public:true but no confirm -> COOLIFY_CONFIRM_REQUIRED; updateDatabase NOT called (vi.fn() not-called assertion)
    - DB-04 (update happy path): update with is_public:true, public_port:5432, confirm:true -> updateDatabase called with is_public:true and public_port in body
    - SAF-04: update response masks engine-specific password (postgres_password / mysql_password / redis_password / etc.) as '***' unless reveal:true
    - DB-03: delete with confirm:true -> deleteDatabase called with all four flags false by default; response { ok:true, uuid, deleted:true, ... }
    - SAF-01: delete without confirm -> COOLIFY_CONFIRM_REQUIRED; deleteDatabase NOT called (vi.fn() not-called assertion)
    - SAF-02: delete with confirm:true, no override flags -> deleteDatabase called with { delete_volumes:false, delete_configurations:false, docker_cleanup:false, delete_connected_networks:false }
    - D-18: update/delete by name with multi-match -> COOLIFY_AMBIGUOUS_MATCH, mutation NOT called
    - SAF-03 (update path): update with unknown field -> Zod rejects (strict schema) before any API call; updateDatabase NOT called
    - delete_preview: returns { uuid, child_resources, would_delete:true } without calling deleteDatabase
    - `npm run build` is green
    - All existing database tests (get/create/start/stop/restart) still GREEN
  </acceptance_criteria>
  <done>
    - Update + delete + delete_preview handlers committed; all `database update`, `database delete`, `database delete_preview` RED scaffolds from 11-00 flipped GREEN; build green; Phase 11 handler work complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> MCP update/delete handler | Untrusted input; Zod .strict() + superRefine is the first gate (SAF-03) |
| MCP handler -> Coolify PATCH /databases/{uuid} | Existing bearer auth + retry + error mapping |
| MCP handler -> Coolify DELETE /databases/{uuid} | Destructive action gated by confirm (SAF-01) + safe defaults (SAF-02) |
| MCP handler -> Coolify GET /resources (for delete_preview) | Read-only; no destructive side-effect |
| Coolify 200 response -> MCP update result | Routed through sanitizeFullProjection for SAF-04 masking |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05-01 | Information Disclosure | database credentials in update response | high | mitigate | sanitizeFullProjection masks engine-specific password fields as '***' unless reveal:true (D-10, SAF-04). Test asserts masked by default. |
| T-11-05-02 | Tampering | delete without confirm | high | mitigate | validateDeleteConfirm throws COOLIFY_CONFIRM_REQUIRED when confirm is false; deleteDatabase NOT called (SAF-01, D-15). Test asserts vi.fn() not-called. |
| T-11-05-03 | Tampering | delete with unsafe defaults | high | mitigate | Zod defaults delete_volumes/delete_configurations/docker_cleanup/delete_connected_networks to false (SAF-02, D-16). Handler passes parsed values to deleteDatabase query params. Coolify API defaults these to true — MCP overrides to false at the Zod layer. |
| T-11-05-04 | Tampering | update payload validation | high | mitigate | Zod .strict() rejects unknown fields; curated allowlist prevents mass-assignment of unintended fields. |
| T-11-05-05 | Information Disclosure | accidental public DB port exposure on update | high | mitigate | D-12 update-path confirm gate (planner discretion): is_public:true requires confirm:true at schema level. Missing confirm -> COOLIFY_CONFIRM_REQUIRED before any updateDatabase call. Mirrors create-path gate for safety consistency. |
| T-11-05-06 | Spoofing | identity resolution by name | medium | mitigate | resolveDatabaseUuid multi-match -> COOLIFY_AMBIGUOUS_MATCH, no mutation (D-18). Existing Phase 5 pattern. |
| T-11-05-07 | Denial of Service | accidental delete via ambiguous name | medium | mitigate | COOLIFY_AMBIGUOUS_MATCH refuses mutation on multi-match (D-18) — agent must narrow to a single UUID. |
| T-11-SC | Tampering | npm installs | high | mitigate | No new packages — reuses zod/ofetch already pinned and audited. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/database.test.ts` — full file GREEN (all 4 database requirements covered: create 8 engines, update, delete, delete_preview, public access confirm gate, instant_deploy, masking, Zod validation, confirm gate, safe defaults)
- `npm run build` (tsup) — green
- `npm run test` — full suite green (no regression in Phase 8/9/10 tests)
- `grep -c "handleDatabaseUpdate" src/mcp/tools/database.ts` — at least 2
- `grep -c "handleDatabaseDelete" src/mcp/tools/database.ts` — at least 3 (definition + 2 switch cases — delete + delete_preview share the prefix)
- `grep -c "validateDeleteConfirm" src/mcp/tools/database.ts` — at least 2 (definition + call)
- `grep -c "COOLIFY_CONFIRM_REQUIRED" src/mcp/tools/database.ts` — at least 3
</verification>

<success_criteria>
- Update with curated fields + engine-specific credentials (DB-02)
- Update-path public access confirm gate (DB-04 update path, D-12 planner discretion)
- Credentials masked unless reveal:true (SAF-04, D-10)
- Delete requires confirm:true or returns COOLIFY_CONFIRM_REQUIRED (SAF-01, D-15, DB-03)
- Delete defaults delete_volumes/delete_configurations/docker_cleanup/delete_connected_networks to false (SAF-02, D-16, DB-03)
- delete_preview returns would_delete + child_resources without deleting (D-15, Phase 8/9/10 parity)
- Identity resolution uuid|name with COOLIFY_AMBIGUOUS_MATCH (D-18)
- Zod validates before any API call (SAF-03)
- 11-00 `database update`, `database delete`, `database delete_preview` RED scaffolds flipped GREEN
- Phase 11 complete: all 9 requirements (SVC-06..SVC-10, DB-01..DB-04) implemented and tested
- No regression in existing database actions or Phase 8/9/10 tests
</success_criteria>

<output>
Create `.planning/phases/11-service-database-crud/11-05-SUMMARY.md` when done.
</output>
