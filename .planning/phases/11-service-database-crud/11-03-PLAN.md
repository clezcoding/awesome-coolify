---
phase: 11-service-database-crud
plan: 03
type: execute
wave: 2
depends_on:
  - 11-00
  - 11-01
files_modified:
  - src/mcp/tools/database.ts
autonomous: true
requirements:
  - DB-01
  - DB-04
must_haves:
  truths:
    - "databaseActionSchema discriminatedUnion includes a 'create' branch keyed by `engine` discriminator with 8 variants: postgresql, mysql, mariadb, mongodb, redis, clickhouse, dragonfly, keydb (D-08)"
    - "create schema requires server_uuid (D-03, D-10); project_uuid XOR project_name; environment_name or environment_uuid; no silent production default — missing -> COOLIFY_VALIDATION_ERROR with ask-human hint"
    - "create schema: instant_deploy defaults to true (D-11 override vs Phase 10 apps default false)"
    - "create schema: is_public defaults false; public_port optional; when is_public === true -> superRefine requires confirm === true else COOLIFY_CONFIRM_REQUIRED at handler time (D-12)"
    - "create schema: engine-specific credential fields per OpenAPI (postgres_user/postgres_password/postgres_db for postgresql; clickhouse_admin_user/clickhouse_admin_password for clickhouse; mongo_initdb_root_password/mongo_initdb_database for mongodb per Pitfall 2; etc.) — caller-supplied only, MCP does not generate secrets (D-09)"
    - "create schema .strict() rejects unknown fields (SAF-03)"
    - "handler dispatches by parsed.engine to the matching create<Engine>Database client function (8 routes)"
    - "handler enforces D-12 confirm gate: if is_public === true && confirm !== true -> throw CoolifyApiError code COOLIFY_CONFIRM_REQUIRED with recoveryHints; create<Engine>Database NOT called"
    - "handler resolves project_uuid from project_name via fetchProjects + resolveProjectUuid when only project_name is given (D-03)"
    - "handler builds curated API body from parsed fields; omits undefined fields"
    - "handler calls matching create<Engine>Database(env.COOLIFY_URL, env.COOLIFY_TOKEN, body, env.COOLIFY_VERIFY_SSL)"
    - "on 200 success: extract database UUID; if instant_deploy true (default) fire-and-forget triggerDatabaseStart; on queue failure return soft success ok:true with start.status='failed_to_queue' + recovery hints — no auto-rollback/delete (D-13)"
    - "create response masks engine-specific password fields and connection strings via sanitizeFullProjection unless reveal:true (SAF-04, D-10)"
    - "Zod validates create payload before any API call (SAF-03); malformed payloads never reach the client"
    - "npx vitest run src/mcp/tools/database.test.ts -t 'database create' is GREEN — 11-00 RED scaffolds flipped green"
  artifacts:
    - "src/mcp/tools/database.ts (extended with createDatabaseSchema + handleDatabaseCreate)"
  key_links:
    - "Imports 8 create<Engine>Database functions + triggerDatabaseStart from ../../api/client.js (added in 11-01)"
    - "Imports COOLIFY_VALIDATION_ERROR + COOLIFY_CONFIRM_REQUIRED + RECOVERY_HINTS + CoolifyApiError + wrapMcpError from ../../utils/errors.js (Phase 10 enriched)"
    - "Imports resolveProjectUuid from ../../utils/project-lookup.js (existing Phase 9 util) for project_name -> project_uuid resolution"
    - "Imports sanitizeFullProjection from ../../utils/projections.js (existing) for SAF-04 masking"
---

<objective>
Wave 2 database create handler: 8 engines, public access confirm gate (D-12), instant_deploy default true, secret masking.

Purpose: Implement the `create` action on the `database` tool. Eight Coolify POST routes dispatched by an `engine` discriminator. Public access (is_public:true) requires confirm:true (D-12 — security-sensitive exposure). instant_deploy defaults true (D-11). Caller-supplied credentials only (D-09). Masked responses unless reveal:true (SAF-04, D-10). Soft success on start-queue failure (D-13).

Output: `createDatabaseSchema` (8-variant discriminated union by engine) + `handleDatabaseCreate` wired into `handleDatabaseAction` switch. Flips the `database create` RED scaffolds from 11-00 to GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-service-database-crud/11-CONTEXT.md
@.planning/phases/11-service-database-crud/11-RESEARCH.md
@.planning/phases/11-service-database-crud/11-PATTERNS.md
@.planning/phases/11-service-database-crud/11-00-SUMMARY.md
@.planning/phases/11-service-database-crud/11-01-SUMMARY.md
@.planning/phases/10-application-crud-safety/10-02-PLAN.md
@src/mcp/tools/database.ts
@src/mcp/tools/application.ts
@src/utils/projections.ts
@src/utils/project-lookup.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add createDatabaseSchema — 8-variant discriminated union by engine (D-03, D-08, D-09, D-10, D-11, D-12, SAF-03, SAF-04)</name>
  <files>src/mcp/tools/database.ts</files>
  <read_first>
    - src/mcp/tools/database.ts (lines 1-40 for imports; lines 37-79 for requireDatabaseIdentifier pattern; lines 117-134 for databaseActionSchema discriminatedUnion)
    - src/mcp/tools/application.ts (lines 238-249 for discriminatedUnion pattern; lines 395-401 for project/env superRefine)
    - docs/coolify_openapi.yaml (lines 3124-3230 POST /databases/postgresql; 3231-3325 clickhouse; 3326-3417 dragonfly; 3418-3512 redis; 3513-3607 keydb; 3608-3711 mariadb; 3712-3815 mysql; 3816-3910 mongodb)
    - .planning/phases/11-service-database-crud/11-CONTEXT.md (D-08..D-13)
    - .planning/phases/11-service-database-crud/11-RESEARCH.md (Pitfall 2 — MongoDB mongo_initdb_root_password / mongo_initdb_database)
  </read_first>
  <behavior>
    - createDatabaseSchema = z.discriminatedUnion('engine', [postgresql, mysql, mariadb, mongodb, redis, clickhouse, dragonfly, keydb])
    - All 8 variants share: action:z.literal('create'), engine:z.literal('<engine>'), server_uuid:z.string(), project_uuid?:z.string(), project_name?:z.string(), environment_name?:z.string(), environment_uuid?:z.string(), name?:z.string(), description?:z.string(), image?:z.string(), is_public:z.boolean().default(false), public_port?:z.number().int(), public_port_timeout?:z.number().int(), limits_memory?:z.string(), limits_memory_swap?:z.string(), limits_memory_swappiness?:z.number().int(), limits_memory_reservation?:z.string(), limits_cpus?:z.string(), limits_cpuset?:z.string(), limits_cpu_shares?:z.number().int(), destination_uuid?:z.string(), instant_deploy:z.boolean().default(true) (D-11), confirm:z.boolean().default(false) (D-12 gate), reveal:z.boolean().default(false) (SAF-04), ...mutationResponseParamsSchema
    - postgresql variant: engine:z.literal('postgresql'), postgres_user?:string, postgres_password?:string, postgres_db?:string, postgres_initdb_args?:string, postgres_host_auth_method?:string, postgres_conf?:string
    - mysql variant: engine:z.literal('mysql'), mysql_user?:string, mysql_password?:string, mysql_root_password?:string, mysql_database?:string
    - mariadb variant: engine:z.literal('mariadb'), mariadb_user?:string, mariadb_password?:string, mariadb_root_password?:string, mariadb_database?:string
    - mongodb variant: engine:z.literal('mongodb'), mongo_initdb_root_password?:string, mongo_initdb_database?:string (per Pitfall 2 — OpenAPI omits these on POST but Coolify accepts them; include in schema)
    - redis variant: engine:z.literal('redis'), redis_password?:string
    - clickhouse variant: engine:z.literal('clickhouse'), clickhouse_admin_user?:string, clickhouse_admin_password?:string
    - dragonfly variant: engine:z.literal('dragonfly'), dragonfly_password?:string
    - keydb variant: engine:z.literal('keydb'), keydb_password?:string
    - superRefine on each variant: at least one of project_uuid or project_name -> else COOLIFY_VALIDATION_ERROR (D-03)
    - superRefine on each variant: at least one of environment_name or environment_uuid -> else COOLIFY_VALIDATION_ERROR (D-03)
    - superRefine on each variant: if is_public === true && confirm !== true -> add COOLIFY_CONFIRM_REQUIRED issue (D-12 — schema-level gate so the error is raised before any client call; mirrors SAF-01 confirm gate pattern)
    - All superRefine issues use `params: { code: 'COOLIFY_VALIDATION_ERROR' }` or `params: { code: 'COOLIFY_CONFIRM_REQUIRED' }` as appropriate
    - Add 'create' branch to databaseActionSchema discriminatedUnion
  </behavior>
  <action>
    Add `createDatabaseSchema` as a `z.discriminatedUnion('engine', [...])` with 8 object variants. Each variant is `z.object({...}).strict()` with the shared fields plus engine-specific credential fields listed above. Apply the project/environment superRefine via a shared helper `requireProjectAndEnvironment(data, ctx)` that adds COOLIFY_VALIDATION_ERROR issues. Apply the D-12 public-access confirm gate via a `requireConfirmForPublicAccess(data, ctx)` helper that adds a COOLIFY_CONFIRM_REQUIRED issue when `is_public === true && confirm !== true`.

    Use `params: { code: 'COOLIFY_VALIDATION_ERROR' }` on project/env superRefine issues and `params: { code: 'COOLIFY_CONFIRM_REQUIRED' }` on the public-access gate issue so `throwValidationError` maps them to the correct error codes.

    Add the `createDatabaseSchema` to the `databaseActionSchema` discriminatedUnion array (currently 4 entries — get/start/stop/restart — add create as the 5th).

    Do NOT implement the handler in this task — only the schema.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/database.test.ts -t "database create"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createDatabaseSchema" src/mcp/tools/database.ts` returns at least 2 (definition + inclusion in discriminatedUnion)
    - `grep -c "discriminatedUnion('engine'" src/mcp/tools/database.ts` returns 1
    - `grep -c "COOLIFY_VALIDATION_ERROR" src/mcp/tools/database.ts` returns at least 2 (project refine + env refine)
    - `grep -c "COOLIFY_CONFIRM_REQUIRED" src/mcp/tools/database.ts` returns at least 2 (schema gate + handler throw in Task 2)
    - `databaseActionSchema.safeParse({ action:'create', engine:'postgresql', server_uuid:'s', project_uuid:'p', environment_name:'production', postgres_user:'u', postgres_password:'pw', postgres_db:'db' }).success === true`
    - `databaseActionSchema.safeParse({ action:'create', engine:'redis', server_uuid:'s', environment_name:'production' }).success === false` (no project — D-03)
    - `databaseActionSchema.safeParse({ action:'create', engine:'postgresql', server_uuid:'s', project_uuid:'p', environment_name:'production', is_public:true, public_port:5432 }).success === false` (D-12 — public without confirm)
    - `databaseActionSchema.safeParse({ action:'create', engine:'postgresql', server_uuid:'s', project_uuid:'p', environment_name:'production', is_public:true, public_port:5432, confirm:true }).success === true`
    - Schema-acceptance tests in `database create` describe block flip GREEN; handler-call tests still RED until Task 2
  </acceptance_criteria>
  <done>
    - createDatabaseSchema committed; schema-level RED tests (D-03, D-08, D-12, SAF-03) flip GREEN; handler-call tests still RED until Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement handleDatabaseCreate — 8-engine dispatcher, D-12 confirm gate, instant_deploy fire-and-forget, masking (DB-01, DB-04, D-03, D-08..D-13, SAF-03, SAF-04)</name>
  <files>src/mcp/tools/database.ts</files>
  <read_first>
    - src/mcp/tools/database.ts (after Task 1 — createDatabaseSchema in place; lines 234-274 for handleDatabaseMutation fire-and-forget pattern; lines 276-336 for handleDatabaseAction switch)
    - src/mcp/tools/application.ts (lines 628-693 for handleApplicationDeploy fire-and-forget + soft-success pattern; lines 800-866 for handleApplicationAction switch)
    - src/utils/projections.ts (sanitizeFullProjection signature for SAF-04 masking)
    - src/utils/project-lookup.ts (resolveProjectUuid signature)
    - .planning/phases/11-service-database-crud/11-CONTEXT.md (D-08..D-13)
    - .planning/phases/11-service-database-crud/11-RESEARCH.md (Pitfall 2 — MongoDB fields; Pitfall 3 — fire-and-forget timing)
  </read_first>
  <behavior>
    - handleDatabaseCreate(parsed, env) dispatches by parsed.engine to the matching create<Engine>Database client function (8 routes)
    - D-12 confirm gate is enforced at schema level (Task 1 superRefine) — handler does not need to re-check; if parsed passed schema, is_public:true implies confirm:true was supplied
    - Builds curated API body from parsed fields: server_uuid, project_uuid (resolved from project_name if needed), environment_name or environment_uuid, name, description, image, is_public, public_port, public_port_timeout, limits_*, destination_uuid, instant_deploy (default true per D-11), plus engine-specific credential fields
    - Resolves project_uuid from project_name via fetchProjects + resolveProjectUuid (existing Phase 9 util) when only project_name is given (D-03)
    - Calls matching create<Engine>Database(env.COOLIFY_URL, env.COOLIFY_TOKEN, body, env.COOLIFY_VERIFY_SSL)
    - On 200 success: extract database UUID; if instant_deploy true (default) fire-and-forget triggerDatabaseStart; on queue failure return soft success ok:true with start.status='failed_to_queue' + recovery hints — no auto-rollback/delete (D-13)
    - On success response: route through sanitizeFullProjection(raw, parsed.reveal) so engine-specific password fields and connection strings are masked as '***' unless reveal:true (D-10, SAF-04)
    - Wire 'create' case into handleDatabaseAction switch (before the default)
  </behavior>
  <action>
    Implement `handleDatabaseCreate(parsed, env)`:

    1. Resolve project_uuid: if `parsed.project_uuid` use it; else if `parsed.project_name` call `resolveProjectUuid(undefined, parsed.project_name, env)` (existing util from project-lookup.ts); else Zod superRefine already rejected (D-03).
    2. Build the curated body object: include `server_uuid`, `project_uuid`, `environment_name` (or `environment_uuid` if that's what the caller supplied), `name`, `description`, `image`, `is_public`, `public_port`, `public_port_timeout`, `limits_memory`, `limits_memory_swap`, `limits_memory_swappiness`, `limits_memory_reservation`, `limits_cpus`, `limits_cpuset`, `limits_cpu_shares`, `destination_uuid`, `instant_deploy`. Plus engine-specific credential fields:
       - postgresql: `postgres_user`, `postgres_password`, `postgres_db`, `postgres_initdb_args`, `postgres_host_auth_method`, `postgres_conf`
       - mysql: `mysql_user`, `mysql_password`, `mysql_root_password`, `mysql_database`
       - mariadb: `mariadb_user`, `mariadb_password`, `mariadb_root_password`, `mariadb_database`
       - mongodb: `mongo_initdb_root_password`, `mongo_initdb_database` (per Pitfall 2 — include even though OpenAPI POST omits them)
       - redis: `redis_password`
       - clickhouse: `clickhouse_admin_user`, `clickhouse_admin_password`
       - dragonfly: `dragonfly_password`
       - keydb: `keydb_password`
       Omit undefined fields (don't send null).
    3. Dispatch by `parsed.engine`:
       - 'postgresql' -> createPostgresqlDatabase
       - 'mysql' -> createMysqlDatabase
       - 'mariadb' -> createMariadbDatabase
       - 'mongodb' -> createMongodbDatabase
       - 'redis' -> createRedisDatabase
       - 'clickhouse' -> createClickhouseDatabase
       - 'dragonfly' -> createDragonflyDatabase
       - 'keydb' -> createKeydbDatabase
    4. Extract database UUID from the 200 response (`response.uuid`).
    5. If `parsed.instant_deploy === true` (default):
       - Call `await triggerDatabaseStart(env.COOLIFY_URL, env.COOLIFY_TOKEN, dbUuid, env.COOLIFY_VERIFY_SSL)` (fire-and-forget per D-11, Pitfall 3).
       - Wrap in try/catch. On success: return `{ uuid: dbUuid, deploy: { status: 'queued' }, hints: ['Use database.get with uuid to inspect connection details', 'Use database.start/stop/restart for lifecycle'] }` via buildReadResponse. Route the response through sanitizeFullProjection(raw, parsed.reveal) so credentials are masked.
       - On catch (start failed to queue): return soft success `{ ok: true, uuid: dbUuid, deploy: { status: 'failed_to_queue', error: <message> }, recoveryHints: [...] }` — DO NOT re-throw, DO NOT delete the created database (D-13).
    6. If `parsed.instant_deploy === false`: return `{ uuid: dbUuid, deploy: { status: 'not_triggered' }, hints: ['Use database.start to start the database'] }`.
    7. Wire `case 'create': return await handleDatabaseCreate(parsed, env);` into `handleDatabaseAction` switch (before the `default`).

    Use the existing `wrapMcpError` outer wrapper in `handleDatabaseAction` for all error propagation. The D-12 confirm gate is enforced at schema level (Task 1 superRefine) so the handler does not need to re-check — if parsed passed schema, is_public:true implies confirm:true was supplied.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/database.test.ts -t "database create"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleDatabaseCreate" src/mcp/tools/database.ts` returns at least 2 (definition + switch case)
    - `grep -c "case 'create'" src/mcp/tools/database.ts` returns 1
    - `npx vitest run src/mcp/tools/database.test.ts -t "database create"` is GREEN
    - DB-01 (8 engines): it.each test passes — each engine dispatches to the matching create<Engine>Database client function
    - DB-01 (instant_deploy default true): create without instant_deploy -> create<Engine>Database called with instant_deploy=true
    - DB-04 (confirm gate): create with is_public:true but no confirm -> COOLIFY_CONFIRM_REQUIRED; create<Engine>Database NOT called (vi.fn() not-called assertion)
    - DB-04 (happy path): create with is_public:true, public_port:5432, confirm:true -> create<Engine>Database called with is_public:true and public_port in body
    - SAF-04: create response masks engine-specific password (postgres_password / mysql_password / redis_password / etc.) as '***' unless reveal:true
    - D-13: instant_deploy start-queue failure -> ok:true, uuid returned, deploy.status='failed_to_queue', no deleteDatabase call
    - SAF-03: missing required fields -> COOLIFY_VALIDATION_ERROR before any create<Engine>Database call
    - `npm run build` is green
  </acceptance_criteria>
  <done>
    - Create handler committed; all `database create` RED scaffolds from 11-00 flipped GREEN; build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> MCP create handler | Untrusted input; Zod (SAF-03) + D-12 confirm gate is the first gate before any API call |
| MCP handler -> Coolify POST /databases/{engine} | 8 create routes; existing bearer auth + retry + error mapping |
| Coolify 200 response -> MCP create result | Routed through sanitizeFullProjection for SAF-04 masking of credentials/connection strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03-01 | Information Disclosure | database credentials in create response | high | mitigate | sanitizeFullProjection masks engine-specific password fields (postgres_password, mysql_password, redis_password, clickhouse_admin_password, etc.) as '***' unless reveal:true (D-10, SAF-04). Test asserts masked by default. |
| T-11-03-02 | Tampering | create payload validation | high | mitigate | Zod discriminatedUnion with .strict() on every variant + superRefine for project/env — rejects before any client call (SAF-03). |
| T-11-03-03 | Information Disclosure | accidental public DB port exposure | high | mitigate | D-12 confirm gate: is_public:true requires confirm:true at schema level (superRefine). Missing confirm -> COOLIFY_CONFIRM_REQUIRED before any create<Engine>Database call. Test asserts vi.fn() not-called. |
| T-11-03-04 | Spoofing | project_uuid resolution from name | medium | mitigate | resolveProjectUuid uses fetchProjects + name match; multi-match -> COOLIFY_AMBIGUOUS_MATCH (existing Phase 9 pattern, D-18 spirit). |
| T-11-03-05 | Tampering | caller-supplied credentials | medium | accept | D-09 — MCP does not generate secrets; caller supplies all passwords. MCP only masks them in responses. |
| T-11-SC | Tampering | npm installs | high | mitigate | No new packages in this plan — reuses yaml (installed in 11-01, audited [OK]) + zod/ofetch already pinned. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/database.test.ts -t "database create"` — GREEN
- `npm run build` (tsup) — green
- `grep -c "handleDatabaseCreate" src/mcp/tools/database.ts` — at least 2
- `grep -c "case 'create'" src/mcp/tools/database.ts` — 1
- `grep -c "discriminatedUnion('engine'" src/mcp/tools/database.ts` — 1
- `grep -c "COOLIFY_CONFIRM_REQUIRED" src/mcp/tools/database.ts` — at least 2 (schema-level D-12 gate from Task 1 superRefine + handler throw for delete confirm gate from Task 2). D-12 public-access enforcement is schema-only — handler does not re-check `is_public && confirm` because the schema superRefine already rejects that case before the handler runs.
- Existing `database get`/`start`/`stop`/`restart` tests still GREEN (no regression)
</verification>

<success_criteria>
- All 8 database engines dispatch to the correct Coolify POST route (DB-01)
- Public access (is_public:true) requires confirm:true or returns COOLIFY_CONFIRM_REQUIRED (DB-04, D-12)
- instant_deploy defaults true, fire-and-forget, soft success on queue failure (D-11, D-13)
- Credentials masked unless reveal:true (SAF-04, D-10)
- Zod rejects malformed payloads before any API call (SAF-03, D-03)
- MongoDB accepts mongo_initdb_root_password / mongo_initdb_database on create (Pitfall 2)
- 11-00 `database create` RED scaffolds flipped GREEN
- No regression in existing database actions
</success_criteria>

<output>
Create `.planning/phases/11-service-database-crud/11-03-SUMMARY.md` when done.
</output>
