---
phase: 19-dx-schemas-mcp-prompts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/shared-read-params.ts
  - src/mcp/tools/system.ts
  - src/mcp/tools/resource.ts
  - src/mcp/tools/docs.ts
  - src/mcp/tools/meta.ts
  - src/mcp/tools/diagnose.ts
  - src/mcp/tools/deployment.ts
  - src/mcp/tools/instance.ts
  - src/mcp/tools/manifest.ts
  - src/mcp/tools/application.ts
  - src/mcp/tools/service.ts
  - src/mcp/tools/database.ts
  - src/mcp/tools/private_key.ts
  - src/mcp/tools/server.ts
  - src/mcp/tools/project.ts
  - src/mcp/tools/environment.ts
  - src/mcp/tools/emergency.ts
autonomous: true
requirements:
  - DX-02
user_setup: []

must_haves:
  truths:
    - A single flat z.object with action enum plus optional action-specific fields is the top-level JSON Schema for every domain tool registered in src/mcp/server.ts (D-01, D-02)
    - No top-level z.discriminatedUnion or z.union remains in any src/mcp/tools/*.ts file exported as the tool inputSchema (D-01)
    - The existing { action, ...fields } call shape is preserved — agents and skills call tools unchanged (D-03)
    - Wrong or missing fields for a selected action produce COOLIFY_VALIDATION_ERROR with action-aware recoveryHints listing the required fields for that action (D-04)
    - Every domain tool file exports a hand-maintained <domain>ActionsCatalog constant (e.g. systemActionsCatalog, applicationActionsCatalog) and a <domain>SafetyFooter constant (e.g. systemSafetyFooter, applicationSafetyFooter) co-located in the same file (D-05, D-06, D-07)
    - Every actionsCatalog uses the `Actions:` prefix with `action(param, param?) · action(...)` tokens separated by ` · ` (D-05)
    - Every safetyFooter uses the `Safety: confirm for destructive ops · optional instance · reveal opt-in only` shape per D-08
    - Cursor tool panel renders top-level parameters for every tool — no empty properties:{} UI (DX-02)
  artifacts:
    - src/mcp/tools/shared-read-params.ts (createFlatActionSchema helper added)
    - 17 domain tool files with flat schemas + co-located catalog/footer constants
  key_links:
    - createFlatActionSchema (shared-read-params.ts) → every domain tool *ActionSchema
    - parseWithInstanceRouting (shared-read-params.ts) → action-aware recoveryHints via CoolifyApiError
    - withInstanceRoutingSchema (shared-read-params.ts) → flat z.object extension only (union branch removed)
  prohibitions:
    - No top-level z.discriminatedUnion or z.union at the MCP JSON Schema boundary in any domain tool inputSchema
    - No empty properties:{} on any registered tool inputSchema
    - No auto-generated catalogs from Zod introspection in v3.1 — catalogs are hand-maintained string constants
    - No removal of the { action, ...fields } call shape — agents and skills must remain non-breaking
    - No silent drop of extra parameters — strict validation must reject disallowed fields per action
---

<objective>
Establish the flat-schema DX foundation for v3.1: replace every top-level z.discriminatedUnion/z.union domain tool schema with a single flat z.object + superRefine, add a reusable createFlatActionSchema helper, and co-locate hand-maintained actionsCatalog + safetyFooter constants in each tool file. Server.ts description wiring and prompts registry ship in plan 19-02.

Purpose: Cursor and other MCP hosts render flat top-level JSON Schema properties as visible parameters; the current discriminatedUnion boundary collapses to "No parameters" (Pitfall 10 / DX-02). Flat schemas plus per-action superRefine preserve the existing { action, ...fields } call shape while restoring parameter visibility and strict action-specific validation (D-01–D-08).
Output: Updated src/mcp/tools/shared-read-params.ts with createFlatActionSchema helper and action-aware recoveryHints; 17 domain tool files migrated to flat schemas with co-located actionsCatalog + safetyFooter constants.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-CONTEXT.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-RESEARCH.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-PATTERNS.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-UI-SPEC.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-VALIDATION.md
@src/mcp/server.ts
@src/mcp/tools/shared-read-params.ts
@src/mcp/tools/system.ts
@src/mcp/tools/application.ts
</context>

## Artifacts this phase produces

- `src/mcp/tools/shared-read-params.ts` — `createFlatActionSchema` helper; simplified `withInstanceRoutingSchema` (ZodObject-only, union branch removed); action-aware `parseWithInstanceRouting` recoveryHints via `actionRequiredFields` lookup
- 17 flat domain tool files in `src/mcp/tools/`, each exporting a flat `<domain>ActionSchema` (built via `createFlatActionSchema`), a hand-maintained `<domain>ActionsCatalog` string, and a `<domain>SafetyFooter` string:
  - Read/action-routed: `system.ts`, `resource.ts`, `docs.ts`, `meta.ts`, `diagnose.ts`, `deployment.ts`, `instance.ts`, `manifest.ts`
  - CRUD: `application.ts`, `service.ts`, `database.ts`, `private_key.ts`, `server.ts`, `project.ts`, `environment.ts`, `emergency.ts`
- No new test files, no `src/mcp/server.ts` changes, no prompts registry (those ship in plan 19-02)

<task type="auto" tdd="true">
  <name>Task 1: Flat schema helper + action-aware recoveryHints in shared-read-params.ts</name>
  <files>src/mcp/tools/shared-read-params.ts</files>
  <read_first>
    - src/mcp/tools/shared-read-params.ts (withInstanceRoutingSchema union branch, parseWithInstanceRouting, safeParseWithInstanceRouting)
    - src/utils/errors.ts (CoolifyApiError, RECOVERY_HINTS)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-PATTERNS.md (createFlatActionSchema builder reference)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-CONTEXT.md (D-01, D-03, D-04)
  </read_first>
  <behavior>
    - createFlatActionSchema<TAction, TShape>(actions, shape, actionAllowedFields, actionRequiredFields?) returns a strict z.object({ action: z.enum(actions), ...shape }).superRefine(...)
    - superRefine rejects unknown keys for the selected action with `Parameter '<key>' is not allowed for action '<action>'`
    - superRefine rejects missing required fields for the selected action with `Action '<action>' requires field '<field>'`
    - withInstanceRoutingSchema keeps ZodObject branch; union/discriminatedUnion branch removed (no longer needed post-migration)
    - parseWithInstanceRouting reads args.action from raw input on failure and builds recoveryHints listing the required fields for that action via actionRequiredFields lookup
    - parseWithInstanceRouting still throws CoolifyApiError with code COOLIFY_VALIDATION_ERROR
  </behavior>
  <action>
    Add createFlatActionSchema to src/mcp/tools/shared-read-params.ts per the PATTERNS.md reference. The builder takes an actions tuple, a ZodRawShape of all optional action-specific fields, an actionAllowedFields map, and an optional actionRequiredFields map. It returns z.object({ action: z.enum(actions).describe('The action to run'), ...shape }).strict().superRefine(...). The superRefine first checks required fields for data.action (issue path [field]) then checks every key of Object.keys(data) against the allowed set (issue path [key]). Remove the union/discriminatedUnion branch from extendSchemaWithInstance and withInstanceRoutingSchema — keep only the ZodObject extend path; throw a clear error if a non-ZodObject is passed (so any future union schema fails loudly during migration rather than silently rendering as oneOf). Update parseWithInstanceRouting to accept an optional actionRequiredFields map (or read it from the schema's def via a new createFlatActionSchema-attached _actionRequiredFields symbol) so recoveryHints can list `Action '<action>' requires: <field>, <field>. Optional: <field>, <field>.` Build the hint string from the actionRequiredFields entries for the selected action; fall back to the existing RECOVERY_HINTS.COOLIFY_VALIDATION_ERROR when no map is available. Keep safeParseWithInstanceRouting strip-before-parse behavior intact so strict flat schemas still accept the instance param. Do not modify sharedReadParamsSchema or sharedLogParamsSchema. Do not change resolveRoutingEnv.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/mcp/shared-read-params.test.ts</automated>
  </verify>
  <done>createFlatActionSchema exported; withInstanceRoutingSchema accepts only ZodObject; parseWithInstanceRouting emits action-aware recoveryHints naming required fields for the selected action; shared-read-params.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate read/action-routed domain tools to flat schemas + co-located catalogs</name>
  <files>src/mcp/tools/system.ts, src/mcp/tools/resource.ts, src/mcp/tools/docs.ts, src/mcp/tools/meta.ts, src/mcp/tools/diagnose.ts, src/mcp/tools/deployment.ts, src/mcp/tools/instance.ts, src/mcp/tools/manifest.ts</files>
  <read_first>
    - src/mcp/tools/system.ts (systemActionSchema discriminatedUnion shape, handleSystemAction)
    - src/mcp/tools/resource.ts (resourceActionSchema)
    - src/mcp/tools/docs.ts (docsActionSchema)
    - src/mcp/tools/meta.ts (metaActionSchema)
    - src/mcp/tools/diagnose.ts (diagnoseToolSchema)
    - src/mcp/tools/deployment.ts (deploymentToolSchema)
    - src/mcp/tools/instance.ts (instanceActionSchema)
    - src/mcp/tools/manifest.ts (manifestActionSchema)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-UI-SPEC.md (catalog/footer format, mono identifiers, ≤120 char wrap)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-CONTEXT.md (D-01, D-02, D-05–D-08)
  </read_first>
  <behavior>
    - Each of the 8 tool files exports a flat z.object *ActionSchema built via createFlatActionSchema — no top-level z.discriminatedUnion or z.union
    - Each tool file exports a hand-maintained <domain>ActionsCatalog string (e.g. systemActionsCatalog, resourceActionsCatalog) starting with `Actions:` and using ` · ` separators with `?` on optional params
    - Each tool file exports a <domain>SafetyFooter string (e.g. systemSafetyFooter, resourceSafetyFooter) following `Safety: confirm for destructive ops · optional instance · reveal opt-in only` (or the routing-appropriate subset per UI-SPEC discretion locks for meta/docs)
    - The { action, ...fields } call shape is preserved — handle*Action signatures unchanged
    - Existing unit tests for these 8 tools still pass (handler input parsing unchanged)
  </behavior>
  <action>
    For each of the 8 read/action-routed tool files, replace the exported *ActionSchema (currently z.discriminatedUnion or z.union) with a call to createFlatActionSchema from shared-read-params.ts. The builder's shape argument is the union of all optional action-specific fields across every action in that tool's enum; actionAllowedFields maps each action to the field keys it accepts; actionRequiredFields maps each action to the field keys it requires. Keep the action enum values identical to the current discriminatedUnion options — do not rename or merge actions. Keep every existing per-action superRefine check (XOR constraints, confirm gates, dry_run defaults) inside the createFlatActionSchema superRefine by composing the new action-specific checks with the existing ones; the createFlatActionSchema superRefine handles only strict-key and required-field checks, the file-level refine composes any additional action invariants. Export two new constants per file using domain-prefixed names: <domain>ActionsCatalog (e.g. systemActionsCatalog, resourceActionsCatalog, docsActionsCatalog, metaActionsCatalog, diagnoseActionsCatalog, deploymentActionsCatalog, instanceActionsCatalog, manifestActionsCatalog) — hand-maintained string per D-05 format, one entry per action with 1–3 key params, ` · ` separated, ≤120 chars per logical line, continuing lines start with `· ` — and <domain>SafetyFooter (e.g. systemSafetyFooter, resourceSafetyFooter, ...) per D-08. For meta and docs (non-action-routed per UI-SPEC discretion locks): if the existing schema already lacks oneOf, keep the structure but still add a <domain>ActionsCatalog only if a real action router exists; otherwise skip the catalog and only add a <domain>SafetyFooter when mutation/routing params exist. Do not modify server.ts in this task — the descriptions are wired in plan 19-02. Do not change handler logic, error helpers, or output schemas. Update the 8 corresponding *.test.ts files only if an existing test asserts the old union shape — flip those assertions to the flat shape; do not add new tests here.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/mcp/system.test.ts tests/mcp/resource.test.ts tests/mcp/docs.test.ts tests/mcp/meta.test.ts tests/mcp/diagnose.test.ts tests/mcp/deployment.test.ts tests/mcp/instance.test.ts tests/mcp/manifest.test.ts</automated>
  </verify>
  <done>All 8 read/action-routed tool files export flat z.object schemas built via createFlatActionSchema, export actionsCatalog + safetyFooter constants, preserve { action, ...fields } call shape, and their existing unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate CRUD domain tools to flat schemas + co-located catalogs</name>
  <files>src/mcp/tools/application.ts, src/mcp/tools/service.ts, src/mcp/tools/database.ts, src/mcp/tools/private_key.ts, src/mcp/tools/server.ts, src/mcp/tools/project.ts, src/mcp/tools/environment.ts, src/mcp/tools/emergency.ts</files>
  <read_first>
    - src/mcp/tools/application.ts (applicationActionSchema outer z.union for nested create source_type, envs:* actions, confirm gates)
    - src/mcp/tools/service.ts (serviceActionSchema, create/update/delete confirm gates)
    - src/mcp/tools/database.ts (databaseActionSchema, 8-engine create discriminator, backup:* actions)
    - src/mcp/tools/private_key.ts (privateKeyActionSchema, delete confirm)
    - src/mcp/tools/server.ts (serverActionSchema, validate action)
    - src/mcp/tools/project.ts (projectActionSchema, create initial_environment required)
    - src/mcp/tools/environment.ts (environmentActionSchema, parent project scope)
    - src/mcp/tools/emergency.ts (emergencyToolSchema, confirm gates)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-CONTEXT.md (D-01, D-02, D-04, D-05–D-08)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-UI-SPEC.md (catalog wrap, destructive confirm copy)
  </read_first>
  <behavior>
    - Each of the 9 CRUD tool files exports a flat z.object *ActionSchema built via createFlatActionSchema — no top-level z.discriminatedUnion or z.union
    - Nested discriminators (e.g. database create engine discriminator, application create source_type) are flattened into optional top-level fields with engine/source_type as optional enum fields; mutual exclusivity enforced via superRefine
    - Existing confirm gates (envs:delete, envs:bulk-update, envs:sync apply, delete, emergency actions) remain enforced at schema or handler level — unchanged behavior
    - Each tool file exports <domain>ActionsCatalog + <domain>SafetyFooter constants per D-05/D-08
    - The { action, ...fields } call shape is preserved — handle*Action signatures unchanged
    - Existing unit tests for these 9 tools still pass
  </behavior>
  <action>
    For each of the 9 CRUD tool files, replace the exported *ActionSchema (currently z.discriminatedUnion or z.union, including the nested z.union in applicationActionSchema for create source_type and the 8-engine createDatabaseSchema discriminator in database.ts) with a call to createFlatActionSchema from shared-read-params.ts. Flatten nested discriminators into top-level optional fields: e.g. database create engine becomes an optional top-level `engine` enum field plus the union of all engine-specific optional fields, with superRefine checking engine-specific required sets; application create source_type becomes an optional top-level `source_type` enum plus git/dockerfile-based optional fields, with superRefine enforcing XOR per source_type. Preserve every existing confirm gate (envs:delete, envs:bulk-update, envs:sync apply/prune, service/database/private_key/server/project/environment delete, emergency stop_all/redeploy_project/restart_project) — keep them at the same layer (schema-level or handler-level) they live today; do not move gates between layers. Preserve every existing per-action superRefine (uuid XOR name, env_file XOR env_content, dry_run defaults, instant_deploy defaults, force_domain_override hints). Compose the file-level superRefine with the createFlatActionSchema strict-key/required checks. Export <domain>ActionsCatalog (e.g. applicationActionsCatalog, serviceActionsCatalog, databaseActionsCatalog, privateKeyActionsCatalog, serverActionsCatalog, projectActionsCatalog, environmentActionsCatalog, emergencyActionsCatalog) — hand-maintained per D-05, ≤120 char wrap with `· ` continuation, `?` on optional params, confirm marked on destructive entries — and <domain>SafetyFooter (e.g. applicationSafetyFooter, serviceSafetyFooter, ...) per D-08 constants. Do not modify server.ts — descriptions ship in plan 19-02. Do not change handler logic, error helpers, or output schemas. Update the 9 corresponding *.test.ts files only where existing tests assert the old union/discriminator shape — flip those assertions to the flat shape; do not add new tests here.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/mcp/application.test.ts tests/mcp/service.test.ts tests/mcp/database.test.ts tests/mcp/private_key.test.ts tests/mcp/server.test.ts tests/mcp/project.test.ts tests/mcp/environment.test.ts tests/mcp/emergency.test.ts</automated>
  </verify>
  <done>All 9 CRUD tool files export flat z.object schemas built via createFlatActionSchema with nested discriminators flattened to top-level optional fields, export actionsCatalog + safetyFooter constants, preserve all confirm gates and { action, ...fields } call shape, and their existing unit tests pass.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool inputSchema | Untrusted agent/client crosses here; Zod validates before any Coolify API call |
| Tool schema → handler | Strict flat schema rejects unknown keys per action before handler runs |
| Handler → Coolify API | Only Zod-validated payloads reach outbound HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | createFlatActionSchema superRefine | high | mitigate | Strict z.object + per-action allowed/required field sets reject unknown keys and missing required fields with COOLIFY_VALIDATION_ERROR + action-aware recoveryHints before any handler or API call (D-04) |
| T-19-02 | Information Disclosure | parseWithInstanceRouting recoveryHints | medium | mitigate | recoveryHints list field names and action names only — never credential values, token fragments, or response data; hints are static strings composed from actionRequiredFields keys |
| T-19-03 | Spoofing | instance routing param | medium | accept | instance param already regex-validated (^[a-z][a-z0-9_-]{1,31}$) in shared-read-params.ts; flat schema preserves this — no change to trust boundary |
| T-19-SC | Tampering | npm package installs | low | accept | No new packages installed in this plan — zod and @modelcontextprotocol/server already pinned in package.json; [SUS] flag on @modelcontextprotocol/server accepted per maintainer approval in RESEARCH |
</threat_model>

<verification>
- `pnpm exec vitest run` full suite green
- `rg -n 'z\.discriminatedUnion|z\.union' src/mcp/tools/*.ts` returns no matches in exported *ActionSchema definitions (only in non-schema helper code if any)
- Every src/mcp/tools/<domain>.ts exports <domain>ActionsCatalog and <domain>SafetyFooter string constants
- `pnpm exec tsc --noEmit` clean (or matches pre-existing baseline)
</verification>

<success_criteria>
- 17 domain tool files migrated to flat z.object + superRefine via createFlatActionSchema
- createFlatActionSchema helper shipped in shared-read-params.ts with action-aware recoveryHints
- withInstanceRoutingSchema union branch removed; flat-only path
- All 17 tool files export hand-maintained <domain>ActionsCatalog + <domain>SafetyFooter constants
- All existing unit tests green; no handler signature changes; no server.ts modification
- Cursor tool panel would render top-level parameters for every tool (manual verification deferred to plan 19-02)
</success_criteria>

<output>
Create `.planning/phases/19-dx-schemas-mcp-prompts/19-01-SUMMARY.md` when done
</output>
</contents>
</Write>
