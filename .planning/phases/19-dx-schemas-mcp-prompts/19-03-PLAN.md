---
phase: 19-dx-schemas-mcp-prompts
plan: 03
type: execute
wave: 3
depends_on:
  - 19-02
files_modified:
  - src/mcp/tools/application.ts
  - src/mcp/tools/service.ts
  - src/mcp/tools/database.ts
  - src/mcp/server.test.ts
autonomous: true
gap_closure: true
requirements:
  - DX-01
  - DX-02
user_setup: []

must_haves:
  truths:
    - "Every actionsCatalog token for env-mutation actions uses schema field names: envs:delete(uuid, env_uuid, confirm) and envs:bulk-update(uuid, entries, confirm) — no `key`/`envs` aliases (D-05, CR-01)"
    - "Every actionsCatalog uses concrete `action(param, param?)` tokens — no `envs:*` or `backup:*` wildcard stubs anywhere in src/mcp/tools/*.ts (D-05)"
    - "applicationActionsCatalog lists every action in the schema enum — including create(source_type, server_uuid), update(uuid), delete(uuid, confirm), delete_preview(uuid) (D-05, WR-01)"
    - "databaseActionsCatalog lists every envs:* and backup:* action as a concrete token with 1-3 key params matching schema field names, AND includes `delete_preview(uuid?, name?)` in the leading CRUD tokens (D-05, WR-02)"
    - "E1 tool catalog always ships hand-maintained full Actions catalog (never empty)"
    - "Happy path = purpose sentence → Actions: catalog → Safety: footer"
    - "Catalog wrap respects ≤120 chars with ` · ` continuation"
    - { statement: "Cursor host may show its own in-flight chrome while listing tools; Phase 19 adds no custom loading UI", verification: backstop }
    - { statement: "Host panel scroll/clip for long schema or error payloads is Cursor-owned; verify readable recoveryHints in live Cursor tool UI", verification: backstop }
  artifacts:
    - Updated applicationActionsCatalog constant in src/mcp/tools/application.ts (CR-01 + WR-01)
    - Updated serviceActionsCatalog constant in src/mcp/tools/service.ts (CR-01)
    - Updated databaseActionsCatalog constant in src/mcp/tools/database.ts (WR-02 / D-05)
    - New regression assertions in src/mcp/server.test.ts asserting catalog tokens use schema field names and no wildcards
  key_links:
    - src/mcp/tools/<domain>.ts actionsCatalog constant → src/mcp/server.ts registerTool description (already wired in 19-02; this plan fixes the catalog string content only)
  prohibitions:
    - No schema shape changes — actionAllowedFields / actionRequiredFields maps untouched
    - No handler logic changes — handle*Action signatures untouched
    - No new prompt registry or wiring changes — prompts.ts and server.ts prompt wiring untouched
    - No re-implementation of schemas/prompts/tests that already passed verification (truths 1-4, 6-8, 10-20 in 19-VERIFICATION.md)
    - No `key` alias in envs:delete catalog tokens; no `envs` alias in envs:bulk-update catalog tokens
    - No `envs:*` or `backup:*` wildcard stubs in any actionsCatalog
---

<objective>
Close the two failed verification gaps from 19-VERIFICATION.md by editing the hand-maintained actionsCatalog string constants in application.ts, service.ts, and database.ts so that catalog tokens advertise schema-accepted parameter names, and add a regression test so the gaps cannot reappear.

Purpose: CR-01 (BLOCKER) — agents following the DX-01 catalog text today emit invalid payloads (`key` for envs:delete, `envs` for envs:bulk-update) and receive COOLIFY_VALIDATION_ERROR. D-05 — database.ts uses `envs:*`/`backup:*` wildcard stubs instead of concrete `action(param, param?)` tokens, hiding confirm-gated and required-field guidance. Both gaps are confined to hand-maintained catalog string constants; schemas, handlers, prompts, wiring, and existing tests are all green and untouched. This plan ships string-only catalog edits plus a regression test.

Output: Three edited actionsCatalog constants (application, service, database) aligned with schema field names and D-05 concreteness; new regression assertions in src/mcp/server.test.ts asserting no `key`/`envs` aliases for env-mutation tokens, no `envs:*`/`backup:*` wildcards, and CRUD tokens present in applicationActionsCatalog.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-CONTEXT.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-RESEARCH.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-PATTERNS.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-UI-SPEC.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-VERIFICATION.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-REVIEW.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-01-SUMMARY.md
@.planning/phases/19-dx-schemas-mcp-prompts/19-02-SUMMARY.md
@src/mcp/tools/application.ts
@src/mcp/tools/service.ts
@src/mcp/tools/database.ts
@src/mcp/server.test.ts
</context>

## Assumptions (unresolved specless probes — explicit, not silently dropped)

- **Adjacency / empty / ordering (DX-01):** Catalog param names must match schema field names exactly (env_uuid, entries); wildcards forbidden; catalog token order follows schema enum order; empty catalog forbidden. These are not specified in a phase SPEC.md (EDGE_ABSENT=1) but are locked by D-05 + CR-01 + the schema field names in actionRequiredFields/actionAllowedFields maps.
- **DX-02, PROMPT-01..04 (unclassified):** No specless probe resolution required for this gap plan — those requirements are SATISFIED per 19-VERIFICATION.md and are not touched here.

## Artifacts this phase produces

- `src/mcp/tools/application.ts` — `applicationActionsCatalog` constant edited: `envs:delete(uuid, env_uuid, confirm)` and `envs:bulk-update(uuid, entries, confirm)`; appended `create(source_type, server_uuid) · update(uuid) · delete(uuid, confirm) · delete_preview(uuid)` (CR-01 + WR-01)
- `src/mcp/tools/service.ts` — `serviceActionsCatalog` constant edited: `envs:delete(uuid, env_uuid, confirm)` (CR-01)
- `src/mcp/tools/database.ts` — `databaseActionsCatalog` constant edited: `envs:*` and `backup:*` wildcards replaced with concrete `action(param, param?)` tokens for all 6 envs actions and all 6 backup actions; `delete_preview(uuid?, name?)` appended to the leading CRUD tokens (was missing despite being in the schema enum — D-05 residual closed) (WR-02 / D-05)
- `src/mcp/server.test.ts` — new `describe` block asserting catalog tokens use schema field names and contain no wildcards (regression guard)

<tasks>

<task type="auto">
  <name>Task 1: Align actionsCatalog constants with schema field names (CR-01 + WR-01 + WR-02)</name>
  <files>src/mcp/tools/application.ts, src/mcp/tools/service.ts, src/mcp/tools/database.ts</files>
  <read_first>
    - src/mcp/tools/application.ts (lines 301-302: applicationActionsCatalog constant; lines 558-630: actionAllowedFields + actionRequiredFields maps for envs:* actions; lines 307-325: action enum)
    - src/mcp/tools/service.ts (lines 271-272: serviceActionsCatalog constant; lines ~430-460: actionAllowedFields/actionRequiredFields for envs:delete)
    - src/mcp/tools/database.ts (lines 224-225: databaseActionsCatalog constant; lines 449-561: actionAllowedFields/actionRequiredFields for envs:* and backup:* actions; lines 230-251: action enum)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-VERIFICATION.md (Gaps section: exact missing items)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-REVIEW.md (CR-01 fix block; WR-01, WR-02 fix blocks)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-UI-SPEC.md (Catalog format: ` · ` separator, ≤120 char wrap with `· ` continuation, `?` on optional params, 1-3 key params per action)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-CONTEXT.md (D-05: concrete action(param, param?) tokens; D-06: hand-maintained)
  </read_first>
  <action>
    Edit ONLY the `applicationActionsCatalog`, `serviceActionsCatalog`, and `databaseActionsCatalog` string constants. Do NOT touch any schema shape, actionAllowedFields map, actionRequiredFields map, superRefine, handler, or test in this task.

    application.ts — replace the `applicationActionsCatalog` string value with a single string that:
    - Keeps the existing prefix `Actions:` and ` · ` separators
    - Fixes the two CR-01 tokens: `envs:delete(uuid, env_uuid, confirm)` and `envs:bulk-update(uuid, entries, confirm)` (was `envs:delete(uuid, key, confirm)` and `envs:bulk-update(uuid, envs, confirm)`)
    - Appends the four missing CRUD tokens after `logs(uuid, lines?)` and before `envs:list`: `create(source_type, server_uuid) · update(uuid) · delete(uuid, confirm) · delete_preview(uuid)`
    - Keeps every other existing token unchanged (get, start, stop, restart, deploy, logs, envs:list, envs:get, envs:create, envs:update, envs:sync)
    - Wraps at ≤120 chars per logical line using `· ` continuation per UI-SPEC (the constant is one JS string with embedded ` · ` separators; break the source string across concatenated lines if it exceeds 120 chars visually, preserving the exact ` · ` separators)

    service.ts — in the `serviceActionsCatalog` string, replace the single token `envs:delete(uuid, key, confirm)` with `envs:delete(uuid, env_uuid, confirm)`. Leave every other token unchanged.

    database.ts — replace the `databaseActionsCatalog` string value. The current leading tokens are `get(uuid?, name?) · start(uuid?) · stop(uuid?) · restart(uuid?) · create(engine, server_uuid) · update(uuid?) · delete(uuid?, confirm)` and are MISSING `delete_preview` even though the schema enum (lines 230-252) includes `delete_preview` (D-05 residual). Append `delete_preview(uuid?, name?)` immediately after `delete(uuid?, confirm)` in the leading tokens (matching the schema's identifier fields `uuid` + `name` per actionRequiredFields line 448). Then remove the trailing `envs:* · backup:*` wildcard stubs and append concrete tokens for every envs and backup action in the schema enum, using 1-3 key params each, matching schema field names from the actionAllowedFields/actionRequiredFields maps:
    - `envs:list(uuid?)`
    - `envs:get(uuid?, env_uuid?, key?)`
    - `envs:create(uuid?, key, value)`
    - `envs:update(uuid?, env_uuid?, key?, value)`
    - `envs:delete(uuid?, env_uuid, confirm)` (required field env_uuid per actionRequiredFields)
    - `envs:bulk-update(uuid?, entries, confirm)` (required field entries per actionRequiredFields)
    - `backup:create(uuid?, frequency)` (required field frequency)
    - `backup:list(uuid?)`
    - `backup:history(uuid?, scheduled_backup_uuid)` (required field scheduled_backup_uuid)
    - `backup:update(uuid?, scheduled_backup_uuid)` (required field scheduled_backup_uuid)
    - `backup:delete(uuid?, scheduled_backup_uuid, confirm)` (required field scheduled_backup_uuid)
    - `backup:now(uuid?, scheduled_backup_uuid)` (required field scheduled_backup_uuid)
    Use ` · ` separators and `· ` continuation for ≤120 char wrap. The final catalog must contain every action in the schema enum (D-05) — no wildcards, no omissions.

    Rationale per CR-01 / WR-01 / WR-02: agents following the DX-01 catalog text today emit `key` for envs:delete and `envs` for envs:bulk-update and get COOLIFY_VALIDATION_ERROR because the flat schema's `.strict()` superRefine rejects unknown keys and actionRequiredFields requires `env_uuid` / `entries`. The database wildcards hide confirm-gated and required-field guidance from the description surface. These are string-only constant edits; no schema or handler changes are required.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/mcp/tools/application.test.ts src/mcp/tools/service.test.ts src/mcp/tools/database.test.ts src/mcp/server.test.ts &amp;&amp; rg -n 'envs:delete\(uuid, env_uuid, confirm\)' src/mcp/tools/application.ts src/mcp/tools/service.ts src/mcp/tools/database.ts &amp;&amp; rg -n 'envs:bulk-update\(uuid, entries, confirm\)' src/mcp/tools/application.ts src/mcp/tools/database.ts &amp;&amp; rg -n 'create\(source_type, server_uuid\)' src/mcp/tools/application.ts &amp;&amp; rg -n 'delete_preview\(uuid\?, name\?\)' src/mcp/tools/database.ts &amp;&amp; rg -n 'backup:delete\(uuid\?, scheduled_backup_uuid, confirm\)' src/mcp/tools/database.ts &amp;&amp; bash -c "test -z \"$(rg -n 'envs:delete\(uuid, key' src/mcp/tools/application.ts src/mcp/tools/service.ts)\" || (echo 'FAIL: env_uuid alias leak' &amp;&amp; exit 1)" &amp;&amp; bash -c "test -z \"$(rg -n 'envs:bulk-update\(uuid, envs' src/mcp/tools/application.ts)\" || (echo 'FAIL: entries alias leak' &amp;&amp; exit 1)" &amp;&amp; bash -c "test -z \"$(rg -n 'envs:\*|backup:\*' src/mcp/tools/database.ts)\" || (echo 'FAIL: wildcard stub leak' &amp;&amp; exit 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'envs:delete\(uuid, env_uuid, confirm\)' src/mcp/tools/application.ts src/mcp/tools/service.ts src/mcp/tools/database.ts` returns one match in each of application.ts and service.ts (database.ts also matches via the new token)
    - `rg -n 'envs:bulk-update\(uuid, entries, confirm\)' src/mcp/tools/application.ts src/mcp/tools/database.ts` returns one match in each file
    - `rg -n 'envs:delete\(uuid, key' src/mcp/tools/application.ts src/mcp/tools/service.ts` returns zero matches
    - `rg -n 'envs:bulk-update\(uuid, envs' src/mcp/tools/application.ts` returns zero matches
    - `rg -n 'envs:\*|backup:\*' src/mcp/tools/database.ts` returns zero matches
    - `rg -n 'create\(source_type, server_uuid\)' src/mcp/tools/application.ts` returns one match in the catalog
    - `rg -n 'update\(uuid\) · delete\(uuid, confirm\) · delete_preview\(uuid\)' src/mcp/tools/application.ts` returns one match
    - `rg -n 'backup:delete\(uuid?, scheduled_backup_uuid, confirm\)' src/mcp/tools/database.ts` returns one match
    - `pnpm exec vitest run src/mcp/tools/application.test.ts src/mcp/tools/service.test.ts src/mcp/tools/database.test.ts src/mcp/server.test.ts` is green (no handler/schema changes; existing tests unaffected)
    - `pnpm exec tsc --noEmit` clean (or matches pre-existing baseline)
  </acceptance_criteria>
  <done>The three actionsCatalog constants advertise only schema-accepted parameter names; applicationActionsCatalog includes all CRUD lifecycle tokens; databaseActionsCatalog has no wildcard stubs; existing application/service/database/server tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression test — catalog tokens use schema field names, no wildcards</name>
  <files>src/mcp/server.test.ts</files>
  <read_first>
    - src/mcp/server.test.ts (existing `every registered tool description contains Actions: and Safety: prefixes` test at lines 223-262; test harness pattern: McpServer + registerCoolifyTools + _registeredTools cast)
    - src/mcp/tools/application.ts (Task 1 edited applicationActionsCatalog — final string)
    - src/mcp/tools/service.ts (Task 1 edited serviceActionsCatalog — final string)
    - src/mcp/tools/database.ts (Task 1 edited databaseActionsCatalog — final string)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-VERIFICATION.md (Gap 1 + Gap 2 missing items — these are the assertions to lock in)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-CONTEXT.md (D-05: concrete tokens; D-06: hand-maintained)
  </read_first>
  <behavior>
    - A new `describe` block in src/mcp/server.test.ts asserts that the three edited catalog constants (imported directly from their tool files) satisfy the CR-01 / WR-01 / WR-02 invariants
    - Assertion A: applicationActionsCatalog contains the exact substring `envs:delete(uuid, env_uuid, confirm)`
    - Assertion B: applicationActionsCatalog contains the exact substring `envs:bulk-update(uuid, entries, confirm)`
    - Assertion C: applicationActionsCatalog contains `create(source_type, server_uuid)`, `update(uuid)`, `delete(uuid, confirm)`, `delete_preview(uuid)`
    - Assertion D: serviceActionsCatalog contains `envs:delete(uuid, env_uuid, confirm)`
    - Assertion E: databaseActionsCatalog contains `envs:delete(uuid?, env_uuid, confirm)` and `envs:bulk-update(uuid?, entries, confirm)`
    - Assertion F (negative): none of the three catalogs contain `envs:delete(uuid, key` — locks CR-01 fix
    - Assertion G (negative): applicationActionsCatalog does not contain `envs:bulk-update(uuid, envs` — locks CR-01 fix
    - Assertion H (negative): databaseActionsCatalog does not contain `envs:*` or `backup:*` — locks WR-02 fix
    - Existing `every registered tool description contains Actions: and Safety: prefixes` test still passes
  </behavior>
  <action>
    Add a new `describe('actionsCatalog schema-field-name regression (Phase 19 gap closure)', () => { ... })` block to src/mcp/server.test.ts. The existing test file already imports action *schemas* from `./tools/application.js`, `./tools/service.js`, and `./tools/database.js` (see lines 10-12 of src/mcp/server.test.ts) — extend those existing `./tools/*` imports to also import `applicationActionsCatalog`, `serviceActionsCatalog`, and `databaseActionsCatalog` respectively. Do NOT introduce `../tools/...` paths; the test file lives at `src/mcp/server.test.ts` so the correct relative import is `./tools/<domain>.js` (matches the existing convention at lines 7-15). Use Vitest `it` + `expect(...).toContain(...)` for positive substring assertions and `expect(...).not.toContain(...)` for negative assertions. Implement the eight assertions A-H above as separate `it` cases with descriptive names so a regression points at the exact invariant that broke. Do NOT use regex over-reach — use exact-string `toContain` / `not.toContain` checks. Do NOT modify the existing `every registered tool description contains Actions: and Safety: prefixes` test. Do NOT assert handler behavior or schema shapes in this task — only catalog string content. Keep the test file's existing imports and structure intact.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/mcp/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/mcp/server.test.ts` green including the new `actionsCatalog schema-field-name regression` describe block
    - The new describe block has at least 8 `it` cases (A-H) covering the CR-01 / WR-01 / WR-02 invariants
    - `rg -n 'envs:delete\(uuid, env_uuid, confirm\)' src/mcp/server.test.ts` returns at least one match (assertion A or D or E)
    - `rg -n 'not\.toContain.*envs:\*' src/mcp/server.test.ts` returns at least one match (assertion H)
    - Existing `every registered tool description contains Actions: and Safety: prefixes` test still passes (no regression)
    - `pnpm exec tsc --noEmit` clean
  </acceptance_criteria>
  <done>src/mcp/server.test.ts ships a regression guard that fails if any future edit re-introduces `key`/`envs` aliases in env-mutation catalog tokens, re-introduces `envs:*`/`backup:*` wildcards, or drops the application CRUD tokens; full server.test.ts suite green.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool description surface | Catalog strings are static metadata served to the host IDE; agents read advertised param names and emit payloads matching them |
| Catalog string → agent payload → flat schema | Catalog param names must match schema field names or agents receive COOLIFY_VALIDATION_ERROR |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-08 | Tampering | hand-maintained actionsCatalog constants | high | mitigate | Catalog tokens edited to use schema-accepted field names (env_uuid, entries); regression test in Task 2 fails on any future re-introduction of `key`/`envs` aliases or `envs:*`/`backup:*` wildcards (CR-01, D-05) |
| T-19-09 | Spoofing | agent payload composition | medium | mitigate | By aligning catalog param names with actionRequiredFields/actionAllowedFields, agents following the DX-01 description now emit payloads the flat schema accepts — no false rejection, no silent field drop |
| T-19-SC | Tampering | npm package installs | low | accept | No new packages installed in this plan — string-only catalog edits + test additions; reuses zod and @modelcontextprotocol/server already pinned |
</threat_model>

<verification>
- `pnpm exec vitest run` full suite green (existing tests + new regression assertions)
- `rg -n 'envs:delete\(uuid, key|envs:bulk-update\(uuid, envs|envs:\*|backup:\*' src/mcp/tools/*.ts` returns zero matches
- `rg -n 'envs:delete\(uuid, env_uuid, confirm\)' src/mcp/tools/application.ts src/mcp/tools/service.ts` returns one match in each file
- `rg -n 'envs:bulk-update\(uuid, entries, confirm\)' src/mcp/tools/application.ts src/mcp/tools/database.ts` returns one match in each file
- `rg -n 'create\(source_type, server_uuid\)' src/mcp/tools/application.ts` returns one match in the catalog
- `pnpm exec tsc --noEmit` clean (or matches pre-existing baseline)
- Re-running 19-VERIFICATION.md truth #5 and truth #9 now PASS (catalogs use schema field names; Cursor tool panel shows correct key parameters)
</verification>

<success_criteria>
- CR-01 BLOCKER closed: application.ts and service.ts envs:delete catalog token uses `env_uuid`; application.ts envs:bulk-update uses `entries`; agents following the DX-01 description emit schema-accepted payloads
- D-05 / WR-02 closed: database.ts actionsCatalog has no `envs:*`/`backup:*` wildcards — every envs and backup action ships as a concrete `action(param, param?)` token with 1-3 key params matching schema field names
- WR-01 closed: application.ts actionsCatalog includes `create(source_type, server_uuid) · update(uuid) · delete(uuid, confirm) · delete_preview(uuid)`
- Regression guard in src/mcp/server.test.ts prevents re-introduction of the gaps
- No schema, handler, prompt, or wiring changes — string-only catalog edits + test additions
- DX-01 unblocked; DX-02 reaffirmed (catalogs now match the flat schemas agents see)
</success_criteria>

<output>
Create `.planning/phases/19-dx-schemas-mcp-prompts/19-03-SUMMARY.md` when done
</output>

