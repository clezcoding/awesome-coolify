---
phase: 31-agent-playbooks
plan: 02
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - src/mcp/prompts.ts
  - tests/mcp/prompts.test.ts
autonomous: false
requirements:
  - PLAY-01
  - PLAY-02
user_setup: []

must_haves:
  truths:
    - "MCP registers six prompts: deploy, diagnose, new-project, incident, rollback, maintenance-window (PLAY-01, D-08)."
    - "incident prompt upgraded to cite diagnose.analyze and deployment.preflight; cross-links rollback on crash_loop/failed deploy (D-08)."
    - "rollback prompt composes deployment.preflight → rollback confirm:false preview → STOP human approval → confirm:true; documents COOLIFY_ROLLBACK_UNAVAILABLE (D-09)."
    - "maintenance-window guides stop → work → start/restart via application|service|database lifecycle; confirm on destructive ops; single-instance scope (D-10, D-16)."
    - "Playbook bodies cite existing atomic tool/action names only — no duplicate API clients (PLAY-02, D-11)."
    - "analyze/recommend/playbooks never auto-set confirm true for destructive mutations (D-20)."
    - "Wave 0 prompt it.fails flipped to passing it()."
  artifacts:
    - path: src/mcp/prompts.ts
      provides: "upgraded incident + new rollback + maintenance-window prompts"
      contains: "maintenance-window"
    - path: tests/mcp/prompts.test.ts
      provides: "GREEN six-prompt registration and composition assertions"
      contains: "rollback"
  key_links:
    - from: incident prompt
      to: diagnose.analyze
      via: "step after UUID resolve (D-08)"
    - from: rollback prompt
      to: deployment.preflight + deployment.rollback
      via: "confirm gate steps (D-09)"
    - from: maintenance-window prompt
      to: application|service|database lifecycle actions
      via: "stop/start/restart composition (D-10)"
  prohibitions:
    - statement: "Do not add a mutating playbook-runner MCP tool (D-07)"
      status: enforced
      verification: "rg -n \"playbook-runner|playbook_runner|name: 'playbook'\" src/mcp/server.ts src/mcp/prompts.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
    - statement: "Do not duplicate a second incident prompt under a new name (D-08)"
      status: enforced
      verification: "rg -n \"registerPrompt\\(\\s*'incident\" src/mcp/prompts.ts | grep -v '^#' | wc -l | awk '{exit ($1==1)?0:1}'"
---

<objective>
Ship parameterized MCP playbook prompts: upgrade incident, add rollback + maintenance-window composing existing atomic tools only.

Purpose: PLAY-01/02 ops orchestration without a playbook-runner mutation tool (D-07).

Output: `prompts.ts` updates, green prompts.test.ts six-prompt suite.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-agent-playbooks/31-CONTEXT.md
@.planning/phases/31-agent-playbooks/31-RESEARCH.md
@.planning/phases/31-agent-playbooks/31-PATTERNS.md
@.planning/phases/31-agent-playbooks/31-01-SUMMARY.md
@src/mcp/prompts.ts
@tests/mcp/prompts.test.ts
@src/utils/deploy-preflight.ts
@src/utils/errors.ts
</context>

## Artifacts this phase produces

- `src/mcp/prompts.ts` — upgraded `incident`; new `rollback`; new `maintenance-window`.
- `tests/mcp/prompts.test.ts` — six-prompt registration + composition GREEN.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm D-09 rollback confirm gate in playbook text</name>
  <files>.planning/phases/31-agent-playbooks/31-CONTEXT.md</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-CONTEXT.md (D-09, D-20)
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 3 rollback, Pitfall 4)
    - .planning/phases/30-deploy-guard/30-RESEARCH.md (rollback confirm semantics) if needed
  </read_first>
  <decision>Confirm locked D-09 one-way contract: rollback playbook text requires explicit human approval before deployment.rollback with confirm true (SAF-01).</decision>
  <context>D-09 publishes confirm language in the rollback prompt. Undoing later would teach agents to auto-confirm destructive rollback.</context>
  <options>
    <option id="approve-confirm-in-prompt">
      <name>Approve confirm-after-human-approval in rollback prompt (D-09 locked)</name>
      <pros>Matches SAF-01, Phase 30 tool gate, D-09, D-20, T-31-02.</pros>
      <cons>Agents need an extra human turn before mutation.</cons>
    </option>
    <option id="stop-and-replan">
      <name>Stop and replan</name>
      <pros>Only if D-09 was recorded incorrectly.</pros>
      <cons>Blocks PLAY-01 rollback flow.</cons>
    </option>
  </options>
  <action>Record checkpoint selection before editing prompts.ts. Continue only with approve-confirm-in-prompt; do not write rollback prompt text that jumps straight to confirm true without a STOP/human-approval step.</action>
  <verify>
    <automated>rg -n "D-09" .planning/phases/31-agent-playbooks/31-CONTEXT.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Checkpoint resolved as approve-confirm-in-prompt before Task 2
  </acceptance_criteria>
  <resume-signal>Select: approve-confirm-in-prompt (expected) or stop-and-replan</resume-signal>
  <done>D-09 gate confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade incident + add rollback and maintenance-window prompts</name>
  <reversibility rating="one-way">D-09 confirm gate in rollback prompt text is a published safety contract.</reversibility>
  <files>src/mcp/prompts.ts, tests/mcp/prompts.test.ts</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-CONTEXT.md (D-07..D-11, D-16, D-20)
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 3 full prompt outlines)
    - .planning/phases/31-agent-playbooks/31-PATTERNS.md (prompts.ts registration + composition)
    - src/mcp/prompts.ts (incident block, optionalInstanceSuffix, registerCoolifyPrompts)
    - tests/mcp/prompts.test.ts (Wave 0 RED cases)
  </read_first>
  <behavior>
    - Six prompts registered (PLAY-01).
    - incident includes diagnose.analyze step and deployment.preflight when rollback considered; cross-link rollback prompt (D-08).
    - rollback args: instance?, uuid?, name?, fqdn?; steps per RESEARCH Pattern 3 including COOLIFY_ROLLBACK_UNAVAILABLE (D-09).
    - maintenance-window requires resource_type application|service|database; stop → work → start/restart; confirm language; optional instance (D-10, D-16).
    - Bodies cite diagnose/deployment/application/service/database/emergency/recipe tool forms only (PLAY-02, D-11).
  </behavior>
  <action>
    Per D-07/D-08: upgrade existing incident prompt body — insert diagnose.analyze after UUID resolve; add deployment.preflight awareness; cross-link rollback; keep existing logs/follow/restart/emergency flow. Add registerPrompt rollback and maintenance-window per RESEARCH Pattern 3 with optionalInstanceSuffix. Required resource_type arg for maintenance-window (no silent default). Keep deploy/diagnose/new-project; optional one-line cross-link only if needed. Flip Wave 0 prompt it.fails to it() and update registration expectation to six names. Per D-11/PLAY-02: prompts are text guides only — no new HTTP clients inside prompts.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/mcp/prompts.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Registered prompt names equal deploy, diagnose, incident, maintenance-window, new-project, rollback (sorted)
    - rollback assistant content contains deployment.preflight and confirm: true and COOLIFY_ROLLBACK_UNAVAILABLE
    - rollback content includes a human-approval STOP step before confirm true
    - incident content contains diagnose.analyze
    - maintenance-window content contains stop and start or restart
    - prompts.ts has no ofetch( or axios or fetch( client calls in prompt handlers
  </acceptance_criteria>
  <done>PLAY-01 and PLAY-02 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: PLAY-02 composition hardening asserts</name>
  <files>tests/mcp/prompts.test.ts</files>
  <read_first>
    - tests/mcp/prompts.test.ts
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 3 Anti-Patterns)
  </read_first>
  <action>
    Add or tighten tests that incident/rollback/maintenance-window cite atomic tool call shapes (diagnose({, deployment({, application({ or service({ or database({) and that rollback never instructs agents to skip the tool-level confirm gate. Optionally add one-line cross-link from diagnose prompt to analyze if still missing. No production API changes.
  </action>
  <verify>
    <automated>npx vitest run tests/mcp/prompts.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Composition tests assert diagnose({ and deployment({ appear in relevant playbooks
    - No test expects a playbook-runner tool
  </acceptance_criteria>
  <done>Playbook composition assertions hardened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt text → agent | Guidance only; tools enforce real confirm gates |
| Agent → deployment.rollback | Destructive; SAF-01 confirm on tool handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-02 | Elevation of Privilege | rollback prompt | high | mitigate | Explicit STOP + human approval before confirm true; tool still enforces COOLIFY_CONFIRM_REQUIRED |
| T-31-07 | Spoofing | prompt social engineering | medium | mitigate | PLAY-02 composition only; no embedded credentials; D-20 no confirm bypass |
| T-31-08 | Tampering | maintenance-window lifecycle | medium | mitigate | Prompt emphasizes confirm on destructive ops; mutations stay on existing gated actions |
| T-31-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npx vitest run tests/mcp/prompts.test.ts -x
</verification>

<success_criteria>
Six prompts registered; incident/rollback/maintenance-window compose atomic tools; D-09 confirm language present; PLAY-01/02 done.
</success_criteria>

<output>
Create `.planning/phases/31-agent-playbooks/31-02-SUMMARY.md` when done
</output>
