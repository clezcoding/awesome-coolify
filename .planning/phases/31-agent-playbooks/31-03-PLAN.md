---
phase: 31-agent-playbooks
plan: 03
type: execute
wave: 3
depends_on:
  - "31-02"
files_modified:
  - src/mcp/tools/recipe.ts
  - src/mcp/tools/recipe.test.ts
autonomous: false
requirements:
  - SREC-01
  - SREC-02
user_setup: []

must_haves:
  truths:
    - "Agent calls recipe.recommend with a stack description and receives plan_steps naming create-one-click / create-app-db / create-git-app plus suggested env keys (SREC-01, D-12, D-13)."
    - "Recommendations use live fetchServiceTemplates catalog_source live — not a hardcoded YAML SoT in-repo (SREC-02, D-13)."
    - "recommend is advisory by default: advisory true; does not mutate Coolify state (D-14)."
    - "Catalog IDs emitted only when Object.hasOwn(templates, id); unknown stacks get structured error + list-types hint (D-14, D-15)."
    - "Fuzzy matches only as ranked suggestions with confidence; prefer exact/high-confidence (D-15)."
    - "Wave 0 recipe recommend it.fails flipped to passing it()."
  artifacts:
    - path: src/mcp/tools/recipe.ts
      provides: "recommend action + handleRecipeRecommend"
      contains: "handleRecipeRecommend"
    - path: src/mcp/tools/recipe.test.ts
      provides: "GREEN recipe recommend tests from Wave 0"
      contains: "recipe recommend"
  key_links:
    - from: handleRecipeRecommend
      to: fetchServiceTemplates
      via: "same live catalog path as create-one-click / service.list-types"
    - from: plan_steps[].recipe_action
      to: create-one-click | create-app-db | create-git-app
      via: "advisory follow-up only (D-14)"
  prohibitions:
    - statement: "Do not create a top-level smart-recipe MCP tool (D-12)"
      status: enforced
      verification: "rg -n \"name: 'smart-recipe'|name: 'smart_recipe'\" src/mcp/server.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
    - statement: "Do not use an in-repo hardcoded service-templates.json as catalog source of truth (SREC-02)"
      status: enforced
      verification: "rg -n \"from '.*service-templates.json'|readFileSync\\(.*service-templates\" src/mcp/tools/recipe.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
---

<objective>
Ship `recipe.recommend`: advisory one-click + env + deploy plan from live `fetchServiceTemplates` catalog.

Purpose: SREC-01/02 smart recipes without hardcoded YAML SoT and without mutating creates (D-14).

Output: `recommend` on recipe tool; Wave 0 recommend tests GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-agent-playbooks/31-CONTEXT.md
@.planning/phases/31-agent-playbooks/31-RESEARCH.md
@.planning/phases/31-agent-playbooks/31-PATTERNS.md
@.planning/phases/31-agent-playbooks/31-00-SUMMARY.md
@.planning/phases/31-agent-playbooks/31-02-SUMMARY.md
@src/mcp/tools/recipe.ts
@src/utils/service-templates.ts
@src/mcp/tools/service.ts
</context>

## Artifacts this phase produces

- `src/mcp/tools/recipe.ts` — `recommend` catalog/schema/handler.
- GREEN `recipe recommend` tests from Wave 0.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm D-14 advisory-only recommend default</name>
  <files>.planning/phases/31-agent-playbooks/31-CONTEXT.md</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-CONTEXT.md (D-14, D-20)
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 4, Security Domain)
  </read_first>
  <decision>Confirm locked D-14 one-way contract: recipe.recommend returns an advisory plan only and does not create services/apps/dbs itself.</decision>
  <context>D-14 publishes recommend as advisory. Undoing later would let a single recommend call mutate Coolify infrastructure without the existing create confirms.</context>
  <options>
    <option id="approve-advisory-recommend">
      <name>Approve advisory-only recommend (D-14 locked)</name>
      <pros>Matches D-14, D-20, existing create-* confirm/validation gates, T-31-03.</pros>
      <cons>Caller must run create actions separately after reading the plan.</cons>
    </option>
    <option id="stop-and-replan">
      <name>Stop and replan</name>
      <pros>Only if D-14 was recorded incorrectly.</pros>
      <cons>Blocks SREC-01.</cons>
    </option>
  </options>
  <action>Record checkpoint selection before wiring handleRecipeRecommend. Continue only with approve-advisory-recommend; do not call createService/createApplication/createDatabase from recommend.</action>
  <verify>
    <automated>rg -n "D-14" .planning/phases/31-agent-playbooks/31-CONTEXT.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Checkpoint resolved as approve-advisory-recommend before Task 2
  </acceptance_criteria>
  <resume-signal>Select: approve-advisory-recommend (expected) or stop-and-replan</resume-signal>
  <done>D-14 gate confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement recipe.recommend from live catalog</name>
  <reversibility rating="one-way">D-14 advisory recommend default is a published safety contract.</reversibility>
  <files>src/mcp/tools/recipe.ts, src/mcp/tools/recipe.test.ts</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-CONTEXT.md (D-12..D-15, D-16..D-17)
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 4 algorithm + response envelope)
    - .planning/phases/31-agent-playbooks/31-PATTERNS.md (recipe.ts + create-one-click analog)
    - src/mcp/tools/recipe.ts (createFlatActionSchema, handleCreateOneClick, fetchServiceTemplates usage)
    - src/utils/service-templates.ts (fetchServiceTemplates)
  </read_first>
  <behavior>
    - Schema accepts required stack/description string; optional server_uuid, project_uuid, environment_name, format, max_chars, instance (D-13, D-16).
    - fetchServiceTemplates(env) used; catalog_source live in response (SREC-02).
    - Next.js + Postgres style stacks produce plan_steps with create-git-app and create-app-db as appropriate (SREC-01).
    - One-click matches only emit catalog_id present in live templates (D-15).
    - Unmappable stack → COOLIFY_VALIDATION_ERROR + service.list-types recovery hint (D-14).
    - advisory true; createService mock never called (D-14).
  </behavior>
  <action>
    Per D-12: extend recipe action catalog + createFlatActionSchema with recommend. Implement handleRecipeRecommend per RESEARCH Pattern 4: tokenize stack; git-app keyword map; DB engine map; score one-click catalog keys/names with confidence exact|high|suggested; never invent IDs. Return buildReadResponse with matches, plan_steps (order, recipe_action, summary, suggested_params, env_keys, follow_up_hint), advisory true, catalog_source live. Wire switch case. Flip Wave 0 recommend it.fails to it(). Discretion: field name stack vs description — pick one required string and document in schema describe. Do not hardcode offline YAML as SoT.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/recipe.test.ts -t recommend -x</automated>
  </verify>
  <acceptance_criteria>
    - recipe action schema includes recommend
    - Happy-path response includes advisory true and catalog_source live and plan_steps array
    - Unknown-stack path returns COOLIFY_VALIDATION_ERROR
    - createService is not called in recommend tests
  </acceptance_criteria>
  <done>SREC-01 and SREC-02 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Confidence ranking + env_keys plan polish</name>
  <reversibility rating="costly">D-15 high-confidence-first matching becomes agent expectation for recommend quality.</reversibility>
  <files>src/mcp/tools/recipe.ts, src/mcp/tools/recipe.test.ts</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pitfall 3 fuzzy invent, Pattern 4)
  </read_first>
  <behavior>
    - DB phrases prefer create-app-db over wrong one-click IDs.
    - suggested confidence never alone auto-executes — plan_steps remain advisory.
    - plan_steps include env_keys such as DATABASE_URL when DB step present.
    - Optional server_uuid/project_uuid prefills appear in suggested_params when provided.
  </behavior>
  <action>
    Tighten ranking so postgres/mysql/mongo/redis DB tokens route to create-app-db; one-click only for explicit service-template names. Ensure suggested matches still validate Object.hasOwn. Add tests for confidence ordering and env_keys. Keep recommend read-only.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/recipe.test.ts -t recommend -x</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts postgres-style stack yields create-app-db plan step
    - Test asserts catalog_id absent from mock templates is not emitted
  </acceptance_criteria>
  <done>Recommend ranking and plan payload polished.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → recipe.recommend | Untrusted stack phrase; advisory plan only |
| CDN/GitHub catalog → matcher | Third-party template JSON; validate keys before emit |
| Recommend response → agent | Plan steps; creates remain on confirm-gated actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-03 | Tampering | catalog matching | high | mitigate | Object.hasOwn guard; no invent IDs; live fetchServiceTemplates only |
| T-31-09 | Elevation of Privilege | recommend auto-create | high | mitigate | D-14 advisory; no create* calls; checkpoint before ship |
| T-31-10 | Spoofing | CDN catalog poison | medium | mitigate | Official coollabsio paths inside fetchServiceTemplates; key validation |
| T-31-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/mcp/tools/recipe.test.ts -t recommend -x
</verification>

<success_criteria>
recipe.recommend green; live catalog; advisory; SREC-01/02 done.
</success_criteria>

<output>
Create `.planning/phases/31-agent-playbooks/31-03-SUMMARY.md` when done
</output>
