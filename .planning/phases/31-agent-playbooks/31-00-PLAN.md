---
phase: 31-agent-playbooks
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/utils/log-patterns.ts
  - src/utils/log-patterns.test.ts
  - src/mcp/tools/diagnose.test.ts
  - src/mcp/tools/recipe.test.ts
  - tests/mcp/prompts.test.ts
  - src/mcp/tools/system.test.ts
autonomous: true
requirements:
  - BRAIN-01
  - BRAIN-02
  - PLAY-01
  - PLAY-02
  - SREC-01
  - SREC-02
user_setup: []

must_haves:
  truths:
    - "Wave 0 locks RED scaffolds for matchLogPatterns four named patterns before implementation (BRAIN-01)."
    - "Wave 0 locks diagnose.analyze RED scaffolds for severity, matched_patterns, FollowUpHint next actions, empty-log advisory, soft partial (BRAIN-01/02, D-06)."
    - "Wave 0 locks recipe.recommend RED scaffolds for plan_steps from live catalog and unknown-stack error (SREC-01/02, D-14)."
    - "Wave 0 locks prompts 4→6 registration RED and composition cite expectations (PLAY-01/02, D-09)."
    - "Wave 0 locks capability key count 15→17 RED for diagnose_analyze and recipe_recommend (D-19)."
    - "All new RED cases use Vitest it.fails so pre-commit remains green."
  artifacts:
    - path: src/utils/log-patterns.test.ts
      provides: "it.fails fixtures for oom, http_5xx_spike, crash_loop, connection_refused"
      contains: "matchLogPatterns"
    - path: src/mcp/tools/diagnose.test.ts
      provides: "it.fails describe diagnose analyze"
      contains: "diagnose analyze"
    - path: src/mcp/tools/recipe.test.ts
      provides: "it.fails describe recipe recommend"
      contains: "recipe recommend"
    - path: tests/mcp/prompts.test.ts
      provides: "it.fails for six prompts including rollback and maintenance-window"
      contains: "maintenance-window"
    - path: src/mcp/tools/system.test.ts
      provides: "it.fails for seventeen capability keys"
      contains: "diagnose_analyze"
    - path: src/utils/log-patterns.ts
      provides: "minimal shell export matchLogPatterns for Wave 0 import wiring only"
      contains: "matchLogPatterns"
  key_links:
    - from: src/utils/log-patterns.test.ts
      to: src/utils/log-patterns.ts
      via: "matchLogPatterns import"
    - from: src/mcp/tools/diagnose.test.ts
      to: src/mcp/tools/diagnose.ts
      via: "handleDiagnoseAction analyze cases"
    - from: src/mcp/tools/recipe.test.ts
      to: src/mcp/tools/recipe.ts
      via: "handleRecipeAction recommend cases"
    - from: tests/mcp/prompts.test.ts
      to: src/mcp/prompts.ts
      via: "registerCoolifyPrompts"
---

<objective>
Wave 0 Nyquist RED scaffolds for Phase 31 Agent Playbooks.

Purpose: Lock BRAIN/PLAY/SREC behaviors with it.fails before production wiring, following Phase 28–30 pattern.

Output: log-patterns shell + RED tests for matchers, diagnose.analyze, recipe.recommend, prompts 6-name set, capability keys; no handler GREEN yet.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-agent-playbooks/31-CONTEXT.md
@.planning/phases/31-agent-playbooks/31-RESEARCH.md
@.planning/phases/31-agent-playbooks/31-PATTERNS.md
@.planning/phases/31-agent-playbooks/31-VALIDATION.md
@.cursor/skills/spike-findings-awesome-coolify/SKILL.md
@src/mcp/tools/diagnose.ts
@src/mcp/tools/diagnose.test.ts
@src/mcp/tools/recipe.ts
@src/mcp/tools/recipe.test.ts
@src/mcp/prompts.ts
@tests/mcp/prompts.test.ts
@src/mcp/tools/system.test.ts
@src/utils/issue-classifier.test.ts
</context>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Analyze logs via rule patterns with severity + next actions | 00 RED, 01 GREEN | COVERED | ROADMAP success 1–2 |
| GOAL | — | Parameterized playbooks: incident, rollback, maintenance-window | 00 RED, 02 GREEN | COVERED | ROADMAP success 3 |
| GOAL | — | Smart stack recommendation from live catalog | 00 RED, 03 GREEN | COVERED | ROADMAP success 4 |
| REQ | BRAIN-01 | Analyze runtime logs for OOM, 5xx spike, crash loop, connection refused | 00, 01 | COVERED | diagnose.analyze + log-patterns |
| REQ | BRAIN-02 | Severity, matched patterns, suggested next actions → diagnose/playbooks | 00, 01 | COVERED | FollowUpHint envelope |
| REQ | PLAY-01 | MCP prompts for incident, rollback, maintenance-window | 00, 02 | COVERED | prompts.ts |
| REQ | PLAY-02 | Compose existing atomic tools only | 00, 02 | COVERED | no parallel clients |
| REQ | SREC-01 | Stack recommendation → one-click + env + deploy plan | 00, 03 | COVERED | recipe.recommend |
| REQ | SREC-02 | Live service.list-types catalog, not hardcoded YAML SoT | 00, 03 | COVERED | fetchServiceTemplates |
| RESEARCH | — | Extend diagnose + recipe (no new top-level tools) | 01, 03 | COVERED | D-01, D-12 |
| RESEARCH | — | Extract log-patterns.ts pure matchers | 00 shell, 01 GREEN | COVERED | |
| RESEARCH | — | Upgrade incident; add rollback + maintenance-window | 02 | COVERED | D-07, D-08 |
| RESEARCH | — | Capabilities + README EN/DE + coverage | 00 RED, 04 GREEN | COVERED | D-19 |
| RESEARCH | — | No new npm packages | all | COVERED | |
| CONTEXT | D-01..D-05 | diagnose.analyze packaging + patterns + hints | 01 | COVERED | |
| CONTEXT | D-06 | Analyze advisory-only (one-way) | 01 gate | COVERED | checkpoint |
| CONTEXT | D-07..D-08, D-10..D-11 | Prompt playbooks composition | 02 | COVERED | |
| CONTEXT | D-09 | Rollback confirm in prompt (one-way) | 02 gate | COVERED | checkpoint |
| CONTEXT | D-12..D-13, D-15 | recipe.recommend live catalog | 03 | COVERED | |
| CONTEXT | D-14 | Recommend advisory default (one-way) | 03 gate | COVERED | checkpoint |
| CONTEXT | D-16..D-20 | Instance scope, errors, no stubs, caps, SAF-01 | 01–04 | COVERED | |
| CONTEXT | Deferred | Service/DB logs, fan-out, ML, auto-playbook, YAML SoT | — | N/A | Deferred Ideas |

## Artifacts this phase produces

- `src/utils/log-patterns.ts` — shell export `matchLogPatterns` (throws or returns empty until Wave 1).
- `src/utils/log-patterns.test.ts` — RED fixtures per pattern ID.
- `src/mcp/tools/diagnose.test.ts` — RED `describe('diagnose analyze')`.
- `src/mcp/tools/recipe.test.ts` — RED `describe('recipe recommend')`.
- `tests/mcp/prompts.test.ts` — RED six-prompt registration + composition cites.
- `src/mcp/tools/system.test.ts` — RED seventeen-key capability expectations.

<tasks>

<task type="tdd">
  <name>Task 1: log-patterns shell + matcher RED scaffolds</name>
  <files>src/utils/log-patterns.ts, src/utils/log-patterns.test.ts</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 2 matchers, Validation Architecture)
    - .planning/phases/31-agent-playbooks/31-PATTERNS.md (log-patterns.ts + test analogs)
    - src/utils/issue-classifier.test.ts (fixture + hint shape style)
    - src/utils/log-helpers.test.ts (inline string fixtures)
  </read_first>
  <behavior>
    - matchLogPatterns([]) returns [].
    - oom fixture → id oom, severity critical, evidence capped.
    - http_5xx_spike fixture (≥5 status-like 5xx lines) → id http_5xx_spike, severity high.
    - crash_loop fixture (≥3 restart/fatal lines) → id crash_loop, severity high.
    - connection_refused fixture → id connection_refused, severity high.
    - Empty-noise lines alone produce no fabricated matches.
  </behavior>
  <action>
    Create src/utils/log-patterns.ts with exported matchLogPatterns(lines: string[]) shell (return [] or throw not-implemented) so tests import cleanly. Create src/utils/log-patterns.test.ts with per-pattern describe blocks and it.fails cases locking BRAIN-01 pattern IDs from RESEARCH Pattern 2. Do not implement real regex bodies yet (Wave 1). Do not import API clients.
  </action>
  <verify>
    <automated>npx vitest run src/utils/log-patterns.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File src/utils/log-patterns.test.ts exists and uses it.fails for the four pattern IDs
    - npm/pre-commit path stays green because failures are wrapped in it.fails
    - log-patterns.ts exports matchLogPatterns
  </acceptance_criteria>
  <done>Matcher RED scaffolds locked for BRAIN-01.</done>
</task>

<task type="tdd">
  <name>Task 2: diagnose.analyze and recipe.recommend RED scaffolds</name>
  <files>src/mcp/tools/diagnose.test.ts, src/mcp/tools/recipe.test.ts</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 1 envelope, Pattern 4 envelope, Wave 0 Gaps)
    - .planning/phases/31-agent-playbooks/31-CONTEXT.md (D-02, D-05, D-06, D-13, D-14, D-15)
    - src/mcp/tools/diagnose.test.ts (describe diagnose logs mock pattern)
    - src/mcp/tools/recipe.test.ts (create-one-click + fetchServiceTemplates mock)
  </read_first>
  <behavior>
    - diagnose.analyze schema accepts uuid and optional deployment_uuid, lines, offset, max_chars, instance (D-02/D-03).
    - analyze on OOM fixture logs returns matched_patterns with severity + FollowUpHint linking diagnose and playbook names (BRAIN-02, D-05).
    - analyze on empty logs returns matched_patterns [] and empty-log hint spirit (D-06).
    - analyze fetch failure returns soft partial analyze_failed without crash (D-06/D-17).
    - analyze response includes advisory: true and does not call mutation clients (D-06).
    - recipe.recommend with stack Next.js + Postgres returns plan_steps with recipe_action values and catalog_source live (SREC-01/02).
    - recommend never invents catalog_id absent from mocked templates (D-15).
    - unknown unmappable stack → structured validation error with list-types recovery hint (D-14).
    - recommend does not call createService / createApplication (D-14).
  </behavior>
  <action>
    Add describe('diagnose analyze') and describe('recipe recommend') blocks with it.fails integration cases per behavior above. Mirror existing fetchApplicationLogs / ofetch mocks from Phase 26/20 tests. Do not wire production handlers yet. Keep existing green tests untouched.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/diagnose.test.ts -t analyze -x ; npx vitest run src/mcp/tools/recipe.test.ts -t recommend -x</automated>
  </verify>
  <acceptance_criteria>
    - diagnose.test.ts contains describe block named diagnose analyze using it.fails
    - recipe.test.ts contains describe block named recipe recommend using it.fails
    - Existing non-analyze/non-recommend describe blocks still pass
  </acceptance_criteria>
  <done>BRAIN and SREC RED handler scaffolds locked.</done>
</task>

<task type="tdd">
  <name>Task 3: prompts 6-name set and capability key RED scaffolds</name>
  <files>tests/mcp/prompts.test.ts, src/mcp/tools/system.test.ts</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 3 prompts, Pitfall 4–5)
    - .planning/phases/31-agent-playbooks/31-CONTEXT.md (D-08, D-09, D-10, D-19)
    - tests/mcp/prompts.test.ts (current four-prompt registration)
    - src/mcp/tools/system.test.ts (CAPABILITY_KEYS fifteen-key list)
  </read_first>
  <behavior>
    - registerCoolifyPrompts registers exactly six names: deploy, diagnose, new-project, incident, rollback, maintenance-window (PLAY-01, D-08).
    - rollback prompt content cites deployment.preflight, deployment.rollback, human approval before confirm true, and COOLIFY_ROLLBACK_UNAVAILABLE (D-09, PLAY-02).
    - maintenance-window cites stop then start/restart on application|service|database with confirm language (D-10).
    - upgraded incident cites diagnose.analyze and deployment.preflight (D-08).
    - Prompt bodies cite MCP tool action names only — no ofetch/axios/fetch client strings (PLAY-02).
    - system.version capabilities expect diagnose_analyze and recipe_recommend (seventeen keys total) (D-19).
  </behavior>
  <action>
    Convert or add it.fails cases so current green four-prompt assertions remain green until Wave 2; add new it.fails for six-prompt set and composition strings. Add it.fails capability expectations for diagnose_analyze and recipe_recommend without changing capabilities.ts yet (Wave 4). Do not implement prompts or capabilities in this plan.
  </action>
  <verify>
    <automated>npx vitest run tests/mcp/prompts.test.ts -x ; npx vitest run src/mcp/tools/system.test.ts -t capabilities -x</automated>
  </verify>
  <acceptance_criteria>
    - prompts.test.ts includes it.fails covering rollback and maintenance-window registration
    - system.test.ts includes it.fails mentioning diagnose_analyze and recipe_recommend
    - Existing fifteen-key green capability test still passes
  </acceptance_criteria>
  <done>PLAY and D-19 RED scaffolds locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → matcher API | Synthetic log lines only; no live Coolify in Wave 0 |
| Agent catalog expectations | RED locks published action/prompt/capability names |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | log-patterns fixtures | low | accept | Fixtures are trusted test data; production matchers capped in Wave 1 |
| T-31-02 | Elevation of Privilege | rollback prompt RED | high | mitigate | Lock STOP + confirm:true language in it.fails before Wave 2 implements prompt text |
| T-31-03 | Tampering | recipe recommend RED | medium | mitigate | Lock live-catalog / no-invent-id assertions in it.fails |
| T-31-SC | Tampering | npm installs | high | accept | No new packages this phase — N/A |
</threat_model>

<verification>
npx vitest run src/utils/log-patterns.test.ts src/mcp/tools/diagnose.test.ts src/mcp/tools/recipe.test.ts tests/mcp/prompts.test.ts src/mcp/tools/system.test.ts -x
</verification>

<success_criteria>
All Wave 0 it.fails scaffolds exist; suite green; no production analyze/recommend/prompt GREEN yet.
</success_criteria>

<output>
Create `.planning/phases/31-agent-playbooks/31-00-SUMMARY.md` when done
</output>
