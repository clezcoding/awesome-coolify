---
phase: 31-agent-playbooks
plan: 01
type: execute
wave: 1
depends_on:
  - "31-00"
files_modified:
  - src/utils/log-patterns.ts
  - src/utils/log-patterns.test.ts
  - src/mcp/tools/diagnose.ts
  - src/mcp/tools/diagnose.test.ts
autonomous: false
requirements:
  - BRAIN-01
  - BRAIN-02
user_setup: []

must_haves:
  truths:
    - "Agent calls diagnose.analyze and receives matched_patterns for OOM, 5xx spike, crash loop, connection refused from application runtime logs (BRAIN-01, D-01..D-04)."
    - "Each match carries severity critical|high and FollowUpHint linking diagnose flows and playbook prompt names incident/rollback (BRAIN-02, D-05)."
    - "analyze is advisory-only: advisory true, no restart/redeploy/rollback client calls (D-06)."
    - "Empty logs return matched_patterns [] with empty-log hint spirit — no fabricated matches (D-06)."
    - "Log fetch failure returns soft partial analyze_failed + recovery hints (D-17)."
    - "Optional deployment_uuid adds build-log scan with source labeling; service/DB logs omitted (D-03, D-18)."
    - "diagnose.logs remains the raw fetch path — analyze does not redefine logs (D-02)."
    - "Wave 0 analyze + log-patterns it.fails flipped to passing it()."
  artifacts:
    - path: src/utils/log-patterns.ts
      provides: "matchLogPatterns + enrichPatternHints"
      exports: ["matchLogPatterns", "enrichPatternHints"]
    - path: src/mcp/tools/diagnose.ts
      provides: "analyze action on diagnose tool"
      contains: "handleDiagnoseAnalyze"
    - path: src/mcp/tools/diagnose.test.ts
      provides: "GREEN diagnose analyze tests from Wave 0"
      contains: "diagnose analyze"
  key_links:
    - from: handleDiagnoseAnalyze
      to: fetchApplicationLogs + buildRuntimeLogPayload
      via: "same path as handleDiagnoseLogs"
    - from: matchLogPatterns
      to: matched_patterns[].hint
      via: "enrichPatternHints → FollowUpHint"
    - from: matched_patterns[].hint.label
      to: "prompt incident / rollback"
      via: "D-05 playbook link in tracer slice"
  prohibitions:
    - statement: "Do not create a top-level log-brain or brain MCP tool (D-01)"
      status: enforced
      verification: "rg -n \"name: 'log-brain'|name: 'brain'\" src/mcp/server.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
    - statement: "Do not fold analyze into diagnose.logs response shape (D-02)"
      status: enforced
      verification: "rg -n \"action: 'analyze'\" src/mcp/tools/diagnose.ts | grep -v '^#' | wc -l | awk '{exit ($1>=1)?0:1}'"
    - statement: "Do not call service or database log endpoints from analyze (D-03, D-18)"
      status: enforced
      verification: "rg -n \"fetchServiceLogs|fetchDatabaseLogs|/services/.*/logs|/databases/.*/logs\" src/mcp/tools/diagnose.ts src/utils/log-patterns.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
---

<objective>
Tracer vertical slice: production-quality `diagnose.analyze` end-to-end — log-patterns → runtime fetch → matched_patterns + FollowUpHint playbook links.

Purpose: Prove Log Brain architecture (BRAIN-01/02) and advisory contract (D-06) before playbook/recipe expansion.

Output: `log-patterns.ts`, `diagnose.analyze` handler, Wave 0 analyze/matcher tests GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-agent-playbooks/31-CONTEXT.md
@.planning/phases/31-agent-playbooks/31-RESEARCH.md
@.planning/phases/31-agent-playbooks/31-PATTERNS.md
@.planning/phases/31-agent-playbooks/31-VALIDATION.md
@.planning/phases/31-agent-playbooks/31-00-SUMMARY.md
@src/mcp/tools/diagnose.ts
@src/utils/log-helpers.ts
@src/utils/diagnose-hints.ts
@src/utils/issue-classifier.ts
</context>

## Artifacts this phase produces

- `src/utils/log-patterns.ts` — deterministic matchers + hint enrichment for four named patterns.
- `src/mcp/tools/diagnose.ts` — `analyze` in catalog, schema, switch, `handleDiagnoseAnalyze`.
- GREEN log-patterns + diagnose analyze tests from Wave 0.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm D-06 advisory-only analyze contract</name>
  <files>.planning/phases/31-agent-playbooks/31-CONTEXT.md</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-CONTEXT.md (D-06, D-20)
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 1, Security Domain)
  </read_first>
  <decision>Confirm locked D-06 one-way safety contract before implementing diagnose.analyze: read-only/advisory only; never restart, redeploy, or roll back from analyze.</decision>
  <context>D-06 publishes analyze as advisory. Undoing after agents rely on it would break the published safety contract and allow triage actions to mutate Coolify state.</context>
  <options>
    <option id="approve-advisory-only">
      <name>Approve advisory-only analyze (D-06 locked)</name>
      <pros>Matches D-06, D-20, SAF-01 separation, threat model T-31-04.</pros>
      <cons>Caller must invoke mutation tools separately after reading hints.</cons>
    </option>
    <option id="stop-and-replan">
      <name>Stop and replan</name>
      <pros>Use only if D-06 was recorded incorrectly.</pros>
      <cons>Blocks BRAIN-01/02 because CONTEXT locks advisory-only.</cons>
    </option>
  </options>
  <action>Record checkpoint selection before touching production files. Continue only with approve-advisory-only; do not add analyze-triggered restart, redeploy, or rollback calls in this plan.</action>
  <verify>
    <automated>rg -n "D-06" .planning/phases/31-agent-playbooks/31-CONTEXT.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Checkpoint resolved as approve-advisory-only before Task 2
    - No production analyze handler written before this checkpoint completes
  </acceptance_criteria>
  <resume-signal>Select: approve-advisory-only (expected) or stop-and-replan</resume-signal>
  <done>D-06 gate confirmed.</done>
</task>

<task type="tracer" tdd="true">
  <name>End-to-end diagnose.analyze — one pattern path to playbook hints</name>
  <reversibility rating="one-way">D-06 advisory-only analyze is a published safety contract; reversing lets triage mutate Coolify.</reversibility>
  <files>src/utils/log-patterns.ts, src/utils/log-patterns.test.ts, src/mcp/tools/diagnose.ts, src/mcp/tools/diagnose.test.ts</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-CONTEXT.md (D-01..D-06, D-16..D-18)
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pattern 1–2, Anti-Patterns, Code Examples)
    - .planning/phases/31-agent-playbooks/31-PATTERNS.md (diagnose.ts + log-patterns analogs)
    - src/mcp/tools/diagnose.ts (handleDiagnoseLogs, catalog, schema, switch)
    - src/utils/log-helpers.ts (buildRuntimeLogPayload, EMPTY_RUNTIME_LOGS_HINT, processDeploymentBuildLogs)
    - src/utils/diagnose-hints.ts (FollowUpHint)
  </read_first>
  <behavior>
    - Agent calls diagnose with action analyze and application uuid.
    - Handler reuses resolveAppUuid + fetchApplicationLogs + buildRuntimeLogPayload from logs path (D-02).
    - matchLogPatterns detects at least oom on fixture; response matched_patterns include severity and hint with tool/action/label.
    - Hint labels or recommended_actions reference playbook prompt names incident and/or rollback (tracer playbook link, D-05).
    - Response advisory true; capped evidence only (not full logs_lines dump).
    - Empty logs → matched_patterns [] + hint; soft partial on fetch error.
  </behavior>
  <action>
    Per D-01/D-02: extend diagnoseActionsCatalog and diagnoseToolSchema with analyze (reuse query/uuid/name/domain/deployment_uuid/lines/offset/max_chars + diagnoseLogsReadParamKeys; omit mode). Implement matchLogPatterns for all four RESEARCH Pattern 2 IDs with discretionary thresholds inside research bounds; export enrichPatternHints mapping to FollowUpHint (diagnose.app, diagnose.logs, prompt incident/rollback). Implement handleDiagnoseAnalyze: same runtime log fetch as handleDiagnoseLogs; optional deployment_uuid build-log scan via processDeploymentBuildLogs with source label; default logs-only (no runDiagnoseAppCore unless opt-in false). Return buildReadResponse with matched_patterns, logs_meta, recommended_actions, advisory true. Soft partial analyze_failed on fetch errors (D-06/D-17). Empty logs use EMPTY_RUNTIME_LOGS_HINT spirit (D-06). Wire switch case analyze. Flip Wave 0 log-patterns and diagnose analyze it.fails to it(). Do not create new top-level tools. Do not implement prompts or recipe.recommend.
  </action>
  <verify>
    <automated>npx vitest run src/utils/log-patterns.test.ts -x && npx vitest run src/mcp/tools/diagnose.test.ts -t analyze -x</automated>
  </verify>
  <acceptance_criteria>
    - diagnoseActionsCatalog includes analyze
    - handleDiagnoseAnalyze exists and returns advisory true on happy path
    - matched_patterns entries include severity and hint with tool and action fields
    - At least one hint/label references incident or rollback playbook name
    - rg finds no triggerDeploy|restartApplication mutation calls inside handleDiagnoseAnalyze body
  </acceptance_criteria>
  <done>Tracer path analyze→hints→playbook link works end-to-end; BRAIN-01/02 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand remaining pattern fixtures + soft partial edges</name>
  <reversibility rating="costly">D-01/D-04/D-05 published analyze envelope and named pattern set become agent habits.</reversibility>
  <files>src/utils/log-patterns.ts, src/utils/log-patterns.test.ts, src/mcp/tools/diagnose.ts, src/mcp/tools/diagnose.test.ts</files>
  <read_first>
    - .planning/phases/31-agent-playbooks/31-RESEARCH.md (Pitfall 1 false positives, Assumptions A1–A3)
    - src/mcp/tools/diagnose.test.ts (Wave 0 analyze cases)
  </read_first>
  <behavior>
    - All four pattern fixtures green via matchLogPatterns and analyze handler.
    - Spike/crash_loop require minimum counts — single noisy line does not falsely match spike/crash_loop.
    - Soft partial and empty-log cases remain green.
    - Optional instance routing via parseWithInstanceRouting (D-16).
  </behavior>
  <action>
    Finish any remaining Wave 0 analyze edge cases: 5xx spike threshold, crash_loop threshold, connection_refused, build-log optional path, instance routing. Cap evidence samples (max 3 lines). Keep analyze descriptions distinct from logs (raw tail vs patterns). Update tool description/safety footer for analyze as advisory. Ensure D-05 recommended_actions dedupes pattern hints.
  </action>
  <verify>
    <automated>npx vitest run src/utils/log-patterns.test.ts src/mcp/tools/diagnose.test.ts -t analyze -x</automated>
  </verify>
  <acceptance_criteria>
    - Four pattern IDs covered by passing tests
    - Empty-log test asserts matched_patterns length 0
    - Soft-partial test asserts analyze_failed code present when fetch rejects
  </acceptance_criteria>
  <done>Full BRAIN-01/02 edge coverage green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → diagnose.analyze | Untrusted identifiers + optional instance; read-only Coolify log fetch |
| Coolify logs → matcher | Untrusted log text; regex on capped line windows only |
| Analyze response → agent | Advisory hints; mutations stay on other tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | log-patterns regex | medium | mitigate | Cap lines/max_chars; evidence max 3; min counts for spike/crash_loop |
| T-31-04 | Elevation of Privilege | diagnose.analyze | high | mitigate | D-06 advisory-only; no mutation client calls; checkpoint before ship |
| T-31-05 | Denial of Service | ReDoS in matchers | medium | mitigate | Simple bounded regexes; input already sliced by lines/max_chars |
| T-31-06 | Information Disclosure | analyze response | low | accept | Same log visibility as diagnose.logs; no secret reveal beyond existing log content |
| T-31-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/utils/log-patterns.test.ts src/mcp/tools/diagnose.test.ts -t analyze -x
</verification>

<success_criteria>
diagnose.analyze production path green; advisory contract held; playbook-linked hints present; BRAIN-01 and BRAIN-02 done.
</success_criteria>

<output>
Create `.planning/phases/31-agent-playbooks/31-01-SUMMARY.md` when done
</output>
