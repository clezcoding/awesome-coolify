---
phase: 16-coolify-cloud-server-branding
plan: 04
type: execute
wave: 2
depends_on: ['16-01', '16-02', '16-03']
files_modified:
  - docs/en/cloud.md
  - docs/de/cloud.md
  - README.md
  - README.de.md
  - .planning/codebase/CONVENTIONS.md
autonomous: false
requirements: [CLD-03]
must_haves:
  truths:
    - "docs/en/cloud.md exists with Coolify Cloud setup path (instance.add/import-env), smoke test (system/meta.version/light list), and known limits (D-10/D-12/D-13)"
    - "docs/de/cloud.md exists as German parity translation of docs/en/cloud.md (D-12)"
    - "README.md has a Coolify Cloud quick overview section with links to docs/en/cloud.md (D-11 — README = quick overview + links only)"
    - "README.de.md has a Coolify Cloud quick overview section with links to docs/de/cloud.md (D-11)"
    - ".planning/codebase/CONVENTIONS.md dual-repo section updated to single-repo (D-07) — references to awesome-coolify-mcp git remote and scripts/sync-public-repo.sh removed or marked retired"
    - "Cursor MCP server list screenshot proves awesome-coolify icon renders after reconnect (D-09) — or documented client limitation with evidence"
  artifacts:
    - docs/en/cloud.md (new topic doc)
    - docs/de/cloud.md (new topic doc, German parity)
    - README.md (Cloud quick overview + links section)
    - README.de.md (Cloud quick overview + links section, German parity)
    - .planning/codebase/CONVENTIONS.md (dual-repo section retired per D-07)
  key_links:
    - "README.md → docs/en/cloud.md (quick overview links to topic doc)"
    - "README.de.md → docs/de/cloud.md"
    - "docs/en/cloud.md → instance.add / import-env / system / cloud-info (agent smoke path per D-13)"
    - "docs/en/cloud.md → COOLIFY_CLOUD_FORBIDDEN / COOLIFY_CLOUD_UNSUPPORTED error codes (cross-link to Plan 16-01)"
  prohibitions:
    - "No full Cloud API handbook — D-10 limits depth to setup + smoke + known limits"
    - "No full README section split for all topics — D-11 ships Cloud as the first topic only; other sections stay in README"
    - "No dual-repo sync steps or references to awesome-coolify-mcp git remote — D-07 single repo"
    - "No live API probe documentation inside cloud.md that suggests cloud-info hits the API — D-16 local/static"
---

<objective>
Ship Coolify Cloud topic docs in EN/DE (docs/en/cloud.md + docs/de/cloud.md) with setup, smoke test, and known limits per D-10/D-12/D-13. Update README.md and README.de.md with a quick Cloud overview + links per D-11 (README stays quick; topic doc owns the depth). Retire the outdated dual-repo section in .planning/codebase/CONVENTIONS.md per D-07. Gate phase completion on a Cursor MCP server list screenshot proving the awesome-coolify icon renders after reconnect (D-09 aggressive verify), or document the client limitation with evidence.

Purpose: CLD-03 — users need an EN/DE Cloud setup path with smoke-test instructions. D-11 shifts docs to topic files; README keeps a quick overview + links. D-07 retires the dual-repo model in conventions. D-09 is the aggressive verify gate that proves BRND-01/02 landed in the actual Cursor UI.
Output: Two new topic docs + two README updates + one CONVENTIONS cleanup + one manual verify checkpoint (D-09 screenshot).
</objective>

<artifacts_produced>
Docs:
- `docs/en/cloud.md` — new topic doc: Coolify Cloud setup (instance.add / import-env), smoke test (system / meta.version / light list / cloud-info), known limits (no server create/validate/delete via API, some endpoints 404 → COOLIFY_CLOUD_UNSUPPORTED), error codes (COOLIFY_CLOUD_FORBIDDEN / COOLIFY_CLOUD_UNSUPPORTED), link back to README install section
- `docs/de/cloud.md` — German parity translation of docs/en/cloud.md (same structure, same code blocks, German prose)
- `README.md` — new '## ☁️ Coolify Cloud' quick overview section (≤15 lines) with link to docs/en/cloud.md
- `README.de.md` — new '## ☁️ Coolify Cloud' quick overview section (German) with link to docs/de/cloud.md
- `.planning/codebase/CONVENTIONS.md` — 'Repository Model' section rewritten to single-repo (clezcoding/awesome-coolify); dual-repo layout block and scripts/sync-public-repo.sh references removed or marked retired per D-07
</artifacts_produced>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md
@.planning/phases/16-coolify-cloud-server-branding/16-RESEARCH.md
@.planning/phases/16-coolify-cloud-server-branding/16-PATTERNS.md
@.planning/phases/16-coolify-cloud-server-branding/16-VALIDATION.md
@.planning/phases/16-coolify-cloud-server-branding/16-01-PLAN.md
@.planning/phases/16-coolify-cloud-server-branding/16-02-PLAN.md
@.planning/phases/16-coolify-cloud-server-branding/16-03-PLAN.md
@README.md
@README.de.md
@.planning/codebase/CONVENTIONS.md
@docs/assets/README.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/en/cloud.md + docs/de/cloud.md (Cloud setup, smoke, limits, errors)</name>
  <files>docs/en/cloud.md, docs/de/cloud.md</files>
  <read_first>
    - README.md (install section structure, env var table, structured errors section — mirror tone/depth in cloud.md)
    - README.de.md (German parity pattern — mirror in docs/de/cloud.md)
    - .planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md (D-10, D-12, D-13)
    - .planning/phases/16-coolify-cloud-server-branding/16-RESEARCH.md (Architecture diagram, Common Pitfalls)
    - .planning/phases/16-coolify-cloud-server-branding/16-01-PLAN.md (COOLIFY_CLOUD_FORBIDDEN / COOLIFY_CLOUD_UNSUPPORTED codes + recovery hints to reference)
    - .planning/phases/16-coolify-cloud-server-branding/16-02-PLAN.md (instance.cloud-info action to document)
    - src/mcp/tools/instance.ts (inferInstanceType — *.coolify.io classification, instance.add/import-env actions)
  </read_first>
  <behavior>
    - docs/en/cloud.md exists with sections: Overview, Setup (instance.add or import-env with COOLIFY_URL=https://app.coolify.io + team-scoped token), Smoke test (instance cloud-info → system health/meta.version → resource list or application get), Known limits (no server create/validate/delete via API on Cloud, some self-hosted endpoints 404 → COOLIFY_CLOUD_UNSUPPORTED, tokens are team-scoped), Error codes (COOLIFY_CLOUD_FORBIDDEN, COOLIFY_CLOUD_UNSUPPORTED with recovery hints), Optional curl one-liner for token sanity (D-13 Claude discretion)
    - docs/de/cloud.md exists as German parity translation — same structure, same code blocks, German prose
    - Both docs link back to README install section and to docs/assets/mcp-icon-192.png branding note (optional)
    - Both docs reference instance.cloud-info as the discovery action (D-16)
    - Both docs reference COOLIFY_CLOUD_FORBIDDEN / COOLIFY_CLOUD_UNSUPPORTED by code and link to Plan 16-01 recovery hints (or inline them)
    - No live API probe documentation suggesting cloud-info hits the API — D-16 local/static
  </behavior>
  <action>
    Create docs/en/cloud.md with the section structure listed in behavior. Setup section: show two paths — (1) instance.add with name 'cloud', url 'https://app.coolify.io', token '<team-scoped-token>', type 'cloud'; (2) import-env with COOLIFY_URL=https://app.coolify.io and COOLIFY_TOKEN set in process env, then instance import-env. Smoke section: instance cloud-info (no API call, reports isCloud/source/limits), then system action health, then meta version, then resource list or application get for one known UUID. Known limits section: list the limits from D-16 knownLimits plus any from RESEARCH Common Pitfalls — Cloud does not support server create/validate/delete via API; some self-hosted endpoints may 404 → COOLIFY_CLOUD_UNSUPPORTED; tokens are team-scoped (verify team ownership of target resource). Error codes section: COOLIFY_CLOUD_FORBIDDEN (403 on *.coolify.io — regenerate team-scoped token, verify team permissions), COOLIFY_CLOUD_UNSUPPORTED (404 on *.coolify.io — use self-hosted alternative or Cloud dashboard). Optional curl one-liner: `curl -H "Authorization: Bearer $COOLIFY_TOKEN" https://app.coolify.io/api/v1/version` for token sanity (D-13). Then create docs/de/cloud.md as the German parity translation — identical structure, identical code blocks, German prose. Use the existing README.de.md tone as a guide. Do NOT document a live cloud-info API probe — D-16 is local/static. Do NOT write a full Cloud API handbook — D-10 limits depth to setup + smoke + known limits. Do NOT split other README sections into topic docs — D-11 ships Cloud as the first topic only.
  </action>
  <verify>
    <automated>test -f docs/en/cloud.md && test -f docs/de/cloud.md && grep -q 'app.coolify.io' docs/en/cloud.md && grep -q 'app.coolify.io' docs/de/cloud.md && grep -q 'COOLIFY_CLOUD_FORBIDDEN' docs/en/cloud.md && grep -q 'COOLIFY_CLOUD_FORBIDDEN' docs/de/cloud.md && grep -q 'cloud-info' docs/en/cloud.md && grep -q 'cloud-info' docs/de/cloud.md</automated>
  </verify>
  <acceptance_criteria>
    - [ ] docs/en/cloud.md exists and contains 'app.coolify.io', 'COOLIFY_CLOUD_FORBIDDEN', 'COOLIFY_CLOUD_UNSUPPORTED', 'cloud-info'
    - [ ] docs/de/cloud.md exists and contains the same key literals (German parity)
    - [ ] Both docs have Setup, Smoke, Known limits, Error codes sections
    - [ ] Both docs reference instance.cloud-info as the discovery action
    - [ ] No live API probe documentation for cloud-info
    - [ ] No full Cloud API handbook (depth = setup + smoke + limits per D-10)
  </acceptance_criteria>
  <done>docs/en/cloud.md and docs/de/cloud.md exist with Setup/Smoke/Known limits/Error codes sections, both reference app.coolify.io, COOLIFY_CLOUD_FORBIDDEN, COOLIFY_CLOUD_UNSUPPORTED, and cloud-info; German parity; no live API probe docs; depth per D-10.</done>
</task>

<task type="auto">
  <name>Task 2: README.md + README.de.md Cloud quick overview + CONVENTIONS dual-repo cleanup</name>
  <files>README.md, README.de.md, .planning/codebase/CONVENTIONS.md</files>
  <read_first>
    - README.md (table of contents lines 64-80, install section, structured errors section — find insertion point for Cloud overview)
    - README.de.md (German parity — mirror structure)
    - .planning/codebase/CONVENTIONS.md (Repository Model lines 5-11 — dual-repo layout to retire)
    - .planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md (D-07, D-11)
    - docs/en/cloud.md (Task 1 output — link target)
    - docs/de/cloud.md (Task 1 output — link target)
  </read_first>
  <behavior>
    - README.md has a new '## ☁️ Coolify Cloud' section (≤15 lines) with a 2-3 sentence overview and a link to docs/en/cloud.md
    - README.de.md has a new '## ☁️ Coolify Cloud' section (German, ≤15 lines) with a link to docs/de/cloud.md
    - README table of contents updated to include the new Cloud section anchor (EN + DE)
    - .planning/codebase/CONVENTIONS.md 'Repository Model' section rewritten to single-repo: 'Single public repo (clezcoding/awesome-coolify) — npm package + GitHub Pages + source; no dual-repo sync'
    - References to scripts/sync-public-repo.sh and 'awesome-coolify-mcp' git remote removed or marked retired per D-07
    - No full Cloud playbook dumped into README — D-11 keeps README quick
  </behavior>
  <action>
    In README.md, insert a new '## ☁️ Coolify Cloud' section after the install section (or after the structured errors section — pick the most natural spot). Content: 2-3 sentences noting awesome-coolify-mcp works with Coolify Cloud (https://app.coolify.io) using the same 14 tools, team-scoped tokens, and structured cloud error codes (COOLIFY_CLOUD_FORBIDDEN / COOLIFY_CLOUD_UNSUPPORTED); link 'Full setup, smoke test, and known limits → docs/en/cloud.md'. Add the section anchor to the table of contents. In README.de.md, mirror the section in German with a link to docs/de/cloud.md. In .planning/codebase/CONVENTIONS.md, replace the 'Repository Model' section's dual-repo layout block (lines 5-11) with a single-repo statement: 'Single public repo (clezcoding/awesome-coolify) — npm package + GitHub Pages + source. No dual-repo sync; npm already points here. Dev-only work stays under .planning/, .cursor/, .agents/, graphify-out/, mcp_features.md (gitignored).' Remove or mark retired any references to scripts/sync-public-repo.sh and the 'awesome-coolify-mcp' separate git remote per D-07. Do NOT split other README sections into topic docs — D-11 ships Cloud as the first topic only. Do NOT dump the full Cloud playbook into README — keep it quick (≤15 lines).
  </action>
  <verify>
    <automated>grep -q 'Coolify Cloud' README.md && grep -q 'docs/en/cloud.md' README.md && grep -q 'Coolify Cloud' README.de.md && grep -q 'docs/de/cloud.md' README.de.md && grep -qi 'single' .planning/codebase/CONVENTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - [ ] README.md has '## ☁️ Coolify Cloud' section with link to docs/en/cloud.md
    - [ ] README.de.md has '## ☁️ Coolify Cloud' section (German) with link to docs/de/cloud.md
    - [ ] README EN/DE tables of contents include the new section anchor
    - [ ] .planning/codebase/CONVENTIONS.md Repository Model rewritten to single-repo per D-07
    - [ ] No references to scripts/sync-public-repo.sh or awesome-coolify-mcp git remote as active (removed or marked retired)
    - [ ] No full Cloud playbook in README (≤15 lines per section)
  </acceptance_criteria>
  <done>README.md and README.de.md each have a ≤15-line Coolify Cloud section linking to docs/en|de/cloud.md; CONVENTIONS.md Repository Model rewritten to single-repo per D-07; sync-public-repo.sh references retired; no full playbook in README.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-09 Cursor MCP server list icon screenshot verify gate</name>
  <files>docs/assets/cursor-icon-verify.png</files>
  <read_first>
    - .planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md (D-09 — aggressive verify gate; document client limitation only if verify fails, with evidence)
    - .planning/phases/16-coolify-cloud-server-branding/16-03-PLAN.md (Task 2 — McpServer constructor wiring of icons/title/description/websiteUrl that this checkpoint verifies)
    - .planning/phases/16-coolify-cloud-server-branding/16-RESEARCH.md (Pitfall 2 Cursor Icon Client Limitation; Open Questions (RESOLVED) — D-09 is the source of truth)
    - src/mcp/server.ts (McpServer constructor — confirm icons array + title/description/websiteUrl are present before running verify)
    - docs/assets/mcp-icon-192.png (the dedicated 192x192 icon asset that must render — confirm file exists and is 192x192 per Plan 16-03 Task 1 acceptance)
    - README.md (one-click install deeplink / MCP server config snippet used to re-add the server in Cursor)
  </read_first>
  <action>Human verifies Cursor MCP server list renders the awesome-coolify icon after reconnect (D-09 aggressive verify gate). See &lt;what-built&gt; and &lt;how-to-verify&gt; below.</action>
  <verify>
    <automated>test -f docs/assets/cursor-icon-verify.png || test -f docs/assets/cursor-icon-verify.md</automated>
  </verify>
  <acceptance_criteria>
    - [ ] jsDelivr serves the icon: `curl -I https://cdn.jsdelivr.net/gh/clezcoding/awesome-coolify@main/docs/assets/mcp-icon-192.png` returns 200 and `content-type: image/png`
    - [ ] Cursor MCP server list shows the Hex Robot Helper icon next to awesome-coolify-mcp after a fresh reconnect (screenshot saved to docs/assets/cursor-icon-verify.png) — D-09 PASS
    - [ ] OR, if the icon does NOT render: screenshot showing fallback `title: 'Awesome Coolify'` and `description` rendering correctly in Cursor + MCP Inspector output (`npx @modelcontextprotocol/inspector awesome-coolify-mcp`) showing `serverInfo.icons` present in the initialize response, saved to docs/assets/cursor-icon-verify.md — D-09 documented client limitation per RESEARCH Pitfall 2 / Open Question 1 (RESOLVED)
    - [ ] Verify artifact (png or md) committed to docs/assets/ and referenced in the phase SUMMARY
    - [ ] Outcome (PASS or documented limitation) recorded in 16-04-SUMMARY.md
  </acceptance_criteria>
  <done>Screenshot attached proving icon renders (D-09 PASS), or client limitation documented with MCP Inspector evidence showing serverInfo.icons present plus screenshot showing fallback title/description render (D-09 documented limitation per RESEARCH Pitfall 2).</done>
  <what-built>
    Plan 16-03 wired McpServer constructor with title 'Awesome Coolify', description, websiteUrl, and icons (jsDelivr PNG, 192x192). The MCP initialize handshake now emits serverInfo.icons + title/description/websiteUrl. This checkpoint proves the icon actually renders in the Cursor MCP server list after reconnect — the aggressive verify gate per D-09.
  </what-built>
  <how-to-verify>
    1. Confirm Plan 16-03 is merged to main and docs/assets/mcp-icon-192.png is pushed (jsDelivr @main cache may lag a few minutes — Pitfall 1).
    2. Verify jsDelivr serves the icon: `curl -I https://cdn.jsdelivr.net/gh/clezcoding/awesome-coolify@main/docs/assets/mcp-icon-192.png` returns 200 and `content-type: image/png`.
    3. In Cursor, open Settings → MCP servers, remove the awesome-coolify-mcp entry if present, then re-add it (or use the one-click install deeplink from README) so the client performs a fresh initialize handshake.
    4. Reconnect the MCP server (Cursor reload or restart).
    5. Open the Cursor MCP server list / chat sidebar and locate awesome-coolify-mcp.
    6. Screenshot the server list showing the Hex Robot Helper icon next to awesome-coolify-mcp. Save the screenshot to docs/assets/cursor-icon-verify.png (or docs/assets/cursor-icon-verify.md with embedded screenshot + notes).
    7. Expected: the branded 192x192 violet robot icon renders next to the server entry. If the icon does NOT render (Cursor client limitation per RESEARCH Pitfall 2 / Open Question 1 RESOLVED), document the limitation with evidence: screenshot showing the fallback title 'Awesome Coolify' and description rendering correctly, plus a note that icons are emitted in serverInfo per MCP SDK but Cursor does not currently render them in all UI surfaces. Attach the MCP Inspector output (`npx @modelcontextprotocol/inspector awesome-coolify-mcp`) showing serverInfo.icons is present in the initialize response.
  </how-to-verify>
  <resume-signal>Type "approved" with the screenshot path, or describe the client limitation with evidence (Inspector output + screenshot showing fallback title/description render)</resume-signal>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → user/agent | Cloud docs cross this boundary; must not embed real tokens or internal hostnames |
| README → public internet | README is public; Cloud section must not leak internal setup details |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Status | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------|-------------|-----------------|
| T-16-04-01 | Information Disclosure | docs/en/cloud.md or docs/de/cloud.md embedding real team-scoped tokens in examples | high | mitigated | mitigate | All examples use placeholder `<team-scoped-token>` or `$COOLIFY_TOKEN` env var — never a real token; smoke section uses env var references only |
| T-16-04-02 | Information Disclosure | README Cloud section leaking internal Coolify Cloud hostnames or team names | low | mitigated | accept | Only public hostname app.coolify.io referenced — no internal hostnames or team names |
| T-16-04-03 | Tampering | CONVENTIONS.md dual-repo section left stale, causing downstream agents to plan sync-to-second-repo | medium | mitigated | mitigate | Repository Model section rewritten to single-repo per D-07; sync-public-repo.sh references retired |
| T-16-04-04 | Spoofing | docs suggesting cloud-info hits the live API (misleading agents into thinking it's a capability probe) | medium | mitigated | mitigate | cloud-info documented as local/static only per D-16; no live API probe language |
| T-16-04-SC | Tampering | npm installs (none new) | high | mitigated | accept | No new external packages; docs + README + CONVENTIONS only |
</threat_model>

<verification>
- `test -f docs/en/cloud.md && test -f docs/de/cloud.md` — both topic docs exist
- `grep -q 'Coolify Cloud' README.md && grep -q 'Coolify Cloud' README.de.md` — README sections present
- `grep -qi 'single' .planning/codebase/CONVENTIONS.md` — dual-repo retired
- `npm test` — no regressions (docs-only plan; tests should stay green)
- Manual D-09: Cursor MCP server list screenshot showing icon (or documented client limitation with Inspector evidence)
</verification>

<success_criteria>
- docs/en/cloud.md and docs/de/cloud.md shipped with Setup/Smoke/Known limits/Error codes per D-10/D-12/D-13
- README.md and README.de.md each have a ≤15-line Cloud quick overview linking to topic docs per D-11
- .planning/codebase/CONVENTIONS.md Repository Model rewritten to single-repo per D-07
- D-09 aggressive verify gate: Cursor screenshot proves icon renders, or client limitation documented with Inspector evidence
- No full Cloud API handbook (D-10 depth)
- No live API probe docs for cloud-info (D-16)
- No dual-repo sync references (D-07)
- No existing test regresses
</success_criteria>

<output>
Create `.planning/phases/16-coolify-cloud-server-branding/16-04-SUMMARY.md` when done. Attach the D-09 screenshot path (docs/assets/cursor-icon-verify.png or .md) to the summary.
</output>