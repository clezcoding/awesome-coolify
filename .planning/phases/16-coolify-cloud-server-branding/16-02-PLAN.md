---
phase: 16-coolify-cloud-server-branding
plan: 02
type: execute
wave: 1
depends_on: ['16-00']
files_modified:
  - src/mcp/tools/instance.ts
  - src/mcp/tools/instance.test.ts
autonomous: true
requirements: [CLD-01]
must_haves:
  truths:
    - "instanceActionSchema discriminatedUnion includes a new 'cloud-info' action branch with optional `instance` param (D-16/D-17)"
    - "handleInstanceAction({action:'cloud-info'}) returns buildReadResponse with data.{isCloud,url,source,setupHints,knownLimits,docsLink} — local/static only, no live API probe (D-16)"
    - "source === 'registry' when explicit instance param resolves to a registered entry (D-17 routing)"
    - "source === 'env' when no instance param and process.env.COOLIFY_URL+COOLIFY_TOKEN both set (D-15 env-only Cloud path)"
    - "source === 'infer' when no instance param, no env, no default — fallback url 'https://app.coolify.io', isCloud true (D-16 fallback)"
    - "isCloud derived from inferInstanceType(resolvedUrl) — registry type:cloud OR hostname *.coolify.io (D-03/D-14)"
    - "cloud-info with unknown instance name returns COOLIFY_INSTANCE_NOT_FOUND envelope (reuses resolveCredentials path — D-17)"
    - "cloud-info response includes setupHints (≥2 actionable EN steps: generate team-scoped token, run instance.add/import-env) and knownLimits (≥2: no server create/validate/delete via API on Cloud, some self-hosted endpoints 404 → COOLIFY_CLOUD_UNSUPPORTED) and docsLink 'docs/en/cloud.md' (D-16 contract)"
    - "cloud-info makes zero network calls — fully local/static (D-16, no live API probe)"
  artifacts:
    - src/mcp/tools/instance.ts (new cloud-info action schema + handler branch)
    - src/mcp/tools/instance.test.ts (Wave 0 RED scaffolds flip GREEN)
  key_links:
    - "instance.cloud-info → InstanceManager.resolveCredentials (reuses Phase 15 precedence: instance param → env → default → COOLIFY_NO_INSTANCE)"
    - "instance.cloud-info → inferInstanceType (existing helper, reused for isCloud derivation)"
    - "instance.cloud-info → buildReadResponse (uniform envelope)"
  prohibitions:
    - "No live API probe inside cloud-info — D-16 explicitly rejects live capability matrix"
    - "No soft _meta.cloudNote warning on instance.add/update — D-14 removed this"
    - "No hard rule that type:cloud requires *.coolify.io URL — D-14 allows any URL with type:cloud"
    - "No new network client or fetcher import — cloud-info is local-only"
---

<objective>
Add a `cloud-info` action to the `instance` tool: a local/static discovery action that reports isCloud, resolved url, source (registry|env|infer), setup hints, known Cloud limits, and a docs link. Reuses Phase 15 InstanceManager.resolveCredentials precedence and the existing inferInstanceType helper. No live API probe. Flips the Wave 0 RED scaffolds in instance.test.ts to GREEN.

Purpose: CLD-01 discoverability — agents (and users) need a single tool call to learn whether the current/target instance is Coolify Cloud, what URL is resolved, where the credentials came from, and what Cloud-specific limits apply, without hitting the live API (D-16). This complements the same-tool-surface guarantee: Cloud connects with the same client as self-hosted, and cloud-info tells the agent what to expect.
Output: Extended src/mcp/tools/instance.ts with cloud-info action schema + handler branch; instance.test.ts GREEN.
</objective>

<artifacts_produced>
Source modules (production code):
- `src/mcp/tools/instance.ts` — new `cloudInfoActionSchema` (z.object({ action: z.literal('cloud-info'), instance: instanceNameSchema.optional() }).strict()); `cloudInfoActionSchema` added to `instanceActionSchema` discriminatedUnion array; new `case 'cloud-info'` branch in `handleInstanceAction` switch that resolves credentials via `InstanceManager.resolveCredentials(parsed.instance, process.env)` (or env-derived), derives `isCloud` via existing `inferInstanceType`, computes `source` per D-16, and returns `buildReadResponse({ isCloud, url, source, setupHints, knownLimits, docsLink })`
Test files:
- `src/mcp/tools/instance.test.ts` — Wave 0 RED scaffolds for cloud-info flip to GREEN
</artifacts_produced>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md
@.planning/phases/16-coolify-cloud-server-branding/16-RESEARCH.md
@.planning/phases/16-coolify-cloud-server-branding/16-PATTERNS.md
@.planning/phases/16-coolify-cloud-server-branding/16-VALIDATION.md
@src/mcp/tools/instance.ts
@src/mcp/tools/instance.test.ts
@src/utils/instance-registry.ts
@src/utils/errors.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: instance.ts — add cloud-info action schema + handler branch (local/static, no live API)</name>
  <files>src/mcp/tools/instance.ts</files>
  <read_first>
    - src/mcp/tools/instance.ts (existing instanceActionSchema discriminatedUnion lines 107-115, handleInstanceAction switch lines 250-342, inferInstanceType lines 222-232, resolveEnvCredentials lines 160-191)
    - src/mcp/tools/instance.test.ts (Wave 0 RED scaffolds for cloud-info — every it.fails is a GREEN target)
    - src/utils/instance-registry.ts (InstanceManager.resolveCredentials precedence: instance param → env both-set → partial env error → registry default → COOLIFY_NO_INSTANCE)
    - src/utils/errors.ts (CoolifyApiError, RECOVERY_HINTS, wrapMcpError — cloud-info reuses COOLIFY_INSTANCE_NOT_FOUND for unknown instance)
    - .planning/phases/16-coolify-cloud-server-branding/16-RESEARCH.md (Pattern: Static Discovery Action, Architecture diagram lines 117-134)
    - .planning/phases/16-coolify-cloud-server-branding/16-PATTERNS.md (Static Cloud-Info Action Schema lines 156-211, Action testing structure lines 283-302)
    - .planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md (D-14, D-15, D-16, D-17)
  </read_first>
  <behavior>
    - All Wave 0 it.fails blocks in instance.test.ts for cloud-info flip to GREEN (or convert to regular it)
    - instanceActionSchema.safeParse({ action: 'cloud-info' }).success === true
    - instanceActionSchema.safeParse({ action: 'cloud-info', instance: 'Bad-Name' }).success === false (instance param still validated against name regex)
    - cloud-info with no instance param + env both set → data.isCloud === inferInstanceType(envUrl), data.url === envUrl, data.source === 'env'
    - cloud-info with explicit registered cloud instance → data.isCloud === true, data.url === registry url, data.source === 'registry'
    - cloud-info with no instance + no env + no default → data.isCloud === true, data.url === 'https://app.coolify.io', data.source === 'infer'
    - cloud-info with unknown instance name → CoolifyApiError envelope code COOLIFY_INSTANCE_NOT_FOUND (reuses resolveCredentials throw)
    - cloud-info data.setupHints length ≥ 2, data.knownLimits length ≥ 2, data.docsLink contains 'docs/'
    - cloud-info makes zero network calls — no createCoolifyClient / fetchHealth / fetchVersion invocation
    - Existing instance actions (list/get/add/update/delete/set-default/import-env) unchanged
  </behavior>
  <action>
    In src/mcp/tools/instance.ts, add a new `cloudInfoActionSchema` const: z.object({ action: z.literal('cloud-info'), instance: instanceNameSchema.optional().describe('Optional instance name to inspect; defaults to env/default resolution') }).strict(). Add `cloudInfoActionSchema` to the `instanceActionSchema` discriminatedUnion array (after importEnvActionSchema). In `handleInstanceAction`, add a new `case 'cloud-info':` branch before the `default` exhaustive check. The branch: (1) try to resolve credentials via `InstanceManager.resolveCredentials(parsed.instance, process.env)` — this throws COOLIFY_INSTANCE_NOT_FOUND for unknown names and COOLIFY_PARTIAL_ENV for partial env, which propagate to wrapMcpError at the handler boundary (existing pattern); (2) compute `resolvedUrl = creds.url`, `resolvedType = inferInstanceType(creds.url)`, and `source = parsed.instance ? 'registry' : (process.env.COOLIFY_URL && process.env.COOLIFY_TOKEN ? 'env' : 'registry')` — when resolveCredentials succeeds via registry default (no instance param, no env), source is 'registry'; when it succeeds via env, source is 'env'; when an explicit instance param is passed, source is 'registry'. (3) If resolveCredentials throws COOLIFY_NO_INSTANCE (no creds anywhere), catch it and fall back to `resolvedUrl = 'https://app.coolify.io'`, `resolvedType = 'cloud'`, `source = 'infer'` per D-16 — do NOT rethrow; the infer path is the documented fallback. (4) Return `buildReadResponse({ isCloud: resolvedType === 'cloud', url: resolvedUrl, source, setupHints: ['Generate a team-scoped token in app.coolify.io under Keys & Tokens.', 'Run instance.add or import-env to register it locally.'], knownLimits: ['Cloud does not support server creation/validation/deletion via API.', 'Some self-hosted endpoints may return 404 (COOLIFY_CLOUD_UNSUPPORTED).'], docsLink: 'docs/en/cloud.md' })`. Do NOT make any network call — no import of createCoolifyClient/fetchHealth/fetchVersion. Do NOT add a soft _meta.cloudNote warning on instance.add/update (D-14 removed it). Do NOT enforce that type:cloud requires *.coolify.io URL (D-14 allows any URL with type:cloud). The handler's outer try/catch already calls wrapMcpError(error) for thrown CoolifyApiError — no change needed there. Update the `instance` tool description in src/mcp/server.ts to mention cloud-info in the action list (append to the existing description string). Run instance.test.ts after to confirm Wave 0 scaffolds flip GREEN and existing instance tests stay GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/instance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All Wave 0 cloud-info it.fails blocks in instance.test.ts flip to GREEN (or converted to regular it and pass)
    - [ ] cloudInfoActionSchema added to instanceActionSchema discriminatedUnion
    - [ ] instanceActionSchema.safeParse({ action: 'cloud-info' }).success === true
    - [ ] cloud-info handler returns data.{isCloud,url,source,setupHints,knownLimits,docsLink}
    - [ ] Three source paths verified: registry (named instance), env (COOLIFY_URL+COOLIFY_TOKEN), infer (fallback https://app.coolify.io)
    - [ ] Unknown instance name → COOLIFY_INSTANCE_NOT_FOUND envelope
    - [ ] Zero network calls in cloud-info branch (no createCoolifyClient/fetchHealth/fetchVersion import or invocation)
    - [ ] Existing instance actions (list/get/add/update/delete/set-default/import-env) unchanged
    - [ ] `npm run build` (tsup) GREEN
  </acceptance_criteria>
  <done>instance.test.ts all cloud-info scaffolds GREEN; cloudInfoActionSchema added to discriminatedUnion; handler branch returns isCloud/url/source/setupHints/knownLimits/docsLink; three source paths verified; unknown instance → COOLIFY_INSTANCE_NOT_FOUND; zero network calls; existing instance actions unchanged; tsup build GREEN.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → cloud-info handler | Untrusted `instance` param crosses here; unknown name must not silently fall back to default |
| cloud-info → local registry/env | Reads ~/.coolify-mcp/instances.json and process.env.COOLIFY_*; no network egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Status | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------|-------------|-----------------|
| T-16-02-01 | Spoofing | unknown instance name silently falling back to env/default, masking misconfiguration | high | mitigated | mitigate | InstanceManager.resolveCredentials throws COOLIFY_INSTANCE_NOT_FOUND for unknown names — cloud-info only falls back to 'infer' when resolveCredentials throws COOLIFY_NO_INSTANCE (no creds anywhere), not for unknown names (D-17) |
| T-16-02-02 | Information Disclosure | cloud-info echoing resolved URL or token in response | medium | mitigated | mitigate | cloud-info returns only `url` (public hostname) — never the token; setupHints/knownLimits/docsLink are static strings with no secrets |
| T-16-02-03 | Information Disclosure | cloud-info response leaking internal self-hosted hostnames | low | mitigated | accept | URL is the resolved Coolify instance URL — same value the agent already passes to instance.add; not a secret |
| T-16-02-04 | Tampering | cloud-info making live API calls that could be abused for SSRF or token replay | high | mitigated | mitigate | cloud-info is local/static only — zero network calls (D-16); no createCoolifyClient/fetchHealth/fetchVersion import in the branch |
| T-16-02-SC | Tampering | npm installs (none new) | high | mitigated | accept | No new external packages; reuses existing instance-registry + errors + formatters |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/instance.test.ts` — GREEN (Wave 0 scaffolds flipped + existing instance tests)
- `npm run build` (tsup) — GREEN
- `npm test` — no regressions vs current suite
- Manual: `npx vitest run src/mcp/tools/instance.test.ts -t 'cloud-info'` shows all cloud-info cases pass
</verification>

<success_criteria>
- cloud-info action added to instance tool per D-16/D-17
- Three source paths verified (registry/env/infer)
- Unknown instance → COOLIFY_INSTANCE_NOT_FOUND (no silent fallback)
- Zero network calls (local/static only per D-16)
- setupHints ≥2, knownLimits ≥2, docsLink 'docs/en/cloud.md' per D-16 contract
- instance tool description in server.ts updated to mention cloud-info
- instance.test.ts fully GREEN
- No existing test regresses
</success_criteria>

<output>
Create `.planning/phases/16-coolify-cloud-server-branding/16-02-SUMMARY.md` when done
</output>
