---
phase: 16-coolify-cloud-server-branding
plan: 01
type: execute
wave: 1
depends_on: ['16-00']
files_modified:
  - src/utils/errors.ts
  - src/utils/errors.test.ts
autonomous: true
requirements: [CLD-02]
must_haves:
  truths:
    - "CoolifyErrorCode union includes 'COOLIFY_CLOUD_FORBIDDEN' and 'COOLIFY_CLOUD_UNSUPPORTED' literals (D-02)"
    - "RECOVERY_HINTS has entries for COOLIFY_CLOUD_FORBIDDEN and COOLIFY_CLOUD_UNSUPPORTED — each ≥1 actionable EN hint mentioning team-scoped token / endpoint-not-supported-on-Cloud respectively (D-04)"
    - "isCloudUrl(url) helper exported from errors.ts: returns true iff hostname === 'coolify.io' OR hostname endsWith '.coolify.io' (D-03, reuse inferInstanceType logic)"
    - "toStructuredError inspects ofetch FetchError.request URL string and sets isCloud=true when isCloudUrl(request) — no global mutable cloud state (D-03, Pitfall 1)"
    - "Cloud hostname HTTP 403 → COOLIFY_CLOUD_FORBIDDEN with recoveryHints; self-hosted 403 stays on existing statusToCode path (D-03)"
    - "Cloud hostname HTTP 404 → COOLIFY_CLOUD_UNSUPPORTED with recoveryHints; self-hosted 404 stays COOLIFY_404 (D-03)"
    - "Token-bearing request URLs and response bodies are redacted via sanitizeMessage/redactSecrets in both cloud and non-cloud paths — no token leakage in cloud error envelopes (T-16-01-01)"
    - "Existing error codes, recovery hints, statusToCode, mapApiError default path, and wrapMcpError behavior unchanged for non-cloud inputs — no regression"
  artifacts:
    - src/utils/errors.ts (extended with 2 new codes + isCloudUrl + cloud-aware toStructuredError/mapApiError branch)
    - src/utils/errors.test.ts (Wave 0 RED scaffolds flip GREEN)
  key_links:
    - "errors.isCloudUrl ← errors.toStructuredError (request URL inspection)"
    - "errors.mapApiError(error, httpStatus, coolifyMessage, isCloud) ← errors.toStructuredError (cloud flag forwarded)"
    - "errors.toStructuredError ← src/api/client.ts withMappedErrors (already throws CoolifyApiError(toStructuredError(error)) — no client change needed)"
  prohibitions:
    - "No global mutable active-cloud state — isCloud derived per-error from request URL (D-03, Pitfall 1)"
    - "No cloud code on self-hosted based on response body alone — must use hostname only (D-03)"
    - "No 501 stubs or invented cloud error codes beyond D-02's two (no billing/plan/rate-limit zoo — deferred per CONTEXT)"
    - "No token echo in cloud error messages — sanitizeMessage + redactSecrets on every path"
---

<objective>
Add Coolify Cloud error codes (COOLIFY_CLOUD_FORBIDDEN, COOLIFY_CLOUD_UNSUPPORTED) and a hostname-based isCloudUrl helper to errors.ts. Wire toStructuredError to inspect ofetch FetchError.request URL and route cloud-host 403/404 to the new codes with actionable recovery hints, while self-hosted requests stay on the existing statusToCode path. Flips the Wave 0 RED scaffolds in errors.test.ts to GREEN.

Purpose: CLD-02 — Cloud-only or permission-denied endpoint failures must return structured recovery hints instead of opaque 403/404 loops. Per D-03, cloud detection uses hostname only (no response-body guesswork on self-hosted) so multi-instance concurrent requests to self-hosted + cloud do not bleed cloud codes.
Output: Extended src/utils/errors.ts with two new codes + isCloudUrl helper + cloud-aware toStructuredError branch; errors.test.ts GREEN.
</objective>

<artifacts_produced>
Source modules (production code):
- `src/utils/errors.ts` — extended `CoolifyErrorCode` union with `COOLIFY_CLOUD_FORBIDDEN`, `COOLIFY_CLOUD_UNSUPPORTED`; extended `RECOVERY_HINTS` with two matching entries; new exported `isCloudUrl(url: string): boolean` helper; `mapApiError` gains an optional `isCloud` parameter and cloud-specific 403/404 branches; `toStructuredError` inspects `fetchError.request` URL string and forwards `isCloud` to `mapApiError`
Test files:
- `src/utils/errors.test.ts` — Wave 0 RED scaffolds (cloud hostname 403/404 mapping, self-hosted negative, code union membership, RECOVERY_HINTS presence) flip to GREEN
</artifacts_produced>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md
@.planning/phases/16-coolify-cloud-server-branding/16-RESEARCH.md
@.planning/phases/16-coolify-cloud-server-branding/16-PATTERNS.md
@.planning/phases/16-coolify-cloud-server-branding/16-VALIDATION.md
@src/utils/errors.ts
@src/utils/errors.test.ts
@src/utils/redact.ts
@src/api/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: errors.ts — add cloud codes + isCloudUrl + cloud-aware toStructuredError/mapApiError</name>
  <files>src/utils/errors.ts</files>
  <read_first>
    - src/utils/errors.ts (current CoolifyErrorCode union lines 3-18, RECOVERY_HINTS lines 38-101, statusToCode lines 107-121, mapApiError lines 146-202, toStructuredError lines 204-246)
    - src/utils/errors.test.ts (Wave 0 RED scaffolds — every it.fails is a GREEN target)
    - src/utils/redact.ts (redactSecrets + sanitizeMessage already applied in mapApiError)
    - src/api/client.ts (withMappedErrors wraps ofetch errors via toStructuredError — no change needed here)
    - src/mcp/tools/instance.ts (inferInstanceType lines 222-232 — same hostname logic to mirror in isCloudUrl)
    - .planning/phases/16-coolify-cloud-server-branding/16-RESEARCH.md (Pattern: Dynamic Cloud Host Inference, Code Examples: Map Cloud API Error dynamically)
    - .planning/phases/16-coolify-cloud-server-branding/16-PATTERNS.md (Dynamic Cloud-specific Error Mapping pattern lines 104-134)
    - .planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md (D-02, D-03, D-04)
  </read_first>
  <behavior>
    - All Wave 0 it.fails blocks in errors.test.ts for cloud mapping flip to GREEN (or convert to regular it)
    - CoolifyErrorCode union includes 'COOLIFY_CLOUD_FORBIDDEN' | 'COOLIFY_CLOUD_UNSUPPORTED'
    - RECOVERY_HINTS.COOLIFY_CLOUD_FORBIDDEN is a string[] with ≥1 hint matching /team-scoped token/i
    - RECOVERY_HINTS.COOLIFY_CLOUD_UNSUPPORTED is a string[] with ≥1 hint matching /Endpoint not supported|not available on Coolify Cloud/i
    - isCloudUrl('https://app.coolify.io/api/v1/servers') === true
    - isCloudUrl('https://coolify.io/') === true
    - isCloudUrl('https://coolify.example.com/') === false
    - isCloudUrl('not-a-url') === false (no throw)
    - toStructuredError of cloud 403 → code COOLIFY_CLOUD_FORBIDDEN, httpStatus 403, recoveryHints from RECOVERY_HINTS.COOLIFY_CLOUD_FORBIDDEN
    - toStructuredError of cloud 404 → code COOLIFY_CLOUD_UNSUPPORTED, httpStatus 404, recoveryHints from RECOVERY_HINTS.COOLIFY_CLOUD_UNSUPPORTED
    - toStructuredError of self-hosted 403 → code stays on existing statusToCode path (COOLIFY_500 default for 403, since 403 not in statusToCode — preserve current behavior, do NOT add 403→COOLIFY_401 mapping in this plan)
    - toStructuredError of self-hosted 404 → code COOLIFY_404 (unchanged)
    - Cloud and non-cloud messages pass through sanitizeMessage (redactSecrets) — no token leakage
    - Existing errors.test.ts cases (401/404/409/422/500/network/timeout) stay GREEN
  </behavior>
  <action>
    Extend the CoolifyErrorCode union in src/utils/errors.ts with two new literals: 'COOLIFY_CLOUD_FORBIDDEN' and 'COOLIFY_CLOUD_UNSUPPORTED'. Add two entries to RECOVERY_HINTS — COOLIFY_CLOUD_FORBIDDEN: ['Regenerate the team-scoped token in app.coolify.io under Keys & Tokens and ensure it has the required abilities.', 'Cloud tokens are team-scoped — verify the token belongs to the team that owns the target resource.'], COOLIFY_CLOUD_UNSUPPORTED: ['This endpoint is not available or behaves differently on Coolify Cloud — use the self-hosted alternative or the Cloud dashboard.', 'See docs/en/cloud.md for the list of known Cloud-unsupported endpoints.']. Add an exported helper `isCloudUrl(url: string): boolean` that mirrors inferInstanceType's hostname logic: try { const hostname = new URL(url).hostname.toLowerCase(); return hostname === 'coolify.io' || hostname.endsWith('.coolify.io'); } catch { return false; }. Add an optional 4th parameter `isCloud = false` to mapApiError. Inside mapApiError, before the existing httpStatus branch logic, add two cloud branches: if (isCloud && httpStatus === 403) return { code: 'COOLIFY_CLOUD_FORBIDDEN', message: sanitizeMessage(coolifyMessage?.trim() || 'Cloud API request forbidden. Check team-scoped token permissions.'), recoveryHints: RECOVERY_HINTS.COOLIFY_CLOUD_FORBIDDEN, httpStatus }; if (isCloud && httpStatus === 404) return { code: 'COOLIFY_CLOUD_UNSUPPORTED', message: sanitizeMessage(coolifyMessage?.trim() || 'Endpoint not supported or not available on Coolify Cloud.'), recoveryHints: RECOVERY_HINTS.COOLIFY_CLOUD_UNSUPPORTED, httpStatus };. In toStructuredError, extract the request URL from fetchError: add `request?: string` to the fetchError type, compute `const requestUrl = typeof fetchError.request === 'string' ? fetchError.request : undefined;` and `const isCloud = requestUrl ? isCloudUrl(requestUrl) : false;`. Forward isCloud into the mapApiError call inside the `typeof status === 'number'` branch: `const envelope = mapApiError(error, status, coolifyMessage, isCloud);`. The conflicts-augmentation block for 409 stays unchanged (cloud 409 still maps to COOLIFY_409 with conflicts hint — out of scope for cloud codes). Do NOT add 403 to statusToCode for the non-cloud path — preserve existing behavior where 403 falls through to COOLIFY_500 default; cloud 403 is intercepted by the new isCloud branch before statusToCode runs. Do NOT touch wrapMcpError — it already redacts envelope.message and recoveryHints via redactSecrets. Do NOT modify src/api/client.ts — withMappedErrors already calls toStructuredError(error) and the ofetch FetchError carries `.request` as the request URL string. Run errors.test.ts after to confirm Wave 0 scaffolds flip GREEN and existing cases stay GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/utils/errors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All Wave 0 cloud-mapping it.fails blocks in errors.test.ts flip to GREEN (or are converted to regular it and pass)
    - [ ] CoolifyErrorCode union includes 'COOLIFY_CLOUD_FORBIDDEN' and 'COOLIFY_CLOUD_UNSUPPORTED'
    - [ ] RECOVERY_HINTS has matching entries with hint strings per D-04
    - [ ] isCloudUrl exported and correctly classifies app.coolify.io / coolify.io / coolify.example.com / invalid URL
    - [ ] toStructuredError of cloud 403 → COOLIFY_CLOUD_FORBIDDEN; cloud 404 → COOLIFY_CLOUD_UNSUPPORTED
    - [ ] Self-hosted 403/404 stay on existing path (no cloud code on non-coolify.io hostnames)
    - [ ] Existing errors.test.ts cases (401/404/409/422/500/network/timeout) stay GREEN — no regression
    - [ ] `npm run build` (tsup) GREEN
  </acceptance_criteria>
  <done>errors.test.ts all cloud-mapping scaffolds GREEN; CoolifyErrorCode union + RECOVERY_HINTS extended with two cloud codes; isCloudUrl exported; toStructuredError routes cloud-host 403/404 to cloud codes via request URL inspection; self-hosted path unchanged; existing tests GREEN; tsup build GREEN.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coolify API (cloud) → error pipeline | ofetch FetchError crosses this boundary carrying request URL + response body; cloud detection must rely on hostname only, never body |
| error pipeline → MCP client | Envelope.message and recoveryHints cross this boundary; must be redacted of tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Status | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------|-------------|-----------------|
| T-16-01-01 | Information Disclosure | cloud error envelopes echoing request URL or response body containing token | high | mitigated | mitigate | sanitizeMessage (redactSecrets) applied to coolifyMessage before assembling cloud envelope; wrapMcpError re-redacts envelope.message and recoveryHints at MCP boundary (existing behavior) |
| T-16-01-02 | Spoofing | self-hosted response body crafted to look "cloud-like" triggering cloud code on self-hosted | high | mitigated | mitigate | isCloud derived strictly from request URL hostname (coolify.io / *.coolify.io) — never from response body (D-03, RESEARCH Anti-Pattern) |
| T-16-01-03 | Tampering | global mutable isCloud state bleeding across concurrent multi-instance requests | high | mitigated | mitigate | isCloud computed per-error inside toStructuredError from fetchError.request — no module-level mutable flag (Pitfall 1 / Don't Hand-Roll) |
| T-16-01-04 | Information Disclosure | RECOVERY_HINTS strings embedding URLs that leak internal hostnames | low | mitigated | accept | Hints reference only public docs (docs/en/cloud.md) and public Cloud hostname app.coolify.io — no internal hostnames |
| T-16-01-SC | Tampering | npm installs (none new) | high | mitigated | accept | No new external packages; only existing zod + node:fs already in tree |
</threat_model>

<verification>
- `npx vitest run src/utils/errors.test.ts` — GREEN (Wave 0 scaffolds flipped + existing cases)
- `npm run build` (tsup) — GREEN
- `npm test` — no regressions vs current suite
- Manual: confirm `isCloudUrl('https://app.coolify.io/api/v1/version')` returns true and `isCloudUrl('https://coolify.example.com/')` returns false via a one-off node -e check (optional)
</verification>

<success_criteria>
- Two cloud error codes + recovery hints in errors.ts per D-02/D-04
- isCloudUrl helper exported, mirrors inferInstanceType hostname logic per D-03
- toStructuredError inspects fetchError.request URL and routes cloud 403/404 to cloud codes
- Self-hosted path unchanged (no cloud code on non-coolify.io hostnames) per D-03
- Token redaction preserved on cloud path (sanitizeMessage + wrapMcpError)
- No global mutable cloud state (Pitfall 1)
- errors.test.ts fully GREEN
- No existing test regresses
</success_criteria>

<output>
Create `.planning/phases/16-coolify-cloud-server-branding/16-01-SUMMARY.md` when done
</output>
