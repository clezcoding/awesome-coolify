---
phase: 16-coolify-cloud-server-branding
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/server.test.ts
  - src/utils/errors.test.ts
  - src/mcp/tools/instance.test.ts
autonomous: true
requirements: [CLD-01, CLD-02, BRND-01, BRND-03]
must_haves:
  truths:
    - "server.test.ts has RED scaffolds asserting McpServer construction exposes title 'Awesome Coolify', description matching package.json, websiteUrl https://github.com/clezcoding/awesome-coolify, and an icons array with a jsDelivr PNG entry — fails until Plan 16-03 wires them (BRND-01/BRND-03)"
    - "errors.test.ts has RED scaffolds asserting 403 on a *.coolify.io request URL maps to COOLIFY_CLOUD_FORBIDDEN and 404 maps to COOLIFY_CLOUD_UNSUPPORTED — fails until Plan 16-01 wires cloud hostname detection (CLD-02)"
    - "errors.test.ts has RED scaffolds asserting a self-hosted hostname (e.g. coolify.example.com) 403 stays COOLIFY_401/COOLIFY_422 path — no cloud code on self-hosted (D-03)"
    - "instance.test.ts has RED scaffolds asserting handleInstanceAction({action:'cloud-info'}) returns isCloud/url/source/setupHints/knownLimits/docsLink — fails until Plan 16-02 wires the cloud-info branch (CLD-01, D-16/D-17)"
    - "instance.test.ts has RED scaffolds for cloud-info source paths: registry (named instance), env (COOLIFY_URL+COOLIFY_TOKEN both set), infer (no creds → fallback https://app.coolify.io) — D-16 source contract"
    - "All RED scaffolds use it.fails or fail at assertion so husky pre-commit stays green — mirrors Phase 10/11/12/15 Wave 0 pattern"
  artifacts:
    - src/mcp/server.test.ts (appended RED scaffolds)
    - src/utils/errors.test.ts (appended RED scaffolds)
    - src/mcp/tools/instance.test.ts (appended RED scaffolds)
  key_links:
    - "server.test.ts → src/mcp/server.ts (constructor metadata assertions)"
    - "errors.test.ts → src/utils/errors.ts (cloud hostname mapping assertions)"
    - "instance.test.ts → src/mcp/tools/instance.ts (cloud-info action assertions)"
  prohibitions:
    - "No GREEN implementations in Wave 0 — only failing scaffolds"
    - "No production code modified — only test files"
    - "No live API calls — cloud-info is local/static per D-16"
---

<objective>
Wave 0 RED scaffold plan for Phase 16. Append failing test cases to three existing test files pinning the contract for: MCP server branding metadata (BRND-01/BRND-03), cloud hostname error mapping (CLD-02), and the instance.cloud-info action (CLD-01/D-16/D-17). Tests fail via it.fails or assertion so husky pre-commit stays green; Plans 16-01..16-03 flip them GREEN without rewriting tests.

Purpose: Lock the behavioral contract before implementation so Plans 16-01..16-04 execute TDD RED→GREEN per task without re-authoring tests.
Output: Three test files with appended failing scaffolds covering CLD-01, CLD-02, BRND-01, BRND-03.
</objective>

<artifacts_produced>
Test files (RED scaffolds, no production code):
- `src/mcp/server.test.ts` — appended RED scaffolds for McpServer branding metadata (BRND-01/BRND-03)
- `src/utils/errors.test.ts` — appended RED scaffolds for cloud hostname 403/404 mapping (CLD-02, D-03)
- `src/mcp/tools/instance.test.ts` — appended RED scaffolds for instance.cloud-info action (CLD-01, D-16/D-17)
</artifacts_produced>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md
@.planning/phases/16-coolify-cloud-server-branding/16-RESEARCH.md
@.planning/phases/16-coolify-cloud-server-branding/16-PATTERNS.md
@.planning/phases/16-coolify-cloud-server-branding/16-VALIDATION.md
@src/mcp/server.ts
@src/utils/errors.ts
@src/mcp/tools/instance.ts
@src/mcp/server.test.ts
@src/utils/errors.test.ts
@src/mcp/tools/instance.test.ts
@package.json
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED scaffold server.test.ts branding + errors.test.ts cloud mapping + instance.test.ts cloud-info</name>
  <files>src/mcp/server.test.ts, src/utils/errors.test.ts, src/mcp/tools/instance.test.ts</files>
  <read_first>
    - src/mcp/server.test.ts (existing tool registration test pattern — readFileSync + string match)
    - src/mcp/server.ts (current McpServer constructor at line 584 — `new McpServer({ name: 'awesome-coolify-mcp', version: '0.1.0' })`)
    - src/utils/errors.test.ts (existing mapApiError/toStructuredError test pattern)
    - src/utils/errors.ts (current CoolifyErrorCode union, RECOVERY_HINTS, mapApiError, toStructuredError)
    - src/mcp/tools/instance.test.ts (existing handleInstanceAction test pattern, COOLIFY_MCP_TEST_REGISTRY_DIR injection)
    - src/mcp/tools/instance.ts (existing inferInstanceType, instanceActionSchema discriminatedUnion, handleInstanceAction switch)
    - .planning/phases/16-coolify-cloud-server-branding/16-RESEARCH.md (Pattern 1 McpServer branding, Dynamic Hostname Cloud Detection, Static Discovery Action)
    - .planning/phases/16-coolify-cloud-server-branding/16-PATTERNS.md (cloud-info action schema, cloud error mapping tests)
    - .planning/phases/16-coolify-cloud-server-branding/16-CONTEXT.md (D-02, D-03, D-08, D-09, D-16, D-17)
    - package.json (description string for BRND-03 assertion)
  </read_first>
  <behavior>
    server.test.ts (append new describe('McpServer branding metadata', ...)):
    - it.fails: source of src/mcp/server.ts contains `title: 'Awesome Coolify'` literal — currently false (no title field yet)
    - it.fails: source contains `websiteUrl: 'https://github.com/clezcoding/awesome-coolify'` literal — currently false
    - it.fails: source contains `description:` key in McpServer constructor block (between `new McpServer({` and the matching `})`) — currently false
    - it.fails: source contains `icons:` key in McpServer constructor block and a jsDelivr URL literal `https://cdn.jsdelivr.net/gh/clezcoding/awesome-coolify@main/docs/assets/mcp-icon-192.png` — currently false
    - it.fails: source contains `mimeType: 'image/png'` and `sizes: ['192x192']` — currently false
    - it.fails: package.json description appears verbatim in server.ts source (BRND-03 package-aligned description) — currently false
    errors.test.ts (append new describe('Cloud hostname error mapping', ...)):
    - it.fails: toStructuredError({ request: 'https://app.coolify.io/api/v1/servers', response: { status: 403, _data: { message: 'Forbidden' } } }).code === 'COOLIFY_CLOUD_FORBIDDEN' — currently returns COOLIFY_500 (403 not in statusToCode, falls through to default COOLIFY_500 — verified in src/utils/errors.ts lines 107-121)
    - it.fails: toStructuredError of cloud 403 envelope has recoveryHints[0] matching /team-scoped token/i
    - it.fails: toStructuredError({ request: 'https://app.coolify.io/api/v1/unsupported', response: { status: 404, _data: { message: 'Not found' } } }).code === 'COOLIFY_CLOUD_UNSUPPORTED' — currently returns COOLIFY_404
    - it.fails: toStructuredError of cloud 404 envelope has recoveryHints[0] matching /Endpoint not supported|not available on Coolify Cloud/i
    - it (GREEN already): toStructuredError({ request: 'https://coolify.example.com/api/v1/servers', response: { status: 403 } }).code does NOT start with 'COOLIFY_CLOUD_' — self-hosted stays on existing path (D-03)
    - it.fails: RECOVERY_HINTS.COOLIFY_CLOUD_FORBIDDEN and RECOVERY_HINTS.COOLIFY_CLOUD_UNSUPPORTED both exist as arrays of length ≥ 1 — currently undefined
    - it.fails: CoolifyErrorCode union includes 'COOLIFY_CLOUD_FORBIDDEN' and 'COOLIFY_CLOUD_UNSUPPORTED' literals — currently not present (compile-time check via assignable type assertion)
    instance.test.ts (append new describe('instance cloud-info action', ...)):
    - it.fails: handleInstanceAction({ action: 'cloud-info' }, testEnv) returns result with data.isCloud === false and data.url === testEnv.COOLIFY_URL and data.source === 'env' (env both set per D-16/D-17)
    - it.fails: after handleInstanceAction({ action: 'add', name: 'my-cloud', url: 'https://app.coolify.io', token: 't', type: 'cloud' }, testEnv), handleInstanceAction({ action: 'cloud-info', instance: 'my-cloud' }, testEnv) returns data.isCloud === true, data.url === 'https://app.coolify.io', data.source === 'registry'
    - it.fails: with no registry, no env (delete process.env.COOLIFY_URL/COOLIFY_TOKEN), handleInstanceAction({ action: 'cloud-info' }, testEnv) returns data.isCloud === true, data.url === 'https://app.coolify.io', data.source === 'infer' (fallback default per D-16)
    - it.fails: cloud-info response data has setupHints array length ≥ 1, knownLimits array length ≥ 1, docsLink string containing 'docs/' (D-16 contract)
    - it.fails: cloud-info with unknown instance name returns COOLIFY_INSTANCE_NOT_FOUND envelope (reuses resolveCredentials path — D-17 routing)
    - it.fails: instanceActionSchema.safeParse({ action: 'cloud-info' }).success === true (schema accepts new branch)
    - it.fails: instanceActionSchema.safeParse({ action: 'cloud-info', instance: 'bad-name-with-uppercase-A' }).success === false (instance param still validated against name regex when present)
  </behavior>
  <action>
    Append three new describe blocks to the existing test files. Use `it.fails` for every assertion that depends on not-yet-implemented production code so husky pre-commit stays green (mirrors Phase 10/11/12/15 Wave 0 pattern). For server.test.ts: add a describe('McpServer branding metadata') block that reads src/mcp/server.ts source via readFileSync and asserts the literal strings/keys listed in behavior; use it.fails because none of the literals exist yet. For errors.test.ts: add a describe('Cloud hostname error mapping') block that constructs ofetch-shaped FetchError objects with a `request` string URL and a `response.{status,_data}` shape, passes them through toStructuredError, and asserts the cloud code + recoveryHints; use it.fails for cloud-code assertions and a regular it for the self-hosted negative case (which already passes today). For CoolifyErrorCode union membership, add a type-level assertion `const _c: CoolifyErrorCode = 'COOLIFY_CLOUD_FORBIDDEN'` inside an it.fails callback (runtime throws because the literal is not in the union — TS compiles but the runtime check via `RECOVERY_HINTS[code]` undefined). For instance.test.ts: add a describe('instance cloud-info action') block that uses the existing COOLIFY_MCP_TEST_REGISTRY_DIR tmpdir pattern and the existing loadInstanceTool() dynamic import; for the env-source case pass testEnv, for the registry-source case add a cloud instance first, for the infer-source case delete process.env.COOLIFY_URL/COOLIFY_TOKEN and pass an env without COOLIFY_URL/COOLIFY_TOKEN (restore in afterEach). All cloud-info assertions use it.fails because the action branch does not exist yet in instanceActionSchema or handleInstanceAction. Do not modify any production code (server.ts, errors.ts, instance.ts) in this plan — scaffolds only. Do not modify the existing test cases — only append new describe blocks. Run `npm run build` (tsup) after to confirm test-file changes do not break the build graph (test files are excluded from tsup).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/server.test.ts src/utils/errors.test.ts src/mcp/tools/instance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] src/mcp/server.test.ts has new describe('McpServer branding metadata') with ≥6 it.fails blocks covering BRND-01/BRND-03
    - [ ] src/utils/errors.test.ts has new describe('Cloud hostname error mapping') with ≥5 it.fails blocks + ≥1 it (self-hosted negative) covering CLD-02/D-03
    - [ ] src/mcp/tools/instance.test.ts has new describe('instance cloud-info action') with ≥6 it.fails blocks covering CLD-01/D-16/D-17
    - [ ] All new it.fails blocks fail (RED) — confirmed by vitest output showing expected failure
    - [ ] Existing test cases in all three files still pass (no regression)
    - [ ] husky pre-commit stays green (it.fails prevents test-only RED commits from blocking)
    - [ ] `npm run build` (tsup) stays green — test files excluded from build graph
  </acceptance_criteria>
  <done>Three test files appended with RED scaffolds: server.test.ts ≥6 it.fails for branding metadata, errors.test.ts ≥5 it.fails + ≥1 it for cloud mapping, instance.test.ts ≥6 it.fails for cloud-info; all new scaffolds RED via it.fails; existing tests unchanged; husky pre-commit green; tsup build green.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → tmpdir | Scaffold tests continue to use os.tmpdir() registries via COOLIFY_MCP_TEST_REGISTRY_DIR; cleanup mandatory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Status | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------|-------------|-----------------|
| T-16-00-SC | Tampering | npm/dev installs (none new) | high | mitigated | accept | No new packages in Wave 0; vitest already pinned in package.json |
| T-16-00-01 | Information Disclosure | test tmp registries containing fake cloud tokens | low | mitigated | mitigate | Existing afterEach cleanup unlinks tmp registry dir; tests use throwaway tokens only |
| T-16-00-02 | Tampering | test scaffolds echo real package.json description into server.ts assertions | low | mitigated | accept | Description is public metadata, not secret; assertions compare strings only |
</threat_model>

<verification>
- `npx vitest run src/mcp/server.test.ts src/utils/errors.test.ts src/mcp/tools/instance.test.ts` — all new it.fails blocks RED (expected failure), existing tests GREEN
- `npm run build` (tsup) — GREEN (test files excluded from build graph)
- `git status` shows exactly 3 modified test files
</verification>

<success_criteria>
- 3 test files updated with appended RED scaffolds, all new blocks RED via it.fails
- BRND-01/BRND-03 contract pinned (title/description/websiteUrl/icons literals in server.ts)
- CLD-02 contract pinned (cloud hostname 403→COOLIFY_CLOUD_FORBIDDEN, 404→COOLIFY_CLOUD_UNSUPPORTED, self-hosted unaffected)
- CLD-01/D-16/D-17 contract pinned (cloud-info action: isCloud/url/source/setupHints/knownLimits/docsLink, three source paths)
- No production code modified in Wave 0
- husky pre-commit stays green
</success_criteria>

<output>
Create `.planning/phases/16-coolify-cloud-server-branding/16-00-SUMMARY.md` when done
</output>
