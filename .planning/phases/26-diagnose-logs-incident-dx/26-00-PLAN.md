---
phase: 26-diagnose-logs-incident-dx
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/tools/diagnose.test.ts
  - src/utils/log-helpers.test.ts
  - tests/mcp/prompts.test.ts
  - src/mcp/tools/system.test.ts
  - docs/coverage-map.yaml
autonomous: true
requirements:
  - DIAG-01
user_setup: []

must_haves:
  truths:
    - "diagnose.test.ts describe('diagnose logs') has it.fails scaffolds for mode:full nested diagnose+logs, mode:logs-only omits diagnose, deployment_uuid XOR, soft partial diagnose_failed+logs, empty runtime hint, defaults lines=100 max_chars=20000 (DIAG-01, D-03–D-09)"
    - "diagnose.test.ts schema it.fails reject logs without identifier and reject deployment_uuid+runtime id combo (DIAG-01, D-04, D-05, T-26-01)"
    - "log-helpers.test.ts has it.fails scaffold for buildRuntimeLogPayload slice/cap parity (OBS-03 guard before extraction)"
    - "prompts.test.ts has it.fails incident assertions for diagnose.logs, deployment.logs, follow, application_logs_follow, app-only guardrail (PROMPT-01, D-10–D-13)"
    - "system.test.ts has it.fails scaffold expecting six capability keys including diagnose_logs (D-14)"
    - "docs/coverage-map.yaml has diagnose.logs row with client+openapi paths (D-17)"
    - "All new RED cases use vitest it.fails so husky pre-commit stays green"
  artifacts:
    - path: src/mcp/tools/diagnose.test.ts
      provides: "describe('diagnose logs') it.fails block"
      contains: "action: 'logs'"
    - path: tests/mcp/prompts.test.ts
      provides: "incident prompt PROMPT-01 RED targets"
      contains: "diagnose.logs"
    - path: docs/coverage-map.yaml
      provides: "diagnose.logs coverage row"
      contains: "diagnose.logs"
  key_links:
    - from: "diagnose.test.ts logs describe"
      to: "handleDiagnoseLogs (Plan 26-01)"
      via: "handleDiagnoseAction mocked fetchApplicationLogs"
    - from: "log-helpers.test.ts"
      to: "buildRuntimeLogPayload (Plan 26-01)"
      via: "dynamic import"
  prohibitions:
    - statement: "Do not implement production handlers or buildRuntimeLogPayload in Wave 0 — scaffolds only"
      status: enforced
      verification: "git diff --name-only -- src/mcp/tools/diagnose.ts src/utils/log-helpers.ts src/mcp/capabilities.ts is empty after this plan"
    - statement: "Do not edit incident prompt body (Plan 26-02)"
      status: enforced
      verification: "git diff --name-only -- src/mcp/prompts.ts is empty after this plan"
    - statement: "Do not embed follow:true in diagnose.logs scaffolds (D-02)"
      status: enforced
      verification: "rg -n \"follow\" src/mcp/tools/diagnose.test.ts | grep -v '^#' | wc -l is 0 or only negative rejection cases"
---

<objective>
Wave 0 Nyquist RED scaffolds for Phase 26 — lock DIAG-01/PROMPT-01/D-14 acceptance as `it.fails` tests before implementation.

Purpose: Plans 26-01..26-03 flip scaffolds GREEN without rewriting intent. Mirrors Phase 24/25 Wave 0 pattern.

Output: diagnose, log-helpers, prompts, system test RED targets; coverage-map row; production handlers untouched.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-diagnose-logs-incident-dx/26-CONTEXT.md
@.planning/phases/26-diagnose-logs-incident-dx/26-RESEARCH.md
@.planning/phases/26-diagnose-logs-incident-dx/26-PATTERNS.md
@.planning/phases/26-diagnose-logs-incident-dx/26-VALIDATION.md
@src/mcp/tools/diagnose.test.ts
@src/mcp/tools/deployment.test.ts
@tests/mcp/prompts.test.ts
@src/mcp/tools/system.test.ts
@docs/coverage-map.yaml
</context>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | diagnose.logs one-shot shortcut | 26-01 |
| GOAL | incident prompt + coolify-setup guidance | 26-02, 26-03 |
| REQ DIAG-01 | composite diagnose+log action | 26-00 (RED), 26-01 (GREEN) |
| REQ PROMPT-01 | incident prompt docs | 26-00 (RED), 26-02 (GREEN) |
| REQ SKILL-01 | coolify-setup troubleshooting | 26-03 |
| D-01..D-09 | diagnose.logs surface + envelope | 26-00, 26-01 |
| D-10..D-13 | incident prompt | 26-00, 26-02 |
| D-14..D-17 | capability + docs | 26-00, 26-02, 26-03 |
| Deferred | service/DB logs, embedded follow | excluded |

## Artifacts this phase produces

- `src/mcp/tools/diagnose.test.ts` — `describe('diagnose logs')` `it.fails` block (schema + handler).
- `src/utils/log-helpers.test.ts` — `buildRuntimeLogPayload` `it.fails` scaffold.
- `tests/mcp/prompts.test.ts` — incident PROMPT-01 `it.fails` assertions.
- `src/mcp/tools/system.test.ts` — six-key capabilities `it.fails` scaffold.
- `docs/coverage-map.yaml` — `diagnose.logs` row (no COVERAGE.md regen here).

<tasks>

<task type="tdd">
  <name>Task 1: RED scaffolds for diagnose.logs schema + handler tests</name>
  <files>src/mcp/tools/diagnose.test.ts, src/utils/log-helpers.test.ts</files>
  <read_first>
    - .planning/phases/26-diagnose-logs-incident-dx/26-CONTEXT.md (D-01–D-09)
    - .planning/phases/26-diagnose-logs-incident-dx/26-PATTERNS.md (diagnose.test.ts, log-helpers analog)
    - .planning/phases/26-diagnose-logs-incident-dx/26-VALIDATION.md (Wave 0 checklist)
    - src/mcp/tools/deployment.test.ts (describe deployment logs lines 615–754)
    - src/mcp/tools/diagnose.test.ts (existing diagnoseToolSchema describe)
    - src/mcp/tools/application.ts (runtime slice/cap lines 1663–1679 — extraction target)
  </read_first>
  <behavior>
    - diagnoseToolSchema parses action logs with uuid; defaults mode full, lines 100, max_chars 20000
    - schema rejects logs without query|uuid|name|domain
    - schema rejects deployment_uuid combined with runtime identifiers (XOR)
    - handleDiagnoseAction logs mode full returns nested diagnose and logs without top-level isError
    - mode logs-only omits diagnose key
    - deployment_uuid fetches build logs only via processDeploymentBuildLogs path
    - diagnose half failure populates diagnose_failed while logs still present
    - empty runtime logs returns soft OK with hint on logs object
  </behavior>
  <action>
    Extend vi.mock for ../../api/client.js with fetchApplicationLogs and fetchDeployment if missing. Add describe('diagnose logs') with at least eight it.fails cases mirroring deployment.test.ts empty-logs soft OK pattern: schema defaults; schema rejects missing identifier; schema rejects XOR; mode full happy path with mocked fetchApplication/fetchApplicationEnvs/fetchAppDeployments/fetchApplicationLogs; mode logs-only omits diagnose; deployment_uuid build-only path; diagnose_failed soft partial when fetchApplication rejects; empty runtime hint when logs string empty. Add describe('buildRuntimeLogPayload') in log-helpers.test.ts with one it.fails asserting slice/cap output shape { uuid, logs_lines, logs_truncated, total_lines } using dynamic import from ./log-helpers.js. Do not edit diagnose.ts or log-helpers.ts production code.
  </action>
  <verify>
    <automated>rg -n "it\\.fails" src/mcp/tools/diagnose.test.ts | wc -l | awk '{exit ($1>=8)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - diagnose.test.ts has at least eight it.fails in logs-related describes
    - log-helpers.test.ts has buildRuntimeLogPayload it.fails scaffold
    - No production changes under src/mcp/tools/diagnose.ts or src/utils/log-helpers.ts
    - Husky pre-commit passes (it.fails stays green)
  </acceptance_criteria>
  <done>DIAG-01 handler/schema RED targets locked; husky green via it.fails.</done>
</task>

<task type="tdd">
  <name>Task 2: RED scaffolds for incident prompt + capabilities + coverage map</name>
  <files>tests/mcp/prompts.test.ts, src/mcp/tools/system.test.ts, docs/coverage-map.yaml</files>
  <read_first>
    - .planning/phases/26-diagnose-logs-incident-dx/26-CONTEXT.md (D-10–D-14, D-17)
    - .planning/phases/26-diagnose-logs-incident-dx/26-PATTERNS.md (prompts.test.ts, system.test.ts, coverage-map)
    - tests/mcp/prompts.test.ts (incident test lines 104–116)
    - src/mcp/tools/system.test.ts (CAPABILITY_KEYS five-key block lines 139–156)
    - docs/coverage-map.yaml (deployment.logs and diagnose.app rows)
  </read_first>
  <behavior>
    - incident prompt content contains diagnose action logs with mode full
    - incident prompt cites deployment action logs, follow true, application_logs_follow capability, and app-only service/DB guardrail
    - system.version capabilities has exactly six keys including diagnose_logs
    - coverage-map.yaml lists diagnose.logs with fetchApplication through fetchDeployment client paths
  </behavior>
  <action>
    In prompts.test.ts add it.fails('incident prompt mentions diagnose.logs, deployment.logs, follow, and app-only guardrail') per 26-PATTERNS.md: call getRegisteredPrompts(server).incident.handler with uuid and project_uuid; assert assistant content contains diagnose({ action: "logs" and mode: "full"; contains deployment({ action: "logs"; matches follow:true; matches application_logs_follow; matches service|database|DB guardrail; still contains application({ action: "restart" and emergency({ action: "redeploy_project". In system.test.ts extend CAPABILITY_KEYS to six entries including diagnose_logs and convert the capabilities count test to it.fails expecting exactly six sorted keys. In docs/coverage-map.yaml add diagnose.logs row with client [fetchApplication, fetchApplicationEnvs, fetchAppDeployments, fetchApplicationLogs, fetchDeployment] and openapi GET paths per 26-PATTERNS.md. Do not edit prompts.ts, capabilities.ts, or regenerate COVERAGE.md.
  </action>
  <verify>
    <automated>rg -n "diagnose\\.logs|diagnose_logs" tests/mcp/prompts.test.ts src/mcp/tools/system.test.ts docs/coverage-map.yaml | wc -l | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - prompts.test.ts has it.fails incident assertions for PROMPT-01
    - system.test.ts has it.fails six-key capabilities scaffold for D-14
    - coverage-map.yaml contains diagnose.logs row
    - src/mcp/prompts.ts and src/mcp/capabilities.ts unchanged
  </acceptance_criteria>
  <done>PROMPT-01 and D-14 RED targets locked; coverage row present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → future handler | RED scaffolds encode expected contracts before production code exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-00-01 | Tampering | diagnose.test.ts scaffolds | low | accept | Wave 0 only; Plan 26-01 implements mitigations |
| T-26-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this phase; slopcheck N/A |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/diagnose.test.ts -t logs -x` — all it.fails (no unexpected failures)
- `npx vitest run tests/mcp/prompts.test.ts -t incident -x`
- `npx vitest run src/mcp/tools/system.test.ts -t capabilities -x`
- `git diff --name-only -- src/mcp/tools/diagnose.ts src/mcp/capabilities.ts src/mcp/prompts.ts` is empty
</verification>

<success_criteria>
All Wave 0 VALIDATION checklist items present as it.fails scaffolds; production handlers untouched; nyquist_compliant ready for 26-01.
</success_criteria>

<output>
Create `.planning/phases/26-diagnose-logs-incident-dx/26-00-SUMMARY.md` when done
</output>
