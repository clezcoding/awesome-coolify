---
phase: 26-diagnose-logs-incident-dx
plan: 01
type: execute
wave: 1
depends_on:
  - "26-00"
files_modified:
  - src/utils/log-helpers.ts
  - src/mcp/tools/application.ts
  - src/mcp/tools/diagnose.ts
  - src/mcp/tools/diagnose.test.ts
autonomous: true
requirements:
  - DIAG-01
user_setup: []

must_haves:
  truths:
    - "Agent calls diagnose action logs with uuid and receives nested diagnose triage plus bounded logs_lines in one buildReadResponse (DIAG-01, D-01, D-02, D-03, D-06)"
    - "mode logs-only omits diagnose key; deployment_uuid returns build logs only with no runtime tail (D-03, D-05)"
    - "mode full with diagnose half failure returns diagnose_failed sibling and logs when fetch succeeds (D-07)"
    - "Empty runtime logs return soft OK with hint on logs object (D-08)"
    - "lines default 100 and max_chars default 20000 match application.logs (D-09, OBS-03)"
    - "application.logs runtime one-shot uses buildRuntimeLogPayload — no duplicated slice/cap logic (OBS-03)"
  artifacts:
    - path: src/utils/log-helpers.ts
      provides: "buildRuntimeLogPayload + EMPTY_RUNTIME_LOGS_HINT"
      exports: ["buildRuntimeLogPayload", "EMPTY_RUNTIME_LOGS_HINT"]
    - path: src/mcp/tools/diagnose.ts
      provides: "logs action schema + handleDiagnoseLogs"
      contains: "handleDiagnoseLogs"
    - path: src/mcp/tools/application.ts
      provides: "runtime branch calls buildRuntimeLogPayload"
      contains: "buildRuntimeLogPayload"
  key_links:
    - from: "handleDiagnoseLogs"
      to: "runDiagnoseAppCore / projectAppDiagnose"
      via: "mode full triage half"
    - from: "handleDiagnoseLogs"
      to: "fetchApplicationLogs + buildRuntimeLogPayload"
      via: "runtime tail path"
    - from: "handleDiagnoseLogs"
      to: "processDeploymentBuildLogs"
      via: "deployment_uuid build path"
    - from: "handleApplicationLogs"
      to: "buildRuntimeLogPayload"
      via: "shared OBS-03 helper"
  prohibitions:
    - statement: "Do not embed follow:true in diagnose.logs (D-02)"
      status: enforced
      verification: "rg -n \"follow\" src/mcp/tools/diagnose.ts | grep -v '^#' | grep -v reject | wc -l is 0"
    - statement: "Do not call handleApplicationAction from diagnose.ts"
      status: enforced
      verification: "rg -n \"handleApplicationAction\" src/mcp/tools/diagnose.ts returns 0"
    - statement: "Do not hard-fail whole action when diagnose half fails but logs succeed (D-07)"
      status: enforced
      verification: "diagnose.test.ts soft partial it passes without isDiagnoseErrorResult"
---

<objective>
Tracer vertical slice: `diagnose.logs` end-to-end — schema, composite handler, shared log helper extraction, and Wave 0 tests GREEN for mode:full runtime happy path plus remaining DIAG-01 edge cases.

Purpose: Prove composite diagnose+log architecture before prompt/capability/docs work in Plans 26-02/26-03.

Output: `buildRuntimeLogPayload`, `handleDiagnoseLogs`, updated `diagnoseActionsCatalog`, green diagnose.logs tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-diagnose-logs-incident-dx/26-CONTEXT.md
@.planning/phases/26-diagnose-logs-incident-dx/26-RESEARCH.md
@.planning/phases/26-diagnose-logs-incident-dx/26-PATTERNS.md
@.planning/phases/26-diagnose-logs-incident-dx/26-00-SUMMARY.md
@src/mcp/tools/diagnose.ts
@src/mcp/tools/application.ts
@src/mcp/tools/deployment.ts
@src/utils/log-helpers.ts
@src/mcp/tools/shared-read-params.ts
</context>

## Artifacts this phase produces

- `src/utils/log-helpers.ts` — `buildRuntimeLogPayload`, `EMPTY_RUNTIME_LOGS_HINT`.
- `src/mcp/tools/application.ts` — runtime branch refactored to shared helper (OBS-03).
- `src/mcp/tools/diagnose.ts` — `logs` action on schema/catalog, `handleDiagnoseLogs`, switch case.
- Green `diagnose.test.ts` and `log-helpers.test.ts` from Wave 0.

<tasks>

<task type="tracer">
  <name>End-to-end diagnose.logs mode:full runtime happy path</name>
  <reversibility rating="costly">D-01 adds logs action to diagnose catalog; D-03 mode default; D-06 envelope shape — agent habits change.</reversibility>
  <files>src/utils/log-helpers.ts, src/mcp/tools/application.ts, src/mcp/tools/diagnose.ts, src/mcp/tools/diagnose.test.ts</files>
  <read_first>
    - .planning/phases/26-diagnose-logs-incident-dx/26-CONTEXT.md (D-01–D-06, D-09)
    - .planning/phases/26-diagnose-logs-incident-dx/26-PATTERNS.md (diagnose.ts schema/handler, log-helpers extract)
    - src/mcp/tools/deployment.ts (deployment.logs schema pattern lines 169–210)
    - src/mcp/tools/diagnose.ts (resolveAppUuid, handleDiagnoseApp lines 289–375)
    - src/mcp/tools/application.ts (handleApplicationLogs runtime branch lines 1624–1679)
    - src/mcp/tools/shared-read-params.ts (sharedLogParamsSchema defaults)
    - src/mcp/tools/diagnose.test.ts (Wave 0 scaffolds)
  </read_first>
  <behavior>
    - diagnoseToolSchema includes action logs with mode enum default full and shared log param defaults lines 100 max_chars 20000
    - handleDiagnoseAction action logs mode full uuid returns data.diagnose and data.logs with logs_lines array
    - buildRuntimeLogPayload extracted; application.ts runtime path calls it with identical output to prior inline logic
    - diagnoseActionsCatalog documents logs(...) signature
    - Wave 0 mode full happy path it.fails flips to green it
  </behavior>
  <action>
    Per D-01: add logs to createFlatActionSchema action list and diagnoseActionsCatalog string. Per D-03/D-04/D-09: add mode enum optional default full, deployment_uuid optional, sharedLogParamsFlatShape fields, superRefine requiring at least one of query|uuid|name|domain for logs action, reject format table, reject follow params if passed. Per locked D-06/D-07: use nested { diagnose?, diagnose_failed?, logs } envelope inside successful buildReadResponse; diagnose_failed sibling (not _meta.diagnose_error) when diagnose half fails — no checkpoint needed, D-07 is locked soft partial. Extract buildRuntimeLogPayload and EMPTY_RUNTIME_LOGS_HINT into log-helpers.ts from application.ts runtime branch verbatim; refactor handleApplicationLogs to call helper. Implement handleDiagnoseLogs: resolveAppUuid, run diagnose.app fetch+project path when mode not logs-only (extract shared runDiagnoseAppCore from handleDiagnoseApp), fetch runtime logs via fetchApplicationLogs with lines+offset third arg matching application.logs, build payload via buildRuntimeLogPayload, return buildReadResponse({ diagnose, logs }). Wire case logs in handleDiagnoseAction switch. Flip Wave 0 mode full schema defaults and happy handler it.fails to it. Do not implement deployment_uuid, soft partial, or empty hint in this task — Task 3.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/diagnose.test.ts -t "mode:full|defaults mode full|parses logs" -x</automated>
  </verify>
  <acceptance_criteria>
    - diagnose({ action: logs, uuid }) returns nested diagnose and logs in one response
    - buildRuntimeLogPayload exists and application.ts uses it
    - diagnoseActionsCatalog mentions logs action
    - At least one former Wave 0 happy-path it.fails is green it
  </acceptance_criteria>
  <done>Single runtime mode:full path works end-to-end and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DIAG-01 edge cases — XOR, logs-only, soft partial, empty hint</name>
  <files>src/mcp/tools/diagnose.ts, src/mcp/tools/diagnose.test.ts, src/utils/log-helpers.test.ts</files>
  <read_first>
    - .planning/phases/26-diagnose-logs-incident-dx/26-CONTEXT.md (D-05, D-07, D-08)
    - .planning/phases/26-diagnose-logs-incident-dx/26-PATTERNS.md (XOR refine, soft partial test)
    - src/mcp/tools/application.ts (deployment_uuid build path lines 1629–1652)
    - src/mcp/tools/deployment.test.ts (empty build logs soft OK lines 736–754)
    - src/utils/log-helpers.ts (processDeploymentBuildLogs)
  </read_first>
  <behavior>
    - mode logs-only response has logs but no diagnose key
    - deployment_uuid fetches build logs via processDeploymentBuildLogs; no runtime fetchApplicationLogs call
    - schema rejects uuid plus deployment_uuid together
    - fetchApplication rejection with successful logs still returns diagnose_failed and logs_lines length greater than 0
    - empty runtime logs string attaches EMPTY_RUNTIME_LOGS_HINT on logs object
    - buildRuntimeLogPayload unit test passes slice/cap semantics
  </behavior>
  <action>
  Per D-05: when deployment_uuid set, fetch deployment and processDeploymentBuildLogs only — skip runtime tail; superRefine XOR rejects combined runtime ids and deployment_uuid. Per D-03: when mode logs-only skip diagnose fetch entirely. Per D-07: catch diagnose half errors, map CoolifyApiError to diagnose_failed { code, message }, still fetch logs; outer wrapMcpError only for total failures (e.g. logs fetch throws). Per D-08: when runtime logs_lines empty attach EMPTY_RUNTIME_LOGS_HINT. Flip all remaining Wave 0 diagnose logs it.fails to it. Flip log-helpers buildRuntimeLogPayload it.fails to it. Run application.test.ts runtime logs regression to confirm OBS-03.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/diagnose.test.ts -t logs -x && npx vitest run src/mcp/tools/application.test.ts -t "runtime logs" -x</automated>
  </verify>
  <acceptance_criteria>
    - All describe diagnose logs tests green
    - XOR schema test green
    - Soft partial and empty hint tests green
    - application.test.ts runtime logs golden tests still green (OBS-03)
  </acceptance_criteria>
  <done>DIAG-01 fully green; OBS-03 regression clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → diagnose.logs | Untrusted params cross Zod schema + superRefine |
| MCP → Coolify API | Read-only log/diagnose fetches; token in env |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Spoofing | parseWithInstanceRouting | medium | mitigate | Reuse resolveRoutingEnv in handleDiagnoseAction wrapper |
| T-26-02 | Denial of Service | log response size | medium | mitigate | Reuse max_chars 20000 and lines 100 defaults via sharedLogParamsSchema (D-09) |
| T-26-03 | Information Disclosure | build logs | medium | mitigate | processDeploymentBuildLogs sensitive gate unchanged |
| T-26-04 | Tampering | deployment_uuid XOR | medium | mitigate | superRefine rejects ambiguous runtime+build id combo (D-05) |
| T-26-SC | Tampering | npm installs | high | mitigate | No new packages; slopcheck N/A |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/diagnose.test.ts -t logs -x`
- `npx vitest run src/mcp/tools/application.test.ts -t "runtime logs" -x`
- `npx vitest run src/utils/log-helpers.test.ts -x`
- `graphify update .` after code edits (per graphify.mdc)
</verification>

<success_criteria>
DIAG-01 satisfied: diagnose.logs composes triage + bounded tail with mode, XOR, soft partial, empty hint; OBS-03 preserved via shared helper; knowledge graph updated via graphify update.
</success_criteria>

<output>
Create `.planning/phases/26-diagnose-logs-incident-dx/26-01-SUMMARY.md` when done
</output>
