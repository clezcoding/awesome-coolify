---
phase: 20-recipes-service-list-types
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - src/mcp/tools/recipe.ts
  - src/mcp/tools/recipe.test.ts
  - src/mcp/server.ts
  - src/mcp/server.test.ts
  - README.md
autonomous: true
requirements:
  - RECIPE-03
user_setup: []

must_haves:
  truths:
    - "recipe create-app-db handler creates a database (engine-dispatched via create<Engine>Database), creates an application, reads internal_db_url from GET /databases/{uuid}, and wires the connection string into the application env under env_key (default DATABASE_URL) via bulkUpdateEnvs (D-13, D-14)"
    - "env_key defaults to DATABASE_URL; optional override respected (D-13)"
    - "Connection string is read from internal_db_url on the GET /databases/{uuid} response; if absent, a fallback URL is constructed engine-specifically from the create params (D-14)"
    - "Partial failure on application create after database create returns COOLIFY_RECIPE_PARTIAL_FAILURE with database_uuid in error.data and recoveryHints telling the user not to delete the database — no auto-rollback (D-15)"
    - "Partial failure on env wiring after both resources created returns COOLIFY_RECIPE_PARTIAL_FAILURE with both application_uuid and database_uuid in error.data — no auto-rollback (D-15)"
    - "connection_string in the success response is masked via sanitizeFullProjection unless reveal:true is passed (D-19, T-20-01)"
    - "instant_deploy defaults to true on create-app-db — both DB and app start immediately unless instant_deploy:false (D-16)"
    - "No confirm gate on create-app-db — create is intentional (D-17)"
    - "No dry-run / preview path on create-app-db (D-18)"
    - "recipe tool registered in src/mcp/server.ts via registerTool('recipe', ...) alongside existing domain tools, with openWorldHint: true and toolOutputSchema (D-08)"
    - "src/mcp/server.test.ts updated: registerTool count assertion bumped from 16 to 17; 'recipe' added to the routed schemas list (recipeActionSchema wrapped with withInstanceRoutingSchema); 'recipe' added to the expectedTools array in the 'every registered tool description contains Actions: and Safety: prefixes' test"
    - "README.md documents the recipe tool with all three actions, args, and the no-confirm / no-dry-run / no-auto-rollback posture (D-17, D-18, D-15)"
    - "recipe.test.ts create-app-db describe block flips from COOLIFY_NOT_IMPLEMENTED stub assertions to real-handler GREEN assertions covering success, env_key override, internal_db_url read, fallback URL construction, partial failure on app create, partial failure on env wiring, masking, instant_deploy default, no-confirm"
  artifacts:
    - "src/mcp/tools/recipe.ts (modified — replace create-app-db stub with real handler; export handleRecipeAction unchanged)"
    - "src/mcp/tools/recipe.test.ts (modified — flip create-app-db describe block to real-handler GREEN assertions)"
    - "src/mcp/server.ts (modified — import recipe tool exports and register 'recipe' tool)"
    - "README.md (modified — document recipe tool with three actions and safety posture)"
  key_links:
    - "src/mcp/tools/recipe.ts handleCreateAppDb → create<Engine>Database (engine-dispatched) → triggerDatabaseStart (if instant_deploy) → createPublicApplication → fetchDatabase (read internal_db_url) → bulkUpdateEnvs (wire env_key) → triggerDeploy (if instant_deploy)"
    - "src/mcp/server.ts registerCoolifyTools → registerTool('recipe', { description: composeToolDescription(...), inputSchema: withInstanceRoutingSchema(recipeActionSchema), outputSchema: toolOutputSchema, annotations: { openWorldHint: true } }, handler)"
    - "src/mcp/tools/recipe.ts handleCreateAppDb → sanitizeFullProjection (mask connection_string unless reveal:true)"
  prohibitions:
    - "No auto-rollback on partial failure (D-15) — return created UUIDs + recoveryHints"
    - "No confirm gate on create-app-db (D-17)"
    - "No dry-run / preview (D-18)"
    - "No unmasked connection_string without reveal:true (D-19, T-20-01)"
    - "No bundled YAML catalog (D-01/D-03)"
    - "No custom template URLs (T-20-02)"
    - "No silent drop of unknown fields — strict Zod validation (Phase 19 D-04)"
    - "No second create-app-db call idempotent — re-calling creates a second app+db pair (specless idempotency resolution)"
    - "No concurrent execution guarantee — create-app-db is sequential (DB → app → wire); interrupted runs leave created resources in place without rollback (specless concurrency resolution)"
---

<objective>
Wire the third recipe action create-app-db (RECIPE-03), register the recipe tool in server.ts, and document the surface in README. This closes Phase 20.

Purpose: Agent runs one recipe call to create an application + database with DATABASE_URL (or env_key override) wired between them. Partial failure returns created UUIDs + recoveryHints — no auto-rollback per D-15. Connection strings masked unless reveal:true per D-19. Tool registration exposes recipe alongside the 16 existing domain tools.

Output: Modified `src/mcp/tools/recipe.ts` with real handleCreateAppDb replacing the Plan 20-02 stub; modified `src/mcp/tools/recipe.test.ts` flipping create-app-db to GREEN; modified `src/mcp/server.ts` registering the recipe tool; modified `README.md` documenting the recipe tool.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-recipes-service-list-types/20-CONTEXT.md
@.planning/phases/20-recipes-service-list-types/20-RESEARCH.md
@.planning/phases/20-recipes-service-list-types/20-PATTERNS.md
@.planning/phases/20-recipes-service-list-types/20-VALIDATION.md
@.planning/phases/20-recipes-service-list-types/20-02-SUMMARY.md
@src/mcp/tools/recipe.ts
@src/mcp/tools/database.ts
@src/mcp/server.ts
@src/api/client.ts
</context>

## Artifacts this phase produces

- `src/mcp/tools/recipe.ts` — `handleCreateAppDb` replaces the COOLIFY_NOT_IMPLEMENTED stub. Engine-dispatched database create (postgresql/mysql/mariadb/mongodb/redis/clickhouse/dragonfly/keydb via create<Engine>Database), optional triggerDatabaseStart, createPublicApplication, fetchDatabase to read internal_db_url, fallback URL construction when internal_db_url absent, bulkUpdateEnvs to wire env_key (default DATABASE_URL), optional triggerDeploy, response with application_uuid + database_uuid + connection_string (masked via sanitizeFullProjection unless reveal:true). Partial-failure paths return COOLIFY_RECIPE_PARTIAL_FAILURE with created UUIDs in error.data and recoveryHints. Exports unchanged.
- `src/mcp/tools/recipe.test.ts` — create-app-db describe block flips from COOLIFY_NOT_IMPLEMENTED assertions to real-handler GREEN assertions.
- `src/mcp/server.ts` — import block adds `handleRecipeAction`, `recipeActionSchema`, `recipeActionsCatalog`, `recipeSafetyFooter` from `./tools/recipe.js`; `registerCoolifyTools` adds a `server.registerTool('recipe', ...)` call with `composeToolDescription('Multi-resource orchestration recipes & dynamic provisioning.', recipeActionsCatalog, recipeSafetyFooter)`, `inputSchema: withInstanceRoutingSchema(recipeActionSchema)`, `outputSchema: toolOutputSchema`, `annotations: { openWorldHint: true }`, and the standard error-result handler wrapper.
- `README.md` — new "Recipes" section (EN + DE if bilingual parity convention applies) documenting the three recipe actions, their args, the no-confirm / no-dry-run / no-auto-rollback / mask-by-default posture, and a one-line example for each.

<!-- gsd-write-continue -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement create-app-db handler with engine dispatch, internal_db_url read, env wiring, partial-failure posture</name>
  <files>src/mcp/tools/recipe.ts, src/mcp/tools/recipe.test.ts</files>
  <read_first>
    - src/mcp/tools/recipe.ts (Plan 20-02 output — handleCreateAppDb stub, recipeActionSchema, dispatch)
    - src/mcp/tools/database.ts (engine-dispatch pattern for createDatabaseSchema, create<Engine>Database client calls, fetchDatabase response shape with internal_db_url)
    - src/mcp/tools/service.ts (bulkUpdateEnvs call shape line 1641, withManifestUpsert pattern)
    - src/api/client.ts (createPostgresqlDatabase line 404, createMysqlDatabase 414, createMariadbDatabase 424, createMongodbDatabase 434, createRedisDatabase 444, createClickhouseDatabase 454, createDragonflyDatabase 464, createKeydbDatabase 474, fetchDatabase 354, triggerDatabaseStart 1017, createPublicApplication 258, triggerDeploy, bulkUpdateEnvs)
    - src/utils/projections.ts (sanitizeFullProjection for connection_string masking)
    - .planning/phases/20-recipes-service-list-types/20-PATTERNS.md (Partial Failure / Wiring Logic lines 115-178)
    - .planning/phases/20-recipes-service-list-types/20-RESEARCH.md (Pitfall 2 — DB connection string format; Pattern: Multi-Step App+DB)
    - .planning/phases/20-recipes-service-list-types/20-CONTEXT.md (D-13, D-14, D-15, D-16, D-17, D-18, D-19)
  </read_first>
  <behavior>
    - handleCreateAppDb dispatches database create by parsed.db_engine: postgresql → createPostgresqlDatabase, mysql → createMysqlDatabase, mariadb → createMariadbDatabase, mongodb → createMongodbDatabase, redis → createRedisDatabase, clickhouse → createClickhouseDatabase, dragonfly → createDragonflyDatabase, keydb → createKeydbDatabase
    - Each create<Engine>Database call uses the same body shape as src/mcp/tools/database.ts handleDatabaseCreate (server_uuid, project_uuid, environment_uuid, name=db_name, engine-specific fields with sensible defaults)
    - On instant_deploy !== false, calls triggerDatabaseStart(dbUuid) — soft ignore failure (log warning, continue) per D-16
    - After DB create, calls createPublicApplication with { server_uuid, project_uuid, environment_uuid, name: app_name, build_pack: 'nixpacks' (default for app+db recipe) } — on failure throws CoolifyApiError({ code: 'COOLIFY_RECIPE_PARTIAL_FAILURE', message: 'Database created successfully but application creation failed: <err>', recoveryHints: ['Do not delete the database; it is fully functional.', 'Manually create the application and link the connection string.'], data: { database_uuid: dbUuid } }) — NO auto-rollback per D-15
    - After app create, calls fetchDatabase(env.COOLIFY_URL, env.COOLIFY_TOKEN, dbUuid, env.COOLIFY_VERIFY_SSL) to read internal_db_url (D-14)
    - If internal_db_url is present, use it as connectionString; else construct engine-specific fallback URL from create params (D-14) — e.g. postgresql: `postgresql://${user}:${password}@${host}:${port}/${db}`; redis: `redis://default:${password}@${host}:${port}`; mongodb: `mongodb://${user}:${password}@${host}:${port}/${db}` — use the same engine-specific fallback logic as src/mcp/tools/database.ts if a helper exists, else inline a minimal constructFallbackUrl switch
    - Calls bulkUpdateEnvs('application', env.COOLIFY_URL, env.COOLIFY_TOKEN, appUuid, [{ key: envKey, value: connectionString }], env.COOLIFY_VERIFY_SSL) where envKey = parsed.env_key ?? 'DATABASE_URL' (D-13)
    - On env-wiring failure, throws CoolifyApiError({ code: 'COOLIFY_RECIPE_PARTIAL_FAILURE', message: 'Application and database created but env wiring failed: <err>', recoveryHints: ['Both resources are created and running.', 'Manually set the env var on the application using the database connection string.', 'Tip: pass `instance` to target a registered Coolify instance or use the manifest tool to record context (D-20 soft manifest hint).'], data: { application_uuid: appUuid, database_uuid: dbUuid } }) — NO auto-rollback per D-15
    - On instant_deploy !== false, calls triggerDeploy(appUuid) — soft ignore failure
    - Returns buildReadResponse({ application_uuid: appUuid, database_uuid: dbUuid, connection_string: connectionString, env_key: envKey, deploy: { status: 'queued' } }, { format, max_chars })
    - connection_string is masked via sanitizeFullProjection on the response unless parsed.reveal === true (D-19, T-20-01)
    - recipe.test.ts create-app-db describe block flips from COOLIFY_NOT_IMPLEMENTED assertions to real-handler GREEN assertions
    - recipe.test.ts adds a D-20 soft manifest hint it case asserting COOLIFY_RECIPE_PARTIAL_FAILURE recoveryHints contains `instance` or `manifest` token on env-wiring failure
  </behavior>
  <action>
    In src/mcp/tools/recipe.ts: replace the handleCreateAppDb stub with a real handler. Add imports for createPostgresqlDatabase, createMysqlDatabase, createMariadbDatabase, createMongodbDatabase, createRedisDatabase, createClickhouseDatabase, createDragonflyDatabase, createKeydbDatabase, fetchDatabase, triggerDatabaseStart, bulkUpdateEnvs, triggerDeploy from '../../api/client.js'. Add sanitizeFullProjection import from '../../utils/projections.js'. Define a dispatchCreateDatabase(engine, env, body) helper that switches on engine to call the matching create<Engine>Database function. Define constructFallbackUrl(engine, params) that builds an engine-specific connection string from create params when internal_db_url is absent. Define handleCreateAppDb(parsed, env): resolve project_uuid + environment_uuid (same pattern as handleCreateGitApp), dispatch DB create, optional triggerDatabaseStart, createPublicApplication inside try/catch that on failure throws COOLIFY_RECIPE_PARTIAL_FAILURE with database_uuid in data, fetchDatabase to read internal_db_url, construct fallback if absent, bulkUpdateEnvs inside try/catch that on failure throws COOLIFY_RECIPE_PARTIAL_FAILURE with both UUIDs in data and a recoveryHints array that includes a D-20 soft manifest hint string ('Tip: pass `instance` to target a registered Coolify instance or use the manifest tool to record context (D-20 soft manifest hint).'), optional triggerDeploy, return buildReadResponse with sanitizeFullProjection masking connection_string unless parsed.reveal. Remove the COOLIFY_NOT_IMPLEMENTED stub. In src/mcp/tools/recipe.test.ts: replace the create-app-db describe block assertions to call the real handler with mocked createPostgresqlDatabase (resolve), createPublicApplication (resolve), fetchDatabase (resolve with { internal_db_url: 'postgresql://user:pass@host:5432/db' }), bulkUpdateEnvs (resolve), triggerDatabaseStart (resolve), triggerDeploy (resolve) and assert the success response shape. Add it cases for: (a) success path with DATABASE_URL default, (b) env_key override, (c) internal_db_url read from fetchDatabase, (d) fallback URL when internal_db_url absent, (e) partial failure on app create returns COOLIFY_RECIPE_PARTIAL_FAILURE with database_uuid in data and deleteDatabase NOT called (no auto-rollback), (f) partial failure on env wiring returns COOLIFY_RECIPE_PARTIAL_FAILURE with both UUIDs in data, (g) connection_string masked unless reveal:true, (h) instant_deploy default true triggers DB start + app deploy, (i) no confirm gate (no confirm param in schema), (j) D-20 soft manifest hint — env-wiring failure recoveryHints contains `instance` or `manifest` token. Mock the 8 create<Engine>Database functions for engine-dispatch assertions (at least postgresql + redis + mongodb to cover the dispatch branches).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/recipe.test.ts -t "create-app-db"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "COOLIFY_RECIPE_PARTIAL_FAILURE" src/mcp/tools/recipe.ts` returns at least 2 matches (app-create + env-wiring partial failure)
    - `rg -n "D-20 soft manifest hint" src/mcp/tools/recipe.ts` returns at least 1 match (env-wiring recoveryHints)
    - `rg -n "sanitizeFullProjection" src/mcp/tools/recipe.ts` returns at least 1 match (masking)
    - `rg -n "COOLIFY_NOT_IMPLEMENTED" src/mcp/tools/recipe.ts` returns 0 matches (stub removed)
    - `rg -n "manifest" src/mcp/tools/recipe.test.ts` returns at least 1 match (D-20 soft manifest hint test)
    - `npx vitest run src/mcp/tools/recipe.test.ts -t "create-app-db"` green with at least 10 it cases
    - `npm run build` (tsup) green
  </acceptance_criteria>
  <done>
    - handleCreateAppDb dispatches DB create by engine, reads internal_db_url, constructs fallback when absent, wires env_key (default DATABASE_URL), returns masked connection_string
    - Partial failure on app create throws COOLIFY_RECIPE_PARTIAL_FAILURE with database_uuid in data — no auto-rollback
    - Partial failure on env wiring throws COOLIFY_RECIPE_PARTIAL_FAILURE with both UUIDs in data — no auto-rollback
    - connection_string masked via sanitizeFullProjection unless reveal:true
    - instant_deploy defaults true; no confirm gate; no dry-run
    - recipe.test.ts create-app-db describe block GREEN with at least 9 it cases
    - COOLIFY_NOT_IMPLEMENTED stub removed from recipe.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Register recipe tool in server.ts + document in README</name>
  <files>src/mcp/server.ts, src/mcp/server.test.ts, README.md</files>
  <read_first>
    - src/mcp/server.ts (import block lines 1-119, registerCoolifyTools registerTool pattern lines 173-707, composeToolDescription helper line 165)
    - src/mcp/server.test.ts (lines 53-79 registerTool count assertion `expect(matches.length).toBe(16)`, lines 81-110 withInstanceRoutingSchema routed list, lines 238-256 expectedTools array, lines 179-202 read-only tool block assertions)
    - src/mcp/tools/recipe.ts (Plan 20-02 + Task 1 output — exports to import)
    - README.md (existing tool documentation sections — find the right insertion point for the Recipes section)
    - .planning/phases/20-recipes-service-list-types/20-CONTEXT.md (D-05, D-08, D-17, D-18, D-19)
    - .planning/phases/19-dx-schemas-mcp-prompts/19-02-SUMMARY.md (README EN/DE parity convention)
  </read_first>
  <behavior>
    - src/mcp/server.ts imports handleRecipeAction, recipeActionSchema, recipeActionsCatalog, recipeSafetyFooter, isRecipeErrorResult from './tools/recipe.js'
    - registerCoolifyTools adds server.registerTool('recipe', { description: composeToolDescription('Multi-resource orchestration recipes & dynamic provisioning.', recipeActionsCatalog, recipeSafetyFooter), inputSchema: withInstanceRoutingSchema(recipeActionSchema), outputSchema: toolOutputSchema, annotations: { openWorldHint: true } }, async (args) => { const result = await handleRecipeAction(args, env); if (isRecipeErrorResult(result)) { return { ...result, structuredContent: { ok: false, error: result.structuredContent.error } }; } return { content: [{ type: 'text', text: result._formattedText }], structuredContent: { ok: true, data: result.data, _meta: result._meta } }; })
    - src/mcp/server.test.ts updated: (1) the `expect(matches.length).toBe(16)` assertion at line 60 is bumped to `expect(matches.length).toBe(17)` to account for the new recipe registerTool; (2) `registerTool(\n    'recipe'` is added to the contains-assertion list; (3) `recipeActionSchema` is appended to the `routed` array at lines 89-102 so the `withInstanceRoutingSchema(recipeActionSchema)` assertion covers recipe; (4) `'recipe'` is appended to the `expectedTools` array at lines 238-255 so the `every registered tool description contains Actions: and Safety: prefixes` test covers recipe; (5) the discriminatedUnion test at lines 112-140 is extended with `expect(recipeActionSchema.safeParse({ action: 'invalid' }).success).toBe(false)` to lock the flat-schema reject-unknown-action invariant for recipe
    - README.md adds a Recipes section documenting the three actions with args, the no-confirm / no-dry-run / no-auto-rollback / mask-by-default posture, and a one-line example for each
    - If README maintains EN/DE parity (per Phase 19 convention), the Recipes section is added in both languages
  </behavior>
  <action>
    In src/mcp/server.ts: add a new import block after the existing emergency import (or after the database import, wherever fits the file's ordering) importing { handleRecipeAction, recipeActionSchema, recipeActionsCatalog, recipeSafetyFooter, isRecipeErrorResult } from './tools/recipe.js'. Inside registerCoolifyTools, after the database registerTool block (or after the last domain tool, before the closing brace), add a server.registerTool('recipe', ...) call per the <behavior> block. Use the same handler wrapper shape as the service/database tools (error-result check + structuredContent passthrough). In src/mcp/server.test.ts: bump the `expect(matches.length).toBe(16)` assertion to `toBe(17)`; add `expect(source).toContain("registerTool(\n    'recipe'")` to the contains-assertion list; append `'recipeActionSchema'` to the `routed` array so the withInstanceRoutingSchema assertion covers it; append `'recipe'` to the `expectedTools` array; add `expect(recipeActionSchema.safeParse({ action: 'invalid' }).success).toBe(false)` to the discriminatedUnion test. Import `recipeActionSchema` from `./tools/recipe.js` at the top of the test file alongside the existing tool-schema imports. In README.md: find the existing tools documentation section and add a new "Recipes" subsection (or update the Tools list to include `recipe`). Document the three actions: `create-git-app(server_uuid, git_repository, git_branch, repo_path?, build_pack?, instant_deploy?)`, `create-app-db(server_uuid, app_name, db_name, db_engine, env_key?, instant_deploy?)`, `create-one-click(server_uuid, type, instant_deploy?)`. Add a Safety note: "Recipe creates are intentional — no confirm gate. No dry-run / preview. Partial failure does NOT auto-rollback; created UUIDs are returned in error.data. Connection strings are masked unless reveal:true." If README has a DE section, mirror the Recipes section in German. Add one-line examples for each action.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "registerTool\('recipe'" src/mcp/server.ts` returns 1 match
    - `rg -n "withInstanceRoutingSchema\(recipeActionSchema\)" src/mcp/server.ts` returns 1 match
    - `rg -n "openWorldHint:\s*true" src/mcp/server.ts` matches within the recipe registerTool block
    - `rg -n "toBe\(17\)" src/mcp/server.test.ts` returns at least 1 match (registerTool count bumped 16→17)
    - `rg -n "'recipe'" src/mcp/server.test.ts` returns matches in contains-assertion, routed array, and expectedTools array
    - `rg -n "recipeActionSchema" src/mcp/server.test.ts` returns at least 2 matches (import + routed list + discriminatedUnion assertion)
    - `npx vitest run src/mcp/server.test.ts` green (existing + new recipe assertions)
    - `rg -c "create-git-app|create-app-db|create-one-click" README.md` returns at least 3 (one per action)
    - `npm run build` (tsup) green
  </acceptance_criteria>
  <done>
    - src/mcp/server.ts imports recipe tool exports and registers the 'recipe' tool with openWorldHint: true
    - src/mcp/server.test.ts updated: registerTool count 16→17, 'recipe' in routed schemas list, recipeActionSchema in withInstanceRoutingSchema assertions, 'recipe' in expectedTools array, discriminatedUnion assertion extended
    - README.md documents the recipe tool with all three actions, args, safety posture, and examples
    - src/mcp/server.test.ts green (existing + new tool registration assertion)
    - npm run build (tsup) green
  </done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → recipe inputSchema | Untrusted agent/client crosses here; Zod validates action + per-action fields before any handler runs |
| recipe handler → Coolify API | Only Zod-validated payloads reach outbound HTTPS to create<Engine>Database / createPublicApplication / fetchDatabase / bulkUpdateEnvs / triggerDeploy |
| Coolify API response → agent-visible data | sanitizeFullProjection masks internal_db_url / connection_string unless reveal:true (D-19, T-20-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | connection_string in create-app-db response | high | mitigate | sanitizeFullProjection masks connection_string to '***' unless parsed.reveal === true. Recovery hint `ask_human_reveal` attached when reveal needed. (D-19, T-20-01) |
| T-20-03-01 | Denial of Service | Partial failure leaves orphaned resources | medium | mitigate | No auto-rollback per D-15 — instead return COOLIFY_RECIPE_PARTIAL_FAILURE with created UUIDs in error.data and recoveryHints telling the user not to delete the database and how to manually complete the wiring. Agent + user can recover deterministically. |
| T-20-03-02 | Repudiation | Duplicate create-app-db not idempotent | low | accept | Re-calling create-app-db creates a second app+db pair. Documented in tool description. (specless idempotency resolution) |
| T-20-03-03 | Tampering | Concurrent create-app-db runs | low | accept | Handler is sequential (DB → app → wire). Interrupted runs leave created resources in place without rollback. Documented. (specless concurrency resolution) |
| T-20-SC | Tampering | npm package installs | low | accept | No new packages — all client functions already pinned in package.json per RESEARCH Package Legitimacy Audit. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/recipe.test.ts` green (all three recipe actions GREEN)
- `npx vitest run src/mcp/server.test.ts` green (recipe tool registered, 16→17 bumped, recipeActionSchema in routed list)
- `npm test` full suite green
- `npm run build` (tsup) green
- `rg -n "registerTool\('recipe'" src/mcp/server.ts` returns 1 match
- `rg -c "create-git-app|create-app-db|create-one-click" README.md` returns at least 3 (one per action)
- `rg -c "COOLIFY_NOT_IMPLEMENTED" src/mcp/tools/recipe.ts` returns 0 (stub removed)
</verification>

<success_criteria>
- RECIPE-03 fully shipped: agent runs recipe create-app-db end-to-end — DB + app created, DATABASE_URL (or env_key override) wired, connection_string masked unless reveal:true
- No auto-rollback on partial failure (D-15) — COOLIFY_RECIPE_PARTIAL_FAILURE returns created UUIDs + recoveryHints
- No confirm gate (D-17), no dry-run (D-18), instant_deploy defaults true (D-16)
- Connection strings masked unless reveal:true (D-19, T-20-01)
- recipe tool registered in server.ts alongside the 16 existing domain tools
- README documents the recipe tool with all three actions and safety posture
- All Phase 20 requirements (RECIPE-01, RECIPE-02, RECIPE-03, RECIPE-04) shipped across Plans 20-01/20-02/20-03
- Full test suite green; npm run build green
</success_criteria>

<output>
Create `.planning/phases/20-recipes-service-list-types/20-03-SUMMARY.md` when done.
</output>
