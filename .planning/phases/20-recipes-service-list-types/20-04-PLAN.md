---
phase: 20-recipes-service-list-types
plan: 04
type: execute
wave: 4
depends_on:
  - 20-03
files_modified:
  - src/mcp/tools/recipe.ts
  - src/mcp/tools/recipe.test.ts
autonomous: true
gap_closure: true
requirements:
  - RECIPE-01
  - RECIPE-02
  - RECIPE-03
  - RECIPE-04
user_setup: []

must_haves:
  truths:
    - "create-git-app AND create-one-click error results carry soft manifest/instance hints in recoveryHints per D-20 (truth #14)"
    - "create-git-app Zod validation failures (throwValidationError) append the canonical MANIFEST_HINT Tip string to recoveryHints (D-20)"
    - "create-git-app runtime CoolifyApiError paths in handleCreateGitApp (missing build_pack + API failures from createPublicApplication/triggerDeploy) append MANIFEST_HINT to recoveryHints (D-20)"
    - "recipe.test.ts asserts create-git-app error recoveryHints match /instance|manifest/i mirroring the create-one-click D-20 case"
    - "Missing build_pack / unknown type → field-specific error + list-types / MANIFEST_HINT (UI Considerations empty E6)"
    - "COOLIFY_VALIDATION_ERROR recoveryHints include MANIFEST_HINT on recipe error paths including create-git-app (UI Considerations error E2 / E6)"
    - "MANIFEST_HINT remains a single Tip string ≤160 chars (UI Considerations overflow/long-text E3)"
    - statement: "Coolify API error text may be long; recoveryHints remain scannable (≤160 chars each)"
      verification: backstop
    - statement: "create-git-app error recoveryHints include MANIFEST_HINT after gap closure (D-20)"
      verification: backstop
  artifacts:
    - "src/mcp/tools/recipe.ts — throwValidationError and handleCreateGitApp error envelopes append MANIFEST_HINT for create-git-app (D-20)"
    - "src/mcp/tools/recipe.test.ts — create-git-app D-20 soft-manifest-hint assertion mirroring create-one-click"
  key_links:
    - "throwValidationError → recoveryHints includes MANIFEST_HINT when args.action is create-git-app (Zod path)"
    - "handleCreateGitApp missing-build_pack CoolifyApiError → recoveryHints includes MANIFEST_HINT"
    - "handleCreateGitApp API CoolifyApiError from createPublicApplication/triggerDeploy → recoveryHints includes MANIFEST_HINT"
    - "handleRecipeAction → wrapMcpError preserves appended recoveryHints on MCP error envelope"
  prohibitions:
    - "Do not weaken or remove MANIFEST_HINT from create-one-click unknown-type or create-app-db env-wiring paths"
    - "Do not introduce confirm gates, dry-run, or auto-rollback"
    - "Do not change MANIFEST_HINT canonical Tip wording (UI-SPEC Copywriting Contract)"
    - "Do not replan or rewrite 20-00..20-03 PLAN.md files"
    - "Do not treat README list-types table drift as blocking (optional advisory only)"
---

<objective>
Close the sole failed verification truth (#14 / D-20): create-git-app error envelopes must carry the same soft manifest/`instance` recovery hint already shipped on create-one-click (and create-app-db env-wiring).

Purpose: Agents hitting Zod validation or Coolify API failures on create-git-app currently see only RECOVERY_HINTS.COOLIFY_VALIDATION_ERROR — no tip to pass `instance` or use the manifest tool. That breaks D-20 parity with create-one-click and blocks Phase 20 completion (13/14).

Output: `recipe.ts` appends canonical `MANIFEST_HINT` on create-git-app Zod + CoolifyApiError paths; `recipe.test.ts` adds a D-20 assertion mirroring the create-one-click case. No other recipe behavior changes.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-recipes-service-list-types/20-CONTEXT.md
@.planning/phases/20-recipes-service-list-types/20-UI-SPEC.md
@.planning/phases/20-recipes-service-list-types/20-VERIFICATION.md
@.planning/phases/20-recipes-service-list-types/20-03-SUMMARY.md
@.planning/phases/20-recipes-service-list-types/.continue-here.md
@src/mcp/tools/recipe.ts
@src/mcp/tools/recipe.test.ts
</context>

## Artifacts this phase produces

- `src/mcp/tools/recipe.ts` — Gap closure only: append existing module-level `MANIFEST_HINT` constant to create-git-app error `recoveryHints` on (1) Zod failures via `throwValidationError` when `args.action === 'create-git-app'`, (2) the runtime missing-`build_pack` `CoolifyApiError` inside `handleCreateGitApp`, and (3) `CoolifyApiError` rethrown from `createPublicApplication` / `triggerDeploy` failures inside `handleCreateGitApp` (dedupe if already present). Leave create-one-click / create-app-db MANIFEST_HINT paths untouched.
- `src/mcp/tools/recipe.test.ts` — New `it` under `describe('recipe create-git-app')` asserting error `recoveryHints` contain `/instance|manifest/i`, mirroring the create-one-click test titled with soft manifest hint / D-20 (use missing `build_pack` / omitted `repo_path` path as the cheapest probe — same path verification used).

## Multi-Source Coverage Audit (gap_closure scope)

| SOURCE | ID | Item | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Dynamic list-types + recipes without forked YAML | 20-00..03 | COVERED | Prior plans; not reopened |
| REQ | RECIPE-01 | service.list-types dynamic discovery | 20-01 | COVERED | Protected; untouched by gap |
| REQ | RECIPE-02 | create-git-app end-to-end | 20-02 + **20-04** | COVERED | Gap closes D-20 hint parity on create-git-app errors |
| REQ | RECIPE-03 | create-app-db wiring | 20-03 | COVERED | Protected; untouched by gap |
| REQ | RECIPE-04 | create-one-click from list-types | 20-02 | COVERED | Already has MANIFEST_HINT; regression-guarded |
| CONTEXT | D-20 | Soft instance/manifest hints on recipe errors | **20-04** | COVERED | Sole failed verification truth |
| RESEARCH | — | Error envelope recoveryHints patterns | 20-04 | COVERED | Mirror create-one-click pattern |
| UI-SPEC | E6 / E2 | MANIFEST_HINT on create-git-app errors | **20-04** | COVERED | Lifted into must_haves |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append MANIFEST_HINT on create-git-app Zod and CoolifyApiError paths (D-20)</name>
  <files>src/mcp/tools/recipe.ts</files>
  <read_first>
    - src/mcp/tools/recipe.ts — module-level MANIFEST_HINT constant (~lines 43-44); throwValidationError (~234-262); handleCreateGitApp (~297-369) missing-build_pack CoolifyApiError and createPublicApplication/triggerDeploy calls; handleCreateOneClick unknown-type recoveryHints array that already ends with MANIFEST_HINT (~682-691) as the copy pattern
    - src/mcp/tools/recipe.test.ts — create-one-click D-20 it at ~675-689 (`error result carries soft manifest hint suggesting instance param or manifest context per D-20`); create-git-app `requires build_pack when repo_path omitted (D-11)` at ~245-258 as the cheapest error probe
    - .planning/phases/20-recipes-service-list-types/20-CONTEXT.md — D-20 soft instance routing / soft manifest hints
    - .planning/phases/20-recipes-service-list-types/20-UI-SPEC.md — Copywriting Contract MANIFEST_HINT canonical Tip; Required-on table for create-git-app Zod / runtime / API gaps; ## UI Considerations error E2/E6 and long-text E6 backstop
    - .planning/phases/20-recipes-service-list-types/20-VERIFICATION.md — gaps[0] missing items (exact closure criteria)
  </read_first>
  <behavior>
    - Calling handleRecipeAction with create-git-app and no repo_path and no build_pack yields isError true and recoveryHints matching /instance|manifest/i (D-20)
    - Calling handleRecipeAction with create-git-app and build_pack dockercompose (D-12 Zod path) yields recoveryHints matching /instance|manifest/i
    - When createPublicApplication rejects with CoolifyApiError during create-git-app, the MCP error recoveryHints include MANIFEST_HINT (or /instance|manifest/i)
    - create-one-click unknown-type D-20 test remains green (no regression)
    - MANIFEST_HINT string content unchanged from UI-SPEC canonical Tip
  </behavior>
  <action>
    Close verification gap truth #14 / D-20 for the create-git-app path only (create-one-click already ships MANIFEST_HINT).

    1. In throwValidationError: build recoveryHints as an array from RECOVERY_HINTS[resolvedCode] (or COOLIFY_422 fallback). When isRecord(args) and args.action === 'create-git-app', append MANIFEST_HINT if not already present. Prefer a tiny local helper (e.g. appendManifestHint) that spreads existing hints and pushes MANIFEST_HINT once — Claude's discretion. Do not change Zod issue messages or error codes.

    2. In handleCreateGitApp missing-build_pack CoolifyApiError (~line 319-324): replace bare RECOVERY_HINTS.COOLIFY_VALIDATION_ERROR with an array that spreads those base hints and appends MANIFEST_HINT (same shape as handleCreateOneClick unknown-type recoveryHints). Per D-20.

    3. In handleCreateGitApp: wrap createPublicApplication and triggerDeploy so that if a CoolifyApiError is thrown, rethrow a CoolifyApiError with the same code/message/data but recoveryHints = prior hints plus MANIFEST_HINT (dedupe). Non-CoolifyApiError errors may continue to bubble to wrapMcpError unchanged. Per UI-SPEC create-git-app API-failure row and verification missing item 1.

    4. Do not modify create-one-click or create-app-db MANIFEST_HINT sites. Do not edit README for list-types table drift (optional advisory — out of scope). Do not change the MANIFEST_HINT constant text.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/mcp/tools/recipe.test.ts -t "create-git-app|create-one-click|D-20|manifest hint"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MANIFEST_HINT" src/mcp/tools/recipe.ts` shows usages in throwValidationError path (or helper called from it), in handleCreateGitApp missing-build_pack throw, and in the API-error rethrow path — not only in create-one-click / create-app-db
    - `rg -n "recoveryHints: RECOVERY_HINTS.COOLIFY_VALIDATION_ERROR," src/mcp/tools/recipe.ts` returns 0 matches inside handleCreateGitApp (bare registry alone is gone; array+MANIFEST_HINT required)
    - Canonical MANIFEST_HINT Tip string still contains both `instance` and `manifest` and stays ≤160 characters
    - Existing create-one-click D-20 it and create-app-db env-wiring D-20 it remain present and green
  </acceptance_criteria>
  <done>create-git-app Zod validation errors and CoolifyApiError throws from handleCreateGitApp include MANIFEST_HINT in recoveryHints per D-20 / truth #14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add create-git-app D-20 recoveryHints regression test</name>
  <files>src/mcp/tools/recipe.test.ts</files>
  <read_first>
    - src/mcp/tools/recipe.test.ts — create-one-click D-20 it (~675-689) as the exact assertion shape to mirror; create-git-app describe block (~105+); baseGitAppArgs; requires-build_pack D-11 it (~245-258)
    - src/mcp/tools/recipe.ts — Task 1 MANIFEST_HINT wiring (confirm which error path the new test will hit)
    - .planning/phases/20-recipes-service-list-types/20-VERIFICATION.md — gaps[0].missing[1]: Add recipe.test.ts assertion mirroring create-one-click D-20 case for a create-git-app error path
  </read_first>
  <behavior>
    - New it under describe('recipe create-git-app') with a title that includes D-20 and soft manifest / instance language
    - Probe: omit repo_path and build_pack (same as D-11) OR mock createPublicApplication to throw CoolifyApiError — either is valid; prefer omit-repo_path path for cheapest green assertion
    - Expect isRecipeErrorResult true
    - Expect structuredContent.error.recoveryHints toEqual expect.arrayContaining([expect.stringMatching(/instance|manifest/i)]) — identical matcher to create-one-click D-20 it
  </behavior>
  <action>
    Add one new vitest `it` inside `describe('recipe create-git-app')` that mirrors the create-one-click D-20 case. Title must clearly reference soft manifest hint and D-20 (e.g. parallel wording to the create-one-click test). Use dynamic import of handleRecipeAction / isRecipeErrorResult like sibling tests. Call with `{ ...baseGitAppArgs, repo_path: undefined }` (and no build_pack) so Zod or runtime validation fails before createPublicApplication. Assert isError, then assert recoveryHints with expect.arrayContaining([expect.stringMatching(/instance|manifest/i)]). Do not weaken or remove the existing D-11 requires-build_pack test — keep both (D-11 checks code; new it checks D-20 hints). Do not add README changes.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/mcp/tools/recipe.test.ts -t "create-git-app"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "D-20" src/mcp/tools/recipe.test.ts` shows at least one match inside the create-git-app describe block (not only create-one-click / create-app-db)
    - New it asserts recoveryHints with stringMatching(/instance|manifest/i) for a create-git-app error result
    - `pnpm exec vitest run src/mcp/tools/recipe.test.ts -t "create-git-app"` is green including the new D-20 case
    - `pnpm exec vitest run src/mcp/tools/recipe.test.ts -t "create-one-click|create-app-db|D-20|manifest"` remains green (no cross-action regression)
  </acceptance_criteria>
  <done>recipe.test.ts locks D-20 create-git-app soft-manifest-hint parity with create-one-click; truth #14 is regression-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Agent → recipe MCP tool | Untrusted args cross into Zod parse + handlers; error envelopes return to agent |
| recipe handler → Coolify API | createPublicApplication / triggerDeploy may throw CoolifyApiError with server-sourced messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-20-04-01 | Information Disclosure | create-git-app recoveryHints | low | accept | MANIFEST_HINT is a static Tip (no secrets); wrapMcpError already redacts secrets in hint strings — gap only appends fixed Tip |
| T-20-04-02 | Information Disclosure | CoolifyApiError API message rethrow | medium | mitigate | Preserve existing redactSecrets via wrapMcpError; only append static MANIFEST_HINT; do not echo raw tokens/URLs into new hints |
| T-20-04-03 | Elevation of Privilege | instance soft hint | low | accept | D-20 is soft guidance only — no hard manifest requirement; wizard owns hard routing in Phase 22; hint cannot escalate privileges |
| T-20-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this gap plan — N/A; if any install appears, block and run legitimacy gate |
</threat_model>

<verification>
- `pnpm exec vitest run src/mcp/tools/recipe.test.ts -t "create-git-app|create-one-click|manifest|D-20"` green
- Runtime probe: handleRecipeAction create-git-app missing build_pack → recoveryHints match /instance|manifest/i
- 20-VERIFICATION.md truth #14 becomes verifiable after re-run of `/gsd-verify-work` / phase verifier
</verification>

<success_criteria>
- Truth #14 / D-20 satisfied for create-git-app AND create-one-click
- Gap missing items 1 and 2 from 20-VERIFICATION.md closed
- No regressions on create-one-click / create-app-db MANIFEST_HINT paths
- Phase 20 ready for re-verification (not marked complete until verifier passes)
</success_criteria>

<output>
Create `.planning/phases/20-recipes-service-list-types/20-04-SUMMARY.md` when done
</output>
