---
phase: 20-recipes-service-list-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/service-templates.ts
  - src/utils/service-templates.test.ts
  - src/mcp/tools/service.ts
  - src/mcp/tools/service.test.ts
autonomous: true
requirements:
  - RECIPE-01
user_setup: []

must_haves:
  truths:
    - "service.list-types action on the service tool fetches service-templates.json at runtime via ofetch from hardcoded jsDelivr/GitHub hosts — no bundled YAML catalog (D-01, D-03)"
    - "Template fetch is pinned to the active instance Coolify version when known; falls back to latest when version unavailable (D-02)"
    - "Offline / fetch failure on both CDN and GitHub Raw produces COOLIFY_FETCH_TEMPLATES_FAILED hard error with recoveryHints — no static fallback catalog (D-03)"
    - "list-types response is slim: array of { id, label } entries only — no full compose/template payload, no category/tag bloat (D-04)"
    - "Type IDs in the response are stable-sorted by id so duplicate or reordered CDN JSON does not cause agent-visible churn (specless probe resolution: ordering)"
    - "Empty service-templates.json response (CDN returns `{}`) is treated as a hard error with COOLIFY_FETCH_TEMPLATES_FAILED — no empty list returned to agent (specless probe resolution: empty input → hard error per D-03)"
    - "fetchServiceTemplates helper is exported from src/utils/service-templates.ts so Plan 20-02 (create-one-click type validation) can reuse it without modifying service.ts"
    - "No user-supplied URL is ever passed to ofetch — only hardcoded cdn.jsdelivr.net and raw.githubusercontent.com hosts (T-20-02 SSRF mitigation)"
  artifacts:
    - "src/utils/service-templates.ts (new — fetchServiceTemplates + slim mapping helper)"
    - "src/utils/service-templates.test.ts (new — unit tests for fetch, version pinning, fallback, hard-error, slim mapping)"
    - "src/mcp/tools/service.ts (modified — add 'list-types' action to serviceActionSchema + handleServiceListTypes handler)"
    - "src/mcp/tools/service.test.ts (modified — add list-types describe block with ofetch mock)"
  key_links:
    - "src/utils/service-templates.ts fetchServiceTemplates → ofetch(cdnUrl) with GitHub Raw fallback → COOLIFY_FETCH_TEMPLATES_FAILED on double failure"
    - "src/mcp/tools/service.ts handleServiceListTypes → fetchServiceTemplates + slim map to { id, label }"
    - "src/mcp/tools/service.ts serviceActionSchema 'list-types' branch → handleServiceListTypes via handleServiceAction switch"
  prohibitions:
    - "No bundled/static service-templates.json in the package — D-03 hard fail on fetch error"
    - "No user-supplied template URL accepted — hardcoded CDN/GitHub hosts only (D-01, T-20-02)"
    - "No full template/compose payload in list-types response — slim IDs + labels only (D-04)"
    - "No category/tag fields in v3.1 list-types response (D-04)"
    - "No silent empty-list return on fetch failure — hard error with recoveryHints (D-03)"
---

<objective>
Ship the dynamic service.list-types action (RECIPE-01) and extract a reusable fetchServiceTemplates helper so Plan 20-02 can validate create-one-click types without duplicating the fetch logic.

Purpose: Agent discovers Coolify one-click service types dynamically from the official service-templates.json on jsDelivr/GitHub — no hand-maintained YAML catalog in the repo (D-01, D-03). Version pinning to the active instance keeps templates compatible (D-02). Slim IDs+labels keep the response agent-friendly (D-04).

Output: New `src/utils/service-templates.ts` with `fetchServiceTemplates(env)` + slim mapping; new `src/utils/service-templates.test.ts`; modified `src/mcp/tools/service.ts` adding the `list-types` action; modified `src/mcp/tools/service.test.ts` with the list-types describe block.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-recipes-service-list-types/20-CONTEXT.md
@.planning/phases/20-recipes-service-list-types/20-RESEARCH.md
@.planning/phases/20-recipes-service-list-types/20-PATTERNS.md
@.planning/phases/20-recipes-service-list-types/20-VALIDATION.md
@src/mcp/tools/service.ts
@src/mcp/tools/service.test.ts
@src/api/client.ts
</context>

## Artifacts this phase produces

- `src/utils/service-templates.ts` — `fetchServiceTemplates(env): Promise<Record<string, { name?: string }>>` fetching `https://cdn.jsdelivr.net/gh/coollabsio/coolify@<version>/templates/service-templates.json` with `https://raw.githubusercontent.com/coollabsio/coolify/<version>/templates/service-templates.json` fallback; version pinned via `fetchVersion` from `../../api/client.js`; throws `CoolifyApiError({ code: 'COOLIFY_FETCH_TEMPLATES_FAILED' })` on double failure or empty `{}` response. Also exports `mapTemplatesToSlimList(raw)` returning `{ id, label }[]` sorted by id.
- `src/utils/service-templates.test.ts` — unit tests for CDN success, GitHub Raw fallback, double-failure hard error, empty-{} hard error, version pinning, slim mapping, stable sort by id.
- `src/mcp/tools/service.ts` — `serviceActionSchema` extended with `'list-types'` action; `serviceActionsCatalog` updated to include `list-types(format?, projection?)`; new `handleServiceListTypes(parsed, env)` calling `fetchServiceTemplates` + `mapTemplatesToSlimList` + `buildReadResponse`; `handleServiceAction` switch dispatches `list-types` to the new handler.
- `src/mcp/tools/service.test.ts` — new `describe('service list-types', ...)` block with ofetch mock for fetchServiceTemplates paths (CDN success, fallback, double failure, empty response, slim mapping, version pin).

<!-- gsd-write-continue -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchServiceTemplates helper + slim mapping in src/utils/service-templates.ts</name>
  <files>src/utils/service-templates.ts, src/utils/service-templates.test.ts</files>
  <read_first>
    - src/api/client.ts (lines 96-110 for fetchVersion signature and return shape)
    - src/utils/errors.ts (CoolifyApiError constructor, RECOVERY_HINTS)
    - src/utils/projections.ts (sanitizeFullProjection for masking context)
    - .planning/phases/20-recipes-service-list-types/20-RESEARCH.md (Pattern 1: Dynamic versions pinning + CDN fetch)
    - .planning/phases/20-recipes-service-list-types/20-PATTERNS.md (Dynamic Fetch & Version Pinning Pattern, lines 250-302)
    - .planning/phases/20-recipes-service-list-types/20-CONTEXT.md (D-01, D-02, D-03, D-04)
  </read_first>
  <behavior>
    - fetchServiceTemplates(env: EnvConfig): Promise<Record<string, { name?: string; description?: string }>> fetches service-templates.json from `https://cdn.jsdelivr.net/gh/coollabsio/coolify@<version>/templates/service-templates.json`
    - On CDN failure, falls back to `https://raw.githubusercontent.com/coollabsio/coolify/<version>/templates/service-templates.json`
    - On both failures, throws CoolifyApiError with code 'COOLIFY_FETCH_TEMPLATES_FAILED' and recoveryHints listing outbound-internet and GitHub-repo-access checks
    - On empty `{}` response (no template keys), throws the same COOLIFY_FETCH_TEMPLATES_FAILED error — empty input is a hard error per D-03 (specless probe resolution: empty input)
    - Version pinning: calls fetchVersion(env.COOLIFY_URL, env.COOLIFY_TOKEN, env.COOLIFY_VERIFY_SSL); if it returns a string or { version: string }, prefixes with 'v' if missing; on fetchVersion failure or 'unknown' value, falls back to 'v4.x' (D-02)
    - Only hardcoded cdn.jsdelivr.net and raw.githubusercontent.com hosts are passed to ofetch — no user-supplied URL accepted (T-20-02 SSRF mitigation)
    - mapTemplatesToSlimList(raw: Record<string, unknown>): { id: string; label: string }[] returns array sorted by id (stable sort) so CDN JSON reordering does not cause agent-visible churn (specless probe resolution: ordering)
    - Both functions exported from src/utils/service-templates.ts
  </behavior>
  <action>
    Create src/utils/service-templates.ts. Import ofetch from 'ofetch', fetchVersion from '../api/client.js', CoolifyApiError + RECOVERY_HINTS from './errors.js', and type EnvConfig from '../config/env.js'. Define fetchServiceTemplates(env) per the <behavior> block: try fetchVersion for pin (soft ignore errors → 'v4.x' fallback), build cdnUrl and githubUrl with the pinned version, await ofetch(cdnUrl, { parseResponse: JSON.parse }), on catch try ofetch(githubUrl, { parseResponse: JSON.parse }), on second catch throw CoolifyApiError({ code: 'COOLIFY_FETCH_TEMPLATES_FAILED', message: 'Could not fetch service templates from CDN or GitHub Raw.', recoveryHints: [...] }). After successful fetch, if Object.keys(raw).length === 0 throw the same error (empty input hard error per D-03). Return the raw record. Define mapTemplatesToSlimList(raw) returning Object.entries(raw).map(([id, details]) => ({ id, label: (isRecord(details) && typeof details.name === 'string') ? details.name : id })).sort((a, b) => a.id.localeCompare(b.id)). Add isRecord helper. Create src/utils/service-templates.test.ts with vi.mock('ofetch') and vi.mock('../api/client.js'). Add it cases for: CDN success returns raw object; GitHub Raw fallback on CDN reject; double failure throws COOLIFY_FETCH_TEMPLATES_FAILED; empty {} response throws COOLIFY_FETCH_TEMPLATES_FAILED; version pin uses fetchVersion return with 'v' prefix when missing; fetchVersion failure falls back to 'v4.x'; mapTemplatesToSlimList sorts by id and uses name when present, falls back to id when name absent. Do NOT modify service.ts in this task — Task 2 wires the action.
  </action>
  <verify>
    <automated>npx vitest run src/utils/service-templates.test.ts</automated>
  </verify>
  <done>
    - fetchServiceTemplates exported from src/utils/service-templates.ts with CDN + GitHub Raw fallback + COOLIFY_FETCH_TEMPLATES_FAILED hard error
    - mapTemplatesToSlimList exported, returns { id, label }[] sorted by id
    - Empty {} response throws COOLIFY_FETCH_TEMPLATES_FAILED (specless empty-input resolution)
    - Only hardcoded cdn.jsdelivr.net / raw.githubusercontent.com hosts passed to ofetch
    - src/utils/service-templates.test.ts green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire 'list-types' action into service tool + extend service.test.ts</name>
  <files>src/mcp/tools/service.ts, src/mcp/tools/service.test.ts</files>
  <read_first>
    - src/mcp/tools/service.ts (serviceActionSchema createFlatActionSchema call, serviceActionsCatalog, handleServiceAction switch, mutationActions list, serviceReadParamKeys)
    - src/mcp/tools/service.test.ts (vi.mock('../../api/client.js') factory, mockService, testEnv, existing describe blocks)
    - src/utils/service-templates.ts (Task 1 output — fetchServiceTemplates + mapTemplatesToSlimList)
    - .planning/phases/20-recipes-service-list-types/20-PATTERNS.md (Action Registration Pattern lines 220-248, list-types Test Cases Pattern lines 319-346)
    - .planning/phases/20-recipes-service-list-types/20-CONTEXT.md (D-04 slim response, D-01/D-03 hard error)
  </read_first>
  <behavior>
    - serviceActionSchema includes 'list-types' in the actions enum tuple
    - 'list-types' allowedFields = [...serviceReadParamKeys] (format, projection, include_full, page, per_page, max_chars, reveal) — no uuid required
    - 'list-types' requiredFields = [] (no required fields — agent calls with just action:'list-types')
    - serviceActionsCatalog string includes `list-types(format?, projection?)` token (per D-05 catalog format)
    - handleServiceAction switch dispatches 'list-types' to handleServiceListTypes
    - handleServiceListTypes calls fetchServiceTemplates(routingEnv), then mapTemplatesToSlimList, then buildReadResponse(slimList, { format, max_chars })
    - service.test.ts adds describe('service list-types', ...) with it cases: (a) CDN success returns slim mapped templates, (b) GitHub Raw fallback returns slim mapped templates, (c) double failure returns COOLIFY_FETCH_TEMPLATES_FAILED error result, (d) empty {} response returns COOLIFY_FETCH_TEMPLATES_FAILED error result, (e) slim response contains only { id, label } — no compose/template/category fields, (f) stable sort by id
    - Existing service.test.ts cases still pass
  </behavior>
  <action>
    In src/mcp/tools/service.ts: add 'list-types' to the actions tuple in createFlatActionSchema (after 'get' or wherever fits the catalog order). Add 'list-types' to the actionAllowedFields map with [...serviceReadParamKeys]. Do not add to actionRequiredFields (no required fields). Update serviceActionsCatalog string to include `list-types(format?, projection?)` in the appropriate position (per D-05 catalog format with ` · ` separators). Import fetchServiceTemplates and mapTemplatesToSlimList from '../../utils/service-templates.js'. Add a new handleServiceListTypes(parsed, env) function: call fetchServiceTemplates(env), then mapTemplatesToSlimList(raw), then return buildReadResponse(slimList, { format: parsed.format, max_chars: parsed.max_chars }). Wrap in try/catch via the existing handleServiceAction try/catch (which calls wrapMcpError). Add `case 'list-types': return await handleServiceListTypes(parsed, routingEnv);` to the switch in handleServiceAction. In src/mcp/tools/service.test.ts: extend the vi.mock('ofetch') factory (or add one if absent) and the vi.mock('../../api/client.js') factory to include fetchVersion stub. Add describe('service list-types', ...) with the it cases listed in <behavior>. Mock ofetch to return the slim templates object for success cases; mock ofetch to reject for failure cases. Assert isServiceErrorResult(result) === true and result.structuredContent.error.code === 'COOLIFY_FETCH_TEMPLATES_FAILED' for failure cases. Assert result.data is an array of { id, label } only (no compose, no template, no category fields) for success cases. Do not modify existing describe blocks.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/service.test.ts -t "service list-types"</automated>
  </verify>
  <done>
    - serviceActionSchema includes 'list-types' action with [...serviceReadParamKeys] allowed fields and no required fields
    - serviceActionsCatalog includes `list-types(format?, projection?)` token
    - handleServiceAction dispatches 'list-types' to handleServiceListTypes
    - handleServiceListTypes calls fetchServiceTemplates + mapTemplatesToSlimList + buildReadResponse
    - service.test.ts has describe('service list-types', ...) with at least 6 it cases covering CDN success, GitHub Raw fallback, double failure, empty {} hard error, slim shape, stable sort
    - Existing service.test.ts cases still pass
  </done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → service.list-types inputSchema | Untrusted agent/client crosses here; Zod validates action + read params before any fetch |
| service tool → jsDelivr/GitHub CDN | Outbound HTTPS to hardcoded hosts only — no user-supplied URL crosses this boundary |
| CDN response → agent-visible data | Slim { id, label } mapping strips full template/compose payload before returning to agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering / SSRF | fetchServiceTemplates URL construction | high | mitigate | Only hardcoded `cdn.jsdelivr.net/gh/coollabsio/coolify@<version>/...` and `raw.githubusercontent.com/coollabsio/coolify/<version>/...` URLs are passed to ofetch. No user-supplied URL accepted. Version is sourced from fetchVersion (Coolify API) or 'v4.x' fallback — never from agent input. (D-01, T-20-02) |
| T-20-01-02 | Denial of Service | Offline / CDN outage | medium | mitigate | Double-fetch failure (CDN + GitHub Raw) throws COOLIFY_FETCH_TEMPLATES_FAILED with recoveryHints — no silent empty list, no static fallback catalog. Agent receives actionable error. (D-03) |
| T-20-01-03 | Information Disclosure | service-templates.json payload | low | mitigate | mapTemplatesToSlimList strips everything except { id, label } — no compose YAML, no env defaults, no credentials leak. (D-04) |
| T-20-SC | Tampering | npm package installs | low | accept | No new packages — ofetch, zod already pinned in package.json per RESEARCH Package Legitimacy Audit. |
</threat_model>

<verification>
- `npx vitest run src/utils/service-templates.test.ts` green
- `npx vitest run src/mcp/tools/service.test.ts` green (existing + new list-types cases)
- `npm run build` (tsup) green
- `rg -n "'list-types'" src/mcp/tools/service.ts` returns matches in actions enum, allowedFields map, switch case, and catalog string
- `rg -c "describe\('service list-types'" src/mcp/tools/service.test.ts` returns at least 1
</verification>

<success_criteria>
- RECIPE-01 fully shipped: agent calls service.list-types and receives slim { id, label }[] from dynamic CDN fetch
- No bundled YAML catalog in the package (D-03)
- Version pinning to active instance with 'v4.x' fallback (D-02)
- Hard error on fetch failure with recoveryHints (D-03)
- Slim response — no compose/template/category bloat (D-04)
- Stable sort by id (specless ordering resolution)
- Empty {} response → hard error (specless empty-input resolution per D-03)
- fetchServiceTemplates helper reusable by Plan 20-02 (create-one-click type validation)
</success_criteria>

<output>
Create `.planning/phases/20-recipes-service-list-types/20-01-SUMMARY.md` when done.
</output>
