---
phase: 20-recipes-service-list-types
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/tools/recipe.test.ts
autonomous: true
requirements:
  - RECIPE-02
  - RECIPE-03
  - RECIPE-04
user_setup: []

must_haves:
  truths:
    - "src/mcp/tools/recipe.test.ts exists with failing (RED) describe blocks for create-git-app (RECIPE-02), create-app-db (RECIPE-03), and create-one-click (RECIPE-04)"
    - "recipe.test.ts uses dynamic import for './recipe.js' so the file can be committed before recipe.ts ships in Plan 20-02 — mirrors the Phase 11/15 husky pre-commit green pattern"
    - "vi.mock('../../api/client.js') factory in recipe.test.ts includes stubs for createPublicApplication, triggerDeploy, bulkUpdateEnvs, fetchDatabase, triggerDatabaseStart, createPostgresqlDatabase, createService, fetchService, fetchResources so tests can mock them before Plan 20-02/20-03 ship the real handlers"
    - "vi.mock('ofetch') factory in recipe.test.ts stubs ofetch so list-types validation paths can be exercised by Plan 20-02 (create-one-click validates type against list-types)"
    - "Running `npx vitest run src/mcp/tools/recipe.test.ts` fails on the new describe blocks (RED) — recipe.ts not yet implemented"
  artifacts:
    - "src/mcp/tools/recipe.test.ts (new RED scaffold file)"
  key_links:
    - "recipe.test.ts dynamic import → './recipe.js' (symbol target for Plan 20-02 to export handleRecipeAction + recipeActionSchema)"
    - "recipe.test.ts vi.mock('../../api/client.js') → 8 client function stubs (createPublicApplication, triggerDeploy, bulkUpdateEnvs, fetchDatabase, triggerDatabaseStart, createPostgresqlDatabase, createService, fetchService, fetchResources)"
  prohibitions:
    - "No recipe.ts implementation in this plan — only RED tests"
    - "No real network calls — ofetch and api/client are vi.mock'd"
    - "No bundled YAML catalog assertions — list-types is dynamic per D-01/D-03"
---

<objective>
Wave 0 RED test scaffolds for Phase 20 — recipe tool (create-git-app, create-app-db, create-one-click).

Purpose: Establish failing tests for RECIPE-02, RECIPE-03, RECIPE-04 before any recipe handler code is written. Plans 20-02 and 20-03 flip these tests GREEN without rewriting them. Mirrors the 10-00 / 11-00 / 15-00 Wave 0 pattern (it.fails + dynamic import keeps husky pre-commit green).

Output: New `src/mcp/tools/recipe.test.ts` with describe blocks that fail at runtime because `./recipe.js` does not yet export `handleRecipeAction` / `recipeActionSchema`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-recipes-service-list-types/20-CONTEXT.md
@.planning/phases/20-recipes-service-list-types/20-RESEARCH.md
@.planning/phases/20-recipes-service-list-types/20-PATTERNS.md
@.planning/phases/20-recipes-service-list-types/20-VALIDATION.md
@src/mcp/tools/service.test.ts
@src/mcp/tools/application.test.ts
</context>

## Artifacts this phase produces

- `src/mcp/tools/recipe.test.ts` — greenfield RED scaffold with three describe blocks (create-git-app, create-app-db, create-one-click), dynamic import of `./recipe.js`, vi.mock factories for `../../api/client.js` and `ofetch`. Tests fail RED at commit time because `recipe.ts` is not yet shipped (Plan 20-02).

<tasks>

<task type="tdd">
  <name>Task 1: Create recipe.test.ts RED scaffold with dynamic import + vi.mock factories</name>
  <files>src/mcp/tools/recipe.test.ts</files>
  <read_first>
    - src/mcp/tools/service.test.ts (lines 1-80 for vi.mock('../../api/client.js') factory + testEnv + mockService patterns)
    - src/mcp/tools/application.test.ts (Phase 10 RED scaffold pattern with it.fails + dynamic import)
    - .planning/phases/20-recipes-service-list-types/20-PATTERNS.md (recipe.test.ts mock setup pattern, lines 186-217)
    - .planning/phases/20-recipes-service-list-types/20-CONTEXT.md (D-05..D-20)
    - .planning/phases/20-recipes-service-list-types/20-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 2)
  </read_first>
  <behavior>
    - recipe.test.ts imports `describe, expect, it, vi, beforeEach` from vitest
    - recipe.test.ts uses dynamic import inside each it() body: `const { handleRecipeAction, recipeActionSchema } = await import('./recipe.js')` so the file type-checks without recipe.ts existing
    - vi.mock('../../api/client.js') factory includes stubs: createPublicApplication, triggerDeploy, triggerApplicationStart, bulkUpdateEnvs, fetchDatabase, triggerDatabaseStart, createPostgresqlDatabase, createMysqlDatabase, createMariadbDatabase, createMongodbDatabase, createRedisDatabase, createClickhouseDatabase, createDragonflyDatabase, createKeydbDatabase, createService, fetchService, fetchResources, fetchVersion — each as vi.fn()
    - vi.mock('ofetch') factory exports `ofetch: vi.fn()`
    - vi.mock('../../utils/manifest.js') factory stubs ManifestManager.autoUpsert / autoRemove / findResourceContext (mirrors service.test.ts pattern)
    - vi.mock('node:fs') factory stubs statSync, realpathSync, readFileSync, openSync, fstatSync, closeSync (mirrors service.test.ts pattern for repo_path + compose_file reads)
    - testEnv constant matches service.test.ts: { COOLIFY_URL, COOLIFY_TOKEN, COOLIFY_VERIFY_SSL:true, COOLIFY_MCP_LOG:'info' }
    - describe('recipe create-git-app', ...) with it cases for: (a) detects dockerfile when repo_path has Dockerfile, (b) detects dockerfile when repo_path has Dockerfile.prod (Dockerfile.* glob, D-10 full), (c) detects dockerfile when repo_path has Dockerfile.dev (D-10 full), (d) defaults to nixpacks when repo_path has no Dockerfile or Dockerfile.* match, (e) build_pack override wins over detection, (f) rejects build_pack=dockercompose with hint to service.create/create-one-click (D-12), (g) requires build_pack when repo_path omitted (D-11), (h) calls createPublicApplication with detected build_pack + git_repository + git_branch, (i) instant_deploy default true triggers deploy (D-16), (j) no confirm gate on create (D-17)
    - describe('recipe create-app-db', ...) with it cases for: (a) creates postgresql DB then app then wires DATABASE_URL env (D-13 default), (b) env_key override respected (D-13), (c) reads internal_db_url from GET /databases/{uuid} (D-14), (d) constructs fallback URL when internal_db_url absent (D-14), (e) partial failure on app create returns COOLIFY_RECIPE_PARTIAL_FAILURE with database_uuid in data and no auto-rollback (D-15), (f) connection_string masked unless reveal:true (D-19), (g) instant_deploy default true (D-16), (h) no confirm gate (D-17), (i) error result carries soft manifest hint suggesting `instance` param or manifest context per D-20
    - describe('recipe create-one-click', ...) with it cases for: (a) validates type against list-types (calls ofetch for service-templates.json) and rejects unknown type with COOLIFY_VALIDATION_ERROR (D-07), (b) delegates to service.create path (createService called with type in body) (D-07), (c) instant_deploy default true (D-16), (d) no confirm gate (D-17), (e) SSRF rejected — type must match a key in service-templates.json, no arbitrary URL passthrough (D-01, T-20-02), (f) error result carries soft manifest hint suggesting `instance` param or manifest context per D-20 (manifest hint pattern from Phase 19)
    - All it cases use `expect(...).rejects` or assert `isRecipeErrorResult(result) === true` for error paths — RED because handleRecipeAction is undefined at import time
    - No existing test files modified
  </behavior>
  <action>
    Create src/mcp/tools/recipe.test.ts. Top of file: vi.mock('../../api/client.js', () => ({ ... })) with all 18 client function stubs as vi.fn(). Then vi.mock('ofetch', () => ({ ofetch: vi.fn() })). Then vi.mock('../../utils/manifest.js', ...). Then vi.mock('node:fs', ...) with realpathSync identity, statSync/openSync/fstatSync/closeSync stubs AND a readdirSync stub returning [] (so Dockerfile.* glob tests can mock a directory listing). Then import { describe, expect, it, vi, beforeEach } from 'vitest' and the client + node:fs symbols after the mocks. Define testEnv constant. Add three describe blocks (create-git-app, create-app-db, create-one-click) with the it cases listed in <behavior>. Each it body must start with `const { handleRecipeAction, recipeActionSchema } = await import('./recipe.js')` so the test file type-checks without recipe.ts existing — the dynamic import fails at runtime, keeping tests RED and husky pre-commit green. For create-git-app Dockerfile.* cases, mock readdirSync(repoPath) to return ['Dockerfile.prod'] (or ['Dockerfile.dev']) and statSync on the matching path to return { isFile: () => true } — assert detectBuildPack resolves to 'dockerfile' (D-10 full per Dockerfile AND Dockerfile.* glob). For create-one-click validation cases, mock ofetch to return a service-templates.json-shaped object (e.g. { actualbudget: { name: 'Actual Budget' }, gitea: { name: 'Gitea' } }) and assert the handler rejects unknown types. For create-app-db partial-failure cases, mock createPostgresqlDatabase to resolve but createPublicApplication to reject, then assert COOLIFY_RECIPE_PARTIAL_FAILURE with database_uuid in data and that deleteDatabase is NOT called (no auto-rollback per D-15). For masking cases, mock fetchDatabase to return { internal_db_url: 'postgresql://user:pass@host:5432/db' } and assert the result data.connection_string is '***' unless reveal:true. For D-20 soft manifest hint cases, assert that error-result structuredContent.error.recoveryHints contains a hint string recommending `instance` param or manifest context (mirror Phase 19 manifest-hint pattern). Do NOT create recipe.ts — only the test file. Tests must fail when run.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/recipe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "describe\('recipe (create-git-app|create-app-db|create-one-click)'" src/mcp/tools/recipe.test.ts` returns 3
    - `rg -n "Dockerfile\.prod|Dockerfile\.dev" src/mcp/tools/recipe.test.ts` returns at least 2 matches (D-10 Dockerfile.* coverage)
    - `rg -n "readdirSync" src/mcp/tools/recipe.test.ts` returns at least 1 match (glob detection scaffolding)
    - `rg -n "manifest" src/mcp/tools/recipe.test.ts` returns at least 2 matches (D-20 soft manifest hint assertions in create-one-click + create-app-db error paths)
    - `rg -c "await import\('./recipe.js'\)" src/mcp/tools/recipe.test.ts` returns at least 22 (one per it case; create-git-app 10, create-app-db 9, create-one-click 6)
    - `npx vitest run src/mcp/tools/recipe.test.ts` exits non-zero with failures in the new describe blocks (RED) — because recipe.ts is not yet shipped
    - `npm run build` (tsup) remains green — dynamic import keeps type-checking clean
  </acceptance_criteria>
  <done>
    - src/mcp/tools/recipe.test.ts exists with three describe blocks (create-git-app, create-app-db, create-one-click)
    - At least 20 it cases total covering RECIPE-02 (8 cases), RECIPE-03 (8 cases), RECIPE-04 (5 cases)
    - vi.mock factories for '../../api/client.js', 'ofetch', '../../utils/manifest.js', 'node:fs' all present
    - npx vitest run src/mcp/tools/recipe.test.ts exits non-zero with failures in the new describe blocks (RED) — because recipe.ts is not yet shipped
    - npm run build (tsup) remains green — dynamic import keeps type-checking clean
  </done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → recipe.ts | RED scaffolds dynamic-import symbols that will exist after 20-02/20-03; no trust boundary crossed at test time |
| Test mocks → ofetch / api/client | vi.mock isolates test from real network and real Coolify API — no outbound HTTPS from test runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-00-01 | Information Disclosure | Test fixtures with fake connection strings | low | accept | Test-only fixtures use synthetic `postgresql://user:pass@host:5432/db` strings; no real credentials. Real masking validated in 20-03 against sanitizeFullProjection contract. |
| T-20-SC | Tampering | npm package installs | low | accept | No new packages in this plan — vitest, ofetch, zod already pinned in package.json. |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/recipe.test.ts` — new describe blocks RED (failing as expected)
- `npm run build` (tsup) — green (dynamic import keeps type-checking clean)
- `rg -c "describe\('recipe (create-git-app|create-app-db|create-one-click)'" src/mcp/tools/recipe.test.ts` returns 3
- `rg -c "await import\('./recipe.js'\)" src/mcp/tools/recipe.test.ts` returns at least 20 (one per it case)
</verification>

<success_criteria>
- All three Phase 20 recipe requirements (RECIPE-02, RECIPE-03, RECIPE-04) have at least one failing (RED) test in recipe.test.ts
- D-12 (dockercompose reject), D-13 (DATABASE_URL default + env_key override), D-14 (internal_db_url read), D-15 (no auto-rollback on partial failure), D-16 (instant_deploy default true), D-17 (no confirm gate), D-19 (masking), D-01/T-20-02 (SSRF reject) all have at least one RED test
- No existing test broken
- Tests structured so Plan 20-02 (create-git-app + create-one-click) and Plan 20-03 (create-app-db) can flip them GREEN without rewrite
- Wave 0 contract satisfied per 10-00 / 11-00 / 15-00 precedent
</success_criteria>

<output>
Create `.planning/phases/20-recipes-service-list-types/20-00-SUMMARY.md` when done.
</output>
