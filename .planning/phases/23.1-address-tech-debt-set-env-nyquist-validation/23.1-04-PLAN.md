---
phase: 23.1-address-tech-debt-set-env-nyquist-validation
plan: 04
type: execute
wave: 2
depends_on:
  - 23.1-01
files_modified:
  - docs/en/setup.md
  - skills/coolify-setup/SKILL.md
  - src/skills/skills-manifest.test.ts
autonomous: true
requirements:
  - SETUP-02
  - SKILL-02
user_setup: []

must_haves:
  truths:
    - "docs/en/setup.md states that set_env:true requires exactly one of env_file or env_content and documents apply/abort behavior (D-01, D-04, D-05)"
    - "skills/coolify-setup/SKILL.md documents env_file/env_content for set_env on greenfield and link-existing (D-01, D-02, D-03)"
    - "Skill/docs do not claim auto-detect of workspace .env without explicit param (D-01)"
  artifacts:
    - path: docs/en/setup.md
      provides: "user-facing set_env param contract"
      contains: "env_content"
    - path: skills/coolify-setup/SKILL.md
      provides: "agent playbook for set_env params"
      contains: "env_file"
  key_links:
    - from: "skills/coolify-setup/SKILL.md set_env docs"
      to: "setup.wire schema env_file/env_content"
      via: "verbatim param names matching application envs:sync"
---

<objective>
Document set_env env_file/env_content contract in user docs and coolify-setup skill so agents pass XOR params (D-01, D-02) after Plan 01 ships.

Purpose: Published schema without docs recreates the no-op confusion; SKILL-02 requires safety/param accuracy.

Output: Updated docs/en/setup.md + skills/coolify-setup/SKILL.md; skills-manifest tests still green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-CONTEXT.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-01-SUMMARY.md
@docs/en/setup.md
@skills/coolify-setup/SKILL.md
@src/mcp/tools/setup.ts
</context>

## Artifacts this phase produces

- `docs/en/setup.md` — optional flags table + set_env subsection with env_file XOR env_content, confirm implicit, conflict_policy abort.
- `skills/coolify-setup/SKILL.md` — same contract for agents (greenfield + link-existing).

<tasks>

<task type="auto">
  <name>Document set_env params in docs/en/setup.md</name>
  <reversibility rating="costly">D-01 published docs contract for env source params.</reversibility>
  <precondition>Plan 23.1-01 has landed env_file/env_content on setupActionSchema</precondition>
  <files>docs/en/setup.md</files>
  <read_first>
    - docs/en/setup.md (optional flags table ~L128-133; env sync step list)
    - src/mcp/tools/setup.ts (set_env describe + env_file/env_content fields)
    - .planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-CONTEXT.md (D-01, D-04, D-05)
  </read_first>
  <action>
    Per D-01/D-04/D-05: update docs/en/setup.md optional flags section so set_env documents required companion params env_file (local path) XOR env_content (inline .env text) — exactly one when set_env:true. State apply runs with confirm implicit and default conflict_policy abort (surface conflicts to human; retry with explicit policy via application envs:sync if needed). Note set_env works for greenfield and link-existing when application_uuid/resource is an application (not one-click service). Do not document workspace .env auto-detect. Keep existing step list and other flags unchanged except clarifying set_env row.
  </action>
  <verify>
    <automated>rg -n "env_file" docs/en/setup.md</automated>
    <automated>rg -n "env_content" docs/en/setup.md</automated>
    <automated>rg -n "conflict_policy|abort" docs/en/setup.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/en/setup.md mentions env_file and env_content near set_env
    - docs state exactly-one / XOR requirement when set_env:true
    - docs mention abort (or conflict_policy abort) for conflicts
  </acceptance_criteria>
  <done>User docs match locked set_env schema contract.</done>
</task>

<task type="auto">
  <name>Document set_env params in coolify-setup skill</name>
  <reversibility rating="costly">D-01/D-02 skill pack contract for agents.</reversibility>
  <files>skills/coolify-setup/SKILL.md, src/skills/skills-manifest.test.ts</files>
  <read_first>
    - skills/coolify-setup/SKILL.md (optional flags list ~L47-52)
    - docs/en/setup.md (updated set_env section from prior task)
    - src/skills/skills-manifest.test.ts (assertions on skill content if any)
  </read_first>
  <action>
    Per D-01/D-02/D-03 and SKILL-02: update skills/coolify-setup/SKILL.md optional flags so set_env lists env_file XOR env_content as required when enabled; note both greenfield and link-existing (application_uuid required for link-existing optional flags); note implicit confirm + conflict_policy abort on setup path; instruct agent to ask human on COOLIFY_CONFIRM_REQUIRED then retry via application envs:sync with explicit policy if overwrite/keep_remote needed. Keep skill frontmatter name coolify-setup. If skills-manifest.test.ts asserts substrings for optional flags, extend asserts for env_file/env_content. Do not add per-IDE skill copies.
  </action>
  <verify>
    <automated>rg -n "env_file" skills/coolify-setup/SKILL.md</automated>
    <automated>rg -n "env_content" skills/coolify-setup/SKILL.md</automated>
    <automated>npx vitest run src/skills/skills-manifest.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - skills/coolify-setup/SKILL.md documents env_file and env_content with set_env
    - skill mentions link-existing and/or application_uuid requirement for optional flags
    - src/skills/skills-manifest.test.ts exits 0
  </acceptance_criteria>
  <done>Agent skill documents set_env XOR params and abort safety; SKILL-02 accurate for env sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill docs → agent behavior | Incorrect docs cause silent wrong apply policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23.1-09 | Information Disclosure | skill examples with secrets | low | mitigate | Use FOO=bar style examples only; never real tokens |
| T-23.1-10 | Tampering | docs omit abort default | medium | mitigate | Explicitly document conflict_policy abort + human retry |
| T-23.1-SC | Tampering | npm installs | low | accept | Docs-only plan |
</threat_model>

<verification>
rg env_file + env_content in docs/en/setup.md and skills/coolify-setup/SKILL.md; skills-manifest tests green.
</verification>

<success_criteria>
Agents and humans see env_file XOR env_content requirement and abort-on-conflict policy for set_env.
</success_criteria>

<output>
Create `.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-04-SUMMARY.md` when done
</output>
