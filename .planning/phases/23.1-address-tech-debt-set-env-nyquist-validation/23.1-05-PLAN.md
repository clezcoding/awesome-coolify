---
phase: 23.1-address-tech-debt-set-env-nyquist-validation
plan: 05
type: execute
wave: 4
depends_on:
  - 23.1-01
  - 23.1-04
files_modified:
  - src/mcp/tools/setup.ts
  - src/mcp/tools/setup.test.ts
  - docs/en/setup.md
  - skills/coolify-setup/SKILL.md
autonomous: true
gap_closure: true
requirements:
  - SETUP-02
user_setup: []

must_haves:
  truths:
    - "On remote/local value conflicts, set_env stops and surfaces COOLIFY_CONFIRM_REQUIRED with conflict payload — no silent partial apply (D-05 via CR-01 omit)"
    - "'env' is absent from steps_completed when conflict resolution still required (D-07)"
    - "setup envs:sync delegation passes dry_run:false and confirm:true without a conflict_policy key (CR-01)"
    - "docs/en/setup.md and coolify-setup SKILL describe omit→CONFIRM_REQUIRED stop, not partial apply"
  artifacts:
    - path: src/mcp/tools/setup.ts
      provides: "set_env branch omits conflict_policy so envs:sync raises COOLIFY_CONFIRM_REQUIRED"
      contains: "envs:sync"
    - path: src/mcp/tools/setup.test.ts
      provides: "conflict test asserts CONFIRM_REQUIRED + no env step; happy paths assert no conflict_policy key"
      contains: "COOLIFY_CONFIRM_REQUIRED"
    - path: docs/en/setup.md
      provides: "set_env conflict stop + retry via application envs:sync"
      contains: "COOLIFY_CONFIRM_REQUIRED"
    - path: skills/coolify-setup/SKILL.md
      provides: "agent playbook for stop-on-conflict set_env path"
      contains: "COOLIFY_CONFIRM_REQUIRED"
  key_links:
    - from: "runOptionalWireSteps set_env envs:sync call"
      to: "validateSyncConflictPolicy(conflicts, undefined, uuid)"
      via: "omitted conflict_policy key on handleApplicationAction args"
    - from: "isApplicationErrorResult(syncResult) + rethrowHandlerError"
      to: "stepsCompleted.push('env') skipped"
      via: "throw before push on COOLIFY_CONFIRM_REQUIRED"
---

<objective>
Gap closure for VERIFICATION D-05/D-07 failure and REVIEW CR-01: stop setup-initiated env sync on value conflicts by omitting conflict_policy (preferred fix), fix the conflict unit test, and align docs/skill.

Purpose: Explicit policy on the setup path makes envs:sync partial-apply non-conflicting keys and return success — setup then marks env complete. Omitting the key restores COOLIFY_CONFIRM_REQUIRED (application.test.ts D-08 path), which is the locked D-05 stop-and-surface intent.

Output: setup.ts omit; green set_env conflict + happy-path tests; docs/skill wording matches production.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-CONTEXT.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-VERIFICATION.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-REVIEW.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-01-SUMMARY.md
@.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-04-SUMMARY.md
@src/mcp/tools/setup.ts
@src/mcp/tools/setup.test.ts
@src/mcp/tools/application.ts
@docs/en/setup.md
@skills/coolify-setup/SKILL.md
</context>

## Artifacts this plan produces

- `src/mcp/tools/setup.ts` — set_env → envs:sync call without conflict_policy key (CR-01 / D-05 intent).
- `src/mcp/tools/setup.test.ts` — happy paths assert key absent; conflict test asserts COOLIFY_CONFIRM_REQUIRED and no `'env'` in steps_completed (D-07).
- `docs/en/setup.md` — conflict stop wording matches omit→CONFIRM_REQUIRED behavior.
- `skills/coolify-setup/SKILL.md` — same agent playbook alignment (keep retry example listing overwrite / keep_remote / abort so skills-manifest regex still passes).

<tasks>

<task type="tracer" tdd="true">
  <name>Omit conflict_policy + green conflict stop path</name>
  <files>src/mcp/tools/setup.ts, src/mcp/tools/setup.test.ts</files>
  <read_first>
    - .planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-VERIFICATION.md (gaps: D-05/D-07)
    - .planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-REVIEW.md (CR-01 preferred omit; WR-02 test mock)
    - .planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-CONTEXT.md (D-05, D-07)
    - src/mcp/tools/setup.ts (set_env branch ~L757-780)
    - src/mcp/tools/application.ts (validateSyncConflictPolicy ~L2371-2394; handleApplicationEnvsSync ~L2492-2504)
    - src/mcp/tools/setup.test.ts (set_env happy paths ~L408-474, conflict test ~L524-561, resume ~L593-623)
    - src/mcp/tools/application.test.ts (apply with conflicts and no conflict_policy → COOLIFY_CONFIRM_REQUIRED ~L2764)
  </read_first>
  <behavior>
    - link-existing / greenfield / resume set_env happy paths: handleApplicationAction called with action envs:sync, dry_run false, confirm true, and the call object has no conflict_policy property; steps_completed contains env on success
    - conflict path: mock handleApplicationAction returning isError COOLIFY_CONFIRM_REQUIRED; setup result is error with that code; call args for envs:sync lack conflict_policy; when result.data.steps_completed is present it does not include env (D-07)
  </behavior>
  <action>
    Gap reason: setup passes an explicit abort-style conflict_policy so validateSyncConflictPolicy early-returns; envs:sync partial-applies and returns success; setup pushes env (VERIFICATION gap + CR-01). Preferred fix per CR-01 (implements D-05 stop-and-surface and D-07): in runOptionalWireSteps set_env branch, remove the conflict_policy property from the object passed to handleApplicationAction so the key is absent (undefined → COOLIFY_CONFIRM_REQUIRED in application.ts, same as application.test.ts no-policy case). Keep dry_run:false, confirm:true, uuid, env source XOR spread, instance (D-04, D-06). Do not add aborted/conflicts post-check branch — omit alone is enough. Update all set_env happy-path and resume expect.objectContaining blocks: drop conflict_policy expectation; after the call assertion, assert the first envs:sync call arg with expect(…).not.toHaveProperty('conflict_policy'). Rewrite the conflict test (currently mocks CONFIRM_REQUIRED — that mock becomes the real production path after omit): keep mock error envelope; assert handleApplicationAction received envs:sync without conflict_policy; assert isSetupErrorResult and error code COOLIFY_CONFIRM_REQUIRED; assert that if result carries steps_completed, it does not include env (IN-02 / D-07). Do not change application.ts.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/setup.test.ts -t set_env -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "conflict_policy" src/mcp/tools/setup.ts` exits 1 (zero matches in production setup source)
    - `rg -n "action: 'envs:sync'" src/mcp/tools/setup.ts` returns at least 1
    - `rg -n "not\.toHaveProperty\('conflict_policy'\)" src/mcp/tools/setup.test.ts` returns at least 1
    - `rg -n "COOLIFY_CONFIRM_REQUIRED" src/mcp/tools/setup.test.ts` returns at least 1 inside describe set_env
    - `npx vitest run src/mcp/tools/setup.test.ts -t set_env` exits 0
  </acceptance_criteria>
  <done>
    Setup set_env delegation omits conflict_policy; conflicts surface as COOLIFY_CONFIRM_REQUIRED via rethrowHandlerError; env step not marked complete on that path; happy-path tests assert key absence (D-05, D-07, CR-01).
  </done>
</task>

<task type="auto">
  <name>Align docs and skill with omit stop contract</name>
  <files>docs/en/setup.md, skills/coolify-setup/SKILL.md</files>
  <read_first>
    - docs/en/setup.md (set_env section ~L143-168)
    - skills/coolify-setup/SKILL.md (set_env params ~L56-77)
    - src/skills/skills-manifest.test.ts (coolify-setup set_env assertions ~L57-64 — regex requires conflict_policy…abort or abort…conflict)
    - .planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-REVIEW.md (WR-01)
  </read_first>
  <action>
    Per WR-01 after CR-01 omit: rewrite docs/en/setup.md set_env conflict sentence so it no longer claims setup defaults to an abort policy that completes with partial apply. State that setup omits conflict_policy on delegated envs:sync, so value conflicts raise COOLIFY_CONFIRM_REQUIRED (setup stops; no silent partial apply); agent asks human then retries via application envs:sync with explicit conflict_policy overwrite, keep_remote, or abort. Same rewrite in skills/coolify-setup/SKILL.md set_env params block — keep a retry code sample that includes conflict_policy with an abort option so skills-manifest.test.ts `/conflict_policy.*abort|abort.*conflict/i` still matches. Do not invent auto-detect or change XOR / modes / confirm wording beyond the conflict-stop clarification (D-01, D-04, D-05 intent).
  </action>
  <verify>
    <automated>npx vitest run src/skills/skills-manifest.test.ts -t set_env -x && npx vitest run src/mcp/tools/setup.test.ts -t set_env -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "COOLIFY_CONFIRM_REQUIRED" docs/en/setup.md` returns at least 1
    - `rg -n "COOLIFY_CONFIRM_REQUIRED" skills/coolify-setup/SKILL.md` returns at least 1
    - `rg -n "omit|omits|without conflict_policy|no conflict_policy" docs/en/setup.md skills/coolify-setup/SKILL.md` returns at least 1 across the two files
    - `rg -n "partial apply|partially appl" docs/en/setup.md skills/coolify-setup/SKILL.md` exits 1 (no matches)
    - `npx vitest run src/skills/skills-manifest.test.ts -t set_env` exits 0
  </acceptance_criteria>
  <done>
    Docs and skill match production stop-on-conflict behavior (D-05) and remain skills-manifest green (SKILL-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → setup.wire(set_env) | Agent/user supplies env_file/env_content; conflicts must not silently mutate remote |
| setup → application envs:sync | Delegation trust; policy omission is the stop gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23.1-05-01 | Tampering | setup set_env → envs:sync | high | mitigate | Omit conflict_policy so conflicts throw COOLIFY_CONFIRM_REQUIRED before apply; no env step until success (D-05/D-07) |
| T-23.1-05-02 | Information Disclosure | conflict error payload | low | accept | Conflict keys/hints already part of envs:sync CONFIRM_REQUIRED contract; no new leak surface |
| T-23.1-05-03 | Denial of Service | repeated conflict retries | low | accept | Same as application envs:sync; human must choose policy |
| T-23.1-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages in this gap plan |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/setup.test.ts -t set_env` green
- `npx vitest run src/skills/skills-manifest.test.ts -t set_env` green
- `rg -n "conflict_policy" src/mcp/tools/setup.ts` has zero matches
- Re-check VERIFICATION truths 4–5 (D-05, D-07) after execute
</verification>

<success_criteria>
- D-05 observable: conflicts surface COOLIFY_CONFIRM_REQUIRED; no silent partial apply via setup
- D-07 observable: env absent from steps_completed on conflict failure path
- CR-01 / WR-01 / WR-02 closed; SETUP-02 conflict contract complete
</success_criteria>

<output>
Create `.planning/phases/23.1-address-tech-debt-set-env-nyquist-validation/23.1-05-SUMMARY.md` when done
</output>
