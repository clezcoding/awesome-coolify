---
phase: 23-openapi-coverage-npm-release
plan: 01
type: execute
wave: 1
depends_on:
  - 23-00
files_modified:
  - package.json
  - pnpm-lock.yaml
  - scripts/openapi-coverage.mjs
  - scripts/lib/openapi-coverage-parse.mjs
  - scripts/lib/openapi-coverage-join.mjs
  - scripts/lib/openapi-coverage-render.mjs
  - docs/coverage-map.yaml
  - docs/coverage-overrides.yaml
  - docs/COVERAGE.md
  - tests/openapi-coverage.test.ts
autonomous: false
requirements:
  - OAPI-01
  - OAPI-02
user_setup: []

must_haves:
  truths:
    - "pnpm run openapi:coverage writes docs/COVERAGE.md with per-action rows (tool.action | client | openapi | bucket | reason) per D-01/D-02/D-03"
    - "pnpm run openapi:coverage -- --check exits 0 when COVERAGE.md matches generator; exits 1 when stale (D-06)"
    - "Four buckets covered|deferred|out-of-scope|gap enforced in classifyRows per D-04"
    - "Tracer slice proves 3-layer join for at least 3 actions (e.g. system.health, application.deploy, manifest.sync out-of-scope) end-to-end"
    - "Scalar parser dereferences docs/coolify_openapi.json without hand-rolled $ref walker"
    - "{ statement: 'Generator never ships in npm tarball (scripts/ excluded)', verification: backstop }"
  artifacts:
    - "scripts/openapi-coverage.mjs"
    - "scripts/lib/openapi-coverage-parse.mjs"
    - "scripts/lib/openapi-coverage-join.mjs"
    - "scripts/lib/openapi-coverage-render.mjs"
    - "docs/coverage-map.yaml (tracer subset)"
    - "docs/coverage-overrides.yaml (minimal seed)"
    - "docs/COVERAGE.md (initial committed report)"
    - "package.json openapi:coverage script + devDeps"
  key_links:
    - "openapi-coverage.mjs --check → byte-compare docs/COVERAGE.md"
    - "parse.mjs dereference → join.mjs classifyRows → render.mjs markdown"
    - "coverage-map.yaml actions → *ActionsCatalog literals (tracer subset)"
    - "vitest openapi-coverage.test.ts → lib modules (GREEN tracer cases)"
  prohibitions:
    - statement: "Do not implement full ~115-action map in tracer plan — Plan 23-02 owns expansion"
      status: enforced
      verification: "rg -c '^  - action:' docs/coverage-map.yaml | awk '$1 < 20'"
    - statement: "Do not add workflow_dispatch publish workflow (D-11)"
      status: enforced
      verification: "test ! -f .github/workflows/publish.yml"
---

<objective>
Tracer slice: end-to-end OpenAPI coverage generator — dereference spec → join 3 layers → render committed `docs/COVERAGE.md` → `--check` drift gate.

Purpose: Prove generator architecture (Scalar parse, YAML join, four buckets, committed artifact) on a minimal action subset before full map expansion in Plan 23-02. Delivers OAPI-01 + OAPI-02 core per D-01–D-06.

Output: Generator CLI + lib modules, tracer `coverage-map.yaml` / `coverage-overrides.yaml`, initial `docs/COVERAGE.md`, GREEN tracer unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md
@.planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md
@.planning/phases/23-openapi-coverage-npm-release/23-PATTERNS.md
@.planning/phases/23-openapi-coverage-npm-release/23-00-PLAN.md
@scripts/changeset-emit-new-tag.mjs
@scripts/kill-mcp.mjs
@scripts/lib/release-publish-gate.mjs
@src/api/client.ts
@src/mcp/tools/service.ts
</context>

## Artifacts this phase produces

- `scripts/openapi-coverage.mjs` — CLI entry: generate (default) or `--check` drift mode per 23-PATTERNS.md kill-mcp flag pattern.
- `scripts/lib/openapi-coverage-parse.mjs` — `indexOpenApiOperations(schema)` via `@scalar/openapi-parser` `dereference()` on `docs/coolify_openapi.json`.
- `scripts/lib/openapi-coverage-join.mjs` — `classifyRows({ operations, map, overrides, catalogs })` with four buckets per D-04; parse `*ActionsCatalog` from `src/mcp/tools/*.ts` for row inventory.
- `scripts/lib/openapi-coverage-render.mjs` — markdown renderer: title `# OpenAPI Coverage — awesome-coolify-mcp`, summary counts, per-action table.
- `docs/coverage-map.yaml` — tracer subset (~5–15 actions): at least one covered REST action, one deferred override, one out-of-scope action.
- `docs/coverage-overrides.yaml` — minimal seed (execute_command out-of-scope stub).
- `docs/COVERAGE.md` — committed generated report (D-03).
- `package.json` — `"openapi:coverage": "node scripts/openapi-coverage.mjs"` script.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Verify @scalar/openapi-parser legitimacy before install</name>
  <read_first>
    - .planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md (Package Legitimacy Audit table — SUS verdict)
    - https://www.npmjs.com/package/@scalar/openapi-parser
    - https://github.com/scalar/scalar/tree/main/packages/openapi-parser
  </read_first>
  <what-built>Package legitimacy audit flags @scalar/openapi-parser@0.28.10 and @scalar/openapi-types@0.9.3 as [SUS] (young publish age). Install blocked until human confirms official Scalar monorepo + download volume.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@scalar/openapi-parser — confirm publisher links to github.com/scalar/scalar
    2. Confirm postinstall script is null (RESEARCH verified)
    3. Open https://github.com/scalar/scalar/packages/openapi-parser — matches expected API (dereference)
    4. Type "approved" to proceed with pnpm add -D @scalar/openapi-parser @scalar/openapi-types
  </how-to-verify>
  <resume-signal>Type "approved" or describe concerns</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end OpenAPI coverage — tracer actions only</name>
  <files>package.json, pnpm-lock.yaml, scripts/openapi-coverage.mjs, scripts/lib/openapi-coverage-parse.mjs, scripts/lib/openapi-coverage-join.mjs, scripts/lib/openapi-coverage-render.mjs, docs/coverage-map.yaml, docs/coverage-overrides.yaml, docs/COVERAGE.md</files>
  <read_first>
    - .planning/phases/23-openapi-coverage-npm-release/23-PATTERNS.md (all openapi-coverage*.mjs patterns)
    - .planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md (Patterns 1–4, join schema)
    - .planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-06)
    - scripts/kill-mcp.mjs (--check argv + process.exit semantics)
    - scripts/changeset-emit-new-tag.mjs (import.meta.url root resolution)
    - src/mcp/tools/service.ts (serviceActionsCatalog format)
    - src/api/client.ts (export function names for map client column)
  </read_first>
  <action>
    After human-verify approval: pnpm add -D @scalar/openapi-parser @scalar/openapi-types. Implement ESM maintainer scripts per 23-PATTERNS.md. parse.mjs: read docs/coolify_openapi.json, dereference, export indexOpenApiOperations returning { key, operationId, tags }[]. join.mjs: load coverage-map.yaml + coverage-overrides.yaml (yaml parse from existing dep); parse *ActionsCatalog from src/mcp/tools/*.ts; emit one row per catalog action per D-02; classify into four buckets per D-04 merging overrides (openapi key + action_overrides). render.mjs: markdown table columns tool.action | client | openapi | bucket | reason. CLI: default writes docs/COVERAGE.md; --check regenerates to string and byte-compares committed file, exit 1 on mismatch with console.error message. Seed coverage-map.yaml with tracer actions: e.g. system.health → fetchHealth + GET /health; application.deploy → linked POST deploy path; manifest.sync → action_override out-of-scope in overrides yaml. Run generator once to commit docs/COVERAGE.md. Add package.json script openapi:coverage.
  </action>
  <verify>
    <automated>pnpm run openapi:coverage</automated>
    <automated>pnpm run openapi:coverage -- --check</automated>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('docs/COVERAGE.md','utf8'); if(!c.includes('tool.action')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - test -f scripts/openapi-coverage.mjs && test -f docs/COVERAGE.md
    - rg -n "dereference" scripts/lib/openapi-coverage-parse.mjs returns at least 1 match
    - rg -n "covered|deferred|out-of-scope|gap" scripts/lib/openapi-coverage-join.mjs | wc -l | awk '$1 >= 4'
    - pnpm run openapi:coverage -- --check exits 0
    - rg -n "openapi:coverage" package.json returns 1
  </acceptance_criteria>
  <done>Tracer path generates committed COVERAGE.md and --check passes</done>
</task>

<task type="auto">
  <name>Flip Wave 0 tracer tests GREEN</name>
  <files>tests/openapi-coverage.test.ts</files>
  <read_first>
    - tests/openapi-coverage.test.ts (Wave 0 RED scaffolds)
    - .planning/phases/23-openapi-coverage-npm-release/23-VALIDATION.md (23-01-01, 23-01-02 task map)
    - scripts/lib/openapi-coverage-parse.mjs
    - scripts/lib/openapi-coverage-join.mjs
    - scripts/openapi-coverage.mjs
  </read_first>
  <action>
    Convert tracer-scope it.fails cases in tests/openapi-coverage.test.ts to bare it: op count ≥136, classifyRows bucket fixtures, assertCoverageFresh/--check stale detection (temporarily corrupt COVERAGE.md in test, restore). Leave any full-map-only cases as it.fails for Plan 23-02 if present. Wire assertCoverageFresh export from openapi-coverage.mjs or test via execFile on --check.
  </action>
  <verify>
    <automated>pnpm test tests/openapi-coverage.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test tests/openapi-coverage.test.ts exits 0
    - rg -c "it\.fails" tests/openapi-coverage.test.ts may be &gt;0 only for Plan 23-02 deferred cases
    - rg -n "indexOpenApiOperations" tests/openapi-coverage.test.ts matches implemented export
  </acceptance_criteria>
  <done>Tracer unit tests GREEN; drift check covered in vitest</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer script → OpenAPI JSON | Reads committed spec only; no network at runtime |
| DevDep install → npm registry | Scalar packages [SUS] — human-verify gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | @scalar/openapi-parser install | high | mitigate | checkpoint:human-verify before pnpm add; legitimacy audit in RESEARCH |
| T-23-02 | Repudiation | stale COVERAGE.md | medium | mitigate | --check in CLI + vitest assertCoverageFresh (D-06) |
| T-23-03 | Information Disclosure | overrides.yaml reasons | low | accept | Public repo; no secrets in overrides |
| T-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [SUS] Scalar packages |
</threat_model>

<verification>
- pnpm run openapi:coverage && pnpm run openapi:coverage -- --check
- pnpm test tests/openapi-coverage.test.ts -x
</verification>

<success_criteria>
- Maintainer runs pnpm run openapi:coverage and gets committed docs/COVERAGE.md
- --check blocks stale report
- Tracer proves 3-layer join architecture
- Scalar deps installed only after human approval
</success_criteria>

<output>
Create `.planning/phases/23-openapi-coverage-npm-release/23-01-SUMMARY.md` when done.
</output>
