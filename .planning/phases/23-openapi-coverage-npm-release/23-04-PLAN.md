---
phase: 23-openapi-coverage-npm-release
plan: 04
type: execute
wave: 3
depends_on:
  - 23-02
  - 23-03
files_modified:
  - .changeset/v31-milestone-1-0-0.md
  - CONTRIBUTING.md
autonomous: false
requirements:
  - PUB-01
user_setup:
  - service: npm
    why: "OIDC trusted publishing for awesome-coolify-mcp@1.0.0"
    dashboard_config:
      - task: "Verify Trusted Publisher configured for workflow release.yml, repo clezcoding/awesome-coolify"
        location: "npmjs.com → awesome-coolify-mcp → Settings → Trusted Publishers"

must_haves:
  truths:
    - "Changeset fragment bumps awesome-coolify-mcp to 1.0.0 (major) per D-10"
    - "Release follows milestone model: Changeset PR → Version Packages → release.yml OIDC publish — no new workflow_dispatch (D-11)"
    - "release.yml OIDC contract unchanged unless blocker documented (D-12)"
    - "release-publish-gate tests still pass (existing idempotency)"
    - "npm trusted publisher verified on npmjs.com before milestone merge (PUB-01 manual gate)"
    - "{ statement: '1.0.0 publish is one-way semver on npm registry', verification: backstop }"
  artifacts:
    - ".changeset/v31-milestone-1-0-0.md"
    - "CONTRIBUTING.md (trusted publisher checklist if missing)"
  key_links:
    - "Changeset major → changeset version → release.yml npm publish"
    - "changeset-emit-new-tag.mjs → npm view idempotency gate"
  prohibitions:
    - statement: "No new workflow_dispatch publish workflow (D-11)"
      status: enforced
      verification: "git diff --name-only .github/workflows/ | rg -v '^$' || true; test ! -f .github/workflows/publish.yml"
    - statement: "Do not modify release.yml OIDC/changesets contract without documented blocker (D-12)"
      status: enforced
      verification: "git diff .github/workflows/release.yml | wc -l | awk '$1 == 0'"
---

<objective>
Ship v3.1 milestone npm release: Changeset bump to `awesome-coolify-mcp@1.0.0` via existing OIDC trusted-publishing path (PUB-01).

Purpose: Close v3.1 with one milestone publish after coverage + pack gates green. Honors D-10/D-11/D-12; reversibility gate before irreversible semver publish.

Output: Milestone Changeset fragment, trusted-publisher verification, release path documentation touch-up if needed.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md
@.planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md
@.planning/phases/23-openapi-coverage-npm-release/23-02-PLAN.md
@.planning/phases/23-openapi-coverage-npm-release/23-03-PLAN.md
@.github/workflows/release.yml
@scripts/changeset-emit-new-tag.mjs
@scripts/lib/release-publish-gate.mjs
@scripts/gsd-ensure-changeset.sh
@CONTRIBUTING.md
@tests/release-publish-gate.test.ts
</context>

## Artifacts this phase produces

- `.changeset/v31-milestone-1-0-0.md` — major bump fragment for awesome-coolify-mcp per D-10 (created after decision checkpoint).
- `CONTRIBUTING.md` — optional trusted-publisher pre-flight checklist if not already documented (npmjs.com workflow filename `release.yml`).

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Verify npm Trusted Publisher before 1.0.0</name>
  <read_first>
    - .github/workflows/release.yml (id-token: write, workflow filename release.yml)
    - .planning/phases/23-openapi-coverage-npm-release/23-RESEARCH.md (Pitfall 5, Environment Availability A2)
    - CONTRIBUTING.md (Milestone npm release section)
    - https://docs.npmjs.com/trusted-publishers
  </read_first>
  <what-built>Pre-flight for PUB-01: npmjs.com Trusted Publisher must list GitHub Actions workflow filename `release.yml`, repository `clezcoding/awesome-coolify`, package `awesome-coolify-mcp`. Not verifiable from git alone (RESEARCH A2).</what-built>
  <how-to-verify>
    1. Log in to npmjs.com → awesome-coolify-mcp → Settings → Trusted Publishers
    2. Confirm publisher exists for GitHub Actions, workflow `release.yml`, repo clezcoding/awesome-coolify
    3. Confirm workflow has permissions.id-token: write (already in release.yml)
    4. Type "approved" if configured, or describe what is missing
  </how-to-verify>
  <resume-signal>Type "approved" or describe missing npm dashboard config</resume-signal>
</task>

<task type="auto">
  <name>Verify release path unchanged + gate tests green</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - .github/workflows/release.yml
    - scripts/changeset-emit-new-tag.mjs
    - scripts/lib/release-publish-gate.mjs
    - tests/release-publish-gate.test.ts
    - .planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md (D-11, D-12)
  </read_first>
  <action>
    Confirm release.yml still uses changesets/action with publish: pnpm run changeset:emit-tag and id-token: write — no edits unless blocker found (D-12). Run pnpm test tests/release-publish-gate.test.ts -x. If CONTRIBUTING.md lacks trusted-publisher checklist, add short subsection under Milestone npm release: workflow filename must be release.yml, OIDC no NPM_TOKEN, link to npm trusted publishers docs. Do not add workflow_dispatch publish workflow (D-11).
  </action>
  <verify>
    <automated>pnpm test tests/release-publish-gate.test.ts -x</automated>
    <automated>rg -n "id-token: write" .github/workflows/release.yml returns 1</automated>
    <automated>rg -n "changeset:emit-tag" .github/workflows/release.yml returns 1</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test tests/release-publish-gate.test.ts exits 0
    - release.yml contains id-token: write and publish: pnpm run changeset:emit-tag
    - No new publish.yml workflow file
    - git diff .github/workflows/release.yml is empty (no unintended changes)
  </acceptance_criteria>
  <done>Release OIDC path verified; gate tests GREEN</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <reversibility rating="one-way">Publishing 1.0.0 to npm is irreversible — consumers pin semver; yanked versions leave registry history (D-10).</reversibility>
  <name>Confirm v3.1 milestone ships 1.0.0</name>
  <read_first>
    - .planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md (D-10, D-11)
    - package.json (current version 0.5.0)
    - scripts/gsd-ensure-changeset.sh (major bump template)
  </read_first>
  <decision>Ship awesome-coolify-mcp@1.0.0 as v3.1 milestone semver (major bump from 0.5.0)</decision>
  <context>Plans 23-01–23-03 deliver coverage map + pack gate. This decision triggers the one-way npm publish after Version Packages merge. Alternative: defer major bump to a later milestone (would violate locked D-10).</context>
  <options>
    <option id="ship-1-0-0">
      <name>Ship 1.0.0 now (D-10)</name>
      <pros>Matches locked CONTEXT decision; signals v3.1 API stability; Changesets major bump 0.5.0→1.0.0</pros>
      <cons>Irreversible semver on npm; requires trusted publisher configured</cons>
    </option>
    <option id="defer-major">
      <name>Defer major bump</name>
      <pros>Avoids immediate registry commitment</pros>
      <cons>Violates locked D-10; blocks phase PUB-01 goal</cons>
    </option>
  </options>
  <resume-signal>Select: ship-1-0-0 (required for D-10) or defer-major (escalate to user — breaks locked decision)</resume-signal>
</task>

<task type="auto">
  <name>Create milestone Changeset for 1.0.0</name>
  <files>.changeset/v31-milestone-1-0-0.md</files>
  <read_first>
    - scripts/gsd-ensure-changeset.sh (fragment template lines 135-138)
    - .planning/phases/23-openapi-coverage-npm-release/23-PATTERNS.md (.changeset major template)
    - .planning/phases/23-openapi-coverage-npm-release/23-CONTEXT.md (D-10, D-11)
    - CONTRIBUTING.md (milestone release flow)
  </read_first>
  <precondition>Decision checkpoint ship-1-0-0 approved; Plans 23-02 and 23-03 SUMMARYs green; full pnpm test passes.</precondition>
  <action>
    After ship-1-0-0 decision: create .changeset/v31-milestone-1-0-0.md with frontmatter "awesome-coolify-mcp": major and body summarizing v3.1: OpenAPI coverage map, committed COVERAGE.md, npm 1.0.0. Per D-11: this fragment lands on milestone-close chore PR; merge triggers Version Packages PR; merging that runs release.yml OIDC publish. Do not run npm publish locally. Do not use [ci skip] on PR tip.
  </action>
  <verify>
    <automated>test -f .changeset/v31-milestone-1-0-0.md && rg -n 'awesome-coolify-mcp.*major' .changeset/v31-milestone-1-0-0.md returns 1</automated>
    <automated>pnpm test -x</automated>
  </verify>
  <acceptance_criteria>
    - .changeset/v31-milestone-1-0-0.md exists with major bump for awesome-coolify-mcp
    - Changeset body mentions OpenAPI coverage and 1.0.0 milestone
    - Full test suite green before PR ship
    - No release.yml modifications in this commit
  </acceptance_criteria>
  <done>Milestone Changeset ready for Version Packages flow (PUB-01)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI release.yml → npm registry | OIDC trusted publishing; no long-lived token |
| Maintainer → npmjs.com dashboard | Trusted publisher config outside git |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-08 | Spoofing | npm publish identity | critical | mitigate | OIDC trusted publisher scoped to release.yml; human-verify dashboard config |
| T-23-09 | Tampering | release.yml changes | high | mitigate | D-12: no contract change unless blocker; git diff gate |
| T-23-10 | Repudiation | duplicate publish | medium | mitigate | changeset-emit-new-tag.mjs + npm view idempotency (existing tests) |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | No new deps in this plan |
</threat_model>

<verification>
- pnpm test tests/release-publish-gate.test.ts -x
- pnpm test (full suite before ship)
- Manual: merge Version Packages PR → confirm OIDC npm publish succeeds
</verification>

<success_criteria>
- Changeset major bump 1.0.0 created per D-10
- Trusted publisher verified
- release.yml unchanged per D-12
- Milestone release path documented
</success_criteria>

<output>
Create `.planning/phases/23-openapi-coverage-npm-release/23-04-SUMMARY.md` when done.
</output>
