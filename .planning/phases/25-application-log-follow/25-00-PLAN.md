---
phase: 25-application-log-follow
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/utils/log-follow-poll.test.ts
  - src/utils/errors.test.ts
  - src/mcp/tools/application.test.ts
  - src/mcp/tools/system.test.ts
  - tests/integration/logs-service-db-flow.test.ts
autonomous: true
requirements:
  - OBS-02
  - OBS-03
user_setup: []

must_haves:
  truths:
    - "log-follow-poll.test.ts has it.fails scaffolds for suffix-overlap dedup, idle stop, timeout stop, and 429 backoff continuation (OBS-02, D-05, D-09, D-15)"
    - "errors.test.ts has it.fails scaffold for COOLIFY_LOG_FOLLOW_TIMEOUT recovery hints referencing application.logs (OBS-02, D-10)"
    - "application.test.ts has it.fails scaffolds for follow schema acceptance, idle OK + stopped_reason idle, timeout dual-signal COOLIFY_LOG_FOLLOW_TIMEOUT + partial logs_lines, follow+deployment_uuid COOLIFY_422 (OBS-02, D-02, D-10, D-11)"
    - "application.test.ts retains existing runtime and build one-shot golden tests unchanged as it (OBS-03, D-03)"
    - "system.test.ts has it.fails scaffold expecting five capability keys including application_logs_follow (OBS-02, D-17)"
    - "logs-service-db-flow.test.ts flips follow schema rejection to it.fails acceptance (OBS-02, D-01)"
    - "All new failing cases use vitest it.fails so husky pre-commit stays green"
  artifacts:
    - path: src/utils/log-follow-poll.test.ts
      provides: "RED targets for follow poll loop"
      contains: "followApplicationLogs"
    - path: src/mcp/tools/application.test.ts
      provides: "follow handler RED + OBS-03 golden runtime/build"
      contains: "follow"
    - path: src/mcp/tools/system.test.ts
      provides: "five-key capabilities RED scaffold"
      contains: "application_logs_follow"
  key_links:
    - from: "log-follow-poll.test.ts"
      to: "src/utils/log-follow-poll.ts (Plan 25-01)"
      via: "dynamic import + fake timers"
    - from: "application.test.ts follow describe"
      to: "handleApplicationLogsFollow (Plan 25-01)"
      via: "handleApplicationAction mocked fetchApplicationLogs"
  assumptions:
    - "EDGE OBS-02 unclassified ($COVERAGE) → follow is bounded MCP-side poll on GET /applications/{uuid}/logs; terminal conditions timeout|idle only (A-EDGE-OBS-02)"
    - "EDGE OBS-03 unclassified ($COVERAGE) → one-shot paths byte-identical when follow absent/false; golden tests lock call args (A-EDGE-OBS-03)"
    - "offset ignored or rejected when follow:true per RESEARCH A3 — superRefine scaffold optional in Wave 0"
  prohibitions:
    - statement: "Do not implement production handlers or log-follow-poll.ts in Wave 0 — scaffolds only"
      status: enforced
      verification: "git diff --name-only -- src/utils/log-follow-poll.ts src/mcp/tools/application.ts src/mcp/capabilities.ts is empty after this plan"
    - statement: "Do not edit incident prompt (D-21)"
      status: enforced
      verification: "rg -n \"incident\" src/mcp/prompts/ --glob '*.ts' returns 0 changed files in this plan diff"
    - statement: "Do not implement pattern-match/until/regex stop (D-08 out of scope Phase 25)"
      status: enforced
      verification: "rg -n \"until|regex.*stop|pattern.?match\" src/utils/log-follow-poll.ts src/mcp/tools/application.ts returns 0 after phase"
---

<objective>
Wave 0 Nyquist RED scaffolds for Phase 25 — lock OBS-02/OBS-03 acceptance as `it.fails` tests before implementation.

Purpose: Plans 25-01..25-03 flip scaffolds GREEN without rewriting intent. Mirrors Phase 24/21 Wave 0 pattern.

Output: log-follow-poll, application, errors, system, and integration test RED targets; OBS-03 golden tests remain green `it`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-application-log-follow/25-CONTEXT.md
@.planning/phases/25-application-log-follow/25-RESEARCH.md
@.planning/phases/25-application-log-follow/25-PATTERNS.md
@.planning/phases/25-application-log-follow/25-VALIDATION.md
@src/utils/deploy-watch-poll.test.ts
@src/mcp/tools/application.test.ts
@src/mcp/tools/system.test.ts
@src/utils/errors.test.ts
@tests/integration/logs-service-db-flow.test.ts
</context>

## Artifacts this phase produces

- `src/utils/log-follow-poll.test.ts` — RED scaffolds for dedup, idle, timeout, 429 backoff.
- `src/utils/errors.test.ts` — RED scaffold for `COOLIFY_LOG_FOLLOW_TIMEOUT` hints.
- `src/mcp/tools/application.test.ts` — follow acceptance + handler behavior `it.fails`; OBS-03 runtime/build golden tests unchanged.
- `src/mcp/tools/system.test.ts` — five capability keys `it.fails` scaffold.
- `tests/integration/logs-service-db-flow.test.ts` — schema accepts `follow:true` `it.fails` target.

<tasks>

<task type="tdd">
  <name>Task 1: RED scaffolds for log-follow-poll unit tests</name>
  <files>src/utils/log-follow-poll.test.ts</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-CONTEXT.md (D-05, D-06, D-09, D-15, D-16)
    - .planning/phases/25-application-log-follow/25-RESEARCH.md (Pattern 3 suffix-overlap dedup; Pitfall 2)
    - .planning/phases/25-application-log-follow/25-PATTERNS.md (log-follow-poll.test.ts analog deploy-watch-poll.test.ts)
    - src/utils/deploy-watch-poll.test.ts (fake timers, timeout test pattern lines 38-66)
  </read_first>
  <behavior>
    - followApplicationLogs with overlapping snapshots appends only new tail lines (dedup)
    - followApplicationLogs stops with stoppedReason idle when no new deduped lines for idleTimeoutMs
    - followApplicationLogs stops with stoppedReason timeout when budget exhausted
    - followApplicationLogs continues poll loop on 429 when isRetryableRateLimit returns retry info
  </behavior>
  <action>
    Create src/utils/log-follow-poll.test.ts with describe('followApplicationLogs') using vi.useFakeTimers beforeEach/afterEach pattern from deploy-watch-poll.test.ts. Dynamic-import ./log-follow-poll.js inside each it.fails. Scaffold four it.fails cases: (1) dedup — mock fetchSnapshot returning overlapping line arrays, assert aggregate length; (2) idle — static snapshot, advance timers past idle_timeout, expect stoppedReason idle; (3) timeout — never-idle trickle, advance past timeoutMs, expect stoppedReason timeout; (4) 429 — fetchSnapshot throws CoolifyApiError httpStatus 429 once then succeeds, assert poll continues. Do not create log-follow-poll.ts production file in this plan.
  </action>
  <verify>
    <automated>rg -n "it\\.fails" src/utils/log-follow-poll.test.ts | wc -l | awk '{exit ($1>=4)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/utils/log-follow-poll.test.ts
    - rg -n "it\\.fails" src/utils/log-follow-poll.test.ts | wc -l | awk '{exit ($1>=4)?0:1}'
    - rg -n "followApplicationLogs" src/utils/log-follow-poll.test.ts | wc -l | awk '{exit ($1>=1)?0:1}'
    - test ! -f src/utils/log-follow-poll.ts
  </acceptance_criteria>
  <done>log-follow-poll RED scaffolds exist; production util absent.</done>
</task>

<task type="tdd">
  <name>Task 2: RED scaffolds for application/errors/system/integration follow tests</name>
  <files>src/mcp/tools/application.test.ts, src/utils/errors.test.ts, src/mcp/tools/system.test.ts, tests/integration/logs-service-db-flow.test.ts</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-CONTEXT.md (D-01, D-02, D-03, D-10, D-11, D-17, D-21)
    - .planning/phases/25-application-log-follow/25-VALIDATION.md (Wave 0 checklist)
    - .planning/phases/25-application-log-follow/25-PATTERNS.md (application.test.ts flip follow rejection; system.test.ts five keys)
    - src/mcp/tools/application.test.ts (rejects follow ~L1087; runtime golden ~L1049)
    - src/mcp/tools/deployment.test.ts (dual-signal timeout pattern ~L524-570)
    - src/mcp/tools/system.test.ts (CAPABILITY_KEYS describe ~L139-169)
    - tests/integration/logs-service-db-flow.test.ts (rejects follow ~L186)
    - src/utils/errors.test.ts (COOLIFY_WATCH_TIMEOUT hint pattern if present)
  </read_first>
  <behavior>
    - applicationLogsSchema accepts action logs uuid follow true (replaces unrecognized_keys rejection)
    - handleApplicationAction logs follow true returns stopped_reason idle on idle stop without error flag
    - handleApplicationAction logs follow true timeout throws COOLIFY_LOG_FOLLOW_TIMEOUT with partial logs_lines in error data
    - applicationLogsSchema rejects follow true with deployment_uuid as COOLIFY_422
    - RECOVERY_HINTS COOLIFY_LOG_FOLLOW_TIMEOUT mentions application.logs and same app id
    - system.version capabilities has exactly five keys including application_logs_follow
    - applicationActionSchema accepts follow true on logs action
    - Existing runtime logs default lines=100 and build logs tests remain passing it cases
  </behavior>
  <action>
    Per D-01/D-02/D-10/D-11/D-17: Replace application.test.ts test applicationLogsSchema rejects follow param with it.fails applicationLogsSchema accepts follow true for runtime uuid. Add describe application logs follow with it.fails: idle stop OK, timeout dual-signal (mirror deployment.test.ts fake timers), follow plus deployment_uuid COOLIFY_422. Keep existing runtime and build one-shot tests as regular it — do not modify their assertions (OBS-03/D-03). In errors.test.ts add it.fails asserting RECOVERY_HINTS.COOLIFY_LOG_FOLLOW_TIMEOUT exists with application.logs re-call hint. In system.test.ts update CAPABILITY_KEYS scaffold to five keys with application_logs_follow as it.fails. In logs-service-db-flow.test.ts rename rejects follow:true per D-05 to accepts follow:true schema and flip to it.fails expect success true (D-05 in Phase 25 is idle/timeout semantics, not rejection). Do not edit production src files.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "runtime logs|build logs" -x</automated>
    <automated>npx vitest run src/mcp/tools/application.test.ts src/mcp/tools/system.test.ts src/utils/errors.test.ts tests/integration/logs-service-db-flow.test.ts -x 2>&1 | rg "Tests.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "rejects follow param with unrecognized_keys" src/mcp/tools/application.test.ts | wc -l | awk '{exit ($1==0)?0:1}'
    - rg -n "application_logs_follow" src/mcp/tools/system.test.ts | wc -l | awk '{exit ($1>=1)?0:1}'
    - rg -n "COOLIFY_LOG_FOLLOW_TIMEOUT" src/utils/errors.test.ts | wc -l | awk '{exit ($1>=1)?0:1}'
    - npx vitest run src/mcp/tools/application.test.ts -t "runtime logs|build logs" exits 0
    - npx vitest run src/mcp/tools/application.test.ts -x exits 0 (it.fails cases pending, not failing suite)
  </acceptance_criteria>
  <done>All follow RED scaffolds in place; OBS-03 golden one-shot tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test scaffolds → executor | RED tests define agent-visible contracts before production code ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | follow poll loop | medium | mitigate | Wave 0 tests lock bounded timeout/idle semantics before implementation (Plan 25-01) |
| T-25-02 | Denial of Service | unbounded follow | high | mitigate | Scaffold asserts timeout and 429 backoff paths (D-13/D-15) |
| T-25-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — N/A |
</threat_model>

<verification>
- `npx vitest run src/utils/log-follow-poll.test.ts src/mcp/tools/application.test.ts src/mcp/tools/system.test.ts -x` — suite green with pending it.fails
- OBS-03 golden tests (`runtime logs`, `build logs`) pass unchanged
</verification>

<success_criteria>
- Wave 0 RED scaffolds cover OBS-02 follow behaviors and OBS-03 regression lock
- No production code files created except test files
- Husky pre-commit stays green via it.fails
</success_criteria>

<output>
Create `.planning/phases/25-application-log-follow/25-00-SUMMARY.md` when done
</output>
