---
phase: 25-application-log-follow
plan: 05
type: execute
wave: 4
depends_on:
  - "25-04"
gap_closure: true
gap_ids:
  - G-25-1
files_modified:
  - src/mcp/tools/application.ts
  - src/mcp/server.ts
  - src/mcp/tools/application.test.ts
autonomous: true
requirements:
  - OBS-02
user_setup: []

must_haves:
  truths:
    - "Live MCP call application logs with follow:true + deployment_uuid returns structuredContent.error.code COOLIFY_422 (D-02), not generic Input validation error"
    - "Handler throwValidationError path still maps Zod issue.params.code COOLIFY_422 for all applicationActionSchema superRefine guards"
    - "Existing follow/one-shot live behavior unchanged (OBS-03)"
  artifacts:
    - path: src/mcp/tools/application.ts
      provides: "applicationActionMcpSchema (boundary) + applicationActionSchema (handler parse)"
    - path: src/mcp/server.ts
      provides: "registerTool uses applicationActionMcpSchema for inputSchema"
    - path: src/mcp/tools/application.test.ts
      provides: "handleApplicationAction follow+deployment_uuid returns COOLIFY_422 structured envelope"
  key_links:
    - from: "server.registerTool inputSchema"
      to: "handleApplicationAction parseApplicationAction"
      via: "MCP SDK passes structural validation; handler runs full schema + throwValidationError"
  prohibitions:
    - statement: "Do not remove follow+deployment_uuid guard (D-02)"
      status: enforced
    - statement: "Do not change follow poll loop behavior"
      status: enforced
---

<objective>
Close UAT gap G-25-1: follow:true + deployment_uuid must return structured COOLIFY_422 through live MCP, not SDK generic validation error.

Root cause: MCP SDK validateToolInput runs full applicationActionSchema superRefine before handler; throwValidationError never executes on failure path.

Fix: Split boundary schema (structural/action-field allowlist only) from handler schema (extraRefine with COOLIFY_422 params). Register boundary schema on server.ts; keep full schema in parseApplicationAction.
</objective>

<tasks>

## Task 1: Split application MCP boundary vs handler schemas

<files>src/mcp/tools/application.ts</files>

1. Extract `applicationExtraRefine` callback (existing logs/deploy/mutation guards) from inline `createFlatActionSchema` call.
2. Export `applicationActionMcpSchema = createFlatActionSchema(..., undefined /* no extraRefine */, zodDefaultFields)` — same shape/allowlists/required fields.
3. Export `applicationActionSchema = createFlatActionSchema(..., applicationExtraRefine, zodDefaultFields)` — handler parse unchanged.
4. `parseApplicationAction` continues using `applicationActionSchema`.

## Task 2: Wire boundary schema in server + regression test

<files>src/mcp/server.ts, src/mcp/tools/application.test.ts</files>

1. `server.ts`: change `application` registerTool `inputSchema` to `withInstanceRoutingSchema(applicationActionMcpSchema)`.
2. Add handler test: `handleApplicationAction({ action: 'logs', follow: true, deployment_uuid: 'dep-x' })` → `structuredContent.error.code === 'COOLIFY_422'` with follow+deployment_uuid message.
3. Run `npx vitest run src/mcp/tools/application.test.ts -t "follow.*deployment_uuid"` green.

</tasks>

<verification>
```bash
npx vitest run src/mcp/tools/application.test.ts -t "follow.*deployment_uuid"
npm run build
```
Manual: re-run UAT test 5 live — expect `code: COOLIFY_422` in structured error.
</verification>
