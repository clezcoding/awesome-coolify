---
phase: 25-application-log-follow
plan: 01
type: execute
wave: 1
depends_on:
  - "25-00"
files_modified:
  - src/utils/deploy-watch-poll.ts
  - src/utils/log-follow-poll.ts
  - src/utils/log-follow-poll.test.ts
  - src/utils/errors.ts
  - src/utils/errors.test.ts
  - src/mcp/tools/application.ts
  - src/mcp/tools/application.test.ts
autonomous: true
requirements:
  - OBS-02
user_setup: []

must_haves:
  truths:
    - "Agent calls application.logs with follow:true and receives deduped aggregate logs_lines until idle stop with stopped_reason idle (OBS-02, D-01, D-05, D-09, D-11)"
    - "followApplicationLogs polls fetchApplicationLogs with lines per poll and suffix-overlap dedup (D-09, D-16)"
    - "Backoff interval math shared with deployment.watch via exported deploy-watch-poll helpers (D-15)"
    - "Follow timeout throws COOLIFY_LOG_FOLLOW_TIMEOUT with partial logs_lines dual-signal (D-10)"
    - "COOLIFY API errors during follow hard-stop with partial aggregate in error data (D-07)"
    - "handleApplicationLogs branches to follow only when parsed.follow === true — one-shot path untouched in this tracer (OBS-03 partial — full golden in 25-02)"
  artifacts:
    - path: src/utils/log-follow-poll.ts
      provides: "followApplicationLogs poll loop"
      exports: ["followApplicationLogs", "LogFollowOutcome"]
    - path: src/mcp/tools/application.ts
      provides: "handleApplicationLogsFollow + follow branch"
      contains: "handleApplicationLogsFollow"
    - path: src/utils/errors.ts
      provides: "COOLIFY_LOG_FOLLOW_TIMEOUT code + RECOVERY_HINTS"
      contains: "COOLIFY_LOG_FOLLOW_TIMEOUT"
  key_links:
    - from: "handleApplicationLogsFollow"
      to: "followApplicationLogs"
      via: "fetchSnapshot closure calling fetchApplicationLogs"
    - from: "followApplicationLogs"
      to: "deploy-watch-poll nextDelayMs/sleep"
      via: "shared backoff math"
    - from: "idle outcome"
      to: "buildReadResponse"
      via: "capLogOutput on aggregate then stopped_reason idle"
  assumptions:
    - "A-EDGE-OBS-02: follow terminal conditions are timeout and idle only — no lifecycle status polling (D-05)"
    - "max_chars default 20000 from sharedLogParamsSchema for follow aggregate cap (RESEARCH A1)"
    - "offset ignored when follow:true — handler does not pass offset to follow path (RESEARCH A3)"
  prohibitions:
    - statement: "Do not couple follow loop to pollDeploymentWithBackoff deployment status checks"
      status: enforced
      verification: "rg -n \"pollDeploymentWithBackoff\" src/utils/log-follow-poll.ts returns 0"
    - statement: "Do not edit incident prompt (D-21)"
      status: enforced
      verification: "no diff under src/mcp/prompts/ in this plan"
---

<objective>
Tracer vertical slice: `application.logs` + `follow:true` end-to-end through schema branch, poll util, Coolify client, and response envelope — idle happy path plus timeout error path.

Purpose: Prove MCP-side follow architecture before schema hardening and OBS-03 regression lock in Plan 25-02.

Output: `log-follow-poll.ts`, exported backoff helpers, `handleApplicationLogsFollow`, `COOLIFY_LOG_FOLLOW_TIMEOUT`, green Wave 0 follow unit/handler tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-application-log-follow/25-CONTEXT.md
@.planning/phases/25-application-log-follow/25-RESEARCH.md
@.planning/phases/25-application-log-follow/25-PATTERNS.md
@.planning/phases/25-application-log-follow/25-00-SUMMARY.md
@src/utils/deploy-watch-poll.ts
@src/mcp/tools/deployment.ts
@src/mcp/tools/application.ts
@src/utils/log-helpers.ts
@src/utils/errors.ts
</context>

## Artifacts this phase produces

- `src/utils/log-follow-poll.ts` — `followApplicationLogs` with dedup, idle, timeout, 429 retry.
- `src/utils/deploy-watch-poll.ts` — exported `nextDelayMs` and `sleep` (watch loop unchanged).
- `src/utils/errors.ts` — `COOLIFY_LOG_FOLLOW_TIMEOUT` + `RECOVERY_HINTS`.
- `src/mcp/tools/application.ts` — `follow` param on `applicationLogsSchema`, `handleApplicationLogsFollow`, top-of-handler branch.
- Green `log-follow-poll.test.ts` and follow handler tests from Wave 0.

<tasks>

<task type="tracer">
  <name>End-to-end application.logs follow:true idle stop</name>
  <reversibility rating="costly">D-01/D-09 publish follow:true on application.logs and aggregate response contract.</reversibility>
  <files>src/utils/deploy-watch-poll.ts, src/utils/log-follow-poll.ts, src/utils/log-follow-poll.test.ts, src/mcp/tools/application.ts, src/mcp/tools/application.test.ts</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-CONTEXT.md (D-01, D-05, D-06, D-09, D-11, D-13, D-15, D-16)
    - .planning/phases/25-application-log-follow/25-RESEARCH.md (Architecture diagram; Pattern 3 dedup; Pitfall 1)
    - .planning/phases/25-application-log-follow/25-PATTERNS.md (deploy-watch-poll export; handleDeploymentWatch isRetryableRateLimit)
    - src/utils/deploy-watch-poll.ts (poll loop lines 40-113; nextDelayMs lines 25-34)
    - src/mcp/tools/application.ts (handleApplicationLogs one-shot ~L1431-1491; applicationLogsSchema ~L155-207)
    - src/mcp/tools/deployment.ts (isRetryableRateLimit ~L389-398)
    - src/utils/log-helpers.ts (capLogOutput)
  </read_first>
  <behavior>
    - followApplicationLogs dedupes overlapping snapshots and returns stoppedReason idle when no new lines for idleTimeoutMs
    - handleApplicationLogs with follow true calls followApplicationLogs then buildReadResponse with logs_lines, stopped_reason idle, poll_count
    - handleApplicationLogs without follow still runs existing one-shot runtime and build branches
    - Default timeout 120s and idle_timeout 60s applied in follow handler when omitted (D-13, D-06)
  </behavior>
  <action>
    Per D-01/D-05/D-09/D-11/D-15/D-16: Export nextDelayMs and sleep from deploy-watch-poll.ts without changing pollDeploymentWithBackoff behavior. Create log-follow-poll.ts with appendDedupedLines suffix-overlap helper and followApplicationLogs accepting fetchSnapshot, timeoutMs, idleTimeoutMs, minIntervalMs, maxIntervalMs, isRetryableRateLimit, random. Loop: fetch snapshot string, split lines, dedupe into aggregate, reset idle clock on new lines, check idle timeout then budget timeout, backoff sleep between polls. Implement handleApplicationLogsFollow in application.ts: resolve uuid via resolveAppMutationUuid, call followApplicationLogs with fetchApplicationLogs closure (lines per poll from parsed.lines ?? 100), cap aggregate via capLogOutput, return buildReadResponse with uuid, logs_lines, logs_truncated, total_lines, stopped_reason, poll_count. Add if (parsed.follow === true) return handleApplicationLogsFollow at top of handleApplicationLogs. Extend applicationLogsSchema with follow boolean optional default false, idle_timeout, min_interval, max_interval, timeout optional — apply handler defaults 120/60/3/30 only when follow true (do not set global zodDefault timeout 120 on applicationActionSchema yet — Plan 25-02). Flip log-follow-poll.test.ts and idle it.fails in application.test.ts to it.
  </action>
  <verify>
    <automated>npx vitest run src/utils/log-follow-poll.test.ts -x</automated>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "idle" -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "export function nextDelayMs|export const sleep" src/utils/deploy-watch-poll.ts | wc -l | awk '{exit ($1>=1)?0:1}'
    - rg -n "followApplicationLogs" src/utils/log-follow-poll.ts | wc -l | awk '{exit ($1>=1)?0:1}'
    - rg -n "handleApplicationLogsFollow" src/mcp/tools/application.ts | wc -l | awk '{exit ($1>=1)?0:1}'
    - rg -n "pollDeploymentWithBackoff" src/utils/log-follow-poll.ts | wc -l | awk '{exit ($1==0)?0:1}'
    - npx vitest run src/utils/log-follow-poll.test.ts exits 0
    - npx vitest run src/mcp/tools/application.test.ts -t "idle" exits 0
  </acceptance_criteria>
  <done>Single follow:true path works end-to-end with idle stop and deduped aggregate.</done>
</task>

<task type="auto" tdd="true">
  <name>Timeout dual-signal + API error partial aggregate</name>
  <reversibility rating="costly">D-07/D-10 lock error codes and partial-log contract on follow failures.</reversibility>
  <files>src/utils/errors.ts, src/utils/errors.test.ts, src/utils/log-follow-poll.ts, src/mcp/tools/application.ts, src/mcp/tools/application.test.ts</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-CONTEXT.md (D-07, D-10)
    - .planning/phases/25-application-log-follow/25-PATTERNS.md (COOLIFY_WATCH_TIMEOUT analog; deployment.test.ts dual-signal)
    - src/utils/errors.ts (COOLIFY_WATCH_TIMEOUT lines 26, 137-140)
    - src/mcp/tools/deployment.ts (handleDeploymentWatch timeout throw ~L432-447)
    - src/mcp/tools/application.test.ts (timeout it.fails scaffold)
  </read_first>
  <behavior>
    - Follow budget exhaustion throws CoolifyApiError COOLIFY_LOG_FOLLOW_TIMEOUT with data containing partial logs_lines, stopped_reason timeout, timed_out true, elapsed_seconds
    - Non-429 Coolify API error during follow throws structured error with partial aggregate collected so far in data
    - RECOVERY_HINTS COOLIFY_LOG_FOLLOW_TIMEOUT advises re-call application.logs with same app id and optional timeout increase
  </behavior>
  <action>
    Per D-10/D-07: Add COOLIFY_LOG_FOLLOW_TIMEOUT to CoolifyErrorCode union and RECOVERY_HINTS with application.logs re-call hints (default 120s mention). In handleApplicationLogsFollow when outcome stoppedReason timeout throw CoolifyApiError mirroring deployment watch dual-signal — include capped partial body in data. On followApplicationLogs non-429 throw, catch in handler and rethrow CoolifyApiError with partial logs_lines in data. Ensure log-follow-poll propagates non-retryable errors after partial aggregate attached. Flip errors.test.ts and application.test.ts timeout it.fails to it; add it.fails or it for API error partial aggregate case. Flip remaining log-follow-poll timeout test to it if not already green.
  </action>
  <verify>
    <automated>npx vitest run src/utils/errors.test.ts src/mcp/tools/application.test.ts -t "timeout|COOLIFY_LOG_FOLLOW" -x</automated>
    <automated>npx vitest run src/utils/log-follow-poll.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "COOLIFY_LOG_FOLLOW_TIMEOUT" src/utils/errors.ts | wc -l | awk '{exit ($1>=2)?0:1}'
    - npx vitest run src/mcp/tools/application.test.ts -t "timeout" exits 0
    - npx vitest run src/utils/errors.test.ts -t "COOLIFY_LOG_FOLLOW" exits 0
    - npx vitest run src/utils/log-follow-poll.test.ts exits 0
  </acceptance_criteria>
  <done>Timeout dual-signal and API-error partial logs ship; Wave 0 timeout/error tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → MCP application.logs | Untrusted follow params (timeout, intervals) |
| MCP → Coolify API | Bearer token; repeated GET /applications/{uuid}/logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Denial of Service | followApplicationLogs | high | mitigate | timeoutMs default 120s, idle_timeout 60s, exponential backoff + 429 Retry-After (D-13/D-15) |
| T-25-02 | Information Disclosure | partial logs on error | medium | accept | D-07 intentional — same trust model as watch timeout snapshot |
| T-25-03 | Tampering | log aggregate | low | accept | Read-only upstream; no mutation |
| T-25-SC | Tampering | npm installs | high | accept | No new packages — N/A |
</threat_model>

<verification>
- `npx vitest run src/utils/log-follow-poll.test.ts src/mcp/tools/application.test.ts -t "follow|idle|timeout" -x`
- `npm test` after plan (wave merge gate)
</verification>

<success_criteria>
- OBS-02 follow idle and timeout paths callable via application.logs follow:true
- deploy-watch-poll watch tests still pass unchanged
- Wave 0 follow scaffolds flipped green for poll util and handler
</success_criteria>

<output>
Create `.planning/phases/25-application-log-follow/25-01-SUMMARY.md` when done
</output>
