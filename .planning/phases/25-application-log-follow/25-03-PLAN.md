---
phase: 25-application-log-follow
plan: 03
type: execute
wave: 2
depends_on:
  - "25-01"
files_modified:
  - src/mcp/capabilities.ts
  - src/mcp/tools/system.test.ts
  - docs/coverage-map.yaml
  - docs/COVERAGE.md
  - README.md
  - README.de.md
autonomous: true
requirements:
  - OBS-02
  - OBS-03
user_setup: []

must_haves:
  truths:
    - "system.version capabilities includes application_logs_follow with supported true and coolify_min_version 4.1.2 (OBS-02, D-17, D-18)"
    - "Capability is soft guidance only — no Zod hard-block on follow (D-19)"
    - "actions catalog and application tool description mention follow:true and system.version capability check (D-04, D-20)"
    - "README EN and DE include one-sentence runtime log follow callout (D-20)"
    - "coverage-map.yaml and COVERAGE.md document application.logs follow:true MCP polling note (D-22, OBS-02)"
    - "incident prompt untouched (D-21)"
  artifacts:
    - path: src/mcp/capabilities.ts
      provides: "application_logs_follow capability entry"
      contains: "application_logs_follow"
    - path: docs/coverage-map.yaml
      provides: "OBS-02 coverage note on application.logs row"
      contains: "application.logs"
    - path: README.md
      provides: "EN follow discovery sentence"
      contains: "follow"
  key_links:
    - from: "COOLIFY_412_CAPABILITIES"
      to: "handleSystemAction version"
      via: "capabilities field passthrough"
    - from: "README capability callout"
      to: "system.version"
      via: "application_logs_follow key name"
  assumptions:
    - "COVERAGE.md may be hand-edited or regen via existing script — match project Phase 24 pattern"
  prohibitions:
    - statement: "Do not edit incident MCP prompt content (D-21)"
      status: enforced
      verification: "git diff --name-only | rg 'prompts/incident' returns empty"
    - statement: "Do not add service/DB log follow capability keys"
      status: enforced
      verification: "rg -n \"service_logs|database_logs\" src/mcp/capabilities.ts returns 0"
---

<objective>
Ship capability discovery and docs for application log follow — `application_logs_follow` flag, coverage row, README EN/DE note, catalog strings.

Purpose: Agents discover follow via system.version (D-17–D-19) and public docs without Phase 26 incident prompt scope.

Output: Fifth capability key, green system.test.ts, updated coverage map + READMEs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-application-log-follow/25-CONTEXT.md
@.planning/phases/25-application-log-follow/25-RESEARCH.md
@.planning/phases/25-application-log-follow/25-PATTERNS.md
@.planning/phases/25-application-log-follow/25-01-SUMMARY.md
@src/mcp/capabilities.ts
@src/mcp/tools/system.ts
@src/mcp/tools/system.test.ts
@docs/coverage-map.yaml
@README.md
</context>

## Artifacts this phase produces

- `application_logs_follow` entry in `src/mcp/capabilities.ts`.
- Green `system.test.ts` five-key capabilities describe.
- `docs/coverage-map.yaml` + `docs/COVERAGE.md` OBS-02 note on `application.logs` row.
- README.md + README.de.md follow sentence near capability discovery callout (~L732).

<tasks>

<task type="auto">
  <name>application_logs_follow capability + system tests</name>
  <reversibility rating="costly">D-17 publishes fifth capability key on system.version.</reversibility>
  <files>src/mcp/capabilities.ts, src/mcp/tools/system.test.ts</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-CONTEXT.md (D-17, D-18, D-19)
    - .planning/phases/25-application-log-follow/25-PATTERNS.md (capabilities.ts application_logs analog)
    - src/mcp/capabilities.ts (COOLIFY_412_CAPABILITIES)
    - src/mcp/tools/system.test.ts (CAPABILITY_KEYS describe)
    - src/mcp/tools/system.ts (capabilities passthrough ~L188)
  </read_first>
  <action>
    Per D-17/D-18/D-19: Add application_logs_follow sibling entry to COOLIFY_412_CAPABILITIES with supported true, coolify_min_version 4.1.2, note string per RESEARCH capability entry (MCP polling on GET /applications/{uuid}/logs). Update system.test.ts CAPABILITY_KEYS to five keys including application_logs_follow; fix test title count; flip it.fails to it. Assert each capability value has supported boolean and coolify_min_version string. Do not add Zod gate in application handler for missing capability (D-19 soft guidance only).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/system.test.ts -t capabilities -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "application_logs_follow" src/mcp/capabilities.ts | wc -l | awk '{exit ($1>=1)?0:1}'
    - npx vitest run src/mcp/tools/system.test.ts -t capabilities exits 0
    - rg -n "application_logs_follow" src/mcp/tools/system.test.ts | wc -l | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <done>system.version exposes five capabilities including application_logs_follow; tests green.</done>
</task>

<task type="auto">
  <name>Catalog, coverage map, README EN/DE follow note</name>
  <files>src/mcp/tools/application.ts, docs/coverage-map.yaml, docs/COVERAGE.md, README.md, README.de.md</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-CONTEXT.md (D-04, D-20, D-22, D-21)
    - .planning/phases/25-application-log-follow/25-PATTERNS.md (README ~L732 callout; coverage-map application.logs row)
    - src/mcp/tools/application.ts (applicationActionsCatalog ~L289; tool register description)
    - docs/coverage-map.yaml (application.logs row ~L18-20)
    - README.md (capability discovery block ~L732)
    - README.de.md (German parity ~L732)
  </read_first>
  <action>
    Per D-04/D-20/D-22: If not already done in Plan 25-02, ensure applicationActionsCatalog logs token lists follow timeout idle_timeout min_interval max_interval and short note to check system.version capabilities.application_logs_follow. Add one sentence to README.md and README.de.md capability callout: runtime log follow via application.logs follow true; bounded polling until idle or timeout; check application_logs_follow flag. Update docs/coverage-map.yaml application.logs row with note column or inline comment documenting MCP-side follow:true bounded polling (OBS-02). Regenerate or hand-update docs/COVERAGE.md matching row — use npm run script if project has coverage regen from Phase 23/24. Do not modify incident prompt files (D-21).
  </action>
  <verify>
    <automated>rg -n "application_logs_follow|follow:true" README.md README.de.md docs/coverage-map.yaml | wc -l | awk '{exit ($1>=3)?0:1}'</automated>
    <automated>npx vitest run src/mcp/tools/system.test.ts -t capabilities -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "follow" README.md | wc -l | awk '{exit ($1>=1)?0:1}'
    - rg -n "follow" README.de.md | wc -l | awk '{exit ($1>=1)?0:1}'
    - rg -n "follow" docs/coverage-map.yaml | wc -l | awk '{exit ($1>=1)?0:1}'
    - git diff --name-only | rg "incident" | wc -l | awk '{exit ($1==0)?0:1}'
    - npm test exits 0
  </acceptance_criteria>
  <done>Public discovery docs and coverage row updated; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs → agent | Capability strings are advisory only (D-19) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Information Disclosure | README follow docs | low | accept | Same trust model as existing application.logs |
| T-25-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
- `npx vitest run src/mcp/tools/system.test.ts -t capabilities -x`
- `npm test` full suite before phase verify-work
- Manual: README EN/DE parity spot-check
</verification>

<success_criteria>
- D-17–D-22 satisfied except D-21 (deferred Phase 26)
- OBS-02 discoverable via system.version + docs
- No incident prompt edits
</success_criteria>

<output>
Create `.planning/phases/25-application-log-follow/25-03-SUMMARY.md` when done
</output>
