---
phase: 25-application-log-follow
plan: 04
type: execute
wave: 3
depends_on:
  - "25-03"
files_modified:
  - src/mcp/tools/application.test.ts
  - .planning/phases/25-application-log-follow/25-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - OBS-02
  - OBS-03
user_setup: []

must_haves:
  truths:
    - "Idle stop fires when every poll returns empty snapshots — stopped_reason idle success, not COOLIFY_LOG_FOLLOW_TIMEOUT (D-05, D-11; VERIFICATION gap #1 / WR-01)"
    - "follow:true interval refines reject invalid ordering when only min_interval or only max_interval is set, using resolved defaults 3s/30s (VERIFICATION gap #2 / WR-03)"
    - "25-VERIFICATION.md reflects gaps closed — truth #2 verified, OBS-02 satisfied, status passed"
    - "OBS-03 one-shot regression tests remain green — no handler/schema behavior changes in this plan"
  artifacts:
    - path: src/utils/log-follow-poll.test.ts
      provides: "WR-01 unit regression stops idle when snapshots stay empty"
      contains: "stops idle when snapshots stay empty"
    - path: src/mcp/tools/application.test.ts
      provides: "Handler empty-logs idle + nested schema one-bound interval tests"
    - path: .planning/phases/25-application-log-follow/25-VERIFICATION.md
      provides: "Re-verification report with gaps closed"
  key_links:
    - from: "followApplicationLogs pollCount===1 idle clock"
      to: "handleApplicationLogsFollow stopped_reason idle"
      via: "empty fetchApplicationLogs logs string end-to-end test"
    - from: "applicationLogsSchema superRefine"
      to: "applicationActionSchema extraRefine"
      via: "both compare min_interval ?? 3 vs max_interval ?? 30"
  prohibitions:
    - statement: "Do not reimplement follow poll loop or handler follow branch (core feature shipped in 25-01..25-03)"
      status: enforced
      verification: "git diff --name-only excludes src/utils/log-follow-poll.ts and src/mcp/tools/application.ts"
    - statement: "Do not edit incident prompt (D-21)"
      status: enforced
      verification: "git diff --name-only | rg 'prompts' returns empty"
---

<objective>
Close Phase 25 verification gaps #1 and #2 after REVIEW-FIX commits (c303118, 6519440): confirm WR-01 empty-snapshot idle stop and WR-03 one-bound interval validation via regression tests, then re-verify and update 25-VERIFICATION.md.

Purpose: VERIFICATION scored 8/10 with two blocking/partial gaps; fixes already landed — this plan locks behavior in tests and updates the verification artifact so OBS-02 can mark satisfied.

Output: Green WR-01/WR-03 regression tests (including handler + nested schema coverage), updated 25-VERIFICATION.md with gaps_closed status.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-application-log-follow/25-CONTEXT.md
@.planning/phases/25-application-log-follow/25-RESEARCH.md
@.planning/phases/25-application-log-follow/25-VERIFICATION.md
@.planning/phases/25-application-log-follow/25-REVIEW.md
@.planning/phases/25-application-log-follow/25-REVIEW-FIX.md
@.planning/phases/25-application-log-follow/25-03-SUMMARY.md
@src/utils/log-follow-poll.ts
@src/utils/log-follow-poll.test.ts
@src/mcp/tools/application.ts
@src/mcp/tools/application.test.ts
</context>

## Artifacts this phase produces

- `src/mcp/tools/application.test.ts` — New handler it: `fetchApplicationLogs` always returns empty `logs` string; `follow:true` with short `idle_timeout` resolves `stopped_reason: idle`, `logs_lines` empty array, no `COOLIFY_LOG_FOLLOW_TIMEOUT` error (WR-01 handler backstop; unit test already in `log-follow-poll.test.ts`).
- `src/mcp/tools/application.test.ts` — New nested-schema it: `applicationLogsSchema.safeParse` with `follow:true`, `uuid`, `max_interval:2` only → fail with min_interval/max_interval ordering message (WR-03; flat `applicationActionSchema` test already at ~L186).
- `.planning/phases/25-application-log-follow/25-VERIFICATION.md` — Re-verified frontmatter (`status: passed`, `gaps: []` or resolved entries), truth #2 row ✓ VERIFIED, OBS-02 row ✓ SATISFIED, score 10/10, note REVIEW-FIX commit refs.

## Multi-Source Coverage Audit (gap_closure scope)

| SOURCE | ID | Item | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Bounded watch-style app log follow + OBS-03 unchanged | 25-00..03 + **25-04** | COVERED | Gap closure completes idle-on-empty truth |
| REQ | OBS-02 | Follow with bounded polling until timeout/terminal | 25-00..03 + **25-04** | COVERED | Gap #1 blocked OBS-02 satisfaction |
| REQ | OBS-03 | One-shot runtime/build paths unchanged | 25-02 | COVERED | Regression suite re-run in Task 2; no code edits |
| CONTEXT | D-05 | Stop on timeout OR idle | **25-04** | COVERED | WR-01 empty-snapshot idle |
| CONTEXT | D-11 | Idle = soft success + stopped_reason idle | **25-04** | COVERED | Handler test asserts no error flag |
| RESEARCH | — | Idle clock on quiet apps | **25-04** | COVERED | c303118 + tests |
| VERIFICATION | gap #1 | Empty snapshots never idle-stop | **25-04** | COVERED | Confirm fix + handler backstop test |
| VERIFICATION | gap #2 | One-bound interval ordering | **25-04** | COVERED | Confirm fix + nested schema test |
| REVIEW | WR-01 | Empty runtime logs idle | **25-04** | COVERED | Fixed c303118 — verify only |
| REVIEW | WR-03 | min/max one bound | **25-04** | COVERED | Fixed 6519440 — verify only |
| REVIEW | WR-02 | Flat timeout min(10) | — | EXCLUDED | Fixed 76a325b; not a VERIFICATION gap |
| REVIEW | IN-01 | timeout ignored one-shot | — | EXCLUDED | Info only |

<tasks>

<task type="auto">
  <name>Task 1: Lock WR-01 empty-snapshot idle stop (poll unit + handler E2E)</name>
  <files>src/mcp/tools/application.test.ts</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-VERIFICATION.md — gaps[0] truth, reason, missing items
    - .planning/phases/25-application-log-follow/25-REVIEW-FIX.md — WR-01 commit c303118 summary
    - src/utils/log-follow-poll.ts — lines 76-89 pollCount===1 idle clock branch
    - src/utils/log-follow-poll.test.ts — existing it `stops idle when snapshots stay empty` (~L44-68)
    - src/mcp/tools/application.test.ts — describe `application logs follow` (~L1506+); sibling `returns stopped_reason idle on idle stop without error flag` uses static lines not empty API response
    - .planning/phases/25-application-log-follow/25-PATTERNS.md — analog `deployment.test.ts` follow/idle test patterns
    - src/mcp/tools/application.ts — handleApplicationLogsFollow stopped_reason idle envelope (~L1721-1733)
  </read_first>
  <action>
    Per D-05/D-11 and VERIFICATION gap #1 (WR-01): **verify only** — do not edit `log-follow-poll.ts` or `application.ts` unless a test run proves the fix is missing (unlikely; c303118 landed).

    1. Run `npx vitest run src/utils/log-follow-poll.test.ts -t "stops idle when snapshots stay empty"` — must pass. If red, halt and report — do not re-ship poll loop in this plan; escalate to orchestrator.

    2. Confirm implementation: `rg -n "pollCount === 1 && !hadNewLines" src/utils/log-follow-poll.ts` returns the idle-clock starter (c303118).

    3. Add one handler-level it under `describe('application logs follow')`: mock `fetchApplicationLogs` to always resolve `{ logs: '' }` (or whitespace-only). Call `handleApplicationAction` with `action: 'logs'`, `uuid`, `follow: true`, `idle_timeout: 1`, `min_interval: 1`, `max_interval: 1`, `timeout: 120`. Assert `isApplicationErrorResult(result)` is false, `data.stopped_reason === 'idle'`, aggregate/logs_lines empty. Title must reference empty runtime logs / quiet app / WR-01. Use fake timers only if needed to match sibling idle test pattern.

    4. Do not modify `src/utils/log-follow-poll.ts`, `src/mcp/tools/application.ts`, or one-shot OBS-03 golden tests.
  </action>
  <verify>
    <automated>npx vitest run src/utils/log-follow-poll.test.ts -t "stops idle when snapshots stay empty" &amp;&amp; npx vitest run src/mcp/tools/application.test.ts -t "empty|quiet|WR-01|snapshots stay empty"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pollCount === 1 && !hadNewLines" src/utils/log-follow-poll.ts` exits 0
    - `npx vitest run src/utils/log-follow-poll.test.ts -t "stops idle when snapshots stay empty"` exits 0
    - New handler it exists under application logs follow describe asserting stopped_reason idle on perpetually empty API logs
    - New handler it asserts no COOLIFY_LOG_FOLLOW_TIMEOUT / error envelope on empty-log idle path
  </acceptance_criteria>
  <done>WR-01 gap closable: empty snapshots idle-stop proven at poll-helper and handler layers.</done>
</task>

<task type="auto">
  <name>Task 2: Lock WR-03 nested schema interval guard + re-verify VERIFICATION.md</name>
  <files>src/mcp/tools/application.test.ts, .planning/phases/25-application-log-follow/25-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/25-application-log-follow/25-VERIFICATION.md — gaps[1] truth, reason, missing items; full report structure for update
    - .planning/phases/25-application-log-follow/25-REVIEW-FIX.md — WR-03 commit 6519440
    - src/mcp/tools/application.ts — applicationLogsSchema superRefine ~L255-266 and applicationActionSchema extraRefine ~L795-806 (both use ?? 3 / ?? 30)
    - src/mcp/tools/application.test.ts — flat schema test `rejects max_interval below default min_interval when only max_interval set` (~L186-202); applicationLogsSchema tests block (~L1252+)
  </read_first>
  <action>
    Per VERIFICATION gap #2 (WR-03): **verify only** — do not edit `application.ts` unless schema test proves refine still missing (unlikely; 6519440 landed).

    1. Confirm both refines: `rg -n "min_interval \\?\\? 3" src/mcp/tools/application.ts` shows matches inside follow:true branches for nested and flat schemas.

    2. Add `applicationLogsSchema` it mirroring flat test: `safeParse({ uuid: 'app-uuid-1', follow: true, max_interval: 2 })` → `success: false` with issue message matching min_interval/max_interval ordering. Optional second case: `min_interval: 31` only → reject (resolved max 30). Do not duplicate flat `applicationActionSchema` test verbatim — nested schema path must be covered.

    3. Run follow regression bundle: `npx vitest run src/utils/log-follow-poll.test.ts src/mcp/tools/application.test.ts -t "follow|idle|timeout|runtime logs|build logs|min_interval|max_interval"`.

    4. Update `.planning/phases/25-application-log-follow/25-VERIFICATION.md`:
       - Frontmatter: `status: passed`, `score: 10/10`, clear `gaps:` array (empty or mark both gaps resolved with fix refs c303118/6519440)
       - Truth table row #2: ✓ VERIFIED with evidence (pollCount idle clock + tests)
       - Requirements OBS-02: ✓ SATISFIED
       - Gaps Summary: state both gaps closed; note WR-02/IN-01 remain informational only
       - Set `re-verification: Yes` with current timestamp

    5. Do not edit implementation files beyond `application.test.ts` and VERIFICATION.md.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "max_interval below default|applicationLogsSchema.*max_interval|min_interval" &amp;&amp; npx vitest run src/utils/log-follow-poll.test.ts src/mcp/tools/application.test.ts -t "follow|idle|runtime logs|build logs" &amp;&amp; npx vitest run tests/integration/logs-service-db-flow.test.ts -t "follow|runtime one-shot"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "min_interval \\?\\? 3" src/mcp/tools/application.ts | wc -l | awk '{exit ($1>=2)?0:1}'`
    - `applicationLogsSchema` test rejects follow:true with only max_interval:2
    - `npx vitest run src/mcp/tools/application.test.ts -t "runtime logs|build logs"` exits 0 (OBS-03 lock)
    - 25-VERIFICATION.md frontmatter status is passed and gaps section shows closure
    - OBS-02 row in VERIFICATION requirements table shows SATISFIED
  </acceptance_criteria>
  <done>WR-03 gap closed; Phase 25 verification report shows 10/10 truths and OBS-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Agent → application.logs follow params | Untrusted interval bounds validated at schema layer (WR-03) |
| Test mocks → handler | Synthetic empty logs; no production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-25-04-01 | Denial of Service | follow poll loop | high | accept | No runtime change — existing timeout/idle caps from 25-01 remain |
| T-25-04-02 | Tampering | schema refine | low | mitigate | WR-03 tests lock resolved-default ordering on both schema entry paths |
| T-25-SC | Tampering | npm installs | high | accept | No new packages in this gap plan |

</threat_model>

<verification>
- `npx vitest run src/utils/log-follow-poll.test.ts`
- `npx vitest run src/mcp/tools/application.test.ts -t "follow|idle|runtime logs|build logs|min_interval"`
- `npx vitest run tests/integration/logs-service-db-flow.test.ts -t "follow|runtime one-shot"`
- 25-VERIFICATION.md status passed, gaps empty
</verification>

<success_criteria>
- VERIFICATION gap #1 (empty-snapshot idle) and gap #2 (one-bound intervals) closed with green automated tests
- OBS-02 marked satisfied in updated verification report
- OBS-03 regression tests unchanged and green
- No edits to `log-follow-poll.ts` or `application.ts` production code
</success_criteria>

<output>
Create `.planning/phases/25-application-log-follow/25-04-SUMMARY.md` when done
</output>
