---
phase: 12-environment-variables-smart-sync
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/mcp/tools/service.ts
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04
  - ENV-06
must_haves:
  truths:
    - "service.ts dispatches envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update actions"
    - "All four flags accepted on service create and bulk-update (service supports is_preview per OpenAPI)"
    - "envs:delete and envs:bulk-update require confirm:true else throw COOLIFY_CONFIRM_REQUIRED"
    - "All env responses mask value as `***` unless reveal:true"
    - "No envs:sync action on service (D-09 app-only)"
    - "All 12-00 service.test.ts RED tests for the six actions now pass GREEN"
  artifacts:
    - "src/mcp/tools/service.ts (extended with envs:* CRUD + bulk branches)"
  key_links:
    - "Handler calls fetchEnvs / createEnv / updateEnvViaBulk / deleteEnv with resource 'service'"
    - "Handler applies sanitizeFullProjection unless reveal:true"
    - "Confirm gates use COOLIFY_CONFIRM_REQUIRED (D-13, D-11)"
---

<objective>
Add envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update to the service MCP tool. Mirrors 12-02 but for services. No sync (D-09 app-only). Service supports all four flags including is_preview per OpenAPI.

Purpose: Parallel delivery of service env CRUD + bulk alongside application (12-02) and database (12-04).
Output: Extended src/mcp/tools/service.ts. All 12-00 service.test.ts RED tests flip GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md
@.planning/phases/12-environment-variables-smart-sync/12-PATTERNS.md
@.planning/phases/12-environment-variables-smart-sync/12-VALIDATION.md
@.planning/phases/12-environment-variables-smart-sync/COVERAGE.md
@.planning/phases/12-environment-variables-smart-sync/12-01-SUMMARY.md
@.planning/phases/12-environment-variables-smart-sync/12-02-SUMMARY.md
@src/mcp/tools/service.ts
@src/mcp/tools/application.ts
@src/api/client.ts
@src/utils/projections.ts
@src/utils/errors.ts
@tests/unit/service.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod schemas for service envs:list/get/create/update/delete/bulk-update</name>
  <read_first>
    - src/mcp/tools/service.ts (existing schema shape, action union, resolveServiceMutationUuid)
    - src/mcp/tools/application.ts (12-02 envs:* schemas — mirror structure)
    - src/api/client.ts (Env, EnvBulkEntry types from 12-01)
    - src/utils/errors.ts (COOLIFY_CONFIRM_REQUIRED, COOLIFY_VALIDATION_ERROR)
    - .planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md (D-01..D-16, D-09 no sync)
    - tests/unit/service.test.ts (RED expectations)
  </read_first>
  <files>src/mcp/tools/service.ts</files>
  <behavior>
    - envs:list accepts optional reveal
    - envs:get accepts env_uuid XOR key, optional reveal
    - envs:create accepts key, value, is_preview, is_literal, is_multiline, is_shown_once (all 4 flags, service supports is_preview per OpenAPI)
    - envs:update accepts env_uuid XOR key, value, optional flag overrides
    - envs:delete requires env_uuid and confirm:true
    - envs:bulk-update requires entries array (min 1) and confirm:true
    - NO envs:sync schema (D-09 app-only)
  </behavior>
  <action>
    Add six Zod schemas to src/mcp/tools/service.ts mirroring the application.ts schemas from 12-02. Include all four flags on create and bulk-update (service supports is_preview per OpenAPI — no omission). Use the same env_uuid XOR key refine pattern. Add the six action strings to the service action union. Do NOT add envs:sync. Run service.test.ts; schema-level RED tests should start passing.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run tests/unit/service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Six envs:* schemas present in service.ts (no sync)
    - All four flags accepted on create and bulk-update
    - envs:get and envs:update enforce env_uuid XOR key
    - envs:delete and envs:bulk-update require confirm
    - Action union extended with six new action strings
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement service envs:list/get/create/update/delete/bulk-update handler branches</name>
  <read_first>
    - src/mcp/tools/service.ts (Task 1 schemas, action dispatch, resolveServiceMutationUuid)
    - src/mcp/tools/application.ts (12-02 handler branches — mirror structure with resource 'service')
    - src/api/client.ts (fetchEnvs, createEnv, updateEnvViaBulk, deleteEnv from 12-01)
    - src/utils/projections.ts (sanitizeFullProjection)
    - src/utils/errors.ts (throwValidationError, COOLIFY_CONFIRM_REQUIRED, COOLIFY_AMBIGUOUS_MATCH)
    - tests/unit/service.test.ts (RED expectations)
  </read_first>
  <files>src/mcp/tools/service.ts</files>
  <behavior>
    - envs:list calls fetchEnvs('service', uuid), returns masked array
    - envs:get resolves by env_uuid or key; ambiguous key throws COOLIFY_AMBIGUOUS_MATCH
    - envs:create calls createEnv('service', uuid, payload) with all 4 flags, returns { uuid, key, masked_value }
    - envs:update by env_uuid resolves key from list then single-element bulk PATCH
    - envs:delete throws COOLIFY_CONFIRM_REQUIRED if confirm !== true; else deleteEnv('service', uuid, env_uuid)
    - envs:bulk-update throws COOLIFY_CONFIRM_REQUIRED if confirm !== true; else updateEnvViaBulk('service', uuid, entries)
    - All responses masked unless reveal:true
    - No plaintext env value in any code path
  </behavior>
  <action>
    Implement the six handler branches in service.ts mirroring application.ts from 12-02 but with resource 'service'. Apply sanitizeFullProjection unless reveal:true. Use throwValidationError with COOLIFY_CONFIRM_REQUIRED for missing confirm on delete and bulk-update. Route all errors through mapApiError (redactSecrets). Run service.test.ts and confirm all six envs:* RED tests now GREEN.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run tests/unit/service.test.ts</automated>
  </verify>
  <done>
    - All six envs:* handler branches implemented for service
    - envs:update by env_uuid resolves key then single-element bulk
    - Confirm gates enforced
    - All responses masked unless reveal:true
    - All 12-00 service.test.ts RED tests GREEN
    - No plaintext env values in any code path
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller → service.ts | Untrusted action + payload; Zod validates |
| service.ts → Coolify API | Outbound HTTP via client.ts with resource 'service' |
| Coolify API → MCP response | Env values masked before return |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03-01 | Information Disclosure | env value in service response | critical | mitigate | sanitizeFullProjection masks value; reveal:true opt-in only (D-14/D-15). |
| T-12-03-02 | Information Disclosure | env value in error paths | critical | mitigate | All errors via mapApiError + redactSecrets; no console.log of payloads. |
| T-12-03-03 | Elevation of Privilege | envs:delete without confirm | high | mitigate | Zod + handler enforce confirm; COOLIFY_CONFIRM_REQUIRED thrown. |
| T-12-03-04 | Elevation of Privilege | envs:bulk-update without confirm | high | mitigate | Same confirm gate. |
| T-12-03-05 | Tampering | envs:update by env_uuid key resolution | medium | mitigate | Resolve key from fetched list by env_uuid; never trust caller key when env_uuid provided. |
</threat_model>

<verification>
- Six envs:* action branches dispatch for service
- All four flags accepted (service supports is_preview)
- Confirm gates on delete and bulk-update
- All 12-00 service.test.ts RED tests GREEN
- No envs:sync action (D-09)
</verification>

<success_criteria>
- Service tool supports envs:list/get/create/update/delete/bulk-update
- Full flag support including is_preview
- Confirm gates enforced
- Secret masking default-on
- All 12-00 service RED tests GREEN
</success_criteria>

<output>
Create `.planning/phases/12-environment-variables-smart-sync/12-03-SUMMARY.md` when done
</output>
