---
phase: 12-environment-variables-smart-sync
plan: 04
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/mcp/tools/database.ts
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04
  - ENV-06
must_haves:
  truths:
    - "database.ts dispatches envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update actions"
    - "Database envs:create/update/bulk-update schemas OMIT is_preview (OpenAPI flag gap, Pitfall 1, D-16)"
    - "Passing is_preview on database create/update/bulk-update throws COOLIFY_VALIDATION_ERROR"
    - "envs:delete and envs:bulk-update require confirm:true else throw COOLIFY_CONFIRM_REQUIRED"
    - "All env responses mask value as `***` unless reveal:true"
    - "No envs:sync action on database (D-09 app-only)"
    - "All 12-00 database.test.ts RED tests for the six actions now pass GREEN"
  artifacts:
    - "src/mcp/tools/database.ts (extended with envs:* CRUD + bulk branches, no is_preview)"
  key_links:
    - "Handler calls fetchEnvs / createEnv / updateEnvViaBulk / deleteEnv with resource 'database'"
    - "Database is_preview rejection enforced at both schema (z.object omits is_preview) and handler (throwValidationError if is_preview sneaks in via unknown key)"
    - "Confirm gates use COOLIFY_CONFIRM_REQUIRED (D-13, D-11)"
---

<objective>
Add envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update to the database MCP tool. Critical: database envs do NOT support is_preview per OpenAPI (Pitfall 1, D-16). The schemas must omit is_preview and the handler must reject any is_preview that sneaks through. No sync (D-09).

Purpose: Parallel delivery of database env CRUD + bulk with the is_preview flag gap enforced, alongside application (12-02) and service (12-03).
Output: Extended src/mcp/tools/database.ts. All 12-00 database.test.ts RED tests flip GREEN including the is_preview rejection tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md
@.planning/phases/12-environment-variables-smart-sync/12-PATTERNS.md
@.planning/phases/12-environment-variables-smart-sync/12-VALIDATION.md
@.planning/phases/12-environment-variables-smart-sync/COVERAGE.md
@.planning/phases/12-environment-variables-smart-sync/12-01-SUMMARY.md
@src/mcp/tools/database.ts
@src/mcp/tools/application.ts
@src/api/client.ts
@src/utils/projections.ts
@src/utils/errors.ts
@tests/unit/database.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod schemas for database envs:list/get/create/update/delete/bulk-update (omit is_preview)</name>
  <read_first>
    - src/mcp/tools/database.ts (existing schema shape, action union, resolveDatabaseUuid, existing confirm gate for public access)
    - src/mcp/tools/application.ts (12-02 envs:* schemas — mirror minus is_preview)
    - src/api/client.ts (Env, EnvBulkEntry types from 12-01; note EnvBulkEntry may include is_preview optionally — database handler must strip it)
    - src/utils/errors.ts (COOLIFY_CONFIRM_REQUIRED, COOLIFY_VALIDATION_ERROR)
    - .planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md (D-09 no sync, D-16 is_preview omission)
    - docs/coolify_openapi.yaml lines 4258-4305 (database bulk body — no is_preview)
    - tests/unit/database.test.ts (RED expectations including is_preview rejection)
  </read_first>
  <files>src/mcp/tools/database.ts</files>
  <behavior>
    - envs:list accepts optional reveal
    - envs:get accepts env_uuid XOR key, optional reveal
    - envs:create accepts key, value, is_literal, is_multiline, is_shown_once — NO is_preview field in schema
    - envs:update accepts env_uuid XOR key, value, optional is_literal/is_multiline/is_shown_once — NO is_preview
    - envs:delete requires env_uuid and confirm:true
    - envs:bulk-update requires entries array (min 1, each entry has NO is_preview) and confirm:true
    - Schemas use .strict() or .omit() to ensure is_preview is rejected if passed (throws COOLIFY_VALIDATION_ERROR)
    - NO envs:sync schema (D-09)
  </behavior>
  <action>
    Add six Zod schemas to src/mcp/tools/database.ts mirroring application.ts from 12-02 but OMITTING is_preview from create, update, and bulk-update entry schemas. Use z.object({ ... }).strict() (or .omit on a shared base) so that passing is_preview triggers Zod's unknown-key rejection, which the handler maps to COOLIFY_VALIDATION_ERROR. Add the six action strings to the database action union. Do NOT add envs:sync. Run database.test.ts; schema-level RED tests including is_preview rejection should start passing.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run tests/unit/database.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Six envs:* schemas present in database.ts (no sync)
    - is_preview omitted from create, update, bulk-update entry schemas
    - .strict() (or equivalent) ensures is_preview rejection at schema level
    - envs:get and envs:update enforce env_uuid XOR key
    - envs:delete and envs:bulk-update require confirm
    - Action union extended
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement database envs:list/get/create/update/delete/bulk-update handler branches (no is_preview)</name>
  <read_first>
    - src/mcp/tools/database.ts (Task 1 schemas, action dispatch, resolveDatabaseUuid)
    - src/mcp/tools/application.ts (12-02 handler branches — mirror with resource 'database' and is_preview stripped)
    - src/api/client.ts (fetchEnvs, createEnv, updateEnvViaBulk, deleteEnv from 12-01; createEnv database branch also guards is_preview client-side)
    - src/utils/projections.ts (sanitizeFullProjection)
    - src/utils/errors.ts (throwValidationError, COOLIFY_CONFIRM_REQUIRED, COOLIFY_VALIDATION_ERROR, COOLIFY_AMBIGUOUS_MATCH)
    - tests/unit/database.test.ts (RED expectations)
  </read_first>
  <files>src/mcp/tools/database.ts</files>
  <behavior>
    - envs:list calls fetchEnvs('database', uuid), returns masked array
    - envs:get resolves by env_uuid or key; ambiguous key throws COOLIFY_AMBIGUOUS_MATCH
    - envs:create calls createEnv('database', uuid, payload) — payload has no is_preview; client.ts also guards
    - envs:update by env_uuid resolves key from list then single-element bulk PATCH (no is_preview in entry)
    - envs:delete throws COOLIFY_CONFIRM_REQUIRED if confirm !== true; else deleteEnv('database', uuid, env_uuid)
    - envs:bulk-update throws COOLIFY_CONFIRM_REQUIRED if confirm !== true; else updateEnvViaBulk('database', uuid, entries) — entries have no is_preview
    - All responses masked unless reveal:true
    - No plaintext env value in any code path
  </behavior>
  <action>
    Implement the six handler branches in database.ts mirroring application.ts from 12-02 but with resource 'database' and no is_preview anywhere. The schema-level .strict() already rejects is_preview; the handler does not need to re-check, but for defense-in-depth, if createEnv or updateEnvViaBulk throws COOLIFY_VALIDATION_ERROR from the client-side guard (12-01), let it propagate. Apply sanitizeFullProjection unless reveal:true. Use throwValidationError with COOLIFY_CONFIRM_REQUIRED for missing confirm. Route errors through mapApiError. Run database.test.ts and confirm all six envs:* RED tests including is_preview rejection tests now GREEN.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run tests/unit/database.test.ts</automated>
  </verify>
  <done>
    - All six envs:* handler branches implemented for database
    - No is_preview in any code path (schema + client guard)
    - envs:update by env_uuid resolves key then single-element bulk
    - Confirm gates enforced
    - All responses masked unless reveal:true
    - All 12-00 database.test.ts RED tests GREEN including is_preview rejection
    - No plaintext env values in any code path
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller → database.ts | Untrusted action + payload; Zod .strict() rejects unknown keys (is_preview) |
| database.ts → Coolify API | Outbound HTTP via client.ts with resource 'database'; client.ts also guards is_preview |
| Coolify API → MCP response | Env values masked before return |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04-01 | Information Disclosure | env value in database response | critical | mitigate | sanitizeFullProjection masks value; reveal:true opt-in only (D-14/D-15). |
| T-12-04-02 | Information Disclosure | env value in error paths | critical | mitigate | All errors via mapApiError + redactSecrets; no console.log of payloads. |
| T-12-04-03 | Elevation of Privilege | envs:delete without confirm | high | mitigate | Zod + handler enforce confirm; COOLIFY_CONFIRM_REQUIRED thrown. |
| T-12-04-04 | Elevation of Privilege | envs:bulk-update without confirm | high | mitigate | Same confirm gate. |
| T-12-04-05 | Tampering | is_preview sneaks into database env payload | high | mitigate | Schema .strict() rejects is_preview with COOLIFY_VALIDATION_ERROR; client.ts createEnv/updateEnvViaBulk database branches also throw client-side before HTTP (defense-in-depth, Pitfall 1, D-16). |
| T-12-04-06 | Tampering | envs:update by env_uuid key resolution | medium | mitigate | Resolve key from fetched list by env_uuid; never trust caller key when env_uuid provided. |
</threat_model>

<verification>
- Six envs:* action branches dispatch for database
- is_preview rejected at schema and client levels
- Confirm gates on delete and bulk-update
- All 12-00 database.test.ts RED tests GREEN including is_preview rejection
- No envs:sync action (D-09)
</verification>

<success_criteria>
- Database tool supports envs:list/get/create/update/delete/bulk-update
- is_preview omitted and rejected throughout (Pitfall 1, D-16)
- Confirm gates enforced
- Secret masking default-on
- All 12-00 database RED tests GREEN
</success_criteria>

<output>
Create `.planning/phases/12-environment-variables-smart-sync/12-04-SUMMARY.md` when done
</output>
