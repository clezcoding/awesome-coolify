---
phase: 12-environment-variables-smart-sync
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/tools/application.test.ts
  - src/mcp/tools/service.test.ts
  - src/mcp/tools/database.test.ts
  - src/api/client.test.ts
  - src/utils/env-parser.test.ts
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04
  - ENV-05
  - ENV-06
must_haves:
  truths:
    - "application.test.ts contains RED describe blocks for envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update, envs:sync"
    - "service.test.ts contains RED describe blocks for envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update (no sync)"
    - "database.test.ts contains RED describe blocks for envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update (no sync, no is_preview)"
    - "client.test.ts contains RED tests for fetchEnvs, createEnv, updateEnvViaBulk, deleteEnv, bulkUpdateEnvs parameterized by resource type"
    - "env-parser.test.ts contains RED tests for parseEnvFile, diffEnvs, detectConflicts"
    - "All new RED tests fail before any implementation in plans 12-01..12-06"
  artifacts:
    - "src/mcp/tools/application.test.ts (extended)"
    - "src/mcp/tools/service.test.ts (extended)"
    - "src/mcp/tools/database.test.ts (extended)"
    - "src/api/client.test.ts (extended)"
    - "src/utils/env-parser.test.ts (new)"
  key_links:
    - "RED tests reference the envs:* action strings that 12-02/03/04/05 handlers will dispatch on"
    - "RED tests reference the client function names that 12-01 will export from src/api/client.ts"
    - "RED tests reference the parser function names that 12-01 will export from src/utils/env-parser.ts"
---

## Artifacts this phase produces

This plan (12-00) produces no production code — only RED test scaffolds. Symbols introduced in test files (referenced by 12-01..12-05 implementations):

- `src/mcp/tools/application.test.ts` — new describe blocks: `describe('application envs:list', ...)`, `envs:get`, `envs:create`, `envs:update`, `envs:delete`, `envs:bulk-update`, `envs:sync`; new fixtures `mockEnv`, `mockEnvList`; vi.mock additions for `fetchEnvs`, `createEnv`, `updateEnvViaBulk`, `bulkUpdateEnvs`, `deleteEnv`.
- `src/mcp/tools/service.test.ts` — six new envs:* describe blocks (no sync); vi.mock additions for the env client functions.
- `src/mcp/tools/database.test.ts` — six new envs:* describe blocks (no sync) plus `is_preview` rejection tests; vi.mock additions for the env client functions.
- `src/api/client.test.ts` — new describe blocks for `fetchEnvs`, `createEnv`, `updateEnvViaBulk`, `bulkUpdateEnvs`, `deleteEnv` parameterized by `ResourceType`.
- `src/utils/env-parser.test.ts` (NEW) — describe blocks for `parseEnvFile`, `diffEnvs`, `detectConflicts`.

No new exports from `src/` production code in this plan.

<objective>
Lay RED TDD scaffolds for all envs:* actions across application, service, and database MCP tools, plus the API client env methods and the local `.env` parser/diff engine. Tests MUST fail before any implementation ships in plans 12-01..12-06.

Purpose: TDD discipline — every handler, client method, and parser function gets a failing test first, so 12-01..12-06 implement against executable specifications rather than prose.
Output: Five test files (four extended, one new) with RED describe blocks covering ENV-01..06 and D-01..D-16.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md
@.planning/phases/12-environment-variables-smart-sync/12-RESEARCH.md
@.planning/phases/12-environment-variables-smart-sync/12-VALIDATION.md
@.planning/phases/12-environment-variables-smart-sync/COVERAGE.md
@src/mcp/tools/application.test.ts
@src/mcp/tools/service.test.ts
@src/mcp/tools/database.test.ts
@src/api/client.test.ts
@src/mcp/tools/application.ts
@src/mcp/tools/service.ts
@src/mcp/tools/database.ts
@src/api/client.ts
@src/utils/projections.ts
@src/utils/errors.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED scaffolds for application.test.ts envs:* including sync</name>
  <read_first>
    - src/mcp/tools/application.test.ts (existing vi.mock pattern + lifecycle describe blocks)
    - src/mcp/tools/application.ts (action dispatch shape, resolveAppMutationUuid, throwValidationError)
    - .planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md (D-01..D-16; D-08 conflict_policy overwrite|keep_remote|abort; D-15 ask_human_reveal)
    - .planning/phases/12-environment-variables-smart-sync/12-VALIDATION.md (ENV-01..06 test map)
  </read_first>
  <files>src/mcp/tools/application.test.ts</files>
  <behavior>
    - envs:list returns array of env summaries with values masked as `***` by default
    - envs:list with reveal:true returns plaintext values AND surfaces an ask_human_reveal recovery hint in the response (D-15)
    - envs:get by env_uuid returns single env with masked value
    - envs:get by key returns single env with masked value
    - envs:get with neither env_uuid nor key throws COOLIFY_VALIDATION_ERROR
    - envs:create with all 4 flags returns created env uuid; subsequent envs:get round-trips the flags (ENV-06)
    - envs:update by env_uuid resolves key from list then bulk-patches one element
    - envs:delete without confirm throws COOLIFY_CONFIRM_REQUIRED
    - envs:delete with confirm returns deletion disposition
    - envs:bulk-update without confirm throws COOLIFY_CONFIRM_REQUIRED
    - envs:bulk-update with confirm applies entries and returns disposition
    - envs:sync with env_file XOR env_content violation throws COOLIFY_VALIDATION_ERROR
    - envs:sync dry_run:true returns diff without writing
    - envs:sync dry_run:false (apply path, default per D-06) without confirm throws COOLIFY_CONFIRM_REQUIRED
    - envs:sync with prune:true without confirm throws COOLIFY_CONFIRM_REQUIRED
    - envs:sync apply with conflicts and NO conflict_policy throws COOLIFY_CONFIRM_REQUIRED with ask-human recovery hint (D-08)
    - envs:sync with conflict_policy:overwrite applies and reports overwritten keys
    - envs:sync with conflict_policy:keep_remote returns kept-remote entries in disposition
    - envs:sync with conflict_policy:abort returns aborted entries in disposition (no throw)
    - envs:sync response never includes plaintext values regardless of reveal
    - envs:sync with neither env_file nor env_content throws COOLIFY_VALIDATION_ERROR
  </behavior>
  <action>
    Extend src/mcp/tools/application.test.ts with new describe blocks for each envs:* action. Follow the existing vi.mock pattern for the Coolify client and resolveAppMutationUuid. Use describe.each over a small fixture table for the masked-vs-revealed value pairs. Reference action strings envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update, envs:sync verbatim — these are the dispatch keys 12-02 will match on. For sync, assert that the returned disposition object has fields added, updated, kept_remote OR aborted (per conflict_policy), conflicts, pruned and that none of them contain plaintext values. Assert confirm-gate errors use the COOLIFY_CONFIRM_REQUIRED and COOLIFY_VALIDATION_ERROR error codes from src/utils/errors.ts. Per D-08, assert that envs:sync apply with conflicts and no conflict_policy throws COOLIFY_CONFIRM_REQUIRED (NOT a COOLIFY_SYNC_CONFLICT code — that code is NOT being introduced) with an ask-human recovery hint mentioning conflict_policy overwrite|keep_remote|abort. Per D-15, assert that envs:list / envs:get with reveal:true surfaces an ask_human_reveal recovery hint in the response. Add an ENV-06 round-trip test: envs:create with is_preview/is_literal/is_multiline/is_shown_once set, then envs:get by the returned env_uuid, and assert the flags match. Tests MUST fail (RED) at commit time because the handler branches and client methods do not exist yet. Do NOT stub handler implementations — only test scaffolds.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/application.test.ts 2>&1 | grep -cE 'envs:(list|get|create|update|delete|bulk-update|sync)' | grep -v '^0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('application envs:" src/mcp/tools/application.test.ts` returns 7 (list, get, create, update, delete, bulk-update, sync)
    - `grep -c "ask_human_reveal" src/mcp/tools/application.test.ts` returns at least 2 (one for list, one for get)
    - `grep -c "conflict_policy" src/mcp/tools/application.test.ts` returns at least 4 (overwrite, keep_remote, abort, missing-policy ask-human)
    - `grep -c "COOLIFY_SYNC_CONFLICT" src/mcp/tools/application.test.ts` returns 0 (D-08 uses COOLIFY_CONFIRM_REQUIRED, not a sync-conflict code)
    - `grep -c "is_preview" src/mcp/tools/application.test.ts` returns at least 1 (ENV-06 round-trip)
    - `npx vitest run src/mcp/tools/application.test.ts` exits non-zero with failures in the new envs:* describe blocks (RED)
    - No existing application lifecycle tests modified (diff confined to new describe blocks)
  </acceptance_criteria>
  <done>
    - application.test.ts contains describe blocks for all seven envs:* actions
    - All new envs:* tests fail with RED status (handler not implemented)
    - No existing application lifecycle tests regressed
    - Confirm-gate and XOR validation tests reference the exact error codes from src/utils/errors.ts
    - D-08 conflict_policy tests use overwrite|keep_remote|abort (no fail/skip, no COOLIFY_SYNC_CONFLICT)
    - D-15 ask_human_reveal recovery hint asserted for reveal:true paths
    - ENV-06 flag round-trip via subsequent envs:get asserted
  </done>
</task>

<task type="tdd">
  <name>Task 2: RED scaffolds for service.test.ts and database.test.ts envs:* (no sync)</name>
  <read_first>
    - src/mcp/tools/service.test.ts
    - src/mcp/tools/database.test.ts
    - src/mcp/tools/service.ts
    - src/mcp/tools/database.ts
    - .planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md (D-09 sync app-only, D-15 ask_human_reveal, D-16 is_preview omission for db)
    - docs/coolify_openapi.yaml (database envs bulk body lacks is_preview)
  </read_first>
  <files>src/mcp/tools/service.test.ts, src/mcp/tools/database.test.ts</files>
  <behavior>
    Service:
    - envs:list returns masked envs
    - envs:list with reveal:true surfaces ask_human_reveal recovery hint (D-15)
    - envs:get by env_uuid or key
    - envs:create with all 4 flags (is_preview, is_literal, is_multiline, is_shown_once); subsequent envs:get round-trips flags (ENV-06)
    - envs:update by env_uuid resolves key then bulk-patches one element
    - envs:delete without confirm throws COOLIFY_CONFIRM_REQUIRED
    - envs:bulk-update with confirm applies entries
    - No envs:sync describe block (D-09 app-only)
    Database:
    - envs:list returns masked envs
    - envs:list with reveal:true surfaces ask_human_reveal recovery hint (D-15)
    - envs:get by env_uuid or key
    - envs:create accepts only is_literal, is_multiline, is_shown_once — passing is_preview throws COOLIFY_VALIDATION_ERROR
    - envs:update by env_uuid — passing is_preview throws COOLIFY_VALIDATION_ERROR
    - envs:delete without confirm throws COOLIFY_CONFIRM_REQUIRED
    - envs:bulk-update — passing is_preview in any entry throws COOLIFY_VALIDATION_ERROR
    - No envs:sync describe block (D-09 app-only)
  </behavior>
  <action>
    Extend src/mcp/tools/service.test.ts and src/mcp/tools/database.test.ts with describe blocks for envs:list, envs:get, envs:create, envs:update, envs:delete, envs:bulk-update. Do NOT add envs:sync blocks (D-09 — sync is application-only). For database tests, include negative tests asserting that passing is_preview on create, update, or any bulk-update entry throws COOLIFY_VALIDATION_ERROR — this encodes the OpenAPI flag gap (Pitfall 1, D-16). For both tools, add a D-15 test asserting that reveal:true surfaces an ask_human_reveal recovery hint in the response. Add an ENV-06 round-trip test for service (create with all 4 flags, then envs:get by env_uuid, assert flags match). For database, the round-trip test uses only the 3 supported flags. Mirror the existing vi.mock + resolveServiceMutationUuid / resolveDatabaseUuid patterns. Tests MUST fail (RED) at commit time. Do NOT stub handler implementations.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/mcp/tools/service.test.ts src/mcp/tools/database.test.ts 2>&1 | grep -cE 'envs:(list|get|create|update|delete|bulk-update)' | grep -v '^0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('service envs:" src/mcp/tools/service.test.ts` returns 6 (no sync)
    - `grep -c "describe('database envs:" src/mcp/tools/database.test.ts` returns 6 (no sync)
    - `grep -c "ask_human_reveal" src/mcp/tools/service.test.ts` returns at least 1
    - `grep -c "ask_human_reveal" src/mcp/tools/database.test.ts` returns at least 1
    - `grep -c "is_preview" src/mcp/tools/database.test.ts` returns at least 3 (create, update, bulk-update rejection)
    - `grep -cE "describe('service envs:sync'|describe('database envs:sync'" src/mcp/tools/service.test.ts src/mcp/tools/database.test.ts` returns 0 (D-09)
    - `npx vitest run src/mcp/tools/service.test.ts src/mcp/tools/database.test.ts` exits non-zero with RED failures in new describe blocks
    - No existing service/database tests modified
  </acceptance_criteria>
  <done>
    - service.test.ts has six envs:* describe blocks (no sync)
    - database.test.ts has six envs:* describe blocks (no sync) plus is_preview rejection tests
    - D-15 ask_human_reveal recovery hint asserted for reveal:true paths in both files
    - ENV-06 flag round-trip asserted (4 flags for service, 3 for database)
    - All new tests RED
    - No existing service/database tests regressed
  </done>
</task>

<task type="tdd">
  <name>Task 3: RED scaffolds for client.test.ts env methods and env-parser.test.ts</name>
  <read_first>
    - src/api/client.test.ts
    - src/api/client.ts (existing client function shape, ofetch usage, retry/error mapping)
    - src/utils/projections.ts (sanitizeFullProjection, redactSecrets)
    - .planning/phases/12-environment-variables-smart-sync/12-RESEARCH.md (env parser + diff algorithm)
    - .planning/phases/12-environment-variables-smart-sync/12-VALIDATION.md (Wave 0 unit test gaps for parser)
    - .planning/phases/12-environment-variables-smart-sync/12-CONTEXT.md (D-08 conflict_policy overwrite|keep_remote|abort)
  </read_first>
  <files>src/api/client.test.ts, src/utils/env-parser.test.ts</files>
  <behavior>
    Client:
    - fetchEnvs('application', uuid) returns array of env objects
    - fetchEnvs('service', uuid) returns array
    - fetchEnvs('database', uuid) returns array
    - createEnv('application', uuid, { key, value, is_preview, is_literal, is_multiline, is_shown_once }) POSTs to /applications/{uuid}/envs
    - createEnv('database', uuid, payload with is_preview) throws (client-side guard, before request)
    - updateEnvViaBulk('application', uuid, [{ key, value, ... }]) PATCHes /applications/{uuid}/envs/bulk with { data: [...] }
    - bulkUpdateEnvs is an alias for updateEnvViaBulk with multi-element array
    - deleteEnv('application', uuid, env_uuid) DELETEs /applications/{uuid}/envs/{env_uuid}
    Parser:
    - parseEnvFile('KEY=value\n# comment\nEMPTY=') returns [{ key: 'KEY', value: 'value' }, { key: 'EMPTY', value: '' }]
    - parseEnvFile strips surrounding single/double quotes from values
    - parseEnvFile unescapes \n inside double-quoted values when is_multiline intent
    - parseEnvFile ignores blank lines and # comments
    - parseEnvFile preserves leading/trailing whitespace inside double quotes
    - diffEnvs(local[], remote[]) returns { added: [], updated: [], unchanged: [], removed: [] } by key
    - diffEnvs marks a key as updated only when local value differs from remote value
    - detectConflicts(local[], remote[], policy) returns conflicts where remote key was modified since local snapshot (out-of-band edit)
    - detectConflicts with policy 'keep_remote' returns keep-remote decisions (no throw)
    - detectConflicts with policy 'abort' returns abort decisions (no throw)
    - detectConflicts with policy 'overwrite' returns empty conflicts (caller will clobber remote)
    - detectConflicts does NOT accept 'fail' or 'skip' (D-08 enum is overwrite|keep_remote|abort)
  </behavior>
  <action>
    Extend src/api/client.test.ts with describe blocks for fetchEnvs, createEnv, updateEnvViaBulk, bulkUpdateEnvs, deleteEnv. Use the existing ofetch mock pattern. Assert URL paths and HTTP verbs exactly. For createEnv on database, assert a client-side guard rejects is_preview before any HTTP call (this is the client-side mirror of the OpenAPI flag gap). Create src/utils/env-parser.test.ts as a new file with describe blocks for parseEnvFile, diffEnvs, detectConflicts. Use plain string fixtures (no .env file IO — pure functions). For detectConflicts, parameterize over the D-08 enum overwrite|keep_remote|abort — do NOT include 'fail' or 'skip' (those were the rejected pre-D-08 design). Tests MUST fail (RED) at commit time because the client methods and env-parser.ts do not exist yet.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx vitest run src/api/client.test.ts src/utils/env-parser.test.ts 2>&1 | grep -cE '(fetchEnvs|createEnv|updateEnvViaBulk|bulkUpdateEnvs|deleteEnv|parseEnvFile|diffEnvs|detectConflicts)' | grep -v '^0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('fetchEnvs'" src/api/client.test.ts` returns at least 1
    - `grep -c "describe('createEnv'" src/api/client.test.ts` returns at least 1
    - `grep -c "describe('updateEnvViaBulk'" src/api/client.test.ts` returns at least 1
    - `grep -c "describe('deleteEnv'" src/api/client.test.ts` returns at least 1
    - `grep -c "describe('parseEnvFile'" src/utils/env-parser.test.ts` returns at least 1
    - `grep -c "describe('diffEnvs'" src/utils/env-parser.test.ts` returns at least 1
    - `grep -c "describe('detectConflicts'" src/utils/env-parser.test.ts` returns at least 1
    - `grep -cE "'(fail|skip)'" src/utils/env-parser.test.ts` returns 0 (D-08 enum is overwrite|keep_remote|abort only)
    - `grep -cE "'(overwrite|keep_remote|abort)'" src/utils/env-parser.test.ts` returns at least 3
    - `npx vitest run src/api/client.test.ts src/utils/env-parser.test.ts` exits non-zero with RED failures
    - Database is_preview rejection test present in client.test.ts
  </acceptance_criteria>
  <done>
    - client.test.ts has describe blocks for all five env client methods, parameterized by resource type
    - env-parser.test.ts exists with describe blocks for parseEnvFile, diffEnvs, detectConflicts
    - Database is_preview rejection test present in client.test.ts
    - detectConflicts tests use only overwrite|keep_remote|abort (D-08)
    - All new tests RED
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → test file | Hard-coded env fixtures cross into test scope; must not contain real secrets |
| RED test → (unimplemented) handler | Tests assert that handlers WILL enforce confirm gates and masking; failure mode is RED, not silent pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-00-01 | Information Disclosure | test fixtures in application.test.ts | low | mitigate | Use synthetic fixture values only (e.g. `FAKE_SECRET_VALUE`); never embed real tokens. Assert masked output is `***` not the fixture value. |
| T-12-00-02 | Tampering | RED tests that pass silently | medium | mitigate | Verify gate: grep for envs: test names AND assert vitest reports failures (RED) before 12-01 ships. |
| T-12-00-03 | Information Disclosure | env-parser.test.ts fixtures | low | mitigate | Pure-string fixtures only; no real .env files loaded from disk. |
</threat_model>

<verification>
- All five test files extended/created
- `npx vitest run src/mcp/tools/application.test.ts src/mcp/tools/service.test.ts src/mcp/tools/database.test.ts src/api/client.test.ts src/utils/env-parser.test.ts` reports RED on every new envs:* and parser test
- No existing tests regressed
- Confirm-gate tests reference exact error codes from src/utils/errors.ts
- Database is_preview rejection tests present in both database.test.ts and client.test.ts
- D-08 conflict_policy tests use overwrite|keep_remote|abort only (no COOLIFY_SYNC_CONFLICT)
- D-15 ask_human_reveal recovery hint asserted for reveal:true paths
- ENV-06 flag round-trip via subsequent envs:get asserted
</verification>

<success_criteria>
- 7 envs:* action describe blocks in src/mcp/tools/application.test.ts (including sync)
- 6 envs:* action describe blocks each in src/mcp/tools/service.test.ts and src/mcp/tools/database.test.ts (no sync)
- 5 client method describe blocks in src/api/client.test.ts (parameterized by resource type)
- 3 parser function describe blocks in src/utils/env-parser.test.ts
- All new tests RED at commit
- Every ENV-* requirement has at least one RED test asserting its behavior
- D-08 conflict_policy enum overwrite|keep_remote|abort only (no COOLIFY_SYNC_CONFLICT)
- D-15 ask_human_reveal recovery hint asserted for reveal:true paths
- ENV-06 flag round-trip via subsequent envs:get asserted
</success_criteria>

<output>
Create `.planning/phases/12-environment-variables-smart-sync/12-00-SUMMARY.md` when done
</output>
