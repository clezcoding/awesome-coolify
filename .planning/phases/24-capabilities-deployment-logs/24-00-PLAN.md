---
phase: 24-capabilities-deployment-logs
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/mcp/tools/system.test.ts
  - src/mcp/tools/meta.test.ts
  - src/mcp/tools/deployment.test.ts
  - src/utils/errors.test.ts
  - src/utils/log-helpers.test.ts
  - docs/coverage-map.yaml
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - OBS-01
user_setup: []

must_haves:
  truths:
    - "system.test.ts has it.fails (or rewritten target) coverage for system.version shape coolifyVersion+mcpVersion+serverName+capabilities and a capabilities describe asserting four D-03 keys with object shape (CAP-01, CAP-02, D-01, D-07)"
    - "meta.test.ts targets mcpVersion from readPackageVersion() matching package.json (CAP-01, D-08)"
    - "deployment.test.ts describe('deployment logs') has it.fails scaffolds for schema XOR, fetch by uuid, latest-by-created_at, no-deployments error, empty-logs soft OK, sensitive-required (OBS-01, D-10–D-16)"
    - "errors.test.ts scaffolds COOLIFY_NO_DEPLOYMENTS recovery hints mentioning application.deploy and deployment.list (D-14)"
    - "docs/coverage-map.yaml contains deployment.logs row mapping fetchDeployment + fetchAppDeployments (OBS-01)"
    - "All new RED cases use vitest it.fails so husky pre-commit stays green"
  artifacts:
    - path: src/mcp/tools/system.test.ts
      provides: "capabilities + version shape RED scaffolds"
      contains: "capabilities"
    - path: src/mcp/tools/deployment.test.ts
      provides: "describe('deployment logs') it.fails block"
      contains: "deployment logs"
    - path: docs/coverage-map.yaml
      provides: "deployment.logs coverage row"
      contains: "deployment.logs"
  key_links:
    - from: "system.test.ts version/capabilities cases"
      to: "handleSystemAction version (Plan 24-01)"
      via: "assert coolifyVersion mcpVersion capabilities"
    - from: "deployment.test.ts logs describe"
      to: "handleDeploymentAction logs (Plan 24-02)"
      via: "safeParse + mocked fetchDeployment/fetchAppDeployments"
  assumptions:
    - "EDGE CAP-01 unclassified ($COVERAGE) → author as D-07 response fields coolifyVersion/mcpVersion/serverName (A-EDGE-CAP-01)"
    - "EDGE CAP-02 unclassified ($COVERAGE) → author as D-01–D-03 static four-key object map (A-EDGE-CAP-02)"
    - "EDGE OBS-01 concurrency ($COVERAGE) → deployment.logs is stateless on-demand fetch; concurrent calls independent; no shared session mutation (A-EDGE-OBS-01)"
  prohibitions:
    - statement: "Do not implement production handlers in Wave 0 — scaffolds only"
      status: enforced
      verification: "git diff --name-only -- src/mcp/tools/system.ts src/mcp/tools/deployment.ts src/mcp/capabilities.ts is empty after this plan"
    - statement: "Do not add diagnose or diagnose_logs capability keys (D-03)"
      status: enforced
      verification: "rg -n \"diagnose_logs|diagnose\" src/mcp/tools/system.test.ts | grep -v '^#' | wc -l is 0 for capability key literals"
    - statement: "Do not create incident prompt content (Phase 26)"
      status: enforced
      verification: "no edits to incident prompt strings in this plan"
---

<objective>
Wave 0 Nyquist RED scaffolds for Phase 24 — lock CAP-01/CAP-02/OBS-01 acceptance as `it.fails` tests before implementation.

Purpose: Plans 24-01..24-03 flip scaffolds GREEN without rewriting intent. Mirrors Phase 21 Wave 0 (`it.fails` keeps husky green).

Output: Extended system/meta/deployment/errors/log-helpers tests; coverage-map row for `deployment.logs`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md
@.planning/phases/24-capabilities-deployment-logs/24-RESEARCH.md
@.planning/phases/24-capabilities-deployment-logs/24-PATTERNS.md
@.planning/phases/24-capabilities-deployment-logs/24-VALIDATION.md
@src/mcp/tools/system.test.ts
@src/mcp/tools/meta.test.ts
@src/mcp/tools/deployment.test.ts
@src/utils/errors.test.ts
@docs/coverage-map.yaml
</context>

## Artifacts this phase produces

- `src/mcp/tools/system.test.ts` — RED targets for `coolifyVersion` / `mcpVersion` / `serverName` / `capabilities` (four D-03 keys).
- `src/mcp/tools/meta.test.ts` — RED/updated target asserting `readPackageVersion()` parity.
- `src/mcp/tools/deployment.test.ts` — `describe('deployment logs')` `it.fails` block.
- `src/utils/errors.test.ts` — `COOLIFY_NO_DEPLOYMENTS` hints scaffold.
- `src/utils/log-helpers.test.ts` — optional `processDeploymentBuildLogs` `it.fails` describe (or skip if covered solely via deployment tests — prefer add minimal describe).
- `docs/coverage-map.yaml` — `deployment.logs` row (docs regen deferred to Plan 24-03).

<tasks>

<task type="tdd">
  <name>Task 1: RED scaffolds for system.version shape + meta package version</name>
  <files>src/mcp/tools/system.test.ts, src/mcp/tools/meta.test.ts</files>
  <read_first>
    - .planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md (D-01, D-03, D-07, D-08)
    - .planning/phases/24-capabilities-deployment-logs/24-VALIDATION.md (24-W0-01, Wave 0 checklist)
    - .planning/phases/24-capabilities-deployment-logs/24-PATTERNS.md (system.test.ts + meta.test.ts sections)
    - src/mcp/tools/system.test.ts (version test ~L118-125; verify coolifyVersion ~L128-140; no-token pattern ~L88-97)
    - src/mcp/tools/meta.test.ts (MCP_VERSION import and assertion)
    - package.json (canonical version field)
  </read_first>
  <behavior>
    - system.version result has coolifyVersion string from mocked API, mcpVersion string, serverName awesome-coolify-mcp, capabilities object
    - capabilities contains exactly keys application_logs, deployment_logs, deployment_watch, deploy_watch
    - each capability value has supported boolean and coolify_min_version string
    - JSON.stringify(version result) does not contain COOLIFY_TOKEN
    - meta.version mcpVersion equals readPackageVersion() output (not hardcoded 0.1.0)
  </behavior>
  <action>
    Per D-01/D-03/D-07/D-08 and VALIDATION Wave 0: convert existing system.version equality on bare version field into it.fails asserting { coolifyVersion, mcpVersion, serverName, capabilities }. Add describe('capabilities') with it.fails cases asserting four D-03 keys and object shape { supported, coolify_min_version }. Keep no-token assertion pattern from health/verify. In meta.test.ts, change expectation path to import readPackageVersion from ../../utils/package-version.js (dynamic import inside it.fails if module absent) and assert mcpVersion matches; use it.fails until Plan 24-01 ships the helper. Do not edit system.ts or meta.ts production code in this plan.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/system.test.ts src/mcp/tools/meta.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "describe\\('capabilities'" src/mcp/tools/system.test.ts returns at least 1
    - rg -n "coolifyVersion" src/mcp/tools/system.test.ts returns at least 2
    - rg -n "application_logs|deployment_logs|deployment_watch|deploy_watch" src/mcp/tools/system.test.ts returns at least 4
    - rg -n "readPackageVersion|it\\.fails" src/mcp/tools/meta.test.ts returns at least 1 each
    - npx vitest run src/mcp/tools/system.test.ts src/mcp/tools/meta.test.ts exits 0
  </acceptance_criteria>
  <done>CAP-01/CAP-02 RED targets locked; husky green via it.fails.</done>
</task>

<task type="tdd">
  <name>Task 2: RED scaffolds for deployment.logs + NO_DEPLOYMENTS + coverage row</name>
  <files>src/mcp/tools/deployment.test.ts, src/utils/errors.test.ts, src/utils/log-helpers.test.ts, docs/coverage-map.yaml</files>
  <read_first>
    - .planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md (D-10–D-16)
    - .planning/phases/24-capabilities-deployment-logs/24-VALIDATION.md (24-W0-02, Wave 0 checklist)
    - .planning/phases/24-capabilities-deployment-logs/24-PATTERNS.md (deployment.test.ts watch analog; coverage-map row)
    - src/mcp/tools/deployment.test.ts (mockDeployments ~L34-48; describe deployment watch)
    - src/utils/errors.test.ts (COOLIFY_WATCH_TIMEOUT hints pattern)
    - docs/coverage-map.yaml (deployment.watch row ~L123-125)
    - src/mcp/tools/application.test.ts (build logs fixture pattern ~L1066-1114)
  </read_first>
  <behavior>
    - Schema: logs with only deployment_uuid succeeds; only application_uuid succeeds; both fail; neither fails
    - Handler by deployment_uuid returns logs_lines envelope fields
    - Handler by application_uuid with mockDeployments resolves dep-3 (newest created_at)
    - Empty deployments list → isError with COOLIFY_NO_DEPLOYMENTS and recoveryHints
    - logs:'' → soft OK empty logs_lines + hint (not error)
    - missing/non-string logs → COOLIFY_403_SENSITIVE_REQUIRED
    - RECOVERY_HINTS.COOLIFY_NO_DEPLOYMENTS mentions application.deploy and deployment.list
  </behavior>
  <action>
    Per D-10–D-16 and OBS-01: add describe('deployment logs') with it.fails cases covering schema XOR, fetch-by-uuid happy path, application_uuid → dep-3 sort, no-deployments error, empty-string logs soft OK, sensitive-required. Reuse mockDeployments timestamps. Add errors.test.ts it.fails (or it if code already present — prefer it.fails) for COOLIFY_NO_DEPLOYMENTS hints. Optionally add describe('processDeploymentBuildLogs') it.fails in log-helpers.test.ts with dynamic import. Insert coverage-map.yaml row after deployment.watch for action deployment.logs with client [fetchDeployment, fetchAppDeployments] and openapi GET /deployments/{uuid} plus GET /deployments/applications/{uuid}. Do not regenerate docs/COVERAGE.md here (Plan 24-03). Do not implement deployment.ts logs handler.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts src/utils/errors.test.ts src/utils/log-helpers.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "describe\\('deployment logs'" src/mcp/tools/deployment.test.ts returns 1
    - rg -c "it\\.fails" src/mcp/tools/deployment.test.ts is greater than count before this plan (at least 6 new it.fails in logs describe)
    - rg -n "COOLIFY_NO_DEPLOYMENTS" src/utils/errors.test.ts returns at least 1
    - rg -n "action: deployment.logs" docs/coverage-map.yaml returns 1
    - npx vitest run src/mcp/tools/deployment.test.ts src/utils/errors.test.ts exits 0
  </acceptance_criteria>
  <done>OBS-01 RED scaffolds + coverage-map row locked; husky green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → handlers | Mocked Coolify API; no live tokens in asserts |
| coverage-map.yaml → CI | Declarative mapping only; no runtime secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-00-01 | Information Disclosure | system.test.ts | low | mitigate | Assert version JSON excludes COOLIFY_TOKEN (scaffold carries pattern into Plan 24-01) |
| T-24-00-02 | Tampering | coverage-map.yaml | low | accept | Row is declarative; Plan 24-03 regenerates COVERAGE.md under test |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in Wave 0 |
</threat_model>

<verification>
npx vitest run src/mcp/tools/system.test.ts src/mcp/tools/meta.test.ts src/mcp/tools/deployment.test.ts src/utils/errors.test.ts -x exits 0; coverage-map has deployment.logs row.
</verification>

<success_criteria>
All Wave 0 VALIDATION checklist items for tests + coverage-map row present as it.fails scaffolds; production handlers untouched.
</success_criteria>

<output>
Create `.planning/phases/24-capabilities-deployment-logs/24-00-SUMMARY.md` when done
</output>
