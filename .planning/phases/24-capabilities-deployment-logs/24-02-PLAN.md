---
phase: 24-capabilities-deployment-logs
plan: 02
type: execute
wave: 2
depends_on:
  - "24-00"
  - "24-01"
files_modified:
  - src/utils/log-helpers.ts
  - src/utils/log-helpers.test.ts
  - src/mcp/tools/deployment.ts
  - src/mcp/tools/deployment.test.ts
  - src/mcp/tools/application.ts
  - src/mcp/tools/application.test.ts
  - src/utils/errors.ts
  - src/utils/errors.test.ts
autonomous: true
requirements:
  - OBS-01
user_setup: []

must_haves:
  truths:
    - "Agent fetches build logs via deployment.logs with deployment_uuid without calling application.logs (OBS-01, D-10)"
    - "deployment.logs accepts XOR deployment_uuid | application_uuid; application_uuid resolves newest by created_at regardless of status (D-13)"
    - "application_uuid with zero deployments returns structured COOLIFY_NO_DEPLOYMENTS + recovery hints — not empty logs body (D-14)"
    - "Existing deployment with empty logs string returns soft OK empty logs_lines + hint (D-16)"
    - "application.logs build path via deployment_uuid still works via shared processDeploymentBuildLogs (D-11, D-17)"
    - "deployment.watch include_logs path unchanged — not routed through deployment.logs (D-12)"
    - "Param parity with application.logs build path: lines, offset, include_hidden, type, format pretty|json, max_chars (D-15)"
  artifacts:
    - path: src/utils/log-helpers.ts
      provides: "processDeploymentBuildLogs shared processor"
      exports: ["processDeploymentBuildLogs"]
    - path: src/mcp/tools/deployment.ts
      provides: "logs action + handleDeploymentLogs"
      contains: "logs"
    - path: src/utils/errors.ts
      provides: "COOLIFY_NO_DEPLOYMENTS code + RECOVERY_HINTS"
      contains: "COOLIFY_NO_DEPLOYMENTS"
  key_links:
    - from: "handleDeploymentLogs"
      to: "fetchDeployment / fetchAppDeployments"
      via: "resolve uuid then processDeploymentBuildLogs then buildReadResponse"
    - from: "handleApplicationLogs build branch"
      to: "processDeploymentBuildLogs"
      via: "thin wrapper after fetchDeployment"
    - from: "application_uuid empty list"
      to: "CoolifyApiError COOLIFY_NO_DEPLOYMENTS"
      via: "throw before fetchDeployment"
  assumptions:
    - "A-EDGE-OBS-01: concurrent deployment.logs calls are independent on-demand fetches; no shared mutable poller state"
    - "created_at is reliable for newest sort (RESEARCH A3); fallback id field if deployment_uuid missing"
    - "Coolify logs field is JSON-encoded string array or plain string (RESEARCH A2)"
  prohibitions:
    - statement: "Do not route deployment.logs through handleApplicationAction"
      status: enforced
      verification: "rg -n \"handleApplicationAction\" src/mcp/tools/deployment.ts returns 0"
    - statement: "Do not route deployment.watch through processDeploymentBuildLogs / logs handler"
      status: enforced
      verification: "handleDeploymentWatch body still uses existing include_logs path"
    - statement: "Do not register a new top-level MCP logs tool"
      status: enforced
      verification: "rg -n \"registerTool\\('logs'\" src/mcp/server.ts returns 0"
    - statement: "Do not remove application.logs build path (D-11)"
      status: enforced
      verification: "npx vitest run src/mcp/tools/application.test.ts -t \"build logs\" exits 0"
---

<objective>
Implement OBS-01: `deployment.logs` action on the existing deployment tool with shared build-log processing, XOR uuid resolution, and structured empty-deployment errors.

Purpose: Domain-correct discovery path for build logs (agents stop needing application.logs + deployment_uuid). Keeps app build path as back-compat.

Output: `processDeploymentBuildLogs`, `deployment.logs` handler+schema, `COOLIFY_NO_DEPLOYMENTS`, green deployment/application/errors tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md
@.planning/phases/24-capabilities-deployment-logs/24-RESEARCH.md
@.planning/phases/24-capabilities-deployment-logs/24-PATTERNS.md
@.planning/phases/24-capabilities-deployment-logs/24-00-SUMMARY.md
@.planning/phases/24-capabilities-deployment-logs/24-01-SUMMARY.md
@src/mcp/tools/deployment.ts
@src/mcp/tools/application.ts
@src/utils/log-helpers.ts
@src/utils/errors.ts
</context>

## Artifacts this phase produces

- `processDeploymentBuildLogs` in `src/utils/log-helpers.ts` (D-17).
- `deployment.logs` action on `deploymentToolSchema` + `handleDeploymentLogs` (D-10, D-13–D-16).
- `COOLIFY_NO_DEPLOYMENTS` in `CoolifyErrorCode` + `RECOVERY_HINTS` (D-14).
- Thin `application.logs` build-path wrapper (D-11).
- Green Wave 0 deployment/errors/log-helpers tests.

<tasks>

<task type="tracer">
  <name>End-to-end deployment.logs by deployment_uuid</name>
  <reversibility rating="costly">D-10/D-15 publish deployment.logs action + param contract on the deployment tool catalog.</reversibility>
  <files>src/utils/log-helpers.ts, src/mcp/tools/deployment.ts, src/mcp/tools/application.ts, src/mcp/tools/deployment.test.ts, src/mcp/tools/application.test.ts, src/utils/log-helpers.test.ts</files>
  <read_first>
    - .planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md (D-10–D-12, D-15–D-17)
    - .planning/phases/24-capabilities-deployment-logs/24-RESEARCH.md (Patterns 3, 5; Pitfalls 5–6)
    - .planning/phases/24-capabilities-deployment-logs/24-PATTERNS.md (deployment.ts watch analog; log-helpers extraction; application build path)
    - src/mcp/tools/application.ts (handleApplicationLogs build path ~L1440-1510; sharedLogParams / offset)
    - src/mcp/tools/deployment.ts (createFlatActionSchema watch registration; handleDeploymentGet fetch pattern)
    - src/utils/log-helpers.ts (parseBuildLogEntries, sliceLogBlob, capLogOutput)
    - src/mcp/tools/shared-read-params.ts (sharedLogParamsFlatShape / schema defaults)
    - src/mcp/tools/deployment.test.ts (Wave 0 logs describe)
  </read_first>
  <behavior>
    - deploymentToolSchema accepts action logs with deployment_uuid and log params defaults (lines 100 etc.)
    - handleDeploymentAction logs with deployment_uuid calls fetchDeployment then returns buildReadResponse envelope with deployment_uuid, status, logs_lines, truncation counters
    - application.logs with deployment_uuid still returns same envelope semantics via shared processor
    - Wave 0 fetch-by-uuid + sensitive-required + empty-logs cases flip green where in scope of this tracer
  </behavior>
  <action>
    Per D-10/D-15/D-17: extract processDeploymentBuildLogs(deploymentUuid, rec, params) into log-helpers.ts from application.ts build branch — include D-16 empty-string soft OK + hint; COOLIFY_403_SENSITIVE_REQUIRED when logs not a string; reuse parseBuildLogEntries/sliceLogBlob/capLogOutput. Refactor application build path to fetchDeployment + processDeploymentBuildLogs + buildReadResponse. Add logs to deployment actions array, deploymentActionsCatalog string, actionAllowedFields, zodDefaultFields, and shared log param fields (spread sharedLogParamsFlatShape + offset). Implement handleDeploymentLogs for deployment_uuid path only in this tracer (application_uuid resolution can stub-throw or be completed in next task — prefer full uuid path green). Dispatch case logs in handleDeploymentAction. Do not call handleApplicationAction. Do not change handleDeploymentWatch include_logs. Flip relevant it.fails in deployment.test.ts and log-helpers.test.ts to it for uuid + empty + sensitive cases.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts -t "logs" -x</automated>
    <automated>npx vitest run src/mcp/tools/application.test.ts -t "build logs" -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "processDeploymentBuildLogs" src/utils/log-helpers.ts returns at least 1
    - rg -n "'logs'" src/mcp/tools/deployment.ts returns at least 3
    - rg -n "processDeploymentBuildLogs" src/mcp/tools/application.ts returns at least 1
    - rg -n "handleApplicationAction" src/mcp/tools/deployment.ts returns 0
    - npx vitest run src/mcp/tools/deployment.test.ts -t "logs" exits 0 for uuid/empty/sensitive cases implemented
    - npx vitest run src/mcp/tools/application.test.ts -t "build logs" exits 0
  </acceptance_criteria>
  <done>deployment.logs by uuid works end-to-end; application build path reuses shared processor.</done>
</task>

<task type="auto" tdd="true">
  <name>XOR application_uuid resolution + COOLIFY_NO_DEPLOYMENTS</name>
  <reversibility rating="costly">D-13/D-14 lock agent-visible latest-deploy policy and no-deployments error contract.</reversibility>
  <files>src/mcp/tools/deployment.ts, src/mcp/tools/deployment.test.ts, src/utils/errors.ts, src/utils/errors.test.ts</files>
  <read_first>
    - .planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md (D-13, D-14)
    - .planning/phases/24-capabilities-deployment-logs/24-RESEARCH.md (Pattern 4; Code Examples NO_DEPLOYMENTS; Pitfall 3–4)
    - .planning/phases/24-capabilities-deployment-logs/24-PATTERNS.md (extraRefine XOR; resolveLatestDeploymentUuid)
    - src/mcp/tools/deployment.ts (handleDeploymentLogs from tracer; list uses fetchAppDeployments)
    - src/mcp/tools/application.ts (logs XOR superRefine ~L189-206 as pattern)
    - src/utils/errors.ts (CoolifyErrorCode + RECOVERY_HINTS)
    - src/mcp/tools/deployment.test.ts (mockDeployments dep-3 newest)
  </read_first>
  <behavior>
    - Schema XOR: exactly one of deployment_uuid or application_uuid required for logs
    - application_uuid with mockDeployments resolves dep-3 and returns that deployment_uuid in envelope
    - empty deployments → isError COOLIFY_NO_DEPLOYMENTS with recoveryHints referencing application.deploy and deployment.list
    - RECOVERY_HINTS.COOLIFY_NO_DEPLOYMENTS defined
  </behavior>
  <action>
    Per D-13/D-14: add extraRefine on deploymentToolSchema for action logs enforcing XOR of deployment_uuid and application_uuid. Implement resolveLatestDeploymentUuid sorting by created_at descending regardless of status; use deployment_uuid or id field. When fetchAppDeployments returns empty array, throw CoolifyApiError with new CoolifyErrorCode COOLIFY_NO_DEPLOYMENTS and RECOVERY_HINTS entries for deploy-first and list-deployments (RESEARCH example). Echo resolved deployment_uuid in success envelope when input was application_uuid. Flip remaining Wave 0 logs it.fails (schema XOR, latest, no deployments) and errors.test.ts to green it.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts -t "logs|schema|latest|no deployments" -x</automated>
    <automated>npx vitest run src/utils/errors.test.ts -t "COOLIFY_NO_DEPLOYMENTS" -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "COOLIFY_NO_DEPLOYMENTS" src/utils/errors.ts returns at least 2
    - rg -n "resolveLatestDeploymentUuid|created_at" src/mcp/tools/deployment.ts returns at least 1
    - rg -n "dep-3" src/mcp/tools/deployment.test.ts returns at least 1 in logs describe
    - npx vitest run src/mcp/tools/deployment.test.ts -t "logs" exits 0
    - npx vitest run src/utils/errors.test.ts -t "COOLIFY_NO_DEPLOYMENTS" exits 0
  </acceptance_criteria>
  <done>OBS-01 full surface: XOR, newest resolution, structured no-deployments error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → deployment.logs | Untrusted uuids + log filter params |
| deployment.logs → Coolify GET /deployments/{uuid} | Authenticated; logs may contain secrets |
| Token abilities | api.sensitive required for logs field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-10 | Information Disclosure | processDeploymentBuildLogs | high | mitigate | Require api.sensitive; throw COOLIFY_403_SENSITIVE_REQUIRED when logs non-string; tool descriptions already warn agents about secret leakage in logs |
| T-24-11 | Tampering | crafted Coolify logs payload | medium | mitigate | Cap via max_chars; treat Coolify as trusted origin; no eval of log content |
| T-24-12 | Denial of Service | large log blobs | medium | mitigate | lines/max_chars bounds via sharedLogParams Zod defaults |
| T-24-13 | Elevation of Privilege | capability soft flags | low | accept | D-04: flags do not gate this handler |
| T-24-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/mcp/tools/deployment.test.ts src/mcp/tools/application.test.ts -t "logs|build logs" src/utils/errors.test.ts -x; full OBS-01 behaviors green.
</verification>

<success_criteria>
Agent can fetch deployment build logs via deployment.logs by uuid or newest application deploy; empty-deploy error structured; app build path unchanged in behavior.
</success_criteria>

<output>
Create `.planning/phases/24-capabilities-deployment-logs/24-02-SUMMARY.md` when done
</output>
