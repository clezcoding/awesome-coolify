---
phase: 24-capabilities-deployment-logs
plan: 01
type: execute
wave: 1
depends_on:
  - "24-00"
files_modified:
  - src/mcp/capabilities.ts
  - src/utils/package-version.ts
  - src/mcp/tools/system.ts
  - src/mcp/tools/meta.ts
  - src/mcp/tools/system.test.ts
  - src/mcp/tools/meta.test.ts
autonomous: false
requirements:
  - CAP-01
  - CAP-02
user_setup: []

must_haves:
  truths:
    - "Agent calls system.version and receives coolifyVersion (Coolify) plus mcpVersion (package.json) plus serverName (CAP-01, D-06, D-07, D-08)"
    - "system.version.capabilities is a flat map of four D-03 keys each { supported, coolify_min_version, note? } from static COOLIFY_412_CAPABILITIES (CAP-02, D-01, D-02)"
    - "meta.version still returns mcpVersion+serverName only — no capabilities field — with mcpVersion from readPackageVersion() (D-06, D-08)"
    - "systemActionsCatalog documents coolifyVersion rename (not dual version alias) (D-09)"
    - "Version JSON never includes API token"
  artifacts:
    - path: src/mcp/capabilities.ts
      provides: "COOLIFY_412_CAPABILITIES static table"
      exports: ["COOLIFY_412_CAPABILITIES"]
      contains: "application_logs"
    - path: src/utils/package-version.ts
      provides: "readPackageVersion cached package.json reader"
      exports: ["readPackageVersion"]
    - path: src/mcp/tools/system.ts
      provides: "extended SystemVersionResult + version handler"
      contains: "coolifyVersion"
    - path: src/mcp/tools/meta.ts
      provides: "meta.version via readPackageVersion"
      contains: "readPackageVersion"
  key_links:
    - from: "handleSystemAction version"
      to: "fetchVersion + readPackageVersion + COOLIFY_412_CAPABILITIES"
      via: "return { coolifyVersion, mcpVersion, serverName, capabilities }"
    - from: "handleMetaAction version"
      to: "readPackageVersion"
      via: "mcpVersion field"
  assumptions:
    - "A-EDGE-CAP-01: response field names locked by D-07; no version alias (D-09)"
    - "A-EDGE-CAP-02: all four keys supported:true for 4.1.2 target (RESEARCH A4)"
    - "package.json remains in npm tarball at package root (RESEARCH A1)"
  prohibitions:
    - statement: "No dual-field alias version alongside coolifyVersion (D-09)"
      status: enforced
      verification: "rg -n \"version:\" src/mcp/tools/system.ts version case returns coolifyVersion only"
    - statement: "No diagnose/diagnose_logs capability keys (D-03)"
      status: enforced
      verification: "rg -n \"diagnose\" src/mcp/capabilities.ts returns 0"
    - statement: "No Zod/schema hard-block on capability flags (D-04)"
      status: enforced
      verification: "capabilities.ts is data-only; no imports into deploymentToolSchema"
    - statement: "meta.version must not emit capabilities"
      status: enforced
      verification: "rg -n \"capabilities\" src/mcp/tools/meta.ts returns 0"
---

<objective>
Implement CAP-01/CAP-02: extend `system.version` with Coolify + MCP package identity and static Coolify 4.1.2 capability flags; fix stale `meta` version source.

Purpose: Agents discover what this MCP supports on the 4.1.2 target before calling log/watch tools (foundation for Phases 25–26).

Output: `capabilities.ts`, `package-version.ts`, updated `system.ts`/`meta.ts`, green system/meta tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md
@.planning/phases/24-capabilities-deployment-logs/24-RESEARCH.md
@.planning/phases/24-capabilities-deployment-logs/24-PATTERNS.md
@.planning/phases/24-capabilities-deployment-logs/24-00-SUMMARY.md
@src/mcp/tools/system.ts
@src/mcp/tools/meta.ts
@package.json
</context>

## Artifacts this phase produces

- `src/mcp/capabilities.ts` — `COOLIFY_412_CAPABILITIES` export (D-01–D-05).
- `src/utils/package-version.ts` — `readPackageVersion()` (D-08).
- `src/mcp/tools/system.ts` — `SystemVersionResult` + version case + catalog note (D-06/D-07/D-09).
- `src/mcp/tools/meta.ts` — package-backed `mcpVersion`; keep `MCP_SERVER_NAME` export.
- Green `system.test.ts` / `meta.test.ts` (flip Wave 0 `it.fails` → `it`).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm system.version field rename (D-07 / D-09)</name>
  <read_first>
    - .planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md (D-07, D-09)
    - src/mcp/tools/system.ts (version case returning { version })
    - src/mcp/tools/system.test.ts (Wave 0 target shape)
  </read_first>
  <decision>Break system.version callers by renaming response field version → coolifyVersion with no dual-field alias</decision>
  <context>Locked in discuss-phase as one-way. Undo would require restoring { version } or shipping both fields — D-09 explicitly rejected alias. Acceptable on 1.0.x with catalog/README note (Plan 24-03).</context>
  <options>
    <option id="rename-coolifyVersion">
      <name>Rename to coolifyVersion only (D-07/D-09)</name>
      <pros>Matches locked CONTEXT; clear Coolify vs MCP identity; catalog documents break</pros>
      <cons>Breaks agents/tests expecting { version }</cons>
    </option>
    <option id="keep-version-alias">
      <name>Keep dual version + coolifyVersion alias</name>
      <pros>Softer migration</pros>
      <cons>Violates locked D-09; confuses agents</cons>
    </option>
  </options>
  <resume-signal>Select: rename-coolifyVersion (required for D-07/D-09) or keep-version-alias (escalate — breaks locked decision)</resume-signal>
  <acceptance_criteria>
    - Maintainer selected rename-coolifyVersion
    - keep-version-alias does not proceed to tracer task
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>End-to-end system.version → coolifyVersion + mcpVersion + capabilities</name>
  <reversibility rating="one-way">D-07 renames published system.version wire shape; callers expecting { version } break without migration.</reversibility>
  <files>src/mcp/capabilities.ts, src/utils/package-version.ts, src/mcp/tools/system.ts, src/mcp/tools/system.test.ts</files>
  <read_first>
    - .planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md (D-01–D-09)
    - .planning/phases/24-capabilities-deployment-logs/24-RESEARCH.md (Patterns 1–2, Pattern 6)
    - .planning/phases/24-capabilities-deployment-logs/24-PATTERNS.md (capabilities.ts, package-version.ts, system.ts)
    - src/mcp/tools/system.ts (SystemVersionResult; version + verify cases; systemActionsCatalog)
    - src/mcp/tools/meta.ts (MCP_SERVER_NAME export)
    - package.json (version field)
    - src/mcp/tools/system.test.ts (Wave 0 scaffolds)
  </read_first>
  <behavior>
    - handleSystemAction({ action:'version' }) returns coolifyVersion from fetchVersion, mcpVersion from readPackageVersion(), serverName MCP_SERVER_NAME, capabilities COOLIFY_412_CAPABILITIES
    - capabilities has exactly four keys from D-03 each with supported:true and coolify_min_version string
    - JSON.stringify(result) does not contain token
    - Wave 0 it.fails for version/capabilities flip to green it
  </behavior>
  <action>
    After rename-coolifyVersion approval: create src/mcp/capabilities.ts exporting COOLIFY_412_CAPABILITIES as const satisfies Record with keys application_logs, deployment_logs, deployment_watch, deploy_watch per RESEARCH Pattern 2 notes and coolify_min_version strings consistent with D-03/D-05 (supported true for all four). Create src/utils/package-version.ts with cached readPackageVersion reading package.json via import.meta.url path resolve dirname ../package.json from utils. Extend SystemVersionResult and version case in system.ts to return coolifyVersion (reuse verify extraction logic or tiny shared inline helper in same file), mcpVersion: readPackageVersion(), serverName from meta MCP_SERVER_NAME import, capabilities: COOLIFY_412_CAPABILITIES. Update systemActionsCatalog to document new response fields and coolifyVersion rename (D-09). Flip system.test.ts version/capabilities it.fails to it. Per D-04: capabilities is soft guidance only — do not wire into Zod. Per D-02: static table only — no OpenAPI diff.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/system.test.ts -t "version|capabilities" -x</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/mcp/capabilities.ts && test -f src/utils/package-version.ts
    - rg -n "COOLIFY_412_CAPABILITIES" src/mcp/capabilities.ts returns at least 1
    - rg -n "coolifyVersion" src/mcp/tools/system.ts returns at least 2
    - rg -n "readPackageVersion" src/mcp/tools/system.ts returns at least 1
    - rg -n "application_logs" src/mcp/capabilities.ts returns 1
    - npx vitest run src/mcp/tools/system.test.ts -t "version|capabilities" exits 0
  </acceptance_criteria>
  <done>system.version returns full CAP-01/CAP-02 payload; Wave 0 system scaffolds green.</done>
</task>

<task type="auto" tdd="true">
  <name>Align meta.version to readPackageVersion; flip meta tests</name>
  <reversibility rating="costly">D-06/D-08 dual surfaces share package identity; changing source later needs coordinated meta+system updates.</reversibility>
  <files>src/mcp/tools/meta.ts, src/mcp/tools/meta.test.ts</files>
  <read_first>
    - src/mcp/tools/meta.ts (MCP_VERSION constant)
    - src/utils/package-version.ts (from tracer)
    - src/mcp/tools/meta.test.ts (Wave 0 scaffold)
    - .planning/phases/24-capabilities-deployment-logs/24-CONTEXT.md (D-06, D-08)
  </read_first>
  <behavior>
    - handleMetaAction({ action:'version' }) returns mcpVersion equal to readPackageVersion() and serverName awesome-coolify-mcp
    - result has no capabilities property
    - stale MCP_VERSION constant removed or no longer used as runtime source
  </behavior>
  <action>
    Per D-06/D-08: replace MCP_VERSION constant usage in handleMetaAction with readPackageVersion(). Keep MCP_SERVER_NAME export. Do not add capabilities to meta. Flip meta.test.ts to green asserting mcpVersion === readPackageVersion(). Remove or stop exporting stale MCP_VERSION if tests no longer import it — update any remaining imports in repo via rg MCP_VERSION. Discretion: capabilities stay on system.version only.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/meta.test.ts src/mcp/tools/system.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "readPackageVersion" src/mcp/tools/meta.ts returns at least 1
    - rg -n "MCP_VERSION\\s*=\\s*'0\\.1\\.0'" src/mcp/tools/meta.ts returns 0
    - rg -n "capabilities" src/mcp/tools/meta.ts returns 0
    - npx vitest run src/mcp/tools/meta.test.ts src/mcp/tools/system.test.ts exits 0
  </acceptance_criteria>
  <done>meta and system share package.json version; CAP-01 accurate on both surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → system.version | Untrusted MCP args (instance routing only); response must not leak token |
| system.version → Coolify GET /version | Authenticated Coolify call; version string untrusted content |
| readPackageVersion → filesystem | Trusted package root package.json only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Information Disclosure | system.version JSON | medium | mitigate | Assert no COOLIFY_TOKEN in serialized result (existing system.test pattern) |
| T-24-02 | Elevation of Privilege | capabilities flags | medium | mitigate | Soft guidance only (D-04); tools stay registered; document not a hard authz gate |
| T-24-03 | Spoofing | mcpVersion filesystem | low | accept | Read only adjacent package.json shipped in npm tarball |
| T-24-SC | Tampering | npm installs | low | accept | No new packages |

</threat_model>

<verification>
npx vitest run src/mcp/tools/system.test.ts src/mcp/tools/meta.test.ts -x exits 0; capabilities has four D-03 keys; meta has no capabilities.
</verification>

<success_criteria>
CAP-01 and CAP-02 observable: system.version returns Coolify + MCP versions + static capability map; meta version fixed to package.json.
</success_criteria>

<output>
Create `.planning/phases/24-capabilities-deployment-logs/24-01-SUMMARY.md` when done
</output>
