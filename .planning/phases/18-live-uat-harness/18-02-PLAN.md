---
phase: 18-live-uat-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - scripts/live-uat.mjs
autonomous: true
requirements:
  - UAT-01
  - UAT-03
user_setup: []

must_haves:
  truths:
    - McpStdioClient spawns dist/index.js as a child process, sends JSON-RPC 2.0 over stdin/stdout, drains NDJSON responses by newline, and rejects any request that exceeds 30 seconds with a timeout error
    - Stdio runner executes every matrix row with mode "stdio" in declared order, captures structuredContent, error code, recoveryHints presence, and duration per row, and never prints the resolved token in any captured field
    - JSON report is written to stdout as a single object with rows array, summary {pass, fail, skip, planned, v3_gaps}, and exit code mapping 0/1/2 per D-20
    - Optional --out <path> writes the same JSON object to the given file path; Markdown report is also generated alongside JSON when --out is set, with one row per test and a summary table
    - Child process is killed with SIGTERM in a finally block and awaited for 500ms before exit regardless of runner outcome
    - Probe assumption (ordering): report rows preserve matrix declaration order so re-runs are diffable
    - Probe assumption (empty): when zero stdio rows match the suite filter the runner emits a v3_gaps entry and skips without crashing
  artifacts:
    - scripts/live-uat.mjs (extended with McpStdioClient, stdio runner, JSON + Markdown report writers)
  key_links:
    - scripts/live-uat.mjs → dist/index.js (spawned child, the real MCP server)
    - scripts/live-uat.mjs → scripts/live-uat.matrix.json (filter by mode stdio)
  prohibitions:
    - Never spawn the child with shell:true (T-18-03)
    - Never write the resolved COOLIFY_TOKEN to stdout, --out, or Markdown — every field passes through redact()
    - Never execute in-process handlers in this plan; only stdio-mode rows run here
    - Never hang the runner — every JSON-RPC request has a 30s timeout and the child is SIGTERM'd in finally
---

<objective>
Wire the stdio runner and the report writers onto the Plan 01 skeleton. Spawn the real MCP server from dist/index.js, send tools/list and every stdio-mode matrix row, capture per-row pass/fail with duration and recovery-hint presence, and emit the canonical JSON report to stdout plus optional --out and a Markdown companion.

Purpose: Stdio is the only mode that proves the real MCP server boots and speaks JSON-RPC 2.0 — the smoke tier and v3 stdio paths (system, instance.cloud-info, manifest get/diff) all live here. JSON report is the canonical machine artifact (UAT-03); Markdown is the human companion (D-18).
Output: scripts/live-uat.mjs with McpStdioClient, stdio runner, JSON + Markdown report writers, exit codes 0/1/2.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-live-uat-harness/18-CONTEXT.md
@.planning/phases/18-live-uat-harness/18-RESEARCH.md
@.planning/phases/18-live-uat-harness/18-PATTERNS.md
@scripts/live-uat-milestone-optional.mjs
@scripts/live-uat.mjs
@src/mcp/server.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: McpStdioClient + stdio runner for matrix rows</name>
  <files>scripts/live-uat.mjs</files>
  <read_first>
    - scripts/live-uat-milestone-optional.mjs (McpStdioClient class, drain, request, timeout, finally SIGTERM)
    - scripts/live-uat.mjs (Plan 01 skeleton: routingEnv, redact, matrix, CLI flags)
    - .planning/phases/18-live-uat-harness/18-CONTEXT.md (D-04 stdio coverage, D-17 never print tokens)
  </read_first>
  <behavior>
    - Spawning dist/index.js with routingEnv plus COOLIFY_MCP_LOG=error yields a child that responds to initialize and tools/call
    - tools/list returns a list of tool names that includes every tool registered in src/mcp/server.ts
    - For each stdio-mode matrix row the runner sends tools/call with name and arguments, captures structuredContent, and records pass/fail, durationMs, errorCode, recoveryHintsPresent, and status
    - A row whose response error code is -32602 or whose structuredContent ok is false is recorded as fail with errorCode from structuredContent.error.code
    - A row that throws or times out is recorded as fail with errorCode UAT_TIMEOUT or UAT_CRASH
    - No captured field contains the resolved COOLIFY_TOKEN
  </behavior>
  <action>
    Extend scripts/live-uat.mjs with a McpStdioClient class ported from live-uat-milestone-optional.mjs: constructor takes the spawned child, maintains a pending Map keyed by JSON-RPC id, drains stdout by newline, parses each line as JSON, resolves the matching pending request, and rejects on a 30-second timeout per request. Add a spawnChild() helper that spawn()s node with argv [resolve(root,'dist/index.js')], env {…process.env, …routingEnv, COOLIFY_MCP_LOG:'error'}, stdio ['pipe','pipe','pipe'], and no shell:true. Add a runStdioRows(matrix, suite) function that filters matrix rows by mode === 'stdio' and suite membership (smoke always; full only when --full is set), spawns the child, calls initialize with protocolVersion '2024-11-05' and clientInfo {name:'live-uat', version:'1.0'}, sends notifications/initialized, then iterates rows in declared order. For each row send tools/call with {name: row.tool, arguments: row.args} (handle tools/list as a special case using method tools/list with empty params). Capture startTime before the call and durationMs after. Extract structuredContent from result.structuredContent; if missing or ok===false set status 'fail' and errorCode from structuredContent.error.code or 'RPC_'+res.error.code. Set recoveryHintsPresent true when structuredContent.error.recoveryHints is a non-empty array. Wrap every captured field in redact() before storing. On thrown error or timeout set status 'fail' and errorCode 'UAT_TIMEOUT' or 'UAT_CRASH'. Always kill the child with SIGTERM in a finally block and await 500ms. Return the rows array. Do not write the report in this task — Task 2 does that.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('child_process'); const env={...process.env,TSX_ACTIVE:'true',UAT_PROJECT_UUID:process.env.UAT_PROJECT_UUID||''}; const r=spawnSync('node',['scripts/live-uat.mjs','--out','/tmp/uat-stdio.json'],{env,encoding:'utf8'}); if(r.status===2){console.log('setup-abort (expected when no live creds) — skeleton gate holds'); process.exit(0);} const rep=JSON.parse(require('fs').readFileSync('/tmp/uat-stdio.json','utf8')); if(!Array.isArray(rep.rows))throw new Error('rows missing'); if(!rep.summary)throw new Error('summary missing'); if(JSON.stringify(rep).includes(process.env.COOLIFY_TOKEN||'__none__')&&process.env.COOLIFY_TOKEN)throw new Error('token leaked in report'); console.log('stdio report rows='+rep.rows.length);"</automated>
  </verify>
  <done>Stdio runner executes every stdio-mode row, captures per-row outcome with duration and recoveryHintsPresent, never leaks the token, and SIGTERMs the child in finally. Report file contains rows array and summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JSON stdout + --out + Markdown report, exit codes 0/1/2</name>
  <files>scripts/live-uat.mjs</files>
  <read_first>
    - scripts/live-uat.mjs (Task 1 stdio runner output shape)
    - .planning/phases/18-live-uat-harness/18-CONTEXT.md (D-17, D-18, D-20)
  </read_first>
  <behavior>
    - With no fails the process exits 0; with one or more fails it exits 1; setup aborts exit 2 (already implemented in Plan 01)
    - JSON report printed to stdout is a single parseable object with rows, summary, and v3_gaps
    - When --out <path> is set the same JSON object is written to that path
    - A Markdown report is generated alongside the JSON when --out is set, with a heading, a summary table, and one row per test
    - No report field contains the resolved COOLIFY_TOKEN
  </behavior>
  <action>
    Extend scripts/live-uat.mjs with a buildReport(rows, v3Gaps) function that returns {rows, summary:{pass:rows.filter(r=>r.status==='pass').length, fail:…, skip:…, planned:…, v3_gaps:v3Gaps.length}, v3_gaps:v3Gaps}. Add a writeMarkdown(report, path) helper that emits a Markdown document with a heading, a summary table (pass/fail/skip/planned/v3_gaps), and a per-row table with id, tool, status, durationMs, errorCode, recoveryHintsPresent. Add a main() that calls runStdioRows, builds the report, prints JSON.stringify(report) to stdout, writes the JSON to --out path when set, writes the Markdown companion to the same path with .md suffix when --out is set, computes exit code (0 when fail count is 0, 1 otherwise), and exits. Wrap every string that touches an output stream in redact(). Reuse the Plan 01 exit-2 path for setup aborts — do not duplicate it. Keep the rows in matrix declaration order so re-runs are diffable.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('child_process'); const fs=require('fs'); const env={...process.env,TSX_ACTIVE:'true',UAT_PROJECT_UUID:process.env.UAT_PROJECT_UUID||''}; const r=spawnSync('node',['scripts/live-uat.mjs','--out','/tmp/uat-report.json'],{env,encoding:'utf8'}); if(r.status===2){console.log('setup-abort — gate holds'); process.exit(0);} const j=JSON.parse(r.stdout); if(!j.summary||typeof j.summary.pass!=='number')throw new Error('summary missing'); if(!fs.existsSync('/tmp/uat-report.json'))throw new Error('json --out missing'); if(!fs.existsSync('/tmp/uat-report.md'))throw new Error('markdown companion missing'); if(r.status!==0&&r.status!==1)throw new Error('exit code must be 0 or 1, got '+r.status); if((r.stdout+fs.readFileSync('/tmp/uat-report.json','utf8')+fs.readFileSync('/tmp/uat-report.md','utf8')).includes(process.env.COOLIFY_TOKEN||'__none__')&&process.env.COOLIFY_TOKEN)throw new Error('token leaked'); console.log('report OK exit='+r.status);"</automated>
  </verify>
  <done>JSON report on stdout is parseable, --out writes JSON plus Markdown companion, exit code is 0 on no fails and 1 on any fail, no token appears in any report surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harness → spawned MCP child | Local stdio pipes; child receives routingEnv including token |
| Harness → stdout/--out/Markdown | All three are leak surfaces for the resolved token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Information Disclosure | report writers | high | mitigate | every captured field and every report string passes through redact() before stdout, --out, or Markdown write; verify greps all three surfaces for the live token |
| T-18-04 | Denial of Service | McpStdioClient timeout | high | mitigate | every JSON-RPC request has a 30-second timeout; child is SIGTERM'd in finally and awaited 500ms so the runner cannot hang the maintainer terminal |
| T-18-03 | Tampering | spawnChild argv | medium | mitigate | spawn() called with explicit argv array, no shell:true; routingEnv is built from resolved creds only, never from unvalidated user input |
</threat_model>

<verification>
- `node scripts/live-uat.mjs --out /tmp/uat.json` exits 0 or 1 (or 2 on setup abort)
- /tmp/uat.json and /tmp/uat.md both exist
- stdout parses as JSON with rows and summary
- None of stdout, /tmp/uat.json, /tmp/uat.md contains the resolved COOLIFY_TOKEN
</verification>

<success_criteria>
- Stdio runner covers every stdio-mode matrix row including v3 mandatory rows
- JSON report on stdout is the canonical machine artifact
- --out writes JSON; Markdown companion written alongside
- Exit codes 0/1/2 enforced per D-20
- No token leak across any surface
</success_criteria>

<output>
Create `.planning/phases/18-live-uat-harness/18-02-SUMMARY.md` when done
</output>
