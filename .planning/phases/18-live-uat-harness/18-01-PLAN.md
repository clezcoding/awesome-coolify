---
phase: 18-live-uat-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/live-uat.matrix.json
  - scripts/live-uat.mjs
autonomous: true
requirements:
  - UAT-01
  - UAT-02
  - UAT-04
  - UAT-05
user_setup: []

must_haves:
  truths:
    - scripts/live-uat.matrix.json exists and declares every MCP tool registered in src/mcp/server.ts with at least one row per tool
    - scripts/live-uat.mjs exists, is executable via `node scripts/live-uat.mjs`, and respawns itself through `npx tsx` when TSX_ACTIVE is unset so TypeScript handler imports work
    - Harness resolves credentials from .cursor/mcp.json (project then user), process.env, and ~/.coolify-mcp/instances.json via InstanceManager.resolveCredentials without ever writing COOLIFY_TOKEN to stdout, stderr, JSON, or Markdown
    - Harness aborts with exit code 2 when UAT_PROJECT_UUID is unset, empty, or does not match the project returned by handleProjectAction get for that UUID
    - Matrix includes the v3 mandatory rows: at least one row with explicit instance arg and one without (multi-instance), one instance.cloud-info row (cloud), and one manifest get plus one manifest diff row (manifest), each tagged with suite:v3
    - Every matrix row carries id, tool, args, type (read|write|destructive), mode (stdio|in-process), and suite (smoke|v3|full) fields so downstream runners can dispatch without re-parsing
    - Probe assumption (empty): when the matrix has zero rows for a tool the runner must emit v3_gaps and skip rather than crash; matrix as committed has no empty tool coverage
    - Probe assumption (ordering): matrix row order is the deterministic execution order; runner preserves it in the report so re-runs are comparable
    - Probe assumption (adjacency): two tools with overlapping domain (e.g., application vs deployment) do not merge in the matrix; each gets its own row set so failures isolate cleanly
  artifacts:
    - scripts/live-uat.matrix.json
    - scripts/live-uat.mjs
  key_links:
    - scripts/live-uat.mjs → InstanceManager.resolveCredentials (src/utils/instance-registry.ts)
    - scripts/live-uat.mjs → handleProjectAction (src/mcp/tools/project.ts) for UAT_PROJECT_UUID precondition check
    - scripts/live-uat.matrix.json → scripts/live-uat.mjs loader (readFileSync + JSON.parse)
  prohibitions:
    - Never print COOLIFY_TOKEN or any resolved credential value to any output stream or report file (D-17, UAT-02)
    - Never auto-create the UAT project; missing UAT_PROJECT_UUID aborts with exit 2 (D-05, D-06, D-08)
    - Never execute write or destructive actions in this plan; only skeleton + matrix ship here (D-09, D-12)
    - Never hardcode test cases inside live-uat.mjs; all coverage lives in the declarative matrix (D-16, RESEARCH anti-pattern)
---

<objective>
Ship the declarative coverage matrix and the harness skeleton: tsx respawn, Zod-validated CLI args, credential resolution with token redaction, UAT_PROJECT_UUID identity gate, and matrix loader. No test execution yet — downstream plans wire stdio and in-process runners onto this skeleton.

Purpose: Establish the single source of truth for coverage (matrix) and the safe credential/identity foundation every runner reuses, so UAT-02 (no token leak) and UAT-05 (identity gate) hold by construction regardless of which runner is invoked.
Output: scripts/live-uat.matrix.json (committed, declarative) and scripts/live-uat.mjs (skeleton with creds, redaction, gate, loader, exit-2 abort).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-live-uat-harness/18-CONTEXT.md
@.planning/phases/18-live-uat-harness/18-RESEARCH.md
@.planning/phases/18-live-uat-harness/18-PATTERNS.md
@scripts/live-uat-milestone-optional.mjs
@src/utils/instance-registry.ts
@src/mcp/server.ts
@src/mcp/tools/project.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create declarative coverage matrix</name>
  <files>scripts/live-uat.matrix.json</files>
  <read_first>
    - src/mcp/server.ts (enumerate every registerTool call — count and names)
    - .planning/phases/18-live-uat-harness/18-CONTEXT.md (D-13, D-14, D-16 v3 mandatory rows)
    - .planning/phases/18-live-uat-harness/18-PATTERNS.md (matrix schema example)
  </read_first>
  <behavior>
    - Matrix loads as a JSON array with one object per test row
    - Every tool registered in src/mcp/server.ts has at least one row
    - v3 mandatory rows exist: multi-instance (one with explicit instance, one without), instance.cloud-info, manifest get, manifest diff — each tagged with tag "suite:v3"
    - Each row has id, tool, args, type, mode, suite fields; destructive rows carry type "destructive"; write rows carry type "write"
    - UAT_PROJECT_UUID placeholder is the literal token UAT_PROJECT_UUID_PLACEHOLDER inside args (runner substitutes at runtime)
  </behavior>
  <action>
    Create scripts/live-uat.matrix.json as a committed declarative array. Enumerate every tool registered in src/mcp/server.ts and give each at least one smoke row (read mode, type read). For domain tools with multiple read actions pick one safe read (e.g., system health, resource list, project get with UAT_PROJECT_UUID_PLACEHOLDER, instance list, manifest get, manifest diff, docs search, diagnose scan, deployment list, application list, service list, database list, server list, private_key list, environment list, emergency list-status). Add the v3 mandatory rows per D-14: one row with an explicit instance arg (use a placeholder instance name "uat-secondary" so the runner can skip with v3_gaps when that instance is not registered) and one row without any instance arg, both targeting system health; one instance.cloud-info row; one manifest get row; one manifest diff row. Tag each v3 row with tag "suite:v3". Add a small set of write and destructive rows (type "write" for application restart, type "destructive" for application delete and manifest clear) but mark them suite "full" so the default runner skips them unless --full is passed. Use mode "stdio" for tools/list, system, instance.cloud-info, manifest get, manifest diff, and one read per domain tool per D-04; use mode "in-process" for the remaining rows. Keep the row order stable and deterministic. Do not inline any token, UUID, or URL — only placeholders.
  </action>
  <verify>
    <automated>node -e "const m=require('./scripts/live-uat.matrix.json'); const s=require('./src/mcp/server.ts'); const fs=require('fs'); const src=fs.readFileSync('./src/mcp/server.ts','utf8'); const names=[...src.matchAll(/registerTool\(\s*\{[^}]*?name:\s*'([^']+)'/g)].map(x=>x[1]); const tools=new Set(m.map(r=>r.tool==='tools/list'?'__list__':r.tool)); const missing=names.filter(n=>!tools.has(n)); if(missing.length)throw new Error('matrix missing tools: '+missing.join(',')); if(!m.some(r=>r.tag==='suite:v3'&&r.args&&r.instance))throw new Error('missing v3 explicit-instance row'); if(!m.some(r=>r.tool==='instance'&&r.args&&r.args.action==='cloud-info'&&r.tag==='suite:v3'))throw new Error('missing v3 cloud-info row'); if(!m.some(r=>r.tool==='manifest'&&r.args.action==='get'&&r.tag==='suite:v3'))throw new Error('missing v3 manifest get row'); if(!m.some(r=>r.tool==='manifest'&&r.args.action==='diff'&&r.tag==='suite:v3'))throw new Error('missing v3 manifest diff row'); console.log('matrix OK rows='+m.length);"</automated>
  </verify>
  <done>Matrix file exists, parses as JSON, covers every registered tool, and contains all four v3 mandatory rows tagged suite:v3. Verification command prints matrix OK with row count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harness skeleton — tsx respawn, CLI args, credentials, redaction, UAT gate, matrix loader</name>
  <files>scripts/live-uat.mjs</files>
  <read_first>
    - scripts/live-uat-milestone-optional.mjs (PREFERRED_NAMES, pickServer, resolveMcpEnv, McpStdioClient timeout pattern)
    - src/utils/instance-registry.ts (InstanceManager.resolveCredentials signature, redaction behavior)
    - src/mcp/tools/project.ts (handleProjectAction get args shape)
    - .planning/phases/18-live-uat-harness/18-CONTEXT.md (D-01, D-02, D-08, D-09, D-12, D-17, D-20)
  </read_first>
  <behavior>
    - Running `node scripts/live-uat.mjs` with no env respawns via npx tsx with TSX_ACTIVE=true and inherits exit status
    - With TSX_ACTIVE=true and no UAT_PROJECT_UUID the process exits with code 2 and a JSON error envelope on stderr (no token printed)
    - With TSX_ACTIVE=true and a UAT_PROJECT_UUID that does not match the live project the process exits with code 2
    - With TSX_ACTIVE=true and a valid UAT_PROJECT_UUID but no credentials anywhere the process exits with code 2
    - A redact(value) helper replaces every occurrence of the resolved COOLIFY_TOKEN with the literal three-star mask before any string is written to stdout, stderr, --out file, or Markdown
    - Matrix loader reads scripts/live-uat.matrix.json, parses it, and substitutes UAT_PROJECT_UUID_PLACEHOLDER with process.env.UAT_PROJECT_UUID in every args value
    - CLI parses --write, --confirm-destructive, --full, --out <path> via Zod; unknown flags error with exit 2
  </behavior>
  <action>
    Create scripts/live-uat.mjs with shebang. Implement the tsx respawn guard at top: when process.env.TSX_ACTIVE is unset, spawnSync npx tsx with argv.slice(1), inherit stdio, set TSX_ACTIVE=true, and exit with the child status. Below the guard, import InstanceManager from ../src/utils/instance-registry.ts and handleProjectAction from ../src/mcp/tools/project.ts (TypeScript imports work because tsx is active). Define a Zod schema for CLI args: write (boolean default false), confirm-destructive (boolean default false), full (boolean default false), out (string optional). Resolve credentials by first reusing the mcp.json pickServer logic from live-uat-milestone-optional.mjs (project then user .cursor/mcp.json), then falling back to process.env COOLIFY_URL/COOLIFY_TOKEN, then InstanceManager.resolveCredentials(undefined, process.env) for the registry default. If no creds found, print a JSON envelope {status:"setup-abort", error:"COOLIFY_NO_INSTANCE"} to stderr and exit 2. Build a routingEnv object containing COOLIFY_URL, COOLIFY_TOKEN, and any instance-specific overrides. Implement redact(value) that takes any string or object and returns a copy with every occurrence of routingEnv.COOLIFY_TOKEN replaced by the literal three-star mask; use it on every output path. Enforce UAT_PROJECT_UUID: if unset or empty, exit 2 with {status:"setup-abort", error:"UAT_PROJECT_UUID_REQUIRED"}. Call handleProjectAction({action:"get", uuid: process.env.UAT_PROJECT_UUID}, routingEnv) and if the structuredContent ok is not true exit 2 with {status:"setup-abort", error:"UAT_PROJECT_UUID_MISMATCH"}. Load the matrix from scripts/live-uat.matrix.json, substitute UAT_PROJECT_UUID_PLACEHOLDER in args with the env UUID, and expose the parsed matrix plus routingEnv, redact, and parsed CLI flags on a module-level state object that downstream runner plans will consume. Do not implement the stdio client, in-process dispatch, or report writers in this plan — leave them as TODO hooks that the next plans fill. End the skeleton by returning early with a JSON envelope {status:"skeleton-ready", rows: matrix.length, toolsCovered: <n>} so the file runs without executing tests yet.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('child_process'); const env={...process.env, TSX_ACTIVE:'true'}; const r1=spawnSync('node',['scripts/live-uat.mjs'],{env:{...env,UAT_PROJECT_UUID:''},encoding:'utf8'}); if(r1.status!==2)throw new Error('empty UUID should exit 2, got '+r1.status); if(r1.stderr.includes('COOLIFY_')){} if(r1.stdout||r1.stderr){ if((r1.stdout+(r1.stderr||'')).includes(process.env.COOLIFY_TOKEN||'__none__')&&process.env.COOLIFY_TOKEN)throw new Error('token leaked');} console.log('exit-2 gate OK');"</automated>
  </verify>
  <done>Skeleton runs, exits 2 on missing/empty UAT_PROJECT_UUID, never prints the resolved token, loads the matrix, and substitutes the UUID placeholder. Downstream plans can extend the skeleton without re-litigating credentials or the identity gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer CLI → local filesystem | Harness reads .cursor/mcp.json, ~/.coolify-mcp/instances.json, process.env — all local, no remote |
| Harness → live Coolify API | Outbound HTTPS with resolved Bearer token; responses may contain secrets |
| Harness → stdout/stderr/--out/Markdown | Any output stream is a leak surface for the resolved token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Information Disclosure | redact() + every output path | high | mitigate | redact() replaces every occurrence of routingEnv.COOLIFY_TOKEN with the three-star mask before any write to stdout, stderr, --out, or Markdown; verified by exit-2 gate test that greps output for the live token |
| T-18-02 | Elevation of Privilege | UAT_PROJECT_UUID gate | high | mitigate | Skeleton aborts with exit 2 when UAT_PROJECT_UUID is unset, empty, or does not match the live project returned by handleProjectAction get; no write/destructive code path exists in this plan |
| T-18-03 | Tampering | tsx respawn spawnSync | medium | mitigate | Respawn uses spawnSync with an explicit argv array (no shell:true); CLI args validated by Zod before any side effect |
| T-18-SC | Tampering | npx tsx invocation | low | accept | tsx is an established dev tool already used in this repo; the [SUS] freshness flag from RESEARCH is accepted per maintainer approval — no new package install in this plan |
</threat_model>

<verification>
- `node -e "require('./scripts/live-uat.matrix.json')"` parses
- Matrix covers every registerTool name in src/mcp/server.ts
- `node scripts/live-uat.mjs` with empty UAT_PROJECT_UUID exits 2
- No output stream contains the resolved COOLIFY_TOKEN
</verification>

<success_criteria>
- Matrix committed with all 14 tools + 4 v3 mandatory rows tagged suite:v3
- Skeleton respawns via tsx, resolves credentials from all three sources, redacts tokens, gates on UAT_PROJECT_UUID, loads matrix, exits 2 on setup failure
- No write or destructive action executes in this plan
</success_criteria>

<output>
Create `.planning/phases/18-live-uat-harness/18-01-SUMMARY.md` when done
</output>
