---
phase: 18-live-uat-harness
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - scripts/live-uat.mjs
autonomous: true
requirements:
  - UAT-01
  - UAT-04
  - UAT-05
user_setup: []

must_haves:
  truths:
    - In-process runner imports every handler from src/mcp/tools/*.ts and dispatches by matrix row tool name, invoking the handler with (args, routingEnv) and capturing structuredContent, durationMs, errorCode, and recoveryHintsPresent
    - Default run executes only rows with type "read"; rows with type "write" are skipped as "planned" unless --write is set; rows with type "destructive" are skipped as "planned" unless both --write and --confirm-destructive are set (D-09, D-10, D-12)
    - Every mutating call (write or destructive) is gated by an identity check that the resolved target UUID belongs to the UAT project; calls targeting resources outside UAT_PROJECT_UUID are skipped as "blocked-outside-uat" even when flags are set (D-05, D-07)
    - v3_gaps array is populated when a v3 mandatory row cannot run because a live precondition is missing (no secondary instance registered, no cloud creds, no manifest file, etc.); the row is skipped with reason and the summary still exits 0 if all other rows pass (D-15)
    - --full expands the executed suite to include rows tagged suite "full" so the entire action matrix runs in-process; without --full only smoke and v3 rows run
    - Combined report merges stdio rows from Plan 02 with in-process rows from this plan in matrix declaration order
    - Probe assumption (adjacency): in-process dispatch never confuses two tools with overlapping domain; each row's tool name maps to exactly one handler
    - Probe assumption (empty): when a handler import fails the runner records UAT_IMPORT_FAIL for that tool's rows and continues, never crashing the whole suite
  artifacts:
    - scripts/live-uat.mjs (extended with in-process runner, two-tier flag gate, v3_gaps, --full expansion, merged report)
  key_links:
    - scripts/live-uat.mjs → every src/mcp/tools/*.ts handler (handleApplicationAction, handleServiceAction, handleDatabaseAction, handleDeploymentAction, handleInstanceAction, handleEnvironmentAction, handleServerAction, handlePrivateKeyAction, handleProjectAction, handleResourceAction, handleSystemAction, handleDiagnoseAction, handleEmergencyAction, handleManifestAction, handleDocsAction, handleMetaAction)
  prohibitions:
    - Never execute a destructive row without both --write and --confirm-destructive (D-10, D-11, UAT-05)
    - Never execute a write row without --write (D-09)
    - Never mutate a resource whose project UUID differs from UAT_PROJECT_UUID (D-05)
    - Never print the resolved COOLIFY_TOKEN in any in-process captured field or report
    - Never auto-cleanup or auto-delete as part of a normal run (D-07)
---

<objective>
Add the in-process runner that executes the bulk of the action matrix directly against imported TypeScript handlers, with two-tier flag gating, UAT-project identity scoping for mutations, v3_gaps skip semantics, and --full expansion. Merge the in-process rows with the stdio rows from Plan 02 into the final report.

Purpose: In-process invocation avoids spawning one child per call while still proving every handler works against live data. The two-tier flag gate (D-09, D-10, D-12) and UAT-project identity scope (D-05, D-07) are the safety core of UAT-05. v3_gaps (D-15) lets the suite pass when optional preconditions are missing.
Output: scripts/live-uat.mjs with in-process runner, flag gate, identity scope, v3_gaps, --full, merged report.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-live-uat-harness/18-CONTEXT.md
@.planning/phases/18-live-uat-harness/18-RESEARCH.md
@.planning/phases/18-live-uat-harness/18-PATTERNS.md
@scripts/live-uat.mjs
@src/mcp/tools/application.ts
@src/mcp/tools/service.ts
@src/mcp/tools/database.ts
@src/mcp/tools/deployment.ts
@src/mcp/tools/instance.ts
@src/mcp/tools/environment.ts
@src/mcp/tools/server.ts
@src/mcp/tools/private_key.ts
@src/mcp/tools/project.ts
@src/mcp/tools/resource.ts
@src/mcp/tools/system.ts
@src/mcp/tools/diagnose.ts
@src/mcp/tools/emergency.ts
@src/mcp/tools/manifest.ts
@src/mcp/tools/docs.ts
@src/mcp/tools/meta.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: In-process handler dispatch + two-tier flag gate + UAT identity scope</name>
  <files>scripts/live-uat.mjs</files>
  <read_first>
    - scripts/live-uat.mjs (Plan 02 stdio runner, routingEnv, redact, CLI flags, matrix loader)
    - src/mcp/tools/*.ts (handler signatures: handleXxxAction(args, env) → {ok, structuredContent, ...})
    - .planning/phases/18-live-uat-harness/18-CONTEXT.md (D-05, D-07, D-09, D-10, D-11, D-12)
  </read_first>
  <behavior>
    - runInProcessRows filters matrix rows by mode === 'in-process' and suite membership using the contract "smoke and v3 always; full only when flags.full is set" (matches runStdioRows in 18-02 verbatim) so v3 mandatory rows run in the default invocation
    - A matrix row with tool "system" and args {action:"health"} dispatched in-process calls handleSystemAction(args, routingEnv) and returns structuredContent
    - A row with type "write" and no --write flag is recorded as status "planned" with errorCode null and not executed
    - A row with type "destructive" and --write but no --confirm-destructive is recorded as status "planned"
    - A row with type "destructive" and both flags set executes and records pass/fail
    - A mutating row whose args target a UUID not belonging to UAT_PROJECT_UUID is recorded as status "blocked-outside-uat" and not executed even when flags are set
    - A handler that throws is recorded as status "fail" with errorCode UAT_CRASH and the suite continues
    - No captured field contains the resolved COOLIFY_TOKEN
  </behavior>
  <action>
    Extend scripts/live-uat.mjs with an in-process handler dispatch map. Import every handle*Action from src/mcp/tools/*.ts (TypeScript imports work because tsx is active). Build a Map from tool name to handler function. Add a runInProcessRows(matrix, flags, routingEnv) function that filters matrix rows by mode === 'in-process' and suite membership using the contract "smoke and v3 always; full only when flags.full is set" — i.e. keep rows where row.suite === 'smoke' || row.suite === 'v3' || (row.suite === 'full' && flags.full). This filter contract MUST match runStdioRows in Plan 18-02 verbatim so v3 mandatory rows (suite:"v3") run in the default invocation and reach UAT-04. For each row in declared order: compute allowed = row.type === 'read' || (row.type === 'write' && flags.write) || (row.type === 'destructive' && flags.write && flags.confirmDestructive). When not allowed, push {id, tool, status:'planned', durationMs:0, errorCode:null, recoveryHintsPresent:false} and continue. When allowed and row.type !== 'read', call a guardUatScope(row, routingEnv) helper that resolves the target resource's project UUID via handleResourceAction or handleApplicationAction get (depending on row.tool) and returns false when the resolved project UUID differs from process.env.UAT_PROJECT_UUID; when false push {status:'blocked-outside-uat', ...} and continue. Otherwise invoke dispatchMap.get(row.tool)(row.args, routingEnv), capture startTime/durationMs, extract structuredContent, set status 'pass' when structuredContent.ok === true, 'fail' when ok === false with errorCode from structuredContent.error.code and recoveryHintsPresent from structuredContent.error.recoveryHints length > 0. On thrown error set status 'fail' and errorCode 'UAT_CRASH'. Wrap every captured field in redact(). Return the rows array. Do not write the report here — Task 2 merges and writes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('./scripts/live-uat.mjs','utf8'); if(!/function runInProcessRows/.test(s))throw new Error('runInProcessRows missing'); const filterRe=/suite\s*===?\s*['\"]smoke['\"]\s*\|\|\s*row\.suite\s*===?\s*['\"]v3['\"]\s*\|\|\s*\(row\.suite\s*===?\s*['\"]full['\"]\s*&&\s*flags\.full\)/; if(!filterRe.test(s))throw new Error('runInProcessRows suite filter contract missing (smoke and v3 always; full only with flags.full) — must match runStdioRows in 18-02'); if(!/row\.type\s*===?\s*['\"]read['\"]\s*\|\|\s*\(row\.type\s*===?\s*['\"]write['\"]\s*&&\s*flags\.write\)\s*\|\|\s*\(row\.type\s*===?\s*['\"]destructive['\"]\s*&&\s*flags\.write\s*&&\s*flags\.confirmDestructive\)/.test(s))throw new Error('two-tier flag gate (read|write+flags.write|destructive+flags.write+flags.confirmDestructive) missing'); if(!/blocked-outside-uat/.test(s))throw new Error('UAT identity scope blocked-outside-uat status missing'); if(!/UAT_CRASH/.test(s))throw new Error('UAT_CRASH errorCode missing'); if(!/redact\(/.test(s))throw new Error('redact() call missing on captured fields'); console.log('in-process structural checks OK (flag gate + identity scope + filter contract)');"</automated>
  </verify>
  <done>In-process runner dispatches every in-process matrix row, gates writes on --write, gates destructive on --write plus --confirm-destructive, blocks mutations outside UAT_PROJECT_UUID, records planned for missing flags, never leaks the token. Structural checks for flag gate, identity scope, and filter contract pass without spawning the live MCP server — keeps task feedback latency under 60s per Nyquist. Live run exercised as the Wave 3 gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: v3_gaps skip + --full expansion + merged report</name>
  <files>scripts/live-uat.mjs</files>
  <read_first>
    - scripts/live-uat.mjs (Task 1 in-process runner, Plan 02 stdio runner + report writers)
    - .planning/phases/18-live-uat-harness/18-CONTEXT.md (D-13, D-14, D-15, D-16)
  </read_first>
  <behavior>
    - When a v3 mandatory row cannot run because a live precondition is missing (no secondary instance, no cloud creds, no manifest file), the row is recorded as status "skip" with a reason and the v3_gaps array gains an entry; the suite still exits 0 when all other rows pass
    - --full flag expands the executed suite to include rows tagged suite "full" so the entire action matrix runs in-process; without --full only smoke and v3 rows run
    - Final report merges stdio rows and in-process rows in matrix declaration order with a single summary and a single v3_gaps array
    - Exit code 0 when fail count is 0 (skips and planned do not fail); 1 when fail count is greater than 0; 2 on setup abort
  </behavior>
  <action>
    Extend scripts/live-uat.mjs with a detectV3Gaps(matrix, routingEnv) function that, for each v3 mandatory row, checks the precondition: for the multi-instance row with explicit instance arg, call InstanceManager to test whether that instance name is registered and resolve to a non-null credential; if not, push {id, reason:'no-secondary-instance'}. For the cloud row, check whether routingEnv.COOLIFY_URL looks like a cloud host or whether instance.cloud-info returns a cloud profile; if not, push {id, reason:'no-cloud-creds'}. For the manifest rows, check whether a manifest file exists at the project root; if not, push {id, reason:'no-manifest'}. Modify runInProcessRows and runStdioRows to skip a v3 row whose id is in v3_gaps with status 'skip' and the reason. Wire --full so the suite filter includes suite 'full' rows when flags.full is true. Replace the main() from Plan 02 with a version that calls both runStdioRows and runInProcessRows, merges their row arrays in matrix declaration order, calls buildReport with the merged rows and v3_gaps, prints JSON to stdout, writes JSON and Markdown to --out when set, and exits 0/1/2. Keep all output fields redacted.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('./scripts/live-uat.mjs','utf8'); if(!/function detectV3Gaps/.test(s))throw new Error('detectV3Gaps missing'); if(!/no-secondary-instance/.test(s))throw new Error('v3_gaps reason no-secondary-instance missing'); if(!/no-cloud-creds/.test(s))throw new Error('v3_gaps reason no-cloud-creds missing'); if(!/no-manifest/.test(s))throw new Error('v3_gaps reason no-manifest missing'); if(!/flags\.full/.test(s))throw new Error('--full expansion wiring missing'); if(!/runStdioRows/.test(s)&&!/runInProcessRows/.test(s))throw new Error('runner invocations missing'); if(!/buildReport/.test(s))throw new Error('buildReport call missing in merged main'); if(!/v3_gaps/.test(s))throw new Error('v3_gaps field missing in report'); console.log('v3_gaps + --full + merged report structural checks OK');"</automated>
  </verify>
  <done>v3_gaps populated for missing preconditions (no-secondary-instance, no-cloud-creds, no-manifest), --full expands suite, merged report preserves matrix order with single summary and v3_gaps array, exit codes 0/1/2 enforced, no token leak. Structural checks pass without spawning the live MCP server — keeps task feedback latency under 60s per Nyquist. Live run exercised as the Wave 3 gate.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harness → in-process handler | Handler runs in same process with routingEnv; any leak is a process-wide leak |
| Harness → live Coolify API via handler | Outbound HTTPS with token; responses may contain secrets |
| Mutating row → live resources | Write/destructive rows can change or delete real Coolify resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-02 | Elevation of Privilege | two-tier flag gate + UAT identity scope | high | mitigate | read-only default; write rows require --write; destructive rows require --write plus --confirm-destructive; mutating rows blocked when target project UUID differs from UAT_PROJECT_UUID; missing flags produce status planned with no execution |
| T-18-05 | Tampering | handler dispatch map | medium | mitigate | dispatch map keyed by exact tool name from matrix; unknown tool name recorded as UAT_IMPORT_FAIL and skipped, never crashes the suite |
| T-18-01 | Information Disclosure | in-process captured fields | high | mitigate | every captured structuredContent and error field passes through redact() before storage in the report |
| T-18-06 | Repudiation | v3_gaps + skip reasons | low | accept | skip reasons are recorded in the report for auditability; no additional logging required at ASVS L1 |
</threat_model>

<verification>
- Structural checks (per task) confirm runInProcessRows, two-tier flag gate, UAT identity scope, v3_gaps, --full expansion, and merged report wiring present without spawning the live server — keeps task feedback latency under 60s per Nyquist
- Wave 3 gate (run before /gsd-verify-work): `node scripts/live-uat.mjs --out /tmp/uat.json` (no flags) and `node scripts/live-uat.mjs --full --out /tmp/uat-full.json` against the live UAT instance exit 0 or 1 (or 2 on setup abort)
- /tmp/uat.json and /tmp/uat-full.json (plus .md companions) exist after the wave-gate runs
- stdout parses as JSON with rows, summary, and v3_gaps
- Mutations blocked outside UAT_PROJECT_UUID
- None of stdout, /tmp/uat*.json, /tmp/uat*.md contains the resolved COOLIFY_TOKEN
</verification>

<success_criteria>
- In-process runner covers every in-process matrix row
- Two-tier flag gate enforced (D-09, D-10, D-12)
- UAT identity scope blocks cross-project mutations (D-05, D-07)
- v3_gaps skip lets the suite pass with missing optional preconditions (D-15)
- --full expands the suite (D-13)
- Merged report preserves matrix order with single summary
- Exit codes 0/1/2 (D-20)
</success_criteria>

<output>
Create `.planning/phases/18-live-uat-harness/18-03-SUMMARY.md` when done
</output>
