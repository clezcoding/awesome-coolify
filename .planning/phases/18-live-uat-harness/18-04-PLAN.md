---
phase: 18-live-uat-harness
plan: 04
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - package.json
  - CONTRIBUTING.md
autonomous: true
requirements:
  - UAT-06
user_setup: []

must_haves:
  truths:
    - package.json scripts contain "uat:live" mapped to "node scripts/live-uat.mjs" so `npm run uat:live` invokes the harness
    - `npm pack --dry-run` output excludes scripts/live-uat.mjs and scripts/live-uat.matrix.json from the published tarball (files allowlist already restricts to dist, .env.example, LICENSE)
    - CONTRIBUTING.md has a Live UAT Harness section documenting how to run `npm run uat:live`, the UAT_PROJECT_UUID precondition, the dedicated UAT project expectation, all four flags (--write, --confirm-destructive, --full, --out), the JSON plus Markdown report interpretation, exit codes 0/1/2, the v3_gaps concept, and the maintainer-local-only stance (no CI, no remote secrets, never in the npm tarball)
    - CONTRIBUTING.md UAT section references the declarative matrix file by path so maintainers can extend coverage without editing the script
  artifacts:
    - package.json (scripts.uat:live added)
    - CONTRIBUTING.md (Live UAT Harness section added)
  key_links:
    - package.json scripts.uat:live → scripts/live-uat.mjs
    - CONTRIBUTING.md → scripts/live-uat.matrix.json (extension guidance)
  prohibitions:
    - Never add scripts/ to the npm files allowlist (D-03, UAT-02)
    - Never document any workflow that puts credentials on a remote or in CI (D-19, UAT-02)
    - Never document auto-create or auto-cleanup as a supported flow (D-06, D-07)
---

<objective>
Wire the `uat:live` npm script, prove the harness is excluded from the npm tarball, and document the live UAT runbook in CONTRIBUTING.md so any maintainer can run the harness and interpret the report.

Purpose: UAT-06 requires CONTRIBUTING.md to document the run + interpretation. D-02 requires `npm run uat:live` as the entry. D-03 requires the harness never ship in the npm package. This plan closes all three without touching the harness itself.
Output: package.json with scripts.uat:live, CONTRIBUTING.md with Live UAT Harness section, npm pack --dry-run proof that scripts/ stays out of the tarball.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-live-uat-harness/18-CONTEXT.md
@.planning/phases/18-live-uat-harness/18-RESEARCH.md
@package.json
@CONTRIBUTING.md
@scripts/live-uat.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add uat:live script and verify npm pack excludes scripts/</name>
  <files>package.json</files>
  <read_first>
    - package.json (current scripts block, files allowlist)
    - .planning/phases/18-live-uat-harness/18-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Edit package.json scripts to add "uat:live": "node scripts/live-uat.mjs" alongside the existing scripts. Do not modify the files allowlist (already restricted to dist, .env.example, LICENSE). Do not add scripts/ to files. Do not change bin or prepublishOnly. After editing, run `npm pack --dry-run` and confirm the printed tarball contents exclude scripts/live-uat.mjs and scripts/live-uat.matrix.json; if either appears, shrink the files allowlist further rather than expanding it.
  </action>
  <verify>
    <automated>node -e "const {spawnSync}=require('child_process'); const p=require('./package.json'); if(p.scripts['uat:live']!=='node scripts/live-uat.mjs')throw new Error('uat:live missing or wrong'); const r=spawnSync('npm',['pack','--dry-run','--json'],{encoding:'utf8'}); if(r.status!==0)throw new Error('npm pack --dry-run failed: '+r.stderr); const files=JSON.parse(r.stdout)[0].files.map(f=>f.path); const bad=files.filter(f=>f.includes('scripts/live-uat')); if(bad.length)throw new Error('harness leaked into tarball: '+bad.join(',')); console.log('uat:live OK + npm pack excludes scripts/live-uat ('+files.length+' files)');"</automated>
  </verify>
  <done>package.json scripts.uat:live exists and maps to node scripts/live-uat.mjs. `npm pack --dry-run` JSON output excludes scripts/live-uat.mjs and scripts/live-uat.matrix.json from the published tarball.</done>
</task>

<task type="auto">
  <name>Task 2: Live UAT Harness section in CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (existing section structure, Local Setup heading)
    - .planning/phases/18-live-uat-harness/18-CONTEXT.md (D-02, D-05, D-06, D-07, D-09, D-10, D-12, D-13, D-17, D-18, D-19, D-20, D-21)
    - scripts/live-uat.mjs (flags, exit codes, matrix path)
  </read_first>
  <action>
    Append a new "## Live UAT Harness" section to CONTRIBUTING.md after the Local Setup section. Document: (1) the entry `npm run uat:live` and that it runs `node scripts/live-uat.mjs`; (2) the precondition that the maintainer must set UAT_PROJECT_UUID to a dedicated UAT project UUID and that the harness aborts with exit 2 when this is missing or mismatched; (3) that the UAT project is a manual prerequisite — no auto-create, no auto-cleanup; (4) the four flags: --write unlocks create/update/restart/deploy, --confirm-destructive additionally unlocks deletes/emergency bulk/manifest prune, --full runs the entire action matrix, --out <path> writes the JSON report to a file plus a Markdown companion; (5) that without --write the harness runs read-only and records write/destructive rows as status planned; (6) that the JSON report is printed to stdout as the canonical machine artifact and the Markdown companion is the human artifact; (7) exit codes: 0 means no fails (skips and planned are fine), 1 means at least one fail, 2 means setup abort; (8) the v3_gaps concept — when a v3 mandatory row cannot run because a live precondition is missing (no secondary instance, no cloud creds, no manifest), the row is skipped and listed in v3_gaps and the suite can still exit 0; (9) that credentials resolve from .cursor/mcp.json, process.env, or ~/.coolify-mcp/instances.json and that tokens are never printed; (10) that the harness is maintainer-local only — no CI job, no remote secrets, never in the npm tarball; (11) that coverage lives in scripts/live-uat.matrix.json and maintainers extend coverage by editing that file, not the script. Use prose and short code fences for the command examples only. Do not link to .planning/ artifacts.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./CONTRIBUTING.md','utf8'); const need=['npm run uat:live','UAT_PROJECT_UUID','--write','--confirm-destructive','--full','--out','v3_gaps','exit 2','scripts/live-uat.matrix.json']; const missing=need.filter(k=>!c.includes(k)); if(missing.length)throw new Error('CONTRIBUTING missing: '+missing.join(',')); if(!/## Live UAT Harness/.test(c))throw new Error('section heading missing'); console.log('CONTRIBUTING UAT section OK');"</automated>
  </verify>
  <done>CONTRIBUTING.md has a Live UAT Harness section covering the entry, preconditions, all four flags, report interpretation, exit codes, v3_gaps, credential stance, and matrix extension guidance.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Block on:** high
**Threats open:** 0

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer → CONTRIBUTING.md | Docs are a leak surface if they ever show a real token or UUID |
| npm publish → public registry | Tarball must never contain the harness or the matrix |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-07 | Information Disclosure | CONTRIBUTING.md examples | medium | mitigate | docs use placeholders only, never a real token or UUID; verify command greps for the literal flag names, not for example values |
| T-18-08 | Tampering | npm files allowlist | high | mitigate | files allowlist stays restricted to dist, .env.example, LICENSE; npm pack --dry-run must exclude scripts/live-uat.mjs and scripts/live-uat.matrix.json; any drift shrinks the allowlist, never expands it |
</threat_model>

<verification>
- `npm run uat:live --silent` (or setup-abort exit 2 when no creds) confirms the script is wired
- `npm pack --dry-run` output excludes scripts/live-uat.mjs and scripts/live-uat.matrix.json
- CONTRIBUTING.md contains the Live UAT Harness section with all required keywords
</verification>

<success_criteria>
- npm run uat:live is the documented entry
- Harness excluded from npm tarball (D-03)
- CONTRIBUTING.md runbook complete (UAT-06, D-21)
- No real token or UUID in docs
</success_criteria>

<output>
Create `.planning/phases/18-live-uat-harness/18-04-SUMMARY.md` when done
</output>
