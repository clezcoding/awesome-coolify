---
phase: 09-project-environment-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/client.ts
  - src/mcp/tools/resource.ts
  - src/utils/project-lookup.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05]
must_haves:
  truths:
    - "client.ts exports createProject, updateProject, deleteProject, fetchEnvironments, fetchEnvironment, createEnvironment, deleteEnvironment (all via createCoolifyClient withMappedErrors)"
    - "resource.list accepts type:'project' and type:'environment' and returns projected summaries per D-04"
    - "project-lookup.ts exports resolveProjectUuid(project_uuid XOR project_name) with COOLIFY_AMBIGUOUS_MATCH on multi-match per D-12/D-14"
    - "project-lookup.ts exports resolveEnvironmentUuid(env_uuid|env_name, project_uuid) using /projects/{uuid}/{name_or_uuid} passthrough per D-13"
    - "Emptiness pre-check uses /resources (client-side) so COOLIFY_409 carries child UUIDs reliably per RESEARCH pitfall 2"
  artifacts:
    - src/api/client.ts (extended)
    - src/mcp/tools/resource.ts (extended)
    - src/utils/project-lookup.ts (extended)
  key_links:
    - "createProject → POST /projects returning { uuid }"
    - "createEnvironment → POST /projects/{uuid}/environments { name } returning { uuid }"
    - "deleteEnvironment → DELETE /projects/{uuid}/environments/{name_or_uuid}"
    - "fetchEnvironment → GET /projects/{uuid}/{name_or_uuid}"
    - "resource.list type=project → fetchProjects → projectProjectSummary"
    - "resource.list type=environment → fetchProjects → flatten project.environments → projectEnvironmentSummary"
  prohibitions:
    - statement: "No environment update client function is added — API has no PATCH (PROHIB_ABSENT)"
      status: enforced
      verification: "grep -c 'updateEnvironment' src/api/client.ts returns 0"
    - statement: "No app/service/db CRUD client functions in this plan — Phases 10-11 (scope fence)"
      status: enforced
      verification: "grep -cE 'createApplication|createService|createDatabase' src/api/client.ts returns 0"
---

<objective>
Wave 1 shared infrastructure: extend the API client with project + environment CRUD, extend resource.list with type=project|environment, and add name-to-UUID resolvers. 09-02 and 09-03 import these without file conflicts.

Purpose: Single home for all Coolify project/environment HTTP calls and discovery so the two handler plans stay thin.
Output: Extended client.ts, resource.ts, project-lookup.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-project-environment-crud/09-CONTEXT.md
@.planning/phases/09-project-environment-crud/09-RESEARCH.md
@.planning/phases/09-project-environment-crud/09-PATTERNS.md
@src/api/client.ts
@src/mcp/tools/resource.ts
@src/utils/project-lookup.ts
@src/utils/errors.ts
@docs/coolify_openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project + environment CRUD to client.ts</name>
  <files>src/api/client.ts</files>
  <read_first>
    - src/api/client.ts (existing fetchProjects/fetchProject + createPrivateKey/updatePrivateKey/deletePrivateKey patterns at lines 124-313)
    - docs/coolify_openapi.yaml lines 5493-5820 (project + environment endpoints)
    - .planning/phases/09-project-environment-crud/09-CONTEXT.md (D-10, D-11, D-13, D-15)
  </read_first>
  <behavior>
    - createProject(url, token, { name, description? }, verifySsl) → POST /projects, returns { uuid }
    - updateProject(url, token, uuid, { name?, description? }, verifySsl) → PATCH /projects/{uuid}, returns { uuid, name, description }
    - deleteProject(url, token, uuid, verifySsl) → DELETE /projects/{uuid}, returns { message }
    - fetchEnvironments(url, token, projectUuid, verifySsl) → GET /projects/{uuid}/environments, returns array (defensive [])
    - fetchEnvironment(url, token, projectUuid, nameOrUuid, verifySsl) → GET /projects/{uuid}/{name_or_uuid}, returns object
    - createEnvironment(url, token, projectUuid, { name }, verifySsl) → POST /projects/{uuid}/environments { name }, returns { uuid }
    - deleteEnvironment(url, token, projectUuid, nameOrUuid, verifySsl) → DELETE /projects/{uuid}/environments/{name_or_uuid}, returns { message }
    - All wrapped by createCoolifyClient (withMappedErrors) for consistent error envelopes
  </behavior>
  <action>
Append to src/api/client.ts after existing fetchProject (line 142). Use createCoolifyClient(url, token, verifySsl) per existing pattern. createProject body: { name, ...(description ? { description } : {}) }. updateProject body: only defined fields. fetchEnvironments: Array.isArray(result) ? result : []. No environment update function (API has no PATCH — prohibition). No app/service/db CRUD (scope fence). Per D-13: fetchEnvironment and deleteEnvironment accept nameOrUuid passthrough (Coolify API resolves name OR uuid server-side). Per D-15: createEnvironment surfaces API 409 via existing mapApiError → COOLIFY_409 (no special handling needed; withMappedErrors already maps 409).
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && npx tsc --noEmit src/api/client.ts 2>&1 | grep -c 'error TS' | grep -qE '^0$' && grep -cE 'export async function (createProject|updateProject|deleteProject|fetchEnvironments|fetchEnvironment|createEnvironment|deleteEnvironment)' src/api/client.ts | grep -q 7 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 7 new exported async functions present: createProject, updateProject, deleteProject, fetchEnvironments, fetchEnvironment, createEnvironment, deleteEnvironment
    - No updateEnvironment function (grep returns 0)
    - tsc --noEmit on client.ts reports 0 new errors
    - All functions use createCoolifyClient (no raw ofetch)
  </acceptance_criteria>
  <done>client.ts extended with 7 project/environment CRUD functions; no environment update; no app/service/db CRUD</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend resource.list with type=project|environment per D-04</name>
  <files>src/mcp/tools/resource.ts</files>
  <read_first>
    - src/mcp/tools/resource.ts (resourceActionSchema lines 10-25, list handler lines 182-235)
    - src/utils/project-lookup.ts (buildProjectEnvironmentIndex)
    - .planning/phases/09-project-environment-crud/09-CONTEXT.md (D-04, Success Criteria #4)
  </read_first>
  <behavior>
    - resourceActionSchema list type enum includes 'project' and 'environment' per D-04
    - list type=project: fetchProjects → projectProjectSummary { uuid, name, description, environment_count }
    - list type=environment: fetchProjects → flatten project.environments → projectEnvironmentSummary { uuid, name, project_uuid, project_name }
    - After environment.create, resource.list type=environment shows the new env (Success Criteria #4)
    - Pagination + format/max_chars via buildReadResponse unchanged
  </behavior>
  <action>
In src/mcp/tools/resource.ts extend the list action schema type enum to ['application','service','database','server','project','environment']. Add two handler branches in the 'list' case before the default fetchResources path. For type==='project': call fetchProjects, map via new local projectProjectSummary(raw) returning { uuid: String(raw.uuid), name: String(raw.name), description: raw.description ?? null, environment_count: Array.isArray(raw.environments) ? raw.environments.length : 0 }. For type==='environment': call fetchProjects, flatten raw.environments (Array.isArray check), map via projectEnvironmentSummary(rawEnv, projectUuid, projectName) returning { uuid: String(rawEnv.uuid), name: String(rawEnv.name), project_uuid: projectUuid, project_name: projectName }. Paginate via paginateArray, return via buildReadResponse with page/per_page/total. Do NOT change the existing server/application/service/database branches. Do NOT add find-branch changes.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && grep -cE \"type: z.enum\(\['application', 'service', 'database', 'server', 'project', 'environment'\]\)\" src/mcp/tools/resource.ts | grep -q 1 && npx tsc --noEmit src/mcp/tools/resource.ts 2>&1 | grep -c 'error TS' | grep -qE '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - type enum includes 'project' and 'environment'
    - list type=project returns project summaries with environment_count
    - list type=environment returns environment summaries with project_uuid + project_name
    - tsc --noEmit on resource.ts reports 0 new errors
    - Existing application/service/database/server branches unchanged
  </acceptance_criteria>
  <done>resource.list accepts and serves type=project and type=environment per D-04</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add resolveProjectUuid + resolveEnvironmentUuid to project-lookup.ts</name>
  <files>src/utils/project-lookup.ts</files>
  <read_first>
    - src/utils/project-lookup.ts (buildProjectEnvironmentIndex pattern)
    - .planning/phases/09-project-environment-crud/09-PATTERNS.md (resolveProjectUuid pattern from emergency.ts lines 386-440)
    - .planning/phases/09-project-environment-crud/09-CONTEXT.md (D-12, D-13, D-14)
  </read_first>
  <behavior>
    - resolveProjectUuid(project_uuid?, project_name?, env): if project_uuid return it; else fetchProjects filter by case-insensitive name substring; 0 matches → COOLIFY_404; >1 → COOLIFY_AMBIGUOUS_MATCH with ranked recovery hints; exactly 1 → return uuid per D-14
    - resolveEnvironmentUuid(env_uuid?, env_name?, project_uuid, env): if env_uuid return it; else fetchEnvironments(project_uuid), filter by case-insensitive name substring; 0 → COOLIFY_404; >1 → COOLIFY_AMBIGUOUS_MATCH; 1 → return name_or_uuid (Coolify accepts name in path) per D-13
    - Both throw CoolifyApiError (not generic Error) so wrapMcpError produces structured envelopes
  </behavior>
  <action>
Append to src/utils/project-lookup.ts. Import fetchProjects, fetchEnvironments from ../api/client.js and CoolifyApiError, RECOVERY_HINTS from ./errors.js. Implement resolveProjectUuid per PATTERNS.md emergency.ts pattern (lines 386-440) — case-insensitive includes() match, COOLIFY_404 on empty, COOLIFY_AMBIGUOUS_MATCH with recovery hints listing matched projects on >1. Implement resolveEnvironmentUuid(envUuid, envName, projectUuid, env): if envUuid provided return it; else fetchEnvironments(env.COOLIFY_URL, env.COOLIFY_TOKEN, projectUuid, env.COOLIFY_VERIFY_SSL), filter environments by name.toLowerCase().includes(envName.toLowerCase()), same 0/>1 error semantics. Return the matched environment's uuid (handler will pass uuid to fetchEnvironment/deleteEnvironment — both work since Coolify accepts name_or_uuid but passing uuid is safest). Export both functions. Do NOT modify buildProjectEnvironmentIndex.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && grep -cE 'export async function (resolveProjectUuid|resolveEnvironmentUuid)' src/utils/project-lookup.ts | grep -q 2 && npx tsc --noEmit src/utils/project-lookup.ts 2>&1 | grep -c 'error TS' | grep -qE '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - resolveProjectUuid and resolveEnvironmentUuid exported
    - resolveProjectUuid: project_uuid XOR project_name enforced (both provided → uuid wins per existing pattern)
    - COOLIFY_AMBIGUOUS_MATCH thrown on >1 name match (no mutation proceeds)
    - COOLIFY_404 thrown on 0 matches
    - tsc --noEmit on project-lookup.ts reports 0 new errors
  </acceptance_criteria>
  <done>project-lookup.ts hosts both resolvers; 09-02 and 09-03 import them for D-12/D-13/D-14 name resolution</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→MCP tool | untrusted Zod-validated args cross here |
| MCP→Coolify API | bearer-token authenticated HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-01-T | Tampering | client.ts CRUD payloads | medium | mitigate | All payloads constructed from Zod-parsed fields; no string interpolation into URL paths beyond validated uuid/name_or_uuid (Coolify routes validate server-side) |
| T-9-01-I | Information disclosure | resource.list projections | low | mitigate | projectProjectSummary + projectEnvironmentSummary emit only uuid/name/description/project_uuid — no secrets exist on these objects |
| T-9-01-SC | Tampering | npm installs (none new) | low | accept | No new packages; reuses approved zod/ofetch from Phase 8 Package Legitimacy Audit |
</threat_model>

<verification>
- 7 new client functions exported with correct HTTP methods per OpenAPI
- resource.list serves type=project and type=environment
- Resolvers throw CoolifyApiError (structured) not generic Error
- npm test green (existing suite unaffected; 09-00 RED tests still RED — they mock these functions)
</verification>

<success_criteria>
- 09-02 and 09-03 can import all needed client + resolver functions without adding more shared code
- D-04 satisfied: resource.list shows projects and environments
- No environment update function (prohibition enforced)
</success_criteria>

<output>
Create `.planning/phases/09-project-environment-crud/09-01-SUMMARY.md` when done
</output>
