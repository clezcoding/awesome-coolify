---
phase: 09-project-environment-crud
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified:
  - src/mcp/server.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05]
must_haves:
  truths:
    - "registerCoolifyTools registers 'project' and 'environment' MCP tools per D-01"
    - "Both tools use toolOutputSchema envelope + openWorldHint only (no readOnlyHint — they mutate) per Phase 8 parity"
    - "Error results propagate via isProjectErrorResult / isEnvironmentErrorResult with structuredContent { ok:false, error }"
    - "Tool descriptions document confirm gates, 409 on non-empty, initial_environment requirement, and name-resolution ambiguity per D-01..D-15"
  artifacts:
    - src/mcp/server.ts (extended)
  key_links:
    - "server.registerTool('project', { inputSchema: projectActionSchema, outputSchema: toolOutputSchema }, handler) → handleProjectAction"
    - "server.registerTool('environment', { inputSchema: environmentActionSchema, outputSchema: toolOutputSchema }, handler) → handleEnvironmentAction"
  prohibitions:
    - statement: "No environment update action registered — API has no PATCH (PROHIB_ABSENT)"
      status: enforced
      verification: "environment tool description mentions only list/get/create/delete/delete_preview"
---

<objective>
Wave 3 registration: wire project + environment tools into the MCP server. Mirrors Phase 8 08-04. After this plan, the two new tools are agent-callable.

Purpose: Make Phase 9 capabilities reachable from MCP clients; completes the phase deliverable.
Output: Extended src/mcp/server.ts with two new registerTool calls.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-project-environment-crud/09-CONTEXT.md
@src/mcp/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register project + environment tools in server.ts</name>
  <files>src/mcp/server.ts</files>
  <read_first>
    - src/mcp/server.ts (registerTool pattern lines 388-448 for private_key + server; toolOutputSchema lines 81-108; import block lines 1-65)
    - src/mcp/tools/project.ts (09-02 exports: handleProjectAction, projectActionSchema, isProjectErrorResult)
    - src/mcp/tools/environment.ts (09-03 exports: handleEnvironmentAction, environmentActionSchema, isEnvironmentErrorResult)
    - .planning/phases/09-project-environment-crud/09-CONTEXT.md (D-01..D-15 for description wording)
  </read_first>
  <action>
In src/mcp/server.ts add two imports after the server.ts import (line 42-46): `import { handleProjectAction, isProjectErrorResult, projectActionSchema } from './tools/project.js';` and `import { handleEnvironmentAction, isEnvironmentErrorResult, environmentActionSchema } from './tools/environment.js';`. Append two registerTool calls inside registerCoolifyTools after the 'server' tool registration (after line 448). For 'project': description 'Project CRUD (list, get, create, update, delete, delete_preview) for Coolify organizational containers. create requires initial_environment (required — no default; agent must ask user for production vs custom name per D-09/D-10) — missing/empty → COOLIFY_422 with recovery hint; response returns { project, environment, environments? } (D-11); the auto-spawned production env is never auto-deleted. get/update/delete/delete_preview accept uuid XOR name; name multi-match → COOLIFY_AMBIGUOUS_MATCH (D-14). delete requires confirm:true (D-05); deleting a project with remaining environments returns COOLIFY_409 with environment_uuids — no force/cascade (D-07). delete_preview lists blockers without deleting (D-08).', inputSchema: projectActionSchema, outputSchema: toolOutputSchema, annotations: { openWorldHint: true }. Handler: `async (args) => { const result = await handleProjectAction(args, env); if (isProjectErrorResult(result)) { return { ...result, structuredContent: { ok: false, error: result.structuredContent.error } }; } return { content: [{ type: 'text', text: result._formattedText }], structuredContent: { ok: true, data: result.data, _meta: result._meta } }; }`. For 'environment': description 'Environment CRUD (list, get, create, delete, delete_preview) scoped to a parent project — no update action (Coolify API has no PATCH). list accepts project_uuid XOR project_name; name multi-match → COOLIFY_AMBIGUOUS_MATCH (D-12). get/delete accept uuid XOR name within the parent project scope (D-13). create with a duplicate name returns COOLIFY_409 with a recovery hint (D-15). delete requires confirm:true (D-05); deleting a non-empty environment (has apps/services/databases) returns COOLIFY_409 with child_resource_uuids — no force/cascade (D-06). delete_preview lists child resources without deleting (D-08).', inputSchema: environmentActionSchema, outputSchema: toolOutputSchema, annotations: { openWorldHint: true }. Handler mirrors project handler with isEnvironmentErrorResult. Do NOT add readOnlyHint (both mutate). Do NOT register any environment update action.
  </action>
  <verify>
    <automated>cd /Users/puzzless/Desktop/awesome-coolify && grep -cE "registerTool\(\s*'(project|environment)'" src/mcp/server.ts | grep -q 2 && npx tsc --noEmit src/mcp/server.ts 2>&1 | grep -c 'error TS' | grep -qE '^0$' && npm test 2>&1 | tail -3 | grep -qE 'passed|Tests.*passed' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 'project' and 'environment' tools registered in registerCoolifyTools
    - Both use toolOutputSchema + openWorldHint only (no readOnlyHint)
    - Both handlers route errors via isProjectErrorResult / isEnvironmentErrorResult with structuredContent { ok:false, error }
    - tsc --noEmit on server.ts reports 0 new errors
    - npm test full suite green
    - No environment update action in either description or schema
  </acceptance_criteria>
  <done>project + environment registered; Phase 9 tools agent-callable; PROJ-01..05 reachable end-to-end</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client→server.registerTool | tool schema + description published to client; client sends args back across this boundary |
| server.ts→handler | args forwarded to handleProjectAction / handleEnvironmentAction (Zod-validated inside) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-04-S | Spoofing | tool registration surface | low | accept | Tool names are static literals; no client-controlled input reaches registerTool |
| T-9-04-I | Information disclosure | tool descriptions | low | accept | Descriptions document safety semantics (confirm, 409, no cascade) — no secrets leaked |
| T-9-04-SC | Tampering | npm installs (none new) | low | accept | No new packages |
</threat_model>

<verification>
- Both tools registered with correct schemas + handlers
- npm test green (full suite including 09-00 RED-now-GREEN tests)
- tsc clean on server.ts
</verification>

<success_criteria>
- Phase 9 complete: project + environment tools agent-callable
- All PROJ-01..05 reachable end-to-end
- Phase ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/09-project-environment-crud/09-04-SUMMARY.md` when done
</output>
