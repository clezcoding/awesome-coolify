---
phase: 30-deploy-guard
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/utils/deploy-preflight.ts
  - src/utils/deploy-preflight.test.ts
  - src/mcp/tools/deployment.test.ts
  - src/mcp/tools/system.test.ts
  - src/api/client.test.ts
autonomous: true
requirements:
  - GUARD-01
  - GUARD-02
  - GUARD-03
user_setup: []

must_haves:
  truths:
    - "Wave 0 locks deployment.preflight RED scaffolds for GUARD-01 and GUARD-02 before implementation, including four named factors, risk_score, risk_level, and soft partial behavior."
    - "Wave 0 locks deployment.rollback RED scaffolds for GUARD-03 and SAF-01 before implementation, including confirm gate, last finished selection, and git pin-before-deploy order."
    - "Wave 0 locks capability expectations for deployment_preflight and deployment_rollback."
    - "All new RED cases use Vitest it.fails so pre-commit remains green."
  artifacts:
    - path: src/mcp/tools/deployment.test.ts
      provides: "it.fails coverage for preflight factors, risk score, read-only contract, rollback confirm, and git pin order"
      contains: "deployment.preflight"
    - path: src/utils/deploy-preflight.test.ts
      provides: "it.fails coverage for findLastSuccessfulDeployment and computeDeployRiskScore"
      contains: "findLastSuccessfulDeployment"
    - path: src/mcp/tools/system.test.ts
      provides: "capability RED expectations for deployment_preflight and deployment_rollback"
      contains: "deployment_preflight"
    - path: src/utils/deploy-preflight.ts
      provides: "minimal shell exports for Wave 0 import wiring only"
      contains: "findLastSuccessfulDeployment"
  key_links:
    - from: src/mcp/tools/deployment.test.ts
      to: src/mcp/tools/deployment.ts
      via: "handleDeploymentAction preflight and rollback cases"
    - from: src/utils/deploy-preflight.test.ts
      to: src/utils/deploy-preflight.ts
      via: "shared rollback and scoring helpers"
    - from: src/mcp/tools/system.test.ts
      to: src/mcp/capabilities.ts
      via: "system.version capabilities assertions"
---

<objective>
Wave 0 Nyquist RED scaffolds for Phase 30 Deploy Guard.

Purpose: Lock GUARD-01, GUARD-02, and GUARD-03 behaviors before production changes, following Phase 28/29 it.fails pattern.

Output: Deployment preflight, rollback, util helper, and capability RED targets; deploy-preflight.ts shell only (no handler wiring).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-deploy-guard/30-RESEARCH.md
@.planning/phases/30-deploy-guard/30-PATTERNS.md
@.planning/phases/30-deploy-guard/30-VALIDATION.md
@.cursor/skills/spike-findings-awesome-coolify/SKILL.md
@src/mcp/tools/deployment.ts
@src/mcp/tools/deployment.test.ts
@src/mcp/tools/intelligence.ts
@src/mcp/tools/system.test.ts
@src/api/client.ts
</context>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Agent assesses deploy risk before mutation | 00, 01 | COVERED | ROADMAP Phase 30 success 1–2 |
| GOAL | — | Agent recovers from failed deployments via rollback | 00, 02 | COVERED | ROADMAP Phase 30 success 3 |
| REQ | GUARD-01 | Deploy preflight: instance health, env completeness, recent failures, DNS readiness | 00 RED, 01 GREEN | COVERED | deployment.preflight composite |
| REQ | GUARD-02 | Preflight returns deploy risk score with named factor breakdown | 00 RED, 01 GREEN | COVERED | risk_score + factors{} |
| REQ | GUARD-03 | Roll back application to last successful deployment | 00 RED, 02 GREEN | COVERED | deployment.rollback composite |
| RESEARCH | — | Extend deployment tool (not new guard tool) | 01, 02 | COVERED | A1 research default |
| RESEARCH | — | deploy-preflight.ts factor collectors + scoring | 00 shell, 01 GREEN | COVERED | |
| RESEARCH | — | No live DNS probe; config-based dns_readiness | 01 | COVERED | |
| RESEARCH | — | Git rollback = updateApplication git_commit_sha + triggerDeploy | 00 RED, 02 GREEN | COVERED | |
| RESEARCH | — | Docker rollback via triggerDeploy docker_tag param | 00 RED, 02 GREEN | COVERED | optional client extension |
| RESEARCH | — | Capabilities/docs/coverage rows | 00 RED, 03 GREEN | COVERED | |
| RESEARCH | — | SAF-01 confirm gate on rollback | 00 RED, 02 GREEN | COVERED | |
| CONTEXT | — | No 30-CONTEXT.md | — | N/A | Research defaults apply |

## Artifacts this phase produces

- `src/utils/deploy-preflight.ts` — shell exports (`findLastSuccessfulDeployment`, `computeDeployRiskScore` stubs throwing or returning placeholders).
- `src/utils/deploy-preflight.test.ts` — RED unit scaffolds for shared helpers.
- `src/mcp/tools/deployment.test.ts` — RED tests for preflight, rollback, confirm gate, read-only preflight.
- `src/mcp/tools/system.test.ts` — RED capability assertions for `deployment_preflight` and `deployment_rollback`.
- `src/api/client.test.ts` — optional RED scaffold for `triggerDeploy` `docker_tag` query param when false/omitted omits force.

<tasks>

<task type="tdd">
  <name>Task 1: deploy-preflight shell and helper RED scaffolds</name>
  <files>src/utils/deploy-preflight.ts, src/utils/deploy-preflight.test.ts</files>
  <read_first>
    - .planning/phases/30-deploy-guard/30-RESEARCH.md (Risk Scoring, Last successful selection, Don't Hand-Roll)
    - src/mcp/tools/intelligence.ts (sortDeploymentsNewestFirst, computeScoreBreakdown, collectDeploymentsFactor)
    - src/utils/diagnose-hints.ts (FollowUpHint)
  </read_first>
  <behavior>
    - `findLastSuccessfulDeployment` returns newest deployment with status finished and skips failed, in_progress, queued, cancelled-by-user (GUARD-03).
    - `findLastSuccessfulDeployment` returns null when no finished deployment exists (GUARD-03).
    - `computeDeployRiskScore` applies deterministic deductions: critical −30, high −15, info −5 each; score clamped 0–100 (GUARD-02).
    - `computeDeployRiskScore` derives risk_level bands: critical when any critical finding or score below 40; high below 70; medium below 85; else low (GUARD-02).
    - `sortDeploymentsNewestFirst` is exported from deploy-preflight util (or re-exported) for shared rollback/preflight use per research.
  </behavior>
  <action>
    Create `src/utils/deploy-preflight.ts` with minimal exported stubs: `sortDeploymentsNewestFirst` (copy logic from intelligence.ts or thin re-export path documented for 30-01 refactor), `findLastSuccessfulDeployment`, `computeDeployRiskScore`, and placeholder `FactorResult` / `DeployPreflightFinding` types matching research envelope. Stubs may throw `new Error('not implemented')` or return fixed placeholders — Wave 0 goal is importable symbols for tests only. Add `src/utils/deploy-preflight.test.ts` with Vitest `it.fails` cases for finished-vs-failed selection, empty history null, score deductions, and risk_level band boundaries. Do not wire MCP handlers in Wave 0.
  </action>
  <verify>
    <automated>npx vitest run src/utils/deploy-preflight.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/deploy-preflight.ts` exports `findLastSuccessfulDeployment` and `computeDeployRiskScore`.
    - `src/utils/deploy-preflight.test.ts` contains `it.fails` cases for selection, scoring, and risk_level bands.
    - `npx vitest run src/utils/deploy-preflight.test.ts -x` exits 0 because every new RED scaffold uses `it.fails`.
  </acceptance_criteria>
  <done>Shared deploy-preflight helper contracts have RED scaffolds ready for 30-01 and 30-02.</done>
</task>

<task type="tdd">
  <name>Task 2: RED scaffolds for deployment.preflight</name>
  <files>src/mcp/tools/deployment.test.ts</files>
  <read_first>
    - .planning/phases/30-deploy-guard/30-RESEARCH.md (Preflight Factors, Response envelope, Anti-Patterns)
    - .planning/phases/30-deploy-guard/30-PATTERNS.md (intelligence scorecard and manifest audit analogs)
    - src/mcp/tools/deployment.test.ts (existing list/get mock patterns)
    - src/mcp/tools/intelligence.test.ts (scorecard factor assertions)
  </read_first>
  <behavior>
    - `deployment.preflight` with resolved application uuid returns `factors` with keys instance_health, env_completeness, recent_deployment_failures, dns_readiness (GUARD-01).
    - Response includes `risk_score` number, `risk_level` band, `score_breakdown`, `findings[]` with FollowUpHint-shaped hints, and `advisory: true` (GUARD-02).
    - `blocking` is true when risk_level is critical or latest deployment is in_progress (research default).
    - Preflight never calls triggerDeploy, updateApplication, or cancelDeployment (read-only contract).
    - Preflight masks env values and never exposes raw secrets via reveal on preflight path (T-30-01).
    - One mocked factor fetch rejection still returns sibling factor results with `partial` metadata (D-17 spirit).
    - Preflight requires scoped application uuid via uuid, name, or fqdn resolution — no fleet-wide fetch without app id.
  </behavior>
  <action>
    Add Vitest `it.fails` scaffolds under `deployment.preflight` describe in `src/mcp/tools/deployment.test.ts`. Reuse existing API client mocks from list/get tests. Mock fetchApplication, fetchApplicationEnvs, fetchAppDeployments, fetchServers, fetchResources, and optional fetchServerDomains. Assert four factor keys, risk_score type, findings hint shape, advisory flag, and read-only spy on mutation clients. Add soft-partial case where one fetch rejects. Add env masking case asserting values are redacted. Use dynamic import if preflight action not yet in schema. Keep `src/mcp/tools/deployment.ts` unchanged in this task.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts -t preflight -x</automated>
  </verify>
  <acceptance_criteria>
    - `deployment.test.ts` contains `it.fails` cases for four factors, risk score, risk_level, partial failure, read-only, and env masking.
    - `npx vitest run src/mcp/tools/deployment.test.ts -t preflight -x` exits 0 via `it.fails`.
    - No production handler wiring in this task.
  </acceptance_criteria>
  <done>GUARD-01 and GUARD-02 preflight behaviors have RED scaffolds ready for 30-01.</done>
</task>

<task type="tdd">
  <name>Task 3: RED scaffolds for deployment.rollback and capabilities</name>
  <files>src/mcp/tools/deployment.test.ts, src/mcp/tools/system.test.ts, src/api/client.test.ts</files>
  <read_first>
    - .planning/phases/30-deploy-guard/30-RESEARCH.md (Rollback orchestration, Confirm Gates, Pitfalls 1–4)
    - src/mcp/tools/emergency.test.ts (COOLIFY_CONFIRM_REQUIRED preview pattern)
    - src/mcp/tools/intelligence.test.ts (cleanup confirm gate)
    - src/mcp/tools/system.test.ts (capability key assertions)
    - src/api/client.ts (triggerDeploy signature)
  </read_first>
  <behavior>
    - `deployment.rollback` without confirm true returns `COOLIFY_CONFIRM_REQUIRED` with preview payload including rollback_target summary (GUARD-03, SAF-01).
    - Rollback with confirm true on git app calls updateApplication with target commit before triggerDeploy (GUARD-03, Pitfall 1).
    - Rollback selects newest deployment with status finished, not failed or in_progress (GUARD-03).
    - Rollback with no finished deployment returns structured unavailable error (COOLIFY_ROLLBACK_UNAVAILABLE or equivalent) and performs no mutation.
    - Rollback on dockerimage build_pack passes docker_tag to triggerDeploy when client supports it (research A3).
    - triggerDeploy omits force query param when false to avoid PHP truthy coercion (Pitfall 4).
    - system.version capabilities expect `deployment_preflight` and `deployment_rollback` keys after 30-03.
  </behavior>
  <action>
    Add Vitest `it.fails` scaffolds under `deployment.rollback` describe in `deployment.test.ts`. Mock fetchAppDeployments with mixed statuses, fetchApplication with build_pack git and dockerimage cases, updateApplication, triggerDeploy, and optional pollDeploymentUntilTerminal. Assert confirm gate error code and preview data. Assert call order updateApplication before triggerDeploy for git path. Assert no writes on missing target or failed confirm. Add optional `it.fails` in `src/api/client.test.ts` for triggerDeploy docker_tag param and omitted force when false. Extend `system.test.ts` with `it.fails` capability expectation for `deployment_preflight` and `deployment_rollback`. Keep deployment handler unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts src/mcp/tools/system.test.ts src/api/client.test.ts -t "rollback|deployment_preflight|docker_tag" -x</automated>
  </verify>
  <acceptance_criteria>
    - `deployment.test.ts` contains `it.fails` rollback cases for confirm gate, last finished selection, git pin order, no-target error, and docker_tag path.
    - `system.test.ts` contains `it.fails` expectation for capability keys `deployment_preflight` and `deployment_rollback`.
    - Targeted vitest command exits 0 via `it.fails`.
    - No deployment handler implementation in this task.
  </acceptance_criteria>
  <done>GUARD-03 rollback and capability discoverability have RED scaffolds ready for 30-02 and 30-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → deployment.preflight/rollback | Untrusted action args choose app identity, confirm, wait, instance |
| Coolify API → MCP factor collectors | Live health/env/deploy/DNS config drives risk findings |
| MCP response → agent | Preflight env data and rollback preview must not leak secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Information Disclosure | preflight env_completeness | high | mitigate | Wave 0 RED locks masked env values; 30-01 never enables reveal on preflight |
| T-30-02 | Tampering | rollback without confirm | high | mitigate | Wave 0 RED locks COOLIFY_CONFIRM_REQUIRED and preview payload before mutation |
| T-30-03 | Spoofing | ambiguous app identity | high | mitigate | Wave 0 RED requires resolveAppMutationUuid path; 30-02 reuses COOLIFY_AMBIGUOUS_MATCH |
| T-30-04 | Tampering | git commit pin injection | medium | mitigate | 30-02 validates commit SHA format before updateApplication PATCH |
| T-30-SC | Tampering | npm installs | low | accept | Phase installs no packages |
</threat_model>

<verification>
Wave 0 passes when RED scaffolds exist, targeted Vitest commands exit 0 through `it.fails`, and deployment handler remains unwired.
</verification>

<success_criteria>
- All Phase 30 requirements have RED test targets.
- GUARD-01/02/03 traceable from tests to later implementation plans.
- Security-critical masking, confirm gate, and read-only preflight behaviors sampled before implementation.
</success_criteria>

<output>
Create `.planning/phases/30-deploy-guard/30-00-SUMMARY.md` when done.
</output>
