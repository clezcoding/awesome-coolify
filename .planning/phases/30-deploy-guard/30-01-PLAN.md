---
phase: 30-deploy-guard
plan: 01
type: execute
wave: 1
depends_on:
  - "30-00"
files_modified:
  - src/utils/deploy-preflight.ts
  - src/utils/deploy-preflight.test.ts
  - src/mcp/tools/deployment.ts
  - src/mcp/tools/deployment.test.ts
  - src/mcp/tools/intelligence.ts
autonomous: true
requirements:
  - GUARD-01
  - GUARD-02
user_setup: []

must_haves:
  truths:
    - "Agent calls deployment.preflight with uuid|name|fqdn and receives risk_score 0–100 with risk_level band (GUARD-02)."
    - "Preflight response includes four named factors: instance_health, env_completeness, recent_deployment_failures, dns_readiness (GUARD-01)."
    - "Response includes advisory:true, score_breakdown, findings[] with FollowUpHint, and blocking when risk_level critical or latest deploy in_progress."
    - "Each factor failure is isolated via Promise.allSettled; partial payload survives single fetch failure."
    - "Preflight is read-only — never calls triggerDeploy, updateApplication, or cancelDeployment."
    - "Env values masked on preflight path — no reveal on preflight (T-30-01)."
    - "DNS readiness uses Coolify field signals only — no node:dns or HTTP probes."
    - "Wave 0 preflight it.fails cases flip to passing it()."
  artifacts:
    - path: src/utils/deploy-preflight.ts
      provides: "buildDeployPreflightReport, factor collectors, computeDeployRiskScore, sortDeploymentsNewestFirst"
      exports: ["buildDeployPreflightReport", "findLastSuccessfulDeployment", "computeDeployRiskScore"]
    - path: src/mcp/tools/deployment.ts
      provides: "preflight action on deployment tool"
      contains: "preflight"
    - path: src/mcp/tools/deployment.test.ts
      provides: "GREEN preflight tests from Wave 0"
      contains: "deployment.preflight"
  key_links:
    - from: handleDeploymentPreflight
      to: resolveAppMutationUuid from application.ts
      via: "shared single-app identifier resolution"
    - from: buildDeployPreflightReport
      to: intelligence.ts deployment factor logic
      via: "extracted sortDeploymentsNewestFirst"
  prohibitions:
    - statement: "Do not add external DNS/HTTP resolution probes (Phase 10)"
      status: enforced
      verification: "rg -n \"node:dns|dns\\.promises|fetch\\(.*fqdn\" src/utils/deploy-preflight.ts src/mcp/tools/deployment.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
    - statement: "Do not create deploy_guard top-level MCP tool (RESEARCH A1)"
      status: enforced
      verification: "rg -n \"name: 'deploy_guard'\" src/mcp/server.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
    - statement: "Do not call fleet intelligence.scorecard inside preflight"
      status: enforced
      verification: "rg -n \"handleIntelligenceScorecard\" src/utils/deploy-preflight.ts src/mcp/tools/deployment.ts | grep -v '^#' | wc -l | awk '{exit ($1==0)?0:1}'"
---

<objective>
Tracer vertical slice: production-quality `deployment.preflight` end-to-end on the existing deployment tool.

Purpose: Prove deploy-preflight architecture before rollback: schema/catalog → handler → util → four factors → risk score → read-only response.

Output: `deploy-preflight.ts`, `deployment.preflight`, Wave 0 preflight tests GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-deploy-guard/30-RESEARCH.md
@.planning/phases/30-deploy-guard/30-PATTERNS.md
@.planning/phases/30-deploy-guard/30-VALIDATION.md
@.planning/phases/30-deploy-guard/30-00-SUMMARY.md
@src/mcp/tools/deployment.ts
@src/mcp/tools/application.ts
@src/mcp/tools/intelligence.ts
@src/utils/env-shared.ts
</context>

## Artifacts this phase produces

- `src/utils/deploy-preflight.ts` — full factor collectors and scoring (replaces Wave 0 stubs).
- `src/mcp/tools/deployment.ts` — `preflight` action.
- `src/mcp/tools/intelligence.ts` — re-export or thin import path if sortDeploymentsNewestFirst moved.
- GREEN deployment.preflight tests.

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end deployment.preflight — four factors with risk score</name>
  <reversibility rating="costly">deployment.preflight action and four factor keys become agent catalog contract.</reversibility>
  <files>src/utils/deploy-preflight.ts, src/utils/deploy-preflight.test.ts, src/mcp/tools/deployment.ts, src/mcp/tools/deployment.test.ts, src/mcp/tools/intelligence.ts</files>
  <read_first>
    - .planning/phases/30-deploy-guard/30-RESEARCH.md (Preflight Factors, Response envelope)
    - src/mcp/tools/deployment.ts (deploymentActionsCatalog, list/get handlers)
    - src/mcp/tools/application.ts (resolveAppMutationUuid)
    - src/mcp/tools/intelligence.ts (collectDeploymentsFactor, computeScoreBreakdown, sortDeploymentsNewestFirst)
  </read_first>
  <behavior>
    - Agent calls `{ action: 'preflight', uuid: '<app-uuid>' }` on deployment tool.
    - Response includes application_uuid, risk_score, risk_level, score_breakdown, factors with four keys, findings[], advisory:true, blocking when critical or in_progress latest deploy.
    - Soft partial when one factor fetch rejects.
    - No mutation client calls.
  </behavior>
  <action>
    Implement full deploy-preflight.ts: move sortDeploymentsNewestFirst from intelligence.ts to deploy-preflight.ts and update intelligence imports; implement four factor collectors per RESEARCH; buildDeployPreflightReport with Promise.allSettled; computeDeployRiskScore with critical/high/info deductions and risk_level bands. Add preflight to deploymentActionsCatalog, deploymentActionSchema, and handleDeploymentAction switch. Resolve app via resolveAppMutationUuid imported from application.ts. Return buildReadResponse envelope. Flip Wave 0 preflight it.fails to it(). Do not implement rollback yet.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts -t preflight -x && npx vitest run src/utils/deploy-preflight.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - deploymentActionsCatalog includes preflight
    - factors keys: instance_health, env_completeness, recent_deployment_failures, dns_readiness
    - triggerDeploy mock not called in preflight tests
  </acceptance_criteria>
  <done>deployment.preflight end-to-end; GUARD-01 and GUARD-02 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Preflight integration with application.deploy hint</name>
  <files>src/mcp/tools/deployment.ts</files>
  <read_first>
    - src/mcp/tools/deployment.ts (preflight handler)
    - src/utils/diagnose-hints.ts (FollowUpHint)
  </read_first>
  <action>
    Add FollowUpHint on preflight response pointing to application.deploy with uuid when risk acceptable, and to diagnose.logs or deployment.get when blocking:true. Ensure ambiguous app resolution surfaces COOLIFY_AMBIGUOUS_MATCH without partial factors.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts -t preflight -x</automated>
  </verify>
  <acceptance_criteria>
    - findings[].hint objects include tool and action fields
    - blocking:true when latest deployment status in_progress in test fixture
  </acceptance_criteria>
  <done>Preflight response agent-actionable with deploy guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → deployment.preflight | Read-only; untrusted app identifiers |
| Preflight response | Env key names only; values masked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01-01 | Information Disclosure | env_completeness | high | mitigate | maskEnvRecord; no reveal on preflight |
| T-30-01-02 | Tampering | DNS SSRF | high | mitigate | Coolify fields only; no external probes |
| T-30-01-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/mcp/tools/deployment.test.ts -t preflight -x && npx vitest run src/utils/deploy-preflight.test.ts -x
</verification>

<success_criteria>
GUARD-01 and GUARD-02 via deployment.preflight with four factors, risk score, soft partials.
</success_criteria>

<output>
Create `.planning/phases/30-deploy-guard/30-01-SUMMARY.md` when done
</output>
