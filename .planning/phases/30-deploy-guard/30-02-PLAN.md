---
phase: 30-deploy-guard
plan: 02
type: execute
wave: 2
depends_on:
  - "30-01"
files_modified:
  - src/utils/deploy-preflight.ts
  - src/utils/errors.ts
  - src/mcp/tools/deployment.ts
  - src/mcp/tools/deployment.test.ts
  - src/api/client.ts
  - src/api/client.test.ts
autonomous: true
requirements:
  - GUARD-03
user_setup: []

must_haves:
  truths:
    - "Agent calls deployment.rollback with confirm:true and redeploys last finished deployment (GUARD-03, SAF-01)."
    - "Without confirm:true, returns COOLIFY_CONFIRM_REQUIRED with rollback_target preview."
    - "Git apps: updateApplication git_commit_sha before triggerDeploy (Pitfall 1)."
    - "Dockerimage apps: triggerDeploy with docker_tag when supported."
    - "No finished deployment returns COOLIFY_ROLLBACK_UNAVAILABLE with no mutations."
    - "triggerDeploy omits force query when false (Pitfall 4)."
    - "Wave 0 rollback it.fails flip to passing it()."
  artifacts:
    - path: src/mcp/tools/deployment.ts
      provides: "rollback action with confirm gate"
      contains: "rollback"
    - path: src/utils/deploy-preflight.ts
      provides: "executeDeploymentRollback orchestration"
      contains: "executeDeploymentRollback"
    - path: src/utils/errors.ts
      provides: "COOLIFY_ROLLBACK_UNAVAILABLE code"
      contains: "COOLIFY_ROLLBACK_UNAVAILABLE"
  key_links:
    - from: executeDeploymentRollback
      to: updateApplication
      via: "git commit pin before deploy"
    - from: executeDeploymentRollback
      to: triggerDeploy
      via: "POST /deploy after pin or docker_tag"
---

<objective>
Implement `deployment.rollback` — confirm-gated composite recovery (GUARD-03).

Purpose: Orchestrate last-finished selection, optional preview without confirm, PATCH pin, and deploy trigger on deployment tool.

Output: rollback handler, error codes, client docker_tag support, GREEN rollback tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-deploy-guard/30-RESEARCH.md
@.planning/phases/30-deploy-guard/30-01-SUMMARY.md
@src/mcp/tools/deployment.ts
@src/mcp/tools/emergency.ts
@src/api/client.ts
@src/utils/deploy-poll.ts
</context>

## Artifacts this phase produces

- `deployment.rollback` with confirm gate and preview.
- `executeDeploymentRollback` in deploy-preflight.ts.
- Optional `triggerDeploy` docker_tag query param in client.ts.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deployment.rollback with confirm gate and git pin path</name>
  <files>src/utils/deploy-preflight.ts, src/utils/errors.ts, src/mcp/tools/deployment.ts, src/mcp/tools/deployment.test.ts, src/api/client.ts, src/api/client.test.ts</files>
  <read_first>
    - .planning/phases/30-deploy-guard/30-RESEARCH.md (Rollback orchestration, Confirm Gates, Pitfalls 1–4)
    - src/mcp/tools/emergency.ts (validateConfirmGate pattern)
    - src/mcp/tools/application.ts (extractDeploymentUuid, handleApplicationDeploy wait)
    - src/api/client.ts (triggerDeploy, updateApplication)
  </read_first>
  <behavior>
    - rollback without confirm returns COOLIFY_CONFIRM_REQUIRED with rollback_target preview.
    - confirm:true on git app: updateApplication then triggerDeploy in that order.
    - No finished deployment: COOLIFY_ROLLBACK_UNAVAILABLE, no API writes.
    - dockerimage: triggerDeploy receives docker_tag when client extended.
  </behavior>
  <action>
    Add COOLIFY_ROLLBACK_UNAVAILABLE to errors.ts. Implement executeDeploymentRollback in deploy-preflight.ts with confirm preview branch and apply branch. Add rollback to deployment schema with confirm, force, wait, timeout, uuid|name|fqdn. Wire handleDeploymentRollback. Extend triggerDeploy to accept optional dockerTag and omit force query when false. Flip Wave 0 rollback it.fails to it(). Validate commit SHA format before PATCH (T-30-04).
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts -t rollback -x && npx vitest run src/api/client.test.ts -t "docker_tag|triggerDeploy" -x</automated>
  </verify>
  <acceptance_criteria>
    - COOLIFY_CONFIRM_REQUIRED when confirm not true
    - updateApplication called before triggerDeploy in git rollback test
    - No mutations when no finished deployment
  </acceptance_criteria>
  <done>GUARD-03 rollback implemented with SAF-01 confirm gate.</done>
</task>

<task type="auto">
  <name>Task 2: Rollback wait path and post-rollback hints</name>
  <files>src/mcp/tools/deployment.ts</files>
  <read_first>
    - src/utils/deploy-poll.ts (pollDeploymentUntilTerminal)
    - src/mcp/tools/deployment.ts (deployment.watch pattern)
  </read_first>
  <action>
  When wait:true on rollback, poll new deployment to terminal using same timeout semantics as application.deploy. Success response includes rolled_back_to summary, deployment_uuid, and hints to deployment.get or deployment.watch. On success suggest deployment.preflight before next application.deploy.
  </action>
  <verify>
    <automated>npx vitest run src/mcp/tools/deployment.test.ts -t rollback -x</automated>
  </verify>
  <acceptance_criteria>
    - wait:true test asserts poll helper invoked
    - rolled_back_to.commit present in success response
  </acceptance_criteria>
  <done>Rollback response complete for agent follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → deployment.rollback | Mutation; confirm gate required (SAF-01) |
| Rollback → live app | PATCH + deploy changes running version |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02-01 | Tampering | rollback without confirm | high | mitigate | COOLIFY_CONFIRM_REQUIRED + preview |
| T-30-02-02 | Tampering | deploy without pin | critical | mitigate | Test asserts updateApplication before triggerDeploy |
| T-30-02-03 | Tampering | commit injection | medium | mitigate | SHA format validation before PATCH |
| T-30-02-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
npx vitest run src/mcp/tools/deployment.test.ts -t rollback -x
</verification>

<success_criteria>
GUARD-03 satisfied via deployment.rollback with confirm gate and composite pin+deploy.
</success_criteria>

<output>
Create `.planning/phases/30-deploy-guard/30-02-SUMMARY.md` when done
</output>
