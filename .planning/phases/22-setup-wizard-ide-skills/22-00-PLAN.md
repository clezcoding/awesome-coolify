---
phase: 22-setup-wizard-ide-skills
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/utils/gh-preflight.test.ts
  - src/mcp/tools/setup.test.ts
  - src/utils/errors.test.ts
  - src/skills/skills-manifest.test.ts
autonomous: true
requirements:
  - SETUP-01
  - SETUP-03
  - SKILL-01
user_setup: []

must_haves:
  truths:
    - "src/utils/gh-preflight.test.ts exists with it.fails RED scaffolds for gh_missing, gh_unauthenticated, timeout/GH_FORCE_TTY env, and ok path (SETUP-01, D-04)"
    - "src/mcp/tools/setup.test.ts exists with it.fails RED scaffolds for preflight pause (COOLIFY_SETUP_PAUSED), preflight ok, and resume re-runs preflight (SETUP-03, D-05/D-06)"
    - "src/utils/errors.test.ts gains describe('setup pause error codes') with it.fails expecting COOLIFY_SETUP_PAUSED in CoolifyErrorCode union and RECOVERY_HINTS resume hint"
    - "src/skills/skills-manifest.test.ts exists with it.fails RED scaffolds for four skill dirs + frontmatter name match (SKILL-01)"
    - "All Wave 0 scaffolds use vitest it.fails so husky pre-commit stays green until Plans 22-01..22-03 flip to bare it"
    - "gh-preflight.test.ts and setup.test.ts use dynamic import of module under test where file absent (mirrors Phase 21 Wave 0)"
    - "{ statement: 'gh preflight subprocess never invokes gh auth login or shell-spawned gh', verification: backstop }"
    - "{ statement: 'setup.test.ts pause scaffold asserts no poll/sleep loop waiting for human gh auth', verification: backstop }"
  artifacts:
    - "src/utils/gh-preflight.test.ts (new RED)"
    - "src/mcp/tools/setup.test.ts (new RED)"
    - "src/utils/errors.test.ts (modified — setup pause it.fails)"
    - "src/skills/skills-manifest.test.ts (new RED)"
  key_links:
    - "gh-preflight.test.ts dynamic import → ./gh-preflight.js (Plan 22-01: checkGhAuth)"
    - "setup.test.ts dynamic import → ./setup.js (Plan 22-01: handleSetupAction preflight/resume)"
    - "skills-manifest.test.ts reads skills/coolify-*/SKILL.md (Plan 22-01/22-03)"
  prohibitions:
    - statement: "Do not create production setup.ts or gh-preflight.ts in Wave 0 — tests only"
      status: enforced
      verification: "git diff --name-only for src/utils/gh-preflight.ts src/mcp/tools/setup.ts is empty after this plan"
    - statement: "No gh auth login subprocess in any scaffold or future implementation (D-04/D-05)"
      status: enforced
      verification: "rg -n 'auth login' src/utils/gh-preflight.test.ts src/mcp/tools/setup.test.ts returns 0 matches in implementation files once shipped"
    - statement: "No auto-push git in setup flow (D-12)"
      status: enforced
      verification: "Wave 0 scaffolds do not assert git push; Plan 22-02 enforces no --push on gh repo create"
---

<objective>
Wave 0 RED test scaffolds for Phase 22 — gh preflight utility, setup MCP tool preflight/resume, COOLIFY_SETUP_PAUSED error code, and skills manifest layout.

Purpose: Lock SETUP-01/03 and SKILL-01 acceptance as failing `it.fails` tests before implementation so Plans 22-01..22-03 flip them GREEN without rewriting intent. Mirrors Phase 21 Wave 0 pattern.

Output: New `gh-preflight.test.ts`, `setup.test.ts`, `skills-manifest.test.ts`; extended `errors.test.ts` setup-pause describe.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md
@.planning/phases/22-setup-wizard-ide-skills/22-RESEARCH.md
@.planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md
@.planning/phases/22-setup-wizard-ide-skills/22-VALIDATION.md
@src/mcp/tools/recipe.test.ts
@src/utils/errors.test.ts
@src/mcp/tools/deployment.test.ts
</context>

## Artifacts this phase produces

- `src/utils/gh-preflight.test.ts` — RED `it.fails` scaffolds (dynamic import `./gh-preflight.js`) for `checkGhAuth`: missing gh, unauthenticated, passes GH_FORCE_TTY + timeout options, ok path.
- `src/mcp/tools/setup.test.ts` — RED `it.fails` scaffolds for `handleSetupAction` preflight → `COOLIFY_SETUP_PAUSED`, preflight ok, resume re-runs preflight after mock auth fix.
- `src/utils/errors.test.ts` — `describe('setup pause error codes')` with `it.fails` for `COOLIFY_SETUP_PAUSED` union + `RECOVERY_HINTS` resume wording.
- `src/skills/skills-manifest.test.ts` — RED `it.fails` for four `skills/coolify-*/SKILL.md` dirs with frontmatter `name:` matching directory.

<tasks>

<task type="tdd">
  <name>Task 1: gh-preflight.test.ts RED scaffolds</name>
  <files>src/utils/gh-preflight.test.ts</files>
  <read_first>
    - .planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md (gh-preflight.ts pattern, execFile mock)
    - .planning/phases/22-setup-wizard-ide-skills/22-RESEARCH.md (Pattern 2 headless gh, GH_TIMEOUT_MS 5000)
    - .planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md (D-04, D-05, D-06)
    - src/mcp/tools/deployment.test.ts (mock external boundary pattern)
  </read_first>
  <behavior>
    - Mock node:child_process execFile via vi.mock
    - Case gh_missing: execFile rejects on --version → checkGhAuth returns { ok:false, reason:'gh_missing' }
    - Case gh_unauthenticated: --version ok, auth status rejects → { ok:false, reason:'gh_unauthenticated' }
    - Case ok: both succeed → { ok:true }
    - Assert execFile called with argv ['gh','--version'] and ['gh','auth','status'], timeout 5000, env includes GH_FORCE_TTY:'0'
    - Never invoke gh auth login in tests or implied implementation
  </behavior>
  <action>
    Create src/utils/gh-preflight.test.ts with vi.mock('node:child_process'). Use it.fails for every case. Dynamic-import checkGhAuth from ./gh-preflight.js inside each case. Do not create gh-preflight.ts. Cover SETUP-01 missing/unauth/ok and D-04 timeout-safe subprocess contract per 22-PATTERNS.md.
  </action>
  <verify>
    <automated>pnpm test -- src/utils/gh-preflight.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File src/utils/gh-preflight.test.ts exists
    - rg -c "it\.fails" src/utils/gh-preflight.test.ts returns at least 3
    - rg -n "gh_missing|gh_unauthenticated" src/utils/gh-preflight.test.ts returns at least 2 matches
    - rg -n "GH_FORCE_TTY" src/utils/gh-preflight.test.ts returns at least 1 match
    - pnpm test -- src/utils/gh-preflight.test.ts exits 0 (it.fails expected-fail contract)
  </acceptance_criteria>
  <done>gh-preflight RED scaffolds committed; husky green via it.fails</done>
</task>

<task type="tdd">
  <name>Task 2: setup.test.ts + errors.test.ts setup-pause RED scaffolds</name>
  <files>src/mcp/tools/setup.test.ts, src/utils/errors.test.ts</files>
  <read_first>
    - src/mcp/tools/recipe.test.ts (vi.mock pattern, mkdtemp workspace root)
    - .planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md (setup.test.ts cases, COOLIFY_SETUP_PAUSED throw)
    - .planning/phases/22-setup-wizard-ide-skills/22-UI-SPEC.md (soft-pause envelope, _formattedText banners)
    - .planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md (D-02, D-05, D-06)
    - src/utils/errors.test.ts (COOLIFY_WATCH_TIMEOUT describe block lines 384-421)
  </read_first>
  <behavior>
    - setup.test.ts mocks ../../utils/gh-preflight.js checkGhAuth
    - preflight pause: checkGhAuth returns unauthenticated → handleSetupAction result isError with code COOLIFY_SETUP_PAUSED and recoveryHints mention resume
    - preflight ok: checkGhAuth ok → result ok with setup_status or equivalent progress field
    - resume: first call pause, second call after mock auth ok → preflight passes (no in-tool sleep/poll between calls)
    - errors.test.ts it.fails: CoolifyErrorCode accepts COOLIFY_SETUP_PAUSED; RECOVERY_HINTS.COOLIFY_SETUP_PAUSED mentions setup resume
  </behavior>
  <action>
    Create src/mcp/tools/setup.test.ts with vi.mock for gh-preflight, dynamic import handleSetupAction from ./setup.js, it.fails for preflight pause/ok/resume per SETUP-03. Extend src/utils/errors.test.ts with describe('setup pause error codes') using it.fails targeting COOLIFY_SETUP_PAUSED union and RECOVERY_HINTS — do not add code to errors.ts yet (Plan 22-01). Do not create setup.ts.
  </action>
  <verify>
    <automated>pnpm test -- src/mcp/tools/setup.test.ts -x</automated>
    <automated>pnpm test -- src/utils/errors.test.ts -t "setup pause" -x</automated>
  </verify>
  <acceptance_criteria>
    - File src/mcp/tools/setup.test.ts exists
    - rg -c "it\.fails" src/mcp/tools/setup.test.ts returns at least 3
    - rg -n "COOLIFY_SETUP_PAUSED" src/mcp/tools/setup.test.ts returns at least 1 match
    - rg -n "setup pause error codes" src/utils/errors.test.ts returns 1 match
    - pnpm test -- src/mcp/tools/setup.test.ts src/utils/errors.test.ts -x exits 0
  </acceptance_criteria>
  <done>setup + errors pause RED scaffolds committed</done>
</task>

<task type="tdd">
  <name>Task 3: skills-manifest.test.ts RED scaffolds</name>
  <files>src/skills/skills-manifest.test.ts</files>
  <read_first>
    - .planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md (skills-manifest.test.ts pattern)
    - .planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md (D-14, D-15)
    - .cursor/skills/spike-findings-awesome-coolify/SKILL.md (frontmatter shape)
  </read_first>
  <behavior>
    - SKILLS array: coolify-setup, coolify-deploy, coolify-diagnose, coolify-incident
    - Each it.fails: read skills/{dir}/SKILL.md, assert frontmatter name matches dir suffix, file exists
    - Plan 22-03 will add SKILL-02 content asserts; Wave 0 only checks existence + name
  </behavior>
  <action>
    Create src/skills/skills-manifest.test.ts listing four skill directories per D-15. Use it.fails expecting files that do not exist yet. Do not create SKILL.md files in Wave 0.
  </action>
  <verify>
    <automated>pnpm test -- src/skills/skills-manifest.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File src/skills/skills-manifest.test.ts exists
    - rg -n "coolify-setup|coolify-deploy|coolify-diagnose|coolify-incident" src/skills/skills-manifest.test.ts returns at least 4 matches
    - rg -c "it\.fails" src/skills/skills-manifest.test.ts returns at least 1
    - pnpm test -- src/skills/skills-manifest.test.ts exits 0
  </acceptance_criteria>
  <done>skills manifest RED scaffold committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks → child_process | Tests must not spawn real gh in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-00-01 | Spoofing | gh mock in tests | low | accept | Unit tests only; no production boundary |
| T-22-SC | Tampering | npm installs | low | accept | No new packages in Wave 0 |
</threat_model>

<verification>
- pnpm test -- src/utils/gh-preflight.test.ts src/mcp/tools/setup.test.ts src/skills/skills-manifest.test.ts -x
- pnpm test -- src/utils/errors.test.ts -t "setup pause" -x
</verification>

<success_criteria>
- All Wave 0 scaffolds use it.fails and exit 0
- No production setup/gh-preflight files created
- RED tests document SETUP-01/03 + SKILL-01 layout intent
</success_criteria>

<output>
Create `.planning/phases/22-setup-wizard-ide-skills/22-00-SUMMARY.md` when done.
</output>
