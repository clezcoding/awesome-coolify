---
phase: 22-setup-wizard-ide-skills
plan: 01
type: execute
wave: 1
depends_on:
  - 22-00
files_modified:
  - src/utils/gh-preflight.ts
  - src/utils/gh-preflight.test.ts
  - src/utils/errors.ts
  - src/utils/errors.test.ts
  - src/mcp/tools/setup.ts
  - src/mcp/tools/setup.test.ts
  - src/mcp/server.ts
  - src/mcp/server.test.ts
  - skills/coolify-setup/SKILL.md
  - src/skills/skills-manifest.test.ts
autonomous: true
requirements:
  - SETUP-01
  - SETUP-03
  - SKILL-01
user_setup: []

must_haves:
  truths:
    - "setup tool registered in server.ts as 18th registerTool; tools/list includes 'setup' (D-01)"
    - "setup({ action: 'preflight' }) calls checkGhAuth; missing/unauth gh returns COOLIFY_SETUP_PAUSED immediately with recoveryHints + _formattedText pause banner per 22-UI-SPEC (SETUP-01, D-04/D-05)"
    - "setup({ action: 'resume' }) re-runs preflight without in-tool poll/sleep for human gh auth (SETUP-03, D-06)"
    - "checkGhAuth uses execFile fixed argv, GH_TIMEOUT_MS 5000, GH_FORCE_TTY:0 — never gh auth login (D-04)"
    - "skills/coolify-setup/SKILL.md exists with frontmatter name coolify-setup documenting preflight/wire/resume, COOLIFY_SETUP_PAUSED soft-pause, modes greenfield|link-existing, optional flags default off (D-14/D-15 partial SKILL-01)"
    - "COOLIFY_SETUP_PAUSED in CoolifyErrorCode union + RECOVERY_HINTS with resume re-call hint"
    - "Partial wizard progress fields present on ok preflight: setup_status, current_step gh_preflight (22-UI-SPEC Agent-Visible UX)"
    - "{ statement: 'preflight path completes under 6s with mocked gh (no indefinite TTY block)', verification: backstop }"
  artifacts:
    - "src/utils/gh-preflight.ts"
    - "src/mcp/tools/setup.ts"
    - "src/mcp/server.ts (setup registration)"
    - "skills/coolify-setup/SKILL.md"
    - "src/utils/errors.ts (COOLIFY_SETUP_PAUSED)"
  key_links:
    - "server.registerTool('setup') → handleSetupAction → checkGhAuth"
    - "checkGhAuth failure → CoolifyApiError COOLIFY_SETUP_PAUSED → wrapMcpError"
    - "coolify-setup SKILL.md → setupActionsCatalog action names verbatim"
  prohibitions:
    - statement: "Never invoke gh auth login or poll-wait for human inside setup handler (D-05/D-06)"
      status: enforced
      verification: "rg -n 'auth login|setInterval|while.*auth' src/mcp/tools/setup.ts src/utils/gh-preflight.ts returns 0"
    - statement: "No auto-push git in preflight/resume path (D-12)"
      status: enforced
      verification: "rg -n 'git push|gh repo create.*--push' src/mcp/tools/setup.ts returns 0 in Plan 22-01 scope"
    - statement: "No per-IDE skill copies as source of truth (D-13)"
      status: enforced
      verification: "canonical skill only under skills/coolify-setup/SKILL.md; no new .cursor/skills/coolify-setup"
---

<objective>
Tracer slice: end-to-end `setup` preflight → gh check → soft-pause or ok, plus `resume` re-preflight, server registration, error code, and first skill pack (`coolify-setup`).

Purpose: Prove MCP setup tool architecture (flat schema, pause envelope, headless gh) before wire/greenfield expansion in Plan 22-02. Delivers SETUP-01 + SETUP-03 core and partial SKILL-01 per D-01/D-04/D-05.

Output: `gh-preflight.ts`, `setup.ts` (preflight + resume only), `server.ts` registration, `COOLIFY_SETUP_PAUSED`, `skills/coolify-setup/SKILL.md`, GREEN unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md
@.planning/phases/22-setup-wizard-ide-skills/22-RESEARCH.md
@.planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md
@.planning/phases/22-setup-wizard-ide-skills/22-UI-SPEC.md
@.planning/phases/22-setup-wizard-ide-skills/22-00-PLAN.md
@src/mcp/tools/recipe.ts
@src/mcp/server.ts
@src/utils/errors.ts
@src/mcp/prompts.ts
</context>

## Artifacts this phase produces

- `src/utils/gh-preflight.ts` — `export type GhPreflightResult`, `export async function checkGhAuth()`, optional `createGhRepoNoPush(repoName, cwd)` stub export reserved for Plan 22-02 (may omit until wire plan).
- `src/mcp/tools/setup.ts` — `setupActionsCatalog`, `setupActionSchema`, `handleSetupAction` with `preflight` + `resume` actions only; `handlePreflight`, `handleResume`; exports `isSetupErrorResult`.
- `src/utils/errors.ts` — `COOLIFY_SETUP_PAUSED` code + `RECOVERY_HINTS.COOLIFY_SETUP_PAUSED`.
- `src/mcp/server.ts` — `registerTool('setup', …)` 18th tool.
- `skills/coolify-setup/SKILL.md` — Agent Skills frontmatter + playbook for preflight/pause/resume/modes/optional flags (wire steps documented for agent; implemented Plan 22-02).

<tasks>

<task type="tracer">
  <name>End-to-end setup preflight → gh check → pause or ok (+ resume)</name>
  <files>src/utils/gh-preflight.ts, src/utils/errors.ts, src/mcp/tools/setup.ts, src/mcp/server.ts, src/mcp/server.test.ts, src/mcp/tools/setup.test.ts, src/utils/gh-preflight.test.ts, src/utils/errors.test.ts</files>
  <read_first>
    - src/mcp/tools/recipe.ts (createFlatActionSchema, handleRecipeAction switch, wrapMcpError pattern)
    - src/mcp/server.ts (recipe registration block ~lines 716-748)
    - src/utils/errors.ts (COOLIFY_WATCH_TIMEOUT pattern for new code + RECOVERY_HINTS)
    - .planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md (setup.ts + gh-preflight + server registration)
    - .planning/phases/22-setup-wizard-ide-skills/22-UI-SPEC.md (soft-pause envelope, _formattedText pause banners)
    - .planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md (D-01, D-02, D-04, D-05, D-06)
  </read_first>
  <action>
    Implement headless gh preflight per D-04 in src/utils/gh-preflight.ts (execFile, timeout 5000, GH_FORCE_TTY:0). Add COOLIFY_SETUP_PAUSED to CoolifyErrorCode and RECOVERY_HINTS per 22-PATTERNS.md (D-05). Create src/mcp/tools/setup.ts with flat schema actions preflight|wire|resume (wire throws not-implemented or validation-only stub returning clear message — full wire is Plan 22-02); implement preflight calling checkGhAuth — on failure throw CoolifyApiError with pause_reason gh_missing|gh_unauthenticated, resume_action resume, recoveryHints, and _formattedText matching 22-UI-SPEC pause templates; on success return buildReadResponse with setup_status in_progress, current_step gh_preflight, steps_completed including gh_preflight. Implement resume per D-06: re-run preflight only (accept same optional params as wire will use); no sleep/poll loop. Register setup in server.ts mirroring recipe tool ok/error mapping. Flip Wave 0 it.fails to bare it in gh-preflight.test.ts, setup.test.ts, errors setup-pause describe. Update server.test.ts: registerTool count 17→18, add setup to expectedTools, assert registerTool('setup') present.
  </action>
  <verify>
    <automated>pnpm test -- src/utils/gh-preflight.test.ts src/mcp/tools/setup.test.ts -x</automated>
    <automated>pnpm test -- src/utils/errors.test.ts -t "setup pause" -x</automated>
    <automated>pnpm test -- src/mcp/server.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "export async function checkGhAuth" src/utils/gh-preflight.ts returns 1
    - rg -n "COOLIFY_SETUP_PAUSED" src/utils/errors.ts returns at least 2 matches
    - rg -n "export async function handleSetupAction" src/mcp/tools/setup.ts returns 1
    - rg -n "registerTool\(\n    'setup'" src/mcp/server.ts returns 1
    - rg -v '^#' src/mcp/server.ts | rg -c "registerTool\(" returns 18
    - rg -n "expect\(matches\.length\)\.toBe\(18\)" src/mcp/server.test.ts returns 1
    - rg -c "it\.fails" src/utils/gh-preflight.test.ts src/mcp/tools/setup.test.ts returns 0
    - pnpm test -- src/utils/gh-preflight.test.ts src/mcp/tools/setup.test.ts src/mcp/server.test.ts -x green
    - npm run build green
  </acceptance_criteria>
  <done>Agent can call setup preflight; gh missing/unauth returns COOLIFY_SETUP_PAUSED immediately; resume re-runs preflight; setup tool listed in MCP server</done>
</task>

<task type="auto">
  <name>Ship coolify-setup SKILL.md (first skill pack)</name>
  <files>skills/coolify-setup/SKILL.md, src/skills/skills-manifest.test.ts</files>
  <read_first>
    - src/mcp/prompts.ts (new-project prompt lines 106-151 — workflow analog)
    - src/mcp/tools/setup.ts (setupActionsCatalog verbatim action names)
    - .planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md (D-14, D-15, D-16 partial, D-17 install command reference)
    - .planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md (coolify-setup SKILL body requirements)
    - https://agentskills.io (SKILL.md frontmatter: name, description)
  </read_first>
  <action>
    Create skills/coolify-setup/SKILL.md per Agent Skills spec per D-14. Frontmatter name coolify-setup. Body documents: canonical install npx skills add clezcoding/awesome-coolify -a cursor -a claude-code -a codex (D-17); workflow 1) setup preflight 2) on COOLIFY_SETUP_PAUSED stop for human gh auth login 3) setup resume with same params 4) setup wire with mode greenfield|link-existing (reference only — implemented Plan 22-02). Document optional flags include_domains, set_env, deploy_and_watch default off (D-10). State never auto-push git (D-12). Use action names exactly from setupActionsCatalog. Mention deployment.watch bounded timeout at high level for deploy_and_watch flag (D-16 partial — full watch docs in coolify-deploy Plan 22-03). In same commit flip coolify-setup it.fails to bare it in src/skills/skills-manifest.test.ts (leave coolify-deploy, coolify-diagnose, coolify-incident as it.fails until Plan 22-03 Task 2 — file creation and test flip must land together).
  </action>
  <verify>
    <automated>test -f skills/coolify-setup/SKILL.md && rg -n "^name: coolify-setup" skills/coolify-setup/SKILL.md && rg -n "preflight|COOLIFY_SETUP_PAUSED|resume" skills/coolify-setup/SKILL.md</automated>
    <automated>pnpm test -- src/skills/skills-manifest.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File skills/coolify-setup/SKILL.md exists
    - rg -n "^name: coolify-setup" skills/coolify-setup/SKILL.md returns 1
    - rg -n "npx skills add clezcoding/awesome-coolify" skills/coolify-setup/SKILL.md returns at least 1 match
    - rg -n "COOLIFY_SETUP_PAUSED|action: \"resume\"" skills/coolify-setup/SKILL.md returns at least 2 matches
    - rg -n "greenfield|link-existing" skills/coolify-setup/SKILL.md returns at least 2 matches
    - rg -n "coolify-setup" src/skills/skills-manifest.test.ts | rg -v "it\.fails" returns at least 1 match for setup case
    - pnpm test -- src/skills/skills-manifest.test.ts -x green
  </acceptance_criteria>
  <done>coolify-setup skill documents setup MCP workflow and pause/resume path for agents</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host → gh subprocess | Untrusted PATH; fixed argv only |
| setup response → agent | Must not leak tokens in _formattedText |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | gh subprocess argv | high | mitigate | execFile with fixed argv arrays; no shell; repo_name validated in Plan 22-02 |
| T-22-02 | Information Disclosure | gh auth output | medium | mitigate | Never --show-token; redactSecrets on _formattedText via existing formatters |
| T-22-03 | Denial of Service | gh subprocess hang | medium | mitigate | 5s timeout on all gh calls (D-04) |
| T-22-SC | Tampering | npm installs | low | accept | No new runtime packages |
</threat_model>

<verification>
- pnpm test -- src/utils/gh-preflight.test.ts src/mcp/tools/setup.test.ts src/mcp/server.test.ts -x
- npm run build
</verification>

<success_criteria>
- SETUP-01: gh presence/auth verified headless-safe with pause guidance
- SETUP-03: resume path re-runs preflight without poll-wait
- Partial SKILL-01: coolify-setup skill exists at canonical path
- D-01/D-04/D-05/D-06 honored
</success_criteria>

<output>
Create `.planning/phases/22-setup-wizard-ide-skills/22-01-SUMMARY.md` when done.
</output>
