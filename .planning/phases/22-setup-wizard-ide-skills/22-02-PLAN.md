---
phase: 22-setup-wizard-ide-skills
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/mcp/tools/setup.ts
  - src/mcp/tools/setup.test.ts
  - src/utils/gh-preflight.ts
  - src/utils/gh-preflight.test.ts
autonomous: false
requirements:
  - SETUP-02
  - SETUP-03
user_setup:
  - service: coolify
    why: "wire greenfield/link-existing calls Coolify API"
    env_vars:
      - name: COOLIFY_URL
        source: "Coolify instance URL"
      - name: COOLIFY_TOKEN
        source: "Coolify API token with project create scope"
  - service: github
    why: "greenfield optional gh repo create step"
    env_vars:
      - name: GH_TOKEN
        source: "Optional headless gh auth alternative to gh auth login"

must_haves:
  truths:
    - "setup wire action mode link-existing validates project/environment/server UUIDs and updates .coolify/manifest.json via ManifestManager without calling recipe create actions (SETUP-02, D-08)"
    - "setup wire action mode greenfield runs gh preflight → optional gh repo create without --push → project/environment linkage → recipe create-git-app|create-app-db|create-one-click → manifest upsert (SETUP-02, D-09)"
    - "Optional flags include_domains, set_env, deploy_and_watch default false; only run when explicitly true (D-10)"
    - "deploy_and_watch when true: triggers deploy, returns deployment_uuid, calls deployment.watch with timeout 300 max; on COOLIFY_WATCH_TIMEOUT returns UUID + recovery hints — never infinite block (D-11)"
    - "wire response includes setup_status, current_step, steps_completed, steps_remaining, manifest_path per 22-UI-SPEC"
    - "resume accepts same wire params as wire for stateless MCP re-entry after pause (D-06, RESEARCH A3)"
    - "Greenfield success suggests manual git push in _formattedText only — never executes git push or gh --push (D-12)"
    - "link-existing mode may skip gh preflight when skip_gh true or no repo step needed (RESEARCH A4)"
    - "{ statement: 'gh repo create argv contains no --push flag', verification: backstop }"
  artifacts:
    - "src/mcp/tools/setup.ts (wire + greenfield + link-existing + optional flags)"
    - "src/utils/gh-preflight.ts (createGhRepoNoPush export)"
  key_links:
    - "handleWire greenfield → handleRecipeAction internal → ManifestManager.upsert"
    - "handleWire link-existing → project.get/server.get validate → ManifestManager"
    - "deploy_and_watch → handleDeploymentAction watch timeout 300"
  prohibitions:
    - statement: "Never auto-push unfinished git — no git push, no gh repo create --push (D-12 one-way)"
      status: enforced
      verification: "rg -n 'git push|\"--push\"|--push' src/mcp/tools/setup.ts src/utils/gh-preflight.ts returns 0 matches in argv/push paths"
    - statement: "link-existing must not call recipe create actions unless explicit resource params provided (D-08)"
      status: enforced
      verification: "setup.test.ts link-existing case: handleRecipeAction mock call count 0"
    - statement: "No in-tool poll waiting for gh auth (D-06)"
      status: enforced
      verification: "rg -n 'setInterval|sleep.*auth|poll.*auth' src/mcp/tools/setup.ts returns 0"
---

<objective>
Expand setup tool with wire action: link-existing manifest linkage and greenfield orchestration (recipes + manifest), optional flags, deploy_and_watch bounded path, and gh repo create without push.

Purpose: Deliver SETUP-02 full wizard wiring and complete SETUP-03 resume-with-params contract per D-07–D-12.

Output: Full `handleWire` + `handleResume` orchestration; extended tests; gh repo create helper.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md
@.planning/phases/22-setup-wizard-ide-skills/22-RESEARCH.md
@.planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md
@.planning/phases/22-setup-wizard-ide-skills/22-UI-SPEC.md
@.planning/phases/22-setup-wizard-ide-skills/22-01-PLAN.md
@src/mcp/tools/recipe.ts
@src/mcp/tools/manifest.ts
@src/mcp/tools/deployment.ts
@src/utils/manifest.ts
@src/mcp/tools/project.ts
</context>

## Artifacts this phase produces

- `src/mcp/tools/setup.ts` — `handleWire`, expanded `handleResume` (preflight then wire), `setupActionSchema` wire fields: mode, project_uuid, environment_uuid, server_uuid, recipe_type, repo_name, include_domains, set_env, deploy_and_watch, skip_gh, instance routing.
- `src/utils/gh-preflight.ts` — `export async function createGhRepoNoPush(repoName, cwd)` with fixed argv, no --push, GH_FORCE_TTY:0.
- `src/mcp/tools/setup.test.ts` — GREEN cases: wire link-existing manifest upsert, greenfield recipe+manifest, resume after pause with params, deploy_and_watch timeout dual-signal.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Public setup wire action contract for greenfield vs link-existing modes (D-07 costly)</decision>
  <context>Both modes share wire action with required mode enum. Fields below shape skills/docs and cannot change cheaply.</context>
  <options>
    <option id="option-a">
      <name>Unified wire action</name>
      <pros>Single action; resume re-supplies same params; matches RESEARCH architecture</pros>
      <cons>Larger Zod schema surface</cons>
    </option>
    <option id="option-b">
      <name>Separate wire-greenfield and wire-link actions</name>
      <pros>Smaller per-action schemas</pros>
      <cons>Breaks D-02 resume simplicity; more skill doc drift</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (recommended per D-02/D-07 research) or option-b</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Implement wire link-existing + greenfield core + manifest write</name>
  <files>src/mcp/tools/setup.ts, src/mcp/tools/setup.test.ts, src/utils/gh-preflight.ts, src/utils/gh-preflight.test.ts</files>
  <read_first>
    - src/mcp/tools/recipe.ts (handleRecipeAction export, create-git-app params)
    - src/utils/manifest.ts (ManifestManager.upsert)
    - src/mcp/tools/manifest.ts (manifest set patterns)
    - .planning/phases/22-setup-wizard-ide-skills/22-PATTERNS.md (internal handler reuse, link-existing branch)
    - .planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md (D-08, D-09, D-10)
    - src/mcp/tools/setup.ts (preflight/resume from Plan 22-01)
  </read_first>
  <behavior>
    - link-existing: validate UUIDs via existing read handlers or API client; ManifestManager.upsert/save; no handleRecipeAction unless application_uuid explicitly provided
    - greenfield: after gh preflight ok, optional createGhRepoNoPush when repo_name set; create/link project+environment+server via existing tool internals; call handleRecipeAction with recipe_type; manifest upsert on API success
    - include_domains, set_env, deploy_and_watch omitted or false → steps skipped
    - Response steps_completed/steps_remaining track gh_preflight, repo, linkage, recipe, manifest per UI-SPEC step keys
  </behavior>
  <action>
    Extend setupActionSchema with wire required fields per option-a (mode required on wire). Implement handleWire branching D-08/D-09 using internal imports handleRecipeAction, ManifestManager, resolveRoutingEnv — do not spawn MCP stdio. Add createGhRepoNoPush in gh-preflight.ts: gh repo create with --private --source . --remote origin, never --push; validate repo_name with strict regex. handleResume: run preflight (unless skip_gh on link-existing) then delegate to handleWire with same parsed params per D-06. Add setup.test.ts cases with mocked recipe/manifest/project/gh; flip any new it.fails to it. Per D-12 complete banner suggests git push manually in _formattedText only.
  </action>
  <verify>
    <automated>pnpm test -- src/mcp/tools/setup.test.ts src/utils/gh-preflight.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "case 'wire'|handleWire" src/mcp/tools/setup.ts returns at least 2 matches
    - rg -n "link-existing|greenfield" src/mcp/tools/setup.ts returns at least 2 matches
    - rg -n "ManifestManager" src/mcp/tools/setup.ts returns at least 1 match
    - rg -n "handleRecipeAction" src/mcp/tools/setup.ts returns at least 1 match
    - rg -n "\"--push\"|--push" src/utils/gh-preflight.ts returns 0
    - pnpm test -- src/mcp/tools/setup.test.ts -x green
    - npm run build green
  </acceptance_criteria>
  <done>SETUP-02: wire updates manifest for both modes; greenfield provisions via recipes; link-existing does not force creates</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Greenfield repo create must never auto-push commits (D-12 one-way)</decision>
  <context>RESEARCH A1 flags gh repo create --source . push behavior as [ASSUMED]. Implementation must omit --push and only suggest manual git push in response text.</context>
  <options>
    <option id="push-never">
      <name>Never pass --push; document manual git push in response</name>
      <pros>Matches REQUIREMENTS out-of-scope; D-12 locked</pros>
      <cons>User must push manually before Coolify git deploy</cons>
    </option>
  </options>
  <resume-signal>Confirm: push-never (required by D-12) or halt for requirements change</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Optional flags + deploy_and_watch bounded path</name>
  <files>src/mcp/tools/setup.ts, src/mcp/tools/setup.test.ts</files>
  <read_first>
    - src/mcp/tools/deployment.ts (handleDeploymentAction watch, timeout default 300, COOLIFY_WATCH_TIMEOUT)
    - .planning/phases/22-setup-wizard-ide-skills/22-UI-SPEC.md (deploy watch timeout banner, optional flags warning)
    - .planning/phases/22-setup-wizard-ide-skills/22-CONTEXT.md (D-10, D-11)
    - .planning/phases/21-deploy-watch/21-CONTEXT.md (watch semantics)
  </read_first>
  <behavior>
    - include_domains true: wire domain step after recipe (use existing application/domain helpers if available; else manifest domains field only with ponytail comment if API path unclear)
    - set_env true: wire env vars step via existing env tools
    - deploy_and_watch true: application deploy wait false then deployment.watch timeout 300; include deployment_uuid in response; on watch timeout return COOLIFY_WATCH_TIMEOUT recovery in data + _formattedText per UI-SPEC
    - Defaults: all three flags false when omitted
  </behavior>
  <action>
    Implement optional flag branches at end of greenfield wire only per D-10. For deploy_and_watch call handleDeploymentAction watch with timeout 300 per D-11; never block without timeout cap. Add setup.test.ts mock case: watch returns COOLIFY_WATCH_TIMEOUT, assert deployment_uuid present and recoveryHints mention deployment.watch re-call. Ensure wire complete response setup_status complete and manifest_path .coolify/manifest.json.
  </action>
  <verify>
    <automated>pnpm test -- src/mcp/tools/setup.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "deploy_and_watch|include_domains|set_env" src/mcp/tools/setup.ts returns at least 3 matches
    - rg -n "handleDeploymentAction" src/mcp/tools/setup.ts returns at least 1 match
    - rg -n "timeout: 300|timeout:300" src/mcp/tools/setup.ts returns at least 1 match
    - pnpm test -- src/mcp/tools/setup.test.ts -x green
  </acceptance_criteria>
  <done>SETUP-02 optional depth + SETUP-03 resume-with-wire-params complete; D-10/D-11 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup wire → Coolify API | Creates resources with user token scope |
| gh repo create → GitHub | Repo name must be validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04 | Tampering | repo_name injection into gh argv | high | mitigate | Strict regex on repo_name before execFile |
| T-22-05 | Elevation | Unauthorized Coolify creates | medium | mitigate | Existing COOLIFY_TOKEN scope; no confirm bypass on destructive ops |
| T-22-06 | Denial of Service | deploy_and_watch long poll | medium | mitigate | Bounded timeout 300; COOLIFY_WATCH_TIMEOUT recovery (D-11) |
| T-22-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
- pnpm test -- src/mcp/tools/setup.test.ts src/utils/gh-preflight.test.ts -x
- pnpm test (full suite)
- npm run build
</verification>

<success_criteria>
- SETUP-02: manifest updated after linkage; greenfield + link-existing both work in tests
- SETUP-03: resume continues wire with re-supplied params
- D-07–D-12 honored including no auto-push
</success_criteria>

<output>
Create `.planning/phases/22-setup-wizard-ide-skills/22-02-SUMMARY.md` when done.
</output>
