{
  "name": "coolify-mcp",
  "version": "0.1.0",
  "description": "MCP server for Coolify — deploy, logs, diagnose via action-based tools",
  "type": "module",
  "bin": {
    "coolify-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "prepare": "npm run build",
    "higgsfield:fix": "bash scripts/fix-higgsfield.sh"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0-beta.3",
    "ofetch": "^1.4.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vitest/coverage-v8": "^1.4.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.4.0"
  }
}
