# Coolify MCP — local CLI credentials (gitignored as `.env`)
# Same keys the IDE injects via mcp.json `env`. Process env always wins.

COOLIFY_URL=https://coolify.example.com
COOLIFY_TOKEN=your-api-token-here
COOLIFY_VERIFY_SSL=true
COOLIFY_MCP_LOG=info
